{
  "name": "@gogol/chat-adapter-null",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "No-op ChatWidgetAdapter — the safe default for CI/dev and unentitled sites. Loads nothing third-party (RFC-0175).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "echo 'No build step required — TypeScript sources consumed directly'",
    "build:check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@gogol/chat": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  },
  "gogol": {
    "testSignal": {
      "signal": "skipped",
      "owner": "architecture",
      "rationale": "Null chat adapter is a no-op adapter covered by chat integration consumers; direct tests are deferred until adapter conformance fixtures exist.",
      "reviewAfter": "2026-10-01"
    }
  }
}
