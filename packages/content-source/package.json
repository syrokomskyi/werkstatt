{
  "name": "@gogol/content-source",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RFC-0141 Content Source Provider port: a single named seam for where content and assets come from. Ships the abstract ContentSourceProvider / AssetRef / ResolvedAsset contracts plus the reference filesystem adapter that reproduces today's glob-based behavior.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./astro": {
      "types": "./src/astro.ts",
      "default": "./src/astro.ts"
    },
    "./cms-git": {
      "types": "./src/adapters/cms-git/index.ts",
      "default": "./src/adapters/cms-git/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "echo 'No build step required — TypeScript sources consumed directly'",
    "build:check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "vitest": "^4.1.10"
  },
  "gogol": {
    "testSignal": {
      "signal": "direct",
      "owner": "architecture",
      "rationale": "Vitest unit tests cover content source types and provider contracts."
    }
  }
}
