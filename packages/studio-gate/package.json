{
  "name": "@gogol/studio-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RFC-0555: stdio MCP server for site owner content editing with mission lifecycle. Projects workpiece.read/write and mission lifecycle commands as MCP tools.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json --noEmit",
    "build:check": "pnpm exec tsc -p tsconfig.json --noEmit",
    "start": "node --import tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@gogol/site-kernel": "workspace:*",
    "@gogol/site-kernel-handoff": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
