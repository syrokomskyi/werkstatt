{
  "name": "@warpgogol/star-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic SSG-rendered SVG diagram of an app's cosmic universe: constellation → stars (pages) → planets (sections) → moons (components at depth=4). (DNA-32, RFC-0028)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./render": {
      "types": "./src/render.ts",
      "default": "./src/render.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "echo 'No build step required — TypeScript sources consumed directly'",
    "build:check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@warpgogol/ontology": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "vitest": "^4.1.10"
  },
  "gogol": {
    "testSignal": {
      "signal": "direct",
      "owner": "architecture",
      "rationale": "Vitest unit tests cover deterministic layout computation and graph structure."
    }
  }
}
