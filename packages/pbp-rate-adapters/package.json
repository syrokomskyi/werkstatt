{
  "name": "@warpgogol/pbp-rate-adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Rate source adapters for external exchange rate APIs (RFC-0744).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "echo 'No build step required — TypeScript sources consumed directly'",
    "build:check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@warpgogol/pbp": "workspace:*"
  },
  "gogol": {
    "testSignal": {
      "signal": "direct",
      "owner": "architecture",
      "rationale": "Vitest unit tests cover ECB XML parsing, cross-rate computation, and registry lookup."
    }
  }
}
