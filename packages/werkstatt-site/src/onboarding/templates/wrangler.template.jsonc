// GENERATED. Do not change this line unless the file contains project specific changes.
{
  // Cloudflare Workers deployment config for {{CLIENT_ID}}
  // RFC-0149 / DNA-1 (SSG static assets + on-demand routes on a single Worker)
  "name": "{{CLIENT_ID}}",
  "compatibility_date": "2026-06-11",
  "compatibility_flags": ["nodejs_compat", "nodejs_compat_populate_process_env"],
  // [RFC-0149] Workers static-assets form. Static files are served from the ASSETS binding.
  // [RFC-0785] main points to src/worker.ts — a custom Worker entry point generated by
  // agent.markdown-negotiation.generate that wraps the Astro Cloudflare handler to intercept
  // Accept: text/markdown requests before matchStaticAsset short-circuits prerendered pages.
  // Do NOT add pages_build_output_dir — that is the legacy Pages form (superseded).
  "main": "src/worker.ts",
  "assets": {
    "directory": "./dist",
    "binding": "ASSETS",
    // [RFC-0785] run_worker_first ensures the Worker intercepts every request
    // (including prerendered static pages) so Accept: text/markdown content
    // negotiation runs before static assets are served directly.
    "run_worker_first": true
  },
  "placement": {
    "mode": "smart"
  },
  "upload_source_maps": true,
  // [RFC-0181] Cloudflare Email Routing send binding — the delivery callback emails
  // lead notifications. Requires Email Routing enabled on the zone + a verified sender
  // (INTEGRATION_EMAIL_FROM) and destination (INTEGRATION_EMAIL_TO). No Cloudflare KV/
  // Queues bindings (forbidden — not EU-pinnable; delivery is Upstash QStash/Redis EU).
  "send_email": [{ "name": "SEND_EMAIL" }],
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1.0,
    "traces": {
      "enabled": true,
      "destinations": ["signoz"],
      "head_sampling_rate": 1.0
    },
    "logs": {
      "enabled": true,
      "head_sampling_rate": 1.0,
      "persist": true,
      "invocation_logs": true
    }
  }
}