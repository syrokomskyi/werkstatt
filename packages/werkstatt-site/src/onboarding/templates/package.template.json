{
  "_": "GENERATED. Do not change this line unless the file contains project specific changes.",
  "name": "{{CLIENT_ID}}",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "pnpm run stop; astro dev",
    "stop": "astro dev stop",
    "build:check": "werkstatt pipeline build.prepare --site {{CLIENT_ID}} && werkstatt pipeline build.check --site {{CLIENT_ID}} && pnpm -s astro:check && astro build && werkstatt pipeline build.post --site {{CLIENT_ID}}",
    "build": "werkstatt pipeline build.prepare --site {{CLIENT_ID}} && pnpm -s astro:check && astro build && werkstatt pipeline build.post --site {{CLIENT_ID}}",
    "build:alt": "node -e \"process.env.PUBLIC_SITE_URL='https://alt.{{DOMAIN}}';require('child_process').spawnSync('pnpm',['run','build'],{stdio:'inherit',env:process.env})\"",
    "build:main": "node -e \"process.env.PUBLIC_SITE_URL='https://{{DOMAIN}}';require('child_process').spawnSync('pnpm',['run','build'],{stdio:'inherit',env:process.env})\"",
    "build:deploy:alt": "pnpm run build:alt && pnpm run deploy:alt",
    "deploy:alt": "werkstatt run deploy.preflight --site {{CLIENT_ID}} && wrangler deploy --name alt-{{CLIENT_ID}} --secrets-file .env",
    "build:deploy:main": "pnpm run build:main && pnpm run deploy:main",
    "deploy:main": "werkstatt run deploy.preflight --site {{CLIENT_ID}} && wrangler deploy --name {{CLIENT_ID}} --secrets-file .env",
    "preview": "wrangler dev",
    "astro": "astro",
    "astro:check": "astro check",
    "site-os:apps": "werkstatt apps list",
    "changelog:generate": "werkstatt run changelog.generate --site {{CLIENT_ID}}",
    "changelog:generate:force": "werkstatt run changelog.generate --site {{CLIENT_ID}} -- --force",
    "changelog:dry-run": "werkstatt run changelog.generate --site {{CLIENT_ID}} -- --dry-run --force",
    "changelog:rebuild-index": "werkstatt run changelog.rebuild-index --site {{CLIENT_ID}}",
    "changelog:backfill": "werkstatt run changelog.backfill --site {{CLIENT_ID}}",
    "icons:gen": "werkstatt run icons.generate --site {{CLIENT_ID}}",
    "icons:clean": "werkstatt run icons.clean --site {{CLIENT_ID}}",
    "i18n:middleware:gen": "werkstatt run i18n.middleware.generate --site {{CLIENT_ID}}",
    "open-source:gen": "werkstatt run open-source.generate --site {{CLIENT_ID}}",
    "check": "werkstatt pipeline check --site {{CLIENT_ID}}",
    "tokens:ds:lint": "werkstatt run tokens.ds.lint --site {{CLIENT_ID}}",
    "tokens:colors:lint": "werkstatt run tokens.colors.lint --site {{CLIENT_ID}}",
    "content:validate": "werkstatt run content.validate --site {{CLIENT_ID}}",
    "compass:inventory": "werkstatt run compass.inventory --site {{CLIENT_ID}}",
    "compass:validate": "werkstatt run compass.validate --site {{CLIENT_ID}}",
    "thin-copy:validate": "werkstatt run thin-copy.validate --site {{CLIENT_ID}}",
    "integrity:init": "werkstatt run integrity.init --site {{CLIENT_ID}}",
    "integrity:update": "werkstatt run integrity.update --site {{CLIENT_ID}}",
    "integrity:verify": "werkstatt run integrity.verify --site {{CLIENT_ID}}",
    "integrity:build-record": "werkstatt run integrity.build-record --site {{CLIENT_ID}}",
    "integrity:sign": "werkstatt run integrity.sign --site {{CLIENT_ID}}",
    "integrity:verify-release": "werkstatt run integrity.verify-release --site {{CLIENT_ID}}",
    "integrity:keys:generate": "werkstatt run integrity.keys.generate --site {{CLIENT_ID}}",
    "integrity:release": "werkstatt pipeline integrity.release --site {{CLIENT_ID}}",
    "integrity:backfill-revisions": "werkstatt run integrity.backfill-revisions --site {{CLIENT_ID}}",
    "upgrade-packages": "pnpm up --latest"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/cloudflare": "^14.1.0",
    "@astrojs/react": "^6.0.0",
    "@fontsource/dm-mono": "^5.1.0",
    "@fontsource/inter": "^5.1.0",
    "@fontsource/playfair-display": "^5.1.0",
    "@warpgogol/forge": "workspace:*",
    "@warpgogol/werkstatt": "workspace:*",
    "@warpgogol/werkstatt-site": "workspace:*",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@upstash/qstash": "^2.11.1",
    "@upstash/redis": "^1.38.0",
    "astro": "7.2.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@quantco/pnpm-licenses": "2.4.2",
    "@types/node": "^26.1.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "autoprefixer": "^10.5.2",
    "postcss": "^8.5.16",
    "sharp": "^0.35.3",
    "terser": "^5.48.0",
    "tsx": "^4.22.5",
    "typescript": "^6.0.3",
    "uuid": "^14.0.1",
    "wrangler": "^4.120.0"
  },
  "gogol": {
    "testSignal": {
      "signal": "skipped",
      "owner": "architecture",
      "rationale": "App author/build checks currently cover the deployable site; direct app unit tests are deferred until an app-level smoke-test harness lands.",
      "reviewAfter": "2026-10-01"
    }
  }
}
