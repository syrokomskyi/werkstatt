{
  "name": "@warpgogol/chat-adapter-uchat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "UChat implementation of ChatWidgetAdapter. Injects the UChat web widget script ONLY on user activation (click-to-load). The only place the UChat origin appears (RFC-0175).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "echo 'No build step required — TypeScript sources consumed directly'",
    "build:check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@warpgogol/chat": "workspace:*"
  },
  "devDependencies": {
    "happy-dom": "^20.11.1",
    "vitest": "^4.1.10"
  },
  "gogol": {
    "testSignal": {
      "signal": "skipped",
      "owner": "architecture",
      "rationale": "UChat adapter is integration-bound and covered by static config validation; direct tests are deferred until browser loader fixtures exist.",
      "reviewAfter": "2026-10-01"
    }
  }
}
