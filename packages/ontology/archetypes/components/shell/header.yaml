id: shell.header
displayName: Shell Header
version: 1.0.0
semanticRole: component-shell.header
description: |
  Starter RFC-0072 archetype seeded from existing manifest metadata.
expectedIntents:
  - guide-navigation
  - orient-visitor
  - establish-identity
expectedIndustryFit:
  - non-profit
layoutHint: horizontal-row
propsSchema:
  $shape: zod
  shape: |
    z.object({}).passthrough()
acceptedCosmicNames:
  - Oberon
constraints: {}
