{
  "name": "@warpgogol/chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vendor-agnostic, consent-gated chat widget port for the Warpgogol platform: ChatWidgetAdapter contract, click-to-load loader, and config schema. Nothing third-party loads before explicit user activation (RFC-0175).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./port": {
      "types": "./src/port.ts",
      "default": "./src/port.ts"
    },
    "./client": {
      "types": "./src/client.ts",
      "default": "./src/client.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "build": "echo 'No build step required — TypeScript sources consumed directly'",
    "build:check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "vitest": "^4.1.10"
  },
  "gogol": {
    "testSignal": {
      "signal": "direct",
      "owner": "architecture",
      "rationale": "Vitest unit tests cover adapter id validation, config schema parsing, and metadata catalog lookups."
    }
  }
}
