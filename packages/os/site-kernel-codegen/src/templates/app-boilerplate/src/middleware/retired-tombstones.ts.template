{{GENERATED_HEADER}}
/*
<MODULE_CONTRACT>
<purpose>410 Gone tombstone middleware generated from system.md retiredRoutes (RFC-0589).</purpose>
<keywords>middleware, tombstone, 410, retired, generated, RFC-0589</keywords>
<responsibilities>
  <item>Match retired page routes and return 410 Gone with Cache-Control: max-age=3600.</item>
  <item>Pass through non-retired requests to the next middleware in the chain.</item>
</responsibilities>
<non-goals>
  <item>Do not handle 301 redirects — those are in _redirects (RFC-0487/RFC-0509).</item>
</non-goals>
</MODULE_CONTRACT>
<MODULE_MAP>
  <entry key="defaultExport">Retired tombstone middleware handler with prefix-based path matching.</entry>
</MODULE_MAP>
<COMPASS_BLOCK id="retired-tombstone-middleware">
</COMPASS_BLOCK>
<CHANGE_SUMMARY>
  <item>Generated by routes.generate (RFC-0589). Tombstone slugs from system.md retiredRoutes status=410.</item>
  <item>GENERATED — do not edit directly; edit system.md and rerun.</item>
</CHANGE_SUMMARY>
*/

// @ai-invariant: High-risk module. Runs on every request. No heavy imports.
import type { MiddlewareHandler } from "astro";

const tombstonePrefixes: ReadonlySet<string> = new Set({{TOMBSTONE_PREFIXES}});

export const onRequest: MiddlewareHandler = (context, next) => {
  const pathname = context.url.pathname;
  for (const prefix of tombstonePrefixes) {
    if (pathname === `/${prefix}` || pathname.startsWith(`/${prefix}/`)) {
      return new Response(null, {
        status: 410,
        headers: { "Cache-Control": "max-age=3600" },
      });
    }
  }
  return next();
};

export default onRequest;
