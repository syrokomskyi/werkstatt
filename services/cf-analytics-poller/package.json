{
  "name": "@warpgogol/cf-analytics-poller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:check": "pnpm exec tsc --noEmit",
    "run:once": "tsx src/run-once.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "@warpgogol/werkstatt-site": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "vitest": "^4.1.10"
  },
  "gogol": {
    "testSignal": {
      "signal": "skipped",
      "owner": "observability",
      "rationale": "Poller behavior is currently guarded by TypeScript and observability command validation; direct service tests are deferred until worker-runtime fixtures exist.",
      "reviewAfter": "2026-10-01"
    }
  }
}
