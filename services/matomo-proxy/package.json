{
  "name": "matomo-proxy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm run gen:upstreams && wrangler dev",
    "build:check": "pnpm run gen:upstreams && tsc --noEmit",
    "check": "werkstatt run matomo.proxy.validate",
    "gen:upstreams": "tsx scripts/gen-upstreams.ts",
    "deploy:dev": "werkstatt run leitstand.service.dev-deploy --service matomo-proxy",
    "deploy:prod": "werkstatt run leitstand.service.promote --service matomo-proxy",
    "rollback": "werkstatt run leitstand.service.rollback --service matomo-proxy"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "@warpgogol/werkstatt-site": "workspace:*"
  },
  "gogol": {
    "testSignal": {
      "signal": "skipped",
      "owner": "architecture",
      "rationale": "Proxy behavior is currently guarded by matomo.proxy.validate and TypeScript; request-fixture tests are scheduled with RFC-0305 fleet controls.",
      "reviewAfter": "2026-10-01"
    }
  }
}
