# fleet-probe-runner — environment variables (RFC-0388).
# Copy to .env and fill values. Never commit real secrets.
# Every key includes a "# How to obtain:" line with concrete instructions (RFC-0388).

# Probe cycle interval in milliseconds (default: 300000 = 5 min).
# How to obtain: Set to a number of milliseconds; leave empty for default (300000).
PROBE_INTERVAL_MS=

# Maximum concurrent probes per cycle (default: 3).
# How to obtain: Set to a number; leave empty for default (3).
PROBE_CONCURRENCY=

# Per-request timeout in milliseconds (default: 10000).
# How to obtain: Set to a number of milliseconds; leave empty for default (10000).
PROBE_REQUEST_TIMEOUT_MS=
