{
  "name": "@warpgogol/telegram-alert-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:check": "pnpm exec tsc --noEmit",
    "deploy:dev": "werkstatt run leitstand.service.dev-deploy --service telegram-alert-bridge",
    "deploy:prod": "werkstatt run leitstand.service.promote --service telegram-alert-bridge",
    "rollback": "werkstatt run leitstand.service.rollback --service telegram-alert-bridge"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  },
  "gogol": {
    "testSignal": {
      "signal": "skipped",
      "owner": "observability",
      "rationale": "Alert bridge behavior is currently guarded by TypeScript and deployment configuration; direct service tests are deferred until webhook fixtures exist.",
      "reviewAfter": "2026-10-01"
    }
  }
}
