# GENERATED. Do not change this line unless the file contains project specific changes.
# DO NOT EDIT THIS FILE. Changes are overwritten on the next build.
# Owner command: rfc.decision-log.generate
# Edit instead: the rfc.decision-log.generate generator source (not this file).
# Regenerate:   forge rfc.decision-log.generate
command: rfc.decision-log.generate
status: ok
count: 866
entries:
  - kind: rejected-alternative
    rfcId: RFC-0001
    title: Introduce RFC governance process
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0001-introduce-rfc-governance-process.md
  - kind: superseded-rfc
    rfcId: RFC-0002
    title: Add multilingual language switcher to nicaragua-projekt
    status: superseded
    closedAt: 2026-05-01
    decisionSummary: "A self-contained Astro component
      `src/components/lang-switcher.astro` is added to `apps/nicaragua-projekt`.
      It:"
    file: docs/rfcs/archive/superseded/rfc-0002-multilingual-language-switcher.md
  - kind: rejected-alternative
    rfcId: RFC-0002
    title: Add multilingual language switcher to nicaragua-projekt
    status: superseded
    alternativesText: >-
      - **Extract to `packages/`**: Rejected — the component is 30 lines of
      Astro markup. The overhead of a shared package is not justified;
      portability is achieved by a documented copy pattern.

      - **React island for interactivity**: Rejected — cyclic navigation via
      `<a>` tags requires zero JS. A pure Astro component with CSS is sufficient
      and aligns with the "no unnecessary JS" invariant.

      - **Dropdown menu**: Rejected — with 2 languages a dropdown is
      over-engineered. A flat list of alpha-2 labels scales acceptably to ~5
      languages without a dropdown.
    file: docs/rfcs/archive/superseded/rfc-0002-multilingual-language-switcher.md
  - kind: rejected-alternative
    rfcId: RFC-0003
    title: Add rfc.check command for RFC artifact integrity
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0003-add-rfc-check-command.md
  - kind: rejected-alternative
    rfcId: RFC-0004
    title: Add componentOverrides to page frontmatter
    status: implemented
    alternativesText: >-
      1. **Inline `pageOverride` objects in `.astro` route files.** Rejected —
      visitor-facing copy in code violates the thin-page invariant.

      2. **Separate `componentPath` per page.** Rejected — breaks the three-way
      mirror and complicates structural checks.
    file: docs/rfcs/archive/implemented/rfc-0004-component-overrides-in-page-frontmatter.md
  - kind: rejected-alternative
    rfcId: RFC-0005
    title: Add dynamic copyright component with year range validation
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Keep string-only copyright | No validation possible, manual year updates
      |

      | Server-side year only | Shows stale year for long-running browser
      sessions |

      | React component with state | Overkill for static year display, hydration
      overhead |

      | Use `new Date().getFullYear()` in Astro template | Build-time only, not
      truly current |

      | Range format "2025-2026" | Hyphen is minus sign; en-dash (–) is correct
      typography |
    file: docs/rfcs/archive/implemented/rfc-0005-dynamic-copyright-component.md
  - kind: rejected-alternative
    rfcId: RFC-0006
    title: Establish Lighthouse Core Web Vitals rules and validation commands
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Separate `site-kernel-lighthouse` package immediately | Adds maintenance
      overhead; Phase 1 needs only static analysis |

      | Use Lighthouse CI GitHub Action instead of site-kernel | Loses
      integration with `site-kernel run check` workflow; requires separate
      config file |

      | 100/100 score requirement | Unrealistic for real sites; thresholds
      should be 90+ for new content, 80+ for legacy |

      | Auto-fix violations | Too risky — performance fixes require human
      judgment (e.g., "where to split chunk") |
    file: docs/rfcs/archive/implemented/rfc-0006-lighthouse-performance-rules.md
  - kind: rejected-alternative
    rfcId: RFC-0007
    title: Restrict client.export to exclude docs, AGENTS.md, agent tooling,
      .windsurfrules, and root .env — include node_modules
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Add exclusions to `.gitignore` | `.gitignore` is version-controlled and
      edits there affect the whole VCS workflow, not just export |

      | Per-app opt-in flag `--exclude-docs` | Exclusions must be unconditional
      — studio-private content must never ship regardless of operator intent |

      | Separate `client.export.strict` command | Adds command surface
      complexity for what is a simple behavioural correction |
    file: docs/rfcs/archive/implemented/rfc-0007-client-export-ecosystem-isolation.md
  - kind: rejected-alternative
    rfcId: RFC-0008
    title: Fall back to default language when a content entry or individual fields
      are missing for the requested language
    status: implemented
    alternativesText: >-
      - **Return 404 for missing language** — rejected because the page is
      enabled in `features.ts` for all languages; returning 404 would silently
      hide content that should be visible.

      - **Filter `getStaticPaths` to only generate routes where the translation
      exists** — rejected because it couples route generation to content
      completeness and makes it harder to see which translations are missing.

      - **Suppress the error entirely with no warning** — rejected; silent
      degradation violates the principle that content gaps must be visible to
      developers at build time.
    file: docs/rfcs/archive/implemented/rfc-0008-content-entry-language-fallback.md
  - kind: rejected-alternative
    rfcId: RFC-0009
    title: Extend component mirror contract from triad to quartet with optional
      client script enforcement
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Keep script naming free-form with a manifest file | Manifest requires
      maintenance; naming convention is self-documenting |

      | Enforce quartet inside `mirror.triad.validate` | Additive concern; triad
      check stays minimal and focused |

      | Use `// @scripts` YAML frontmatter in `.astro` | Frontmatter in `.astro`
      is non-standard; comment directive is simpler |

      | Validate CSS inside existing `tokens.ds.lint` | Different concern; CSS
      presence ≠ CSS token correctness |
    file: docs/rfcs/archive/implemented/rfc-0009-component-quartet-mirror.md
  - kind: superseded-rfc
    rfcId: RFC-0010
    title: Add client-side language detection with hard fallback to default
    status: superseded
    closedAt: 2026-05-01
    decisionSummary: "Root language detection moves from server-side middleware to
      **client-side JavaScript** in the static `index.astro` page. The
      decision:"
    file: docs/rfcs/archive/superseded/rfc-0010-client-side-language-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0010
    title: Add client-side language detection with hard fallback to default
    status: superseded
    alternativesText: >-
      - **Keep server-side detection with SSR mode** — rejected because `output:
      "static"` is a deliberate invariant for CDN hosting and fast cold starts.

      - **Use Astro.preferredLocale** — rejected because it requires `output:
      "server"` or hybrid mode and does not eliminate the header access warning.

      - **Generate multiple root files** — rejected because it would require
      `/index.html` to exist per language, which complicates CDN configuration
      and violates the single-entry-point pattern.

      - **Remove root redirect entirely** — rejected because direct navigation
      to `/` is common; forcing 404 is poor UX.
    file: docs/rfcs/archive/superseded/rfc-0010-client-side-language-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0011
    title: "Establish script placement contract: src/scripts canonical pattern,
      colocation, and deferred loading"
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Allow S-1 scripts in layout with a comment exemption | Would normalize
      the anti-pattern; exemptions accumulate |

      | Enforce placement via lint rules only (eslint/stylelint) | `.astro`
      files are not standard JS/CSS; kernel commands are the established
      validation mechanism |
    file: docs/rfcs/archive/implemented/rfc-0011-script-placement-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0012
    title: Modularize semantic page builders and derive types from content schemas
    status: implemented
    alternativesText: >-
      ### Keep pages.ts but import types from schemas


      Would fix the type duplication (problem 1) but not the scaling problem
      (problem 2) or the missing-builder detection (problem 3). Still leaves a
      monolith that grows linearly with page count.


      ### Use a single buildContentPageSemantic for all non-home pages


      Already the current approach for simple markdown pages. Works when pages
      are structurally identical, but breaks down when pages have unique section
      compositions (home page with 9 sections, about page with people
      enrichment, projects page with initiatives). Per-page files allow each
      builder to import only the types it needs.


      ### Auto-generate semantic builders from page schemas


      Premature. The semantic projection logic involves non-trivial decisions
      (which sections to include, how to map component content to answer blocks,
      which enrichments to attach). These are editorial decisions, not derivable
      from schemas alone.
    file: docs/rfcs/archive/implemented/rfc-0012-semantic-layer-modularization.md
  - kind: rejected-alternative
    rfcId: RFC-0013
    title: Implement universal footer component for brand consistency
    status: implemented
    alternativesText: >-
      1. **Keep separate footers**: Rejected - leads to maintenance burden and
      inconsistency

      2. **Shared package footer**: Rejected - premature abstraction; stabilize
      in apps first

      3. **No photo positioning**: Rejected - brands need flexibility for visual
      identity
    file: docs/rfcs/archive/implemented/rfc-0013-universal-footer-component.md
  - kind: rejected-alternative
    rfcId: RFC-0014
    title: Extend quartet mirror contract to page routes
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Implement as a separate `route.slug.validate` command | Inconsistent
      with the quartet architecture — the same naming alignment principle
      already governs components; pages should follow the same model inside the
      same command |

      | Warn instead of error | The mismatch has no legitimate intentional use
      case; warnings would accumulate silently |

      | Fix the three violating routes without a check | The fix would drift
      again without enforcement; the check is the durable solution |

      | Enforce inside `mirroring.validate` | Different concern —
      `mirroring.validate` checks cross-language file presence, not the
      route-to-content slug binding |
    file: docs/rfcs/archive/implemented/rfc-0014-extend-quartet-mirror-contract-to-page-routes.md
  - kind: rejected-alternative
    rfcId: RFC-0015
    title: Extend GRACE codegen commands to support packages/ alongside apps/
    status: implemented
    alternativesText: >-
      ### Register grace.\* commands inside each package's own kernel.config.ts


      Packages are not kernel hosts — they do not ship a `kernel.config.ts`.
      Adding one per package solely for GRACE commands would invert the
      dependency model (`packages/` → `site-kernel` command registration).
      Rejected.


      ### Add a separate grace.packages.backfill command


      Duplicating the command surface for `packages/` creates divergence in
      quality rules, prompt templates, and failure modes. A flag on the existing
      command is simpler and consistent with how `--app` already scopes the same
      commands. Rejected.


      ### Keep packages/ as a manual-only GRACE zone


      Already the current state. Rejected because it creates a permanent
      inventory gap and makes the stated `source-markup.xml` rollout policy
      unenforceable by tooling.
    file: docs/rfcs/archive/implemented/rfc-0015-grace-commands-packages-support.md
  - kind: rejected-alternative
    rfcId: RFC-0016
    title: Extend icons.generate command with index.ts exports
    status: implemented
    alternativesText: >-
      1. **Separate command**: Add `icons.index.generate` instead of extending
      `icons.generate`.
         - _Rejected_: Would fragment the workflow; index generation should happen automatically when icons change.

      2. **Named exports only from per-set index files**:
         - _Rejected_: Still requires knowing which set an icon belongs to; doesn't solve the discoverability problem.

      3. **Keep current state (manual deep imports)**:
         - _Rejected_: Poor developer experience and agent ergonomics.
    file: docs/rfcs/archive/implemented/rfc-0016-extend-icons-generate-with-index-exports.md
  - kind: rejected-alternative
    rfcId: RFC-0017
    title: Extend grace clear with history depth parameter
    status: implemented
    alternativesText: >-
      <!-- What else was considered and why was it rejected?
           Be brief but honest — this section prevents re-litigating old decisions. -->
    file: docs/rfcs/archive/implemented/rfc-0017-extend-grace-clear-with-history-depth-parameter.md
  - kind: superseded-rfc
    rfcId: RFC-0018
    title: Define content-declared feature graph and visibility integrity checks
    status: superseded
    closedAt: 2026-04-21
    decisionSummary: "`nicaragua-projekt` moves from an authored boolean feature
      tree to a **content-declared feature graph**."
    file: docs/rfcs/archive/superseded/rfc-0018-define-content-declared-feature-graph-and-visibility-integrity-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0018
    title: Define content-declared feature graph and visibility integrity checks
    status: superseded
    alternativesText: >-
      1. **Keep `src/configure/features.ts` as the canonical authored tree and
      add more nested booleans**
         - Rejected because it keeps structural meaning outside content and does not solve link/anchor integrity.

      2. **Store only behavior toggles in content and keep visibility in
      config**
         - Rejected because it would split one concern into two sources of truth.

      3. **Keep raw local `href` values and add more `featureFlag` fields**
         - Rejected because dead links still depend on human discipline rather than graph resolution.

      4. **Infer all anchors and items from `.astro` source without explicit
      content declarations**
         - Rejected because item-level behavior and business meaning would remain implicit and unstable.
    file: docs/rfcs/archive/superseded/rfc-0018-define-content-declared-feature-graph-and-visibility-integrity-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0019
    title: Promote page-section-component-content architecture and structure checks
      to all apps
    status: implemented
    alternativesText: >-
      1. Keep the hierarchy as documentation only
         - Rejected because the repository already relies on shared pre-build checks for core architecture invariants; this rule is too central to remain manual.

      2. Treat breadcrumbs as a permanent route-level exception
         - Rejected because it weakens the very hierarchy this RFC is trying to formalize and leaves navigation outside the section model.

      3. Keep RFC-0018 app-only and create app-local checks in each site
         - Rejected because the requested goal is a shared architecture for `apps/*`, and app-local duplication would weaken the Site OS model.

      4. Infer the entire hierarchy only from file paths without feature
      metadata
         - Rejected because the build also needs structural meaning such as section roles and navigation intent, not just file existence.

      5. Move `header` and `footer` into the page section tree
         - Rejected because they are shared shell components, not page-body sections, and forcing them into the hierarchy would blur ownership boundaries.
    file: docs/rfcs/archive/implemented/rfc-0019-promote-page-section-component-content-architecture-and-structure-checks-to-all-apps.md
  - kind: rejected-alternative
    rfcId: RFC-0020
    title: Adopt layer-specific file suffix contracts and suffix-aware OS validation
    status: implemented
    alternativesText: >-
      1. Keep component naming location-only
         - Rejected because top-level `src/components/` files still lack an explicit layer marker while section files already have one.

      2. Add `-page` to route files
         - Rejected because route stems surface into URLs and should stay concise and implementation-free.

      3. Add `-style` to CSS files
         - Rejected because `.css` plus `src/styles/` already identifies the style layer clearly.

      4. Keep raw filename comparison inside validators
         - Rejected because required suffix adoption would create artificial drift between logical identity and physical filename.

      5. Ignore `AGENTS.md` only in a few commands
         - Rejected because instruction metadata should be globally excluded by contract, not by scattered incidental exceptions.

      6. Keep the unused `src/components/logo/` folder in the proving app
         - Rejected because it adds dead structure precisely when the proving rollout should reduce ambiguity.
    file: docs/rfcs/archive/implemented/rfc-0020-adopt-layer-specific-file-suffix-contracts-and-suffix-aware-os-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0021
    title: Establish layouts content layer and OS validation contract
    status: implemented
    alternativesText: >-
      1. **Keep layouts as components**: Rejected because layouts are
      architecturally distinct (shell vs. visitor-facing body) and deserve their
      own namespace.


      2. **Keep `-component` suffix**: Rejected because it breaks the
      established pattern where page schemas use bare names, and layouts are not
      components.


      3. **Validate only in proving app**: Rejected because this is a
      workspace-scoped architectural contract that should apply uniformly to all
      apps.
    file: docs/rfcs/archive/implemented/rfc-0021-establish-layouts-content-layer-and-os-validation-contract.md
  - kind: superseded-rfc
    rfcId: RFC-0022
    title: Unify Semantic and Astro Infrastructure in @gogol/share
    status: superseded
    closedAt: 2026-05-01
    decisionSummary: The `@gogol/share` package is promoted to be the canonical home
      for all app-agnostic platform intelligence.
    file: docs/rfcs/archive/superseded/rfc-0022-unify-semantic-and-astro-infrastructure-in-shared-package.md
  - kind: rejected-alternative
    rfcId: RFC-0022
    title: Unify Semantic and Astro Infrastructure in @gogol/share
    status: superseded
    alternativesText: >-
      - **Create `@gogol/site-kernel-semantic`**: Rejected to avoid package
      proliferation. The user explicitly requested to consolidate in
      `@gogol/share`.

      - **Keep Astro utilities out of `@gogol/share`**: Rejected because many
      content utilities are fundamentally tied to how Astro handles collections,
      and separation would create awkward abstractions.
    file: docs/rfcs/archive/superseded/rfc-0022-unify-semantic-and-astro-infrastructure-in-shared-package.md
  - kind: rejected-alternative
    rfcId: RFC-0023
    title: Introduce Uni UI Ontology and manifest-driven component registry
    status: implemented
    alternativesText: >-
      1. **Rename `@gogol/ui` → `@gogol/icons` + create fresh `@gogol/ui`.**
      Rejected: the current `@gogol/ui` description already reads "Shared UI
      components and icons"; the package was designed to hold both. Avoiding the
      rename reduces churn on downstream imports.

      2. **Keep `manifest.json` instead of `manifest.yaml`.** Rejected: YAML is
      more pleasant to hand-author (comments, multi-line strings), aligns with
      the Astro content-collections frontmatter format already in use, and Zod
      validates the parsed object identically. The JSON argument of the source
      spec ("AI agents read JSON") applies to the **registry** (which remains
      JSON), not to individual hand-authored manifests.

      3. **Keep cosmic vocabulary (`star`, `planet`, `moon`,
      `cosmos.registry.json`).** Rejected: the warpgogol file layout is already
      technical (`pages/sections/components`); mixing metaphors between
      filesystem and vocabulary costs cognitive load without adding value. The
      `Uni UI Ontology` brand preserves spec traceability while aligning
      vocabulary with implementation.

      4. **`Intent` as a closed enum.** Rejected: intent vocabulary evolves
      faster than roles. Leaving it open avoids ceremony for every new verb.

      5. **Introduce Asteroid / Element as a fourth layer.** Rejected by the
      source spec (§0). Standalone service elements use `standalone: true` on a
      component.

      6. **Stateful registry via Cloudflare Durable Objects.** Rejected by the
      source spec (§0, §8.1). Static build-time registry is sufficient.

      7. **Poetic codenames (Vega, Europa, Charon) now.** Rejected: dual naming
      is valuable when many variants of the same role exist. Today each role has
      one implementation. Defer to a naming RFC when variant count justifies the
      cognitive overhead.

      8. **Business layer as part of `@gogol/share`.** Rejected: business is a
      first-class domain (11 schemas, rich loader logic, its own Astro
      collection). Housing it separately makes its boundaries (site-description
      vocabulary) obvious and keeps `@gogol/share` focused on cross-cutting
      helpers.

      9. **Business layer as part of `@gogol/ontology`.** Rejected: ontology is
      about UI selection; business is about site description. Conflating them in
      one package would muddle responsibilities and inflate its API surface.
    file: docs/rfcs/archive/implemented/rfc-0023-introduce-uni-ui-ontology-and-manifest-driven-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0024
    title: Establish business layer as canonical site description
    status: implemented
    alternativesText: >-
      1. **Fold business schemas into `@gogol/share`.** Rejected. `@gogol/share`
      is platform infrastructure (astro helpers, semantic extractors,
      middleware); business is domain identity. Mixing them would blur DNA-19's
      closed-vocabulary boundary and force every app that needs `share/astro` to
      also pull in business schemas.

      2. **Fold business schemas into `@gogol/ontology`.** Rejected.
      `@gogol/ontology` is a closed-enum vocabulary for the UI surface
      (`SemanticRole`, `ComponentRole`, `Industry`, `Layer`). Business schemas
      are open, structured, per-site data — a category error if co-housed.

      3. **Keep business schemas app-local, share only a `Profile` TypeScript
      type.** Rejected. The loader logic (language fallback, caches,
      cross-reference resolution) is where the complexity lives. Sharing only
      types reproduces exactly today's drift problem.

      4. **Move business content into a shared `packages/business-content/`
      seed.** Rejected. Violates DNA-4 (canonical content in `src/content/`) and
      defeats the purpose of per-client customization. Content is the one thing
      that MUST stay app-local.

      5. **Introduce a CMS fetch layer.** Rejected. Out of scope for a
      build-time static pipeline (DNA-3 Astro SSG). Can be revisited in a later
      RFC if a client drives it.
    file: docs/rfcs/archive/implemented/rfc-0024-establish-business-layer-as-canonical-site-description.md
  - kind: rejected-alternative
    rfcId: RFC-0025
    title: Activate cosmic overlay and feature-first app layout
    status: implemented
    alternativesText: >-
      1. **Keep parallel `styles/`, `scripts/`, `assets/` trees grouped by layer
      (the proposal from the ChatGPT discussion).** Rejected. Groups by layer
      solve name collisions but retain four parallel trees — developers still
      open four folders to work on one section. Feature-first colocation solves
      both problems with one tree.


      2. **Flat `features/` folder collapsing `pages/sections/components/`
      (another variant from the same discussion).** Rejected. The Uni Ontology
      `Layer` enum is a real semantic distinction (stars orbit galaxies, planets
      orbit stars, moons orbit planets — different dependencies, different
      lifecycle). Collapsing them into a flat `features/` tree would surface a
      structural inconsistency with the package side
      (`packages/ui/src/{pages,sections,components}/`) and lose the semantic cue
      at the directory level.


      3. **Allow per-section CSS overrides in apps as "escape hatches with
      warnings".** Rejected. Escape hatches with warnings rot: every project
      accumulates `// TODO` hatches that become de-facto policy. The
      client-as-committer model turns this from a style issue into a
      _deployment-safety_ issue — a client can accidentally break visual
      consistency across a site. ERROR-level is the correct posture.


      4. **Open cosmic name catalog (strings validated only by regex).**
      Rejected. Two clients both authoring a `Quasar` section at different
      layers would collide silently until registry build. The closed catalog is
      a natural fit for the `@gogol/ontology` closed-enum pattern (DNA-19).


      5. **Keep `cosmicName = id` (never activate the Universe overlay).**
      Rejected. This would retroactively make RFC-0023's "Universe" naming a
      dead metaphor and forfeit the brand-layer value described in the
      Perplexity Deep Research documents. The passport (RFC-0028) depends on
      distinct `cosmicName` values for the Star Map view; without activation,
      RFC-0028 becomes meaningless.


      6. **Co-locate constellation and biome manifests inside each app
      (`apps/<app>/constellation.yaml`).** Rejected. Per-app constellations
      defeat the point: the whole value is _reusable composition patterns_.
      Workspace-library under `@gogol/ontology/` matches how other
      closed-vocabulary artifacts are organized.


      7. **Put biome overrides into SCSS/PostCSS instead of native CSS cascade
      layers.** Rejected. CSS cascade layers are broadly supported across the
      target browsers, compose cleanly with `data-biome` attribute selectors, do
      not require a preprocessor in the build pipeline, and keep `@gogol/tokens`
      framework-agnostic.


      8. **Client edits auto-deploy without `client.edit.validate`.** Rejected.
      The auto-deploy-on-commit workflow is the whole delivery model; a
      misbehaving commit that touches `astro.config.mjs` would break production
      for a client. The validator is the contract that makes auto-deploy safe.


      9. **`client.edit.validate` by GitHub file-path permissions instead of an
      OS command.** Rejected. GitHub CODEOWNERS and branch-protection rules
      cannot express partial-file rules (the `identity.biome` exception in
      `system.yaml`). An OS-level command with semantic awareness of YAML
      structure is the correct layer.
    file: docs/rfcs/archive/implemented/rfc-0025-activate-cosmic-overlay-and-feature-first-app-layout.md
  - kind: rejected-alternative
    rfcId: RFC-0026
    title: Block-declarative pages and RuntimeContext pipeline
    status: implemented
    alternativesText: >-
      1. **Markdown body as the composition primitive
      (MDX/components-in-markdown).** Rejected. Two incompatible styles of the
      same thing (structured frontmatter + free-form body) invite drift; schema
      validation is weaker; client-editable safety is harder to enforce because
      arbitrary JSX can import arbitrary modules.


      2. **Block composition in the `.astro` route file, not in content.**
      Rejected. Contradicts the client-as-committer model — clients cannot edit
      `.astro`. Forces engineering-side changes for any content reorder.
      Violates DNA-7 (thin page routes).


      3. **Block visibility as a separate grammar from feature-graph
      visibility.** Rejected. Two grammars means two parsers, two evaluators,
      two documents. Authors switching context between "feature-level" and
      "block-level" visibility is a preventable source of errors. Shared grammar
      with two consumers is the clean design.


      4. **Extensible `RuntimeContext` (authors add fields).** Rejected. The
      whole point of reserving the type is forward-compat for specific planned
      features (persona, experiments). Open extensibility means every feature
      invents its own context key, defeating the consolidation.


      5. **Block props untyped (`Record<string, unknown>` everywhere).**
      Rejected. Section manifests already declare propsSchemas (DNA-5, DNA-18);
      skipping validation at build time discards an already-paid-for guarantee
      and turns prop-shape regressions into runtime bugs.


      6. **Per-app buildPage implementations.** Rejected. DNA-7 demands thin
      routes. One pipeline, one contract, one place to evolve.


      7. **`visibility:` as a truthy JavaScript expression string (`visibility:
      "feature:hero-enabled && locale === 'de'"`).** Rejected. String
      expressions invite sandboxing concerns and make static analysis harder. A
      closed discriminated-union grammar is machine-analyzable and cheap to
      parse.


      8. **Lazy visibility evaluation (runtime, not build-time).** Rejected. The
      static export target (Cloudflare CDN, `output: "static"` per DNA-1)
      precludes runtime visibility logic. All visibility resolves at build time
      against `EMPTY_RUNTIME_CONTEXT(lang)` per-locale. RFC-0027 introduces
      per-request decisions via edge functions, not runtime re-render.
    file: docs/rfcs/archive/implemented/rfc-0026-block-declarative-pages-and-runtime-context.md
  - kind: rejected-alternative
    rfcId: RFC-0027
    title: "Growth Layer: events, funnels, experiments, and vendor-agnostic adapter"
    status: implemented
    alternativesText: >-
      1. **Server-side experiment decisions via Cloudflare Workers.** Rejected.
      DNA-1 forbids server-side runtime; adding Workers for experiments only is
      a slippery slope that would inevitably expand to other concerns.
      Client-side decision with pre-rendered variants is the architecturally
      consistent choice.


      2. **Single bundled adapter (only one vendor ever, chosen by the
      studio).** Rejected. Studio clients will span vendors driven by their own
      compliance and procurement decisions. Locking a vendor defeats the
      portable-client model.


      3. **Adapters as content (YAML-declared vendor configuration).** Rejected.
      Vendors ship code (SDK init, cookie handling, consent surface
      integration). Code belongs in packages, not content. `system.yaml.growth`
      references _which_ adapter; the adapter itself is a package.


      4. **Free-form event emission with post-hoc catalog generation.**
      Rejected. DNA-19 established closed vocabularies as the invariant; events
      are a vocabulary. Allowing free-form emission means rename audits become
      grep-audits that silently miss dynamic ids.


      5. **Experiments as engineering-only content.** Rejected. The
      client-as-committer model's whole point is that clients ship changes
      without engineering gatekeeping. Experiments are a core client activity
      (copy, price, CTA testing).


      6. **Flag namespace `experimentId.variantId` (dot-separated) or
      `experimentId/variantId`.** Rejected in favour of colon. Dots collide with
      DNS-style ids readers mentally parse; slashes collide with path separators
      in some analytics vendors. Colon is unambiguous and matches conventions
      like `role:donor`.


      7. **Block prop-level variant substitution in Pattern B.** Rejected for
      MVP. Visibility-gated variants (Pattern A) are sufficient; prop-level
      variant substitution requires a larger design for how variant manifests
      merge with base props. Deferred.


      8. **Multi-vendor per concern (analytics sent to both Plausible and
      GA4).** Rejected. Double-counting, consent-surface sprawl, cost
      multiplication, and attribution drift across vendors. If a client needs
      two vendors for distinct purposes (e.g., Plausible for business,
      Cloudflare Web Analytics for infrastructure), they are two concerns, not
      two bindings on one concern.


      9. **Persona detection server-side (edge cookies, geo, device).** Rejected
      for this RFC. Hints exist (Cloudflare geo headers, `Accept-Language`) but
      coupling `segment` resolution to edge introduces runtime dependency this
      RFC avoids. Deferred to a future Persona RFC if driven by client demand.
    file: docs/rfcs/archive/implemented/rfc-0027-growth-layer-events-funnels-experiments.md
  - kind: rejected-alternative
    rfcId: RFC-0028
    title: Cosmic Passport, Star Map View, and Nebula Score
    status: implemented
    alternativesText: >-
      1. **Skip W3C VC; use a plain HMAC signature.** Rejected. HMAC requires
      shared-secret distribution which is worse than public-key for auditor
      scenarios. W3C VC is mature, tooling exists, and DIDs (`did:web`)
      naturally align with the studio's domain-per-app model.


      2. **Store the passport in `/api/passport` as a dynamic endpoint.**
      Rejected. DNA-1 forbids server runtime. Static JSON at `/.well-known/` is
      the canonical pattern for site-level metadata (RFC 8615).


      3. **Per-app Nebula weightings.** Rejected. Cross-site comparability is
      the whole point. Let clients with unusual priorities argue for global
      weighting changes via RFC.


      4. **Star Map rendered client-side via D3.js or Three.js.** Rejected.
      Client-side rendering adds bundle weight and makes the map unavailable in
      crawlers, screenshots, PDFs. SSG SVG + optional ~2KB interactivity island
      is the right balance.


      5. **Include visitor analytics in the passport.** Rejected. Passport is
      about the _site_, not about its visitors. Visitor data lives in Growth
      (RFC-0027), vendor-hosted, and requires consent to render publicly.
      Passport remains PII-free.


      6. **Use a Merkle tree over the entire `dist/` output for provenance.**
      Rejected as overkill. System-hash + commit-sha + VC signature provide the
      integrity we need; per-file Merkle hashes add complexity without clear
      demand. Deferred to a future compliance RFC if driven.


      7. **Pulsar as a live websocket or SSE endpoint.** Rejected. Runtime
      infrastructure violates DNA-1. Scheduled rebuild + `builtAt` on a static
      page provides the freshness signal at zero runtime cost.


      8. **Indexable by default.** Rejected. Passports expose architectural
      details (section versions, build history, commit SHAs) — SEO-indexing them
      by default risks leaking information that clients did not opt into
      exposing. Opt-in is the safer default.


      9. **Sign passports with a workspace-wide key instead of per-app.**
      Rejected. Per-app keys enable independent rotation, independent revocation
      semantics, and clearer audit trails. The operational cost of per-app keys
      is low (keys are generated once, rotated rarely).


      10. **Embed passport inside a human-facing PDF.** Rejected for MVP. JSON +
      SVG + HTML page cover the machine-readable and human-readable needs. PDF
      export is a future convenience, not a contract.
    file: docs/rfcs/archive/implemented/rfc-0028-cosmic-passport-star-map-nebula-score.md
  - kind: superseded-rfc
    rfcId: RFC-0029
    title: Greenfield rebuild of nicaragua-projekt and client-onboarding playbook
    status: superseded
    closedAt: 2026-05-18
    decisionSummary: This RFC orchestrates the migration of
      `apps/nicaragua-projekt/` and the extraction of
      `@gogol/site-kernel-onboarding`. No new contracts. No new DNA invariants.
      One new composite command, one new package, two new orchestration
      commands.
    file: docs/rfcs/archive/superseded/rfc-0029-greenfield-rebuild-and-client-onboarding-playbook.md
  - kind: rejected-alternative
    rfcId: RFC-0029
    title: Greenfield rebuild of nicaragua-projekt and client-onboarding playbook
    status: superseded
    alternativesText: >-
      1. **Land each RFC's app-side wave separately, in different PR series.**
      Rejected. Validator interleaving (some pass, others fail) creates
      intermediate states where agents have no clear reference. Single ordered
      sequence is the discipline.


      2. **Skip the rebuild; preserve current `nicaragua-projekt` and apply
      contracts incrementally.** Rejected per user explicit guidance: no
      backward compatibility, no legacy. The studio has no live clients; the
      cost of a clean break is zero.


      3. **Build the onboarding scaffold first, then use it to scaffold a new
      `apps/nicaragua-projekt-v2/`, then archive the old one.** Rejected. The
      scaffold cannot be designed in the abstract — it must be extracted from a
      real, working, fully-compliant app. Phase E depends on Phase C.


      4. **Pure CLI scaffold vs. interactive wizard.** Rejected for MVP. CLI
      determinism makes the scaffold reproducible from a single command line —
      useful for CI, useful for documentation, useful for agents. A wizard layer
      can come later without changing the CLI contract.


      5. **`onboarding.checklist` integration with the passport.** The checklist
      could itself become a passport-attached document showing onboarding
      readiness. Deferred until checklist adoption is real.


      6. **Scaffold for white-labeled studio resellers.** Out of scope.


      _Resolved inside this RFC, not deferred:_


      - **Single ordered migration vs interleaved waves** — single ordered
      phases.

      - **Compatibility mode for partial migration** — permanently forbidden;
      binary readiness only.

      - **Per-client engineering scripts** — permanently forbidden; one playbook
      governs all onboardings.

      - **Scaffold extraction timing** — Phase E only, after Phase C produces
      the canonical reference.

      - **Composite vs per-RFC readiness commands** — composite
      (`app.contract.full`) only.
    file: docs/rfcs/archive/superseded/rfc-0029-greenfield-rebuild-and-client-onboarding-playbook.md
  - kind: superseded-rfc
    rfcId: RFC-0030
    title: Runtime scaffold completion and KernelCommandResult envelope contract
    status: superseded
    closedAt: 2026-05-18
    decisionSummary: Two tightly-coupled deliverables in one RFC because they share
      the same root cause (scaffold/contract incompleteness) and the same
      migration window (post-RFC-0029 cleanup).
    file: docs/rfcs/archive/superseded/rfc-0030-runtime-scaffold-completion-and-result-envelope.md
  - kind: rejected-alternative
    rfcId: RFC-0030
    title: Runtime scaffold completion and KernelCommandResult envelope contract
    status: superseded
    alternativesText: >-
      1. **Make the result envelope a TypeScript discriminated union enforced at
      the type level.** Rejected — would require all check command return types
      to widen `KernelCommandResult` itself, which affects the kernel package
      and many consumers. Lint-based enforcement is sufficient and keeps the
      kernel contract stable.


      2. **Embed the runtime shell inside `@gogol/site-kernel-astro` as a
      code-free runtime.** Rejected — the existing pattern is per-app code in
      `src/` because Astro requires concrete files in `src/pages/`,
      `src/layouts/`, etc. Extracting them as a runtime would require a
      build-time codegen step that contradicts the "everything is a real file in
      your repo" principle.


      3. **Generate the runtime templates lazily from `apps/nicaragua-projekt/`
      at scaffold time.** Rejected — couples the scaffold to a specific
      reference app being present and pristine. Templates as committed files are
      the canonical source of truth.


      4. **Skip Phase C and rely on manual smoke testing.** Rejected — the whole
      point of `app.contract.full` is automated readiness verification; the
      scaffold deserves the same CI coverage.
    file: docs/rfcs/archive/superseded/rfc-0030-runtime-scaffold-completion-and-result-envelope.md
  - kind: rejected-alternative
    rfcId: RFC-0031
    title: Canonicalize colocated source assets and feature-scoped client scripts
    status: implemented
    alternativesText: >-
      1. **Keep all editable images in `public/`.** Rejected. It maximizes URL
      simplicity at the cost of build-time optimization, hashed invalidation,
      and source colocation.

      2. **Use only root `src/assets/`, never `src/content/**/assets/`.\*\*
      Rejected. It restores a parallel-tree model and weakens bundle locality.

      3. **Put feature scripts in `public/scripts/`.** Rejected. This bypasses
      Astro bundling and repeats the exact problems RFC-0011 was created to
      prevent.

      4. **Allow arbitrary TypeScript under `src/content/`.** Rejected. Only
      bounded feature entry modules are in scope; shared or app-global logic
      still belongs elsewhere.
    file: docs/rfcs/archive/implemented/rfc-0031-canonicalize-colocated-source-assets-and-feature-scoped-client-scripts.md
  - kind: rejected-alternative
    rfcId: RFC-0032
    title: Enforce app-agnostic utility extraction to @gogol/share
    status: implemented
    alternativesText: >-
      1. **Lint imports only (not exports).** Rejected. Import-source linting
      alone misses entirely local re-implementations that are never imported
      from `@gogol/share`.

      2. **Document the rule more prominently.** Rejected. Documentation-only
      rules have already proven insufficient — RFC-0022 documented the rule;
      this RFC enforces it.

      3. **Per-app allowlist for known exceptions.** Considered for future
      extension. Initial wave has no allowlist to keep the rule strict and avoid
      carve-out accumulation.
    file: docs/rfcs/archive/implemented/rfc-0032-enforce-app-agnostic-utility-extraction-to-webgogol-share.md
  - kind: superseded-rfc
    rfcId: RFC-0033
    title: Retire app-local content schemas and migrate feature graph schema to
      @gogol/share
    status: superseded
    closedAt: 2026-05-18
    decisionSummary: Three tightly scoped changes in dependency order.
    file: docs/rfcs/archive/superseded/rfc-0033-retire-app-local-content-schemas-and-migrate-feature-graph-to-share.md
  - kind: rejected-alternative
    rfcId: RFC-0033
    title: Retire app-local content schemas and migrate feature graph schema to
      @gogol/share
    status: superseded
    alternativesText: >-
      1. **Keep per-section Zod schemas alongside `manifest.yaml`
      `propsSchema`.** Rejected. Dual validators with no sync mechanism produce
      silent drift (already demonstrated by the `team` vs `members`
      discrepancy). One source of truth: `propsSchema` in `manifest.yaml`.


      2. **Move component and page schemas to `packages/ui/` alongside the
      components.** Rejected for component schemas: `propsSchema` in
      `manifest.yaml` already serves that role in JSON Schema; a parallel Zod
      file adds nothing. Rejected for page schemas (`agbPageSchema`, etc.):
      these are page-specific field lists that describe one client's page
      structure — not reusable cross-app logic.


      3. **Keep `features.ts` app-local and accept copy-paste.** Rejected. The
      schema is structurally identical across all apps implementing RFC-0018.
      Copy-paste divergence is the exact problem `@gogol/share` solves.
    file: docs/rfcs/archive/superseded/rfc-0033-retire-app-local-content-schemas-and-migrate-feature-graph-to-share.md
  - kind: rejected-alternative
    rfcId: RFC-0034
    title: Colocate component-content TypeScript types with packages/ui components
    status: implemented
    alternativesText: >-
      1. **Put all content types in `@gogol/share`.** Rejected — content types
      are component-specific. `BrandLabelComponentContent` belongs next to
      `brand-label-component.astro`, not in a generic utilities package.
      Colocation makes discovery trivial.


      2. **Generate types from `propsSchema` in `manifest.yaml`.** Appealing but
      premature — JSON Schema → TypeScript codegen adds toolchain complexity.
      Manual `.types.ts` is simpler and equally correct. Can be revisited in a
      future RFC.


      3. **Keep `@schemas` alias and types in the app.** Rejected. Copy-paste
      per app, Wave 3 of RFC-0033 unexecutable indefinitely.
    file: docs/rfcs/archive/implemented/rfc-0034-colocate-component-content-types-with-packages-ui-components.md
  - kind: rejected-alternative
    rfcId: RFC-0035
    title: Unify section component props contract to eliminate renderer conditionals
    status: implemented
    alternativesText: >-
      - **Renderer reads manifest.yaml to determine props:** Rejected —
      manifests are build-time metadata, not runtime dispatch logic. Coupling
      renderer to manifest schema adds complexity without benefit.

      - **Props passed as spread `{...block.props}`:** Rejected — loses type
      safety, makes it impossible to validate that required system props
      (`lang`, `sectionNumber`) are present.

      - **Each section exports a `getProps(block)` function:** Rejected —
      over-engineering; simple destructuring from `pageOverride` is sufficient.
    file: docs/rfcs/archive/implemented/rfc-0035-unify-section-component-props-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0036
    title: Extend blocks-renderer to support shell-level and MoonCatalog components
    status: implemented
    alternativesText: >-
      1. **Keep Background in page frontmatter**
         - Rejected: Mixes content (page prose) with presentation (background config)
         - Violates DNA-22 clientEditable boundaries

      2. **Add Atlas to planets[] and treat as regular section**
         - Rejected: Background is shell-layer, not content-layer
         - Would appear in `page.blocks[]` client-editable area

      3. **Separate ShellRenderer component**
         - Rejected: Fragmentation of block pipeline
         - BlocksRenderer should handle all block types uniformly
    file: docs/rfcs/archive/implemented/rfc-0036-extend-blocks-renderer-to-support-shell-level-and-mooncatalog-components.md
  - kind: rejected-alternative
    rfcId: RFC-0037
    title: "Supersede RFC-0022: Allow astro:content in @gogol/share"
    status: implemented
    alternativesText: >-
      1. **Keep RFC-0022 restriction, move buildPage to separate package.**
         - Rejected. Package proliferation adds complexity. `@gogol/share` is the correct home for shared Astro infrastructure.

      2. **Keep RFC-0022 restriction for all astro:content imports.**
         - Rejected. RFC-0026 already violated this in practice with `buildPage()`. The restriction was unenforceable.
    file: docs/rfcs/archive/implemented/rfc-0037-supersede-rfc-0022-allow-astro-content-in-share-and-mandate-thin-apps.md
  - kind: rejected-alternative
    rfcId: RFC-0038
    title: Content-declared language configuration and visitor language detection
    status: implemented
    alternativesText: >-
      1. **Keep languages in code, add YAML overlay.**
         - Rejected. Still requires engineering for new languages. Violates DNA-22.

      2. **Store language config in separate file (i18n.yaml).**
         - Rejected. Fragmentation. `system.md` is already the canonical system manifest per RFC-0025.

      3. **Support runtime language switching (SPA style).**
         - Rejected. Out of scope. Static site target (DNA-1) means page reload on lang change.

      4. **Multi-vendor i18n (different translation providers per language).**
         - Deferred. Add `provider` field in future RFC if needed. Not required at MVP.
    file: docs/rfcs/archive/implemented/rfc-0038-content-declared-language-configuration-and-visitor-language-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0039
    title: "Section Background Control: transparent and verticalFade"
    status: implemented
    alternativesText: >-
      1. **Single enum property** (`backgroundStyle: 'solid' | 'transparent' |
      'fade' | 'fade-inverted'`): Rejected because it prevents composability and
      makes the matrix harder to extend.


      2. **CSS custom properties in content** (`--section-bg: transparent`):
      Rejected because it breaks the design system contract and allows arbitrary
      values.


      3. **Separate `fadeInverted` boolean**: Rejected because combining
      `transparent` + `verticalFade` achieves the same effect more elegantly.
    file: docs/rfcs/archive/implemented/rfc-0039-section-background-control-transparent-and-verticalfade.md
  - kind: rejected-alternative
    rfcId: RFC-0040
    title: Add GSAP as shared motion library and animated stat counter for impact
      sections
    status: implemented
    alternativesText: >-
      - **CountUp.js** — lighter (~4 KB) but provides only numeric animation
      with no shared motion stack for future use. GSAP amortizes its cost across
      all future motion needs.

      - **Odometer.js** — visually compelling "digit-roll" effect but requires
      theme CSS injection and does not integrate with GSAP timelines. Assessed
      as premature for 3-item KPI blocks.

      - **IntersectionObserver + requestAnimationFrame** — viable vanilla
      approach with zero deps but requires reimplementing easing, ScrollTrigger
      restart logic, and reduced-motion handling manually. Maintenance burden
      outweighs the bundle saving.

      - **Colocating GSAP import inside `impact-section.astro` directly** —
      violates RFC-0011 (S-1 scripts must live in `src/scripts/components/`, not
      inside `.astro` files). Also prevents deferred loading.
    file: docs/rfcs/archive/implemented/rfc-0040-gsap-motion-library-and-animated-stat-counter.md
  - kind: rejected-alternative
    rfcId: RFC-0041
    title: Inline number animation for prose sections (SSR pre-wrap + GSAP)
    status: implemented
    alternativesText: >-
      - **Client-side text-node walk** — simpler to implement but causes layout
      shift (CLS > 0) as DOM mutations happen after paint. Rejected per RFC-0006
      Lighthouse invariant.

      - **Custom Remark/Rehype plugin** — most elegant Markdown-native approach
      but requires modifying the shared Astro config, which is a cross-workspace
      change requiring its own RFC. The `wrapInlineNumbers` string-based
      approach stays inside the component boundary.

      - **Scaling duration by value** — `1988 / 100 * 0.08s` per digit produces
      visually uneven animation (years animate slowly, small numbers flash).
      Fixed 3 s duration is deliberately uniform.

      - **Animating headings** — numbers in `<h1>`–`<h4>` are part of structural
      text; count-up animation there is disorienting and reduces readability.
      Headings are excluded.
    file: docs/rfcs/archive/implemented/rfc-0041-inline-number-animation-for-prose-sections.md
  - kind: rejected-alternative
    rfcId: RFC-0042
    title: Semantic content from page blocks with explicit NEED-THIS markers —
      remove stub content drift
    status: implemented
    alternativesText: >-
      1. **Keep stubs as optional defaults.** Rejected — creates permanent
      two-source problem. Drift is inevitable; explicit markers are cleaner.


      2. **Auto-promote stub content to page entries.** Rejected — mixes
      migration with architecture. Manual promotion ensures site owners review
      and own their content.


      3. **Schema-level required field validation only.** Rejected — doesn't
      help site owners understand _which_ fields are missing. `NEED_THIS_*`
      markers provide visual feedback.


      4. **Type-level strictness (TypeScript errors for missing props).**
      Rejected — content is runtime data, not compile-time. Need runtime
      validation that produces clear messages for content authors.
    file: docs/rfcs/archive/implemented/rfc-0042-semantic-content-from-pages-with-explicit-markers.md
  - kind: rejected-alternative
    rfcId: RFC-0043
    title: Enforce webp-only image formats with public folder exceptions
    status: implemented
    alternativesText: >-
      - **Extension-only validation**: Rejected because files can have incorrect
      extensions

      - **Allow all formats in assets**: Rejected due to performance and
      consistency concerns

      - **Separate command for public vs assets**: Rejected because single
      command with location-aware logic is simpler

      - **Include packages/\* in scope**: Rejected because packages contain
      shared resources that may need different rules
    file: docs/rfcs/archive/implemented/rfc-0043-enforce-webp-only-image-formats-with-public-exceptions.md
  - kind: rejected-alternative
    rfcId: RFC-0044
    title: Move navigation configuration to content layer
    status: implemented
    alternativesText: >-
      **Alternative 1: Extend header/footer component content only**


      - Keep navigation.ts hardcoded, add more fields to component content files

      - Rejected: This doesn't solve the central problem—targets are still
      defined in engineering code

      - Component-level navLinks are already content-declared, but they
      reference targets that don't exist in content


      **Alternative 2: Use system.yaml for navigation configuration**


      - Add navigation structure to `src/content/assets/system.md`

      - Rejected: system.md is for system-level configuration (biome, feature
      flags, shell blocks), not content structure

      - Navigation is visitor-facing structure, not system configuration


      **Alternative 3: Create a new package @gogol/navigation**


      - Move all navigation logic to a shared package

      - Rejected: Navigation targets are app-specific (different sites have
      different navigation structures)

      - The resolver logic is already in @gogol/share (feature graph), only the
      target definitions need to be app-local


      **Alternative 4: Keep navigation.ts as-is, document it as
      engineering-only**


      - Accept that navigation changes require engineering intervention

      - Rejected: Violates DNA-22 principle that visitor-facing structure should
      be content-declared

      - Creates unnecessary dependency on engineering for routine content
      changes
    file: docs/rfcs/archive/implemented/rfc-0044-move-navigation-configuration-to-content-layer.md
  - kind: rejected-alternative
    rfcId: RFC-0045
    title: Enable content data references in markdown files
    status: implemented
    alternativesText: >-
      1. **Use `@gogol/business/loaders` in components**: Requires TypeScript
      code changes, violating client-editable surface contract (DNA-22).
      Rejected because content editors should not need to edit code.


      2. **YAML anchors/aliases across files**: Not supported by Astro's
      frontmatter parsing. Would require custom YAML loader. Rejected due to
      complexity and lack of native support.


      3. **Runtime data fetching**: Would break static site guarantees and add
      runtime overhead. Rejected to maintain performance and static generation.


      4. **Build-time script to generate prose files**: Would create generated
      files that are hard to maintain and edit. Rejected to keep prose files
      directly editable.
    file: docs/rfcs/archive/implemented/rfc-0045-enable-content-data-references-in-markdown-files.md
  - kind: rejected-alternative
    rfcId: RFC-0046
    title: Move navigation schema to shared package with configurable groups
    status: implemented
    alternativesText: >-
      **Alternative 1: Keep schema app-local (status quo)**


      - Each app maintains its own copy of navigation.ts

      - Rejected: Violates DRY, creates maintenance burden

      - Schema changes require updating every app manually


      **Alternative 2: Hardcode all possible groups in shared schema**


      - Create a comprehensive enum like `["navigation", "legal", "contact",
      "social", "products", "services", "blog", ...]`

      - Rejected: Impossible to predict all future group needs; bloated enum

      - Still doesn't allow truly custom groupings


      **Alternative 3: Remove group field entirely**


      - Remove the `group` field from the schema

      - Rejected: Groups are useful for organizing navigation in UI (header vs
      footer)

      - Would require refactoring header/footer components


      **Alternative 4: Use JSON schema instead of Zod**


      - Define navigation schema in JSON schema format

      - Rejected: Inconsistent with rest of codebase (Zod everywhere)

      - Loses TypeScript type inference benefits
    file: docs/rfcs/archive/implemented/rfc-0046-move-navigation-schema-to-shared-package-with-configurable-groups.md
  - kind: rejected-alternative
    rfcId: RFC-0047
    title: Simplify thin app content surface for CMS-friendly sites
    status: implemented
    alternativesText: >-
      **Keep `src/content/components/**`.** Rejected because clients and CMS
      users should not edit implementation-shaped folders. Component
      implementation lives in `packages/ui`; app content should use semantic
      domains.


      **Create `src/content/media/**`.** Rejected. Displayed media should be
      colocated under `assets/\*\*` near the content domain that owns it so
      Astro can optimize it and localization is explicit.


      **Keep both `system.yaml` and `system.md`.** Rejected. The mirror pair
      created drift risk and manual synchronization overhead.


      **Keep feature graph as an advanced layer.** Rejected for the default
      model. It duplicates page block visibility and makes simple sites harder
      to maintain. A future RFC may reintroduce advanced
      experimentation/segmentation through Growth-specific contracts if needed.


      **Keep cosmic names in page markdown.** Rejected for CMS-facing content.
      Cosmic names remain valuable internally but are not suitable for client
      editing.


      **Move all app scripts and styles into packages.** Rejected.
      `layout-orchestrator.ts` and `src/styles/**` are deliberate thin app
      extension points for performance and brand identity.
    file: docs/rfcs/archive/implemented/rfc-0047-simplify-thin-app-content-surface-for-cms-friendly-sites.md
  - kind: rejected-alternative
    rfcId: RFC-0048
    title: Support localized page slugs and route resolution
    status: implemented
    alternativesText: >-
      **Keep per-language `navigation.md routeSlug` as canonical.** Rejected.
      Navigation should own labels, order, and grouping, not the canonical URL
      table used by routes, breadcrumbs, semantic outputs, and language
      switching.


      **Use page file names as the only route source.** Rejected. File names are
      convenient but cannot reliably pair localized siblings when slugs differ,
      and they make fallback route generation ambiguous.


      **Put localized slugs only in page frontmatter.** Rejected as the durable
      source because missing localized page entries would have no localized slug
      for RFC-0008 fallback paths. Page frontmatter may mirror or validate
      identity, but `system.md` owns the complete page route table.


      **Use `cosmicStar` as the cross-language identity.** Rejected. Cosmic
      names are internal platform identifiers and should not become the
      author-facing or navigation-facing page identity.
    file: docs/rfcs/archive/implemented/rfc-0048-support-localized-page-slugs-and-route-resolution.md
  - kind: rejected-alternative
    rfcId: RFC-0049
    title: Generate hreflang sitemap from localized route registry
    status: implemented
    alternativesText: >-
      **Use `@astrojs/sitemap`.** Rejected. The integration discovers URLs from
      generated static output after Astro's build completes. It cannot read the
      route registry and therefore cannot pair `/de/datenschutz` with
      `/en/privacy` when slugs differ by language. Using the integration
      alongside a custom endpoint would produce conflicting output files.


      **Generate alternates post-hoc by slug matching.** Rejected. Matching
      `/de/datenschutz` to its English sibling by guessing that the path
      structure is analogous (`/en/<slug>`) fails when slugs differ. The route
      registry already supplies the pairing; using it is strictly more reliable.


      **Put sitemap logic inside the app.** Rejected. Generation logic in
      `@gogol/share` is usable by all apps without duplication. The command
      remains in `site-kernel-checks` as a thin delivery point, consistent with
      how `open-source.generate` works.


      **Sitemap index with per-language sitemaps.** Rejected for now. The
      current split is by page category (`content` vs `legal`), not by language.
      Per-language files would duplicate the hreflang cluster structure (each
      language file would still need to reference all language alternates),
      making the index larger without clear SEO benefit at the current site
      scale.


      **Include `changefreq` and `priority`.** Rejected. Google's documentation
      explicitly states these fields are ignored by its crawlers. Emitting them
      adds noise without benefit.


      **Derive `<lastmod>` from filesystem mtime.** Rejected. Git does not track
      file timestamps as versioned data. During clone or checkout, files receive
      timestamps tied to the extraction moment, not the actual last content
      change. On CI platforms (including GitHub Actions → Cloudflare Pages),
      `fs.stat.mtime` in the build environment cannot be treated as the true
      content modification date of a page. This would produce misleading
      `lastmod` values that change on every build even when content is
      unchanged. The only reliable source of truth is the content author, who
      updates `system.md pages[].sitemap.lastmod` when a page is meaningfully
      changed.


      **Add `hreflang` only in `<head>`, skip sitemap.** Rejected. Google
      recommends providing hreflang signals in both `<head>` and sitemap. A
      sitemap also makes the full URL inventory machine-readable for other
      tools. Both are needed.
    file: docs/rfcs/archive/implemented/rfc-0049-generate-hreflang-sitemap-from-route-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0050
    title: Generate static llms.txt and llms-full.txt during build
    status: implemented
    alternativesText: >-
      **Keep dynamic API routes and add `prerender = true`.** Rejected. Astro
      would generate the files into `dist/` during build, but they would not
      exist in `public/` and could not be pre-validated by a site-kernel
      command. It also does not solve the problem of making the generation logic
      reusable across apps or runnable without Astro server context.


      **Generate llms.txt via an Astro integration hook (`astro:build:done`).**
      Rejected. Integration hooks run _after_ the Astro build, making it
      impossible to validate the files before deployment. It also ties the
      generation to Astro internals instead of the site-kernel command model
      that RFC-0049 established.


      **Duplicate semantic model construction inside `site-kernel-checks`.**
      Rejected. The parsing logic for pages, prose, site labels, and business
      data already exists in `@gogol/business`. Extracting it into
      `@gogol/site-kernel-content` as a framework-agnostic loader avoids
      duplication and keeps the command thin.


      **Generate only `llms.txt` and drop `llms-full.txt`.** Rejected. Both
      formats serve different consumers: `llms.txt` is the compact index
      required by the `llms.txt` standard, while `llms-full.txt` provides the
      complete semantic content for deeper model context windows.
    file: docs/rfcs/archive/implemented/rfc-0050-generate-static-llms-txt-and-llms-full-txt-during-build.md
  - kind: rejected-alternative
    rfcId: RFC-0051
    title: Generate static ai.txt during build with CMS-declared AI policy
    status: implemented
    alternativesText: >-
      **Keep no `ai.txt`.** Rejected. As AI crawlers proliferate, having no
      machine-readable policy endpoint leaves the site without a standardized
      way to communicate preferences. The file is trivially cheap to generate
      and carries no operational cost.


      **Use a separate `ai.yaml` file instead of embedding in `system.md`.**
      Rejected. The `system.md` file is already the single canonical manifest
      for app-level configuration. Splitting AI policy into a separate file
      would create a parallel configuration source that must be kept in sync.
      The `ai:` block is optional and self-contained; adding it to `system.md`
      is consistent with how `growth:`, `release:`, and `identity:` are already
      handled.


      **Generate `ai.txt` via an Astro API route.** Rejected. The static
      generate pattern (RFC-0049, RFC-0050) is now the established approach for
      well-known text files. An API route would need post-build static
      generation to work with `output: 'static'`, defeating the purpose.


      **Use `robots.txt` comments for AI instructions.** Rejected. While some
      providers (e.g., OpenAI) read `robots.txt` for opt-out, the format does
      not support the richer policy metadata needed — licensing, attribution,
      provider-specific overrides, and usage-type granularity.


      **Adopt a specific vendor's `ai.txt` format.** Rejected. No single format
      is yet standard. The format proposed here (INI-like key-value with
      `[provider]` sections) is the most commonly emerging pattern and is
      trivially parseable by both humans and machines. It is designed as version
      `0.1` — the format can evolve as community standards solidify.


      **Skip validation; let CI errors surface from downstream tools.**
      Rejected. Without validation, a malformed `ai.txt` would go undetected
      until an AI provider reports problems or a manual audit catches them. The
      validation command provides a CI gate that catches structural issues
      before deployment.
    file: docs/rfcs/archive/implemented/rfc-0051-generate-static-ai-txt-during-build-with-cms-policy.md
  - kind: rejected-alternative
    rfcId: RFC-0052
    title: Generate content-managed robots.txt during build with CMS policy
    status: implemented
    alternativesText: >-
      **Keep the manual `public/robots.txt`.** Rejected. The manual file is
      invisible to validation, not content-managed, and must be maintained per
      app. Every other static discovery file (sitemap, llms, ai.txt) is
      generated. Robots.txt should follow the same pattern for consistency.


      **Use an Astro API route (`src/pages/robots.txt.ts`).** Rejected. The
      existing pattern for all static discovery files is generation during
      `build.prepare` into `public/`. API routes add request-time overhead and
      are inconsistent with the sitemap/llms/ai.txt approach.


      **Embed robots.txt logic in the existing sitemap command.** Rejected.
      Robots.txt is a separate concern (crawl policy vs. URL inventory).
      Combining them violates separation of concerns and makes validation
      harder.


      **Support inline raw robots.txt in system.md as a single string.**
      Rejected. A structured `robots:` block is content-managed and can be
      validated, defaulted, and composed programmatically. A raw string would
      bypass validation and defeat the purpose of content management.


      **Add `@astrojs/robots-txt` integration.** Rejected. The integration
      framework does not exist in Astro core and adding a dependency for a
      single file that can be generated in 50 lines of code is unnecessary
      weight.
    file: docs/rfcs/archive/implemented/rfc-0052-generate-content-managed-robots-txt-during-build.md
  - kind: rejected-alternative
    rfcId: RFC-0053
    title: Image resolution contract with language fallback for all apps
    status: implemented
    alternativesText: |-
      **Alternative 1: Keep full paths in content**

      - Rejected: Content authors should not know internal path structure
      - Rejected: Hardcodes language and extension, prevents fallback

      **Alternative 2: Each section has own resolution logic**

      - Rejected: Violates DRY, creates maintenance burden
      - Rejected: Inconsistent behavior across sections

      **Alternative 3: Build-time image processing pipeline**

      - Rejected: Overkill for current needs
      - Rejected: Would require significant infrastructure changes
    file: docs/rfcs/archive/implemented/rfc-0053-image-resolution-contract-with-language-fallback.md
  - kind: rejected-alternative
    rfcId: RFC-0054
    title: Adopt pageId-based content file naming for locale-independent content
      resolution
    status: implemented
    alternativesText: >-
      **Keep the current approach.** Content files continue using route slugs.
      This is the simplest option but keeps the coupling between route slugs and
      file names indefinitely.


      **Use a `contentFile` field in system.md.** Allow pages to declare an
      explicit content file path decoupled from both pageId and route slug. This
      adds schema complexity without clear benefit over the pageId convention.


      **Use numeric IDs.** Replace route slugs with opaque numeric IDs in
      filenames. This loses human readability and makes debugging harder.
    file: docs/rfcs/archive/implemented/rfc-0054-pageid-based-content-file-naming.md
  - kind: rejected-alternative
    rfcId: RFC-0055
    title: Eliminate hardcoded language lists from middleware via build-time i18n
      constants
    status: implemented
    alternativesText: >-
      1. **`loadI18nConfigSync` at module scope** (initial RFC-0055 proposal):
      Rejected after user review. Keeps an additional package dependency
      (`@gogol/site-kernel-content`) in the app middleware file, violating the
      thin-app principle. The CLI codegen approach removes this dependency
      entirely.


      2. **Codegen approach** (`generateLanguageDetectionMiddleware` from
      RFC-0038 Wave 4): Already exists in `@gogol/site-kernel-content` but
      generates entire middleware files with custom logic. Rejected because it
      replaces the lightweight factory pattern with a heavier codegen step and
      creates generated files that are harder to reason about.


      3. **Async `getCollection("system")` in middleware**: Rejected. Content
      Collections require full Astro runtime context. Middleware runs before
      Astro's content layer is initialized, making `getCollection` unreliable or
      unavailable.


      4. **Vite virtual module / `define` constant injection**: Would inject
      `__SUPPORTED_LANGS__` at build time via `astro.config.mjs`. Rejected as
      over-engineered — no new infrastructure needed with the CLI pattern.


      5. **Keep hardcoded values**: Rejected. Violates DNA-22 and the RFC-0038
      contract. The manual sync burden is a known source of drift.
    file: docs/rfcs/archive/implemented/rfc-0055-eliminate-hardcoded-language-lists-from-middleware-via-build-time-i18n-constants.md
  - kind: superseded-rfc
    rfcId: RFC-0070
    title: "Define onboarding lifecycle: .input/.output folders, 00-brief contract,
      phase semantics"
    status: superseded
    closedAt: 2026-05-18
    decisionSummary: "The repository establishes a single, named onboarding surface
      at the workspace root:"
    file: docs/rfcs/archive/implemented/rfc-0070-onboarding-lifecycle-input-output-and-brief-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0070
    title: "Define onboarding lifecycle: .input/.output folders, 00-brief contract,
      phase semantics"
    status: superseded
    alternativesText: >-
      - **Keep one `onboarding/<client>/` per client in repo.** Rejected —
      accumulates dead client folders; conflicts with the 1-3-active-apps
      invariant.

      - **Inline the brief inside `system.md identity`.** Rejected — `system.md`
      is the _built_ manifest. The brief is the _input_ and must exist before
      `system.md` is generated.

      - **Make biome/family/constellation choices part of the brief.** Rejected
      — those are ecosystem-internal decisions the agent should propose from
      materials; they are not the human-side bargain.

      - **Track phase status in `.output/status.json`.** Rejected — markdown is
      the right shape for Windsurf review; the validators read kernel-command
      output, not the status file.
    file: docs/rfcs/archive/implemented/rfc-0070-onboarding-lifecycle-input-output-and-brief-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0071
    title: Extend biome schema, introduce site-family catalog, add deterministic
      token derivation
    status: implemented
    alternativesText: >-
      - **Keep schema narrow, allow `tokens-override.css` again.** Rejected —
      overrides fragment the design system and prevent biome reuse.

      - **Derive palette via LLM.** Rejected — LLM is fine for axis
      classification but not for numeric values. OKLCH math is reproducible;
      LLM-picked colors are not.

      - **No site families, just biomes.** Rejected — the family layer is what
      makes the next Handwerk client cheaper than the first.
    file: docs/rfcs/archive/implemented/rfc-0071-biome-synthesis-and-site-families.md
  - kind: rejected-alternative
    rfcId: RFC-0072
    title: Introduce section-archetype catalog, system.md compiler, and section
      scaffolding contract
    status: implemented
    alternativesText: >-
      - **Embed archetypes inside manifests.** Rejected — the same archetype is
      shared across many sections; centralizing keeps `propsSchema` and
      constraints DRY.

      - **LLM-based matching as a kernel command.** Rejected — matching is the
      agent's prompt-time decision; the kernel provides the catalog and the
      picker.

      - **Hand-author `system.md`.** Rejected — the file is a pure projection;
      hand-authoring is busywork and a drift surface.

      - **Allow array `cosmicName` in constellation slots.** Rejected — DNA-23
      invariant. Two slots in sequence with `optional: true` are the way.
    file: docs/rfcs/archive/implemented/rfc-0072-archetype-catalog-and-section-library-growth.md
  - kind: rejected-alternative
    rfcId: RFC-0073
    title: "Establish content discipline validators: coverage, voice, business,
      references"
    status: implemented
    alternativesText: >-
      - **Embed atomization logic in TypeScript.** Rejected — atomization is LLM
      judgment; gating that in code adds ceremony without value.

      - **Skip the coverage gate.** Rejected — sentences silently dropping is
      the single most expensive QA failure today.

      - **Use one mega-validator `content.validate`.** Rejected — single-purpose
      commands match the ecosystem convention and surface findings cleanly.
    file: docs/rfcs/archive/implemented/rfc-0073-content-discipline-validators.md
  - kind: rejected-alternative
    rfcId: RFC-0074
    title: Split material audits into deterministic kernel validators and cached LLM
      audits
    status: implemented
    alternativesText: >-
      - **One mega audit command.** Rejected — different audits have different
      runtimes and concerns; focused local runs matter.

      - **External SaaS audit service.** Rejected — audits must run in CI
      without external paid dependencies and must be reproducible from source.

      - **No LLM caching.** Rejected — cost-prohibitive and non-deterministic.

      - **Embed LLM rules in TypeScript.** Rejected — rules live in family YAMLs
      so editing them does not require a package version bump.
    file: docs/rfcs/archive/implemented/rfc-0074-deterministic-and-llm-audit-validators.md
  - kind: rejected-alternative
    rfcId: RFC-0075
    title: Rename STANDARD_CHECK_PIPELINE to APPS_CHECK_PIPELINE, add
      PACKAGES_CHECK_PIPELINE, rewrite .agents/workflows/ for self-orchestration
    status: implemented
    alternativesText: >-
      - **Keep the pipeline name, just add a separate package pipeline under a
      new symbol.** Rejected — the name lied; fixing it is cheap.

      - **Drive everything through `app.contract.full` and skip the new
      `apps-check.run`.** Rejected — `app.contract.full` is the _full readiness_
      gate (includes the heavy `app.qa.validate`); developers and agents need a
      faster everyday loop.

      - **Use a YAML-only workflow format.** Rejected — markdown frontmatter +
      body matches the rest of the doc surface; agents read prose.

      - **Build a workflow engine with retries and state machines.** Rejected at
      this scope — sequences are linear; status.md is enough state.
    file: docs/rfcs/archive/implemented/rfc-0075-pipelines-rename-and-workflow-self-orchestration.md
  - kind: superseded-rfc
    rfcId: RFC-0076
    title: Formalize onboarding input and phase output contracts
    status: superseded
    closedAt: 2026-05-18
    decisionSummary: "The kernel gains two app-scoped commands:"
    file: docs/rfcs/archive/implemented/rfc-0076-formalize-onboarding-input-and-phase-output-contracts.md
  - kind: rejected-alternative
    rfcId: RFC-0076
    title: Formalize onboarding input and phase output contracts
    status: superseded
    alternativesText: >-
      - **Keep missing-output warnings in each validator.** Rejected because
      this spreads phase policy across unrelated commands and lets audit phase
      pass with incomplete upstream work.

      - **Store phase state in a database.** Rejected because the repository
      already uses source-controlled artifacts and should remain reproducible
      from files.

      - **Make validators read raw `onboarding/.input` directly.** Rejected
      because RFC-0074 intentionally audits assembled apps and distilled
      outputs, not raw research bundles.
    file: docs/rfcs/archive/implemented/rfc-0076-formalize-onboarding-input-and-phase-output-contracts.md
  - kind: rejected-alternative
    rfcId: RFC-0077
    title: Remove legacy app compatibility surfaces from CMS-friendly onboarding
    status: implemented
    alternativesText: >-
      - **Keep compatibility warnings for one release.** Rejected because the
      project explicitly does not need backward compatibility for legacy app
      structures.

      - **Leave legacy validators as no-ops.** Rejected because no-op validators
      mislead agents and humans into thinking a contract is still enforced.

      - **Support both `system.yaml` and `system.md`.** Rejected because two
      manifest sources create ambiguity and drift.
    file: docs/rfcs/archive/implemented/rfc-0077-remove-legacy-app-compatibility-surfaces.md
  - kind: superseded-rfc
    rfcId: RFC-0078
    title: Generate engineering boilerplate in apps for radically thin client sites
    status: superseded
    closedAt: 2026-05-21
    decisionSummary: "The site OS gains a set of generation commands that derive
      engineering artifacts from the canonical manifest
      (`src/content/system.md`) plus a small per-app configuration surface. The
      commands follow the pattern established by RFC-0049: write artifacts into
      the project tree, not into `dist/`."
    file: docs/rfcs/archive/superseded/rfc-0078-generate-engineering-boilerplate-in-apps-for-radically-thin-client-sites.md
  - kind: rejected-alternative
    rfcId: RFC-0078
    title: Generate engineering boilerplate in apps for radically thin client sites
    status: superseded
    alternativesText: >-
      **Auto-discovery (instead of `kernel.wire`).** The kernel could detect
      installed packages at runtime and auto-register their commands without any
      `tools/` files. This was rejected because: (a) it makes the module
      registration invisible — a developer cannot see which commands are
      available without running the kernel; (b) it couples package installation
      to CLI availability, which is fragile; (c) the `tools/` directory
      documents the app's OS surface explicitly and is useful for debugging.


      **Hand-rolled generation per-file (instead of `routes.generate` monolithic
      command).** Each entrypoint could have its own command
      (`pages.index.generate`, `pages.catchall.generate`,
      `content.config.generate`, etc.). This was rejected because it
      over-fragments the CLI surface. All five thin entrypoints are generated
      together, regenerate as a unit, and are almost never regenerated
      independently.


      **Gitignore all generated files (including `tools/`).** This was rejected
      because generated engineering files should be auditable in code review.
      Only truly transient artifacts (biome.generated.css, language-redirect.ts)
      remain gitignored.


      **Moving `astro.config.mjs` into a shared package.** This was rejected
      because Vite aliases and integrations are inherently app-specific.
      Template regeneration via `config.regenerate` is sufficient.
    file: docs/rfcs/archive/superseded/rfc-0078-generate-engineering-boilerplate-in-apps-for-radically-thin-client-sites.md
  - kind: superseded-rfc
    rfcId: RFC-0079
    title: Generate AGENTS.md files for every app from templates
    status: superseded
    closedAt: 2026-05-21
    decisionSummary: "The site OS gains an `agents.generate` command in
      `@gogol/site-kernel-codegen` that generates `apps/<app>/AGENTS.md`,
      `apps/<app>/src/content/AGENTS.md`, and `apps/<app>/src/styles/AGENTS.md`
      from templates stored under
      `packages/os/site-kernel-codegen/src/templates/app-boilerplate/` alongside
      the other RFC-0078 boilerplate templates. The command follows the RFC-0078
      generation-first discipline:"
    file: docs/rfcs/archive/superseded/rfc-0079-generate-agents-md-files-for-every-app.md
  - kind: rejected-alternative
    rfcId: RFC-0079
    title: Generate AGENTS.md files for every app from templates
    status: superseded
    alternativesText: >-
      **Keep AGENTS.md hand-authored, enforce via review.** This was rejected
      because it reproduces the linear-scaling maintenance problem RFC-0078
      solved for engineering boilerplate. Every shared pattern change requires
      manual propagation to all app-level `AGENTS.md` files.


      **Merge all app guidance into apps/AGENTS.md (no app-level file).** This
      was rejected because app-level `AGENTS.md` carries biome-specific page
      composition patterns, generated-file contracts, and routing rules that
      differ meaningfully per site. Putting all of that in the shared file would
      make it unwieldy and force agents to filter out irrelevant guidance on
      every change.


      **Generate only a minimal stub, leave the rest hand-authored.** This was
      rejected because it still requires manual maintenance of the majority of
      the file. A generated stub that is immediately extended by hand loses the
      regenerability property.


      **One template per biome (instead of one universal template with
      tokens).** This was considered as an extension. In the first iteration, a
      single universal template with `{{BIOME_DISPLAY_NAME}}` and `{{BIOME_ID}}`
      tokens covers all biomes. Per-biome template variants may be added as
      follow-up if the canonical page composition diverges sufficiently between
      biomes.


      **Separate `templates/agents/` directory (instead of co-locating with
      `app-boilerplate`).** An isolated `templates/agents/` directory was
      created initially but moved into `templates/app-boilerplate/` to maintain
      a single consistent hierarchy that mirrors the target app path. This
      matches the established convention for all other RFC-0078 boilerplate
      templates and eliminates the need for a second `readTemplate` helper.
    file: docs/rfcs/archive/superseded/rfc-0079-generate-agents-md-files-for-every-app.md
  - kind: rejected-alternative
    rfcId: RFC-0080
    title: Adopt template suffix before extension for template files
    status: implemented
    alternativesText: >-
      1. **Keep `.X.template` and add IDE configuration to treat these as their
      underlying types.** Rejected — requires per-IDE configuration that would
      need to be maintained across all developer environments. The `.template.X`
      pattern works automatically in all modern IDEs without configuration.


      2. **Use a separate `templates/` directory outside `src/` to avoid
      tsconfig issues.** Rejected — violates RFC-0078's co-location principle.
      Templates belong with the code that reads them.


      3. **Prefix instead of suffix: `template.AGENTS.md`.** Rejected — breaks
      alphabetical sorting and makes it harder to see the file type at a glance.
      The suffix position preserves the "real" extension at the end where IDEs
      expect it.
    file: docs/rfcs/archive/implemented/rfc-0080-adopt-template-suffix-before-extension-for-template-files.md
  - kind: rejected-alternative
    rfcId: RFC-0081
    title: Unified generated-file governance protocol with project-specific override
      marker
    status: implemented
    alternativesText: >-
      **Gitignore all generated files.** Rejected — this was the actual negative
      experience that triggered this RFC. Loss of local regression control and
      PR visibility is unacceptable.


      **Hash-based content comparison.** Instead of a marker, compare generated
      output byte-for-byte with the expected output and skip on match. Rejected
      — this requires running the generator every time just to compare, provides
      no feedback loop for intentional customisation, and cannot distinguish
      "customised by project" from "regenerated from newer template."


      **Hash-based marker (e.g. `GENERATED // sha256:<hash>`).** Rejected — the
      marker should be human-readable and project-customisation should be a
      deliberate act (remove the line), not an accidental hash collision.


      **Opt-in marker (file opts into generation instead of opting out).**
      Rejected — the generation-first discipline is already established. The
      default should be "generated" with "project-customised" being the explicit
      opt-out.


      **Separate registry file (e.g. `generated-files.json` listing which files
      are managed).** Rejected — creates a synchronisation problem between the
      registry and the filesystem. The marker should be self-describing in each
      file.
    file: docs/rfcs/archive/implemented/rfc-0081-generated-file-governance-protocol.md
  - kind: rejected-alternative
    rfcId: RFC-0082
    title: Shared multi-document YAML helper for onboarding artifacts
    status: implemented
    alternativesText: >-
      - **Forbid the two-doc shape and require single-doc with passthrough on
      every schema.** Rejected because the strict schemas are valuable for
      catching typos in author-supplied data; relaxing every schema permanently
      is a quality loss.

      - **Move RFC-0076 metadata to a sidecar `.meta.yaml`.** Rejected because
      it doubles the file count, makes `git diff` noisier, and breaks the "one
      artifact, one path" mental model the workflows are built on.
    file: docs/rfcs/archive/implemented/rfc-0082-shared-multi-document-yaml-helper-for-onboarding-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0083
    title: Cross-catalog cosmic-name validation and rename helper
    status: implemented
    alternativesText: >-
      - **Auto-pick during section.scaffold.** `pickCosmicName` already does
      this, but cannot help when `acceptedCosmicNames` has a single entry
      already taken.

      - **Open the catalogs.** Rejected; DNA-19 closes them deliberately.
    file: docs/rfcs/archive/implemented/rfc-0083-cross-catalog-cosmic-name-validation-and-rename-helper.md
  - kind: rejected-alternative
    rfcId: RFC-0084
    title: Open archetype-derived semantic role catalog
    status: implemented
    alternativesText: >-
      - **Keep the enum and auto-generate it from archetype YAMLs at build
      time.** Half-measure: still requires a build step before manifests
      validate, and the resulting type is a static union that breaks when a new
      archetype lands without a rebuild.

      - **Move all role declaration to the manifest, ignoring archetype's
      `semanticRole`.** Worse — it deletes the contract that ties manifests to
      archetypes.
    file: docs/rfcs/archive/implemented/rfc-0084-open-archetype-derived-semantic-role-catalog.md
  - kind: rejected-alternative
    rfcId: RFC-0085
    title: Split onboarding gates into content-author and post-build phases
    status: implemented
    alternativesText: >-
      - **Add a `--skip-postbuild` flag to `apps-check.run`.** Half-measure:
      agents still have to know which flag to pass per phase, and the noise
      persists when the flag is forgotten.

      - **Auto-build inside the kernel command.** Rejected: builds are long,
      expensive, and side-effecting; the kernel should not start one without an
      explicit human / workflow request.
    file: docs/rfcs/archive/implemented/rfc-0085-split-onboarding-gates-into-content-author-and-post-build-phases.md
  - kind: rejected-alternative
    rfcId: RFC-0086
    title: Print kernel command diagnostics in text output mode
    status: implemented
    alternativesText: >-
      - **Force every command to write diagnostics to stderr.** Cleaner stream
      separation, but breaks the existing `[ERROR]` line-format consumers and is
      a larger surface change.

      - **Add a `--verbose` flag.** Half-measure: agents and humans both want
      the diagnostics by default on failure; making them opt-in misses the
      value.
    file: docs/rfcs/archive/implemented/rfc-0086-print-kernel-command-diagnostics-in-text-output-mode.md
  - kind: rejected-alternative
    rfcId: RFC-0087
    title: Content-driven generation contract
    status: implemented
    alternativesText: >-
      - **Forbid hand-editing of generated files entirely.** Too restrictive —
      an early-stage app sometimes needs a one-off tweak before the system.md
      schema catches up. The GENERATED marker already gives us soft enforcement
      (re-run overwrites unless marker is removed).

      - **Make every generated file readable from a JSON manifest in
      `.cache/`.** Adds an indirection layer that doesn't solve the drift —
      agents and humans still read the materialized file, not the manifest.
    file: docs/rfcs/archive/implemented/rfc-0087-content-driven-generation-contract-single-owner-idempotent-system-md-sourced.md
  - kind: rejected-alternative
    rfcId: RFC-0088
    title: Word-boundary matching for content.voice.lint forbidden phrases
    status: implemented
    alternativesText: >-
      - **Skip generated files (with `# GENERATED` marker) from voice lint.**
      Half-measure: hides legitimate voice violations in files like `system.md`
      that happen to be generated.

      - **Allow per-app `forbiddenPhrases` to opt out of substring matching with
      a flag.** Adds configuration surface for what should be a workspace-wide
      default.
    file: docs/rfcs/archive/implemented/rfc-0088-word-boundary-matching-for-content-voice-lint-forbidden-phrases.md
  - kind: rejected-alternative
    rfcId: RFC-0089
    title: Astro subpath exports must include .astro extension
    status: implemented
    alternativesText: >-
      - **Drop the `.astro` extension from the consumer import.** Would force
      re-scaffolding every app and is contrary to the Astro community convention
      of suffixing component imports.

      - **Use Astro's own resolver in static builds.** Not under our control;
      Vite is the build engine.
    file: docs/rfcs/archive/implemented/rfc-0089-astro-subpath-exports-must-include-astro-extension.md
  - kind: rejected-alternative
    rfcId: RFC-0090
    title: Page file names must derive from pageId via pageIdToContentFileSlug
    status: implemented
    alternativesText: >-
      - **Make `resolvePageRoute` accept either the pageId-slug OR the
      route-slug filename.** Adds runtime ambiguity (which to prefer if both
      exist?) and hides the cause of subtle bugs.

      - **Move the home file to `src/pages/index.astro`-style routing.**
      Conflicts with the i18n + system.md page composition model.
    file: docs/rfcs/archive/implemented/rfc-0090-page-file-names-must-derive-from-pageid-via-pageidtocontentfileslug.md
  - kind: rejected-alternative
    rfcId: RFC-0091
    title: Derive PLANET_IMPORT_PATHS and BLOCK_TYPE_TO_COSMIC_NAME from archetype
      catalog
    status: implemented
    alternativesText: >-
      - **Keep the tables in `page.ts` but require `section.scaffold` to write
      them.** Adds write surface to a previously-read-only file; brittle.

      - **Make the resolver scan `packages/ui/src/sections/` at runtime.** Adds
      I/O on the page-render hot path; not Astro-compatible (build needs static
      imports).
    file: docs/rfcs/archive/implemented/rfc-0091-derive-planet-import-paths-and-block-type-to-cosmic-name-from-archetype-catalog.md
  - kind: rejected-alternative
    rfcId: RFC-0092
    title: Relative imports in source-consumed packages must use the .ts extension
    status: implemented
    alternativesText: >-
      - **Universal `.js` + Vite `extensionAlias` in every app.** Tested in this
      session; `extensionAlias: { ".js": [".ts", ".tsx", ".js"] }` did NOT fix
      Astro dev — the error trace is `node:internal/modules/esm/resolve`, which
      is Node's native loader and not under Vite's reach.

      - **Build every package to `dist/` first.** Doubles type-check work,
      breaks the source-consumed decision, slows the dev loop.

      - **Per-package `allowImportingTsExtensions: true` overrides.** Already
      proved insufficient — share had the flag, but `@gogol/site-kernel-content`
      consumes share's source and didn't inherit it. The shared base is the only
      place that fixes this once and for all.
    file: docs/rfcs/archive/implemented/rfc-0092-relative-imports-in-source-consumed-packages-must-use-ts-extension.md
  - kind: rejected-alternative
    rfcId: RFC-0093
    title: section.scaffold must emit a content-aware starter
    status: implemented
    alternativesText: >-
      - **Render bespoke markup per archetype from the YAML manifest.**
      Generating component HTML from a YAML schema is brittle and forces the
      manifest to know layout details. The starter handles the 80% case; authors
      keep full control of the remaining 20%.

      - **Block-list `JSON.stringify(pageOverride)` in code review only.**
      Already proven insufficient — the May 2026 onboarding shipped 9 stubs and
      `apps-check.run` was fully green.

      - **Make the scaffold prompt for fields interactively.** Adds friction to
      the workflow without solving the "still ships a stub" failure mode.
    file: docs/rfcs/archive/implemented/rfc-0093-section-scaffold-must-emit-content-aware-starter-not-json-dump.md
  - kind: rejected-alternative
    rfcId: RFC-0094
    title: Shared @gogol/ui components must not carry app-specific default pageIds
    status: implemented
    alternativesText: >-
      - **Add an app-config registry that maps shared components to per-app
      defaults.** Adds a registry layer and indirection. Per-block /
      per-`identity` props already give every app a per-component override
      surface.

      - **Detect the pattern with a lint that flags `…Target = "…"` literals.**
      Possible, but the surface is small (header + final-cta were the only
      offenders found). A lint costs more than the value once the offenders are
      gone. RFC-0094 is a contract change supported by the existing review
      process.

      - **Keep the defaults and rename them to a generic value (`"contact"`).**
      Trades one app-specific default for another. Nicaragua's page is
      `donateContact`, warpgogol's is `contact`; any choice is wrong for some
      app.
    file: docs/rfcs/archive/implemented/rfc-0094-shared-ui-components-must-not-carry-app-specific-default-page-ids.md
  - kind: rejected-alternative
    rfcId: RFC-0095
    title: Shared site primitives in scaffold + need-marker and footer-legal guards
    status: implemented
    alternativesText: >-
      - **Move `.btn` / `.container` into `@gogol/ui` styles imported by each
      component.** Adds a hidden import-order dependency and breaks the existing
      `@layer base` ownership. Keeping primitives in the app's `global.css`
      matches RFC-0071's layer cascade exactly.

      - **Make `need()` env-aware (return empty string in production).** Hides
      the issue silently — author never learns which field was missing. The
      post-build validator surfaces it loudly and points at the exact field.

      - **Block long `brandTagline` as a hard error.** Too strict — apps may
      legitimately want a short tagline that's a few chars over 40. Soft hint
      preserves author agency while flagging the trade-off.

      - **Hardcode Impressum + Datenschutz prose into shared components.**
      Couples legal text to runtime code; operators can't review or edit
      per-site. RFC-0096's scaffold approach keeps the text in `src/content/`
      where it belongs.
    file: docs/rfcs/archive/implemented/rfc-0095-shared-site-primitives-in-scaffold-and-need-marker-and-footer-legal-guards.md
  - kind: rejected-alternative
    rfcId: RFC-0096
    title: legal.scaffold — generate Impressum and Datenschutz page stubs for
      DE/AT/CH locales
    status: implemented
    alternativesText: >-
      - **Per-locale Markdown snippets in `@gogol/share` consumed by a shared
      layout.** Couples the legal text to the runtime layer — operators can't
      edit per-app without forking share. Scaffold + GENERATED_MARKER keeps text
      editable.

      - **Generate at build time, not scaffold time.** Hides the legal text from
      the operator. Authors should see the actual prose in `src/content/` so
      they can review it before launch.

      - **Use external services (e.g. iubenda).** Adds a third-party dependency
      for content most apps need only the minimum-compliance version of.
    file: docs/rfcs/archive/implemented/rfc-0096-legal-scaffold-for-impressum-and-datenschutz-stubs.md
  - kind: rejected-alternative
    rfcId: RFC-0097
    title: Locale-asymmetric pages (per-page locale opt-in)
    status: implemented
    alternativesText: >-
      - **Auto-create UK locale stubs for asymmetric pages.** Generates broken
      machine-translated content; trains users to ignore the locale switcher.
      Explicit `locales: [de]` is honest.

      - **Per-app override of the route warning policy.** Hides the signal
      globally; still drowns real bugs in the noise.

      - **Infer `locales` from `routes` keys.** Tempting, but conflates "I
      haven't authored this yet" with "this page is intentionally DE-only". The
      explicit field captures intent.
    file: docs/rfcs/archive/implemented/rfc-0097-locale-asymmetric-pages-per-page-locale-opt-in.md
  - kind: rejected-alternative
    rfcId: RFC-0098
    title: Biome-scoped shadows and gradients
    status: implemented
    alternativesText: >-
      - **Strip nicaragua-specific gradients from studio tokens entirely.**
      Would force every biome to declare them or accept blank gradients.
      Migration risk for any consumer not yet covered. Optional + fallback keeps
      the move safe.

      - **Add `gradients` as a free-form `Record<string, string>` in biome.**
      Loses type safety; promotes biome-key drift across files. Closed enum is
      friendlier.

      - **Use CSS `light-dark()` instead of biome scope.** Solves a different
      problem (dark mode within one biome), not cross-biome divergence.
    file: docs/rfcs/archive/implemented/rfc-0098-biome-scoped-shadows-and-gradients-and-deferred-biome-extends.md
  - kind: rejected-alternative
    rfcId: RFC-0099
    title: Page-driven shared component context fallback without legacy
      componentContent
    status: implemented
    alternativesText: >-
      - **Keep `componentContent` for shell components only.** Rejected because
      it preserves a second content system and keeps agent behavior ambiguous.

      - **Make fallback implicit without `system.md` metadata.** Rejected
      because route/file ordering is not a stable architectural contract.

      - **Support a temporary dual mode during migration.** Rejected because the
      user explicitly wants no backward compatibility and the repository already
      trends toward removing legacy compatibility surfaces.

      - **Push shared defaults into `@gogol/ui` components.** Rejected because
      it violates `RFC-0094` and hides app-specific decisions in package source.
    file: docs/rfcs/archive/implemented/rfc-0099-page-driven-shared-component-context-fallback-without-legacy-componentcontent.md
  - kind: superseded-rfc
    rfcId: RFC-0100
    title: Standardize section content contracts and icon-capable list items without
      legacy
    status: superseded
    closedAt: 2026-05-27
    decisionSummary: "The shared UI platform adopts **DNA-38: Standardized authored
      section-content contracts** for reusable content structures, beginning
      with list-based sections."
    file: docs/rfcs/archive/superseded/rfc-0100-standardize-section-content-contracts-and-icon-capable-list-items-without-legacy.md
  - kind: rejected-alternative
    rfcId: RFC-0100
    title: Standardize section content contracts and icon-capable list items without
      legacy
    status: superseded
    alternativesText: >-
      - **Add icon support section by section without a shared contract.**
      Rejected because it solves the symptom, not the authored-content
      inconsistency.

      - **Keep string items and add optional section-level default icons.**
      Rejected because it preserves two parallel mental models and weakens
      standardization.

      - **Standardize only runtime rendering and leave schemas permissive.**
      Rejected because package-owned authored surfaces must be validated
      explicitly, not implied by implementation.

      - **Allow gradual dual-mode migration.** Rejected because the user
      explicitly wants no backward compatibility and the repo direction already
      favors removing legacy surfaces.
    file: docs/rfcs/archive/superseded/rfc-0100-standardize-section-content-contracts-and-icon-capable-list-items-without-legacy.md
  - kind: rejected-alternative
    rfcId: RFC-0101
    title: Section shell and canonical visual modifier contract
    status: implemented
    alternativesText: >-
      - **Keep flat modifier props and add stricter validation.** Rejected
      because the combinatorial CSS does not disappear.

      - **Embed background inside a new "ContentLayout" component.** Rejected
      because authors then must always wrap, breaking RFC-0072 thin sections.

      - **Move shell into apps.** Rejected because RFC-0072 requires shared
      section structure to live in `packages/ui`.
    file: docs/rfcs/archive/implemented/rfc-0101-section-shell-and-canonical-visual-modifier-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0102
    title: Section header with tone-segmented headings and independent alignment
    status: implemented
    alternativesText: >-
      - **Markdown-inline tone syntax** (`heading: "**Notausgang als**
      beruflicher Standard"`). Rejected: less deterministic for AI agents and
      reviewers; requires a parser.

      - **Per-section `level` heuristic.** Rejected: archetype-driven
      `defaultHeader.level` is clearer.

      - **Keep `.section-number` in `global.css`.** Rejected: with
      `<SectionHeader>` in `packages/ui`, the primitive moves with its only
      consumer.
    file: docs/rfcs/archive/implemented/rfc-0102-section-header-with-tone-segmented-headings-and-independent-alignment.md
  - kind: rejected-alternative
    rfcId: RFC-0103
    title: Section body content contract and canonical body components
    status: implemented
    alternativesText: >-
      - **Keep per-section body shapes.** Rejected: blocks the AI-agent assembly
      invariant.

      - **Use one mega-component `<SectionBody>` that branches internally.**
      Rejected: hurts tree-shaking and makes the contract less explicit. One
      component per kind is clearer.

      - **Allow `composite` to wrap one of the standard kinds.** Rejected:
      composite is opaque by design.
    file: docs/rfcs/archive/implemented/rfc-0103-section-body-content-contract-and-canonical-body-components.md
  - kind: rejected-alternative
    rfcId: RFC-0104
    title: Canonical CTA contract, SectionImage, and image-fade primitive
    status: implemented
    alternativesText: >-
      - **Keep flat CTA props per section.** Rejected; impedes brand voice
      linting.

      - **Polymorphic single `<Cta>` that handles both single and group.**
      Rejected; the two-shape contract is clearer for agents and for archetypes
      that allow exactly one CTA.

      - **Image fades as biome tokens.** Rejected; fade is a layout decision per
      image, not a brand decision.
    file: docs/rfcs/archive/implemented/rfc-0104-canonical-cta-and-section-image-and-image-fade-primitive.md
  - kind: rejected-alternative
    rfcId: RFC-0105
    title: Site background as a distinct shell layer independent from section shell
    status: implemented
    alternativesText: >-
      - **Merge site and section background into one tagged union.** Rejected:
      the layer semantics differ (shell vs section); merging confuses agents.

      - **Keep the name `background` and prefix the section variant
      `section-background`.** Rejected: `<SectionShell>.background` is the
      natural name for the section-level concept; renaming the shell variant
      once is cleaner.

      - **Put site background config into `system.md`.** Rejected: page-level
      differentiation is needed (home gets a hero image bg, legal pages do not).
    file: docs/rfcs/archive/implemented/rfc-0105-site-background-as-distinct-shell-layer-independent-from-section-shell.md
  - kind: rejected-alternative
    rfcId: RFC-0106
    title: GSAP-native motion primitives bound to biome motion stance
    status: implemented
    alternativesText: >-
      - **Embed motion into biome only (no per-section config).** Rejected:
      agents need to opt sections out (long-form text legal pages don't want
      reveal even when biome allows it) — the page wins downward, never upward.

      - **CSS-only animations via `@keyframes`.** Rejected: GSAP is already in
      the stack and gives consistent ScrollTrigger semantics and
      `prefers-reduced-motion` handling.

      - **Per-section JS hooks.** Rejected: scattered animation code is exactly
      what this RFC removes.
    file: docs/rfcs/archive/implemented/rfc-0106-gsap-native-motion-primitives-bound-to-biome-motion-stance.md
  - kind: rejected-alternative
    rfcId: RFC-0107
    title: "Flag-day rollout of the section framework: archetypes, validators, apps,
      workflows, and agent rules"
    status: implemented
    alternativesText: >-
      - **Land each RFC independently with shims between them.** Rejected: shim
      layers contradict the "no legacy" rule and produce six broken intermediate
      states.

      - **Migrate one app first, defer the other.** Rejected: both apps share
      `packages/ui`, so once shared sections rewrite, both apps must migrate.

      - **Skip the workflow / agent-rule updates this round.** Rejected: a new
      onboarded app would be the first to fail unless agents see the new
      contract.
    file: docs/rfcs/archive/implemented/rfc-0107-flag-day-rollout-section-framework-archetypes-validators-apps-workflows-and-agents.md
  - kind: rejected-alternative
    rfcId: RFC-0108
    title: Section framework completion report and proposals for the next
      architectural wave
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0108-section-framework-completion-report-and-next-steps.md
  - kind: rejected-alternative
    rfcId: RFC-0109
    title: ComponentRole enum expansion for section framework primitives
    status: implemented
    alternativesText: >-
      - **Open vocabulary (`z.string()`).** Rejected — breaks DNA-19 audit
      discipline and removes the closest-match hint at validation time.

      - **Single shared `framework` role for all primitives.** Rejected — loses
      semantic precision in cross-manifest validators (e.g.
      `section.body.contract.validate` wants to find every component with `role:
      section-body`).

      - **One role per body kind (`section-body-list`, ...).** Rejected —
      explodes the enum without gain; the archetype id already distinguishes
      kinds.
    file: docs/rfcs/archive/implemented/rfc-0109-componentrole-enum-expansion-for-section-framework-primitives.md
  - kind: rejected-alternative
    rfcId: RFC-0110
    title: Shared section props fragment catalog and composeManifestPropsSchema
    status: implemented
    alternativesText: >-
      - **JSON Schema `$ref` with external resolver.** Rejected — adds a
      resolver step, breaks the simple in-process compose, and conflicts with
      the Astro/Vite bundling model.

      - **Generate manifests from a single source.** Rejected — manifest YAML is
      human-readable contract; codegen reduces clarity and complicates review.

      - **Move schema into TypeScript only (no YAML).** Rejected — section
      archetype YAMLs are partly authored by humans and AI agents; YAML
      consumption is the canonical path.
    file: docs/rfcs/archive/implemented/rfc-0110-shared-section-props-fragment-catalog-and-compose-manifest-props-schema.md
  - kind: rejected-alternative
    rfcId: RFC-0111
    title: Static validator suite for the section framework contracts
    status: implemented
    alternativesText: >-
      - **One mega-validator.** Rejected; granular validators give precise
      failure isolation and faster local feedback.

      - **Runtime browser tests.** Rejected for these contract rules; runtime
      checks are slower and don't gate the build.

      - **Type-only enforcement.** Rejected; many invariants (no raw `<section>`
      element, body kind ↔ component import) are file-level rules not
      expressible in TS types alone.
    file: docs/rfcs/archive/implemented/rfc-0111-static-validator-suite-for-section-framework-contracts.md
  - kind: rejected-alternative
    rfcId: RFC-0112
    title: section.scaffold per-bodyKind templates and canonical fragment output
    status: implemented
    alternativesText: >-
      - **One mega-template with conditional blocks.** Rejected — the
      conditional logic is unreadable and brittle compared to separate per-kind
      templates.

      - **Generate body code inline.** Rejected — body rendering belongs to the
      RFC-0103 body components in `@gogol/ui`; the scaffold must not duplicate
      it.

      - **Skip composite scaffolding.** Rejected — keep
      `composite.astro.template` as a placeholder + agent prompt; composite
      archetypes are rare and human- authored anyway.
    file: docs/rfcs/archive/implemented/rfc-0112-section-scaffold-per-body-kind-templates-and-canonical-fragment-output.md
  - kind: rejected-alternative
    rfcId: RFC-0113
    title: GSAP counter and inline-number cross-link cleanup under RFC-0106
    status: implemented
    alternativesText: >-
      - **Supersede RFC-0040 / RFC-0041.** Rejected — the scripts and
      orchestrator opt-ins remain canonical; only the authoring surface moved.

      - **Rename the GSAP scripts.** Rejected — identity stable; renaming would
      force a churn in build manifests and import paths for no gain.
    file: docs/rfcs/archive/implemented/rfc-0113-gsap-counter-and-inline-number-cross-link-cleanup-under-rfc-0106.md
  - kind: rejected-alternative
    rfcId: RFC-0114
    title: Biome-driven site-background defaults derivation
    status: implemented
    alternativesText: >-
      - **Bake site-background into `biome.tokens.derive` without a separate
      command.** Considered — the separate `biome.site-background.derive` makes
      the step composable and easier to re-run when the axes change.

      - **Author the background per-app only.** Rejected — duplicates the biome
      DNA across siblings and contradicts the "biome is visual DNA" principle.

      - **Move site background to a global stylesheet.** Rejected — section
      authors lose control over per-page transparency / parallax.
    file: docs/rfcs/archive/implemented/rfc-0114-biome-driven-site-background-defaults-derivation.md
  - kind: rejected-alternative
    rfcId: RFC-0115
    title: Team / PersonProfile migration to SectionImage and imageFade removal
    status: implemented
    alternativesText: >-
      - **Convert team to `body.kind: cards`.** Considered — `StandardCard` is
      close to `TeamMember` but lacks `slug` (business member binding) and the
      per-member animation toggles. Keeping team as composite is simpler and
      preserves existing semantics.

      - **Leave team carve-out indefinitely.** Rejected — long-tail carve-outs
      poison the framework; flagging them as a separate RFC ensures the cleanup
      actually happens.
    file: docs/rfcs/archive/implemented/rfc-0115-team-person-profile-migration-to-section-image-and-imagefade-removal.md
  - kind: rejected-alternative
    rfcId: RFC-0116
    title: Full enforcement of RFC-0111 motion, site-background, and orchestrator
      rules
    status: implemented
    alternativesText: >-
      - **Bundle all rules into RFC-0111.** Rejected — RFC-0111 established the
      skeleton; separating full enforcement from the initial skeleton keeps each
      RFC reviewable.

      - **Per-rule flags to disable enforcement.** Rejected — full enforcement
      is the baseline; any needed exceptions must be declared in an allow-list
      inside the validator, not as command flags.
    file: docs/rfcs/archive/implemented/rfc-0116-full-enforcement-of-rfc-0111-motion-site-background-and-orchestrator-rules.md
  - kind: rejected-alternative
    rfcId: RFC-0117
    title: biome.site-background.derive and onboarding.scaffold integration for site
      backgrounds
    status: implemented
    alternativesText: >-
      - **Onboarding always asks the user for an imageName.** Rejected — the
      point of biome inheritance is to remove repeated questions when the visual
      DNA already encodes the answer.

      - **Make `siteBackground` required on every biome.** Rejected — apps with
      solid `--ds-color-bg` paint don't need the noise.
    file: docs/rfcs/archive/implemented/rfc-0117-biome-site-background-deriver-and-onboarding-scaffold-integration.md
  - kind: rejected-alternative
    rfcId: RFC-0118
    title: SectionImage variable parallax speed and variant
    status: implemented
    alternativesText: >-
      - **Keep parallax boolean-only and require config on `<SectionShell
      motion.parallax>` instead.** Rejected — section motion config governs the
      whole section, but parallax is naturally an image-level property; keeping
      it on `<SectionImage>` is the cleaner mental model.

      - **Use a string scale (`slow | medium | fast`) instead of variant
      names.** Rejected — RFC-0106 already defines `ParallaxVariant` and reusing
      the names keeps the system coherent.
    file: docs/rfcs/archive/implemented/rfc-0118-sectionimage-variable-parallax-speed-and-variant.md
  - kind: rejected-alternative
    rfcId: RFC-0119
    title: Shared section props fragment versioning and pinning
    status: implemented
    alternativesText: >-
      - **Semver three-part versions.** Rejected — overkill for a small, curated
      catalog; a monotonic integer is sufficient and reads as a clear monotonic
      timeline.

      - **Git tag pinning.** Rejected — couples consumer state to repo history;
      the catalog ships inside the monorepo and the in-process resolver is the
      simpler path.

      - **No pinning, just changelog.** Rejected — agents and humans need a pin
      syntax during long migrations; otherwise breaking changes are always
      immediate.
    file: docs/rfcs/archive/implemented/rfc-0119-shared-section-props-fragment-versioning-and-pinning.md
  - kind: rejected-alternative
    rfcId: RFC-0120
    title: AST-grade section framework validators
    status: implemented
    alternativesText: >-
      - **Tree-sitter Astro grammar.** Considered — overkill for the four rule
      families; `@astrojs/compiler` is already a workspace dep.

      - **TS-ESLint plugin.** Rejected — couples to ESLint runtime; we want the
      validators to remain part of the kernel pipeline with their own envelope.

      - **Astro Content Collections introspection.** Rejected — only captures
      frontmatter, not the JSX component tree.
    file: docs/rfcs/archive/implemented/rfc-0120-ast-grade-section-framework-validators.md
  - kind: rejected-alternative
    rfcId: RFC-0121
    title: Adopt section background images through page-driven context
    status: implemented
    alternativesText: >-
      - **Add `backgroundImage` to every section.** Rejected because RFC-0101
      already defines `SectionBackground`; a parallel field would create
      permanent schema drift.

      - **Use only `site-background` plus transparent sections.** Rejected
      because the requirement is per-section image choice, while SiteBackground
      is page-shell scope.

      - **Let biomes choose section images.** Rejected because image choice is
      content/context-specific. Biomes may provide visual DNA and site defaults,
      but they are not the source of truth for individual section imagery.

      - **Merge `SiteBackgroundConfig` into `SectionBackground`.** Rejected
      because site layers support multi-layer fixed viewport backgrounds and
      site-level parallax, while section backgrounds are single-section paint
      primitives.
    file: docs/rfcs/archive/implemented/rfc-0121-adopt-section-background-images-through-page-driven-context.md
  - kind: rejected-alternative
    rfcId: RFC-0122
    title: "`tokens.colors.section-shell.lint` — close RFC-0108 open work for
      section-shell token contract"
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0122-tokens-colors-section-shell-lint.md
  - kind: rejected-alternative
    rfcId: RFC-0123
    title: Drop legacy `*.props.schema.ts` files — manifest `propsSchemaCompose` is
      the single source of truth
    status: implemented
    alternativesText: The Decision section above explicitly rejects the alternatives
      considered (per-manifest opt-out flags, archetype-YAML stubs, lowercase
      template files, etc.). This stub points readers there.
    file: docs/rfcs/archive/implemented/rfc-0123-drop-legacy-section-props-schema-files.md
  - kind: rejected-alternative
    rfcId: RFC-0124
    title: Section-framework token contract — every consumed `--ds-*` token must
      exist in `@gogol/tokens`
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0124-section-framework-token-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0125
    title: Close RFC-0108 Proposal G — constellation-level site background is
      superseded by biome-level derivation
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0125-close-rfc-0108-proposal-g-constellation-site-background.md
  - kind: rejected-alternative
    rfcId: RFC-0126
    title: Utility-section allow-list for section-framework structural validators
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0126-utility-section-allow-list-for-section-framework-validators.md
  - kind: rejected-alternative
    rfcId: RFC-0127
    title: Composite CTA allow-list and markdown-section image-fade nested shape
    status: implemented
    alternativesText: The Decision section above explicitly rejects the alternatives
      considered (per-manifest opt-out flags, archetype-YAML stubs, lowercase
      template files, etc.). This stub points readers there.
    file: docs/rfcs/archive/implemented/rfc-0127-composite-cta-allow-list-and-markdown-section-image-fade-shape.md
  - kind: rejected-alternative
    rfcId: RFC-0128
    title: markdown-section heading migration to `<SectionHeader>` (closes RFC-0127
      §HEAD-01)
    status: implemented
    alternativesText: The Decision section above explicitly rejects the alternatives
      considered (per-manifest opt-out flags, archetype-YAML stubs, lowercase
      template files, etc.). This stub points readers there.
    file: docs/rfcs/archive/implemented/rfc-0128-markdown-section-header-migration-to-section-header.md
  - kind: rejected-alternative
    rfcId: RFC-0129
    title: Biome-driven site-background pipeline — completion scope for RFC-0114 /
      RFC-0117
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0129-biome-site-background-pipeline-completion-scope.md
  - kind: rejected-alternative
    rfcId: RFC-0130
    title: Framework-internal archetypes allow-list for
      `archetype.registry.validate` (closes RFC-0108 §Proposal E)
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0130-framework-internal-archetypes-allow-list.md
  - kind: rejected-alternative
    rfcId: RFC-0131
    title: charity-donation-trust family.yaml — remap placeholder archetypes to the
      live catalog
    status: implemented
    alternativesText: The Decision section above explicitly rejects the alternatives
      considered (per-manifest opt-out flags, archetype-YAML stubs, lowercase
      template files, etc.). This stub points readers there.
    file: docs/rfcs/archive/implemented/rfc-0131-charity-donation-trust-family-archetype-remap.md
  - kind: rejected-alternative
    rfcId: RFC-0132
    title: "`naming.convention.lint` — exempt SHOUTY_SNAKE first-segment stems"
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0132-naming-convention-exemption-extension.md
  - kind: rejected-alternative
    rfcId: RFC-0133
    title: Session closeout — grace.backfill bulk pass, RFC-0114/0117/0129 pipeline
      completion, and packages-check.run zero-failure state
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0133-session-closeout-grace-backfill-and-biome-site-background-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0134
    title: Composable UI effects system — glass-first foundation
    status: implemented
    alternativesText: >-
      ### Keep `glass` and add more singleton props


      Rejected. This would create `glass`, `shadow`, `glow`, `fill`, `gradient`,
      and `texture` props at multiple nesting levels. It does not scale and
      repeats the current drift.


      ### Keep `surface.glass` and expand `surface`


      Rejected as the primary architecture. `surface` helps for card-like
      bodies, but not every effect target is a surface. Effects may apply to
      media, CTAs, shell backgrounds, rows, columns, or named slots.


      ### Add CSS-only modifiers per section


      Rejected. CSS modifiers hide the contract from content validation and make
      CMS/authoring behavior unpredictable.


      ### Implement all effects now


      Rejected. The architecture should reserve stackable effects, but
      implementation should prove the model with one effect kind: glass.
    file: docs/rfcs/archive/implemented/rfc-0134-composable-ui-effects-system-glass-first.md
  - kind: rejected-alternative
    rfcId: RFC-0135
    title: "Amend-onboarding lifecycle and batch contract: intake new materials into
      an already-onboarded app"
    status: implemented
    alternativesText: >-
      - **A mode flag on the greenfield brief (`--amend`).** Rejected by the
      founder: amend has an inverted precondition, per-batch bundles, and a
      provenance obligation that would bloat the single-tenant greenfield
      contract. A parallel lifecycle is cleaner.

      - **Keep provenance only in the process repo (`onboarding/.output`).**
      Rejected — it violates П-1: an extracted app could not answer where its
      claims came from. Provenance must ride inside `apps/<id>/`.

      - **Overwrite prior-version atoms on a new source version.** Rejected —
      destroys the audit chain. Supersession is recorded, not erased (П-7).

      - **A single batch-scoped coverage file.** Rejected — coverage must be
      cumulative per site to detect duplicates across batches (П-4, П-6).
    file: docs/rfcs/archive/implemented/rfc-0135-amend-onboarding-lifecycle-and-batch-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0136
    title: "Amend workflow chain and validators: .agents/workflows-amend/
      self-orchestration, delta-audit, and pause taxonomy"
    status: implemented
    alternativesText: >-
      - **Reuse the greenfield chain with conditional steps.** Rejected — the
      founder chose a separate chain; an inverted precondition and a
      compose-time branch would make the greenfield files harder to read and
      lint.

      - **Run the full `apps-check.run` after every batch.** Rejected — slow,
      re-flags settled content, discards the LLM cache (П-5). The delta scope is
      the point.

      - **Auto-pause on every new public route.** Rejected — contradicts the
      founder's autonomy decision. New routes are reported in handoff, not
      blocked.

      - **A standalone amend audit engine separate from RFC-0074 validators.**
      Rejected — duplicates logic; `audit.delta.run` wraps the existing
      validators with a scope and cache, reusing their internal rules.
    file: docs/rfcs/archive/implemented/rfc-0136-amend-workflow-chain-and-validators.md
  - kind: rejected-alternative
    rfcId: RFC-0137
    title: Sync app dependency versions into onboarding templates
    status: implemented
    alternativesText: >-
      - **Dependabot / Renovate for templates**: Rejected because templates are
      not installable packages; Dependabot cannot open PRs against
      `.template.json` files.

      - **Manual copy-paste**: Rejected because it is error-prone and invisible
      to other engineers.

      - **Diff-aware merge**: Rejected because the user explicitly requested "no
      comparisons, just overwrite versions." The simplest correct behavior is
      blind overwrite of the relevant fields.
    file: docs/rfcs/archive/implemented/rfc-0137-sync-app-versions-to-onboarding-template.md
  - kind: rejected-alternative
    rfcId: RFC-0138
    title: Resolve content references in block props at render time and add a
      business offer/guarantees schema
    status: implemented
    alternativesText: >-
      - **Resolve references inside each section component.** Rejected —
      duplicates logic across every section, easy to forget, and couples
      sections to the business layer. One shared substitution point is simpler
      and uniform.

      - **A client-side runtime resolver.** Rejected — violates the RFC-0050
      static-generation principle and adds runtime cost for data known at build.

      - **Keep hardcoding and add a lint that prices match across pages.**
      Rejected — treats the symptom (drift detection) instead of the cause (no
      single source). Also can't govern guarantee claims.

      - **Make `offer` mandatory.** Rejected — breaks apps that don't model an
      offer; the schema is additive by design.
    file: docs/rfcs/archive/implemented/rfc-0138-resolve-content-references-in-block-props-and-business-offer-schema.md
  - kind: rejected-alternative
    rfcId: RFC-0139
    title: "Delta-scoped amend author gate: judge an amend batch on what it changed,
      not on pre-existing whole-app debt"
    status: implemented
    alternativesText: >-
      - **Baseline-diff (snapshot all findings at a0, subtract at a3).**
      Heavier, needs a stored snapshot and stable finding identity; duplicates
      what file-scoping achieves directly.

      - **Fix all pre-existing debt before any amend.** Doesn't scale; makes
      every batch hostage to unrelated backlog — the precise anti-pattern this
      RFC removes.

      - **Per-pageId scope instead of per-file.** Per-page validators
      (page.block/shell) could take `--scope-pages`, but
      references/voice/business operate on files (incl. prose and business), so
      a uniform `--scope-files` is simpler and covers all five.
    file: docs/rfcs/archive/implemented/rfc-0139-delta-scoped-amend-author-gate.md
  - kind: superseded-rfc
    rfcId: RFC-0140
    title: Section-owned server API routes and default Cloudflare adapter
    status: superseded
    closedAt: 2026-06-03
    decisionSummary: "Three coordinated changes, all owned by the OS:"
    file: docs/rfcs/archive/superseded/rfc-0140-section-owned-server-api-routes-and-default-cloudflare-adapter.md
  - kind: rejected-alternative
    rfcId: RFC-0140
    title: Section-owned server API routes and default Cloudflare adapter
    status: superseded
    alternativesText: >-
      - **Full handler code generated into each site** (from a template, like
      `wrangler`): rejected — duplicates logic across dozens of sites, drifts,
      and a bug fix would touch every app. Thin re-export keeps logic in one
      place.

      - **Generate endpoints for every API-declaring section regardless of
      use**: rejected — ships dead routes to sites that do not use the section;
      the build would render unused on-demand functions.

      - **Dedicated runtime package for handlers** (separate from `@gogol/ui`):
      rejected for now — sections already own their full quintet; co-location
      keeps the section self-contained. Can be revisited if handler logic grows.

      - **`@astrojs/cloudflare` adapter + Astro on-demand routes** (the
      originally accepted design): rejected during implementation — the
      Workers-first adapter restructures the build output, breaking SSG
      build-time image optimization and colliding with the Pages `ASSETS`
      binding. Cloudflare Pages Functions deliver the same on-demand capability
      with zero adapter and a fully static site.

      - **`output: "server"`**: rejected — would make the whole site on-demand
      and lose the CDN-first SSG model.
    file: docs/rfcs/archive/superseded/rfc-0140-section-owned-server-api-routes-and-default-cloudflare-adapter.md
  - kind: rejected-alternative
    rfcId: RFC-0141
    title: Content Source Provider abstraction and asset-reference decoupling
    status: implemented
    alternativesText: >-
      **Write a native Astro Content Layer loader per CMS directly (skip the
      port).** Rejected for Phase 0: it couples the live site to CMS uptime and
      loses build-time validation and reproducible passports, and it still
      leaves asset resolution filesystem-bound. The port keeps those choices
      open per site.


      **Decouple assets only, defer the content port.** Rejected. Assets are the
      largest single item, but without the content port the "no module reads
      files directly" invariant stays unprotected, and a future CMS still
      requires editing every content call site.


      **Make asset tokens carry their kind (e.g. `local:hero-bg` vs
      `remote:...`).** Rejected. Tokens must stay opaque so the same content can
      be served by either provider; the _provider_, not the token, decides
      locality.


      **Push `system.md` through the provider too.** Rejected. The route
      registry, shell composition, growth, and passport config are architecture,
      not client content. They stay engineering-owned in Git regardless of where
      copy lives.
    file: docs/rfcs/archive/implemented/rfc-0141-content-source-provider-abstraction-and-asset-reference-decoupling.md
  - kind: rejected-alternative
    rfcId: RFC-0142
    title: Configurable llms.txt / llms-full.txt page inclusion policy
    status: implemented
    alternativesText: >-
      **Reuse `sitemapExclude` for LLMS too.** Rejected. It conflates two
      independent concerns; legal pages belong in the sitemap but not in the LLM
      context.


      **Pure default-by-`semanticType`, no per-page field.** Rejected. Legal
      pages share `semanticType: content` with valuable FAQ/prose pages and
      cannot be distinguished by type. A per-page override is unavoidable.


      **A separate `llms.config.yaml` per app.** Rejected, consistent with
      RFC-0051: `system.md` is the single canonical manifest. A parallel file
      would drift.


      **Truncate long bodies by byte budget instead of per-page depth.**
      Rejected. Truncation produces arbitrary, mid-section cuts and still wastes
      budget on irrelevant pages. Depth selection is semantic and predictable.


      **Filter in the command, not the formatter.** Rejected. The formatters are
      the single place that knows the projection shape; pushing filtering into
      the command would duplicate projection logic and break the pure-formatter
      boundary (DNA-25).
    file: docs/rfcs/archive/implemented/rfc-0142-configurable-llms-page-inclusion-policy.md
  - kind: rejected-alternative
    rfcId: RFC-0143
    title: Per-page output projection schema and generator contract
    status: implemented
    alternativesText: >-
      **Generic `generators: { <id>: <freeform> }` registry in the manifest.**
      Rejected. It trades per-generator type safety for false uniformity and
      duplicates the existing command registry. The pipeline already is the
      registry; the manifest should hold _typed config_, not a generator
      catalog.


      **Keep adding top-level per-page fields (`sitemapExclude`, `llms`, …).**
      Rejected. Field sprawl with no shared structure or validation; exactly the
      drift this RFC stops while the cost is still near-zero.


      **Fold `ai:` / `robots:` into `output` too.** Rejected. `output` is
      per-page; `ai`/`robots` are site-wide. Forcing both under one key would
      conflate two different config lifecycles.


      **Make `output` an open object (no `.strict()`).** Rejected. An open
      object re-creates the "unvalidated extension field" problem at a deeper
      level. The closed object makes adding a generator an explicit, reviewed
      schema change — which is the desired control point.


      **Do nothing until a third generator appears.** Rejected. The third
      per-page generator is already arriving (RFC-0142 `llms`). Standardizing
      now, with two apps, is the cheapest it will ever be.
    file: docs/rfcs/archive/implemented/rfc-0143-per-page-output-projection-schema-and-generator-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0144
    title: Consolidate the dual semantic-model builders onto the content-source port
    status: implemented
    alternativesText: >-
      **Keep two paths, add a shared helper for the gate only.** Rejected.
      RFC-0142/0143 already share `resolvePageOutput`, yet the surrounding
      construction is still duplicated — partial sharing did not stop the
      lockstep edits.


      **Consolidate onto fs directly, not the CSP.** Rejected. It would
      re-hardcode the filesystem assumption RFC-0141 just removed and block a
      future CMS-backed semantic model.


      **Delete the Astro per-page path and build JSON-LD from the disk loader at
      build time.** Rejected for now. JSON-LD is emitted per page at render;
      routing it through a build-time pre-pass is a larger change than
      consolidation and is not required to remove the duplication. The shared
      builder leaves that door open.


      **Just delete the dead full-site Astro path and stop.** Rejected as
      insufficient. It removes the unused code but leaves the two **live**
      per-page builders duplicated — the actual drift source.
    file: docs/rfcs/archive/implemented/rfc-0144-consolidate-semantic-model-builders-on-content-source-port.md
  - kind: rejected-alternative
    rfcId: RFC-0145
    title: Uniform source resolution for OS packages — drop the dist indirection
    status: implemented
    alternativesText: >-
      **Keep dist, fix the staleness with a turbo dependency.** Make every
      kernel command depend on a fresh `build`. Rejected. It keeps the compile
      tax and the split model; it only papers over the staleness with more
      rebuilds. The inconsistency (src here, dist there) remains.


      **Keep dist, add a dev watcher.** Rejected. A watcher is stateful, easy to
      forget to run, and does not help one-shot CLI invocations (the common
      case). Source resolution is stateless and always correct.


      **Move everything to dist (make app-layer packages compile too).**
      Rejected. It is the wrong direction: it would slow Astro builds, break the
      instant-edit DX that `@gogol/share` relies on, and add a bundling concern
      for packages Vite already handles from source.


      **Publish OS packages as compiled artifacts.** Rejected. They are private
      workspace packages with a single in-repo runner (tsx). There is no
      external distribution requirement to justify a compile step.
    file: docs/rfcs/archive/implemented/rfc-0145-uniform-source-resolution-for-os-packages.md
  - kind: rejected-alternative
    rfcId: RFC-0146
    title: Node-side Content Source Provider and semantic readers on the port
    status: implemented
    alternativesText: >-
      **Keep the RFC-0144 reader seam as the permanent abstraction.** Rejected.
      The seam works but is a second, semantic-only port; RFC-0141 already
      defines the canonical one. Two ports for "where content comes from" is the
      duplication this line of work is removing.


      **Leave substitution divergent (astro raw, disk substituted).** Rejected.
      It is a latent correctness bug — JSON-LD can ship literal `{business.*}`
      tokens to search engines. Reconciling is the point.


      **Substitute nowhere (drop RFC-0045 in the semantic layer).** Rejected.
      llms would regress to literal tokens; substitution is desired, just
      inconsistently applied today.
    file: docs/rfcs/archive/implemented/rfc-0146-node-content-source-provider-and-semantic-on-port.md
  - kind: rejected-alternative
    rfcId: RFC-0147
    title: Project the business offer and service catalog into AI-facing outputs
    status: implemented
    alternativesText: >-
      **Author a separate AI summary file by hand.** Rejected. It would
      duplicate the offer and drift from the RFC-0138 source of truth; the whole
      point is to project existing structured data.


      **Put the offer on a page (per-page `output`).** Rejected.
      Pricing/guarantees are site-wide facts about the business, not properties
      of one page; projecting them at the organization level matches their
      nature and avoids duplication across pages.


      **Only fix warpgogol-com's missing `semanticType`.** Rejected as
      insufficient. That alone would surface prose answer-blocks but still omit
      the structured offer — the highest-value data — because no loader reads
      it.
    file: docs/rfcs/archive/implemented/rfc-0147-project-business-offer-and-service-catalog-into-ai-outputs.md
  - kind: rejected-alternative
    rfcId: RFC-0148
    title: Business schema projection registry — declarative business-data
      projection to pages and AI
    status: implemented
    alternativesText: >-
      **Keep RFC-0147's targeted offer/service projection only.** Rejected per
      the directive: the requirement is general — _any_ present business file
      projects. A registry is the app-agnostic, extensible form; ad-hoc
      additions repeat the duplication this RFC removes.


      **Project every business schema by default.** Rejected.
      `external-services` and `compliance` are operational/private; defaulting
      them public is a data-exposure risk. Visibility must be declared, not
      assumed.


      **A generic `business: { ... }` JSON-LD dump.** Rejected. Untyped dumping
      defeats schema.org structure and the privacy boundary; projectors map to
      specific, correct schema.org types.


      **Per-app projection config.** Rejected. Projection is a property of the
      schema, not the app; declaring it once app-agnostically is the point.
    file: docs/rfcs/archive/implemented/rfc-0148-business-schema-projection-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0149
    title: Unify deployment on Cloudflare Workers via the Astro adapter; retire
      Pages Functions
    status: implemented
    alternativesText: >-
      - **Keep RFC-0140 (static + Pages Functions).** Rejected: a second, legacy
      deployment model that fights the platform and couples the endpoint to
      Cloudflare. The operational misfires are its symptom.

      - **Raw custom Worker entrypoint wrapping Astro.** Rejected: reintroduces
      a Cloudflare-coupled signature and bespoke routing; the adapter already
      gives static assets + on-demand routes + an extensible Worker.

      - **`output: "server"` with `prerender = true` on content.** Rejected:
      inverts the static-first default (DNA-01) and widens the on-demand surface
      unnecessarily.

      - **`imageService: "cloudflare"` (runtime Image Resizing).** Rejected as
      default: provisions a paid binding and moves optimization to request time
      for no benefit while assets are local. Reserved as the documented path for
      the future remote-CMS asset provider (RFC-0141).

      - **Raw `import { env } from "cloudflare:workers"` for secrets.**
      Rejected: host-coupled and untyped; `astro:env/server` is portable and
      type-safe.
    file: docs/rfcs/archive/implemented/rfc-0149-unify-cloudflare-workers-deployment-and-retire-pages-functions.md
  - kind: rejected-alternative
    rfcId: RFC-0150
    title: Adopt content-driven OG preview image generation
    status: implemented
    alternativesText: >-
      ### Keep one manually authored image per site only


      Rejected as the only architecture because it does not scale to
      page-specific previews or template-driven client customization. It remains
      supported as the fallback baseline.


      ### Screenshot actual pages at build time


      Rejected for the initial implementation. It is slower, more brittle, and
      more sensitive to animation, lazy loading, viewport differences, fonts,
      and browser behavior. Template-driven rendering from a normalized model is
      more deterministic for thousands of sites.


      ### Generate dynamically on Cloudflare for every request


      Rejected as baseline correctness. Social crawlers need reliable and fast
      assets, and not every deployment target should require Worker-compatible
      image rendering. Dynamic serving may be useful as an optional cache-backed
      adapter.


      ### Store generated previews outside `public/`


      Rejected because social metadata needs stable public URLs, and apps should
      remain self-contained deployable workspaces. Source intent may live in
      content; final social assets live in `public/`.


      ### Overwrite generated files on every build


      Rejected because clients may manually replace images. The explicit
      operator workflow is: delete the image from `public/`, then rerun
      generation.
    file: docs/rfcs/archive/implemented/rfc-0150-content-driven-og-preview-image-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0151
    title: Typographic effects for section headings — host-class effect kinds and
      registry-based EffectHost
    status: implemented
    alternativesText: >-
      - **Wrap the heading in `EffectHost`.** Rejected: `.effect-host` clips
      with `overflow: hidden` and adds an isolated box, breaking glow/shadow
      that must paint outside glyph bounds, and inserting a redundant DOM node.
      Text effects apply in place.

      - **Per-section heading CSS / a new `headingShadow` prop family.**
      Rejected: re-creates the fragmentation and prop drift RFC-0134 abolished;
      hides effects from validation.

      - **Per-glyph effects (wobble, typewriter) in this RFC.** Rejected
      (explicit non-goal): require DOM text decomposition, harming
      accessibility, selection, and copy, and typewriter induces CLS. Reserved
      for a dedicated future RFC if ever needed.

      - **A client JS runtime for interactive tilt.** Rejected for this wave:
      all kinds here are static CSS; no JS, no reduced-motion surface to manage.
    file: docs/rfcs/archive/implemented/rfc-0151-typographic-effects-for-section-headings.md
  - kind: rejected-alternative
    rfcId: RFC-0152
    title: Image Provider Port and Cloudflare runtime optimization
    status: implemented
    alternativesText: >-
      - **Post-build `dist/client/_astro → dist/_astro` sync (expert
      proposal).** Rejected, proven non-viable: the failure occurs _during_
      `astro build`'s image-generation phase, so a post-build copy runs too late
      (the derivatives are never produced and the build already exited 1); and
      deploy serves from `dist/client`, so syncing _into_ `dist/_astro` targets
      a directory that is never deployed. Right altitude, wrong mechanism
      (timing + direction).

      - **Custom runtime resize API / "own Image" that resizes.** Rejected:
      sharp cannot run in workerd at request time (RFC-0149); any runtime
      resizer must call `/cdn-cgi/image/` or a DAM anyway — i.e. it
      re-implements the `cloudflare` mode. A thin provider-aware _component_ is
      kept; a self-hosted _resizer_ is not.

      - **Keep `<img>` (current hotfix) as final.** Rejected: loses `srcset`;
      with max-quality in-repo originals this ships oversized images.

      - **`build-portable` (sharp) with a symlink workaround now.** Deferred:
      preserves host-portability but adds workaround code over an adapter bug
      and does not resize remote CMS images; not aligned with the chosen
      Cloudflare-native direction.
    file: docs/rfcs/archive/implemented/rfc-0152-image-provider-port-and-cloudflare-runtime-optimization.md
  - kind: rejected-alternative
    rfcId: RFC-0153
    title: Harden rfc.validate and rfc.check with lifecycle and referential rules
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0153-harden-rfc-validate-and-check-with-lifecycle-and-referential-rules.md
  - kind: rejected-alternative
    rfcId: RFC-0154
    title: Build must not mutate tracked business content
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0154-build-must-not-mutate-tracked-business-content.md
  - kind: rejected-alternative
    rfcId: RFC-0155
    title: GRACE markers for section-owned client and CSS assets
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0155-grace-markers-for-section-owned-client-and-css-assets.md
  - kind: rejected-alternative
    rfcId: RFC-0156
    title: Effects contract and coverage validators
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0156-effects-contract-and-coverage-validators.md
  - kind: rejected-alternative
    rfcId: RFC-0157
    title: Formalize the amends relationship and RFC navigability
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0157-formalize-amends-relationship-and-rfc-navigability.md
  - kind: rejected-alternative
    rfcId: RFC-0158
    title: Keep the DNA registry in sync with establishing RFCs via
      dna.registry.validate
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0158-dna-registry-validate-keep-architecture-dna-in-sync.md
  - kind: rejected-alternative
    rfcId: RFC-0159
    title: Serve default-language home at root for AI-crawler accessibility
    status: implemented
    alternativesText: >-
      - **Keep redirect, add content to the stub.** Rejected: still costs a
      redirect hop and risks duplicate content between stub and
      `/<defaultLang>/`.

      - **Option B canonical (`/<defaultLang>/` canonical, `/` carries `<link
      canonical>`).** Viable but leaves `/` non-canonical, weakening the clean
      root that the offer references; kept as fallback if 301 is infeasible.

      - **Server-side UA sniffing to serve final locale to bots only.**
      Rejected: cloaking risk, harder to verify, and conflicts with static-first
      delivery.
    file: docs/rfcs/archive/implemented/rfc-0159-serve-default-language-home-at-root-for-ai-crawler-accessibility.md
  - kind: rejected-alternative
    rfcId: RFC-0160
    title: Unprefixed default-language routing (default at /, non-default under
      /<lang>/)
    status: implemented
    alternativesText: >-
      - **Single optional `[...path].astro` parsing lang from the first
      segment.** Rejected: more runtime logic, harder to statically validate,
      easy to shadow language codes.

      - **Keep `/de/` and only alias `/` (RFC-0159 Option B).** Rejected by
      product — leaves the default audience on prefixed URLs and a `/de/`
      duplicate.

      - **Drop prefixes for all languages (slug-only).** Rejected: collides
      across languages and breaks hreflang clustering.
    file: docs/rfcs/archive/implemented/rfc-0160-unprefixed-default-language-routing.md
  - kind: rejected-alternative
    rfcId: RFC-0161
    title: Reclassify the growth and cosmic-passport layers from DNA invariants to
      features
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0161-reclassify-growth-and-cosmic-layers-from-dna-to-feature.md
  - kind: rejected-alternative
    rfcId: RFC-0162
    title: Wire social and Open Graph meta tags into the shared layout
    status: implemented
    alternativesText: >-
      - **Per-app head injection:** rejected — violates DNA-25 and re-introduces
      duplicated copy.

      - **Static `og:image` per site (single brand image):** rejected — wastes
      the per-page RFC-0150 images and weakens per-article previews needed by
      RFC-0167.

      - **Astro integration (`astro-seo`):** rejected — adds a dependency that
      does not understand the `SemanticPageModel` and would become a second
      source of truth.
    file: docs/rfcs/archive/implemented/rfc-0162-wire-social-and-open-graph-meta-tags-into-the-shared-layout.md
  - kind: rejected-alternative
    rfcId: RFC-0163
    title: Correct per-page JSON-LD URLs and emit organization identity nodes
    status: implemented
    alternativesText: >-
      - **Keep root `@id`, only fix `url`:** rejected — non-unique `@id` still
      breaks cross-page entity graphs.

      - **Hand-author `sameAs` per app in `system.md`:** rejected — duplicates
      business data; socials already belong in the business layer (RFC-0148).

      - **Emit `speakable` from a new authoring field:** rejected —
      `answerBlocks` already encode the extractable answers.
    file: docs/rfcs/archive/implemented/rfc-0163-correct-per-page-json-ld-urls-and-emit-organization-identity-nodes.md
  - kind: superseded-rfc
    rfcId: RFC-0164
    title: Self-host web fonts and remove the Google Fonts hotlink
    status: superseded
    closedAt: 2026-07-09
    decisionSummary: "Fonts are self-hosted. A build step `fonts.generate`
      materializes the biome's font faces as same-origin `woff2` assets plus an
      `@font-face` stylesheet under the app, with `font-display: swap` and a
      `<link rel=\"preload\">` for the primary weights. The shared layout stops
      emitting any `fonts.googleapis.com`/`fonts.gstatic.com` markup. Font
      selection moves into the biome/token layer (`@gogol/tokens` +
      `packages/ontology/biomes/*`). A validator `fonts.selfhost.validate` fails
      any head that references an external font origin and joins
      `apps-check.run`."
    file: docs/rfcs/archive/superseded/rfc-0164-self-host-web-fonts-and-remove-the-google-fonts-hotlink.md
  - kind: rejected-alternative
    rfcId: RFC-0164
    title: Self-host web fonts and remove the Google Fonts hotlink
    status: superseded
    alternativesText: >-
      - **`@fontsource` npm packages imported in CSS:** acceptable and likely
      the implementation source for the `woff2` binaries, but the assets must
      still be emitted same-origin via `fonts.generate` to keep the head free of
      bundler-specific URLs and to preload deterministically.

      - **Astro `@fontsource` + Fontaine fallback only:** good for CLS but still
      requires removing the Google markup; folded into this RFC.

      - **Keep Google Fonts with a consent banner:** rejected — cookies/consent
      banners are out of scope and the storage policy forbids cookies;
      self-hosting removes the problem entirely.
    file: docs/rfcs/archive/superseded/rfc-0164-self-host-web-fonts-and-remove-the-google-fonts-hotlink.md
  - kind: rejected-alternative
    rfcId: RFC-0165
    title: Add per-page robots control, image sitemap, and RSS feeds
    status: implemented
    alternativesText: >-
      - **Astro's `@astrojs/rss` / `@astrojs/sitemap`:** rejected as the owner —
      they bypass the `SemanticSiteModel` and the Generator Contract, creating a
      second source of truth; the pure formatters in `@gogol/share` keep one
      model.

      - **News sitemap (`news:news`):** deferred — only relevant for sites
      publishing >30 articles/2 days; revisit when a client needs it.

      - **Per-page robots in a separate file:** rejected — indexability belongs
      in the existing per-page `output` projection.
    file: docs/rfcs/archive/implemented/rfc-0165-add-per-page-robots-control-image-sitemap-and-rss-feeds.md
  - kind: rejected-alternative
    rfcId: RFC-0166
    title: Generate per-page Markdown projections at build time
    status: implemented
    alternativesText: >-
      - **Port the runtime worker route as-is:** rejected — reintroduces
      same-origin recursion, service-binding complexity, request-path
      `turndown`/`cheerio`, and timeouts, for content that is already Markdown.

      - **Derive Markdown by scraping built HTML at build time:** rejected —
      lossy and pointless when the structured model exists; HTML scraping is
      reserved for the CMS `richText` case where HTML is the only input.

      - **Single combined `llms-full.txt` only:** rejected — agents increasingly
      fetch the specific page URL; a per-URL twin maximizes citation precision.
    file: docs/rfcs/archive/implemented/rfc-0166-generate-per-page-markdown-projections-at-build-time.md
  - kind: rejected-alternative
    rfcId: RFC-0167
    title: Add a sellable Blog and Articles content module
    status: implemented
    alternativesText: >-
      - **Reuse `semanticType: content` with optional dates:** rejected —
      conflates legal/info pages with editorial content and gives no module
      boundary to sell or gate.

      - **External blogging platform embed (Medium/Ghost):** rejected — breaks
      the thin/own-your-content model and the same-origin SEO surface.

      - **Tags as free strings:** rejected for the schema — tags resolve against
      a per-app taxonomy so `keywords`/`articleSection` stay coherent.
    file: docs/rfcs/archive/implemented/rfc-0167-add-a-sellable-blog-and-articles-content-module.md
  - kind: rejected-alternative
    rfcId: RFC-0168
    title: Introduce the Integration Port for outbound channels and CRM
    status: implemented
    alternativesText: >-
      - **Keep per-section transport, add a second `if` per channel:** rejected
      — duplicates transport/secret logic and has no module boundary to sell or
      gate.

      - **Use a third-party form backend (Formspree/Basin):** rejected — adds a
      third-party dependency, weakens privacy, and cannot upsert to the client's
      CRM.

      - **Direct Pipedrive SDK in the route:** rejected — violates the
      no-vendor-SDK-in-app-code rule mirrored from growth/content-source ports.
    file: docs/rfcs/archive/implemented/rfc-0168-introduce-the-integration-port-for-outbound-channels-and-crm.md
  - kind: rejected-alternative
    rfcId: RFC-0169
    title: Resolve subscription entitlements from Stripe to gate features
    status: implemented
    alternativesText: >-
      - **Pipedrive as entitlement source:** rejected — it is a CRM, not a
      billing/entitlements system; would require a custom layer (the thing we
      are avoiding).

      - **Custom entitlements datastore:** rejected — duplicates Stripe
      Entitlements; more code, more drift, no benefit.

      - **Runtime-only gating (no build-time resolve):** rejected — static SSG
      cannot conditionally include routes/modules at request time; build-time
      resolution is required, with runtime re-check as the complement.
    file: docs/rfcs/archive/implemented/rfc-0169-resolve-subscription-entitlements-from-stripe-to-gate-features.md
  - kind: superseded-rfc
    rfcId: RFC-0170
    title: Add the Matomo growth adapter and retire Plausible
    status: superseded
    closedAt: 2026-06-06
    decisionSummary: "A new `@gogol/growth-adapter-matomo` package implements the
      existing `GrowthAdapter` contract: it loads/initializes Matomo (Matomo Tag
      Manager or the `matomo.js` tracker) from `vendor.options` (`url`,
      `siteId`, optional `cookieless`), maps `page-view` to a Matomo pageview
      and all other `EventName` values to Matomo events/custom dimensions, and
      is a no-op for `identifySegment` until RFC-0027 activates segments.
      `@gogol/growth-adapter-plausible` is removed, along with its dependency
      and any catalog/docs references. The supported adapter set becomes exactly
      `matomo` and `null`. Both reference apps keep `null` for CI/dev and
      document Matomo as the production vendor."
    file: docs/rfcs/archive/superseded/rfc-0170-add-the-matomo-growth-adapter-and-retire-plausible.md
  - kind: rejected-alternative
    rfcId: RFC-0170
    title: Add the Matomo growth adapter and retire Plausible
    status: superseded
    alternativesText: >-
      - **Keep Plausible alongside Matomo:** rejected — the product decision is
      a minimal supported surface (`matomo`/`null`); two cloud-analytics
      adapters is maintenance with no use.

      - **GA4 adapter:** rejected — cookie/consent and EU-privacy posture
      conflict with the no-cookies storage policy.

      - **Leave Plausible deprecated-but-present:** rejected — dead adapter code
      and an unused dependency invite drift; remove it cleanly while no app
      depends on it.
    file: docs/rfcs/archive/superseded/rfc-0170-add-the-matomo-growth-adapter-and-retire-plausible.md
  - kind: rejected-alternative
    rfcId: RFC-0171
    title: Add a headless CMS content-source adapter with preview and rebuild
    status: implemented
    alternativesText: >-
      - **Author the CMS field config by hand:** rejected — creates a second
      source of truth that drifts from the Zod schemas; `cms.schema.generate`
      keeps one.

      - **Go fully SSR/ISR for all content:** rejected — abandons the
      thin/static efficiency for the common case; SSR is reserved, opt-in, for
      hot domains via `prerender = false`.

      - **Adopt one commercial CMS as mandatory:** rejected — the port's purpose
      is adapter-interchangeability; ship a reference adapter, keep the contract
      open.

      - **Mutate `dist` on publish:** rejected — violates RFC-0049; publish
      triggers a rebuild, it does not patch build output.
    file: docs/rfcs/archive/implemented/rfc-0171-add-a-headless-cms-content-source-adapter-with-preview-and-rebuild.md
  - kind: rejected-alternative
    rfcId: RFC-0172
    title: Resolve render-time content images into the image sitemap
    status: implemented
    alternativesText: >-
      - **Astro `astro:build:done` integration emitting a per-page image
      manifest:** rejected as the first cut — it adds an integration and a
      manifest artifact, while the rendered HTML already contains the resolved
      URL and is already scanned post-build by `cloudflare.assets.validate`. The
      manifest may be revisited if HTML scraping proves fragile, but the
      `data-content-image` hook makes scraping a stable contract, not a guess.

      - **Reproduce Astro/Vite content hashing pre-build:** rejected — brittle,
      duplicates the bundler, and forbidden by RFC-0152.

      - **Augment the existing `sitemap-content.xml`/`-legal.xml` in `dist`
      post-build:** rejected — it would make the committed `public/` sitemaps
      diverge from the served `dist/` ones (RFC-0049 drift). A dedicated,
      additive image sitemap keeps each file single-purpose.

      - **Keep authoring `output.image` per page:** rejected as the primary path
      — it is the duplicate bookkeeping this initiative set out to remove.
      `output.image` remains valid as an explicit override; it simply flows
      through the same render→harvest path.
    file: docs/rfcs/archive/implemented/rfc-0172-resolve-render-time-content-images-into-the-image-sitemap.md
  - kind: rejected-alternative
    rfcId: RFC-0173
    title: Close three ecosystem validator and build defects
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0173-close-three-ecosystem-validator-and-build-defects.md
  - kind: rejected-alternative
    rfcId: RFC-0174
    title: Binding-language policy for translated legal documents
    status: implemented
    alternativesText: >-
      - **Put the toggle in `system.md` (reuse RFC-0097 `locales`).** Rejected:
      `system.md` is engineering-owned, not in the DNA-22 client-editable
      whitelist, and `locales` _hides_ a page rather than _falling back_ — the
      client could not self-serve the "show the German one instead" behavior the
      initiative requires.

      - **A new top-level client `context/` collection.** The initiative brief
      mentioned a "context folder." Rejected as a new surface: it would expand
      the DNA-22 whitelist and duplicate the role of the existing
      client-editable `pages` content. The per-page `translation` block achieves
      the same client-self-service intent within an existing surface. (If a
      dedicated client-config surface is later wanted for unrelated reasons,
      that is its own RFC.)

      - **Render the notice as a hard-coded layout string.** Rejected: violates
      DNA-8 (would not be a content-fed component) and could not be
      localized/validated/reused as a section.

      - **Make the notice opt-in.** Rejected: opt-in defeats the
      legal-protection purpose; the whole value is that it is default-on and
      non-removable for `unofficial` legal renders.

      - **Auto-translate the disclaimer/legal copy at build time.** Rejected per
      non-goals: translation stays an authoring activity under glossary control;
      the platform must not synthesize legal text.
    file: docs/rfcs/archive/implemented/rfc-0174-binding-language-policy-for-translated-legal-documents.md
  - kind: rejected-alternative
    rfcId: RFC-0175
    title: Introduce a consent-gated vendor-agnostic chat widget port
    status: implemented
    alternativesText: >-
      - **Eagerly load the vendor script (with or without a banner):** rejected
      — sets third-party storage/beacons before any user action; a banner adds a
      third-party CMP and is weaker than not loading at all. Click-to-load is
      stricter and matches the chosen posture.

      - **Server-proxy the widget through our own endpoint:** rejected — pulls
      the studio into the PII data path and contradicts RFC-0176's vendor-native
      default; also needs stateful infra the static stack does not have.

      - **Hardwire UChat into the section (no port):** rejected — throwaway
      work; a second chat vendor or a swap would mean re-editing section code
      and re-deriving consent handling. Ports are the house pattern
      (RFC-0027/0141).
    file: docs/rfcs/archive/implemented/rfc-0175-introduce-a-consent-gated-vendor-agnostic-chat-widget-port.md
  - kind: rejected-alternative
    rfcId: RFC-0176
    title: Generalize the integration port into a source-agnostic destination hub
    status: implemented
    alternativesText: >-
      - **Studio-central, multi-tenant hub:** rejected — pools many clients' PII
      into one studio-operated system (heavy processor footprint, single breach
      blast radius, cross-tenant). The client's own site is the hub instead.

      - **Keep the flat channel/CRM split:** rejected — cannot express
      calendar/scheduler destinations or the gogol-adapter vs vendor-native
      choice; forces a new mechanism per destination.

      - **Pure stateless fan-out (no queue):** rejected — cannot retry or dedup,
      so a destination outage loses leads; incompatible with a
      reliability-positioned studio. The queue holds events in-flight only
      (still no datastore).

      - **vendor-native as the default (UChat orchestrates):** rejected —
      scatters integration logic into per-client GUI flows, locks the client
      into UChat, and is inconsistent with the form already routing through the
      site. gogol-adapter is the default; vendor-native is the fallback.
    file: docs/rfcs/archive/implemented/rfc-0176-generalize-the-integration-port-into-a-source-agnostic-destination-hub.md
  - kind: rejected-alternative
    rfcId: RFC-0177
    title: Codify storage and third-party-script policy for consent-gated widgets
    status: implemented
    alternativesText: >-
      - **Cookie banner / third-party CMP:** rejected — adds a third-party
      script and maintenance and is _weaker_ than click-to-load (a banner still
      implies an always-present script). Not loading until activation is the
      stronger control.

      - **General post-consent storage exception for any module:** rejected —
      broadens the surface; the policy stays strict, the widget carve (clause 2)
      and the server-side carve (clause 4) are narrow and explicit.

      - **Forbid all server-side state (pure stateless hub):** rejected — would
      block calendar/email (OAuth refresh tokens) and reliable delivery (queue);
      clause 4 permits _credentials + in-flight events_ while still forbidding a
      visitor-data store.

      - **Ban the chat widget outright:** rejected — kills a deliberate paid
      product; click-to-load + disclosure + DPA make it compliant without
      abandoning the posture.
    file: docs/rfcs/archive/implemented/rfc-0177-codify-storage-and-third-party-script-policy-for-consent-gated-widgets.md
  - kind: rejected-alternative
    rfcId: RFC-0178
    title: Reconcile orphaned archetype references and role/semanticRole drift
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0178-reconcile-orphaned-archetype-references-and-role-semanticrole-drift.md
  - kind: rejected-alternative
    rfcId: RFC-0179
    title: Adopt Workers for Platforms with shared sharded delivery for
      thousand-site scale
    status: accepted
    alternativesText: >-
      - **Keep per-client consumer (RFC-0176 as-is):** rejected — caps the
      account at ~250 sites; the studio rejected that ceiling.

      - **Consumer/producer inside the WfP tenant:** rejected — Queue bindings
      on dispatch-namespace user Workers are undocumented and
      one-consumer-per-queue makes per-tenant consumers a dead end at scale.

      - **Single global queue for all sites:** rejected — no blast-radius
      isolation and no residency axis; a single wedged consumer or poison
      message would stall every client.

      - **Shared consumer that executes adapters directly (reads all tokens):**
      rejected — re-creates the studio-central token pool and PII datastore
      RFC-0176 forbids; dynamic dispatch keeps tokens and execution in the
      tenant.

      - **End-to-end PII encryption (tenant encrypts contact fields; shared
      layer carries ciphertext):** deferred, not rejected — strengthens the
      posture but adds key management; `eventId` dedup still works on
      ciphertext. Revisit if the DPA relaxation proves insufficient for a given
      jurisdiction.
    file: docs/rfcs/rfc-0179-adopt-workers-for-platforms-with-shared-sharded-delivery-for-thousand-site-scale.md
  - kind: rejected-alternative
    rfcId: RFC-0180
    title: Generate and provision per-site integration infrastructure from system.md
    status: implemented
    alternativesText: >-
      - **Keep hand-edited `wrangler.jsonc`:** rejected — does not scale past a
      handful of sites and permanently violates RFC-0081.

      - **Terraform / Pulumi:** rejected for now — adds an IaC tool and its own
      state management to the stack; wrangler is already idempotent by resource
      name and the kernel already owns generation. Revisit only if multi-account
      orchestration outgrows the CLI.

      - **Cloudflare API directly from onboarding (no generate/provision
      split):** rejected — couples offline content generation to network calls;
      the split keeps `generate` pure (agent/CI-safe) and isolates side effects
      in `provision`.

      - **Store resource IDs as the source of truth (a registry DB):** rejected
      — names derive from `siteId`; an external registry is a second source of
      truth to drift. IDs are looked up idempotently.
    file: docs/rfcs/archive/implemented/rfc-0180-generate-and-provision-per-site-integration-infrastructure-from-system-md.md
  - kind: rejected-alternative
    rfcId: RFC-0181
    title: EU-resident delivery via Upstash QStash and Redis
    status: implemented
    alternativesText: >-
      - **Cloudflare Queues + KV (RFC-0179):** rejected for EU sites — cannot be
      EU-pinned (Regional Services excludes Queues/Cron; KV has no
      jurisdiction).

      - **Cloudflare Durable Objects (jurisdiction=eu) as queue+dedup:** viable
      for EU residency but re-implements retry/DLQ by hand; QStash provides them
      managed. Revisit if Upstash dependency is undesirable.

      - **Keep CF backbone, relax to "EU-region best-effort under SCC":**
      rejected — contradicts the firm EU-exclusive requirement.

      - **EU-incorporated/self-hosted (Hetzner/Scaleway + self-hosted
      Redis/queue):** the only path to structural sovereignty (no US parent);
      deferred as tier 2 — heavier ops, not warranted at current volume.
    file: docs/rfcs/archive/implemented/rfc-0181-eu-resident-delivery-via-upstash-qstash-and-redis.md
  - kind: rejected-rfc
    rfcId: RFC-0182
    title: Validate Cloudflare Regional Services per site
    status: rejected
    closedAt: 2026-06-08
    decisionSummary: The Site OS gains a workspace-scoped command named
      `cloudflare.regional-services.validate` in `@gogol/site-kernel-checks`.
    alternativesText: >-
      - **Standalone Node script per site.** Rejected because it would duplicate
      environment naming, result envelopes, pipeline wiring, and app discovery
      outside Site OS.

      - **Always require EU for every site.** Rejected because the user
      requirement says an empty context value means all zones are allowed.

      - **Provision and validate in one command.** Rejected because validation
      is safe and pipeline-friendly, while provisioning mutates external
      Cloudflare state and belongs to operator/CD commands.

      - **Rely only on legal documentation.** Rejected because the failure mode
      is operational drift in provider settings, which documentation cannot
      detect.
    file: docs/rfcs/archive/rejected/rfc-0182-validate-cloudflare-regional-services-per-site.md
  - kind: rejected-alternative
    rfcId: RFC-0182
    title: Validate Cloudflare Regional Services per site
    status: rejected
    alternativesText: >-
      - **Standalone Node script per site.** Rejected because it would duplicate
      environment naming, result envelopes, pipeline wiring, and app discovery
      outside Site OS.

      - **Always require EU for every site.** Rejected because the user
      requirement says an empty context value means all zones are allowed.

      - **Provision and validate in one command.** Rejected because validation
      is safe and pipeline-friendly, while provisioning mutates external
      Cloudflare state and belongs to operator/CD commands.

      - **Rely only on legal documentation.** Rejected because the failure mode
      is operational drift in provider settings, which documentation cannot
      detect.
    file: docs/rfcs/archive/rejected/rfc-0182-validate-cloudflare-regional-services-per-site.md
  - kind: rejected-alternative
    rfcId: RFC-0183
    title: Adopt content-layer feature policy without a features directory
    status: implemented
    alternativesText: >-
      ### Reuse `src/content/features/**`


      Rejected. It conflicts with RFC-0047 and reintroduces a removed content
      surface.


      ### Keep `src/configure/features.ts`


      Rejected. It makes feature policy code-owned rather than CMS/content-owned
      and blocks author-facing management.


      ### Add a remote feature flag service


      Rejected. The platform currently needs build-time deterministic policy,
      not live remote feature management.


      ### Use ad hoc booleans in each component


      Rejected. That recreates drift between rendering, navigation, semantic
      outputs, and shared components.
    file: docs/rfcs/archive/implemented/rfc-0183-content-layer-feature-policy-without-features-directory.md
  - kind: rejected-alternative
    rfcId: RFC-0184
    title: Align llms text projections with AI-readable Markdown contract
    status: implemented
    alternativesText: >-
      - **Leave existing format unchanged:** rejected because the ecosystem
      already owns generation and can cheaply align with the de facto Markdown
      convention without increasing runtime complexity.

      - **Create a new `llms-v2.txt`:** rejected because the convention expects
      `/llms.txt`; versioning would reduce discoverability.

      - **Move all content into `llms-full.txt` and keep `llms.txt` tiny:**
      rejected for pass 1. `llms.txt` should remain a curated index with enough
      descriptions to guide fetch decisions.

      - **Add runtime Markdown export now:** rejected because RFC-0166 already
      chose build-time per-page Markdown projections and explicitly forbids
      request-time HTML conversion.
    file: docs/rfcs/archive/implemented/rfc-0184-align-llms-text-projections-with-ai-readable-markdown-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0185
    title: Strip generated markers from distribution artifacts
    status: implemented
    alternativesText: >-
      - **Remove markers from `public/` generation:** rejected because it breaks
      RFC-0081 ownership detection and would make generated files look manually
      maintained.

      - **Astro integration only:** rejected because `sitemap-images.xml` is
      produced after Astro build by `dist.sitemap.images.generate`; an Astro
      build hook would not clean later post-build artifacts.

      - **Strip inside each generator:** rejected because it duplicates marker
      syntax knowledge and misses files copied by Astro from `public/`.

      - **Runtime Worker filtering:** rejected because static artifacts should
      be correct before deployment and because platform-specific request
      filtering would add unnecessary runtime complexity.

      - **Stop writing markers into dist-only generators:** insufficient alone.
      It solves `sitemap-images.xml` but not copied `public/` artifacts.
    file: docs/rfcs/archive/implemented/rfc-0185-strip-generated-markers-from-distribution-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0186
    title: Introduce a shared Lagebild sync worker with tenant registry
    status: implemented
    alternativesText: >-
      - **Keep per-site Workers:** rejected — multiplies deploy targets and
      observability fragmentation.

      - **Merge sync cron into site Worker (Astro entrypoint):** rejected —
      mixes site HTTP traffic with background sync, increases blast radius, and
      complicates the Astro/cloudflare entrypoint.

      - **Use Cloudflare Queues for tenant fan-out:** rejected — Queues cannot
      be EU-pinned (RFC-0181, RFC-0182).

      - **Store secrets in Supabase:** rejected — violates the "secrets never in
      database" policy.
    file: docs/rfcs/archive/implemented/rfc-0186-lagebild-shared-sync-worker-and-tenant-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0187
    title: Validate unresolved content references in built HTML
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0187-validate-unresolved-content-references-in-built-html.md
  - kind: rejected-alternative
    rfcId: RFC-0188
    title: Declare a visitor sales funnel state machine for Lagebild
    status: implemented
    alternativesText: >-
      - **Keep the existing UChat bot as-is and just replace webhook URLs:**
      rejected. It keeps Make.com in the path, keeps the canonical state and
      pricing inside the vendor GUI, and cannot be versioned/validated. We keep
      UChat as the runtime but move process authority into the platform.

      - **Build a first-party (non-UChat) conversation UI:** rejected. The
      studio standardizes on UChat as the chat runtime and replicates it across
      clients; rebuilding the chat front end in-house is unnecessary scope. The
      governance win comes from owning the state machine, content, and state —
      not from owning the widget.

      - **Keep Make.com as a temporary bridge between UChat and Lagebild:**
      rejected outright. Make.com is not versioned with the repository, cannot
      be validated by Site OS, keeps business-critical state outside GRACE/RFC
      governance, and adds a third-party hop for visitor PII. UChat integrates
      with Lagebild directly via API + signed webhooks. There is no transitional
      exception.

      - **Let UChat own the canonical state machine and transition graph:**
      rejected. UChat's flow builder cannot be versioned, validated, or
      localized under our governance as the source of truth, and would re-create
      the original drift. UChat renders the conversation; the platform owns
      stages, transitions, pricing, and consent of record.

      - **Use Pipedrive stages / the Pipedrive webhook router as the canonical
      state machine:** rejected. Pipedrive is a sync destination/mirror, not the
      visitor-facing engine; its stage ids are tenant/vendor-specific and must
      not dispatch the funnel.

      - **Build a studio-central CRM:** rejected. RFC-0176 and RFC-0186
      deliberately avoid a studio-central PII CRM; Lagebild is a tenant-scoped
      buffer and sync layer.
    file: docs/rfcs/archive/implemented/rfc-0188-declare-visitor-sales-funnel-state-machine-for-lagebild.md
  - kind: rejected-alternative
    rfcId: RFC-0189
    title: Add `ui.i18n.lint` validator for hardcoded strings in shared UI components
    status: implemented
    alternativesText: >-
      | Alternative | Why rejected |

      | --- | --- |

      | Make `resolveLabel` require `fallback: ""` at the type level | Would
      break legitimate uses where a short placeholder (e.g. `"..."`) is
      acceptable. Type-level enforcement is too coarse. |

      | Extend `content.voice.lint` to scan `.astro` files |
      `content.voice.lint` is scoped to prose/markdown and operates on a
      different AST. Mixing concerns would complicate both validators. |

      | Use an ESLint plugin instead of a Site OS command | ESLint is not
      uniformly configured across all packages in the monorepo, and Site OS
      already owns workspace-level structural validation. Keeping i18n checks in
      the kernel preserves a single source of truth for build-time gates. |

      | Ban `resolveLabel` fallback entirely | Some sections genuinely need a
      minimal placeholder when the key is optional and no site default exists. A
      blanket ban would be too rigid. |
    file: docs/rfcs/archive/implemented/rfc-0189-add-ui-i18n-lint-validator-for-hardcoded-strings.md
  - kind: rejected-alternative
    rfcId: RFC-0190
    title: Model organizations and the multi-site client graph in Lagebild
    status: implemented
    alternativesText: >-
      - **Keep the company as a deal field/title (no entity):** rejected. It
      cannot be grouped, reported, billed against, or linked across a Person's
      multiple sites — the exact manager pain this RFC fixes.

      - **Organization-centric model (Org primary, Person a contact under it):**
      rejected. It breaks the "orderer orders for others" case where the Person
      is not a member of the target company.

      - **A contact↔organization membership join table:** rejected as premature.
      The Person→Organization relationship is fully derivable from their deals
      for the pilot; a membership table can come later if org hierarchies are
      needed.

      - **Studio-central organizations registry:** rejected. RFC-0176/0186
      deliberately avoid a studio-central PII store; the Organization stays
      tenant-scoped.
    file: docs/rfcs/archive/implemented/rfc-0190-model-organizations-and-the-multi-site-client-graph-in-lagebild.md
  - kind: rejected-alternative
    rfcId: RFC-0191
    title: Add client lifecycle and Stripe Billing to the Lagebild funnel
    status: implemented
    alternativesText: >-
      - **Keep Pipedrive's webhook router / Make.com for payments:** rejected
      (RFC-0188). It puts billing state in a vendor GUI and a third-party hop
      for PII; Stripe enters the first-party inbound route instead.

      - **Checkout-only (no subscription lifecycle):** rejected. The offer _is_
      a subscription; renewals, modules, dunning, and churn are core operational
      truth, not an afterthought.

      - **Let Stripe be the CRM:** rejected. Stripe is the billing authority,
      not the operational board; managers run delivery/retention in Pipedrive,
      mirrored from Lagebild.

      - **Manual invoicing only:** rejected for the recurring base, but retained
      as a _capability_ — managers may issue ad-hoc/custom invoices in Stripe,
      recorded as lifecycle events.

      - **One Stripe Customer per Person (not per Organization):** rejected.
      Per-Organization billing keeps clean per-company books for a Person who
      orders multiple sites (RFC-0190).
    file: docs/rfcs/archive/implemented/rfc-0191-add-client-lifecycle-and-stripe-billing-to-the-lagebild-funnel.md
  - kind: rejected-alternative
    rfcId: RFC-0192
    title: Introduce the Programmatic Surface route-source port
    status: implemented
    alternativesText: >-
      - **Author generated pages directly into `system.md pages[]`:** rejected —
      defeats programmatic generation and bloats the manifest to thousands of
      entries.

      - **Compute the surface live inside `getRouteRegistry()`:** rejected — the
      registry is a thin runtime-adjacent path; running the combinatorial engine
      there is heavy and risks pulling `node:fs`/dataset reads into worker
      bundles. A build-time artifact keeps the runtime path thin.

      - **Port the foerderung engine verbatim per business:** rejected — couples
      the engine to one dataset shape; the whole point is one axis-generic
      engine + many Blueprints.

      - **A new top-level dynamic Astro route per family (legacy model):**
      rejected — bypasses the registry and re-implements
      sitemap/hreflang/semantic per family.
    file: docs/rfcs/archive/implemented/rfc-0192-introduce-the-programmatic-surface-route-source-port.md
  - kind: rejected-alternative
    rfcId: RFC-0193
    title: Define the pSEO Blueprint contract and pilot the website-local surface
    status: implemented
    alternativesText: >-
      - **Extend the Constellation schema with axes instead of a new Blueprint
      object:** rejected — constellations are deliberately one ordered slot list
      per page; overloading them with datasets and combinatorics breaks their
      "schema simplicity is the contract" rule.

      - **Per-business TypeScript provider:** rejected — re-couples the engine
      to datasets and loses the declarative, reviewable, client-editable
      surface.

      - **Keep datasets as app-local Astro collections (legacy):** rejected —
      blocks the future CMS path (RFC-0141/0171).

      - **Free-form relevance code per family:** rejected — relevance is
      expressed as axis-generic weighted rules so it is uniform and testable.
    file: docs/rfcs/archive/implemented/rfc-0193-define-the-pseo-blueprint-contract-and-pilot-the-website-local-surface.md
  - kind: rejected-alternative
    rfcId: RFC-0194
    title: Add the pSEO substance gate and validator suite
    status: implemented
    alternativesText: >-
      - **Word-count threshold:** rejected — trivially gamed and orthogonal to
      real uniqueness; block-structure + token-uniqueness is harder to fake.

      - **Manual review:** rejected — does not scale to thousands of pages.

      - **LLM "is this thin?" judgement at build:** rejected for the gate —
      nondeterministic and slow; LLM enrichment is a separate, frozen-at-build
      concern (RFC-0197). The gate stays deterministic.

      - **Only suppress, never fail the build:** rejected — a family that is
      mostly thin should block, not silently ship a near-empty index.
    file: docs/rfcs/archive/implemented/rfc-0194-add-the-pseo-substance-gate-and-validator-suite.md
  - kind: rejected-alternative
    rfcId: RFC-0195
    title: Project the Programmatic Surface as a dual SEO and GEO surface
    status: implemented
    alternativesText: >-
      - **HTML-only, rely on crawlers to parse rendered pages:** rejected —
      generative engines ingest Markdown/llms.txt far more reliably; HTML-only
      under-serves the surface.

      - **A bespoke GEO export pipeline:** rejected — duplicates
      RFC-0143/0166/0184; the existing projection already does this for authored
      pages.

      - **Include all generated pages in llms.txt:** rejected — would bloat the
      index with broad/thin pages; GEO depth + substance gate bound it.
    file: docs/rfcs/archive/implemented/rfc-0195-project-the-programmatic-surface-as-a-dual-seo-and-geo-surface.md
  - kind: rejected-alternative
    rfcId: RFC-0196
    title: Couple pSEO indexability to entitlement tier and a freshness ledger
    status: implemented
    alternativesText: >-
      - **Separate "page-count add-on" product decoupled from quality:**
      rejected — would index arbitrary pages regardless of substance; coupling
      budget to the substance ranking keeps the cheapest tier's index clean.

      - **Time-bomb hard-delete of stale pages:** rejected — `noindex`-decay is
      reversible and preserves URLs/GEO twins; deletion churns the URL set and
      breaks links.

      - **Live freshness checks at build (call sources):** rejected — slow,
      nondeterministic, and violates the build-time-only stance; freshness is
      authored record metadata.
    file: docs/rfcs/archive/implemented/rfc-0196-couple-pseo-indexability-to-entitlement-tier-and-a-freshness-ledger.md
  - kind: rejected-alternative
    rfcId: RFC-0197
    title: Enrich blueprint fields with build-time frozen LLM generation
    status: implemented
    alternativesText: >-
      - **Generate per build:** rejected — nondeterministic builds, cost, and
      hallucination risk on every CI run.

      - **Generate at request time:** rejected — defeats static delivery, adds
      latency and cost, and is unreviewable.

      - **Free LLM text with no provenance/approval:** rejected — unreviewed AI
      text in production is a trust and legal risk; provenance + approval are
      mandatory.

      - **Feed LLM judgement into the substance gate:** rejected — the gate must
      stay deterministic (RFC-0194); LLM output is content, not a scoring
      oracle.
    file: docs/rfcs/archive/implemented/rfc-0197-enrich-blueprint-fields-with-build-time-frozen-llm-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0198
    title: Render the Programmatic Surface in the cosmic star-map
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0198-render-the-programmatic-surface-in-the-cosmic-star-map.md
  - kind: rejected-alternative
    rfcId: RFC-0199
    title: Add per-language record slugs to the Programmatic Surface generator
    status: implemented
    alternativesText: >-
      - **Translate static prefixes only (mixed paths).** Rejected: produces
      `/uk/sait/elektriker/berlin`, mixing languages within one path — worse SEO
      and worse UX than consistent German segments. This RFC exists specifically
      to avoid that compromise.

      - **Separate uk blueprints.** Rejected: duplicates axis/level/policy
      definitions and breaks the single-blueprint-per-surface identity that
      RFC-0193 relies on.

      - **Derive uk slug by transliterating `name` at build time.** Rejected:
      non-deterministic across transliteration schemes, can't be reviewed as a
      permanent SEO asset, and offers no author control.
    file: docs/rfcs/archive/implemented/rfc-0199-add-per-language-record-slugs-to-the-programmatic-surface-generator.md
  - kind: rejected-alternative
    rfcId: RFC-0200
    title: "Add a People module: canonical person records, an embeddable People
      section, and gated per-member profile pages"
    status: implemented
    alternativesText: >-
      - **Keep people data in `company.md` / extend the existing `team` schema
      in place.** Rejected — it preserves the inline-vs-bio split and the route
      hack, and keeps governance identity denormalized. The single canonical
      record is the whole point.

      - **A standalone `@gogol/people` package.** Rejected for the first cut —
      like Blog (RFC-0167), People has no standalone runtime/port logic; it
      composes existing layers (business data, ui section, share
      routing/JSON-LD, checks). A package adds ceremony without a seam.

      - **Author one `.md` page per member (the Blog-article path) for profile
      pages.** Rejected per the accepted decision — it duplicates each person's
      identity into a page file and forces hand-authoring N pages. Virtual
      routes sourced from the Person records (RFC-0192) keep a single source of
      truth.

      - **A pre-materialized `people.generated.json` artifact (full Programmatic
      Surface machinery).** Rejected — people are few (5–50); the route-registry
      merge can enumerate the `people` collection directly at build time without
      a generate step, freshness budget, or substance scoring.

      - **Gate the whole module (section + pages) behind one entitlement.**
      Rejected per the accepted decision — only per-member profile pages are
      paid; the section, the team page, and all people JSON-LD stay in the free
      baseline.
    file: docs/rfcs/archive/implemented/rfc-0200-people-module-canonical-person-records-embeddable-section-and-gated-profile-pages.md
  - kind: rejected-alternative
    rfcId: RFC-0201
    title: Add biome token coverage and contrast-intent validation
    status: implemented
    alternativesText: >-
      - **Manual visual QA only:** rejected because generated page families can
      produce many URLs and the defect is deterministic.

      - **Screenshot regression testing:** useful later, but too expensive and
      too late in the pipeline for this root cause.

      - **Patch `local.css`:** rejected as a canonical fix because app style
      files may be generated and because token ownership belongs to the
      biome/generator layer.

      - **Ban all `*-on-dark` tokens in shared UI:** rejected because some
      sections intentionally render on dark surfaces; the validator should
      understand biome/surface context instead of banning useful tokens.
    file: docs/rfcs/archive/implemented/rfc-0201-add-biome-token-coverage-and-contrast-intent-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0202
    title: Add living photos — opt-in looping video over any authored image
    status: implemented
    alternativesText: >-
      - **Raw `<video>` per section / a `videoName` prop family.** Rejected:
      re-creates the fragmentation RFC-0104/0152 abolished, drifts poster and
      clip apart, and hides assets from validation. The `live` config + derived
      sibling clip keeps a single source of truth.

      - **A full media player (Plyr/Mux) now.** Rejected for this RFC
      (non-goal): living photos are decorative, muted, looping micro-clips where
      native `<video>` is the correct, lightest tool — exactly the expert's
      first tier. The expert's Plyr/Mux tiers (controls, captions, streaming,
      analytics) belong to a future _content-video_ RFC with its own
      accessibility (captions/transcript) contract.

      - **`client:load` Astro island per video.** Rejected: hydrating each clip
      forfeits Astro's light-by-default posture. One opt-in, scheduler-deferred,
      presence-gated module (the RFC-0106 pattern) covers every living photo on
      a page and ships nothing when there are none.

      - **Always `loading="lazy"` on the video.** Rejected as a blanket default:
      per MDN, lazy interacts with `autoplay`/`poster`/`preload` and an
      off-screen lazy clip won't begin loading. We instead express laziness
      through `trigger` (`in-viewport` defers the fetch via the observer) and
      keep `autoplay`/above-the-fold eager.

      - **Writing the clip path in markdown.** Rejected by requirement:
      derive-by-convention removes an entire class of poster/clip mismatch and
      keeps authoring (and CMS) minimal.

      - **mp4/H.264 `<source>` fallback now.** Deferred: the pipeline emits
      `.webm` and modern target browsers support it; adding a second `<source>`
      is a clean follow-up behind the same `live` contract.
    file: docs/rfcs/archive/implemented/rfc-0202-add-living-photos-as-opt-in-looping-video-over-authored-images.md
  - kind: rejected-alternative
    rfcId: RFC-0203
    title: Adopt a canonical Diagnostic model for all static checks
    status: implemented
    alternativesText: >-
      - **Adopt SARIF as the internal model.** Rejected. SARIF is an interchange
      format; its nested `runs[].results[].locations[].physicalLocation.region`,
      `tool.driver.rules[]`, and `artifacts[]` shape is a heavy, hand-unfriendly
      target for ~97 producers and a regression against
      `resultFromViolations(cmd, [...])`. Mature analyzers (PVS-Studio,
      clang-tidy) keep a native model and treat SARIF as one output backend — we
      do the same, at the boundary.

      - **Big-bang rewrite of all checks.** Rejected — high risk and churn for
      no incremental value; the shim + phased lint achieves the same end state
      safely.

      - **A new finding superschema separate from `auditFindingSchema`.**
      Rejected by decision: the audit finding is already 90% of the target; we
      promote it rather than fork it.

      - **Status quo (string violations).** Rejected — no stable ids, no
      agent-actionable structure, and the renderer is condemned to sniff four
      shapes forever.
    file: docs/rfcs/archive/implemented/rfc-0203-adopt-a-canonical-diagnostic-model-for-all-static-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0204
    title: Build-portable image provider with pre-generated responsive variants
    status: implemented
    alternativesText: >-
      - **Enable Cloudflare Image Transformations
      (`PUBLIC_CF_IMAGE_TRANSFORM=on`).** Valid and zero-code, but couples
      mobile performance to a per-zone paid feature; not portable across hosts.
      Kept as a complementary option, not the platform fix.

      - **Astro `<Image>`/`getImage`/`astro:assets` build generation.**
      Rejected: this is the exact RFC-0152 blocker (`dist/_astro` vs
      `dist/client/_astro` ENOENT under `@astrojs/cloudflare`), and
      `buildImageSources` is synchronous while `getImage` is async.

      - **Runtime resize API.** Rejected per RFC-0149: sharp cannot run in
      workerd; any runtime resize must call `/cdn-cgi/image` and re-implements
      `cloudflare-runtime`.

      - **Shrink source images only (content hygiene).** Helps immediately but
      is per-asset manual discipline, not a durable provider-level contract; can
      be layered on top.
    file: docs/rfcs/archive/implemented/rfc-0204-build-portable-image-provider-with-pre-generated-responsive-variants.md
  - kind: rejected-alternative
    rfcId: RFC-0205
    title: Add `ui.silent-defaults.lint` and `page.blocks.mirror.validate` to
      prevent silent UI text degradation
    status: implemented
    alternativesText: >-
      1. **Runtime guard in components.** Inject a `console.warn` or Astro
      build-time error when `labels` is missing. Rejected: static validation is
      cheaper, reproducible, and does not require every component to import a
      runtime helper.

      2. **Merge arrays in `deepMergeEntryData`.** Change the merge logic so
      localized `blocks` arrays merge per-element with the default-language
      array. Rejected: this would silently mix DE and EN props in unpredictable
      ways; explicit per-page authorship is the intended CMS-friendly model
      (RFC-0047). The validator approach keeps the merge behavior simple while
      catching authoring mistakes.

      3. **Expand `need.markers.validate` to scan for empty tags.** Rejected:
      empty tags are valid HTML (`<span></span>`); distinguishing "intentionally
      empty" from "accidentally empty" requires semantic knowledge of the
      component, which the post-build HTML scanner does not have.

      4. **Use Zod `strict()` on the entire `blocks[].props` object.** Already
      done in many manifests, but useless when `labels` is not declared in the
      schema at all.
    file: docs/rfcs/archive/implemented/rfc-0205-add-ui-silent-defaults-lint-and-page-blocks-mirror-validate-to-prevent-silent-ui-text-degradation.md
  - kind: rejected-alternative
    rfcId: RFC-0206
    title: Add content link, path, and anchor validation to Site OS checks
    status: implemented
    alternativesText: >-
      - **Post-build HTML crawler**: Rejected. The bug is in authored content,
      not in rendering. Catching it after build is too late and produces less
      actionable diagnostics (line numbers in YAML are lost).

      - **Per-app manual testing**: Rejected. Relies on human discipline, which
      already failed.

      - **Extending `seo.internal-linking.validate`**: Rejected. That command
      audits rendered HTML for SEO structure (orphaned pages, crawl depth). It
      does not inspect authored content YAML/markdown and cannot report
      `LINK-02` prefix violations.
    file: docs/rfcs/archive/implemented/rfc-0206-add-content-link-and-anchor-validation-to-site-os-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0207
    title: Elevate the website-local surface with bespoke AI narrative, images, and
      full Ukrainian
    status: implemented
    alternativesText: >-
      - **Per-tuple narrative only (no record reuse).** Rejected as the _sole_
      model: N×M generations and N×M approvals for content (FAQs, focus points)
      that is genuinely per-industry, not per-pair. The hybrid keeps factual
      content per-record and spends generation only on the connective narrative
      that must be unique.

      - **Hand-author everything (port the fat site verbatim).** Rejected: does
      not scale to thousands of pages and ignores the founder's explicit intent
      to use AI for strong prose. The fat-site data is still ported — as the
      _record-level_ substance — but the narrative is generated.

      - **Build-time LLM calls.** Rejected: breaks the RFC-0197 determinism
      invariant (non-reproducible builds, build-path cost/latency, hard CI
      failures). Generation stays an explicit offline step.

      - **AI-generated or purchased-stock images.** Rejected this pass per the
      founder: migrate owned assets, add the rest manually. Avoids an image-gen
      pipeline and third-party licensing/GDPR exposure (already flagged for
      fonts).

      - **Auto-approve enriched content.** Rejected this pass: the founder chose
      freeze + human review. `surface.enrich.review` makes review fast;
      trusted-prompt auto-approve is a deferred follow-up.
    file: docs/rfcs/archive/implemented/rfc-0207-elevate-website-local-surface-with-bespoke-narrative-images-and-localization.md
  - kind: rejected-alternative
    rfcId: RFC-0208
    title: Semantic Block Text Extraction for Comprehensive Markdown Twins
    status: implemented
    alternativesText: >-
      - **Force migration to prose blocks**: Reject all block-based pages,
      require `contentRef` to `prose/*.md`. Rejected: blocks are a legitimate
      content surface (RFC-0047); migration is massive churn for existing apps.

      - **Extract at component level**: Have Astro components emit text during
      SSR, capture via hook. Rejected: couples extraction to rendering; breaks
      the semantic layer's separation from UI.

      - **JSON twins instead of markdown**: Emit structured JSON for LLMs.
      Rejected: markdown is the established contract (RFC-0166); easier for
      humans to read/debug; LLMs handle markdown natively.

      - **Runtime extraction**: Extract text at request time from rendered HTML.
      Rejected: too slow; requires DOM parsing; fails for static generation.
    file: docs/rfcs/archive/implemented/rfc-0208-semantic-block-text-extraction-for-comprehensive-markdown-twins.md
  - kind: rejected-alternative
    rfcId: RFC-0209
    title: Project the hero lead image as the page primaryImage / og:image
    status: implemented
    alternativesText: >-
      - **Build-time token→URL manifest in the pre-render handler.** Resolve
      tokens in `page-handler.ts` from a generated content-asset URL map (like
      the RFC-0204 variants manifest). Rejected as heavier and provider-coupled
      — the render layer already resolves tokens; duplicating that map invites
      drift.

      - **Post-build `<head>` rewrite.** Harvest the rendered lead image (as
      RFC-0172 does for the sitemap) and rewrite og:image in the built HTML.
      Rejected: fragile head-string rewriting, and it cannot feed JSON-LD
      `primaryImageOfPage` cleanly.

      - **Require authors/baker to always set `output.image`.** Rejected:
      redundant with `leadImage`, easy to forget, and impossible to keep in sync
      for generated surface pages.
    file: docs/rfcs/archive/implemented/rfc-0209-project-the-hero-lead-image-as-the-page-primary-image.md
  - kind: rejected-alternative
    rfcId: RFC-0210
    title: Unified video & living-photo playback contract with build-time
      HLS/MP4/WebM generation
    status: implemented
    alternativesText: >-
      - **Two separate stacks (keep `<LivePhoto>` + add a new
      `<ContentVideo>`).** Rejected: duplicates resolver, runtime, validator,
      and iOS quirks — the exact fragmentation the founder asked to avoid ("одна
      универсальная технология"). One contract with profiles is the durable
      shape.

      - **WebM-only or MP4-only as the single universal format.** Rejected per
      the expert: WebM-only is risky as a production lowest-common-denominator;
      MP4-only loses adaptive bitrate on long-form. HLS-first + MP4-fallback +
      optional WebM is the universal **delivery contract**, not a single file
      format.

      - **A hand-rolled player UI.** Rejected (non-goal): controls, fullscreen,
      captions UI, and keyboard/screen-reader behavior across
      Safari/iOS/Chrome/Firefox is an open-ended debugging surface. Plyr is the
      battle-tested choice; the only cost (JS weight) is neutralized by lazy
      click-to-load gating.

      - **Eager Plyr/hls.js, or an Astro island per video.** Rejected: forfeits
      the green-Lighthouse posture. One presence-gated, scheduler-deferred,
      click-to-load runtime ships nothing when there is no feature video.

      - **Transcode in `build.prepare` with plain skip-on-exists (literal
      RFC-0204 parity).** Rejected in favor of a content-addressed cache:
      re-encoding a 30-minute video on every cold build is unacceptable; the
      cache makes cold deploys from a warm cache do zero ffmpeg work while
      staying idempotent.

      - **Transcode in workerd at request time.** Rejected (RFC-0149 precedent):
      no sharp/ffmpeg in workerd; any runtime transform must be a hosted
      provider. Static, pre-generated HLS/MP4/WebM is the portable default.

      - **Generate all three formats for every video unconditionally (the
      original brief).** Amended to **profile-driven** generation: HLS over a
      muted, looping, decorative ambient/background clip is wasted build time,
      storage, and runtime complexity for zero benefit. Feature gets
      HLS+MP4(+WebM); ambient/background get WebM+MP4.

      - **Filename language suffix (`promo-uk.mp4` / `promo-de.mp4`).** Rejected
      for consistency: the framework already addresses language by directory
      (`<lang>/assets/`) via `resolveImage`. The pilot files are renamed to
      `promo.mp4` under each language's assets folder so `resolveMedia` is the
      exact analogue of `resolveImage`.
    file: docs/rfcs/archive/implemented/rfc-0210-add-a-unified-video-playback-contract-with-build-time-hls-mp4-webm-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0211
    title: "Content Knowledge Lifecycle: claims as the atomic unit of durable site
      truth"
    status: implemented
    alternativesText: >-
      - **Graph database (Neo4j) / temporal KG engine (Graphiti/Zep).**
      Rejected: it contradicts the content-as-data, version-controlled,
      AI-agent-buildable philosophy, adds a runtime dependency, and duplicates
      state that git + closed schemas already hold. The knowledge graph is a
      projection, not a store.

      - **A separate external "content control" service with its own
      dashboard/Jira/VS Code plugin.** Rejected as the _primary_ mechanism: it
      re-creates a second source of truth beside the repo. Dashboards may
      consume CKL artifacts later, but the artifacts live in the repo.

      - **Per-module freshness (keep Freshness Ledger and enriched provenance
      separate).** Rejected: it guarantees drift between modules and triples
      maintenance. One claim model, specialized per provenance kind.

      - **Build-time only, no proactive clock.** Rejected: it is the status quo.
      Facts expire silently between builds; the whole point is to plan _before_
      the build (RFC-0216).
    file: docs/rfcs/archive/implemented/rfc-0211-content-knowledge-lifecycle-claims-as-the-atomic-unit-of-durable-site-truth.md
  - kind: rejected-alternative
    rfcId: RFC-0212
    title: Field-level provenance and temporal-validity annotations for canonical
      content records
    status: implemented
    alternativesText: >-
      - **Inline frontmatter annotation (`residents: { value, asOf, … }`).**
      Rejected: wraps the value, breaks reference resolution and the clean
      editable body, and forces a schema change on every field.

      - **A single per-app `claims.yaml`.** Rejected: couples unrelated records
      into one file, making diffs and ownership noisy. Per-record sidecars
      mirror the record graph.

      - **Annotate only via a database.** Rejected per RFC-0211 — provenance is
      version-controlled content.

      - **Make annotation mandatory for all fields.** Rejected: forces ceremony
      on trivial copy; the load-bearing heuristic targets effort where staleness
      actually bites.
    file: docs/rfcs/archive/implemented/rfc-0212-field-level-provenance-and-temporal-validity-annotations-for-canonical-content-records.md
  - kind: rejected-alternative
    rfcId: RFC-0213
    title: content.freshness.validate and the authored-content Freshness Ledger
    status: implemented
    alternativesText: >-
      - **Reuse `surface.freshness` directly for authored content.** Rejected:
      surface freshness is keyed on generated route substance/age, not on
      per-field claim windows; sharing the _decay helper_ is right, sharing the
      _command_ is not.

      - **Evaluate freshness at render time and hide stale blocks.** Rejected as
      the primary mechanism: it hides drift from maintainers instead of
      surfacing it; hiding may be an _option_ (`noindex`/suppress) but the
      default is to report and plan (RFC-0216).

      - **Block any expired claim immediately.** Rejected: too brittle for
      advisory facts; criticality is author-declared, and promotion is staged.
    file: docs/rfcs/archive/implemented/rfc-0213-content-freshness-validate-and-the-authored-content-freshness-ledger.md
  - kind: rejected-alternative
    rfcId: RFC-0214
    title: External source binding and the shared Truth Monitor worker
    status: implemented
    alternativesText: >-
      - **Per-site monitor workers.** Rejected outright by RFC-0186 discipline;
      thousands of workers is the anti-pattern the shared Lagebild worker exists
      to prevent.

      - **Auto-apply diverged values to content.** Rejected: external sources
      can be wrong, transiently malformed, or adversarial; applying a fetched
      value as live fact without review violates the NEED_THIS /
      human-in-the-loop posture (RFC-0136, RFC-0218). The monitor proposes, a
      human/agent disposes.

      - **Scrape any URL mentioned in content.** Rejected: implicit scraping is
      fragile and a privacy/abuse risk; sources are explicit, typed descriptors
      with an extraction contract.

      - **Run fetching at site build time.** Rejected: couples deploys to
      third-party availability and EU egress timing; the cadence belongs in a
      scheduled worker, not the build.
    file: docs/rfcs/archive/implemented/rfc-0214-external-source-binding-and-the-shared-truth-monitor-worker.md
  - kind: rejected-alternative
    rfcId: RFC-0215
    title: "Derived-content staleness: derivedFrom and sourceHash stamping for
      translations and generated copy"
    status: implemented
    alternativesText: >-
      - **Translation-management system (Crowdin/TMS) as source of truth.**
      Rejected as primary: re-creates a second store outside the repo; the stamp
      keeps derivation status version-controlled. A TMS may _feed_ stamps later.

      - **Compare full rendered text instead of a hash.** Rejected: noisy, slow,
      and couples to markup; a normalized content hash is stable and cheap.

      - **Treat translations as references.** Rejected: natural-language
      translation is not a substitutable reference; it is a human/agent
      transformation that must be re-done, not re-resolved.

      - **Block any outdated derivative immediately.** Rejected: too brittle for
      non-critical copy; criticality is author-declared and promotion staged,
      consistent with RFC-0213.
    file: docs/rfcs/archive/implemented/rfc-0215-derived-content-staleness-derivedfrom-and-sourcehash-stamping.md
  - kind: rejected-alternative
    rfcId: RFC-0216
    title: "Proactive maintenance planning: the content review calendar and task
      router"
    status: implemented
    alternativesText: >-
      - **Adopt Jira/GitLab issues as the system of record.** Rejected as
      primary: couples content health to an external tracker and a second source
      of truth. The plan artifact is in-repo and deterministic; routing to a
      tracker is an optional export, not the store.

      - **Calendar-only, no event triggers.** Rejected: pure calendar reviews
      cause "calendar burnout" and miss event-driven drift (a programme
      closing). The plan blends time triggers and divergence triggers.

      - **Per-build warnings with no consolidation.** Rejected: this is the
      status quo that produces alert fatigue; consolidation into dated tasks is
      the point.

      - **Block on any drift.** Rejected: over-blocks advisory content;
      amber/red by author-declared criticality is the calibrated gate.
    file: docs/rfcs/archive/implemented/rfc-0216-proactive-maintenance-planning-the-content-review-calendar-and-task-router.md
  - kind: rejected-alternative
    rfcId: RFC-0217
    title: "The Claim Ledger: append-only fact lineage and temporal knowledge-graph
      projection"
    status: implemented
    alternativesText: >-
      - **Temporal KG engine (Graphiti/Zep) or a graph DB.** Rejected per
      RFC-0211: external runtime + second source of truth. An append-only NDJSON
      ledger + on-demand projection gives temporal query without a service, and
      stays diffable and signable.

      - **Rely on git history alone.** Rejected: git tracks file bytes, not
      semantic claims; it cannot answer "value of subject S as of D" across
      renames/locale moves, and carries no provenance/source per change.

      - **Mutable current-state JSON only (no event log).** Rejected: loses
      lineage — the whole point of the decades layer is _why and when_ a fact
      changed, which only an append-only log preserves.

      - **Store the graph as the authority and derive files from it.** Rejected:
      inverts the content-as-data model; files stay authoritative, the graph is
      derived.
    file: docs/rfcs/archive/implemented/rfc-0217-the-claim-ledger-append-only-fact-lineage-and-temporal-knowledge-graph-projection.md
  - kind: rejected-alternative
    rfcId: RFC-0218
    title: "Agent operating model for the Content Knowledge Lifecycle: authoring,
      sourcing, and maintenance discipline"
    status: implemented
    alternativesText: >-
      - **Rely on validators alone, no behavioral policy.** Rejected: validators
      catch shape and currency, not judgment ("did you actually verify before
      re-stamping?"); the audit trail + policy cover the rest.

      - **Fully autonomous agents, no human gates.** Rejected: legal/price facts
      and external-source enabling carry real-world liability; the platform's
      existing NEED_THIS/approval posture is deliberate and extended here, not
      removed.

      - **A separate agent-ops tool outside the repo.** Rejected: the rules
      belong in `AGENTS.md` and the workflows agents already read, not a
      detached system.
    file: docs/rfcs/archive/implemented/rfc-0218-agent-operating-model-for-the-content-knowledge-lifecycle.md
  - kind: rejected-alternative
    rfcId: RFC-0219
    title: Generate a state-chart projection of the deal lifecycle for agent
      navigation
    status: implemented
    alternativesText: >-
      - **Hand-authored Mermaid diagram in the spec:** rejected. It would drift
      from `FUNNEL_TRANSITIONS` and become misleading; the codebase's discipline
      is generate-and-drift-guard.

      - **Single mega-diagram (funnel + lifecycle + invoices in one graph):**
      rejected for the default. Visually overloaded and harder to navigate; the
      layered form keeps each machine legible. (The code maps remain the unified
      machine-readable form.)

      - **Leave edges unlabelled, keep the prose trigger table only:** rejected.
      The whole point is a self-sufficient navigable graph; an unlabelled graph
      still forces a cross-reference.

      - **Adopt Event Sourcing / replay to derive State:** rejected and
      explicitly out of scope. State is already the source of truth in Postgres;
      events are transport (RFC-0186/0188). This RFC makes the State's
      transition graph explicit, not the persistence strategy.

      - **Emit SCXML/XState instead of Mermaid:** rejected as the primary
      format. Mermaid renders on GitHub and is highly agent-legible; the typed
      code maps already are the machine-consumable form. A machine format can be
      added later if a runtime interpreter ever needs it.
    file: docs/rfcs/archive/implemented/rfc-0219-generate-a-state-chart-projection-of-the-deal-lifecycle-for-agent-navigation.md
  - kind: rejected-alternative
    rfcId: RFC-0220
    title: Introduce site-wide material credits and provenance disclosures
    status: implemented
    alternativesText: >-
      - **Inline `credits:` in every block.** Rejected because it duplicates
      data across localized pages and every section would need a bespoke render
      path. Sidecars make one material token one record.

      - **A single global `site/<lang>/credits.md` edited by hand.** Rejected
      because it cannot reliably prove coverage for every media/image reference
      and would drift from the visible UI.

      - **Treat AI agents as authors.** Rejected because AI participation is
      provenance, not a human byline. The schema models AI agents, models, and
      platforms as separate role-bearing parties.

      - **Only JSON-LD, no visible UI.** Rejected because users need
      human-readable transparency and because visible disclosures are easier to
      audit than hidden metadata.

      - **Start with C2PA/IPTC writing.** Rejected for phase 1 because
      build-time embedded metadata mutation introduces tooling and binary-file
      risk. The structured source record comes first; metadata writing can
      consume it later.
    file: docs/rfcs/archive/implemented/rfc-0220-introduce-site-wide-material-credits-and-provenance-disclosures.md
  - kind: rejected-alternative
    rfcId: RFC-0221
    title: Site handoff bundle and version-aware absorb for internal site
      circulation across evolving ecosystems
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Reuse `client.export` for internal handoff | Ships a redundant full copy
      of `packages/` the recipient already has, and co-mingles authored source
      with disposable generated artifacts — no clean re-absorb line. |

      | `git merge` the bundle into the recipient's tree | Merges drifted
      generated files and possibly `packages/` edits; under a no-legacy policy
      this becomes archaeology within a few round trips. Always-regenerate
      avoids it. |

      | Ship generated files and "fix them up" on receipt | Generated artifacts
      are disposable by doctrine; round-tripping them is the primary source of
      compounding drift. |

      | Symmetric backward + forward compatibility | The ecosystem deletes
      legacy by design. Only forward migration + regeneration is consistent with
      that and far cheaper to maintain. |

      | Keep migrations ad hoc (status quo) | No continuity guarantee; a stale
      site is hand-migrated by reading RFCs. The registry + `migrator.validate`
      makes the path deterministic and enforceable. |

      | Pin each site to its original ecosystem version (freeze) | That is
      exactly the external `client.export` legacy-capsule path; it does not let
      an internal developer evolve the site on the current ecosystem, which is
      the whole goal here. |
    file: docs/rfcs/archive/implemented/rfc-0221-site-handoff-bundle-and-version-aware-absorb.md
  - kind: rejected-alternative
    rfcId: RFC-0222
    title: Generate and validate human-facing architecture docs
    status: implemented
    alternativesText: Keeping `docs/COMMANDS.md` fully hand-maintained was rejected
      because it repeatedly drifts during command rollout. Generating all README
      prose was rejected because prose carries human context; only closed
      architecture facts should be validated.
    file: docs/rfcs/archive/implemented/rfc-0222-generate-and-validate-human-facing-architecture-docs.md
  - kind: rejected-alternative
    rfcId: RFC-0223
    title: Search- and AI-ready material credit projection
    status: implemented
    alternativesText: >-
      - **Emit the license label as `ImageObject.license`.** Rejected: search
      engines treat `license` as a URL; a label there is noise and never yields
      the licensable treatment.

      - **Add a separate `provenance` block for AI fields.** Rejected: AI
      participants are already first-class parties; versioning them in place
      keeps one role chain.

      - **Implement C2PA now.** Rejected again for the same reason as RFC-0220:
      binary metadata mutation is a heavier, separately-owned phase.
    file: docs/rfcs/archive/implemented/rfc-0223-search-and-ai-ready-material-credit-projection.md
  - kind: rejected-alternative
    rfcId: RFC-0224
    title: Allow agents to stamp RFC implementation status
    status: implemented
    alternativesText: >-
      - **Keep all transitions human.** Rejected: it forces a human round-trip
      for pure completion bookkeeping that an agent can verify
      deterministically.

      - **Let agents perform any transition.** Rejected: acceptance, rejection,
      and supersession are design commitments that must stay human.

      - **Enforce the actor in `rfc.validate`.** Rejected for now: the lifecycle
      has no machine actor identity; documentation governance is the established
      mechanism and out of scope to change here.
    file: docs/rfcs/archive/implemented/rfc-0224-allow-agents-to-stamp-rfc-implementation-status.md
  - kind: rejected-alternative
    rfcId: RFC-0225
    title: Reposition warpgogol-com around owned digital assets for Handwerk
    status: implemented
    alternativesText: >-
      **Scenario A: one Handwerk focus only.** This is fast and clear, but
      leaves no visible architecture for later segment expansion.


      **Scenario B: four public segment doors now.** This uses the segmentation
      idea too early. Without signed cases in each segment, it risks making
      Warpgogol look like a generic agency for everyone.


      **Scenario C: one focus with modular expansion.** This is selected. It
      preserves launch focus, keeps the founder-scale workload realistic, and
      leaves a clear condition for future segment expansion.
    file: docs/rfcs/archive/implemented/rfc-0225-reposition-webgogol-com-around-owned-digital-assets-for-handwerk.md
  - kind: rejected-alternative
    rfcId: RFC-0226
    title: Write embedded content credentials from material credit records
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0226-write-embedded-content-credentials-from-material-credit-records.md
  - kind: rejected-alternative
    rfcId: RFC-0227
    title: Link material credit nodes into the page graph for licensable media
    status: implemented
    alternativesText: >-
      - **Keep islands, just add edges.** Rejected: leaving two emitters risks
      duplicate nodes for one asset; ownership must consolidate.

      - **Always emit primary-image and credit nodes separately and dedupe
      downstream.** Rejected: a shared `@id` at emission is simpler and
      verifiable than post-hoc de-duplication.

      - **Wait for RFC-0226 (embedded metadata) first.** Rejected: page-graph
      linking is independent and immediately useful for search; the two can land
      in either order.
    file: docs/rfcs/archive/implemented/rfc-0227-link-material-credit-nodes-into-the-page-graph-for-licensable-media.md
  - kind: rejected-alternative
    rfcId: RFC-0228
    title: Refine material credit coverage with editorial intent and prose authorship
    status: implemented
    alternativesText: >-
      - **Pure heuristic classification.** Rejected: heuristics alone repeat
      today's false positives; a conservative default plus human override is
      more honest.

      - **Separate decorative and prose into two RFCs.** Rejected: both are the
      same "which materials owe a credit" coverage question and share the
      validator/report surface.

      - **Suppress decorative via an ignore-list per app.** Rejected: an intent
      marker on the contract/record is reusable and self-documenting, unlike a
      per-app denylist.
    file: docs/rfcs/archive/implemented/rfc-0228-refine-material-credit-coverage-with-editorial-intent-and-prose-authorship.md
  - kind: rejected-alternative
    rfcId: RFC-0229
    title: Canonical breadcrumb trail for all pages including Programmatic Surface
    status: implemented
    alternativesText: >-
      - **Author breadcrumbs per page in `system.md`/frontmatter.** Rejected as
      the primary mechanism: it does not scale to thousands of generated PSEO
      pages and would drift from the URL hierarchy. An optional `parentPageId`
      override is kept for authored edge cases only.

      - **Keep two builders, add a parity check.** Rejected: two emitters for
      one fact is exactly today's defect; consolidation to one trail is simpler
      and makes parity structural rather than policed-after-the-fact.

      - **Derive crumb names from slug segments.** Rejected: slugs are not
      grammatical (`muenchen`, not `München`); titles already carry the correct,
      localized phrasing.

      - **Render breadcrumbs only when a site opts in via the planet
      declaration.** Rejected: the user requirement is universal ("all pages of
      all sites"); opt-in is the status quo that left PSEO bare. Authored
      opt-out remains possible by future policy if a site needs it.
    file: docs/rfcs/archive/implemented/rfc-0229-canonical-breadcrumb-trail-for-all-pages-including-programmatic-surface.md
  - kind: rejected-alternative
    rfcId: RFC-0230
    title: Add `share.i18n.lint` for UI-facing shared helpers
    status: implemented
    alternativesText: >-
      - **Expand `ui.i18n.lint` to scan `packages/share/src`.** Rejected: the UI
      command's name, RFC-0189 scope, and heuristics are renderer-oriented.
      `@gogol/share` needs different allowlists and target classification.

      - **Add a blanket no-human-strings rule for all of `@gogol/share`.**
      Rejected: too many legitimate schema, protocol, and diagnostic strings
      would create noisy failures and encourage broad suppressions.

      - **Fold this into `material.credits.validate`.** Rejected: material
      credits validation is app-scoped and content-record-focused. This is a
      workspace package hygiene rule.

      - **Rely on review comments.** Rejected: the failure is easy to miss
      because it looks like normal helper code and passes existing checks.
    file: docs/rfcs/archive/implemented/rfc-0230-add-share-i18n-lint-for-ui-facing-shared-helpers.md
  - kind: rejected-alternative
    rfcId: RFC-0231
    title: Unified attribution visibility policy for credits, prose authorship, and
      footer copyright
    status: implemented
    alternativesText: >-
      - **Keep per-section `showCredit` booleans.** Rejected: the hack already
      fragments across sections, each copy can null the credit and drop JSON-LD,
      and there is no site/page scope. This is the status quo the RFC exists to
      remove.

      - **Per-asset-only suppression (`display` in the sidecar, no other
      levels).** Rejected: the same asset legitimately renders differently in
      different placements (quiet in a dense hero, shown in an editorial
      section). Asset-only cannot express placement intent.

      - **A real "off" state that drops provenance.** Rejected by the founder
      decision: hiding must keep JSON-LD and the credits page (legal attribution
      + Google licensable metadata). If there is genuinely nothing to attribute,
      the answer is "no sidecar," not "off."

      - **Build on the RFC-0183 Feature Policy runtime.** Rejected:
      feature-policy is Phase-0 with known blockers; attribution visibility is
      small and closed and should not inherit that risk.

      - **A separate `attribution.display.validate` command.** Rejected to avoid
      command sprawl; folded into the existing `material.credits.validate` /
      `report`.
    file: docs/rfcs/archive/implemented/rfc-0231-unified-attribution-visibility-policy-for-credits-prose-and-copyright.md
  - kind: rejected-alternative
    rfcId: RFC-0232
    title: Render the material credits page as an optimized media gallery
    status: implemented
    alternativesText: >-
      - **Extend `markdown-section` for `contentRef: "prose/credits"`:**
      rejected — couples credits-specific media resolution to the generic
      markdown renderer, can only reach the `prose` domain today, and is less
      reusable across apps.

      - **Author media tokens/HTML into the generated prose:** rejected —
      duplicates filenames already derivable from the credit record and violates
      the no-HTML-in-`.md` rule.

      - **Drop the prose generator entirely:** deferred — keeping it as a no-JS
      / machine-readable fallback is low cost and avoids breaking any consumer
      that reads it.
    file: docs/rfcs/archive/implemented/rfc-0232-render-the-material-credits-page-as-an-optimized-media-gallery.md
  - kind: rejected-alternative
    rfcId: RFC-0233
    title: A holistic, extensible visual-control system for the site build
    status: implemented
    alternativesText: >-
      - **A new `@gogol/site-kernel-visual` package.** Cleaner isolation, but
      duplicates Diagnostic/renderer/loader infrastructure and adds a turborepo
      package to maintain. Rejected in favor of a domain inside
      `site-kernel-checks`.

      - **Fold everything into `app.qa.validate` (RFC-0074).** Fewer new
      entities, but mixes deterministic gating with non-deterministic LLM audits
      and gives the visual contract no enumerable registry. Rejected; instead
      VCS _feeds_ a subset into `app.qa.validate`.

      - **Hard-fail every visual finding like `css.important.lint`.** Satisfies
      "block the build," but Tier-2/3 heuristics would block client builds on
      false positives. Rejected in favor of severity-class gating.

      - **A new schema field to mark "last section" intent.** Avoids inference,
      but adds authoring burden and a field that can itself go stale. Rejected;
      position is inferred from existing fields + block order.
    file: docs/rfcs/archive/implemented/rfc-0233-visual-control-system-for-the-site-build.md
  - kind: rejected-alternative
    rfcId: RFC-0234
    title: iOS-playable video coverage and the build-time video-fallback guard
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0234-ios-playable-video-coverage-and-the-video-fallback-guard.md
  - kind: rejected-alternative
    rfcId: RFC-0235
    title: Egress text normalization to strip AI-authorship typographic signals from
      all public output
    status: implemented
    alternativesText: >-
      - **Global source find-replace / pre-commit rewrite of `src/content/`.**
      Rejected and explicitly excluded by the founder: destructive, loses author
      intent, and loses the battle against re-pasted content. Sources must stay
      pristine.

      - **A pure lint/gate (à la `content.voice.lint`) that blocks builds on
      signals.** Rejected as the primary mechanism: it is "control," forces
      manual remediation, and transforms nothing. We keep only a **warn-only**
      backstop.

      - **Dist-sweep only (no render/generator layers).** Rejected: misses
      `astro dev` parity and rasterized OG text, and re-parsing serialized
      HTML/JSON to swap quotes is riskier (escaping) than normalizing structured
      text before serialization. Hybrid uses render-time prevention to shrink
      what the sweep must fix.

      - **Source/render-level only (rehype + per-generator `normalizeText`).**
      Rejected as the sole mechanism: every future generator or new file type
      silently regresses coverage and there is no single proof of completeness.
      The dist adapter + backstop provide that proof.

      - **Just disable `smartypants`.** Insufficient: ignores author-pasted
      signals, HTML entities, generator-injected separators (the OG `—`),
      special spaces, and zero-width cruft.
    file: docs/rfcs/archive/implemented/rfc-0235-egress-text-normalization-to-strip-ai-authorship-typographic-signals.md
  - kind: rejected-alternative
    rfcId: RFC-0236
    title: "Generated-file guard: drift-validate, agent instructions, and source
      comments for codegen outputs"
    status: implemented
    alternativesText: >-
      - **Fail `build.prepare` if the credits file is edited.** Rejected:
      `build.prepare` runs `material.credits.generate`, which overwrites the
      file unconditionally. It is already self-healing. A gate there would block
      valid workflows where the developer edits the file as a scratchpad before
      fixing the YAML.

      - **Checksum or `.gitattributes` lock on the generated file.** Rejected:
      heavy tooling with poor agent readability. A prose check command plus a
      human-readable error message is more actionable.

      - **Single-layer fix (AGENTS.md only).** Insufficient: agents operate
      under constrained context windows and may not have loaded `AGENTS.md` when
      starting an edit. The in-file comment (Layer 3) and the CI gate (Layer 1)
      provide defence-in-depth without relying on prior context loading.
    file: docs/rfcs/archive/implemented/rfc-0236-generated-file-guard-drift-validate-and-agent-instructions.md
  - kind: rejected-alternative
    rfcId: RFC-0237
    title: Geo cascade ontology and the shared gogol geo package
    status: implemented
    alternativesText: >-
      - **Country slug = readable localized name (`deutschland`/`germany`), the
      doctrine R3 default.** Rejected by explicit client decision in favor of
      the stable, language-neutral alpha-3 (`deu`).

      - **Keep the gazetteer in each app.** Rejected: duplicates data across
      sites and breaks the thin-app rule; the doctrine explicitly allows holding
      countries/cities in `packages/*`.

      - **Hand-roll geo data as JSON in `@gogol/ontology`.** Rejected: ISO
      tables and city lists drift; maintained libraries (`i18n-iso-countries`,
      `iso-3166-2`, `country-state-city`) track the standards and ship localized
      names for free.

      - **Region slug = full ISO 3166-2 code (`de-bw`).** Rejected: the doctrine
      fixes the readable subdivision part (`bw`); the full code is retained as
      identity only.

      - **Runtime geocoding service.** Rejected: violates the offline-build and
      cookie-free/no-egress posture; all geo resolution is build-time and
      bundled.
    file: docs/rfcs/archive/implemented/rfc-0237-geo-cascade-ontology-and-the-shared-gogol-geo-package.md
  - kind: rejected-alternative
    rfcId: RFC-0238
    title: Website local v2 the geo demand cascade and Bedarfskarte content model
    status: implemented
    alternativesText: >-
      - **Keep demand as the deepest level of a separate `demand` Blueprint with
      its own budget (doctrine R1 alternative).** Rejected for now: a single
      five-axis tuple keeps hub-spoke linking and one budget simple; splitting
      later is a one-line Blueprint change.

      - **Add a `/zapyty/` (demands index) segment before the demand slug.**
      Rejected: extra depth with no value; the city page already lists its
      Bedarfskarten as children.

      - **Allow intent-modifier URLs (the "modifier matrix" expert proposal).**
      Rejected: violates anti-thin discipline; modifiers dilute, analyses deepen
      (doctrine §8.5, §9.9).

      - **District (Bezirk) as a sixth axis.** Deferred: districts are page
      content in v2; an axis can be added later by RFC if demand proves it
      (doctrine R4).
    file: docs/rfcs/archive/implemented/rfc-0238-website-local-v2-the-geo-demand-cascade-and-bedarfskarte-content-model.md
  - kind: rejected-alternative
    rfcId: RFC-0239
    title: Offer family for owned business services and removal of website service
      and posluha
    status: implemented
    alternativesText: >-
      - **Rename `/posluha/` instead of deleting it.** Rejected: the problem is
      semantic conflation, not naming; renaming would preserve the impersonation
      hazard.

      - **Keep a single `services` family serving both offers and demands.**
      Rejected: it is exactly the conflation the doctrine resolves; page types
      and JSON-LD voice differ.

      - **Emit 301 redirects from old `/posluha/` URLs.** Rejected by explicit
      client decision (no legacy, no backward compatibility); the surface is
      generated, so old URLs simply cease to exist.

      - **Allow `LocalBusiness` on Bodenstation offer pages.** Rejected: the
      studio is not the local provider of those services; `Service`/studio
      markup only (doctrine §8.2, §9.4).
    file: docs/rfcs/archive/implemented/rfc-0239-offer-family-for-owned-business-services-and-removal-of-website-service-and-posluha.md
  - kind: rejected-alternative
    rfcId: RFC-0240
    title: Modular productization of the Programmatic Surface via entitlements
    status: implemented
    alternativesText: >-
      - **One mega `pseo` entitlement covering offers and booking too.**
      Rejected: the client requires independently purchasable modules (doctrine
      §9.10); coarse gating prevents tariff differentiation.

      - **Rank-&-Rent (studio owns the lead stream and rents it).** Rejected
      (doctrine §9.11): contradicts the client-ownership model (`Übertragbares
      Eigentum`, `Notausgang`, no lock-in).

      - **Allow `aggregateRating` on Bodenstation with studio reviews.**
      Rejected: the studio is not the local provider of the mapped services;
      misleading + UWG risk.

      - **Per-site custom budgets in content.** Rejected: budgets are a tariff
      property resolved from Stripe tiers, not editable content, to keep
      productization consistent.
    file: docs/rfcs/archive/implemented/rfc-0240-modular-productization-of-the-programmatic-surface-via-entitlements.md
  - kind: rejected-alternative
    rfcId: RFC-0241
    title: HDRI as an external cited source with an identity firewall
    status: implemented
    alternativesText: >-
      - **Brand HDRI as a studio sub-project.** Rejected (doctrine §9.12):
      conflict of interest; weaker E-E-A-T; the point of HDRI is independence.

      - **Inline HDRI numbers without provenance.** Rejected: violates
      Anti-Fabrikation and CKL; numbers without source decay silently.

      - **A bespoke HDRI subsystem outside CKL.** Rejected: HDRI is just an
      external source; reusing CKL avoids a parallel provenance engine.

      - **Auto-enable Truth Monitor for HDRI.** Rejected: enabling external
      fetch is a human gate (RFC-0214), and HDRI releases are periodic, not
      streaming.
    file: docs/rfcs/archive/implemented/rfc-0241-hdri-as-an-external-cited-source-with-an-identity-firewall.md
  - kind: rejected-alternative
    rfcId: RFC-0242
    title: Bodenstation full dogfooding datasets for warpgogol com
    status: implemented
    alternativesText: >-
      - **Demo only to city level (doctrine R2 alternative).** Rejected by the
      chosen decision: the studio tests the full offer (incl. Bedarfskarten) on
      itself.

      - **`LocalBusiness` markup on Bodenstation to "look local".** Rejected:
      impersonation/doorway risk; the studio is not the local provider (doctrine
      §8.2–8.4).

      - **Synthetic placeholder Bedarfskarten to fill the cascade.** Rejected:
      violates the substance gate and Anti-Fabrikation; thin pages are dropped,
      not faked.

      - **Skip dogfooding and sell directly.** Rejected: the doctrine makes
      dogfooding the proof of the module.
    file: docs/rfcs/archive/implemented/rfc-0242-bodenstation-full-dogfooding-datasets-for-webgogol-com.md
  - kind: rejected-alternative
    rfcId: RFC-0243
    title: "Authored Offer Pillar: Replace PSEO-generated /leistungen/ with authored
      listing page"
    status: implemented
    alternativesText: >-
      - **Keep PSEO pillar and augment it with authored overrides.** Rejected
      because the depth-0 pillar is fundamentally a list projection; it cannot
      carry a curated narrative or cross-selling logic without turning into an
      authored page.

      - **Move authored offers to `/angebot/` and keep `/leistungen/` as PSEO.**
      Rejected because it splits the service namespace and forces visitors to
      learn two paths for one concept.

      - **Delete depth-1 offer Blueprint entirely.** Rejected because thin/geo
      detail pages (e.g. per-city local SEO) still need a generation surface;
      the Blueprint narrows but does not disappear.
    file: docs/rfcs/archive/implemented/rfc-0243-authored-offer-pillar-replace-pseo-leistungen-with-authored-listing-page.md
  - kind: rejected-alternative
    rfcId: RFC-0244
    title: Hierarchical demand records with path-derived slugs for multi-axis pSEO
      datasets
    status: implemented
    alternativesText: >-
      - **Keep flat folder + mandatory `slug`.** Rejected: does not solve the 2
      000-file ceiling or author navigation problem.

      - **Use sub-folders but keep `slug` mandatory.** Rejected: the path
      already carries the information; manual duplication is a drift risk.

      - **Derive slug from frontmatter fields (`industries[0]-city-slug`).**
      Rejected: the file system is the author-facing surface; frontmatter is
      machine-facing. Requiring authors to keep folder name and frontmatter in
      sync recreates the drift problem.

      - **Change the URL structure to include industry in the demand segment.**
      Rejected: the blueprint `{demand}` token maps to the leaf segment;
      changing URLs requires a blueprint RFC and breaks existing index.
    file: docs/rfcs/archive/implemented/rfc-0244-hierarchical-demand-records-with-path-derived-slugs.md
  - kind: rejected-alternative
    rfcId: RFC-0245
    title: Add an Agent Control Plane and maintenance debt ledger
    status: implemented
    alternativesText: Expanding `docs/COMMANDS.md` into a giant prose guide was
      rejected because agents need a structured manifest, not more hand-authored
      prose. Making every warning fail-hard was rejected because several checks
      are intentionally warn-first during CKL/material-credit rollout.
    file: docs/rfcs/archive/implemented/rfc-0245-add-an-agent-control-plane-and-maintenance-debt-ledger.md
  - kind: rejected-alternative
    rfcId: RFC-0246
    title: Harden Agent Control Plane workspace discovery and pipeline execution
    status: implemented
    alternativesText: >-
      Keeping `collectPackageDirectories()` hardcoded was rejected because every
      new workspace family would need a code edit and agents would not know the
      list is incomplete.


      Treating `packages.check` as an obsolete alias was rejected because it is
      already present in root configuration and therefore agent-visible. An
      advertised pipeline must either run or be removed by a separate accepted
      RFC.


      Keeping the epoch `generatedAt` without explanation was rejected because
      it optimizes diff stability at the cost of agent interpretation.
    file: docs/rfcs/archive/implemented/rfc-0246-harden-agent-control-plane-workspace-discovery-and-pipeline-execution.md
  - kind: rejected-alternative
    rfcId: RFC-0247
    title: Complete canonical warning diagnostics and maintenance debt coverage
    status: implemented
    alternativesText: >-
      Parsing warning strings from summaries inside `maintenance.debt.report`
      was rejected because it would bless the anti-pattern and force
      command-specific string parsing.


      Promoting every warning to a failing error was rejected because CKL,
      material credits, and rollout checks intentionally use warn-first phases.


      Extending only `diagnostic.shape.lint` was considered, but a separate
      `warning.diagnostics.lint` gives the repository a focused guard for the
      summary-only warning class while leaving RFC-0203 shape rules stable.
    file: docs/rfcs/archive/implemented/rfc-0247-complete-canonical-warning-diagnostics-and-maintenance-debt-coverage.md
  - kind: rejected-alternative
    rfcId: RFC-0248
    title: Unify content asset resolution and validation contracts
    status: implemented
    alternativesText: >-
      Duplicating the runtime resolver logic in the validator was rejected
      because it already drifted once and will drift again.


      Making validation trust Vite's `contentAssetImages` map directly was
      rejected because app-scoped static checks run against source files and
      should not require a full Astro/Vite build.


      Silently normalizing extensions was rejected because it hides authoring
      violations and trains agents to preserve invalid content.
    file: docs/rfcs/archive/implemented/rfc-0248-unify-content-asset-resolution-and-validation-contracts.md
  - kind: rejected-alternative
    rfcId: RFC-0249
    title: Establish autonomous package quality and CI gates
    status: implemented
    alternativesText: >-
      Relying on `pnpm test` alone was rejected because it cannot distinguish
      real tests from placeholders.


      Putting all logic directly into GitHub Actions was rejected because local
      agents need the same deterministic gate without reading CI YAML.


      Disabling unknown ESLint rule warnings was rejected because stale disable
      comments make agents believe a rule exists when it does not.


      Allowing `as any` at dynamic import boundaries was rejected for
      workspace-internal APIs. When a dynamic module lacks a type, the seam
      should use `unknown`, a type guard, or a narrow interface.
    file: docs/rfcs/archive/implemented/rfc-0249-establish-autonomous-package-quality-and-ci-gates.md
  - kind: rejected-alternative
    rfcId: RFC-0250
    title: Promote runtime section fallbacks and semantic targets to diagnostics
    status: implemented
    alternativesText: >-
      Keeping the runtime warnings and relying on humans to inspect build logs
      was rejected because the repository is explicitly optimized for autonomous
      agent maintenance.


      Making `resolveImage` throw on every missing image was rejected because
      some images are intentionally optional. The static validator must
      understand optionality and authored intent rather than making the
      low-level resolver fail hard.


      Keeping `"hero-1"` and `"donateContact"` as universal defaults was
      rejected because they are app-specific conventions from one site, not a
      package-level contract.
    file: docs/rfcs/archive/implemented/rfc-0250-promote-runtime-section-fallbacks-and-semantic-targets-to-diagnostics.md
  - kind: rejected-alternative
    rfcId: RFC-0251
    title: Establish test signal maturity and maintenance debt baselines
    status: implemented
    alternativesText: >-
      Making all absent/noop test signals fail immediately was rejected because
      it would block unrelated architecture work and force low-value placeholder
      tests.


      Ignoring no-op/absent tests after RFC-0249 was rejected because it would
      turn `test.signal.validate` into passive reporting instead of an
      improvement path.


      Failing on every current maintenance warning was rejected because many
      warnings are known rollout debt and content cleanup tasks.


      Keeping a manual markdown debt list was rejected because diagnostics are
      already structured; the baseline should be generated from the canonical
      ledger.
    file: docs/rfcs/archive/implemented/rfc-0251-establish-test-signal-maturity-and-maintenance-debt-baselines.md
  - kind: rejected-alternative
    rfcId: RFC-0252
    title: Validate RFC command lifecycle metadata
    status: implemented
    alternativesText: >-
      Editing the three current RFCs by hand without a validator was rejected
      because the same drift will recur.


      Removing `commands.proposed` after implementation was rejected because it
      loses proposal history. Keeping both `proposed` and `added` is more
      useful: proposed records design intent; added records implementation
      reality.


      Making `commands.added` mandatory for every implemented RFC was considered
      too strict because many RFCs change policy or content without adding
      commands.
    file: docs/rfcs/archive/implemented/rfc-0252-validate-rfc-command-lifecycle-metadata.md
  - kind: rejected-alternative
    rfcId: RFC-0253
    title: Share workspace discovery and parse CI workflows structurally
    status: implemented
    alternativesText: >-
      Keeping duplicate discovery functions was rejected because RFC-0246
      already established workspace discovery as a core Agent Control Plane
      invariant.


      Using a general glob library directly in each command was rejected because
      it repeats policy decisions about ignored directories, negative patterns,
      package kind classification, and deterministic ordering.


      Keeping `source.includes(command)` in `ci.local.validate` was rejected
      because it cannot distinguish comments, step names, documentation blocks,
      and actual commands.


      Executing the workflow locally to prove commands run was rejected as too
      heavy and platform-dependent. Structural parsing is sufficient for this
      RFC.
    file: docs/rfcs/archive/implemented/rfc-0253-share-workspace-discovery-and-parse-ci-workflows-structurally.md
  - kind: rejected-alternative
    rfcId: RFC-0254
    title: Quiet build/check logs with structured diagnostic output
    status: implemented
    alternativesText: >-
      Leaving raw console logs in place was rejected because noisy green builds
      train maintainers to ignore logs.


      Silencing fallback logs entirely was rejected because content fallback is
      part of the localization contract and must remain observable.


      Parsing raw log strings after the fact was rejected because it would
      preserve an unstable text channel instead of moving source modules to
      structured events.


      Making every fallback a warning was rejected because many fallback events
      are expected and contract-compliant.
    file: docs/rfcs/archive/implemented/rfc-0254-quiet-build-check-logs-with-structured-diagnostic-output.md
  - kind: rejected-alternative
    rfcId: RFC-0255
    title: Record pipeline step timing and timeout telemetry
    status: implemented
    alternativesText: >-
      Relying on GitHub Actions timestamps was rejected because local agents and
      app-level commands need the same view, and GitHub timing is too coarse for
      Site OS substeps.


      Increasing all timeouts was rejected because it hides the difference
      between slow and stuck.


      Persisting every timing run to committed files was rejected because timing
      is environment-dependent and would create useless churn.


      Profiling only Astro builds was rejected because Site OS generators and
      validators are equally important in this architecture.
    file: docs/rfcs/archive/implemented/rfc-0255-record-pipeline-step-timing-and-timeout-telemetry.md
  - kind: rejected-alternative
    rfcId: RFC-0256
    title: Plan advisory maintenance debt burn-down queues
    status: implemented
    alternativesText: >-
      Using only `maintenance.debt.triage.report` was rejected because triage
      groups are too broad for separate implementation sessions.


      Putting debt tasks into an external project-management tool was rejected
      because the repository must remain self-describing for agents and future
      maintainers.


      Editing the generated baseline by hand was rejected because the baseline
      is a generated acceptance artifact, not a plan.


      Fixing all accepted warnings in one cleanup was rejected because it mixes
      unrelated ownership, factual provenance, media rights, URL strategy, and
      source normalization concerns.


      Failing CI on all currently accepted debt was rejected because that would
      break unrelated client-site work. Queues allow deliberate burn-down
      without removing the guard against new debt.
    file: docs/rfcs/archive/implemented/rfc-0256-plan-advisory-maintenance-debt-burn-down-queues.md
  - kind: rejected-alternative
    rfcId: RFC-0257
    title: Adopt print-ready visitor pages and SSG PDF generation
    status: implemented
    alternativesText: >-
      1. **Separate `/print/` route.** Rejected because it doubles the URL
      space, creates duplicate content concerns, and requires app-local routing
      changes. The `?print` parameter keeps the print view on the same canonical
      URL.

      2. **Client-side PDF generation with libraries like `html2pdf.js` or
      `paged.js`.** Rejected because it requires shipping extra JS to every
      visitor, depends on browser capabilities, and produces inconsistent
      results. Build-time Playwright is deterministic and invisible to visitors.

      3. **Use Astro's `export` or `@astrojs/pdf` if available.** Rejected
      because there is no stable Astro-native PDF path that integrates with our
      custom section framework, motion gates, and content contract. Playwright
      is the stable, testable baseline.

      4. **Generate PDFs into `public/` before build.** Rejected because PDFs
      need the rendered HTML and CSS, which only exist after `astro build`.
      Writing into `public/` after build would require a second copy step and
      complicates the `dist/client` contract.

      5. **Use Puppeteer instead of Playwright.** Playwright was chosen because
      it is already the de-facto standard for browser automation in this
      codebase's test tooling (if added later) and has a single Chromium install
      command. The Image Provider Port and other OS commands do not conflict
      with Playwright.
    file: docs/rfcs/archive/implemented/rfc-0257-print-ready-visitor-pages-with-ssg-pdf-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0258
    title: Make workspace-shared file writes atomic and parallel-build safe
    status: implemented
    alternativesText: >-
      - **Cross-process lock files** (`proper-lockfile`): rejected — stale-lock
      recovery is its own failure class, and Windows lock semantics are
      unreliable. Convergent atomic writes need no mutual exclusion because
      outputs are deterministic (RFC-0087).

      - **`turbo run build --concurrency=1`**: rejected — punishes every build
      to mask a fixable defect.

      - **Moving all shared generation out of per-app pipelines**: partially
      adopted later by rfc-0259 (root turbo task). Standalone `pnpm --filter
      <app> build` still needs safe in-pipeline writes, so this primitive is
      required regardless.
    file: docs/rfcs/archive/implemented/rfc-0258-make-workspace-shared-file-writes-atomic-and-parallel-build-safe.md
  - kind: rejected-alternative
    rfcId: RFC-0259
    title: Align turbo task contracts with generated build artifacts
    status: implemented
    alternativesText: >-
      - **Exhaustively hand-listing outputs in turbo.json now**: rejected — the
      list is long, app-specific, and will drift silently; drift reintroduces
      the stale-artifact bug with higher confidence than `cache: false`.

      - **Keeping cache on and documenting the caveat**: rejected —
      documentation does not protect autonomous agents; the orchestrator
      contract must be mechanically honest.

      - **Moving all generated artifacts under `dist/`**: rejected — violates
      RFC-0049 and breaks Astro's copy model.
    file: docs/rfcs/archive/implemented/rfc-0259-align-turbo-task-contracts-with-generated-build-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0260
    title: Declare kernel command flags as typed schemas
    status: implemented
    alternativesText: >-
      - **Keep the global set, add a comment**: rejected — the failure mode is
      invisible to the agent editing a different file; comments do not gate.

      - **Adopt commander/yargs**: rejected — external CLI frameworks fight the
      kernel's registry/pipeline model and its RFC-0203 diagnostics; the needed
      parser is ~100 lines.

      - **Zod schemas for flags**: considered; rejected for the flag spec itself
      because rfc-0266 needs plain-JSON-serializable specs. **As-built:** the
      `ExecuteKernelCommandOptions`/`ExecuteKernelPipelineOptions` runtime guard
      also stayed dependency-free (a first-party Levenshtein nearest-key check)
      rather than adopting Zod, consistent with keeping the kernel parser
      first-party.
    file: docs/rfcs/archive/implemented/rfc-0260-declare-kernel-command-flags-as-typed-schemas.md
  - kind: rejected-alternative
    rfcId: RFC-0261
    title: Ratchet diagnostic quality and check fixture coverage
    status: implemented
    alternativesText: >-
      - **Big-bang migration of all 38 modules**: rejected — high-risk diff,
      blocks other work, and RFC-0256 queues exist precisely for this shape of
      debt.

      - **Deleting `resultFromViolations` immediately**: rejected — breaks 38
      modules at once; the ratchet gets the same end state safely.

      - **Coverage via c8/istanbul thresholds instead of fixture lint**:
      rejected — line coverage does not prove red/green behavior; a check can be
      100% covered by tests that never assert a failure.
    file: docs/rfcs/archive/implemented/rfc-0261-ratchet-diagnostic-quality-and-check-fixture-coverage.md
  - kind: rejected-alternative
    rfcId: RFC-0262
    title: Generate section prop types from manifest propsSchema
    status: implemented
    alternativesText: >-
      - **TypeScript types as source, JSON Schema generated**: rejected — the
      manifest YAML is already the machine-validated, CMS-facing,
      registry-consumed source (RFC-0023/0091); inverting would put the source
      of truth in a file agents must parse with a TS compiler.

      - **Zod as the single source**: rejected — propsSchema is consumed as
      plain JSON Schema by `page.block.validate` and external tooling;
      introducing a third representation deepens the problem.

      - **Production render-time validation**: rejected — build.check already
      gates deploys; paying Ajv cost on every static-build render adds build
      time for no new safety.

      - **`json-schema-to-typescript` as the emitter**: considered per the
      Design section; **as-built** uses a first-party recursive emitter instead
      (object/array/string/number/boolean/enum/const/oneOf/anyOf — the actual
      subset packages/ui manifests use), avoiding a new dependency and
      version-drift risk, consistent with the Design section's "or an equivalent
      deterministic first-party emitter" latitude. Likewise the dev
      `validateProps` hook uses a first-party structural checker rather than a
      lazy `ajv` import, avoiding a new @gogol/share runtime dependency;
      workspace-root discovery walks up from `process.cwd()` to
      `pnpm-workspace.yaml` (mirroring `getSectionPropsSchema`'s existing
      fs-based manifest reads) and every resolution failure is caught and
      logged, never thrown — only a genuine prop-shape violation throws
      `PAGE-PROPS-01`.
    file: docs/rfcs/archive/implemented/rfc-0262-generate-section-prop-types-from-manifest-propsschema.md
  - kind: rejected-alternative
    rfcId: RFC-0263
    title: Derive section numbering behavior from the archetype registry
    status: implemented
    alternativesText: >-
      - **A `hideSectionNumber` default inside each hero manifest's
      propsSchema**: rejected — the behavior is an interaction between the
      dispatcher and section numbering, not a per-instance authored prop;
      authors may still override per block.

      - **Keeping the set with a louder comment**: rejected — comments do not
      travel to the manifests where agents actually work.
    file: docs/rfcs/archive/implemented/rfc-0263-derive-section-numbering-behavior-from-the-archetype-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0264
    title: Split the share package barrel into subpath entry points
    status: implemented
    alternativesText: >-
      - **Splitting `@gogol/share` into multiple packages**: rejected for now —
      workspace-package proliferation has its own agent cost (more manifests,
      more turbo wiring); subpaths deliver the isolation without the overhead.
      May be revisited if a domain grows a genuinely independent lifecycle.

      - **Banning the root barrel entirely**: rejected — a thin stable core
      (`buildPage`, `RuntimeContext`, base schemas) is a legitimate convenience;
      the threshold keeps it honest.
    file: docs/rfcs/archive/implemented/rfc-0264-split-the-share-package-barrel-into-subpath-entry-points.md
  - kind: rejected-alternative
    rfcId: RFC-0265
    title: Enforce commit message hygiene for agent commits
    status: implemented
    alternativesText: >-
      - **husky commit-msg hook**: rejected as the primary mechanism — hooks are
      per-clone opt-in and this repo's agents commit from many environments; CI
      is the authoritative gate. A hook MAY be added later as a convenience.

      - **Off-the-shelf commitlint**: viable, but the narration rules
      (`COMMIT-03`) are custom anyway, the kernel already owns
      command/diagnostic infrastructure, and one less node toolchain dependency
      keeps the surface small.
    file: docs/rfcs/archive/implemented/rfc-0265-enforce-commit-message-hygiene-for-agent-commits.md
  - kind: rejected-alternative
    rfcId: RFC-0266
    title: Generate command surfaces from a single command manifest
    status: implemented
    alternativesText: >-
      - **Keep COMMANDS.md hand-maintained with a freshness check**: rejected —
      a checker that diffs prose against 363 commands is harder than generating
      the prose.

      - **Declare IO in a separate YAML catalog instead of the definition**:
      rejected — separating declaration from implementation recreates the
      KERNEL_BOOLEAN_FLAGS failure mode this series exists to kill.
    file: docs/rfcs/archive/implemented/rfc-0266-generate-command-surfaces-from-a-single-command-manifest.md
  - kind: rejected-alternative
    rfcId: RFC-0267
    title: Route kernel command IO through a WorkspaceIO port
    status: implemented
    alternativesText: >-
      - **Monkey-patching `node:fs` in the executor for enforcement**: rejected
      — global patching is fragile across ESM boundaries and punishes
      non-command code sharing the process.

      - **OS-level sandboxing (worker threads with restricted fs)**: rejected
      for now — heavy, platform-specific on Windows; the port gives 90% of the
      value and is a prerequisite for real sandboxing later anyway.

      - **Skipping the port and only declaring IO (rfc-0266 alone)**: rejected —
      declarations without enforcement rot into documentation.
    file: docs/rfcs/archive/implemented/rfc-0267-route-kernel-command-io-through-a-workspaceio-port.md
  - kind: rejected-alternative
    rfcId: RFC-0268
    title: Make RFC acceptance criteria machine-checkable
    status: implemented
    alternativesText: >-
      - **Executing probes inside `rfc.check`**: rejected — `rfc.check` verifies
      artifact existence cheaply across all RFCs; probes can run builds and
      belong behind an explicit per-RFC invocation.

      - **Free-form shell probes**: rejected — unaudited shell in frontmatter is
      an injection surface and platform-dependent; the closed vocabulary +
      site-kernel-only run commands keep probes safe and portable.

      - **Requiring probes on all new RFCs immediately**: rejected —
      policy/architecture RFCs often have genuinely prose-only criteria; forcing
      probes would produce theater probes.
    file: docs/rfcs/archive/implemented/rfc-0268-make-rfc-acceptance-criteria-machine-checkable.md
  - kind: rejected-alternative
    rfcId: RFC-0269
    title: Emit a golden behavior snapshot per app
    status: implemented
    alternativesText: >-
      - **Full-HTML golden files**: rejected — megabytes of noise, hash churn,
      unreviewable diffs; the structured projection is the reviewable core.

      - **CI-only comparison against the base branch (no committed file)**:
      rejected — requires building both branches in CI (slow) and gives local
      agents nothing; the committed-projection pattern is already the house
      style.

      - **Extending seo.structured-data.validate to cover everything**: rejected
      — a correctness validator encodes what MUST be; the snapshot detects what
      CHANGED, including things no rule owns yet.
    file: docs/rfcs/archive/implemented/rfc-0269-emit-a-golden-behavior-snapshot-per-app.md
  - kind: rejected-alternative
    rfcId: RFC-0270
    title: Derive pipeline timing budgets from telemetry
    status: implemented
    alternativesText: >-
      - **Committing raw telemetry**: rejected — non-deterministic,
      machine-specific, unbounded growth; only the aggregate is shared.

      - **Auto-tuning `timeoutMs` kill limits**: rejected — a corrupted or
      unlucky history could lower a kill limit and abort healthy builds; hard
      limits remain human-owned.

      - **CI-sourced budgets**: deferred — CI and dev-machine profiles differ;
      local budgets serve the local agent, and a CI dimension can be added to
      the record shape later (`schemaVersion` covers it).
    file: docs/rfcs/archive/implemented/rfc-0270-derive-pipeline-timing-budgets-from-telemetry.md
  - kind: rejected-alternative
    rfcId: RFC-0271
    title: Declare PSEO module contexts and authoring language
    status: implemented
    alternativesText: >-
      - **Use `defaultLanguage` as the master locale.** Rejected: routing
      fallback and editorial review language are different concepts.

      - **Declare master locale per Blueprint only.** Rejected: the same module
      needs shared entitlement, budget, source boundaries, and translator notes
      across Blueprints.

      - **One global app generation context.** Rejected: modules have different
      risk profiles, product promises, evidence requirements, and translation
      rules.
    file: docs/rfcs/archive/implemented/rfc-0271-declare-pseo-module-contexts-and-authoring-language.md
  - kind: rejected-alternative
    rfcId: RFC-0272
    title: Add PSEO artifact readiness and derived translation generation
    status: implemented
    alternativesText: >-
      - **Use `approved: true` as ready-for-translation.** Rejected: rendering
      approval and source-stability approval are different human decisions.

      - **Translate during normal build.** Rejected: this breaks RFC-0197
      determinism, makes CI cost unpredictable, and can ship provider failures
      as build failures.

      - **Auto-approve translations.** Rejected: target-language quality still
      needs review, especially for local SEO pages and legally sensitive claims.

      - **Translate prose-to-prose only.** Rejected: the translator must see the
      structured record and glossary so output can be validated against source
      facts in every language.
    file: docs/rfcs/archive/implemented/rfc-0272-add-pseo-artifact-readiness-and-derived-translation-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0273
    title: Generate approved translator notes per PSEO module language
    status: implemented
    alternativesText: >-
      - **One global translator note.** Rejected: modules differ in claims,
      tone, audience, and legal/product risk.

      - **Hard-code translator prompt in TypeScript.** Rejected: the
      translator's instructions need review, provenance, and version control.

      - **Let the translation model infer style from source only.** Rejected:
      local SEO pages need explicit fact preservation and anti-keyword-stuffing
      rules.

      - **Put terminology only in note prose.** Rejected: terms must be
      machine-readable so validators can catch drift and mistranslation.
    file: docs/rfcs/archive/implemented/rfc-0273-generate-approved-translator-notes-per-pseo-module-language.md
  - kind: rejected-alternative
    rfcId: RFC-0274
    title: Harden PSEO indexability with evidence and duplicate gates
    status: implemented
    alternativesText: >-
      - **Raise `substanceMin` only.** Rejected: a higher score still measures
      structure, not proof of page-specific value.

      - **Use LLM quality scoring.** Rejected: non-deterministic and hard to
      audit; LLMs may help generate content, not judge indexability.

      - **Synonym spinning.** Rejected: it creates noise and can worsen spam
      risk.

      - **Keep emitting every eligible tuple as noindex.** Rejected for deep
      pages: pages without evidence should usually not exist yet.
    file: docs/rfcs/archive/implemented/rfc-0274-harden-pseo-indexability-with-evidence-and-duplicate-gates.md
  - kind: superseded-rfc
    rfcId: RFC-0275
    title: Scale programmatic surface generation with record-driven sharded artifacts
    status: superseded
    closedAt: 2026-07-13
    decisionSummary: "Programmatic Surface generation moves to a **record-driven,
      sharded artifact architecture**:"
    file: docs/rfcs/archive/superseded/rfc-0275-scale-programmatic-surface-generation-with-record-driven-sharded-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0275
    title: Scale programmatic surface generation with record-driven sharded artifacts
    status: superseded
    alternativesText: >-
      - **Keep one giant JSON and raise CI memory.** Rejected: it delays the
      failure and makes local agent work unpleasant.

      - **Generate every route in Astro directly from records.** Rejected: it
      bypasses the route-source port and weakens validation artifacts.

      - **Use SSR for long-tail pages.** Rejected for this product stage: static
      delivery and deterministic artifacts are still core constraints.

      - **Optimize for 100k+ pages on one client site now.** Rejected: the
      product need is fleet-scale managed coverage, and editorial review remains
      the real bottleneck.
    file: docs/rfcs/archive/superseded/rfc-0275-scale-programmatic-surface-generation-with-record-driven-sharded-artifacts.md
  - kind: superseded-rfc
    rfcId: RFC-0276
    title: Introduce site Bordbuch as append-only state and mission ledger
    status: superseded
    closedAt: 2026-07-20
    decisionSummary: "Each managed site gains a **Bordbuch**: an append-only,
      hash-chained mission/event ledger plus generated noindex projections."
    file: docs/rfcs/archive/implemented/rfc-0276-introduce-site-bordbuch-as-append-only-state-and-mission-ledger.md
  - kind: rejected-alternative
    rfcId: RFC-0276
    title: Introduce site Bordbuch as append-only state and mission ledger
    status: superseded
    alternativesText: >-
      - **Use git log as Bordbuch.** Rejected: git tracks code commits, not site
      missions, generated artifact state, access events, or validation
      transitions.

      - **Use Quartalsbericht as the status page.** Rejected: Quartalsbericht is
      quarterly and client-readable; Bordbuch is continuous and operational.

      - **Use Stellarpass only.** Rejected: Stellarpass signs current state; it
      does not explain how the site arrived there.

      - **Build Leitstand first.** Rejected: fleet control should consume a
      per-site primitive, not invent its own hidden state store.
    file: docs/rfcs/archive/implemented/rfc-0276-introduce-site-bordbuch-as-append-only-state-and-mission-ledger.md
  - kind: rejected-alternative
    rfcId: RFC-0277
    title: Govern PSEO as managed visibility program with proof gates
    status: implemented
    alternativesText: >-
      - **Sell index budget directly.** Rejected: the platform does not control
      Google indexation and it creates destructive downgrade pressure.

      - **Keep PSEO as pure internal tooling forever.** Rejected as premature:
      the engine may become a valuable managed program after proof gates pass.

      - **Scale dogfood first and decide later.** Rejected: route count without
      demand and observability can create SEO risk before learning anything
      transferable to client sites.
    file: docs/rfcs/archive/implemented/rfc-0277-govern-pseo-as-managed-visibility-program-with-proof-gates.md
  - kind: rejected-alternative
    rfcId: RFC-0278
    title: Establish graduated PSEO autonomy levels and typed approval authority
    status: implemented
    alternativesText: >-
      - **Keep `approved: boolean` and add a config flag "aiApproves".**
      Rejected: a flag is not evidence; it flips trust without measuring it and
      cannot demote itself.

      - **One global autonomy setting per site.** Rejected: claims and
      structural blocks carry different risk; German legal copy cannot share a
      dial with teaser grids.

      - **Jump straight to AI-only and rely on the circuit-breaker.** Rejected:
      the breaker (RFC-0283) is a floor for damage, not a substitute for earned
      calibration; unproven autonomy would trip it constantly.

      - **Never remove humans (Fable's terminal stance).** Rejected as the
      _terminal_ design: it is the correct L0/L1 posture but contradicts the
      fleet-scale, decade-horizon goal; this RFC keeps Fable's caution as the
      _starting_ point of a governed climb.
    file: docs/rfcs/archive/implemented/rfc-0278-establish-graduated-pseo-autonomy-levels-and-typed-approval-authority.md
  - kind: rejected-alternative
    rfcId: RFC-0279
    title: Add an auditable AI reviewer as a governed approval gate
    status: implemented
    alternativesText: >-
      - **No AI reviewer; humans forever.** Rejected: it makes RFC-0278's upper
      levels unreachable and caps the fleet at review throughput.

      - **Let the generator self-grade.** Rejected: `REV-01`; a model grading
      its own output has no independent signal and games any threshold.

      - **Use only deterministic scores as the gate.** Rejected: they measure
      structure, not grounding/tone/register; Fable and RFC-0274 already show
      the substance score is necessary-not-sufficient.

      - **Single-shot, high-temperature judgment.** Rejected: unreproducible and
      uncalibratable; the harness (temp 0, self-consistency, closed schema,
      logging) is what makes the judge auditable.
    file: docs/rfcs/archive/implemented/rfc-0279-add-an-auditable-ai-reviewer-as-a-governed-approval-gate.md
  - kind: rejected-alternative
    rfcId: RFC-0280
    title: Ingest search-demand signals as a first-class surface record source
    status: implemented
    alternativesText: >-
      - **Keep demand as a human research step.** Rejected: it is the
      per-cluster analogue of per-artifact review and does not scale to a fleet.

      - **Infer demand from an LLM guess.** Rejected: unfounded volume estimates
      are worse than none; import measured data and mark gaps honestly.

      - **Call GSC/Keyword APIs during build.** Rejected: it breaks
      deterministic build and couples SSG to external availability and rate
      limits.

      - **Gate only on records, never on demand.** Rejected: this is exactly the
      failure Fable B1 names — pages with records but no searchers.
    file: docs/rfcs/archive/implemented/rfc-0280-ingest-search-demand-signals-as-a-first-class-surface-record-source.md
  - kind: rejected-alternative
    rfcId: RFC-0281
    title: Drive deep surface existence from Werk-Register evidence joins
    status: implemented
    alternativesText: >-
      - **Keep cartesian-then-filter, tighten the substance score.** Rejected: a
      higher score still measures structure; it cannot manufacture a real local
      reference.

      - **Generate synthetic "case studies" with an LLM.** Rejected: invented
      works are UWG exposure and destroy the trust thesis; only anchored real
      works qualify.

      - **Use demand alone as the existence key.** Rejected: demand says a page
      _could_ be read; evidence says a page _deserves_ to exist. The deepest
      pages need both (RFC-0280 + this RFC).

      - **Publish all Werke automatically.** Rejected: consent and redaction are
      mandatory; client ownership includes control over exposure.
    file: docs/rfcs/archive/implemented/rfc-0281-drive-deep-surface-existence-from-werk-register-evidence-joins.md
  - kind: rejected-alternative
    rfcId: RFC-0282
    title: Close the PSEO visibility feedback loop into automated cluster actions
    status: implemented
    alternativesText: >-
      - **Real-time dashboard.** Rejected: the decision cadence is weekly, not
      per-second; versioned snapshots are auditable and diffable, a live
      dashboard is not.

      - **Enrich everything up front, measure later.** Rejected: Fable §E.6; it
      spends the expensive resource (review/enrichment) before any signal
      justifies it.

      - **Let humans read GSC and decide.** Rejected: that is the per-cluster
      throughput trap; the loop must be machine-readable so the fleet can act.

      - **Auto-delete non-performers.** Rejected: violates RFC-0277 URL
      non-destruction; retirement is quality-gated with redirects, not silent
      deletion.
    file: docs/rfcs/archive/implemented/rfc-0282-close-the-pseo-visibility-feedback-loop-into-automated-cluster-actions.md
  - kind: rejected-alternative
    rfcId: RFC-0283
    title: Add an automated safety circuit-breaker and surface rollback
    status: implemented
    alternativesText: >-
      - **Rely on humans watching GSC.** Rejected: too slow for one site,
      impossible for a fleet; the harm compounds before anyone looks.

      - **Rollback by deleting the surface.** Rejected: violates RFC-0277 URL
      non-destruction and discards accumulated link equity; rollback restores a
      good state, it does not scorch earth.

      - **Treat the breaker as the quality gate.** Rejected: it is a damage
      floor that fires on realized harm; quality is earned upstream via
      calibration (RFC-0278/0279). Both are needed.

      - **Manual rollback runbook only.** Rejected: an unautomated reflex is not
      a reflex; autonomy requires the reversal to be as automatic as the
      expansion.
    file: docs/rfcs/archive/implemented/rfc-0283-add-an-automated-safety-circuit-breaker-and-surface-rollback.md
  - kind: rejected-alternative
    rfcId: RFC-0284
    title: Introduce the fleet Leitstand as an autonomous cross-site control plane
    status: implemented
    alternativesText: >-
      - **Build the Leitstand as the primary state store.** Rejected: it would
      duplicate per-site Bordbuch/autonomy/breaker and create drift; the plane
      consumes primitives, it does not own them.

      - **No control plane; run sites individually.** Rejected: manual per-site
      operation is the fleet-scale throughput trap and has no place for a global
      kill-switch or budget.

      - **Let the Leitstand approve content centrally.** Rejected: approval
      authority is per scope (RFC-0278); centralizing it would erase field-class
      and locale nuance and concentrate risk.

      - **Wait for RFC-0179 hosting first.** Rejected: control-plane
      observability and scheduling are useful before multi-tenant runtime and
      inform its requirements.
    file: docs/rfcs/archive/implemented/rfc-0284-introduce-the-fleet-leitstand-as-an-autonomous-cross-site-control-plane.md
  - kind: rejected-alternative
    rfcId: RFC-0285
    title: Govern human involvement as a measured, shrinking escalation budget
    status: implemented
    alternativesText: >-
      - **Keep human review as a standing default.** Rejected: it is the
      throughput ceiling (Fable §B6) and never measures itself, so it can
      neither shrink nor prove the AI-only goal.

      - **Cap human review by headcount only.** Rejected: a raw cap without
      feedback produces rubber-stamping; the budget must be coupled to
      calibration so it shrinks by getting _better_, not by getting _lazier_.

      - **Auto-approve when the queue is full.** Rejected: exhaustion must
      block, never bypass; `ESC-01` is a hard error.

      - **Track effort but not require feedback.** Rejected: an intervention
      that does not reduce future interventions is pure cost; `ESC-02` makes the
      learning loop mandatory.
    file: docs/rfcs/archive/implemented/rfc-0285-govern-human-involvement-as-a-measured-shrinking-escalation-budget.md
  - kind: rejected-alternative
    rfcId: RFC-0286
    title: Establish the agent surface with one capability manifest and protocol
      projections
    status: implemented
    alternativesText: >-
      - **Hand-built REST + MCP servers per site.** Rejected: N sites × 2
      servers of drifting hand-written surface violates thin-site DNA and
      becomes unmaintainable legacy at fleet scale; exactly the failure AS-1
      exists to prevent.

      - **MCP as a wrapper proxying a REST layer.** Rejected as an architectural
      statement (accepted as an implementation detail): making REST the "real"
      API couples the surface's lifetime to one protocol generation. Both are
      sibling projections; the manifest is the API.

      - **Runtime read API (SSR endpoints serving business data).** Rejected:
      business data changes at build time; static files give identical semantics
      with zero runtime cost/risk across thousands of sites (AS-6, DNA-01).

      - **A GraphQL surface.** Rejected: the consumers are AI agents speaking
      HTTP+JSON and MCP; a query language adds server runtime and schema-drift
      surface with no consumer demand.

      - **Waiting for protocol convergence before building anything.** Rejected:
      the knowledge tier and discovery are protocol-neutral and valuable today;
      AS-5/AS-7 make later protocol churn cheap by construction.
    file: docs/rfcs/archive/implemented/rfc-0286-establish-the-agent-surface-with-one-capability-manifest-and-protocol-projections.md
  - kind: rejected-alternative
    rfcId: RFC-0287
    title: Project the business layer as static agent knowledge files
    status: implemented
    alternativesText: >-
      - **Serve JSON-LD as the knowledge tier.** Rejected: schema.org vocabulary
      is optimized for search-engine entity reconciliation, not tool
      consumption; agents would still need mapping code. JSON-LD remains for
      search; knowledge files are typed and flat.

      - **One combined knowledge.json.** Rejected: per-domain files keep
      responses small, let `knowledgeDisabled` withhold selectively, and align
      cache/freshness granularity with the domain model.

      - **Per-language files (`/de/offer.json`).** Rejected: the `byLanguage`
      map inside one file mirrors RFC-0008 overlay semantics exactly and spares
      agents a second fetch to learn language coverage.

      - **Committing the files (like twins).** Rejected: twins are
      content-reviewable prose; knowledge files are pure derivations whose bytes
      depend on entitlement-resolved builds. Gitignored-generated matches
      `surface.generated.json` precedent.
    file: docs/rfcs/archive/implemented/rfc-0287-project-the-business-layer-as-static-agent-knowledge-files.md
  - kind: rejected-alternative
    rfcId: RFC-0288
    title: Introduce the closed capability catalog for agent actions
    status: implemented
    alternativesText: >-
      - **Per-app capability files.** Rejected: N sites × drift; the
      closed-vocabulary pattern (growth events) is the ecosystem's proven
      answer, and cross-site interop (RFC-0292) depends on shared ids meaning
      the same thing everywhere.

      - **Deriving actions implicitly from rendered sections (no catalog).**
      Rejected: implicit derivation cannot carry typed I/O schemas, versions, or
      limits, and gives protocols nothing stable to project.

      - **Full JSON Schema for input/output.** Rejected: unbounded schema
      features fracture across OpenAPI/MCP projections and invite validator
      divergence; the closed subset is bijectively projectable.

      - **A generic `agent.capability.invoke` single endpoint.** Rejected for
      the declaration layer: one URL per action keeps OpenAPI honest, rate
      limits per-verb, and logs legible. (The gate may still route internally
      through one handler — an RFC-0290 implementation detail.)
    file: docs/rfcs/archive/implemented/rfc-0288-introduce-the-closed-capability-catalog-for-agent-actions.md
  - kind: rejected-alternative
    rfcId: RFC-0289
    title: Generate an OpenAPI projection of the agent surface
    status: implemented
    alternativesText: >-
      - **Adopt `openapi-types` / swagger libraries.** Rejected: the projected
      subset is small and closed; a local structural type plus fixtures is
      cheaper than a dependency treadmill on a decades horizon.

      - **YAML output.** Rejected: JSON matches every other generated artifact,
      diffing, and hashing; OpenAPI consumers accept JSON universally.

      - **Documenting the whole site API (webhooks, form endpoint).** Rejected:
      those are private contracts between the site and its vendors; publishing
      them invites abuse and freezes internals (nonGoal).

      - **Serving at `/openapi.json` root.** Rejected: `.well-known/` co-locates
      all machine-identity artifacts (passport, agent.json, bordbuch) — one
      directory to secure, sign, and reason about.
    file: docs/rfcs/archive/implemented/rfc-0289-generate-an-openapi-projection-of-the-agent-surface.md
  - kind: rejected-alternative
    rfcId: RFC-0290
    title: Add the agent gate runtime with a stateless MCP endpoint
    status: implemented
    alternativesText: >-
      - **`@modelcontextprotocol/sdk`.** Rejected: the SDK targets the full
      protocol (sessions, SSE, notifications) and moves fast; the stateless
      subset is a few hundred lines against a pinned spec with fixtures. Owning
      it removes a supply-chain and churn dependency from a thousand-site fleet.
      Revisit only if the needed subset grows stateful.

      - **Hosted/central MCP service for all sites.** Rejected: violates
      per-client isolation (RFC-0176/0177) — the studio must never operate a
      cross-tenant funnel for client leads; and it breaks the "client leaves
      with a working site" portability commitment.

      - **MCP endpoint as a separate Worker.** Rejected: doubles the per-site
      Worker count against the RFC-0179 ceiling for no isolation gain (same
      account, same tokens).

      - **stdio MCP / packaged connector.** Rejected: sites are remote assets;
      remote Streamable HTTP is the only transport that serves external agents
      without installing anything.

      - **One generated file per action route.** Rejected in favor of the single
      dynamic `[id].ts`: fewer generated files, identical behavior, and the
      manifest — not the file tree — remains the authority (AS-1).
    file: docs/rfcs/archive/implemented/rfc-0290-add-the-agent-gate-runtime-with-a-stateless-mcp-endpoint.md
  - kind: rejected-alternative
    rfcId: RFC-0291
    title: Establish trust and abuse controls for the agent surface
    status: implemented
    alternativesText: >-
      - **A dedicated agent-surface keypair.** Rejected: two identities to
      rotate, publish, and explain; the passport key is already the site's
      public identity anchor.

      - **JWS/JOSE envelope for agent.json.** Rejected: the passport already
      established raw Ed25519 + multibase + canonical bytes; consistency beats
      format plurality, and verification stays dependency-free.

      - **Durable Object / KV-backed exact rate limiting.** Rejected for v1:
      adds bindings and cost per site for marginal gain over in-isolate limiting
      + platform WAF phase 2; revisit if real abuse data demands exactness.

      - **Requiring signed agent identity to invoke actions.** Rejected:
      adoption of Web Bot Auth is early; requiring it today zeroes the action
      tier's utility. Observe-first, enforce when the ecosystem matures.

      - **Turnstile on action routes.** Rejected: liveness tests select for
      humans — the opposite of this surface's purpose.
    file: docs/rfcs/archive/implemented/rfc-0291-establish-trust-and-abuse-controls-for-the-agent-surface.md
  - kind: rejected-alternative
    rfcId: RFC-0292
    title: Federate agent surfaces across the fleet
    status: implemented
    alternativesText: >-
      - **Runtime registry service (workers-hosted discovery API).** Rejected: a
      service in the discovery path is an availability and trust liability, and
      the first step down the central-hub slope; a generated file serves every
      current consumer.

      - **Network-fetching each site's deployed agent.json at generate time.**
      Rejected: nondeterministic, environment-coupled, and wrong layer — the
      repo builds the truth; deployed-vs-built drift is what
      `agent.manifest.verify --url` (RFC-0291) checks per site.

      - **Folding the catalog into `fleet.status.generated.json`.** Rejected:
      status is operational telemetry with its own churn cadence; the agent
      catalog is structural and diff-reviewed — separate files keep diffs
      legible.

      - **Special intra-fleet invocation channel (service bindings between
      sibling sites).** Rejected: siblings-as-external-consumers keeps one code
      path, preserves portability (a departing site loses nothing), and honors
      isolation.
    file: docs/rfcs/archive/implemented/rfc-0292-federate-agent-surfaces-across-the-fleet.md
  - kind: rejected-alternative
    rfcId: RFC-0293
    title: Establish check-warpgogol-com as a URL-first quality product
    status: implemented
    alternativesText: >-
      - **Source-first validator for WGogol apps.** Rejected: useful internally,
      but duplicates existing validators and does not become a product for
      arbitrary sites.

      - **Make `check-warpgogol-com` itself run browser checks in production.**
      Rejected for the first implementation: Cloudflare Worker request paths are
      not a safe place for Playwright or long crawls.

      - **Third-party SaaS checker integration.** Rejected as the spine.
      External APIs may be adapters later, but the core evidence graph and
      diagnostics must be first-party and reproducible.
    file: docs/rfcs/archive/implemented/rfc-0293-establish-check-webgogol-com-as-a-url-first-quality-product.md
  - kind: rejected-alternative
    rfcId: RFC-0294
    title: Capture a rendered site evidence graph from URL targets
    status: implemented
    alternativesText: >-
      - **Let each validator crawl on demand.** Rejected: non-reproducible and
      unsafe.

      - **Only use static HTML fetches.** Rejected: many deployed failures
      appear only after rendering and responsive layout.

      - **Store HAR files wholesale.** Rejected for MVP: too large and
      leak-prone. Keep a structured network summary instead.
    file: docs/rfcs/archive/implemented/rfc-0294-capture-a-rendered-site-evidence-graph-from-url-targets.md
  - kind: rejected-alternative
    rfcId: RFC-0295
    title: Publish optional Warpgogol check hints in .well-known
    status: implemented
    alternativesText: >-
      - **Let the checker read repository source for WGogol targets.** Rejected:
      breaks URL-first truth and cannot work for already-deployed external
      targets.

      - **Put this data into `agent.json`.** Rejected: agent capabilities and
      repair hints have different consumers and privacy posture.

      - **Expose full block props.** Rejected: unnecessary and leaks more than
      the checker needs.
    file: docs/rfcs/archive/implemented/rfc-0295-publish-optional-webgogol-check-hints-in-well-known.md
  - kind: rejected-alternative
    rfcId: RFC-0296
    title: Run URL checks through a Node renderer and artifact store
    status: implemented
    alternativesText: >-
      - **Run checks inside the deployed app.** Rejected for MVP due to
      browser/runtime limits.

      - **Immediately build a remote runner service.** Rejected: local and CI
      modes are enough to dogfood alt deployments, and the artifact contract
      preserves the upgrade path.

      - **Store runs in app source.** Rejected: screenshots/reports are
      generated artifacts and often target-specific.
    file: docs/rfcs/archive/implemented/rfc-0296-run-url-checks-through-a-node-renderer-and-artifact-store.md
  - kind: rejected-alternative
    rfcId: RFC-0297
    title: Standardize check diagnostics, reports, and agent action packs
    status: implemented
    alternativesText: >-
      - **HTML report only.** Rejected: humans need HTML, agents need
      deterministic JSON.

      - **Use maintenance debt queues directly.** Rejected for MVP: external
      sites have no repository queue; action packs are portable.

      - **Let LLM write the whole action pack.** Rejected: grouping can use
      deterministic rules; AI can contribute suggested text but not own the
      schema.
    file: docs/rfcs/archive/implemented/rfc-0297-standardize-check-diagnostics-reports-and-agent-action-packs.md
  - kind: rejected-alternative
    rfcId: RFC-0298
    title: Add deterministic URL-first quality checks
    status: implemented
    alternativesText: >-
      - **Run Lighthouse only.** Rejected: useful but too broad and not tailored
      to multilingual business presence.

      - **Only use source validators for WGogol sites.** Rejected by RFC-0293.

      - **Put all deterministic rules in one command.** Rejected: focused
      commands make debugging and CI output clearer.
    file: docs/rfcs/archive/implemented/rfc-0298-add-deterministic-url-first-quality-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0299
    title: Add a cached AI audience review lane for rendered sites
    status: implemented
    alternativesText: >-
      - **Ask an LLM to review the live URL directly.** Rejected: uncontrolled
      crawling, unstable evidence, and weak auditability.

      - **Make AI score a deploy gate immediately.** Rejected: subjective and
      uncalibrated; warning-first.

      - **One universal audience.** Rejected: business presence quality depends
      on audience context.
    file: docs/rfcs/archive/implemented/rfc-0299-add-a-cached-ai-audience-review-lane-for-rendered-sites.md
  - kind: rejected-alternative
    rfcId: RFC-0300
    title: Create the check-warpgogol-com operator app
    status: implemented
    alternativesText: |-
      - **No app; CLI only.** Rejected: product needs a human-facing surface.
      - **Put the runner in the app.** Rejected by RFC-0296.
      - **Build outside apps/\*.** Rejected: dogfooding the ecosystem matters.
    file: docs/rfcs/archive/implemented/rfc-0300-create-the-check-webgogol-com-operator-app.md
  - kind: rejected-alternative
    rfcId: RFC-0301
    title: Gate deploy:main on alt URL checks
    status: implemented
    alternativesText: >-
      - **Run checks before deploy:alt.** Rejected: URL-first checks need the
      deployed target.

      - **Block on AI warnings from day one.** Rejected: subjective findings
      need calibration.

      - **Merge into existing deploy command.** Rejected: separate gate command
      is easier to run and debug.
    file: docs/rfcs/archive/implemented/rfc-0301-gate-deploy-main-on-alt-url-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0302
    title: Govern external site check safety, privacy, and consent
    status: implemented
    alternativesText: >-
      - **Trust the user-provided URL.** Rejected: redirects and links can leave
      the intended host.

      - **Disable screenshots by default.** Rejected for value; screenshots are
      core evidence, but policy and redaction must govern them.

      - **Allow AI review by default.** Rejected: sending website
      text/screenshots to a model is a separate consent decision.
    file: docs/rfcs/archive/implemented/rfc-0302-govern-external-site-check-safety-privacy-and-consent.md
  - kind: rejected-alternative
    rfcId: RFC-0303
    title: Consolidate shared fs/text helpers and split oversized OS source files
    status: implemented
    alternativesText: >-
      - **Put helpers in `@gogol/site-kernel`.** Rejected: `@gogol/site-kernel`
      depends on `@gogol/site-kernel-content`, so `content`'s
      `collectMarkdownFiles` could not reuse a kernel-hosted walker without a
      cycle. `@gogol/share` is the only universal, lower-level home.

      - **A new dedicated `@gogol/kernel-fs` package.** Rejected: package
      proliferation has real agent cost (manifests, turbo wiring) — the same
      trade-off RFC-0264 rejected. A server-only subpath delivers isolation
      without a new workspace package.

      - **Split oversized files into new workspace packages.** Rejected: they
      are cohesive command groups; folder-of-files with a thin re-export shim
      keeps the public surface identical at far lower risk.

      - **A codemod that auto-inlines a shared walker.** Rejected: the value is
      _removing_ copies, not templating them; a lint that forbids the copy is
      the durable fix.
    file: docs/rfcs/archive/implemented/rfc-0303-consolidate-shared-fs-helpers-and-split-oversized-os-files.md
  - kind: rejected-alternative
    rfcId: RFC-0304
    title: Introduce backs workspaces and the Check Warpgogol runner backend
    status: implemented
    alternativesText: >-
      - **Put backend workers under `packages/*`.** Rejected because packages
      are reusable logic, not deployment compositions.

      - **Put the runner inside `apps/check-warpgogol-com`.** Rejected because
      Playwright/Chromium must not run inside the Astro request path.

      - **Use `workers/*` instead of `backs/*`.** Rejected because the ecosystem
      will need non-edge backends such as Node runners and customer integration
      processes.

      - **Choose Cloudflare Queues/R2/D1 immediately.** Deferred. The local
      store proves the product contract first; cloud adapters can follow without
      changing UI or runner contracts.
    file: docs/rfcs/archive/implemented/rfc-0304-introduce-backs-workspaces-and-check-webgogol-runner-backend.md
  - kind: rejected-alternative
    rfcId: RFC-0305
    title: Connect Matomo through a first-party analytics proxy and Messkanon
    status: implemented
    alternativesText: >-
      - **Keep RFC-0170 and only add `disableBrowserFeatureDetection`.**
      Rejected. It leaves direct Matomo Cloud traffic, generic event mapping, no
      provisioning, no proxy, and no fleet operations.

      - **Paste a Matomo snippet into each Astro app.** Rejected. It violates
      thin-app composition, makes dimensions and goals app-local, and creates
      instant drift across the fleet.

      - **Use Matomo Tag Manager as the stable integration surface.** Rejected
      for v1. It adds a second configuration authority outside Git, consumes
      container limits, and encourages UI-managed analytics changes.

      - **Do not proxy and accept ad-blocker undercount.** Rejected by owner
      decision. First-party proxying is required for the production topology,
      bounded by the privacy controls in this RFC.

      - **One Matomo siteId for the whole fleet.** Rejected. It destroys
      per-client export, access boundaries, reporting, and silence detection.

      - **One Matomo instance per client.** Rejected for v1. It maximizes
      legal/account isolation but makes onboarding, reporting, and cost control
      too heavy for thin sites.

      - **Give every client a Matomo login.** Rejected as the default. Standard
      Cloud user limits and the product's report-page model both point to
      report/export-first access.

      - **Track all current `@warpgogol/growth` events in Matomo.** Rejected.
      The thin-site baseline measures contact outcomes, not every growth/system
      event.
    file: docs/rfcs/archive/implemented/rfc-0305-connect-matomo-through-first-party-analytics-proxy-and-messkanon.md
  - kind: rejected-alternative
    rfcId: RFC-0306
    title: Switch per-page Markdown twins from /route/index.md to sibling /route.md
      URLs
    status: implemented
    alternativesText: >-
      - **Keep both schemes (emit `/route.md` AND `/route/index.md`):** rejected
      — the decision is no legacy and no back-compat; double emission doubles
      artifacts and invites drift.

      - **Server redirect `/route/index.md` → `/route.md`:** rejected —
      build-time SSG, no runtime route (RFC-0166), and nothing external links
      the old URLs yet.

      - **Detect home pages via a `page.isHome` flag instead of the language
      set:** rejected as the primary rule because the layout and PSEO surface do
      not always carry that flag, whereas the supported-language set is
      available everywhere; the structural rule is total and deterministic.

      - **Glob `"/*.md"` for the agent manifest:** rejected — it implies
      single-level only; `"/**.md"` covers nested twins.
    file: docs/rfcs/archive/implemented/rfc-0306-switch-per-page-markdown-twins-to-sibling-dot-md-urls.md
  - kind: rejected-alternative
    rfcId: RFC-0307
    title: Establish a generated public artifact readiness contract
    status: implemented
    alternativesText: >-
      - **Patch `apps/warpgogol-com/public/` directly.** Rejected. The audit
      looked at one snapshot, but the platform needs a rule for every current
      and future app.

      - **Put every check into one deploy-only probe.** Rejected. Local
      generated-artifact errors should fail before deployment; only true runtime
      behavior belongs in network probes.

      - **Delete declarations when files are missing.** Rejected as a default.
      Missing declarations are sometimes generator bugs, not optional features.
    file: docs/rfcs/archive/implemented/rfc-0307-establish-generated-public-artifact-readiness-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0308
    title: Sign agent surface and knowledge artifacts
    status: implemented
    alternativesText: >-
      - **Keep `proof: null` until RFC-0291.** Rejected. Artifact authenticity
      is independent from request authentication and can be finished earlier.

      - **Sign raw JSON bytes.** Rejected. Pretty-printing and key order would
      make signatures brittle.

      - **Publish per-artifact sidecar signatures only.** Rejected for v1
      because embedded proof keeps the verification path self-contained for
      agents.
    file: docs/rfcs/archive/implemented/rfc-0308-sign-agent-surface-and-knowledge-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0309
    title: Generate the installable icon and webmanifest suite for every site
    status: implemented
    alternativesText: >-
      - **Keep only SVG favicon.** Rejected because it does not satisfy
      mobile/installability surfaces.

      - **Commit hand-made PNGs per app.** Rejected because the owner decision
      is generation-first.

      - **Require bespoke brand design before generating files.** Rejected. A
      deterministic v1 seed is acceptable; completeness of the public contract
      is the immediate goal.
    file: docs/rfcs/archive/implemented/rfc-0309-generate-installable-icon-and-webmanifest-suite.md
  - kind: rejected-alternative
    rfcId: RFC-0310
    title: Generate shared biome-aware 404 pages for every site
    status: implemented
    alternativesText: >-
      - **Let every app hand-author 404.** Rejected; apps are composition-only.

      - **Serve a generic unstyled platform 404.** Rejected; the owner asked for
      a professional page that mimics each site's biome.

      - **Implement search/suggestions in v1.** Deferred; the first contract is
      status correctness and clear navigation.
    file: docs/rfcs/archive/implemented/rfc-0310-generate-shared-biome-aware-404-pages.md
  - kind: rejected-alternative
    rfcId: RFC-0311
    title: Submit changed site URLs to IndexNow after deploy
    status: implemented
    alternativesText: >-
      - **Generate random per-site keys.** Rejected by owner decision; the key
      is deterministic from app id.

      - **Submit every public URL.** Rejected; IndexNow receives canonical HTML
      pages only.

      - **Run live submission during local builds.** Rejected; network
      submission belongs after deploy.
    file: docs/rfcs/archive/implemented/rfc-0311-submit-indexnow-after-deploy.md
  - kind: rejected-alternative
    rfcId: RFC-0312
    title: Generate humans.txt from team, credits, and site metadata
    status: implemented
    alternativesText: >-
      - **Skip humans.txt because it is optional.** Rejected; it is a cheap
      transparency artifact aligned with the platform's credits posture.

      - **Paste full credits into humans.txt.** Rejected; link to the generated
      credits page instead.

      - **Infer missing team data.** Rejected; generators must not invent people
      or roles.
    file: docs/rfcs/archive/implemented/rfc-0312-generate-humans-txt-from-team-and-credits.md
  - kind: rejected-alternative
    rfcId: RFC-0313
    title: Declare an open AI crawler and training policy for studio sites
    status: implemented
    alternativesText: >-
      - **Emit `tdmrep.json` by default.** Rejected because it reserves rights,
      opposite to the owner decision.

      - **Leave AI policy informal.** Rejected; future agents need a stable
      default.

      - **Enumerate every AI crawler user agent.** Rejected for v1; the policy
      statement and robots consistency are enough.
    file: docs/rfcs/archive/implemented/rfc-0313-declare-open-ai-crawler-and-training-policy.md
  - kind: rejected-alternative
    rfcId: RFC-0314
    title: Standardize security.txt and vulnerability disclosure for every site
    status: implemented
    alternativesText: >-
      - **Keep vulnerability contact only on the website.** Rejected;
      `security.txt` is the standard discovery path.

      - **Set a very long expiry.** Rejected; stale security contacts are worse
      than explicit renewal.

      - **Require signed security.txt in v1.** Deferred; the immediate gap is
      the missing contact file.
    file: docs/rfcs/archive/implemented/rfc-0314-standardize-security-txt-and-vulnerability-disclosure.md
  - kind: rejected-alternative
    rfcId: RFC-0315
    title: Standardize HTTP security headers and public runtime probes
    status: implemented
    alternativesText: >-
      - **Only check headers manually with curl.** Rejected; manual checks do
      not scale across apps.

      - **Use a very strict CSP immediately.** Rejected for v1 because existing
      Astro/style output and integrations need a staged hardening path.

      - **Make live probes part of offline package checks.** Rejected; network
      checks belong after deploy or against an explicit preview URL.
    file: docs/rfcs/archive/implemented/rfc-0315-standardize-http-security-headers-and-public-runtime-probes.md
  - kind: rejected-alternative
    rfcId: RFC-0316
    title: Add public surface hygiene lint for generated artifacts
    status: implemented
    alternativesText: >-
      - **Only strengthen each specialized validator.** Rejected. Some defects
      are format-agnostic and belong to a shared public-text lint.

      - **Use recursive grep scripts in CI.** Rejected. The checks need
      canonical diagnostics, app routing context, and local/runtime URL
      classification.

      - **Fetch every public URL during normal checks.** Rejected. Network
      checks belong to explicit runtime probes.
    file: docs/rfcs/archive/implemented/rfc-0316-add-public-surface-hygiene-lint-for-generated-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0317
    title: Unify canonical URL, lastmod, and feed generation
    status: implemented
    alternativesText: >-
      - **Use build date for `<lastmod>`.** Rejected. This was the previous
      anti-pattern and devalues the signal.

      - **Use filesystem mtime.** Rejected. Git checkouts and generated files
      make it non-semantic.

      - **Leave product page in RSS.** Rejected. RSS is for dated/new content;
      offers belong in sitemap, llms, and agent knowledge.

      - **Make `feed.json` a later optional feature.** Rejected. JSON Feed is
      cheap once the feed item set is corrected and is useful for agents.
    file: docs/rfcs/archive/implemented/rfc-0317-unify-canonical-url-lastmod-and-feed-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0318
    title: Clean managed public trees and redirect retired PSEO URLs
    status: implemented
    alternativesText: >-
      - **Delete stale files once manually.** Rejected. The defect returns on
      the next generator change.

      - **Serve old PSEO URLs forever.** Rejected. Duplicate orphan content
      breaks the canonical surface.

      - **Return 410 for all retired pages.** Rejected as a default because some
      URLs may already have impressions and should consolidate to nearest live
      canonicals.
    file: docs/rfcs/archive/implemented/rfc-0318-clean-managed-public-trees-and-redirect-retired-pseo-urls.md
  - kind: rejected-alternative
    rfcId: RFC-0319
    title: Harden agent knowledge JSON envelopes
    status: implemented
    alternativesText: >-
      - **Keep empty strings and document them as unknown.** Rejected. Omission
      is clearer and safer for machine consumers.

      - **Use `null` for unknown facts.** Rejected as the default because
      unknown facts should not look intentionally authored.

      - **Keep freshness optional.** Rejected. Public agent knowledge is a fact
      surface; consumers need a verification date.
    file: docs/rfcs/archive/implemented/rfc-0319-harden-agent-knowledge-json-envelopes.md
  - kind: rejected-alternative
    rfcId: RFC-0320
    title: Add portable provenance frontmatter to Markdown twins
    status: implemented
    alternativesText: >-
      - **Keep key-value lines after the heading.** Rejected. YAML frontmatter
      is easier for tools to parse and is a familiar Markdown convention.

      - **Hash the entire document.** Rejected for v1 because self-referential
      hashes require canonical serialization rules for frontmatter.

      - **Make `lastModified` optional.** Rejected for twins. A portable
      artifact should carry a version date once the update-stamp resolver
      exists.
    file: docs/rfcs/archive/implemented/rfc-0320-add-portable-provenance-frontmatter-to-markdown-twins.md
  - kind: rejected-alternative
    rfcId: RFC-0321
    title: Close warpgogol-com offer content audit decisions
    status: implemented
    alternativesText: >-
      - **Patch generated public Markdown files.** Rejected. Generated files are
      projections and will be overwritten.

      - **Add one-off grep validators for each phrase.** Rejected. The platform
      should not scale by adding permanent microchecks for one client.

      - **Leave both audience registers.** Rejected by owner decision.
      `Kund:innen` is the German register for this site.

      - **Normalize ASCII hyphens to long dashes.** Rejected by owner decision.
    file: docs/rfcs/archive/implemented/rfc-0321-close-webgogol-com-offer-content-audit-decisions.md
  - kind: rejected-alternative
    rfcId: RFC-0322
    title: Publish offer capacity waves and a live slot counter
    status: implemented
    alternativesText: >-
      - **Hardcode "3 freie Plaetze" in prose.** Rejected. It freezes and has no
      source.

      - **Make the counter depend on a live CRM request.** Rejected for v1. It
      couples public rendering to private operational data and runtime
      availability.

      - **Use build date only.** Rejected. A static SSG countdown can freeze
      between deployments.

      - **Hide capacity entirely.** Rejected by owner decision; the public wave
      is part of the product proof when implemented honestly.
    file: docs/rfcs/archive/implemented/rfc-0322-publish-offer-capacity-waves-and-live-slot-counter.md
  - kind: rejected-alternative
    rfcId: RFC-0323
    title: Govern comparative commercial claims with source bindings
    status: implemented
    alternativesText: >-
      - **Treat comparative claims as normal CKL claims.** Rejected. The owner
      specifically requires a deploy block when review is due.

      - **Create site-specific checks for MyHammer, Blauarbeit, Wix, or IONOS.**
      Rejected. Future client sites will compare different vendors.

      - **Forbid all competitor comparisons.** Rejected. Comparisons can be
      useful when they are narrow, sourced, and current.

      - **Run live scraping during build.** Rejected. Builds must not depend on
      third-party availability; source checks belong to manual review or the
      Truth Monitor.
    file: docs/rfcs/archive/implemented/rfc-0323-govern-comparative-commercial-claims-with-source-bindings.md
  - kind: rejected-alternative
    rfcId: RFC-0324
    title: Require local evidence for indexable geo PSEO city pages
    status: implemented
    alternativesText: >-
      - **Raise the old substance score.** Rejected. Template volume would still
      count as substance.

      - **Use LLM quality review as the gate.** Rejected. LLM review may assist
      authoring but cannot be a deterministic deploy gate.

      - **Noindex the entire branch.** Rejected. The owner decision is to build
      substantive city pages.

      - **Keep intermediate pages indexable until filled.** Rejected.
      Navigational pages without unique substance should not compete in the
      index.
    file: docs/rfcs/archive/implemented/rfc-0324-require-local-evidence-for-indexable-geo-pseo-city-pages.md
  - kind: superseded-rfc
    rfcId: RFC-0325
    title: Upgrade Ratgeber articles to substantive dated editorial content
    status: superseded
    closedAt: 2026-07-06
    decisionSummary: Draft a Ratgeber content upgrade contract, but do not implement
      it until this RFC is accepted.
    file: docs/rfcs/archive/implemented/rfc-0325-upgrade-ratgeber-articles-to-substantive-dated-editorial-content.md
  - kind: rejected-alternative
    rfcId: RFC-0325
    title: Upgrade Ratgeber articles to substantive dated editorial content
    status: superseded
    alternativesText: >-
      - **Delete thin Ratgeber pages.** Rejected for now; the topics are useful.

      - **Accept the current stub length.** Rejected by audit, but
      implementation is deferred.

      - **Use AI filler to reach 500 words.** Rejected. The goal is helpful
      depth, not volume.
    file: docs/rfcs/archive/implemented/rfc-0325-upgrade-ratgeber-articles-to-substantive-dated-editorial-content.md
  - kind: rejected-alternative
    rfcId: RFC-0326
    title: Report files modified by kernel commands in execution reports
    status: implemented
    alternativesText: >-
      - **New `createTracingIO(base)` adapter** (separate from
      `createDefaultIO`): rejected — it would require the executor to wrap
      `createDefaultIO()` in `createTracingIO()` at every call site, adding
      indirection without benefit. The user chose to build tracing directly into
      `createDefaultIO()` to keep the adapter count at 3 (default+tracing,
      recording, read-only) rather than 4.


      - **Per-command `filesModified` in each command's `execute()` return**:
      rejected — 150+ command modules would each need to track and report their
      own writes. The port-based approach captures this centrally with zero
      per-command changes.


      - **Filesystem watcher (chokidar/fswatch)**: rejected — heavy,
      platform-specific, requires a running watcher process, and races with the
      command execution. The port-based approach is deterministic and
      synchronous with the command's own writes.


      - **Post-hoc `git diff`**: rejected — requires a clean git state before
      the command runs, doesn't capture writes to gitignored files (e.g.
      `dist/`, `public/_img/`), and is slow for large repos. The port-based
      approach captures all writes regardless of git status.


      - **Only add `filesModified` to dry-run reports (extend `writeIntents`)**:
      rejected — agents need this information for real runs, not just dry runs.
      The whole point is to tell agents what actually changed on disk.
    file: docs/rfcs/archive/implemented/rfc-0326-report-files-modified-by-kernel-commands-in-execution-reports.md
  - kind: rejected-alternative
    rfcId: RFC-0327
    title: Project configuration baseline into the ecosystem manifest
    status: implemented
    alternativesText: >-
      A separate `baseline.generated.json` file with its own `baseline.emit`
      command was rejected because it would create a second generated artifact
      with its own drift cycle, duplicating infrastructure that
      `ecosystem.manifest.generate`/`validate` already provide.


      A separate `baseline.validate` command was rejected because
      `ecosystem.manifest.validate` already detects drift — adding a second
      validator for the same data would be redundant.


      Keeping the RFC list as counts only (status quo) was rejected because
      counts do not let an agent check whether a specific DNA invariant is
      established or which RFCs reference it.
    file: docs/rfcs/archive/implemented/rfc-0327-project-configuration-baseline-into-ecosystem-manifest.md
  - kind: rejected-alternative
    rfcId: RFC-0328
    title: "Introduce semanticType: legal for legal pages"
    status: implemented
    alternativesText: >-
      **Keep the current pattern (`semanticType: content` +
      `output.sitemap.category: legal`).** Rejected. It is manual, error-prone,
      and leaves no closed type for validators or audit helpers. The sitemap
      category is a projection concern, not a semantic type.


      **Introduce a top-level `legalPages: []` registry in `system.md`.**
      Rejected. It duplicates the `pages` array and breaks the principle that a
      page's identity and role live in one place. Cosmic names already give
      pages stable identities; the semantic type should live alongside them.


      **Use `pageId` prefix or a separate `category: legal` field.** Rejected.
      `pageId` is a stable identifier, not a classifier. A separate `category`
      field would duplicate `output.sitemap.category` and still fail to enter
      the semantic model.


      **Add `semanticType: legal` but keep the legacy pattern as a deprecated
      alias.** Rejected. The user explicitly requested no backward compatibility
      and no legacy. The legacy pattern is mechanically migratable and there are
      only three apps, so a flag-day change is cheaper than a deprecation
      period.


      **Map `semanticType: legal` to a special schema.org type.** Rejected.
      schema.org does not define a `LegalPage` type. `WebPage` remains correct;
      the value of the type is in defaults and validation, not in JSON-LD type
      expansion.
    file: docs/rfcs/archive/implemented/rfc-0328-introduce-semantic-type-legal-for-legal-pages.md
  - kind: rejected-alternative
    rfcId: RFC-0329
    title: Generate an RFC decision log and consult it during RFC creation
    status: implemented
    alternativesText: >-
      - **LLM/embedding-based semantic matching in rfc.create**: rejected for v1
      — non-deterministic, requires network/tokens, and keyword overlap on
      titles is sufficient for the dominant failure mode (same nouns, same
      topic). Can be revisited if keyword matching demonstrably misses repeats.

      - **Hard gate (rfc.create fails when matches found)**: rejected —
      relevance is judgment; a hard gate on keyword overlap would produce false
      blocks and train agents to game titles.

      - **A frontmatter marker (`decisionLogConsulted: true`) validated by
      rfc.validate**: rejected — agents would set it mechanically; it proves
      ritual, not consultation. The printed-at-creation design puts the
      information in the agent's context instead, which is what actually changes
      behavior.

      - **Storing the log outside docs/rfcs/**: rejected — colocating with the
      RFCs keeps the RFC directory the single home of decision knowledge.

      - **Aggregating only rejected/superseded RFCs (no rejected-alternative
      kind)**: rejected — most "no" decisions live inside accepted RFCs'
      Alternatives sections; dropping them would miss the majority of the
      corpus.
    file: docs/rfcs/archive/implemented/rfc-0329-generate-an-rfc-decision-log-and-consult-it-during-rfc-creation.md
  - kind: rejected-alternative
    rfcId: RFC-0330
    title: Emit per-RFC verification evidence artifacts
    status: implemented
    alternativesText: >-
      - **Hand-authored `verification:` frontmatter block**
      (method/coverage/pipeline, per the original space-program proposal):
      rejected — agents fill such fields templately; the information becomes
      noise. Evidence must be derived from execution (unanimous position of the
      2026-07 expert critique).

      - **Persisting inside `rfc.acceptance.run` instead of a new command**:
      rejected — acceptance.run is a fast, side-effect-free check used casually;
      silently writing files on every casual run would surprise callers and
      churn git. Explicit emission marks intent ("this run is the record").

      - **Evidence in frontmatter of the RFC itself**: rejected — mixes
      generated data into a hand-authored file, breaking the RFC-0081
      generated/authored partition.

      - **Cryptographic signing now (RFC 9421 / RFC-0291)**: rejected for v1 —
      no external consumer exists; git history already provides tamper-evidence
      for the internal one. Envelope is signature-ready.

      - **Append-only evidence ledger (one file, many runs)**: rejected — git
      history of the per-RFC file already provides the run sequence; a ledger
      duplicates git.
    file: docs/rfcs/archive/implemented/rfc-0330-emit-per-rfc-verification-evidence-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0331
    title: Add satisfies DNA-trace frontmatter and coverage validation
    status: implemented
    alternativesText: >-
      - **Full RTM with a product-goals registry (`requirements.index.yaml`,
      `PRODUCT-GOAL-*`)**: rejected — the ecosystem has no separate
      product-requirements layer and does not need one; inventing it would
      create a registry agents fill templately. The DNA registry is the real
      requirements layer (consensus of the 2026-07 expert critique).

      - **Overloading `related:` with a naming convention (e.g. `DNA-35!` for
      satisfaction)**: rejected — magic suffixes in a shared field are invisible
      to schema validation and easy to get wrong; a separate typed key is
      self-documenting.

      - **Requiring `satisfies` on all kinds**: rejected — command/policy RFCs
      often serve operations, not invariants; forcing a reference would produce
      fake links (the exact hand-authored-noise failure this batch avoids).

      - **Backfilling satisfies from related: for 316 RFCs**: rejected by
      founder decision (2026-07-06) — mechanical backfill would fabricate
      satisfaction claims nobody actually asserted.

      - **Hard-failing on uncovered DNA invariants (reverse coverage as
      error)**: rejected — with forward-only adoption, most invariants stay
      uncovered for a long time; an error would either be ignored or force fake
      backfill.
    file: docs/rfcs/archive/implemented/rfc-0331-add-satisfies-dna-trace-frontmatter-and-coverage-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0332
    title: Derive change impact class and advisory check profiles
    status: implemented
    alternativesText: >-
      - **Hand-authored `regressionRisk:` frontmatter on RFCs**: rejected —
      agents cannot honestly self-assess regression risk and fill such fields
      templately; derived classification is reproducible and argues from
      evidence (the diff).

      - **Gating CI on the derived class immediately**: rejected by founder
      decision (2026-07-06) — maximum fleet savings, but amends DNA-35 and
      weakens guarantees; deferred to a dedicated RFC once the advisory
      classifier has accumulated trust.

      - **Dependency-graph (blast-radius) analysis in v1**: rejected for scope —
      the manifest graph exists (RFC-0327), but graph-walking multiplies edge
      cases; path rules cover the dominant cost asymmetry (content vs packages
      vs OS) at a fraction of the complexity. Follow-up documented.

      - **Putting the rules in a config file (YAML) instead of code**: rejected
      — the ruleset is an architectural statement that should change via RFC and
      code review, not silent config edits; a typed constant with tests is the
      right friction.

      - **Per-command turbo cache as sufficient substitute**: rejected — caching
      accelerates re-runs of the same scope; it does not answer "which scope is
      proportionate", and cold caches at fleet scale still cost O(fleet).
    file: docs/rfcs/archive/implemented/rfc-0332-derive-change-impact-class-and-advisory-check-profiles.md
  - kind: rejected-alternative
    rfcId: RFC-0333
    title: Add an independent black-box QA runner against built output
    status: implemented
    alternativesText: >-
      - **LLM QA agent in v1**: rejected by founder decision (2026-07-06) —
      token cost per run, non-determinism, and partial overlap with RFC-0299;
      deterministic probes first, exploration later.

      - **Static HTML assertions instead of a browser** (extend
      behavior.snapshot): rejected as the _only_ mechanism — it cannot see
      hydration failures, runtime console errors, or client-script behavior,
      which are exactly the regressions the implementing agent's own contour
      misses most often.

      - **A separate QA spec file per app** (instead of probes in RFC
      frontmatter): rejected — it would create a second hand-authored
      expectation registry that drifts from the RFCs; keeping expectations in
      the RFC that motivated them preserves the spec-to-check trace and reuses
      the whole RFC-0268 toolchain (V-22, evidence via RFC-0330).

      - **Executing page probes inside rfc.acceptance.run**: rejected —
      acceptance.run is fast and repo-level by contract (RFC-0268 non-goal:
      probes must not require builds); page probes require a built dist and a
      browser.

      - **Process-level sandboxing of the runner**: rejected for v1 — input
      isolation delivers the verification independence; OS-level isolation adds
      ops complexity without changing what the runner can conclude.
    file: docs/rfcs/archive/implemented/rfc-0333-add-an-independent-black-box-qa-runner-against-built-output.md
  - kind: rejected-alternative
    rfcId: RFC-0334
    title: Add rfc.supersede.propose escalation for blocked implementations
    status: implemented
    alternativesText: >-
      - **Free-form escalation (agent just writes a note / asks in chat)**:
      rejected — it is the status quo; unstructured escalations are
      inconsistent, unsearchable, and invisible to the decision log.

      - **A separate consultation-file format (not an RFC draft)**: rejected — a
      new artifact type needs its own lifecycle, validation, and storage; a
      pre-linked draft RFC reuses the entire existing governance machine, and
      the human's decision lands exactly where decisions live.

      - **Letting the command set `supersededBy` on the target immediately**:
      rejected — that is a decision, and decisions are human (RFC-0224).
      Proposal ≠ supersession.

      - **Automatic conflict detection (static analysis of invariant
      violations)**: rejected for v1 — DNA invariants are prose+validator
      hybrids; detection is the validators' job, judgment is the agent's, and
      the escalation channel must exist regardless of how the conflict was
      found.

      - **Amend-oriented variant (`rfc.amend.propose`)**: deferred — amendment
      proposals are a milder flavor of the same ritual; if practice shows
      demand, extend this command with `--mode amend` in a follow-up rather than
      shipping two commands now.
    file: docs/rfcs/archive/implemented/rfc-0334-add-rfc-supersede-propose-escalation-for-blocked-implementations.md
  - kind: rejected-alternative
    rfcId: RFC-0335
    title: Require reviewer identity on newly decided RFCs
    status: implemented
    alternativesText: >-
      - **Backfilling `reviewers: ["human:andrii"]` onto all decided historical
      RFCs**: rejected by founder decision (2026-07-06) — it would be a bulk
      fabrication of records that were never made; an audit trail that begins
      with fabricated entries is worse than one that begins late.

      - **A separate `rfc.review.validate` command** (per the original expert
      proposal): rejected — one more command for one rule; `rfc.validate` is the
      established home for frontmatter integrity, and V-25 is frontmatter
      integrity.

      - **Richer review records (`reviewedAt`, `reviewNotes`) now**: rejected
      for v1 — `updatedAt` plus git history already dates the decision; notes
      belong in review conversation. Identity is the one field that cannot be
      reconstructed later, so it is the one field mandated.

      - **Free-form reviewer strings (no typed prefix)**: rejected — the
      `human:`/`agent:` grammar is already established by RFC-0278; a second,
      untyped convention for the same concept one layer up would guarantee
      eventual divergence.

      - **Allowing `agent:` entries immediately** (trusting RFC-0279's
      reviewer): rejected — RFC-0279's reviewer is scoped to content approval;
      extending its authority to architecture governance is a founder-level
      decision that deserves its own RFC, not a side effect of a validation
      rule.
    file: docs/rfcs/archive/implemented/rfc-0335-require-reviewer-identity-on-newly-decided-rfcs.md
  - kind: rejected-alternative
    rfcId: RFC-0336
    title: Deter agent edits to generated files and generate .gitattributes from the
      artifact registries
    status: implemented
    alternativesText: >-
      - **Hand-maintain the `.gitattributes` list.** Rejected. It would drift
      the moment any command adds an output; the registries already know every
      path.

      - **Add `-diff`/`binary` to generated text.** Rejected per the founder
      decision — reviewers must still see generated changes locally;
      `linguist-generated` gives the GitHub collapse without hiding content.

      - **A git `pre-commit` hook / shell grep in CI instead of a kernel
      command.** Rejected for the same reason RFC-0316 rejected CI-only greps:
      enforcement in this repo is kernel commands with canonical diagnostics,
      ownership context, and offline determinism, wired into the standard
      pipelines.

      - **Mass-rewrite the advisory block across all generated files now.**
      Rejected as a huge one-time diff and regen churn; incremental adoption via
      the helper reaches the same end state.

      - **Drop the single-line marker in favor of the block only.** Rejected —
      `hasGeneratedMarker` and dozens of call sites depend on the exact
      substring; the block wraps it instead.
    file: docs/rfcs/archive/implemented/rfc-0336-deter-agent-edits-to-generated-files-and-generate-gitattributes.md
  - kind: rejected-alternative
    rfcId: RFC-0337
    title: Establish the observability port and closed telemetry conventions
    status: implemented
    alternativesText: >-
      - **prom-client `/metrics` pull endpoints (the research document's core
      proposal).** Rejected: static sites have nothing to scrape; Worker
      isolates are ephemeral and many, so a scrape hits one random isolate with
      a fresh registry.

      - **Adopt `@opentelemetry/sdk-node` + auto-instrumentations.** Rejected
      for the port: heavy dependency tree, Node-only, and its long-lived-process
      assumptions do not fit short-lived kernel commands or Workers. A
      hand-rolled OTLP JSON client is ~200 lines and fixture-testable.

      - **Put helpers in `@gogol/share`.** Rejected: share is already broad; the
      observability port deserves an owned, closed surface with its own
      validator.

      - **Vendor-neutral abstraction over multiple backends.** Rejected as
      premature; the port is two env vars + OTLP, which is already
      backend-swappable by construction.
    file: docs/rfcs/archive/implemented/rfc-0337-establish-the-observability-port-and-closed-telemetry-conventions.md
  - kind: rejected-alternative
    rfcId: RFC-0338
    title: Deploy the self-hosted SigNoz observability stack on an EU VPS
    status: implemented
    alternativesText: >-
      - **SigNoz Cloud.** Rejected: EU-residency control and cost predictability
      favor self-hosting; the studio explicitly chose self-hosted.

      - **Grafana LGTM stack.** Rejected: three backends to operate instead of
      one OTLP-native product; SigNoz's MCP server (RFC-0344) is a strategic fit
      for the AI-first operating model.

      - **Cloudflare-proxied (orange-cloud) hostnames.** Rejected: the
      observability plane must not share fate with Cloudflare, whose outages are
      among the incidents it must observe.

      - **Kubernetes/Helm deployment.** Rejected at current scale (3 sites); the
      compose flavor on one VPS is sufficient and dramatically cheaper to
      operate.

      - **Terraform/Ansible provisioning.** Rejected per founder decision on
      tooling; a once-executed documented runbook is adequate for a single pet
      server.
    file: docs/rfcs/archive/implemented/rfc-0338-deploy-the-self-hosted-signoz-observability-stack-on-an-eu-vps.md
  - kind: rejected-alternative
    rfcId: RFC-0339
    title: Export Cloudflare Workers traces to the observability backend
    status: implemented
    alternativesText: >-
      - **In-worker OTLP SDK (e.g. otel-cf-workers) instead of platform
      auto-tracing.** Rejected: adds a runtime dependency and per-request
      overhead to every worker for what the platform provides declaratively.

      - **Per-site destinations.** Rejected: one account-level destination with
      `service.name` disambiguation is simpler and matches the fleet model.

      - **Sampling below 1.0 now.** Rejected: current API-route traffic is tiny;
      full sampling maximizes diagnostic value. Revisit via amendment when fleet
      scale raises ingest cost (tail sampling belongs in the RFC-0338 collector
      then).

      - **Automating destination creation via Cloudflare API.** Rejected for v1:
      a one-time dashboard step documented in the runbook is cheaper than
      maintaining an API integration for a write-once object.
    file: docs/rfcs/archive/implemented/rfc-0339-export-cloudflare-workers-traces-to-the-observability-backend.md
  - kind: rejected-alternative
    rfcId: RFC-0340
    title: Emit factory telemetry from kernel command runs
    status: implemented
    alternativesText: >-
      - **Wrap pipelines only (not individual commands).** Rejected: per-command
      grain is where diagnosis happens ("which step got slow/red"), and the
      choke point makes it equally cheap.

      - **Emit spans (one trace per pipeline run).** Deferred: valuable, but
      metrics answer the trend questions first; spans double the wire format
      surface. A follow-up RFC can add trace emission through the same port.

      - **Parse Turborepo run summaries for cache metrics.** Deferred to its own
      RFC: different data source (`.turbo/runs` files), different lifecycle
      (post-run parsing), and turbo's summary schema versioning deserves
      isolated treatment.

      - **Local telemetry spool file + batch uploader.** Rejected: introduces
      state, cleanup, and privacy questions for near-zero benefit over
      fire-and-forget.
    file: docs/rfcs/archive/implemented/rfc-0340-emit-factory-telemetry-from-kernel-command-runs.md
  - kind: rejected-alternative
    rfcId: RFC-0341
    title: Add scheduled fleet probes that push synthetic metrics
    status: implemented
    alternativesText: >-
      - **Deploy OneUptime/Uptime Kuma alongside SigNoz.** Rejected: second
      product to operate, strictly weaker checks than the in-house engine, and
      no content sentinels; the studio's check stack already exists.

      - **Cloudflare Workers cron probing.** Rejected: probing Cloudflare-served
      sites from inside Cloudflare hides an entire class of failures (CF
      outage/misconfig); the EU VPS is a true external vantage.

      - **Playwright for every pulse.** Rejected: ~100× cost per probe for
      negligible added signal at 5-minute cadence; browser depth belongs in the
      daily deep lane.

      - **Reusing backs/check-warpgogol-runner directly.** Rejected: that runner
      is queue-driven, product-scoped (third-party URLs, Playwright-first).
      Shared logic belongs in packages, not by coupling two deployables.
    file: docs/rfcs/archive/implemented/rfc-0341-add-scheduled-fleet-probes-that-push-synthetic-metrics.md
  - kind: rejected-alternative
    rfcId: RFC-0342
    title: Manage observability alerts as generated code through the SigNoz API
    status: implemented
    alternativesText: >-
      - **Terraform + SigNoz provider.** Rejected by founder decision:
      introduces a toolchain alien to the ecosystem for one concern the
      generated-artifact model already covers; state files add operational
      surface.

      - **Alertmanager-style config file mounted into SigNoz.** Rejected: SigNoz
      manages rules in its own store/API; fighting that with file mounts is
      fragile across upgrades.

      - **Native Slack instead of Telegram bridge.** Rejected: founder chose
      email + Telegram; the studio does not live in Slack.

      - **Grafana OnCall / PagerDuty.** Rejected: paging-grade escalation is
      overkill for the current team size; channels are revisitable by amending
      the channel list.
    file: docs/rfcs/archive/implemented/rfc-0342-manage-observability-alerts-as-generated-code-through-the-signoz-api.md
  - kind: rejected-alternative
    rfcId: RFC-0343
    title: Poll Cloudflare analytics into fleet delivery metrics
    status: implemented
    alternativesText: >-
      - **Logpush (per-request logs) into the collector.** Rejected: plan-gated,
      high volume, per-visitor fields — wrong side of the Matomo/GDPR boundary;
      aggregates answer the operational questions.

      - **A Cloudflare Worker cron as the poller.** Rejected: needs state
      (watermark) and would put the CF-watching component inside CF; the VPS
      already hosts scheduled services with volumes.

      - **Zone auto-discovery via API.** Rejected: an authored, validated map is
      smaller, reviewable, and avoids accidentally ingesting non-fleet zones on
      the same account.

      - **Scraping the CF dashboard REST endpoints per zone.** Rejected: the
      GraphQL API is the documented, stable surface for aggregated analytics.
    file: docs/rfcs/archive/implemented/rfc-0343-poll-cloudflare-analytics-into-fleet-delivery-metrics.md
  - kind: rejected-alternative
    rfcId: RFC-0344
    title: Grant agents read-only telemetry access through the SigNoz MCP server
    status: implemented
    alternativesText: >-
      - **Give agents the SigNoz web UI via browser tooling.** Rejected:
      brittle, slow, unauditable, and screenshots are not evidence artifacts.

      - **Admin-role key for convenience.** Rejected: collapses the RFC-0342
      mutation lane and makes drift invisible.

      - **A bespoke wgogol MCP server wrapping the SigNoz API.** Rejected for
      v1: maintenance surface without added capability; revisit only if the
      official MCP surface proves insufficient.

      - **No policy (just hand out a key).** Rejected: the operating model
      (RFC-0218) exists precisely to make agent lanes explicit and auditable.
    file: docs/rfcs/archive/implemented/rfc-0344-grant-agents-read-only-telemetry-access-through-the-signoz-mcp-server.md
  - kind: rejected-alternative
    rfcId: RFC-0345
    title: Make generated file writes idempotent and content-deterministic
    status: implemented
    alternativesText: >-
      1. **Keep `generatedAt` as a real timestamp, exclude it from the
      write-if-changed comparison**: More complex — each file type would need a
      custom comparison function that knows which fields to strip. Rejected in
      favor of the simpler `null` approach already established by
      `surface-breaker.ts`, `pseo-visibility.ts`, and `surface-demand.ts`.


      2. **Per-generator private `writeIfChanged` (no shared helper)**: Already
      the status quo in 3 locations. Rejected because it duplicates the pattern
      further and makes future maintenance harder.


      3. **Use `context.io.writeFile` instead of `writeFileAtomic`**: The
      `WorkspaceIO` port (RFC-0267) is for command-scoped IO interception, not
      for parallel-safe atomic writes. `writeFileIfChanged` needs
      `writeFileAtomic` for workspace-root files like `uni.registry.json` that
      are written by parallel app builds.
    file: docs/rfcs/archive/implemented/rfc-0345-make-generated-file-writes-idempotent-and-content-deterministic.md
  - kind: superseded-rfc
    rfcId: RFC-0346
    title: Mandate .env.example and deploy-script contracts for apps and backs
    status: superseded
    closedAt: 2026-07-20
    decisionSummary: The workspace gains a workspace-scoped env-and-deploy contract
      with five rules and four commands.
    file: docs/rfcs/archive/implemented/rfc-0346-mandate-env-example-and-deploy-script-contracts-for-apps-and-backs.md
  - kind: rejected-alternative
    rfcId: RFC-0346
    title: Mandate .env.example and deploy-script contracts for apps and backs
    status: superseded
    alternativesText: >-
      - **Extend `env.example.generate` to `backs/*`:** Rejected. Each backend
      is unique — its env surface depends on the runtime (Docker, wrangler,
      compose), upstream services, and deployment target. A generator would need
      per-backend templates, which is more complexity than hand-authoring a
      small `.env.example`.

      - **Git hook for `.env` / `.env.production` presence:** Rejected. Git
      hooks are local-only and not enforced in CI. A Site OS command is
      CI-portable and integrates with existing pipelines.

      - **Mandate `.env.example` for `packages/*`:** Rejected. Packages consume
      env via adapter interfaces (`GrowthAdapter`, `IntegrationPort`), not
      direct `process.env` reads. The contract boundary is the deployable
      project.

      - **Single `.env` for apps (no `.env` / `.env.production` split):**
      Rejected. The alt/main deploy split is established by RFC-0149 and the
      onboarding template. Two files map to two `wrangler deploy --secrets-file`
      targets.
    file: docs/rfcs/archive/implemented/rfc-0346-mandate-env-example-and-deploy-script-contracts-for-apps-and-backs.md
  - kind: rejected-alternative
    rfcId: RFC-0347
    title: Adopt property-based testing for pure functions with fast-check
    status: implemented
    alternativesText: >-
      **Keep both test runners (`node:test` + `vitest`).** Rejected because PBT
      adoption would require two integration patterns, and agents would need to
      know which runner each package uses. Standardizing on one runner removes
      ambiguity.


      **Standardize on `node:test` instead of `vitest`.** Rejected because
      vitest is already used by 5 packages including the critical
      `@gogol/site-kernel-checks`, and vitest provides better watch mode,
      parallelism, and fast-check integration out of the box. Migrating vitest
      packages to `node:test` would be higher churn for less benefit.


      **Add an OS validation command for PBT coverage.** Rejected per founder
      decision. Enforcement is through agent discipline and the DNA invariant. A
      future RFC can add automated coverage checking if needed.


      **Use a closed list of functions instead of selection criteria.** Rejected
      per founder decision. Criteria with examples are more maintainable and let
      agents identify new qualifying functions as the codebase grows.


      **Use `fast-check` without standardizing the test runner.** Rejected
      because `fast-check` works with both runners but the import patterns
      differ (`fc.assert` inside `node:test`'s `test()` vs vitest's `test()`).
      Standardizing avoids confusion.
    file: docs/rfcs/archive/implemented/rfc-0347-adopt-property-based-testing-for-pure-functions-with-fast-check.md
  - kind: rejected-alternative
    rfcId: RFC-0348
    title: Collapse GRACE markup to a two-block contract
    status: implemented
    alternativesText: >-
      - **Keep `responsibilities`, drop only `MODULE_MAP`/`keywords`/anchors.**
      Rejected. `responsibilities` restates code; `purpose` + `non-goals`
      already carry the non-derivable signal (what it's for, what it must not
      do). Keeping it perpetuates the token cost this RFC exists to cut.

      - **Keep anchors for `full`-mode files only.** Rejected. No consumer reads
      anchors; a "sometimes required, never used" element is the worst of both
      worlds.

      - **Make the blocks optional / advisory.** Rejected. Optional
      machine-readable contracts drift to absent. The value comes from every
      file having a purpose and non-goals; the cut is in _what_ is required, not
      _whether_.

      - **Preserve the `full`/`reduced` mode names as aliases.** Rejected — no
      legacy. Two names for one requirement is the accidental complexity being
      removed.
    file: docs/rfcs/archive/implemented/rfc-0348-collapse-grace-markup-to-a-two-block-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0349
    title: Govern CHANGE_SUMMARY retention and protect decision lines
    status: implemented
    alternativesText: >-
      - **Replace CHANGE_SUMMARY with a `git log` reference.** Rejected. `git
      log` gives commit messages, not "which RFC changed this file's contract
      and why"; the curated decision log is strictly more useful to an agent and
      is what the founder chose to keep.

      - **Cap by total item count regardless of class.** Rejected. It would
      eventually delete protected decision lines, violating the founder
      invariant.

      - **LLM-summarize old items into one.** Rejected — non-deterministic,
      needs an API key, and manufactures prose with the authority of a decision
      record but no provenance (the exact anti-pattern this whole series
      removes).
    file: docs/rfcs/archive/implemented/rfc-0349-govern-change-summary-retention-and-protect-decision-lines.md
  - kind: rejected-alternative
    rfcId: RFC-0350
    title: Retire LLM GRACE generators and make backfill deterministic
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0350-retire-llm-grace-generators-and-make-backfill-deterministic.md
  - kind: rejected-alternative
    rfcId: RFC-0351
    title: Add grace.invariant.add helper and authoring discipline
    status: implemented
    alternativesText: No alternatives were recorded in the original mini-RFC form.
      (Backfilled during mini-template retirement.)
    file: docs/rfcs/archive/implemented/rfc-0351-add-grace-invariant-add-helper-and-authoring-discipline.md
  - kind: rejected-alternative
    rfcId: RFC-0352
    title: Audit GRACE block truth on a per-file revision cadence
    status: implemented
    alternativesText: >-
      - **Run the semantic check inside a command via an LLM.** Rejected. It
      would put a non-deterministic, API-keyed model call into the build
      toolchain — the exact pattern RFC-0350 removes — and hide the judgment
      from git. The work-order protocol keeps commands deterministic and the
      judgment attributable to an in-session agent.

      - **Audit every file every build.** Rejected. Semantic review is
      expensive; the revision-delta signal targets exactly the files likely to
      have drifted.

      - **A new per-file audit counter.** Rejected. The integrity registry
      already computes `revision` from git; a second counter would drift from
      it.

      - **Hard-fail in `build.check`.** Rejected per founder decision — it would
      block unrelated hotfixes when a hot file crosses the threshold.
      Warn-in-build + fail-in-QA gives notice without blocking day-to-day work.
    file: docs/rfcs/archive/implemented/rfc-0352-audit-grace-block-truth-on-a-per-file-revision-cadence.md
  - kind: rejected-alternative
    rfcId: RFC-0353
    title: Rename GRACE to Compass across the ecosystem
    status: implemented
    alternativesText: >-
      | Name | Rejected because |

      | --- | --- |

      | **Bearing** | Additional meanings: mechanical bearing, load-bearing
      capacity |

      | **Beacon** | Implies an external signal, not a description of module
      boundaries |

      | **Needle** | Too narrow: only points direction, does not preserve
      engineering intent |

      | **North** | Implies a single absolute correct direction; the system
      describes per-module constraints |

      | **Chart** | Overlaps with Sternenkarte (star map); describes a map, not
      constraints |

      | **Guide** | Too close to Wegweiser, which manages the change route |

      | **Radar** | Detects risks but does not preserve engineering intent |

      | **Intent** | Weaker: asserts purpose without boundaries, non-goals, or
      invariants |

      | **Code Compass** | Too literal, startup-tone, and the contour may cover
      non-code sources |

      | **Module Compass** | Narrows to modules; markup can appear at file,
      package, and architecture levels |

      | **Source Compass** (as namespace) | Longer without practical benefit;
      `compass.*` is sufficient |
    file: docs/rfcs/archive/implemented/rfc-0353-rename-grace-to-compass-across-the-ecosystem.md
  - kind: rejected-alternative
    rfcId: RFC-0354
    title: Establish the Sternsystem bundle contract and fleet registry
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Git submodules for sites | Submodules couple the site's git history to
      the monorepo's, require `.gitmodules` coordination, and make independent
      lifecycle (create/archive/transfer) awkward. A registry + gitignored cache
      clone is simpler and decouples the histories. |

      | Keep sites as `apps/` members and scale the monorepo | `pnpm install`
      and Turborepo's task graph do not scale to thousands of workspace members.
      The coupling is structural, not fixable by tooling. |

      | Use `handoff.pack` bundles as the persistent unit (no separate
      Sternsystem concept) | `handoff.pack` is a transfer artifact with a lock;
      it lacks the persistent pin, bordbuch, and registry integration that a
      durable site unit needs. Generalizing it into a Sternsystem is the natural
      next step. |

      | Store the fleet registry in JSON instead of YAML | YAML is
      human-readable and editable, which matters for a registry that will be
      reviewed and curated. JSON is machine-only. The Zod schema validates both.
      |

      | Register sites in `fleet/fleet.sites.json` (existing) |
      `fleet.sites.json` is a flat list of app names for the pilot deploy
      runner. It has no schema, no version pins, no repo URLs, and no lifecycle
      state. A new registry with a proper schema is cleaner than retrofitting. |
    file: docs/rfcs/archive/implemented/rfc-0354-establish-the-sternsystem-bundle-contract-and-fleet-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0355
    title: Mission lifecycle and Bordbuch
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Allow concurrent open missions per system | Invites conflicting changes
      and merge complexity. Sequential missions with a single open constraint
      enforce reviewable, non-conflicting change. If parallelism is needed,
      split the system. |

      | Store Bordbuch in the Werkstatt monorepo instead of the Sternsystem's
      repo | The Bordbuch is the Sternsystem's history, not the platform's.
      Storing it in the Sternsystem's repo keeps the history co-located with the
      site and allows the Sternsystem to be transferred with its full history
      intact. |

      | Use git log as the Bordbuch | Git log records commits, not mission
      lifecycle (open/abort). Aborted missions leave no git trace. The Bordbuch
      captures the full intent-and-outcome record, including failed attempts. |

      | Store mission state in the registry instead of a separate manifest | The
      registry is a summary index; the mission manifest carries detail (brief,
      pin at open, materialization state) that does not belong in a fleet-wide
      registry. |

      | Use a database for the Bordbuch | An NDJSON hash-chain file in the
      Sternsystem's repo is simpler, portable, reviewable, and already matches
      RFC-0276. A database adds infrastructure without benefit for MVP. |
    file: docs/rfcs/archive/implemented/rfc-0355-mission-lifecycle-and-bordbuch.md
  - kind: rejected-alternative
    rfcId: RFC-0356
    title: Mission materialization from pinned Sternsystem bundles
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Materialize directly into `apps/<app>/` (reuse `handoff.absorb` as-is) |
      Couples missions to the `apps/` workspace member model. The whole point of
      the Sternsystem architecture is to decouple sites from `apps/`. Mission
      Werkstücke are ephemeral and live in `missions/`, not `apps/`. |

      | Skip materialization — let agents work directly on the Sternsystem cache
      clone | No validation gate, no ephemeral isolation. Changes would be
      committed directly to the Sternsystem's repo without passing through the
      mission lifecycle. This defeats the purpose of missions. |

      | Use `handoff.pack` + `handoff.absorb` as the materialization flow (pack
      the Sternsystem, absorb into the mission) | Adds an unnecessary pack step.
      The Sternsystem's authored set is already in the cache clone;
      materialization can copy it directly. The pack/absorb round trip is for
      inter-developer transfer, not for mission materialization. |

      | Require a full independent `pnpm install` in the mission Werkstück | The
      Werkstück is buildable through the pinned platform worktree. It may use
      the platform install/store, but it must not become an independently
      dependency-managed site repo. |

      | Name the local build directory `release/` | Confuses a disposable
      mission-local build with a published Release (RFC-0357). `distribution/`
      keeps the local deployable output clearly non-canonical. |

      | Keep `apps/` sites after extraction | Rejected by the final architecture
      decision. Extraction is complete only after the source app is removed and
      mission materialization validates. |
    file: docs/rfcs/archive/implemented/rfc-0356-mission-materialization-from-pinned-sternsystem-bundles.md
  - kind: rejected-alternative
    rfcId: RFC-0357
    title: Release discipline and behavior snapshot diff gating
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Use the mission's validation build as the release (no separate
      production build) | The readable build and production build may use
      different optimization configs. The behavior snapshot diff catches
      structural drift between them. Skipping the production build means
      shipping an unoptimized build or trusting that optimization doesn't change
      structure. |

      | Compare DOM screenshots instead of structural facts | Screenshots are
      environment-sensitive (browser version, rendering timing, font
      availability). Structural facts (routes, sitemap, hashes) are
      deterministic and machine-checkable. |

      | Skip the diff gate — trust the build | Silent route drops and sitemap
      changes have been observed in practice when optimization passes are
      misconfigured. The diff gate is the safety net. |

      | Store releases in the Sternsystem's repo | Release artifacts include
      `dist/` (build output) which is large and binary. The Sternsystem's repo
      carries authored data; durable release artifacts live in the RFC-0363
      artifact store. |

      | Treat `releases/<id>/dist` as durable truth | Local caches are easy to
      delete or corrupt. Deployment, rollback, and Notausgang need
      content-addressed artifacts with retention rules, so the durable truth is
      the artifact reference in `release.yaml`. |

      | Use semver auto-bumping based on capability diff | The operator chooses
      the semver. Auto-bumping based on capability tiers (green/yellow/red) is a
      heuristic that may not match the operator's intent. The discipline gates
      ensure safety; the semver communicates intent. |
    file: docs/rfcs/archive/implemented/rfc-0357-release-discipline-and-behavior-snapshot-diff-gating.md
  - kind: rejected-alternative
    rfcId: RFC-0358
    title: Fleet propagation and the Leitstand
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Direct `wrangler pages deploy` without a Leitstand abstraction | No
      health checks, no rollback, no fleet-wide status. Each deployment would be
      a manual, unverified operation. |

      | Use Cloudflare Workers Builds or Pages CI/CD instead of a Leitstand |
      Couples the fleet to a single vendor's CI/CD. The adapter interface
      abstracts over vendors, allowing future migration. |

      | Propagate at `release.publish` time (merge publish + propagate) |
      Separating publish from propagate allows the operator to review the
      release artifact before deployment. Publish makes it immutable; propagate
      deploys it. |

      | Store deployment state in a database | The registry's `deployment` block
      is a simple YAML field. A database adds infrastructure without benefit at
      this scale. |

      | Use blue-green deployment | MVP is direct propagation with rollback.
      Blue-green adds complexity (two targets, traffic switching) that is not
      needed yet. |

      | Store deployment credentials in the registry | Registry files are
      authored data and may be committed. MVP uses secret references only;
      secret values stay in the operator environment or vendor secret store. |
    file: docs/rfcs/archive/implemented/rfc-0358-fleet-propagation-and-the-leitstand.md
  - kind: rejected-alternative
    rfcId: RFC-0359
    title: "Notausgang: full site export with dist artifacts and history"
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Use `client.export` for the emergency exit | Ships the full platform
      source (the studio's IP). The client gets a self-contained rebuildable
      project, but the studio loses control of its platform code. Notausgang
      ships dist (servable) without platform source. |

      | Use `handoff.pack` for the emergency exit | Ships only authored content,
      no dist. The client cannot serve the site without the platform. Notausgang
      ships dist so the client can serve immediately. |

      | Ship dist only (no authored content, no history) | The client loses
      their content source and audit trail. Notausgang includes both for
      completeness and client ownership. |

      | Null integrations by deleting config files | Runtime config files are
      excluded from `site/`; data records that must remain are nulled through
      the declarative integration inventory so structure is preserved without
      secrets. |

      | Require manual integration nulling | Error-prone. The operator may
      forget to null an integration key. Declarative nulling with
      `notausgang.validate`'s live key scan is the safety net. |

      | Export from local `releases/<id>/dist` only | Local caches may be stale
      or missing. The export must restore from RFC-0363 and prove hashes match
      the published release. |
    file: docs/rfcs/archive/implemented/rfc-0359-notausgang-full-site-export-with-dist-artifacts-and-history.md
  - kind: rejected-alternative
    rfcId: RFC-0360
    title: Extend filename conventions to the whole repo
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Keep scanning only `apps/` and `packages/` | Leaves the Werkstatt
      directories (`systems/`, `missions/`, `releases/`, `agents/`) unenforced.
      Inconsistent naming would go undetected. |

      | Add new roots individually (`systems`, `missions`, etc.) to the
      hard-coded array only | Still hard-coded to one command; future
      directories would drift from ecosystem/command manifests. The
      topology-derived approach is future-proof while still rejecting
      unregistered root sandboxes. |

      | Scan every top-level directory automatically | Allows arbitrary root
      sandboxes and generated trash to become de facto repo structure. Unknown
      top-level directories must fail until registered. |

      | Create a separate `naming.convention.lint.repo` command | Two commands
      for the same invariant is confusing. One command, extended scope, is
      simpler. |

      | Scan everything including dot-directories | Dot-directories (`.git`,
      `.turbo`, `.astro`) contain generated/tool-managed files with
      non-kebab-case names. They must remain exempt. |
    file: docs/rfcs/archive/implemented/rfc-0360-extend-filename-conventions-to-the-whole-repo.md
  - kind: rejected-alternative
    rfcId: RFC-0361
    title: Naming policies for Sternsystem ids, mission ids, release ids, and
      Bordbuch entries
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Keep naming policies only in Zod schemas (no standalone command) | No
      way to audit naming across all artifacts without parsing each schema
      individually. A consolidated command is needed for fleet-wide audits. |

      | Extend `naming.convention.lint` to also validate ids |
      `naming.convention.lint` is a filename-level validator (DNA-6). Mixing
      filename and id validation in one command conflates two concerns. |

      | Use a single regex for all ids | Different id types have different
      formats (system id, mission id, release id). A single regex would be
      overly complex and hard to maintain. |

      | Auto-generate ids instead of validating | Auto-generation is a
      command-level concern (`mission.open` generates the next id). Validation
      is a separate concern that checks all artifacts regardless of how they
      were created. |

      | Add `--fix` to rewrite invalid ids | Rewriting ids and Bordbuch history
      is high risk and can break audit trails. The validator may emit a
      migration plan, but humans or dedicated migration commands perform the
      changes. |
    file: docs/rfcs/archive/implemented/rfc-0361-naming-policies-for-sternsystem-mission-release-and-bordbuch.md
  - kind: rejected-alternative
    rfcId: RFC-0362
    title: Werkstatt consistency primitives for locks, idempotency, and atomic state
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Use a database for all Werkstatt state | Too much infrastructure for MVP
      and conflicts with the data/git-centered operating model. |

      | Rely only on git conflicts | Git conflicts do not protect local sequence
      allocation, staging directories, deployment leases, or repeated CLI
      retries. |

      | Let each command implement its own lock file | This recreates the drift
      problem the RFC is solving; recovery and validation need one helper
      contract. |

      | Delete stale locks automatically without inspection | Could hide partial
      writes. Recovery must classify related artifacts before removing a lock. |

      | Extend existing `readLock`/`hashFile` from `bundle-io.ts` | Those
      helpers are narrow RFC-0221 transfer-lock utilities without scopes,
      heartbeats, idempotency, or atomic staging. Generalizing them in place
      would conflate transfer-lock and Werkstatt-lock semantics; a new
      `werkstatt/` module keeps the contracts separate. |
    file: docs/rfcs/archive/implemented/rfc-0362-werkstatt-consistency-primitives-for-locks-idempotency-and-atomic-state.md
  - kind: rejected-alternative
    rfcId: RFC-0363
    title: Release artifact store and retention contract
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Keep only `releases/<id>/dist/` | It is a gitignored local cache with no
      durability, retention, or content-addressing. |

      | Store artifacts in Sternsystem git repos | Build output is large and
      binary; Sternsystem repos stay data-only. |

      | Require cloud object storage immediately | MVP can start local while
      preserving a provider abstraction for R2/S3 later. |

      | Let deployment providers be the artifact store | Deployment targets are
      not audit stores and may not preserve rollback/notausgang requirements. |
    file: docs/rfcs/archive/implemented/rfc-0363-release-artifact-store-and-retention-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0364
    title: Semantic fingerprint package and hash governance
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Keep using `crypto.createHash` directly | Direct byte hashes do not
      document semantic intent and drift across helpers. |

      | Use only byte hashes everywhere | Correct for artifacts, too noisy for
      platform semantic compatibility and content fingerprints. |

      | Use regex-based normalization | Too fragile for TypeScript, Astro, CSS,
      JSONC, Markdown, and YAML. Parser-backed normalization is required. |

      | Pull in a general-purpose build cache hash library only | Build cache
      hashes optimize rebuilds; they do not define project-level policy or
      validation against ad hoc hash usage. |

      | Extend `site-kernel-integrity/src/hash.ts` or `check-core/src/hash.ts`
      with semantic normalization | Both modules are byte-only helpers scattered
      across two packages with no shared contract; adding parser-backed AST
      normalization to either would couple a specific consumer package with
      general-purpose parsing and leave the other consumer without the API. |
    file: docs/rfcs/archive/implemented/rfc-0364-semantic-fingerprint-package-and-hash-governance.md
  - kind: rejected-alternative
    rfcId: RFC-0365
    title: Rename backs to services and clarify integration topology
    status: implemented
    alternativesText: >-
      - **Keep `backs/*`.** Rejected because the name is internal shorthand and
      does not read well for external maintainers, contractors, or
      post-Notausgang ecosystem work.

      - **Rename to `backends/*`.** Clear, but slightly narrower than the actual
      layer: compose stacks, proxies, schedulers, bridges, and worker envelopes
      are all services.

      - **Move everything under `packages/*`.** Rejected because deployable
      composition would again mix with reusable libraries.

      - **Delete `integrations/*`.** Rejected because source descriptors and
      monitor state are useful declarative registries and are not service
      workspaces.

      - **Use `services.validate` instead of `services.workspace.validate`.**
      Rejected because the workspace validator has a specific boundary role.
      `services.check.run` is the composite pipeline.
    file: docs/rfcs/archive/implemented/rfc-0365-rename-backs-to-services-and-clarify-integration-topology.md
  - kind: superseded-rfc
    rfcId: RFC-0366
    title: Introduce Architectural Decision Records and retire the RFC mini-template
    status: superseded
    closedAt: 2026-07-20
    decisionSummary: The kernel gains an `adr.*` command domain that treats
      Architectural Decision Records (ADRs) as first-class Site OS artifacts.
      ADRs live in `docs/adrs/`, follow a lightweight template, and are
      validated by `adr.validate`.
    file: docs/rfcs/archive/implemented/rfc-0366-introduce-architectural-decision-records-and-retire-mini-rfc-template.md
  - kind: rejected-alternative
    rfcId: RFC-0366
    title: Introduce Architectural Decision Records and retire the RFC mini-template
    status: superseded
    alternativesText: >-
      1. **Keep the mini-template for command/policy RFCs.** Rejected because
      the mini-template already failed to reduce ceremony enough, and the
      existing command/policy RFCs are better with full analysis. ADRs now cover
      the lightweight niche.

      2. **Use only informal documentation (wiki, Notion, PR comments).**
      Rejected because informal docs are not version-controlled, not
      machine-validated, and not discoverable by agents during implementation.

      3. **Extend every RFC with a "micro-decision" appendix.** Rejected because
      it would bloat RFCs and blur the line between a decision record and a
      governance document. A separate ADR file keeps RFCs focused and ADRs
      granular.
    file: docs/rfcs/archive/implemented/rfc-0366-introduce-architectural-decision-records-and-retire-mini-rfc-template.md
  - kind: rejected-alternative
    rfcId: RFC-0367
    title: Archive terminal-status RFC and ADR documents into status subdirectories
    status: implemented
    alternativesText: >-
      - **Status-named subdirectories at root level
      (`docs/rfcs/implemented/`).** Rejected in favor of a single `archive/`
      parent to keep the root directory clean — one entry point for all archived
      documents rather than three.

      - **Index-based tracking (manifest of moved files).** Rejected — adds a
      state file that can drift from the filesystem. Recursive discovery is
      simpler and always correct.

      - **Amend RFC-0366 instead of superseding.** Rejected — the ADR lifecycle
      change is substantial enough (two new statuses, three new frontmatter
      fields, full RFC parity) that a clean supersede is clearer than layering
      amendments.

      - **One-way archive (only move terminal into subdirs, never back).**
      Rejected — bidirectional movement ensures the filesystem state always
      matches the document's current status, even if a status is reverted.

      - **Pipeline gate (fail build.check if terminal files are in root).**
      Rejected — archiving is housekeeping, not a correctness gate. Adding it to
      pipelines would create noise for work-in-progress branches.
    file: docs/rfcs/archive/implemented/rfc-0367-archive-terminal-status-rfc-and-adr-documents-into-status-subdirectories.md
  - kind: rejected-alternative
    rfcId: RFC-0368
    title: Establish Windows agent tooling parity and command allow-list
    status: implemented
    alternativesText: >-
      - **Mandate WSL for everything.** Rejected. Native Windows tools (git,
      node, pnpm, docker) are faster and better integrated with the IDE. WSL is
      the bridge, not the default.

      - **Mandate Chocolatey or Scoop instead of winget.** Rejected. winget is
      built into Windows 10/11 and is the path of least friction for new
      contributors. The skill may offer choco/scoop as an optional branch for
      advanced operators.

      - **Provide a Docker-based dev container.** Rejected. Docker Desktop is
      listed as an optional tool, but requiring it raises the barrier too high
      for a policy whose goal is to make Windows AI editing reliable, not to
      standardize a Linux container.

      - **Add a `.bashrc`/`.profile` polyfill.** Rejected. Agents must know the
      real environment, not a fake one. Polyfills hide tool provenance and make
      debugging harder.
    file: docs/rfcs/archive/implemented/rfc-0368-establish-windows-agent-tooling-parity-and-command-allow-list.md
  - kind: rejected-alternative
    rfcId: RFC-0369
    title: Implement agent.environment.audit command
    status: implemented
    alternativesText: >-
      - **Shell script instead of TypeScript command.** Rejected because a
      Node-based command is cross-platform, easier to maintain, and can reuse
      existing `@gogol/share` utilities and the kernel logger.

      - **Inline detection inside the `windows-ai-tooling` skill only.**
      Rejected because a skill is user-invoked and not available to every agent
      session; a Site OS command is accessible to all agents and CI.

      - **Make the command fail hard on missing tools.** Rejected because
      environment state varies across machines; an advisory report lets
      operators decide what to install.

      - **Add Linux/macOS-specific install hints.** Rejected as out of scope;
      the primary gap is Windows, and the same Node APIs already produce correct
      results on POSIX systems without extra hints.
    file: docs/rfcs/archive/implemented/rfc-0369-implement-agent-environment-audit-command.md
  - kind: rejected-alternative
    rfcId: RFC-0370
    title: Establish operator preferences file and my-preferences skill
    status: implemented
    alternativesText: >-
      - **Environment variables** (`PREFERENCES_BOT_LANGUAGE=ru`): rejected
      because they are session-local and not committed; preferences should
      travel with the repository.

      - **Section in `AGENTS.md`**: rejected because `AGENTS.md` is agent
      instructions, not operator settings; editing it for every language change
      would be noisy.

      - **JSON file**: rejected because Markdown with frontmatter is already the
      canonical CMS-friendly content format in this repo (RFC-0047).
    file: docs/rfcs/archive/implemented/rfc-0370-establish-operator-preferences-file-and-my-preferences-skill.md
  - kind: rejected-alternative
    rfcId: RFC-0371
    title: Adopt Fontsource CSS imports and remove the copy-to-public font pipeline
    status: implemented
    alternativesText: >-
      - **Keep the copy-to-public pipeline (RFC-0164 as-is):** rejected — it
      stores font binaries in the thin site, uses a global union registry, and
      duplicates Vite's native asset bundling.

      - **Astro Font Provider API with `@fontsource/*` auto-detection:** Astro 6
      can auto-detect `@fontsource/*` packages from `package.json`. This is
      complementary but does not provide biome-driven weight selection or the
      validation rules this RFC requires. The `fonts.imports.generate` command
      produces explicit, auditable CSS imports rather than relying on implicit
      auto-detection.

      - **Variable font packages (`@fontsource-variable/*`):** rejected for this
      RFC to stay consistent with the current static-weight pattern. A future
      RFC may adopt variable fonts if a biome needs continuous weight axis
      control.

      - **Google Fonts CDN with consent banner:** rejected — cookies/consent
      banners are forbidden by the storage policy, and self-hosting removes the
      GDPR exposure entirely.

      - **System-only font stack (no Fontsource):** rejected — the biome
      typography tokens reference specific font families (Playfair Display, DM
      Mono, Inter, Lora) that must be self-hosted for visual consistency.
    file: docs/rfcs/archive/implemented/rfc-0371-adopt-fontsource-css-imports-and-remove-copy-to-public-font-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0372
    title: Unify semantic block projection and eliminate home page special casing
    status: implemented
    alternativesText: >-
      - **Extend `buildHomePageSemantic()` with warpgogol-com block IDs:**
      Rejected — perpetuates the two-path architecture and requires updating the
      hardcoded list every time a new block type is added to any home page. The
      user explicitly requested no legacy.

      - **Hybrid: keep home builder for answerBlocks, add extractContentBlocks
      for contentBlocks:** Rejected — maintains the split model and two
      rendering paths. The user explicitly requested a clean break.

      - **Keep `bodyText` as a computed getter from `blocks`:** Rejected — adds
      complexity for no consumer benefit. All consumers can read `blocks`
      directly.

      - **Soft validator (warn, not fail) for missing extractors:** Rejected —
      the user explicitly requested a hard contract. Silent omissions in
      Markdown twins are the exact problem this RFC solves.

      - **Strengthen `page.blocks.validate` in-place instead of renaming to
      `page.blocks.extract.validate`:** Rejected — the rename follows the
      established `page.blocks.<aspect>.validate` naming pattern
      (`page.blocks.mirror.validate` already exists). `page.blocks.validate` is
      the only generic name in the family; the new name communicates its
      specific role (extractor coverage). The churn (two generated baselines,
      one command table entry, one pipeline registration) is mechanical and
      one-time.
    file: docs/rfcs/archive/implemented/rfc-0372-unify-semantic-block-projection-and-eliminate-home-page-special-casing.md
  - kind: rejected-alternative
    rfcId: RFC-0373
    title: Extract services at organization level from the business catalog
    status: implemented
    alternativesText: >-
      **Keep services per-page (Option B).** Rejected. Services are site-wide
      facts about the business, not properties of individual pages. Per-page
      projection duplicates the same service list across every page's semantic
      model and creates a false per-page granularity. The offer projection
      (RFC-0147) is already org-level for the same reason.


      **Combined org + page (Option C).** Rejected. Two projection paths for the
      same concept create ambiguity: which source wins? The business catalog is
      the canonical source; block-derived services are a fallback that existed
      only because the catalog projection was deferred. Once the catalog is
      wired, the fallback is unnecessary.


      **Extend `SemanticService` with category, serviceType, pricingOptions.**
      Rejected as over-engineering for this RFC. The minimal shape (id, name,
      description) is sufficient for JSON-LD `Service` nodes and llms.txt.
      Richer fields can be added by a future RFC if AI consumers need them.


      **Synthesize description from category + serviceType.** Rejected.
      Synthesized descriptions are less faithful than authored ones. Adding
      `description` to the business schema is a one-line change that gives
      authors control.
    file: docs/rfcs/archive/implemented/rfc-0373-extract-services-at-organization-level-from-business-catalog.md
  - kind: rejected-alternative
    rfcId: RFC-0374
    title: Extract @gogol/forge — portable feature implementation ecosystem
    status: implemented
    alternativesText: >-
      1. **Two packages (wg-specific + general)**: Rejected because skills call
      each other across the boundary (e.g., `improve-codebase-architecture`
      calls `/grilling`, `wg-idea` calls `/wg-idea-create-rfc`). A single
      package keeps cross-references simple.


      2. **Forge = skills + contract only (no OS migration)**: Rejected because
      the operator explicitly wants a full portable ecosystem, not just a skill
      store. Without migrating generic commands, forge cannot self-deploy or
      self-validate.


      3. **Parallel runtime (forge CLI separate from site-kernel)**: Rejected
      because it creates two command registries, two CLI entry points, and
      doubles maintenance. The existing `KernelModule` interface is sufficient
      for forge to register commands in the same registry.


      4. **Re-export (forge wraps site-kernel modules)**: Rejected because it
      does not achieve portability — forge would depend on site-kernel, which
      contains project-specific code. The whole point is that forge is
      installable in a project that does not have site-kernel.


      5. **Copy-based discovery**: Rejected because it creates duplication
      between `packages/forge/skills/` and `.agents/skills/`. However, this is
      the chosen approach (see Decision) because it is the only mechanism that
      works with the current IDE architecture without requiring symlink support
      or IDE changes. The duplication is managed by `forge.init` (sync command)
      and the registry is the source of truth.


      6. **ADR instead of RFC**: Rejected because this is a cross-workspace
      architectural decision affecting monorepo topology, package boundaries,
      and command ownership — RFC scope, not ADR scope.
    file: docs/rfcs/archive/implemented/rfc-0374-extract-forge-portable-feature-implementation-ecosystem.md
  - kind: rejected-alternative
    rfcId: RFC-0375
    title: Universal generated-file detection and agent lookup command
    status: implemented
    alternativesText: >-
      - **Sidecar files for binary assets** (e.g. `foo.webp.generated.json`).
      Rejected — creates thousands of sidecar files next to image/video
      variants. The registry already knows every generated path; sidecars
      duplicate that information at filesystem cost.


      - **Concrete `generated-files.manifest.json` listing every individual
      file.** Rejected — would be enormous (hundreds of image variants, HLS
      segments), needs constant regeneration, and drifts. Dynamic lookup via
      glob-matching against existing registries is always current.


      - **Path-based heuristic without `markerPolicy` field.** Rejected —
      `public/**` → B is a clean default, but explicit `markerPolicy` in
      `OwnershipEntry` allows exceptions and is machine-readable without
      ambiguity. The field is the source of truth; the path rule is the
      convention for populating it.


      - **Keep markers in public files, move to footer.** Rejected — `llms.txt`
      and `public/*.md` are consumed by LLMs that read the full file. A footer
      marker is still pollution, just at the end. The registry-only approach
      eliminates the marker entirely from external-facing output.


      - **Registry-only for all files (remove marker everywhere).** Rejected —
      internal text files benefit from self-describing markers. An agent reading
      `src/pages/index.astro` sees the advisory block immediately, without
      running a command. The marker also enables `dist.generated-marker.strip`
      to clean copied files. Category A files keep the marker.


      - **Incremental rollout (binary first, then public text).** Rejected —
      creates a transition period where two mechanisms work in parallel and
      `generated.marker.validate` behavior is ambiguous. One-shot rollout is
      cleaner and forward-only.


      - **Two separate RFCs.** Rejected — the lookup command, edit guard
      extension, and marker removal are tightly coupled. The lookup command
      needs `markerPolicy` to report category; the edit guard needs `module` for
      Category B resolution; the marker removal needs `markerPolicy` to know
      which files to update. Splitting would create intermediate states where
      the pieces don't compose.
    file: docs/rfcs/archive/implemented/rfc-0375-universal-generated-file-detection-and-agent-lookup.md
  - kind: rejected-alternative
    rfcId: RFC-0376
    title: Migrate generated artifacts and project configs from JSON to YAML
    status: implemented
    alternativesText: >-
      - **Keep JSON for Astro-consumed generated manifests.** Rejected —
      `import.meta.glob` with `query: "?raw"` reads any text file as a string;
      parsing happens in userland code. The codebase already reads
      `.manifest.yaml` and `.credits.yaml` via this mechanism. Changing
      `loadGeneratedManifest()` is a 3-line edit.


      - **Incremental migration with feature flags.** Rejected — forward-only
      architecture. A transition period creates double maintenance (both
      formats, two lint passes, dual read paths). Big-bang is cleaner for ~35
      files.


      - **Size-based exceptions for large generated registries.** Rejected —
      generated files are not hand-read. Uniformity is more valuable than
      compactness. `yaml.stringify()` with block-style is the default for
      machine-generated output.


      - **Hardcoded whitelist in lint command code.** Rejected — a config file
      is more transparent and auditable. The whitelist changes rarely (only when
      a new tool is added), but when it does, a config file makes the change
      visible in a PR without code changes.


      - **Keep `buildGeneratedJsonAdvisory()` as dead code.** Rejected —
      forward-only. Dead code confuses agents and maintainers. The function
      exists solely because JSON lacks comments; YAML removes that constraint.


      - **Use `js-yaml` instead of `yaml` (Eemeli AY).** Rejected — `js-yaml`
      drops comments on `dump()`, which is critical for generated advisory
      headers. `yaml` preserves comments and is already in the dependency tree.


      - **Use Vite built-in YAML support (no `?raw`).** Rejected — would require
      a Vite plugin (`vite-plugin-yaml`) and removes control over parsing
      options. The `?raw` + `yaml.parse()` pattern is already proven in the
      codebase.
    file: docs/rfcs/archive/implemented/rfc-0376-migrate-generated-artifacts-and-project-configs-from-json-to-yaml.md
  - kind: rejected-alternative
    rfcId: RFC-0377
    title: Standardize semantic frontmatter and body sections for route Markdown twins
    status: implemented
    alternativesText: >-
      - **Keep provenance-only frontmatter, add semantic metadata to JSON-LD
      only.** Rejected. JSON-LD is embedded in HTML and not available to agents
      that fetch the `.md` twin directly. The twin must be self-describing.

      - **Use a separate sidecar JSON file per twin (e.g. `preis.md.json`).**
      Rejected. Doubles the number of generated files and breaks the
      single-artifact contract. YAML frontmatter is the established convention
      for Markdown metadata.

      - **Make all semantic fields authored (no derivation).** Rejected. Would
      impose per-page authoring burden for fields that can be reliably derived
      from `SemanticPageType`. The hybrid approach (authored `audience` with
      fallback, derived everything else) minimizes authoring cost while allowing
      per-page overrides.

      - **Keep the current body structure (`H1 → lead → blocks → people →
      initiatives`).** Rejected. The current structure is page-type-dependent
      and unpredictable for agent navigation. The standardized section pattern
      (Summary, Business context, Data / APIs, User flows, Constraints) is
      page-type-independent and maps to the AGENTS.md/SKILL.md convention.

      - **Use JSON frontmatter instead of YAML.** Rejected. YAML is the
      established convention in this ecosystem (system.md, manifests, RFCs) and
      is more readable for the optional `tags` list.
    file: docs/rfcs/archive/implemented/rfc-0377-standardize-semantic-frontmatter-and-body-sections-for-route-markdown-twins.md
  - kind: rejected-alternative
    rfcId: RFC-0378
    title: Redefine the Werkstatt command surface beyond apps
    status: implemented
    alternativesText: >-
      - **Keep the `--app` flag name and only extend discovery.** Rejected:
      after `apps/` is deleted, the flag name is a permanent semantic lie; the
      ecosystem already paid for the same fix once (RFC-0365 backs → services)
      and established the rename discipline.

      - **Alias `--app` to `--site` during a transition window.** Rejected:
      violates the forward-only DNA (no dual paths); the alias would leak into
      agent memories and docs and never die.

      - **Materialize missions inside `apps/` to avoid touching discovery.**
      Rejected: reintroduces the workspace-member coupling RFC-0354 exists to
      remove, and makes `apps/` removal impossible.

      - **Teach every command about missions individually.** Rejected: N edit
      sites instead of one resolver seam; guarantees drift.
    file: docs/rfcs/archive/implemented/rfc-0378-redefine-the-werkstatt-command-surface-beyond-apps.md
  - kind: rejected-alternative
    rfcId: RFC-0379
    title: Implement the cloudflare-workers Leitstand adapter with health verification
    status: implemented
    alternativesText: >-
      - **Implement `cloudflare-pages` as RFC-0358 originally named.** Rejected:
      RFC-0149 retired the Pages form; the ecosystem deploys single Workers.
      Keeping the name would codify a dead platform shape.

      - **Deploy via Cloudflare REST API instead of wrangler.** Rejected:
      wrangler is already the proven deploy path for `warpgogol-com`
      (`deploy:alt`/`deploy:main` scripts), handles bundling/limits/secrets
      natively, and is pinned in the workspace. The adapter seam allows an
      API-based adapter later without a contract change.

      - **Traffic-split canary on one Worker.** Rejected for this wave: adds
      runtime routing complexity; the alt Worker already provides a
      full-fidelity staging environment. Canary remains a future adapter
      extension.

      - **Automatic rollback on failed health check.** Rejected: on the alt
      channel rollback is pointless (nothing user-facing broke); on main it can
      mask a bad _previous_ release. Rollback stays an explicit operator/agent
      decision.
    file: docs/rfcs/archive/implemented/rfc-0379-implement-the-cloudflare-workers-leitstand-adapter-with-health-verification.md
  - kind: rejected-alternative
    rfcId: RFC-0380
    title: Complete Notausgang integrity verification
    status: implemented
    alternativesText: >-
      - **New `notausgang.verify` command:** Rejected — adding a second command
      splits the validation surface and creates confusion about which to run.
      Upgrading the existing command is cleaner.

      - **Keep JSON artifacts:** Rejected — RFC-0376 mandates YAML for generated
      artifacts. Notausgang manifests are generated artifacts.

      - **Keep ad hoc hashing:** Rejected — DNA-53 explicitly forbids new ad hoc
      hashing helpers outside `@gogol/fingerprint`.

      - **Full Bordbuch semantic validation (event ordering, type-specific
      fields):** Rejected as out of scope — this RFC validates structural
      integrity (valid JSON, required fields), not semantic consistency of the
      event log. That belongs in a separate Bordbuch validation RFC.
    file: docs/rfcs/archive/implemented/rfc-0380-complete-notausgang-integrity-verification.md
  - kind: rejected-alternative
    rfcId: RFC-0381
    title: Execute the pilot migration wave and retire apps
    status: implemented
    alternativesText: >-
      - **Keep apps/ as a parallel structure:** Rejected — DNA-44 explicitly
      states that after extraction validates, the source `apps/<id>` workspace
      is removed. Keeping it would violate the invariant and create dual
      representation.

      - **Propagate to main first:** Rejected — the operator decided alt-first
      to catch issues on a non-production URL before affecting production
      traffic.

      - **Use GitHub for Sternsystem git hosting:** Rejected for pilot — local
      bare repo is simpler and sufficient. GitHub hosting will be a separate
      step for collaboration and CI.

      - **Retire apps/ in a separate RFC:** Rejected — the extraction and
      retirement are one atomic migration. Splitting them leaves the platform in
      a half-migrated state with no clear owner for the cleanup step.

      - **Run multiple sites in the pilot:** Rejected — `warpgogol-com` is the
      only remaining site in `apps/`. A single-site pilot is the minimal viable
      proof.
    file: docs/rfcs/archive/implemented/rfc-0381-execute-the-pilot-migration-wave-and-retire-apps.md
  - kind: rejected-alternative
    rfcId: RFC-0382
    title: Accelerate RFC commands with manifest-first validation and SQLite cache
    status: implemented
    alternativesText: >-
      - **better-sqlite3 as mandatory dependency:** Rejected — native module
      risk on Windows without Visual Studio Build Tools. Graceful degradation
      ensures commands always work.

      - **Pure-JS SQLite (sql.js, node:sqlite):** Rejected — sql.js loads the
      entire DB into memory (slow for persistent cache); `node:sqlite` requires
      Node 22+ and is still experimental. better-sqlite3 with fallback is more
      robust.

      - **File-based YAML/JSON cache:** Rejected — 367 entries with per-entry
      mtime+hash checks in a flat file is slower than SQLite indexed lookup.
      SQLite also provides atomic writes and crash safety.

      - **Caching tsImport results:** Rejected for this RFC — manifest-first
      (Phase 1) already eliminates the primary tsImport bottleneck for
      `rfc.validate`. tsImport caching is a separate risk and deserves its own
      RFC.

      - **Two separate RFCs:** Rejected — manifest-first and SQLite cache are
      one coherent acceleration effort. One RFC with two phases provides a
      single narrative and acceptance gate.
    file: docs/rfcs/archive/implemented/rfc-0382-accelerate-rfc-commands-with-manifest-first-validation-and-sqlite-cache.md
  - kind: rejected-alternative
    rfcId: RFC-0383
    title: Add surface.graph.validate for SEO link-structure diagnostics
    status: draft
    alternativesText: >-
      - **Precompute adjacency indexes during generation (RFC-0275 AC-5).**
      Rejected: the current surface has dozens to low-hundreds of entries.
      In-memory graph computation from the artifact is milliseconds. Adjacency
      indexes are a premature optimization for 10K+ page surfaces.

      - **Add graph checks to `surface.validate`.** Rejected: `surface.validate`
      already has a broad scope (structural integrity, slug collisions,
      redirects, narratives). Graph diagnostics are a distinct concern with
      different rule semantics. A separate command keeps each validator focused
      and allows independent pipeline gating.

      - **Use a third-party SEO crawler.** Rejected: external crawlers run after
      deployment, not in CI. They cannot inspect the generated surface before
      Astro SSG. They also cannot map links to `pageId` — they see URLs, not the
      surface model.
    file: docs/rfcs/rfc-0383-add-surface-graph-validate-for-seo-link-structure-diagnostics.md
  - kind: rejected-alternative
    rfcId: RFC-0384
    title: Add surface.plan.generate for pre-build sizing visibility and supersede
      RFC-0275
    status: draft
    alternativesText: >-
      - **Keep RFC-0275 as draft and implement fully.** Rejected: 70% of
      RFC-0275 optimizes for a scale the product does not target. The RFC itself
      states "the default single-site target is bounded managed coverage, not
      six-figure route counts" and "editorial review remains the real
      bottleneck." Implementing sharding, adjacency indexes, and dirty flags now
      would be weeks of work for no observable benefit.

      - **Close RFC-0275 as rejected.** Rejected: two of its ideas (plan
      command, graph validator) have genuine value. Superseding with extraction
      is more precise than blanket rejection.

      - **Add plan checks to `surface.validate`.** Rejected: `surface.validate`
      runs _after_ generation. The whole point of the plan is to run _before_
      baking and SSG. A separate command is the correct architectural boundary.

      - **Use Astro build hooks for cardinality checks.** Rejected: Astro SSG
      starts after `surface.generate` writes the artifact. By then, the
      expensive generation is already done. The check must run before baking,
      inside the kernel command layer.
    file: docs/rfcs/rfc-0384-add-surface-plan-generate-for-pre-build-sizing-visibility-and-supersede-rfc-0275.md
  - kind: rejected-alternative
    rfcId: RFC-0385
    title: Fix Lagebild buffer tenant-secret key drift
    status: implemented
    alternativesText: >-
      - **Read both `TENANT_ID` and `SUPABASE_BUFFER_TENANT_ID` (compatibility
      alias).** Rejected: violates forward-only discipline; leaves two names for
      one fact and re-opens the drift.

      - **Rename the read to `TENANT_ID` instead of the contract.** Rejected:
      `TENANT_ID` is unprefixed and collides with unrelated tenant variables;
      the `SUPABASE_BUFFER_*` prefix is the established convention for this
      adapter's secrets and is already what the spec documents.
    file: docs/rfcs/archive/implemented/rfc-0385-fix-lagebild-buffer-tenant-secret-key-drift.md
  - kind: rejected-alternative
    rfcId: RFC-0386
    title: Complete Stripe lifecycle sync deltas for Tier 2
    status: implemented
    alternativesText: >-
      - **Keep manual operator confirmation permanently.** Rejected: Tier 2's
      whole purpose is automated billing; a permanent manual branch is legacy
      the ecosystem forbids.

      - **Drive Pipedrive P3/P4 from a Stripe-native automation (e.g.
      Make.com).** Rejected: RFC-0188 forbids Make.com anywhere in the
      funnel/billing path; the platform owns the projection through the shared
      worker.

      - **Persist lifecycle directly from the webhook route.** Rejected:
      violates RFC-0177 (in-flight only at inbound) and DNA-1 (route stays a
      thin proxy); persistence belongs to the buffer via the delivery callback.
    file: docs/rfcs/archive/implemented/rfc-0386-complete-stripe-lifecycle-sync-deltas-for-tier-2.md
  - kind: rejected-alternative
    rfcId: RFC-0387
    title: Activate warpgogol-com integration and go-live runbook
    status: draft
    alternativesText: >-
      - **Edit `system.md` directly in a working copy.** Rejected: sites are
      data-only Sternsystems; all changes pass through a mission (DNA-46/47).

      - **Keep the direct `pipedrive` CRM destination and add the buffer
      alongside.** Rejected: `integration.config.validate` reports
      `multiple-active-executors` for `(kind=crm)`; buffer-first is the single
      chosen executor.

      - **Document the runbook only in `docs/specs`, not an RFC.** Rejected:
      activation amends the operational contract of RFC-0186/0188/0191 and
      defines a pipeline-promotion policy (Phase 9), which needs RFC governance;
      the spec is the detailed companion, kept in sync by this RFC.
    file: docs/rfcs/rfc-0387-activate-webgogol-com-integration-and-go-live-runbook.md
  - kind: rejected-alternative
    rfcId: RFC-0388
    title: Unify env-file standard and deploy preflight for systems, services, and
      root
    status: implemented
    alternativesText: >-
      - **Amend RFC-0346 instead of superseding:** Rejected. The file naming
      changes (`.env.production` → `.env.main`, `.dev.vars` → `.env`), new
      commands (`deploy.preflight`, `env.alt.check`, `env.main.check`), and
      expanded scope (root, `systems/*`) are substantial enough to warrant a
      clean supersede.

      - **Keep `.dev.vars` for Cloudflare Workers:** Rejected. Wrangler 4.x
      reads `.env` natively for both `wrangler dev` and `wrangler deploy
      --secrets-file`. Having two conventions (`.dev.vars` for Workers, `.env`
      for everything else) adds cognitive load without benefit.

      - **Support backward compatibility with `.env.production`:** Rejected. All
      env files are gitignored and exist only on operator machines. The
      migration is a one-time rename with no external consumers.

      - **Optional `# How to obtain:` (convention, not enforced):** Rejected.
      The operator explicitly requested enforced instructions. Without
      enforcement, instructions drift and onboarding suffers.

      - **Separate `.env.local` for local dev (not `.env`):** Rejected. `astro
      dev` and `wrangler dev` both read `.env` natively. Introducing
      `.env.local` would require custom loading logic. Using `.env` for local
      dev is the Vite/Wrangler convention.
    file: docs/rfcs/archive/implemented/rfc-0388-unify-env-file-standard-and-deploy-preflight-for-systems-services-and-root.md
  - kind: rejected-alternative
    rfcId: RFC-0389
    title: Full boilerplate generation for mission materialization
    status: implemented
    alternativesText: >-
      1. **Run `build.prepare` pipeline instead of inline codegen calls.**
      Instead of calling individual codegen generators, run the full
      `SITES_BUILD_PREPARE_PIPELINE` against the staging Werkstück. Rejected
      because the pipeline includes validators (`yaml.contract.lint`,
      `manifest.contract.validate`, `mirror.quintet.validate`,
      `uni.registry.build`, `generated.files.validate`) that may fail on a
      freshly staged Werkstück before all generators have run. The pipeline is
      designed for an already-bootstrapped site, not for initial
      materialization. The correct pattern is the one `onboarding.scaffold`
      uses: write templates → run `kernel.wire` → run codegen generators → then
      the pipeline can run.


      2. **Copy an existing app directory as a template.** Rejected because it
      violates the generation-first principle (RFC-0078) and would carry stale
      generated markers, app-specific content, and potential drift. Templates
      are the canonical source.


      3. **Defer to a future `mission.scaffold` command.** Rejected because
      RFC-0356 §1.1 steps 7-8 already mandate that `mission.materialize`
      generates runtime boilerplate and regenerates derived artifacts. Creating
      a separate command would split the materialization contract and require
      two commands where one is specified.


      4. **Generate only `package.json` and `astro.config.mjs` from templates,
      skip codegen.** Rejected because the Werkstück would still lack routes,
      middleware, styles, scripts, public infrastructure, and tools/ wiring.
      `mission.validate` would still fail. The codegen generators are fast and
      deterministic; there is no reason to skip them.
    file: docs/rfcs/archive/implemented/rfc-0389-full-boilerplate-generation-for-missions.md
  - kind: rejected-alternative
    rfcId: RFC-0390
    title: Cache pipeline command results by declared reads hash
    status: implemented
    alternativesText: >-
      - **Auto-tracking via `context.io` interception:** Wrap `readFile` in
      `createDefaultIO()` to collect read paths automatically. Rejected because
      many commands use direct `node:fs/promises` imports, not `context.io`, so
      tracking would be incomplete. Also requires monkey-patching or IO
      abstraction adoption across all commands.

      - **New `inputs` field instead of reusing `reads`:** Rejected because
      `reads` already exists (RFC-0266), some commands already declare it, and
      the `<app>` token resolution is already defined. Adding a parallel field
      would create confusion and duplication.

      - **Full `KernelExecutionReport` caching vs. pass/fail only:** Rejected
      pass/fail-only because cached failures would lose diagnostic output (logs,
      violations), making cache hits less useful than real executions.

      - **Import-graph analysis for module hash:** Rejected as over-engineering.
      Hashing the entire `src/` directory of the command's package is simpler
      and safer (any change invalidates all commands in that package, which is
      acceptable).

      - **Turbo cache for `build:check`:** Rejected because `turbo.json` has
      `cache: false` for `build:check` tasks by design — the 176 internal
      commands are not visible to Turbo. Our cache operates inside the process,
      orthogonal to Turbo's task-level cache.

      - **File-based cache (JSON files on disk):** Rejected because the
      `CacheLayer` (RFC-0382) with SQLite already exists and handles mtime +
      contentHash invalidation. Adding a parallel file-based cache would
      duplicate infrastructure.
    file: docs/rfcs/archive/implemented/rfc-0390-cache-pipeline-command-results-by-declared-reads-hash.md
  - kind: rejected-alternative
    rfcId: RFC-0391
    title: Complete forge autonomy with forge.yaml project config and portable init
    status: implemented
    alternativesText: >-
      - **Keep configuration implicit (detect everything at runtime).**
      Rejected: detection is fine for bootstrap defaults, but agents and
      commands need one stable, reviewable record of the decisions; re-detection
      on every run is slow and non-deterministic across machines.

      - **Store forge config inside `package.json` (`"forge"` key).** Rejected:
      not stack-agnostic (a Python or Rust project has no `package.json`), and
      violates the YAML-only artifact decision (RFC-0376).

      - **Write a retroactive RFC for the already-landed autonomy refactor.**
      Rejected: forward-only governance documents decisions that still need
      executing; the landed refactor is acknowledged in Context instead.

      - **Keep `packages/os/site-kernel/src/rfc/` as a fallback.** Rejected:
      dual code paths are forbidden (forward-only); `tools/kernel.config.ts`
      already consumes the forge module.
    file: docs/rfcs/archive/implemented/rfc-0391-complete-forge-autonomy-with-forge-yaml-project-config-and-portable-init.md
  - kind: rejected-alternative
    rfcId: RFC-0392
    title: Add forge stack profiles and the fo-harvest self-growth loop
    status: implemented
    alternativesText: >-
      - **Frozen template repositories (degit-style).** Rejected: template
      snapshots rot; install-based profiles always produce current dependency
      versions.

      - **Supporting arbitrary stacks in `@1` (Cargo, Poetry, Go modules).**
      Rejected: honest scope control — Turborepo + pnpm is the only monorepo
      shape forge understands today; pretending otherwise produces broken
      scaffolds. Unsupported stacks are reported.

      - **Automatic harvest (scheduled scan, auto-port).** Rejected: portability
      judgment needs the operator; auto-porting would flood the canonical forge
      with project-specific noise.

      - **Extending `forge.port.scaffold` to scaffold projects.** Rejected:
      skill/command skeletons and whole-monorepo creation are different
      responsibilities with different failure modes; overloading one command
      hides both.
    file: docs/rfcs/archive/implemented/rfc-0392-add-forge-stack-profiles-and-the-fo-harvest-self-growth-loop.md
  - kind: rejected-alternative
    rfcId: RFC-0393
    title: Introduce the forge bindings contract and de-hardcode fo-skills
    status: implemented
    alternativesText: >-
      - **Per-stack skill variants** (`fo-idea-astro`, `fo-idea-python`).
      Rejected: combinatorial explosion, instant divergence, `fo-harvest`
      becomes unmanageable.

      - **Projects edit deployed skill copies.** Rejected: this is the status
      quo failure mode — deployed copies fork from canonical forge and updates
      become merge conflicts.

      - **Environment variables instead of `forge.yaml` bindings.** Rejected:
      not reviewable, not versioned with the project, invisible to agents
      reading the repo.

      - **A binding indirection service (runtime resolver CLI).** Rejected:
      over-engineering — skills are prose read by agents; a documented lookup
      convention into one YAML file is sufficient and debuggable.
    file: docs/rfcs/archive/implemented/rfc-0393-introduce-the-forge-bindings-contract-and-de-hardcode-fo-skills.md
  - kind: rejected-alternative
    rfcId: RFC-0394
    title: Adopt specification vendoring with forge-spec.yaml and spec.validate
    status: implemented
    alternativesText: >-
      - **Reference specs in place (external path / Obsidian vault).** Rejected:
      not versioned with the code, invisible to CI and other agents, breaks
      reproducibility.

      - **Vendor only `forge-spec.yaml`, discard source documents.** Rejected:
      RFC-0396 materialized RFCs normatively reference source sections instead
      of copying model content — the sources must be in-repo.

      - **Git submodule per spec.** Rejected: heavy machinery for "a consultant
      sent a folder of markdown"; submodules are a known operational footgun on
      Windows agent environments.

      - **Mass-import spec decisions into `docs/adrs/`.** Rejected: id collision
      with repository ADRs, dual source of truth, and 50 project-registry
      entries meaningless outside the spec's context. Selective promotion with
      `promotedTo` covers the genuine cases.

      - **Require external authors to deliver `forge-spec.yaml`.** Rejected:
      kills the main scenario — packages arrive in arbitrary formats; the
      adapter (RFC-0395) builds the projection.
    file: docs/rfcs/archive/implemented/rfc-0394-adopt-specification-vendoring-with-forge-spec-yaml-and-spec-validate.md
  - kind: rejected-alternative
    rfcId: RFC-0395
    title: Add the fo-spec-ingest skill for specification packages
    status: implemented
    alternativesText: >-
      - **Extend `fo-idea` step 4 to handle 65-node series.** Rejected: series
      decomposition and spec ingest differ in kind — per-document grilling vs.
      spec-delta grilling, agent decomposition vs. pre-existing roadmap;
      overloading one skill produces an unreadable instruction set.

      - **Three separate skills (ingest / spec-audit / spec-grill), as the
      external expert proposed.** Rejected: audit and grilling are inseparable
      steps of the same acceptance flow, exactly as grilling lives inside
      `fo-idea-create-rfc`; three skills triple the orchestration surface for no
      autonomy gain.

      - **Mechanical `spec.ingest` CLI command.** Rejected: role identification,
      projection mapping, and grilling need judgment; a command would either
      fail on real-world packages or hide LLM calls inside a CLI — skills are
      the ecosystem's tool for judgment work.

      - **Per-RFC grilling of every roadmap node at ingest.** Rejected: the
      spec's decision log is evidence that those decisions were already
      stress-tested by its authors; re-grilling 65 nodes is waste — the delta
      against project invariants is the honest unit.
    file: docs/rfcs/archive/implemented/rfc-0395-add-the-fo-spec-ingest-skill-for-specification-packages.md
  - kind: rejected-alternative
    rfcId: RFC-0396
    title: Materialize spec RFCs lazily with specRef traceability and spec.status
    status: implemented
    alternativesText: >-
      - **Bulk generation of the whole roadmap.** Rejected: review debt, stale
      tails invalidated by early implementation lessons, corpus bloat degrading
      RFC command performance.

      - **Strictly one-at-a-time materialization.** Rejected: loses parallelism
      of independent graph branches and batch review convenience; the
      front-portion model keeps freshness without serializing everything.

      - **Extended 20-section template for spec RFCs.** Rejected: copying model
      content creates a second source of truth that diverges at the first
      amendment; normative references keep the spec authoritative (RFC-0394).

      - **Wave-level acceptance ceremony (human accepts each wave).** Rejected:
      adds a gate with no information — waves are already described in the
      accepted spec; the audit-verdict valve covers per-RFC anomalies.

      - **Fully human per-RFC acceptance (status quo).** Rejected: 43 ceremonies
      for Wave 1 re-litigate a decision the operator already made at spec
      acceptance; the reviewer identity is preserved via inheritance.
    file: docs/rfcs/archive/implemented/rfc-0396-materialize-spec-rfcs-lazily-with-specref-traceability-and-spec-status.md
  - kind: rejected-alternative
    rfcId: RFC-0397
    title: Establish the spec amendment loop
    status: implemented
    alternativesText: >-
      - **Edit `forge-spec.yaml` directly.** Rejected: destroys decision history
      and makes integrity checking meaningless; the projection would silently
      drift from the snapshot.

      - **Full re-vendor for every correction.** Rejected: `@N+1` per typo-scale
      finding is absurd ceremony; re-vendor is reserved for author-delivered
      major revisions.

      - **Amendments as full RFCs.** Rejected: a spec correction is spec-scoped,
      not a repository architecture decision; the lightweight ADR-style gate
      matches the decision's blast radius. Corrections that DO change repository
      contracts still require their own RFC — the amendment then references it.

      - **Auto-apply amendments as patches to snapshot copies.** Rejected:
      generated “effective text” artifacts double the surface that can drift;
      readers compose snapshot + accepted amendments, and materialized RFCs cite
      both explicitly.
    file: docs/rfcs/archive/implemented/rfc-0397-establish-the-spec-amendment-loop.md
  - kind: rejected-alternative
    rfcId: RFC-0398
    title: Program Charter and Terminology
    status: implemented
    alternativesText: >-
      - **UOM as the system name.** Rejected (ADR-001): too focused on Offering,
      does not cover the full business graph.

      - **Extend `@gogol/business` incrementally.** Rejected: the existing
      schema (flat `.md` files with presentation strings like `"70 € / Monat"`)
      cannot represent Charge/Plan/Adjustment, federated product identity, typed
      Policies, or Claims with Evidence without a breaking rewrite. ADR-002
      (logical model, not physical format) and ADR-028 (projection is not source
      of truth) require a new model.

      - **One monolithic RFC.** Rejected (roadmap §2): a single RFC would be
      unreviewable and block parallel work. The 65-node roadmap decomposes the
      program into independently implementable RFCs with explicit dependencies.

      - **Compatibility layer.** Rejected (ADR-043): no compatibility layer. Old
      files are deleted after 100% coverage and clean build. Forward-only.
    file: docs/rfcs/archive/implemented/rfc-0398-program-charter-and-terminology.md
  - kind: rejected-alternative
    rfcId: RFC-0399
    title: Namespace, Entity Envelope and URI Policy
    status: implemented
    alternativesText: >-
      - **Place envelope types in `@gogol/business`.** Rejected:
      `@gogol/business` is the legacy layer (DNA-20) that PBP replaces. Mixing
      PBP types into the legacy package creates confusion about which is
      canonical and violates the forward-only principle (ADR-043).

      - **Place envelope types in `@gogol/ontology`.** Rejected:
      `@gogol/ontology` is the UI taxonomy package (cosmic catalogs, manifests,
      intents, industries). PBP is a data layer, not a UI layer. Mixing them
      blurs the boundary between data model and UI taxonomy.

      - **Use Zod schemas instead of TypeScript interfaces.** Rejected for this
      RFC: the envelope is a structural contract, not a validation schema. Zod
      schemas for individual entities are defined in downstream RFCs
      (RFC-PBP-010+). The envelope interface can be wrapped in Zod by downstream
      RFCs if needed.

      - **Use `pbp@1` instead of `pbp/{entity}@1`.** Rejected: the spec uses
      per-entity schema IDs (`pbp/business@1`, `pbp/product@1`, etc.) so that
      individual entities can evolve independently within the `@1` namespace. A
      single `pbp@1` would couple all entities to a single schema version.
    file: docs/rfcs/archive/implemented/rfc-0399-namespace-entity-envelope-and-uri-policy.md
  - kind: rejected-alternative
    rfcId: RFC-0400
    title: Primitive Types and Controlled Vocabularies
    status: implemented
    alternativesText: >-
      - **Use Zod schemas instead of TypeScript interfaces.** Rejected for the
      same reason as RFC-0399: primitives are structural contracts. Downstream
      RFCs can wrap them in Zod. Validation utilities (`validateDecimal`,
      `validateMoneyRange`) are plain functions, not Zod schemas, to keep the
      package dependency-free.

      - **Use `number` for Money.** Rejected: ADR-012 explicitly mandates
      decimal string, not float. Using `number` would introduce floating-point
      precision errors.

      - **Merge SemanticStatus into EntityStatus.** Rejected: they describe
      different dimensions. EntityStatus is publication state
      (draft/published/...); SemanticStatus is field-value state
      (declared/not-declared/invalid/...). Mixing them conflates two orthogonal
      concepts.

      - **Use a single `PbpValue` union type.** Rejected: the spec defines
      distinct primitives with different fields. A union would lose type safety
      and make downstream schemas harder to write.
    file: docs/rfcs/archive/implemented/rfc-0400-primitive-types-and-controlled-vocabularies.md
  - kind: rejected-alternative
    rfcId: RFC-0401
    title: Schema Evolution and Compatibility
    status: implemented
    alternativesText: >-
      - **Use JSON Schema for compatibility checking.** Rejected: JSON Schema
      tooling is heavy and the PBP schema model is simpler than full JSON
      Schema. A lightweight TypeScript validator is sufficient for the
      additive-only check.

      - **No validator — rely on code review.** Rejected: the spec explicitly
      mandates `@1` stability (§3.10). Manual discipline is insufficient when
      multiple agents and RFCs are adding fields.

      - **Use semver for schema versioning.** Rejected: PBP uses `@N`
      (major-only), not semver. Additive changes within `@1` are allowed without
      version bump. Semver's minor/patch distinction is unnecessary.
    file: docs/rfcs/archive/implemented/rfc-0401-schema-evolution-and-compatibility.md
  - kind: rejected-alternative
    rfcId: RFC-0402
    title: Package and Source Profiles
    status: implemented
    alternativesText: >-
      - **Use Zod schemas for the package manifest.** Rejected for consistency
      with RFC-0399/0400: types are structural contracts. The compiler RFC can
      wrap them in Zod for runtime validation.

      - **Define source adapter types as an open string.** Rejected: the spec
      defines exactly 5 adapter types (§3.2). An open string would allow typos
      and invalid types. A closed union with a guard function is safer.

      - **Merge build config into build request.** Rejected: build config is
      package-level (static, in the manifest), while build request is per-build
      (dynamic, per invocation). They serve different purposes.
    file: docs/rfcs/archive/implemented/rfc-0402-package-and-source-profiles.md
  - kind: rejected-alternative
    rfcId: RFC-0403
    title: Business
    status: implemented
    alternativesText: >-
      - **Flatten Business fields into PbpEntity.** Rejected: not all entities
      have `businessModel`, `markets`, `industries`, etc. The envelope must
      remain generic.

      - **Include pricing in Business.** Rejected: entity-model §5.3 explicitly
      excludes prices. Pricing belongs to Offering (RFC-PBP-030).

      - **Use a single `refs` map instead of typed ref maps.** Rejected: typed
      ref maps (`brandRefs`, `placeRefs`, etc.) provide compile-time safety and
      make the entity structure self-documenting.
    file: docs/rfcs/archive/implemented/rfc-0403-business.md
  - kind: rejected-alternative
    rfcId: RFC-0404
    title: Federated Product Identity
    status: implemented
    alternativesText: >-
      - **Central product registry.** Rejected (ADR-003): governance, identity
      disputes, custom services, and long-term survivability require federation.

      - **Merge Product and CatalogEntry.** Rejected (ADR-005): Product is "what
      it is", CatalogEntry is "how a business lists it", Offering is "how a
      business sells it".

      - **Open string for product kind.** Rejected: the spec defines 13 kinds. A
      closed union prevents typos and invalid kinds.
    file: docs/rfcs/archive/implemented/rfc-0404-federated-product-identity.md
  - kind: rejected-alternative
    rfcId: RFC-0405
    title: Claim
    status: implemented
    alternativesText: >-
      - **Keep `*.claims.yaml` sidecars.** Rejected (ADR-018): path-based
      identity, duplicated governance, no first-class entity status.

      - **Merge Claim into the entity it references.** Rejected: claims need
      independent governance, review schedules, and evidence references.
      Embedding them would bloat the parent entity.

      - **Make governance optional.** Rejected: claims without authority and
      review schedule are unverifiable assertions. Governance is required.
    file: docs/rfcs/archive/implemented/rfc-0405-claim.md
  - kind: rejected-alternative
    rfcId: RFC-0406
    title: Localization and Fallback
    status: implemented
    alternativesText: >-
      - **Deep-merge arrays.** Rejected (compiler §7.4): arrays are not
      deep-merged. Localizable lists are fully replaced by the locale record.
      This prevents partial array overrides that lead to inconsistent state.

      - **Per-field fallback chains.** Rejected: non-default locales fall back
      to the default locale only (ADR-026), not to other non-default locales.
      This prevents deep fallback chains.
    file: docs/rfcs/archive/implemented/rfc-0406-localization-and-fallback.md
  - kind: rejected-alternative
    rfcId: RFC-0407
    title: Reference Resolution and Graph Integrity
    status: implemented
    alternativesText: >-
      - **Merge reference class into PbpEntityRef.** Rejected: reference class
      is a build-time concern (how the resolver treats the ref), not an
      entity-level concern (what the ref points to). Keeping them separate
      avoids coupling entity definitions to compiler behavior.

      - **Open string for graph error kinds.** Rejected: a closed union ensures
      the compiler handles all error types explicitly.
    file: docs/rfcs/archive/implemented/rfc-0407-reference-resolution-and-graph-integrity.md
  - kind: rejected-alternative
    rfcId: RFC-0408
    title: Legacy Extraction Contract
    status: implemented
    alternativesText: >-
      - **Compatibility layer.** Rejected (ADR-043): no alias fields, no
      dual-write period. Legacy files are deleted after 100% coverage and clean
      build.

      - **Untyped migration report.** Rejected: a typed extraction result
      ensures the migration agent produces consistent, machine-checkable output.

      - **Inline extraction in entity RFCs.** Rejected: extraction is a
      cross-cutting migration concern, not an entity concern. Keeping it
      separate allows the migration agent to evolve independently.
    file: docs/rfcs/archive/implemented/rfc-0408-legacy-extraction-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0409
    title: LegalIdentity and Public/Private Boundary
    status: implemented
    alternativesText: >-
      - **Include private data with a flag.** Rejected (§6.1): private data must
      not appear in public PBP at all. A flag is insufficient — the data must be
      excluded from the entity.

      - **Merge LegalIdentity into Business.** Rejected: LegalIdentity has its
      own federated identity and can be shared across businesses. It is a
      separate entity.
    file: docs/rfcs/archive/implemented/rfc-0409-legalidentity-and-public-private-boundary.md
  - kind: rejected-alternative
    rfcId: RFC-0410
    title: Brand and Person Relations
    status: implemented
    alternativesText: >-
      - **Merge Brand into Business.** Rejected: Brand is a federated entity
      with its own identity. A business can have multiple brands.

      - **Include visual identity fields.** Rejected: canonical business facts
      must not mix with design tokens (§7). Visual identity is a separate
      concern.
    file: docs/rfcs/archive/implemented/rfc-0410-brand-and-person-relations.md
  - kind: rejected-alternative
    rfcId: RFC-0411
    title: Place and Territory
    status: implemented
    alternativesText: >-
      - **Merge Place into Business.** Rejected: Place is a federated entity.
      One address can have multiple roles through Business relation (§8.1).

      - **Include service territory in Place.** Rejected (§8.1): service
      territory is not stored in Place.
    file: docs/rfcs/archive/implemented/rfc-0411-place-and-territory.md
  - kind: rejected-alternative
    rfcId: RFC-0412
    title: ContactPoint
    status: implemented
    alternativesText: >-
      - **Merge ContactPoint into Business.** Rejected: ContactPoint is a
      federated entity. A business can have multiple contact points.

      - **Open string for channel.** Rejected: a closed union prevents typos and
      invalid channels.
    file: docs/rfcs/archive/implemented/rfc-0412-contactpoint.md
  - kind: rejected-alternative
    rfcId: RFC-0413
    title: WebPresence
    status: implemented
    alternativesText: >-
      - **Merge WebPresence into Business.** Rejected: WebPresence is a
      federated entity. A business can have multiple web presences.

      - **Store domain and origin as fields.** Rejected (§10): `domain` and
      `origin` are derivable from `canonicalUrl`.
    file: docs/rfcs/archive/implemented/rfc-0413-webpresence.md
  - kind: rejected-alternative
    rfcId: RFC-0414
    title: Category Registry
    status: implemented
    alternativesText: >-
      - **Freeform string categories.** Rejected: typed categories with
      `broaderRef` enable hierarchy validation and external vocabulary mapping.

      - **Merge categories into Product.** Rejected: categories are global
      semantic entities, not business-specific.
    file: docs/rfcs/archive/implemented/rfc-0414-category-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0415
    title: Product Schema
    status: implemented
    alternativesText: >-
      - **Merge ProductGroup into Product.** Rejected: ProductGroup is a
      separate entity with its own identity. A group can have many variants.

      - **Use array for variation axes.** Rejected (ADR-027): keyed maps use
      semantic keys, not array indices.
    file: docs/rfcs/archive/implemented/rfc-0415-product-schema.md
  - kind: rejected-alternative
    rfcId: RFC-0416
    title: EvidenceSource
    status: implemented
    alternativesText: >-
      - **Merge EvidenceSource into Claim.** Rejected: evidence sources are
      reusable across claims and have their own identity.

      - **Open string for evidence kind.** Rejected: a closed union prevents
      invalid evidence types.
    file: docs/rfcs/archive/implemented/rfc-0416-evidencesource.md
  - kind: rejected-alternative
    rfcId: RFC-0417
    title: Disclosure
    status: implemented
    alternativesText: >-
      - **Merge Disclosure into Claim.** Rejected: disclosures are not claims —
      they are mandatory or informative statements about dependencies, data
      processing, etc.

      - **Open string for kind.** Rejected: a closed union prevents invalid
      disclosure types.
    file: docs/rfcs/archive/implemented/rfc-0417-disclosure.md
  - kind: rejected-alternative
    rfcId: RFC-0418
    title: Credential
    status: implemented
    alternativesText: >-
      - **Merge Credential into Business.** Rejected: credentials are federated
      entities with their own identity. A business can have multiple credentials
      from different issuers.

      - **Open string for credential kind.** Rejected: a closed union prevents
      invalid credential types.
    file: docs/rfcs/archive/implemented/rfc-0418-credential.md
  - kind: rejected-alternative
    rfcId: RFC-0419
    title: Review and AggregateRating
    status: implemented
    alternativesText: >-
      - **Merge Review into AggregateRating.** Rejected: individual reviews and
      aggregate ratings are distinct entities with different lifecycles.

      - **Open string for content mode.** Rejected: a closed union prevents
      invalid content modes.
    file: docs/rfcs/archive/implemented/rfc-0419-review-and-aggregaterating.md
  - kind: rejected-alternative
    rfcId: RFC-0420
    title: PublicDocument
    status: implemented
    alternativesText: >-
      - **Merge PublicDocument into Business.** Rejected: documents are separate
      entities with their own governance lifecycle.

      - **Open string for document kind.** Rejected: a closed union prevents
      invalid document types.
    file: docs/rfcs/archive/implemented/rfc-0420-publicdocument.md
  - kind: rejected-alternative
    rfcId: RFC-0421
    title: Runtime State Overlay
    status: implemented
    alternativesText: >-
      - **Store runtime state in entity files.** Rejected (system-spec §4.4):
      frequently changing values live outside Git.

      - **Open string for stale behavior.** Rejected: a closed union ensures all
      projection behaviors are handled explicitly.
    file: docs/rfcs/archive/implemented/rfc-0421-runtime-state-overlay.md
  - kind: rejected-alternative
    rfcId: RFC-0422
    title: Validation and Error Codes
    status: implemented
    alternativesText: >-
      - **Open string for severity.** Rejected: a closed union ensures all
      severity levels are handled explicitly.

      - **Open string for error prefix.** Rejected: 15 stable prefixes are
      defined by the spec. A closed union prevents invalid prefixes.
    file: docs/rfcs/archive/implemented/rfc-0422-validation-and-error-codes.md
  - kind: rejected-alternative
    rfcId: RFC-0423
    title: Registry and Resolver
    status: implemented
    alternativesText: >-
      - **Open string for registry kind.** Rejected: a closed union prevents
      invalid registry types.

      - **Merge resolver into compiler.** Rejected: resolver is a reusable
      component that can be used outside the compiler.
    file: docs/rfcs/archive/implemented/rfc-0423-registry-and-resolver.md
  - kind: rejected-alternative
    rfcId: RFC-0424
    title: Normalization Contract
    status: implemented
    alternativesText: >-
      - **Open string for decision status.** Rejected: a closed union ensures
      all decision statuses are handled explicitly.

      - **Merge normalization into extraction.** Rejected: normalization is a
      separate concern from extraction. Extraction reads legacy files;
      normalization decides what to do with each field.
    file: docs/rfcs/archive/implemented/rfc-0424-normalization-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0425
    title: ProductGroup and ProductVariant
    status: implemented
    alternativesText: >-
      - **Merge invariants into RFC-0415.** Rejected: RFC-0415 covers the
      Product Schema (RFC-PBP-022), while this RFC covers the variant-specific
      invariants (RFC-PBP-023). They are separate spec nodes with separate
      concerns.

      - **Enforce invariants in TypeScript.** Rejected: TypeScript cannot
      enforce "all required axes must be specified" at the type level. This is a
      compiler concern.
    file: docs/rfcs/archive/implemented/rfc-0425-productgroup-and-productvariant.md
  - kind: rejected-alternative
    rfcId: RFC-0426
    title: Bundles and Composition
    status: implemented
    alternativesText: >-
      - **Separate Bundle entity.** Rejected: the spec models bundles as
      Products with `kind: "bundle"`, not as a separate entity. A separate
      entity would duplicate Product fields.

      - **Model composition at Offering level.** Rejected for intrinsic bundles:
      intrinsic composition is a product fact, not a seller-specific offering
      fact. Offering-level inclusion is a separate concern.
    file: docs/rfcs/archive/implemented/rfc-0426-bundles-and-composition.md
  - kind: rejected-alternative
    rfcId: RFC-0427
    title: Catalog and CatalogEntry
    status: implemented
    alternativesText: >-
      - **Merge Catalog into Business.** Rejected: Catalog is a federated entity
      with its own identity. A business can have multiple catalogs.

      - **Store SKU on Product.** Rejected: SKU is seller-specific local data,
      not a product fact. The spec explicitly states "SKU is not stored as
      Product external identifier" (system-spec §10.6).
    file: docs/rfcs/archive/implemented/rfc-0427-catalog-and-catalogentry.md
  - kind: rejected-alternative
    rfcId: RFC-0428
    title: Compiler Pipeline
    status: implemented
    alternativesText: >-
      - **Implement compiler in this RFC.** Rejected: this RFC defines the
      pipeline contract (types), not the implementation. The compiler
      implementation requires all entity types, validation rules, and projection
      targets to be defined first.

      - **Fewer phases.** Rejected: the spec defines 14 phases. Collapsing them
      would lose clarity and testability.
    file: docs/rfcs/archive/implemented/rfc-0428-compiler-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0429
    title: Offering Core
    status: implemented
    alternativesText: >-
      - **Merge Offering into Product.** Rejected: Product is a universal
      entity; Offering is seller-specific with pricing and terms.

      - **Separate Pricing entity.** Rejected: pricing is embedded in Offering
      as a sub-structure, not a separate federated entity.
    file: docs/rfcs/archive/implemented/rfc-0429-offering-core.md
  - kind: rejected-alternative
    rfcId: RFC-0430
    title: Incremental and Bulk Processing
    status: implemented
    alternativesText: >-
      - **Full rebuild always.** Rejected: the spec explicitly requires
      incremental builds for large catalogs (compiler §26).

      - **External cache system.** Rejected: cache key contract is part of the
      PBP type system.
    file: docs/rfcs/archive/implemented/rfc-0430-incremental-and-bulk-processing.md
  - kind: rejected-alternative
    rfcId: RFC-0431
    title: Derivation Contract
    status: implemented
    alternativesText: >-
      - **Embed derivations in Offering.** Rejected: derivations are computed at
      build time, not stored in source entities.

      - **Skip provenance.** Rejected: the spec requires provenance for every
      derived value (compiler §1).
    file: docs/rfcs/archive/implemented/rfc-0431-derivation-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0432
    title: Schema.org Mapping
    status: implemented
    alternativesText: >-
      - **Automatic Schema.org generation without mapping ref.** Rejected: the
      spec requires explicit, versioned mapping references (§18.1).

      - **Silent loss.** Rejected: the spec mandates a loss report for
      unmappable data (§18.4).
    file: docs/rfcs/archive/implemented/rfc-0432-schema-org-mapping.md
  - kind: rejected-alternative
    rfcId: RFC-0433
    title: CRM Projection
    status: implemented
    alternativesText: '- **Embed CRM IDs in PBP entities.** Rejected: the spec
      states "CRM adapter maintains external IDs separately" (compiler §22).'
    file: docs/rfcs/archive/implemented/rfc-0433-crm-projection.md
  - kind: rejected-alternative
    rfcId: RFC-0434
    title: MachineUsePolicy and AI Access Projections
    status: implemented
    alternativesText: >-
      - **Tie to llms.txt convention.** Rejected: the spec explicitly states
      "PBP is not tied to a single llms.txt convention" (system-spec §25).

      - **Boolean permissions only.** Rejected: the spec implies conditional
      permissions (e.g., attribution required). The `conditional` verdict
      handles this.
    file: docs/rfcs/archive/implemented/rfc-0434-machineusepolicy-and-ai-access-projections.md
  - kind: rejected-alternative
    rfcId: RFC-0435
    title: Git Revision and Publication Snapshot
    status: implemented
    alternativesText: >-
      - **Skip canonical serialization.** Rejected: the spec mandates RFC 8785
      canonicalization (compiler §24.3).

      - **Include build paths in snapshot.** Rejected: the spec explicitly
      excludes build paths, local filesystem paths, and non-deterministic
      metrics (compiler §24.2).
    file: docs/rfcs/archive/implemented/rfc-0435-git-revision-and-publication-snapshot.md
  - kind: rejected-alternative
    rfcId: RFC-0436
    title: Offering Relations
    status: implemented
    alternativesText: "- **Separate relations file.** Rejected: relations are part
      of the Offering entity and were already exported in RFC-0429."
    file: docs/rfcs/archive/implemented/rfc-0436-offering-relations.md
  - kind: rejected-alternative
    rfcId: RFC-0437
    title: "Pricing Core: Charge, Plan, Adjustment"
    status: implemented
    alternativesText: >-
      - **Single flat charge type.** Rejected: the spec distinguishes one-time,
      recurring, usage, and deposit with different fields.

      - **Amount as string only.** Rejected: the spec defines 4 amount models
      (fixed, range, tiered, unit-rate) with different shapes.
    file: docs/rfcs/archive/implemented/rfc-0437-pricing-core-charge-plan-adjustment.md
  - kind: rejected-alternative
    rfcId: RFC-0438
    title: Terms and Commercial Lifecycle
    status: implemented
    alternativesText: "- **Embed terms in Plan only.** Rejected: terms exist at both
      Offering level and Plan level; Plan overrides specific fields."
    file: docs/rfcs/archive/implemented/rfc-0438-terms-and-commercial-lifecycle.md
  - kind: rejected-alternative
    rfcId: RFC-0439
    title: Policy Base and Scope
    status: implemented
    alternativesText: "- **Single flat policy entity.** Rejected: the spec defines
      specialized schemas per kind. The base envelope is common, but `kind`
      drives specialization."
    file: docs/rfcs/archive/implemented/rfc-0439-policy-base-and-scope.md
  - kind: rejected-alternative
    rfcId: RFC-0440
    title: ComparisonProfile
    status: implemented
    alternativesText: "- **Embed comparison in Offering.** Rejected: comparison is a
      registry-level entity that applies to categories, not individual
      offerings."
    file: docs/rfcs/archive/implemented/rfc-0440-comparisonprofile.md
  - kind: rejected-alternative
    rfcId: RFC-0441
    title: Buyer View Schema
    status: implemented
    alternativesText: "- **Hardcode sections in compiler.** Rejected: the spec
      defines BuyerViewSchema as a registry entity that can vary per
      deployment."
    file: docs/rfcs/archive/implemented/rfc-0441-buyer-view-schema.md
  - kind: rejected-alternative
    rfcId: RFC-0442
    title: Canonical Serialization
    status: implemented
    alternativesText: "- **Non-canonical serialization.** Rejected: the spec
      mandates RFC 8785 canonicalization for deterministic digests."
    file: docs/rfcs/archive/implemented/rfc-0442-canonical-serialization.md
  - kind: rejected-alternative
    rfcId: RFC-0443
    title: Shopify/PIM Adapter Profile
    status: implemented
    alternativesText: "- **Generic adapter without Shopify-specific profile.**
      Rejected: Shopify has specific concepts (collections, metafields) that
      benefit from a typed profile."
    file: docs/rfcs/archive/implemented/rfc-0443-shopify-pim-adapter-profile.md
  - kind: rejected-alternative
    rfcId: RFC-0444
    title: Usage, Range and Tiered Pricing
    status: implemented
    alternativesText: "- **Separate file for usage/range/tiered.** Rejected: these
      are amount model variants within the same `PbpCharge` structure."
    file: docs/rfcs/archive/implemented/rfc-0444-usage-range-and-tiered-pricing.md
  - kind: rejected-alternative
    rfcId: RFC-0445
    title: Allowances, Overage and Deposits
    status: implemented
    alternativesText: "- **Separate overage type.** Rejected: overage is a usage
      charge referenced by allowance, not a separate entity."
    file: docs/rfcs/archive/implemented/rfc-0445-allowances-overage-and-deposits.md
  - kind: rejected-alternative
    rfcId: RFC-0446
    title: Tax and Buyer Presentation
    status: implemented
    alternativesText: "- **Embed tax in charge.** Rejected: tax is a
      pricing-header-level concern, not per-charge."
    file: docs/rfcs/archive/implemented/rfc-0446-tax-and-buyer-presentation.md
  - kind: rejected-alternative
    rfcId: RFC-0447
    title: Service Level Policy
    status: implemented
    alternativesText: "- **Generic policy without specialization.** Rejected: SLA
      has specific fields (objective, measurement, exclusions, remedy) that need
      typed contracts."
    file: docs/rfcs/archive/implemented/rfc-0447-service-level-policy.md
  - kind: rejected-alternative
    rfcId: RFC-0448
    title: Guarantee and Remedy
    status: implemented
    alternativesText: "- **Merge guarantee into SLA.** Rejected: guarantees have
      different structure (condition + remedy) from SLA (objective + measurement
      + exclusions + remedy)."
    file: docs/rfcs/archive/implemented/rfc-0448-guarantee-and-remedy.md
  - kind: rejected-alternative
    rfcId: RFC-0449
    title: Ownership, License and Portability
    status: implemented
    alternativesText: "- **Generic asset map.** Rejected: the spec defines specific
      asset types (domain, customerContent, builtWebsite, sourceCode,
      thirdPartyComponents) that benefit from typed fields."
    file: docs/rfcs/archive/implemented/rfc-0449-ownership-license-and-portability.md
  - kind: rejected-alternative
    rfcId: RFC-0450
    title: Exit and Data Package
    status: implemented
    alternativesText: "- **Merge exit into ownership.** Rejected: exit is a separate
      concern with trigger, delivery target, and data package format."
    file: docs/rfcs/archive/implemented/rfc-0450-exit-and-data-package.md
  - kind: rejected-alternative
    rfcId: RFC-0451
    title: Fulfillment, Shipping, Pickup and Return
    status: implemented
    alternativesText: "- **Embed fulfillment in Offering.** Rejected: fulfillment is
      a complex sub-structure that benefits from its own typed interface."
    file: docs/rfcs/archive/implemented/rfc-0451-fulfillment-shipping-pickup-and-return.md
  - kind: rejected-alternative
    rfcId: RFC-0452
    title: Data Retention and Deletion
    status: implemented
    alternativesText: "- **Generic policy without specialization.** Rejected: data
      retention has specific fields (retention periods, deletion method) that
      need typed contracts."
    file: docs/rfcs/archive/implemented/rfc-0452-data-retention-and-deletion.md
  - kind: rejected-alternative
    rfcId: RFC-0453
    title: First-Year Cost and TCO
    status: implemented
    alternativesText: "- **Single generic derivation.** Rejected: first-year cost
      and TCO have specific input/output contracts that benefit from explicit
      typing."
    file: docs/rfcs/archive/implemented/rfc-0453-first-year-cost-and-tco.md
  - kind: rejected-alternative
    rfcId: RFC-0454
    title: Comparison Projection
    status: implemented
    alternativesText: '- **Forced ranking.** Rejected: the spec explicitly states
      "no forced ranking" (compiler §23).'
    file: docs/rfcs/archive/implemented/rfc-0454-comparison-projection.md
  - kind: rejected-alternative
    rfcId: RFC-0455
    title: Website Projection Contract
    status: implemented
    alternativesText: "- **Hardcode website rendering.** Rejected: the projection
      must be deterministic and reference source entities."
    file: docs/rfcs/archive/implemented/rfc-0455-website-projection-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0456
    title: AI Answer Projection
    status: implemented
    alternativesText: "- **Reuse PbpAiAccessProjection only.** Rejected: the full
      projection needs structured fact content, not just allowed/denied lists."
    file: docs/rfcs/archive/implemented/rfc-0456-ai-answer-projection.md
  - kind: rejected-alternative
    rfcId: RFC-0457
    title: Quote and Contract Inputs
    status: implemented
    alternativesText: "- **Merge quote and contract.** Rejected: contract requires
      terms and policy refs that quote does not."
    file: docs/rfcs/archive/implemented/rfc-0457-quote-and-contract-inputs.md
  - kind: rejected-alternative
    rfcId: RFC-0458
    title: Invoice Input
    status: implemented
    alternativesText: "- **Merge invoice into quote.** Rejected: invoice requires
      tax and customer reference that quote does not."
    file: docs/rfcs/archive/implemented/rfc-0458-invoice-input.md
  - kind: rejected-alternative
    rfcId: RFC-0459
    title: Signature Envelope
    status: implemented
    alternativesText: "- **Single algorithm.** Rejected: different deployments may
      use different signing algorithms."
    file: docs/rfcs/archive/implemented/rfc-0459-signature-envelope.md
  - kind: rejected-alternative
    rfcId: RFC-0460
    title: Sichtpass / Verifiable Credential Mapping
    status: implemented
    alternativesText: "- **Direct VC without mapping.** Rejected: PBP entities need
      explicit field-to-claim mapping for VC interoperability."
    file: docs/rfcs/archive/implemented/rfc-0460-sichtpass-verifiable-credential-mapping.md
  - kind: rejected-alternative
    rfcId: RFC-0461
    title: Warpgogol Legacy Migration
    status: implemented
    alternativesText: >-
      - **Compatibility layer.** Rejected: ADR-043 explicitly states no
      compatibility layer. Migration is a clean cutover.

      - **Parallel run.** Rejected: both systems running in parallel creates
      confusion and maintenance burden.
    file: docs/rfcs/archive/implemented/rfc-0461-webgogol-legacy-migration.md
  - kind: rejected-alternative
    rfcId: RFC-0462
    title: Migration Coverage and Cutover
    status: implemented
    alternativesText: >-
      - **Cutover without verification.** Rejected: deleting `@gogol/business`
      without full coverage verification risks data loss.

      - **Permanent compatibility layer.** Rejected: ADR-043 explicitly states
      no compatibility layer.
    file: docs/rfcs/archive/implemented/rfc-0462-migration-coverage-and-cutover.md
  - kind: rejected-alternative
    rfcId: RFC-0463
    title: Enforce acceptance criteria completeness and evidence on implemented RFCs
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Warning instead of error for V-26 | Warnings are ignored by agents and
      CI. The RFC-0356 failure proves that prose-only enforcement does not work
      — the agent marked `[ ]` and stamped `implemented` anyway. Only a hard
      error stops the transition. |

      | Post-cutoff only (like V-23/V-24/V-25) | The failure mode is not
      metadata-related — it is document-quality. An implemented RFC with
      unchecked criteria is wrong regardless of when it was created. Applying
      post-cutoff would leave RFC-0356 and similar pre-cutoff RFCs in an invalid
      state indefinitely. |

      | Allow "deferred" annotations on `[ ]` at `implemented` | This is the
      current behavior that failed. "Deferred" is a prose escape hatch with no
      enforcement. If work is deferred, the RFC is not implemented — it stays
      `accepted` or is split. |

      | Allow `[x]` without evidence but require probe evidence (V-23) only |
      V-23 only applies to post-cutoff RFCs with acceptance probes. Many RFCs
      have no probes. V-27 covers all checked criteria regardless of probe
      presence. |

      | Add a new `rfc.acceptance.audit` command instead of rules in
      `rfc.validate` | A separate command is opt-in and easy to skip. Rules in
      `rfc.validate` are mandatory and already run in CI. |
    file: docs/rfcs/archive/implemented/rfc-0463-enforce-acceptance-criteria-completeness-and-evidence-on-implemented-rfcs.md
  - kind: rejected-alternative
    rfcId: RFC-0464
    title: Backfill evidence annotations and resolve unchecked criteria on existing
      implemented RFCs
    status: implemented
    alternativesText: >-
      | Alternative | Rejected because |

      | --- | --- |

      | Scripted generic evidence (auto-add `(evidence: TODO)` to every `[x]`) |
      Fake evidence defeats the purpose of V-27. The RFC-0463 Risks section
      explicitly warns against this. |

      | Skip backfill, leave violations in place | `rfc.validate` runs in
      `build.check` and CI. 3011 violations would block all pipelines
      indefinitely. |

      | Mass-supersede all 165 V-26 RFCs | Some RFCs may have completed work
      that was simply not checked. Triage is needed before supersede. |
    file: docs/rfcs/archive/implemented/rfc-0464-backfill-evidence-annotations-and-resolve-unchecked-criteria-on-existing-implemented-rfcs.md
  - kind: rejected-alternative
    rfcId: RFC-0465
    title: Triage and resolve pre-existing validation violations
    status: implemented
    alternativesText: >-
      - **Clean all historical frontmatter** (rejected): Updating 500+ RFC
      frontmatter fields to remove or rename 1200+ command references is
      impractical. Many commands were intentionally renamed or merged during
      ecosystem refactoring, and tracking the full rename history provides no
      value.

      - **Register all historical commands** (rejected): Many commands were
      intentionally removed or merged. Re-registering them as live commands
      would pollute the command surface with dead entries.

      - **Remove `specRef` from PBP RFCs** (rejected): `specRef` is the
      canonical traceability mechanism for spec-materialized RFCs (RFC-0396).
      Removing it would break the spec → RFC traceability chain.
    file: docs/rfcs/archive/implemented/rfc-0465-triage-and-resolve-pre-existing-validation-violations.md
  - kind: rejected-alternative
    rfcId: RFC-0466
    title: PBP Runtime — Zod Schemas, Loaders, and Astro Collections
    status: implemented
    alternativesText: >-
      - **Extend `@gogol/business` incrementally.** Rejected (ADR-002, RFC-0398
      §Alternatives): the existing schema cannot represent
      Charge/Plan/Adjustment, federated product identity, typed Policies, or
      Claims with Evidence without a breaking rewrite.

      - **Define schemas in each entity RFC.** Rejected: the 65 entity RFCs are
      contract-only by design (roadmap §2 principle 1: "One RFC — one
      architectural responsibility"). Adding implementation to each would
      require reopening 30+ implemented RFCs.

      - **Use JSON Schema instead of Zod.** Rejected: the monorepo standard is
      Zod (used by `@gogol/business`, `@gogol/share`, `@gogol/ontology`,
      `@gogol/check-core`). Introducing a second validation framework creates
      friction.

      - **Put schemas in a separate package.** Rejected: `@gogol/pbp` is the
      single home for all PBP types, schemas, loaders, and projection contracts
      (RFC-0399, `packages/pbp/AGENTS.md`).
    file: docs/rfcs/archive/implemented/rfc-0466-pbp-runtime-zod-schemas-loaders-and-astro-collections.md
  - kind: rejected-alternative
    rfcId: RFC-0467
    title: PBP Compiler Implementation
    status: implemented
    alternativesText: >-
      - **Astro content collections as the compiler.** Rejected: Astro content
      collections validate frontmatter but do not provide multi-phase
      compilation, reference resolution, cycle detection, derivation, or
      projection generation. The compiler is a separate concern.

      - **Implement compiler in site-kernel.** Rejected: the compiler is
      PBP-specific, not a general-purpose kernel feature. It belongs in
      `@gogol/pbp`.

      - **Use an existing headless CMS compiler.** Rejected: PBP has specific
      requirements (determinism, locale fallback, graph integrity, Buyer View)
      that no off-the-shelf compiler satisfies.

      - **Skip the compiler, use loaders directly.** Rejected: loaders
      (RFC-0466) provide raw entity access. The compiler provides the resolved
      graph, validated references, derivations, and projections — all required
      for a working site.
    file: docs/rfcs/archive/implemented/rfc-0467-pbp-compiler-implementation.md
  - kind: rejected-alternative
    rfcId: RFC-0468
    title: Warpgogol PBP Content Creation
    status: implemented
    alternativesText: >-
      - **Transform legacy files in-place.** Rejected: the PBP entity structure
      is fundamentally different (Product/CatalogEntry/Offering decomposition,
      typed Policies, Claims with Evidence). In-place transformation would break
      the legacy site during migration.

      - **Generate content programmatically.** Rejected: the migration requires
      semantic decisions (entity decomposition, field classification, owner
      decisions) that cannot be automated without human judgment. The agent
      creates draft files; the operator reviews and decides.

      - **Skip draft entities.** Rejected: the migration plan (§29) explicitly
      requires creating valid draft manifests for unresolved items, isolating
      unresolved facts, and blocking only final publication.
    file: docs/rfcs/archive/implemented/rfc-0468-webgogol-pbp-content-creation.md
  - kind: rejected-alternative
    rfcId: RFC-0469
    title: Warpgogol PBP Site Cutover
    status: implemented
    alternativesText: >-
      - **Gradual per-page cutover.** Rejected: the `SemanticSiteProfile` is a
      single object consumed by all pages. Switching pages individually would
      require maintaining both `buildSiteSemanticProfile` and
      `buildPbpSemanticProfile` simultaneously, creating confusion.

      - **Feature flag.** Rejected: no backward compatibility is desired (user
      requirement). A feature flag implies both systems running, which is
      explicitly rejected (ADR-043).

      - **Delete legacy first, then cutover.** Rejected: legacy files must be
      present as rollback safety net until cutover is verified. Deletion is
      RFC-0470.
    file: docs/rfcs/archive/implemented/rfc-0469-webgogol-pbp-site-cutover.md
  - kind: rejected-alternative
    rfcId: RFC-0470
    title: Legacy Business Layer Deletion
    status: implemented
    alternativesText: >-
      - **Keep `@gogol/business` as a compatibility shim.** Rejected: the user
      requirement is "no legacy" and "no backward compatibility." A
      compatibility shim is explicitly rejected (ADR-043).

      - **Deprecate but don't delete.** Rejected: a deprecated but present
      package still consumes maintenance attention and confuses agents. The user
      requirement is "no legacy should remain."

      - **Delete incrementally (schemas first, then loaders, then content).**
      Rejected: the package is a single unit. Partial deletion creates broken
      imports. A single atomic commit is safer.

      - **Archive instead of delete.** Rejected: git history is the archive.
      Keeping dead code on disk is the problem.
    file: docs/rfcs/archive/implemented/rfc-0470-legacy-business-layer-deletion.md
  - kind: rejected-alternative
    rfcId: RFC-0471
    title: Legacy Business Layer Content Migration and Package Deletion
    status: implemented
    alternativesText: >-
      - **Keep `@gogol/business` as a compatibility shim.** Rejected: the user
      requirement is "no legacy" and "no backward compatibility." A
      compatibility shim is explicitly rejected (ADR-043).

      - **Deprecate but don't delete.** Rejected: a deprecated but present
      package still consumes maintenance attention and confuses agents.

      - **Delete incrementally (content first, then package).** Rejected: the
      package is a single unit. Partial deletion creates broken imports. A
      single atomic commit is safer. Content reference migration is a
      precondition, not a separate deletion step.

      - **Archive instead of delete.** Rejected: git history is the archive.
      Keeping dead code on disk is the problem.
    file: docs/rfcs/archive/implemented/rfc-0471-legacy-business-layer-content-migration-and-package-deletion.md
  - kind: rejected-alternative
    rfcId: RFC-0472
    title: Add sternsystem.sync command for external repository mirroring
    status: implemented
    alternativesText: >-
      1. **Replace `repo` with GitHub URL directly.** Set `repo:
      git@github.com:syrokomskyi/warpgogol-com.git` in the registry. Rejected:
      loses offline capability and local `file://` speed. `mission.materialize`
      and `mission.reconcile` would require network access for every fetch/push.


      2. **Replace `repo` with `localRepo` + `remoteRepo` fields.** Two explicit
      fields instead of `repo` + `mirror`. Rejected: breaks the existing
      `fleetRegistryEntrySchema` contract (renaming `repo`), requires changes to
      `syncCacheClone` and `mission.reconcile`, and adds complexity for a single
      mirror. The `mirror` approach is additive and non-breaking.


      3. **Post-receive git hook in bare repo.** A `hooks/post-receive` script
      that auto-pushes to GitHub on every push. Rejected: not visible to the
      platform, not recorded in Bordbuch, not discoverable by agents, and
      requires manual file management outside the command surface.


      4. **Automate sync in `mission.reconcile` with `--sync-mirror` flag.**
      Rejected: couples reconcile to network availability, breaks offline-safe
      invariant of the mission pipeline. Sync should be a separate, explicit
      operation.


      5. **ADR instead of RFC.** Rejected: adds a new Site OS command, modifies
      `AGENTS.md`, and changes a cross-workspace schema in `@gogol/ontology` —
      three criteria that disqualify ADR per the `fo-idea-create-adr` skill
      rules.


      6. **`git push --mirror` for exact mirroring.** Rejected: `--mirror`
      deletes remote branches not present locally, which can destroy work pushed
      directly to GitHub. Default to current-branch push; `--all` flag covers
      all branches + tags without deletion.
    file: docs/rfcs/archive/implemented/rfc-0472-add-sternsystem-sync-command-for-external-repository-mirroring.md
  - kind: rejected-alternative
    rfcId: RFC-0473
    title: Unify Bordbuch schemas and retire the site-level ledger
    status: implemented
    alternativesText: >-
      1. **Keep both schemas, fix the path.** Update `site-bordbuch.ts` to read
      from `systems/<id>/bordbuch/events.ndjson` instead of
      `<app>/src/bordbuch/events.ndjson`. Rejected: perpetuates schema
      divergence, two append code paths, two validation code paths, and two
      status projection shapes. The dual-schema complexity is the root cause of
      the current orphaned-path bug.


      2. **Merge `bordbuchEventSchema` into `bordbuchEntrySchema` as a union.**
      Accept both shapes in the canonical schema. Rejected: union schemas are
      fragile, hash-chain validation becomes ambiguous (two valid shapes for the
      same event ID), and the `mission-NNNNNN` vs `event-NNNNNN` ID formats are
      incompatible.


      3. **Move runtime events to a separate ledger.** Keep the Sternsystem
      Bordbuch for lifecycle events only; create a new `runtime-events.ndjson`
      for PSEO/IndexNow/breaker events. Rejected: fragments the operational
      history, requires a second hash-chain, and breaks the "single immutable
      timeline" invariant from DNA-46. Runtime events are part of the
      Sternsystem's story.


      4. **ADR instead of RFC.** Rejected: deletes a package module
      (`@gogol/surface/bordbuch.ts`), changes cross-workspace schemas in
      `@gogol/ontology`, adds a new command (`bordbuch.status`), and supersedes
      RFC-0276 — four criteria that require a full RFC.


      5. **Defer until PSEO/Stellarpass/Sichtpass pipelines are migrated to
      Sternsystem topology.** Rejected: the current code is already broken
      (orphaned path), and the schema extension is backward-compatible.
      Deferring leaves silent no-ops in production pipelines.


      6. **Keep `deploy` as a separate kind from `deployment`.** Rejected:
      `deployment` already exists in the canonical schema with writer-role
      `leitstand`. Having both `deploy` and `deployment` is redundant. Old
      `deploy` callers use `deployment`.
    file: docs/rfcs/archive/implemented/rfc-0473-unify-bordbuch-schemas-and-retire-site-level-ledger.md
  - kind: rejected-alternative
    rfcId: RFC-0474
    title: Recover content lost during legacy business layer deletion
    status: implemented
    alternativesText: >-
      1. **Re-create the `business/` directory as a compatibility layer.**
      Rejected: DNA-20 is superseded (RFC-0471). Restoring the old structure
      would contradict the PBP architecture and re-introduce the dual-schema
      complexity that was eliminated.


      2. **Restore content via a new mission with `mission.reconcile`.**
      Rejected: the mission workpiece is gitignored and ephemeral. Content
      recovery must target the canonical Sternsystem directly, not a temporary
      workpiece that would be discarded.


      3. **Skip UK translations and rely on DE fallback.** Rejected: Ukrainian
      was the primary language for the original content. Relying on fallback
      would lose the Ukrainian translations permanently — this is content
      recovery, not content creation.


      4. **Create new PBP entity types for FAQ and compliance.** Rejected: FAQ
      will be a separate pluggable module (future RFC). GoBD compliance dates
      are operational metadata, not a public business entity — they belong in
      `meta.md`.


      5. **Keep claims-sidecars as separate `.claims.yaml` files.** Rejected:
      PBP `governance` blocks are the canonical place for entity-level metadata.
      Creating sidecar files would re-introduce the RFC-0212 CKL pattern that
      PBP replaces.


      6. **ADR instead of RFC.** Rejected: this RFC touches multiple content
      collections, creates 37+ new files, migrates ledger subjects, and is
      cross-workspace. An ADR is too lightweight for this scope.
    file: docs/rfcs/archive/implemented/rfc-0474-recover-content-lost-during-legacy-business-layer-deletion.md
  - kind: rejected-alternative
    rfcId: RFC-0475
    title: Pluggable FAQ content module and content recovery
    status: implemented
    alternativesText: >-
      1. **Add FAQ to `@gogol/share` instead of a new package.** Rejected:
      `@gogol/share` already owns FAQ semantic logic (JSON-LD, page builder),
      but collection definition + schema + validator are a distinct concern. A
      dedicated package keeps the boundary clean and allows sites to opt out of
      FAQ entirely by not registering the collection.


      2. **FAQ as a PBP entity inside `business-profile/{lang}/faq/`.**
      Rejected: PBP namespace `pbp/*@1` is frozen. Adding a FAQ entity type
      would require `pbp/*@2` and a migration contract. FAQ is not a business
      profile entity — it is site content.


      3. **FAQ inside `pages/{lang}/faq/`.** Rejected: `pages` collection uses
      `markdownCollectionLoader` for page shell content. FAQ needs structured
      fields (`question`, `answer`, `order`, `tags`) for sorting, filtering, and
      JSON-LD generation. A permissive pages schema would not enforce these
      fields.


      4. **Restore FAQ content without a package, directly in the site.**
      Rejected: FAQ collection definition, schema, and validator are reusable
      across sites. Embedding them in a single site violates the monorepo
      composition principle (AGENTS.md: "A site's job is composition only").


      5. **Feature flag for FAQ collection registration.** Rejected: adds
      complexity for no benefit. The collection is no-op when `src/content/faq/`
      is empty (same pattern as `people`). Sites without FAQ simply do not
      create the directory.


      6. **Dedicated FAQ pages (`/faq/`).** Rejected: legacy FAQ never had
      dedicated pages. FAQ entries are embedded in existing pages (services,
      about) via `faq-list-section`. Adding route generation would expand scope
      without a demonstrated need.
    file: docs/rfcs/archive/implemented/rfc-0475-pluggable-faq-content-module-and-content-recovery.md
  - kind: rejected-alternative
    rfcId: RFC-0476
    title: Enforce verified RFC implementation transitions
    status: implemented
    alternativesText: >-
      - **Add more prose to AGENTS.md only.** Rejected: the existing policy
      already prohibited premature implementation; documentation alone did not
      prevent direct frontmatter edits.

      - **Rely on CI only.** Rejected: CI provides merge-time detection but no
      safe, repeatable mutation path for contributors and can be bypassed if
      branch protection is absent.

      - **Let contributors edit frontmatter after a validate command.**
      Rejected: validation and mutation remain separable, allowing the RFC to
      drift between the two operations.

      - **Add a `--stamp` flag to `rfc.validate`.** Rejected: `rfc.validate`
      must remain fast and side-effect free; mutation and locking belong to a
      dedicated command.

      - **Check GitHub branch rules through the API.** Rejected: normal CI must
      not require an administrative token or fail on external network/API
      availability. The offline policy is versioned and the operator applies the
      matching setting.

      - **Run arbitrary shell checks from the stamp command.** Rejected:
      RFC-0268 deliberately constrains acceptance probes to an auditable
      vocabulary.
    file: docs/rfcs/archive/implemented/rfc-0476-enforce-verified-rfc-implementation-transitions.md
  - kind: rejected-alternative
    rfcId: RFC-0477
    title: Ensure bordbuch git synchronization on mission lifecycle and add
      sternsystem.status
    status: implemented
    alternativesText: >-
      1. **Automate `sternsystem.sync` inside `mission.close`.** Rejected:
      RFC-0472 explicitly states sync is a manual operator action. Push to
      GitHub may require SSH keys and network access; `mission.close` should not
      fail due to external network issues. Instead, `mission.close` warns when
      the mirror is behind.


      2. **Add bordbuch commit to `mission.reconcile` only.** Rejected:
      `mission.reconcile` already commits, but the problem is that entries from
      `mission.open` (written before reconcile) are lost when reconcile does
      `git reset --hard`. The fix must be at the source — each command that
      writes bordbuch must commit its own entry.


      3. **Separate `bordbuch.commit` command.** Rejected: adds an extra step
      that operators must remember. The commit should be automatic and
      transparent, part of the lifecycle command itself.


      4. **Extend `sternsystem.validate` instead of adding
      `sternsystem.status`.** Rejected: `sternsystem.validate` checks structural
      integrity (registry schema, pin file, mirror URL). `sternsystem.status`
      checks runtime synchronization (SHA comparison, dirty files, bordbuch
      events). Different concerns, different audiences.
    file: docs/rfcs/archive/implemented/rfc-0477-ensure-bordbuch-git-synchronization-on-mission-lifecycle-and-add-sternsystem-status.md
  - kind: rejected-alternative
    rfcId: RFC-0478
    title: Platform versioning enforcement and RFC-id monotonicity
    status: implemented
    alternativesText: >-
      - **Monotonic integer instead of SemVer.** Rejected: breaks npm/pnpm
      tooling compatibility. SemVer remains the primary version; semantic hash
      is the enforcement guard.

      - **Git hook for version bump enforcement.** Rejected: `--no-verify`
      bypasses hooks. CI-level validation (`platform.consistency.validate` in
      `ci.local.validate`) is non-bypassable.

      - **Automatic version bump on RFC merge.** Rejected: the RFC declares
      intent (`versionBump`), but the actual `package.json` edit is a separate
      commit. This keeps the RFC as the decision and the version bump as the
      action, both auditable.

      - **V-28 as warning instead of error.** Rejected: monotonicity is critical
      for migrator registry ordering (RFC-0479). A warning would be ignored,
      breaking the ordering assumption.
    file: docs/rfcs/archive/implemented/rfc-0478-platform-versioning-enforcement-and-rfc-monotonicity.md
  - kind: rejected-alternative
    rfcId: RFC-0479
    title: Migrator registry and mission.migrate step
    status: implemented
    alternativesText: >-
      - **Re-onboarding instead of migrators.** Rejected: onboarding is a heavy
      process with human involvement; migrators are automated and deterministic.

      - **Big-bang migration per major release.** Rejected: blocks platform
      development between major releases; migrators allow incremental migration
      per breaking change.

      - **Migrators tied to SemVer, not RFC-ids.** Rejected: SemVer may not
      correspond 1:1 to breaking changes; RFC-ids are stable, unique, and
      monotonically increasing (V-28).

      - **Migrator checkpoint/state file.** Rejected: idempotency makes
      checkpoints unnecessary; restart from beginning is safe and simpler.

      - **Interactive migration (pause for operator input).** Rejected: breaks
      CI/autonomous mode; operator fixes content in workpiece and re-runs, which
      is already the mission workflow.

      - **Declarative migration rules (YAML).** Rejected: insufficient
      expressiveness for complex transformations (JSON-LD restructuring,
      ontology changes). TypeScript migrators are fully expressive and
      type-safe.
    file: docs/rfcs/archive/implemented/rfc-0479-migrator-registry-and-mission-migrate-step.md
  - kind: rejected-alternative
    rfcId: RFC-0480
    title: Mission git workpiece, edits-only-through-missions, and Layer C protection
    status: implemented
    alternativesText: >-
      - **Workpiece as sub-directory of cache clone (shared ancestor).**
      Rejected: breaks isolation — abort would leave the cache clone in a dirty
      state. Fresh git repository per mission is cleaner.

      - **`git cherry-pick` instead of `format-patch` + `git am`.** Rejected:
      cherry-pick requires shared ancestor or manual range; `format-patch` +
      `git am` is the standard git mechanism for transferring commits between
      unrelated repositories.

      - **Delete workpiece on close.** Rejected: prevents `mission.preview` for
      side-by-side comparison, which is a stated requirement.

      - **Automatic workpiece cleanup by age.** Rejected as sole mechanism:
      explicit `mission.cleanup` gives operator control; age-based is a
      supplement, not the primary path.

      - **Branch protection as sole enforcement for
      edits-only-through-missions.** Rejected: branch protection is on the git
      hosting side (GitHub/GitLab), not controllable by the platform.
      Bordbuch-vs-git-log check is platform-side enforcement.

      - **SemVer-based C-contract versioning.** Rejected: C-contract changes are
      tied to RFCs, not SemVer. `breaksC: true` in the RFC frontmatter is the
      signal, validated by `rfc.validate`.

      - **Declarative C-contract as closed enum (DNA-19 style).** Rejected: URL
      patterns and JSON-LD types evolve with the platform; a closed enum would
      require an RFC for every new route pattern. The declarative contract is
      versioned in `packages/ontology/` and changes through the normal RFC
      process with `breaksC: true`.
    file: docs/rfcs/archive/implemented/rfc-0480-mission-git-workpiece-edits-only-through-missions-and-layer-c-protection.md
  - kind: rejected-alternative
    rfcId: RFC-0481
    title: Create PBP business singleton via migrator and complete RFC-0471 content
      migration
    status: implemented
    alternativesText: >-
      - **Migrate all 329 content references in this RFC.** Rejected: the scope
      is too large (329 references in 32 files, each requiring manual mapping
      from `{business.offer.price.monthly}` to PBP entity paths). This RFC stays
      focused on the minimum needed to unblock builds — the business singleton.
      Content reference migration is a separate future RFC.

      - **Manually create `business.md` without a migrator.** Rejected: without
      a migrator, every new materialization would be missing the file. The
      migrator ensures idempotent, repeatable creation.

      - **Delete `business/` directory now and let content references fail.**
      Rejected: 329 content references would break, causing 32 pages to fail.
      The stopgap `business` collection registration keeps them resolving until
      a future RFC migrates them.

      - **Create `business.md` from `business-profile/de/company.md` instead of
      `business/de/company.md`.** Rejected: `business-profile/de/company.md` is
      in the old format (not PBP), and the migrator's job is to transform old to
      new. Using the old `business/` source is the correct direction.
    file: docs/rfcs/archive/implemented/rfc-0481-migrate-legacy-business-content-to-pbp-business-profile.md
  - kind: rejected-alternative
    rfcId: RFC-0482
    title: PBP presentation fields for legacy business data migration
    status: implemented
    alternativesText: >-
      - **Create a separate `site-presentation` content collection.** Rejected:
      fragments business data across two collections, violating the
      single-source-of-truth principle. Presentation data is entity-scoped and
      belongs on the entity.


      - **Extend RFC-0045 content references to support formatters.** Rejected:
      adds complexity to the reference resolution mechanism
      (`{collection.file.field|formatCurrency}`) and changes a foundational RFC.
      A flexible `presentation` record achieves the same goal with zero resolver
      changes.


      - **Define typed sub-schemas for each presentation field.** Rejected:
      presentation data is site-specific (different sites have different price
      formats, guarantee labels, meta dates). A `z.record(z.string(),
      z.unknown())` bag is the right granularity — structural validation belongs
      in the PBP spec, not in presentation.


      - **Bump to `pbp/*@2`.** Rejected: adding optional fields is additive
      within `@1`. A namespace bump requires a migration contract and breaks all
      existing entities. No breaking change is being made.
    file: docs/rfcs/archive/implemented/rfc-0482-pbp-presentation-fields-for-legacy-business-data-migration.md
  - kind: rejected-alternative
    rfcId: RFC-0483
    title: Migrate legacy business content references to PBP and remove stopgap
      collection
    status: implemented
    alternativesText: >-
      - **Inline values (hardcode).** Rejected: violates RFC-0045 "Never
      hardcode business data" and creates maintenance burden for 329 references.


      - **Keep stopgap indefinitely.** Rejected: the stopgap was always intended
      to be temporary. It duplicates data, confuses the PBP compiler's
      strictness model, and blocks `@gogol/business` deletion cleanup.


      - **Partial migration (structural only).** Rejected: 41/61 patterns would
      remain on the stopgap, preventing its deletion. RFC-0482 enables full
      migration.
    file: docs/rfcs/archive/implemented/rfc-0483-migrate-legacy-business-content-references-to-pbp-and-remove-stopgap.md
  - kind: rejected-alternative
    rfcId: RFC-0484
    title: "Manifest resolver: skip group directories silently"
    status: implemented
    alternativesText: >-
      - **Add manifest files to group directories.** Rejected: group directories
      are not components and should not participate in the cosmic name catalog.
      Adding manifests would pollute the catalog with non-component entries.


      - **Filter group directories by name.** Rejected: hardcoding a list of
      group directory names is brittle and requires maintenance. The absence of
      a manifest file is the correct signal.


      - **Check for subdirectories before trying to read a manifest.** Rejected:
      adds complexity for a simple noise-suppression fix. The current
      try/catch/continue flow is correct — only the logging is unnecessary.
    file: docs/rfcs/archive/implemented/rfc-0484-manifest-resolver-skip-group-directories-silently.md
  - kind: rejected-alternative
    rfcId: RFC-0485
    title: Remove stale font preload for /fonts/inter-400.woff2
    status: implemented
    alternativesText: >-
      - **Dynamic preload path from generated CSS.** Read the hashed font URL
      from the generated CSS and inject it as the preload href. Rejected: adds
      complexity (CSS parsing at build time) for minimal benefit. The
      `@font-face` declaration in the CSS already provides the browser with the
      font URL — the preload hint only saves the CSS parse step, which is
      negligible for self-hosted fonts inlined in `global.css`.


      - **Copy font files to public/fonts/.** Copy the `@fontsource` woff2 files
      to `public/fonts/` so the preload path resolves. Rejected: duplicates font
      files and breaks the Vite bundling model. The `@fontsource` CSS imports
      are the single source of truth for font assets.


      - **Keep preload, fix path.** Change the href to the Vite-hashed URL.
      Rejected: the hash changes on every build, making a hardcoded path
      impossible. A dynamic approach would require build-time CSS parsing, which
      is the same as the "dynamic preload" alternative above.
    file: docs/rfcs/archive/implemented/rfc-0485-remove-stale-font-preload-path.md
  - kind: rejected-alternative
    rfcId: RFC-0486
    title: Suppress Vite dynamic import warning in growth provider
    status: implemented
    alternativesText: >-
      - **Static import map.** Replace the variable specifier with explicit
      `import()` calls using string literals (e.g. `() =>
      import("@gogol/growth-adapter-matomo")`). Rejected: Vite would try to
      resolve `@gogol/growth-adapter-matomo` in every consuming app, including
      apps that don't depend on it. This creates build failures, not just
      warnings.


      - **Move loader map to app-local code.** Each app defines its own loader
      map with static imports for its declared adapters. Rejected: the AGENTS.md
      for `@gogol/growth` explicitly states "the host owns the static adapter
      loader map" — moving it to app-local code breaks the portability contract.


      - **Vite config suppression.** Suppress the warning globally via
      `vite.config.mjs` `build.rollupOptions.onwarn` or `optimizeDeps`.
      Rejected: suppresses all dynamic import warnings, including ones that
      might indicate real issues. The `/* @vite-ignore */` comment is scoped to
      the specific intentional import.
    file: docs/rfcs/archive/implemented/rfc-0486-suppress-vite-dynamic-import-warning-in-growth-provider.md
  - kind: rejected-alternative
    rfcId: RFC-0487
    title: Remove B2C widerruf routes from warpgogol-com
    status: implemented
    alternativesText: >-
      - **Keep pages, add B2B-only disclaimer.** Rejected: the expert analysis
      is unambiguous — keeping B2C legal pages on a B2B-only site creates legal
      exposure and contradicts the AGB. A disclaimer does not fix the
      fundamental mismatch.


      - **Redirect to /notausgang/.** Rejected: Widerruf (consumer right) and
      Kündigung (contract termination) are different legal mechanisms.
      Redirecting would conflate them. 410 Gone is the correct signal — the
      content is permanently removed.


      - **Return 404 instead of 410.** Rejected: 410 Gone is more precise than
      404 — it tells search engines and clients that the resource was
      intentionally removed, not that there's a broken link. The expert
      explicitly recommends 410.


      - **Remove pages but keep prose files as unlinked reference.** Rejected:
      the expert explicitly says not to keep them as reference materials.
      Unlinked prose files could still be discovered and create confusion.
    file: docs/rfcs/archive/implemented/rfc-0487-remove-b2c-widerruf-routes-from-webgogol-com.md
  - kind: rejected-alternative
    rfcId: RFC-0488
    title: Redesign the material credits page as a provenance registry
    status: implemented
    alternativesText: >-
      - **Hand-edit the generated page.** Rejected — the page is generator-owned
      (RFC-0081 marker). Hand-editing would be overwritten on the next
      `material.credits.generate` run.

      - **Create a separate provenance registry collection.** Rejected —
      duplicates the sidecar system and creates drift. The sidecar-per-asset
      model is the canonical source.

      - **Drop the prose fallback entirely.** Rejected — it serves no-JS readers
      and machine-readable consumers. Keeping it is low cost.

      - **Add client-side filtering.** Deferred — the page is server-rendered.
      Filters are a possible future enhancement but not needed for the
      provenance registry to be useful.

      - **Hardcode the rights audit results.** Rejected — the operator must make
      a legal decision about the Stuttgart Marathon photo and Komoot screenshot.
      The RFC provides the framework; the operator provides the decision.
    file: docs/rfcs/archive/implemented/rfc-0488-redesign-material-credits-page-as-provenance-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0489
    title: Redesign the open-source page as a deployment-specific SBOM registry
    status: implemented
    alternativesText: >-
      - **Hand-edit the generated page.** Rejected — the page is generator-owned
      (RFC-0081 marker). Hand-editing would be overwritten on the next
      `open-source.generate` run.

      - **Keep the current generator and just add UK translation.** Rejected —
      the fundamental problems (39 000-line DOM, workspace-wide inventory, no
      SBOM, no license normalization) remain. A translation-only fix would
      double the problem by creating a second 39 000-line page.

      - **Remove the page entirely and link to an external SBOM.** Rejected —
      the page is a legal compliance requirement (Drittanbieter-Hinweise) and
      must be accessible on the site. External SBOMs are not a substitute for a
      human-readable page.

      - **Generate SPDX SBOM instead of CycloneDX.** Deferred — CycloneDX is the
      primary format because it has better tooling support in the JS ecosystem.
      SPDX can be added later if a consumer requires it.

      - **Add client-side search and filtering.** Deferred — the compact table
      (a few hundred rows after deduplication and scope filtering) is manageable
      without client-side JavaScript. Filters are a possible future enhancement.

      - **Implement a full license policy engine.** Deferred — the validator
      checks for unknown/conflicting licenses and blocks, but a full
      allowed/blocked list with waiver workflows is a separate RFC.

      - **Implement historical SBOM archiving.** Deferred — the page shows the
      current deployment only. History is a future enhancement.
    file: docs/rfcs/archive/implemented/rfc-0489-redesign-open-source-page-as-deployment-specific-sbom-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0490
    title: Reposition the website-local depth-0 pillar as an industry navigation hub
    status: implemented
    alternativesText: >-
      - **Hand-edit the generated page.** Rejected — the page is generator-owned
      (RFC-0081 marker). Hand-editing would be overwritten on the next
      `surface.generate` run.

      - **Create an authored page at `pages/uk/website.md`.** Rejected — the
      `/website/` route is owned by the surface generator. Creating an authored
      page would conflict with the generated route and break the surface
      registry.

      - **Add new block archetypes (industry-catalog, adaptation-grid,
      product-price).** Rejected — the hub layout can be expressed with existing
      archetypes (hero, markdown, audience-cards, final-cta). Adding new
      archetypes increases the schema surface without clear benefit.

      - **Hardcode the pillar content in the baker.** Rejected — the pillar
      content (headings, leads, CTAs) is language-specific and belongs in the
      blueprint, not in TypeScript. The baker reads the blueprint; it does not
      contain content.

      - **Use a bespoke narrative (RFC-0207) for the pillar hero.** Deferred —
      the narrative system is designed for depth ≥1 tuple-specific pages. The
      pillar is a single page with stable content that belongs in the blueprint.
      A future RFC may add narrative support if bespoke pillar copy is desired.

      - **Add client-side industry search.** Deferred — the catalog is
      server-rendered with 2–15 industries. Search adds complexity without clear
      value at this scale. A future RFC may add it when the catalog grows beyond
      ~20 industries.

      - **Create a separate industry registry collection.** Rejected — the
      surface axis universe is already the canonical industry list. A separate
      registry would create drift and dual maintenance.

      - **Extend `surface.validate` instead of creating
      `surface.hub.validate`.** Rejected — `surface.validate` validates the
      blueprint schema (structural correctness of all levels and axes). The hub
      checks are semantic (hero CTA is an anchor, no commercial promises, title
      communicates hub function) and apply only to depth-0 pillar pages with a
      `pillar` block. Mixing schema validation with hub-specific semantic checks
      in one command would blur the command's purpose and make the output harder
      to act on. A separate command keeps the concern boundary clear.
    file: docs/rfcs/archive/implemented/rfc-0490-reposition-website-local-pillar-as-industry-navigation-hub.md
  - kind: rejected-alternative
    rfcId: RFC-0491
    title: Enforce rfc.create as the only sanctioned path for RFC creation and fix
      spec.materialize archive scanning
    status: implemented
    alternativesText: >-
      - **Add a pre-commit hook that validates RFC numbers.** Rejected — the
      WGogol platform does not use git hooks for contract enforcement (contracts
      are enforced through commands and agent instructions). A pre-commit hook
      would be a new pattern that requires CI setup and is bypassed by
      `--no-verify`.


      - **Make `rfc.create` the only way to create RFC files (block manual
      creation).** Rejected — there is no mechanism to block manual file
      creation in a monorepo. The contract is enforced through AGENTS.md
      instructions and post-hoc validation (V-02, V-28), not through filesystem
      permissions.


      - **Merge `spec.materialize`'s RFC creation into `rfc.create`.** Rejected
      — `spec.materialize` has spec-specific scaffolding (specRef traceability,
      node binding, amendment resolution) that `rfc.create` does not need. The
      fix is to share the file-scanning function, not to merge the creation
      logic.


      - **Add a `--dry-run` flag to `rfc.create` instead of a separate
      `rfc.next-id` command.** Rejected — `rfc.create --dry-run` would still
      require a `--title` flag, which the agent may not have yet. `rfc.next-id`
      is a pure number query with no prerequisites. It is also useful for
      referencing future RFC numbers in other documents (e.g. "this will be
      RFC-NNNN") before the RFC is created.


      - **Scan only active RFCs (not archive) for `rfc.next-id`.** Rejected —
      this is the exact bug that `spec.materialize` has. Archived RFCs occupy
      number space and must be included in the max calculation to prevent
      duplicates and non-monotonic ids.


      - **Add a `--next-id` flag to `rfc.list` instead of a separate command.**
      Rejected — `rfc.list` returns a list of RFC entries (id, title, status,
      kind, scope, owners, dates, file). `rfc.next-id` returns a single number.
      The semantic distinction is strong: `rfc.next-id` is a pure scalar query
      with no filtering, no entry parsing, and a minimal result type
      (`RfcNextIdResult` with 5 fields vs. `RfcListResult` with an entries
      array). Adding `--next-id` to `rfc.list` would conflate two different
      output shapes behind one command and require conditional return-type logic
      in the handler. A separate command keeps each handler focused.
    file: docs/rfcs/archive/implemented/rfc-0491-enforce-rfc-create-as-the-only-sanctioned-path-for-rfc-creation-and-fix-spec-materialize-archive-scanning.md
  - kind: rejected-alternative
    rfcId: RFC-0492
    title: Industry dossier model for surface/industries pages
    status: implemented
    alternativesText: >-
      1. **Content-only fixes without schema changes.** Rejected — the expert
      analysis shows the gap is structural. Without new fields, every industry
      page is the same template with different nouns. No amount of text editing
      can produce trade-specific service taxonomies, customer journeys, or
      recommended architectures.


      2. **New block archetypes per dossier section.** Rejected — the existing
      block types (cardGrid, listCards, md, linkedCardGrid, ctaBlock) are
      sufficient to express the dossier structure. Adding new archetypes would
      increase the component surface without adding expressive power. If a
      future dossier section needs a genuinely new visual pattern, a separate
      RFC should propose it.


      3. **LLM-generated dossier fields.** Rejected — the dossier fields are
      authored content that reflects trade-specific expertise. LLM generation
      risks hallucinating service categories, trust signals, or architecture
      recommendations that do not match the actual trade. The enrichment
      pipeline (RFC-0197/0207) remains limited to narratives (hero/bridge
      prose).


      4. **Hardcoded industry dossiers in the blueprint.** Rejected — the
      dossier fields belong in the content collection
      (`surface/industries/{lang}/*.md`), not in the blueprint YAML. The
      blueprint defines structure; the content collection defines substance.
      This separation is consistent with RFC-0193.


      5. **Separate `industry-dossier` content collection.** Rejected — adding a
      new collection would require a new axis universe source, new loading
      logic, and a new mapping between the industry axis and the dossier
      collection. Extending the existing industry record frontmatter is simpler
      and maintains the 1:1 relationship between an industry axis value and its
      dossier.


      6. **Result-claim phrases allowed with measurement definition.** Deferred
      — the expert suggests that result claims could be allowed when accompanied
      by a `measurementDefinition` block (baseline, period, sample, evidence,
      limitations). This is a valuable future extension but adds complexity that
      is not needed for the initial dossier model. This RFC prohibits result
      claims unconditionally; a future RFC can introduce the measurement-gated
      exception.
    file: docs/rfcs/archive/implemented/rfc-0492-industry-dossier-model-for-surface-industries-pages.md
  - kind: rejected-alternative
    rfcId: RFC-0493
    title: YAML quoting policy and parse validation
    status: implemented
    alternativesText: >-
      ### Why not yamllint or yq?


      `yamllint` (Python) and `yq` (Go) are external runtime dependencies. This
      monorepo is Node.js/TypeScript-only. The `yaml` (Eemeli AY) npm package is
      already a project dependency and covers parse validation + duplicate-key
      detection. `eslint-plugin-yml` is a Node.js ESLint plugin that covers
      quoting-style enforcement with auto-fix. Adding Python or Go runtimes for
      YAML tooling would violate the ecosystem's tooling boundary.


      ### Why not just Prettier?


      Prettier normalizes quoting when run (`pnpm format`), but it does not
      **gate** — an agent can commit unformatted YAML and CI won't catch it
      unless `pnpm format:check` is in the pipeline. `eslint-plugin-yml` with
      `yml/plain-scalar` provides a CI-gateable lint rule with auto-fix.
      `yaml.parse.validate` catches parse errors that Prettier would silently
      reformat (or fail on). Together they form a defense-in-depth: agent guide
      (prevention) → Prettier (normalization) → eslint-plugin-yml (enforcement)
      → yaml.parse.validate (validation).


      ### Why not extend `yaml.contract.lint` instead of a new command?


      `yaml.contract.lint` checks file extensions and JSON-in-YAML content — it
      does not parse file contents. Adding parse validation to it would change
      its scope from "extension contract" to "extension + content contract",
      violating the one-responsibility principle. A separate command keeps the
      concerns clean: `yaml.contract.lint` governs which files should be YAML;
      `yaml.parse.validate` governs whether YAML files are valid.
    file: docs/rfcs/archive/implemented/rfc-0493-yaml-quoting-policy-and-parse-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0494
    title: City content collection for depth-4 local context
    status: implemented
    alternativesText: >-
      1. **Amendment to RFC-0492.** Rejected — RFC-0492's gap is a design error
      (incorrect assumption about an existing collection), but fixing it
      requires a new content collection, baker changes, and expand.ts changes
      that go beyond a simple correction. An amendment would conflate the
      original RFC's scope (industry dossier model) with a new concern (city
      content collection). A separate RFC keeps the concerns clean.


      2. **New blueprint axis (`cityContent`).** Rejected — adding a new axis
      without a `match` field in demand records complicates the axis model. The
      city axis already exists; the content collection is a supplementary data
      layer, not a separate axis.


      3. **Replace `provider: geo.cities` with `collection: cities`.** Rejected
      — this would require duplicating geo data (slug, name, localized slugs)
      inside content records, violating the separation between geo (structural)
      and content (editorial). It would also break the geo catalog validation
      pipeline.


      4. **Declare content collection in blueprint YAML.** Rejected — adding an
      optional `contentCollection` field to the blueprint axis schema adds
      configuration surface for a single use case. The implicit loading (derive
      collection name from provider name, e.g. `geo.cities` → `cities`) is
      simpler and generalizes to future geo-provider axes that may need
      supplementary content.


      5. **Separate `surface.cities.validate` command.** Rejected — the existing
      `surface.doorway-risk.report` (RFC-0492) already validates the presence of
      city context fields. Adding a second validator would duplicate the check
      with different semantics.
    file: docs/rfcs/archive/implemented/rfc-0494-city-content-collection-for-depth-4-local-context.md
  - kind: rejected-alternative
    rfcId: RFC-0495
    title: Surface URL restructuring — remove country/region segments from canonical
      URLs
    status: implemented
    alternativesText: >-
      - **Keep `{country}/{region}` in canonical URLs.** Rejected: the expert
      review (file 14.3) identified these segments as non-descriptive and
      harmful to SEO. The city slug already implies country and region.

      - **Remove country and region axes entirely from the blueprint.**
      Rejected: the axes are needed for eligibility (which city records are
      live) and navigation hubs (depth-2, depth-3). Only the slug templates drop
      them from the URL path.

      - **Add a separate `surface.url.migrate` command.** Rejected: the existing
      `buildRetiredSurfaceRedirectBlock` in `public.infrastructure.generate`
      already generates redirect entries for surface URLs. Adding a separate
      command duplicates this responsibility. Reversing the redirect direction
      in the existing function is simpler.

      - **Add a separate `surface.redirect.validate` command.** Rejected: the
      existing `redirect.map.validate` command in `managed-public.ts` already
      validates the `_redirects` file. Extending it to check for old URL
      patterns is simpler than a new command.

      - **Runtime HTTP probing for redirect verification.** Rejected: the
      ecosystem is build-time-focused. Static validation of the `_redirects`
      file is sufficient — the hosting layer (Cloudflare Pages) guarantees 301
      behavior for declared redirects.

      - **Gradual URL migration with both old and new URLs live.** Rejected:
      forward-only — no dual-path. The redirect map handles old URLs; new URLs
      are canonical from deployment.
    file: docs/rfcs/archive/implemented/rfc-0495-surface-url-restructuring-remove-country-region-segments.md
  - kind: rejected-alternative
    rfcId: RFC-0496
    title: Service content collection and service dossier pages
    status: implemented
    alternativesText: >-
      1. **Depth 1.5 in `website-local` blueprint.** Rejected — the blueprint
      schema requires `depth: z.number().int().min(0)` (integer depths only).
      Fractional depths fail Zod validation. Additionally, the eligibility
      engine uses axis order position to determine which axes are active at each
      depth — inserting a `service` axis between `industry` and `country` would
      make depth-2 = `industry × service`, not `industry × country`, breaking
      the geo cascade.


      2. **Renumber all depths in `website-local`.** Rejected — inserting
      `service` at depth-2 would shift country→3, region→4, city→5, demand→6.
      This requires updating all depth-dependent config (`minRecordsPerDepth`,
      `noindexBelowPerDepth`, `substanceMinPerDepth`, `evidencePerDepth`,
      `demandPerDepth`, `depthRoles`, `slaDaysPerDepth`, `regionalGateDepths`)
      in the blueprint, all depth comparisons in the eligibility engine, and all
      depth-specific baker specializations. The blast radius is disproportionate
      to the benefit — a separate surface achieves the same goal without
      touching the geo cascade.


      3. **Extend blueprint schema to support non-sequential levels.** Rejected
      — over-engineering. The positional axis model is fundamental to the
      eligibility engine. Adding support for non-positional axes or fractional
      depths would complicate every depth-dependent function (`childrenOf`,
      `siblingsOf`, `skipSingletonChildren`, `levelByDepth`). A separate surface
      is simpler.


      4. **Service pages as authored (non-surface) pages.** Rejected — authored
      pages lack the eligibility engine, publication gate, claim restriction
      enforcement, and automatic cross-linking that the surface model provides.
      Service pages need the same governance as industry pages (RFC-0492).


      5. **LLM-generated service records.** Rejected — service records carry
      trade-specific expertise (variants, price models, booking requirements)
      that must be authored by operators who understand the trade. LLM
      generation risks hallucinating service variants, price models, or booking
      workflows that do not match the actual trade. The enrichment pipeline
      (RFC-0197/0207) remains limited to narratives.


      6. **Separate `service-dossier` content collection outside the surface
      model.** Rejected — adding a collection outside the surface model would
      require a new loading path, a new mapping to routes, and a new validation
      pipeline. Using the existing surface model (new blueprint + new
      collection) is simpler and reuses all existing infrastructure.
    file: docs/rfcs/archive/implemented/rfc-0496-service-content-collection-and-service-dossier-pages.md
  - kind: rejected-alternative
    rfcId: RFC-0497
    title: City×service intersection gate — explicit intersection records
    status: implemented
    alternativesText: >-
      1. **Extend `surface.doorway-risk.report` instead of new commands.**
      Rejected — `surface.doorway-risk.report` (RFC-0492) is a diagnostic for
      depth-4 city pages. Intersection gate enforcement requires a blocking
      validator (publication gate, similarity thresholds, substance independence
      test) and a scaling report (industry/city/service/intersection counts).
      These are distinct purposes from doorway-risk diagnosis. Adding them as
      flags would overload the existing command with different semantics.


      2. **Merge `surface.intersection.validate` and
      `surface.intersection.report` into one command.** Rejected — the validate
      command is a blocking gate (exits non-zero on failure), while the report
      command is diagnostic (always exits 0). Merging them would require a
      `--report` flag to switch between blocking and diagnostic behavior, which
      is inconsistent with the ecosystem pattern (RFC-0492 separated
      `surface.industry.validate` from `surface.duplicate-content.report`).


      3. **Auto-generate intersection records from demand records.** Rejected —
      intersection records require editorial approval and local expertise
      (`localServiceQuestions`, `localEvidence`, `uniqueContentBlocks`).
      Auto-generation would defeat the gate's purpose: the intersection record
      exists precisely to prevent automatic page generation without human
      review.


      4. **Keep depth-5 pages as noindex stubs instead of not emitting them.**
      Rejected — the expert review (file 14.3) explicitly identified noindex
      stubs as insufficient. Google may still crawl noindex pages and flag them
      as doorway/scaled content. Absence of the page (no route, no sitemap
      entry, no internal link) is the only clean signal.
    file: docs/rfcs/archive/implemented/rfc-0497-city-service-intersection-gate-explicit-intersection-records.md
  - kind: rejected-alternative
    rfcId: RFC-0498
    title: Structured data policy for all surface depths
    status: implemented
    alternativesText: >-
      1. **New `surface.structured-data.validate` command.** Rejected — the
      existing `seo.structured-data.validate` (RFC-0074) already scans rendered
      HTML for JSON-LD types. Adding prohibited-type checking is a natural
      extension of the existing validator, not a separate concern. A new command
      would duplicate the HTML scanning infrastructure and create confusion
      about which validator is authoritative for surface pages. RFC-0492's
      `surface.industry.validate` earned its existence because it checks
      publication gates, claim restrictions, and duplicate content — much more
      than type checking. RFC-0498's enforcement is purely type-based, which
      fits naturally as an extension of the existing structured data validator.


      2. **Inline the per-depth policy in the baker code.** Rejected — the
      per-depth type policy is a declarative contract that belongs in the
      C-contract (`jsonld-types.yaml`), not in baker code. Inlining would make
      the policy invisible to `surface.contract.validate` and would require code
      changes for every policy adjustment. The declarative approach allows
      operators to adjust the policy by editing YAML, not code.


      3. **Extend `surface.contract.validate` only, without
      `seo.structured-data.validate` changes.** Rejected —
      `surface.contract.validate` checks the C-contract against generated
      artifacts, but it does not scan rendered HTML.
      `seo.structured-data.validate` scans the actual rendered output, which is
      the authoritative source for what search engines see. Both checks are
      needed: the C-contract check catches policy drift at build time, and the
      rendered HTML check catches baker bugs that emit wrong types despite the
      contract.
    file: docs/rfcs/archive/implemented/rfc-0498-structured-data-policy-for-all-surface-depths.md
  - kind: rejected-alternative
    rfcId: RFC-0499
    title: Media metadata leakage prevention on surface pages
    status: implemented
    alternativesText: >-
      - **Extend `material.credits.validate` instead of a new command.**
      Rejected: `material.credits.validate` validates sidecar schema and
      coverage — it does not scan rendered HTML. Media-leakage detection
      requires post-build HTML scanning, which is a different pipeline stage
      (`sites-check-postbuild` vs `build.check`). The existing
      `surface.*.validate` pattern (`surface.hub.validate`,
      `surface.service.validate`, `surface.intersection.validate`) establishes
      the precedent for per-concern surface validators.

      - **Naive substring matching for all prohibited strings.** Rejected:
      "Gemini" and "Organization" are common words that appear legitimately in
      prose. Context-aware matching is required to avoid false positives (see
      Risks).

      - **Suppress all media from surface pages.** Rejected: AI-generated images
      may illustrate architectural concepts. The RFC allows them with a
      `Konzeptillustration` label — removing them would degrade the page
      quality.

      - **Handle this in the credits sidecar schema.** Rejected: the sidecar
      schema is the canonical source (RFC-0220). The problem is not the schema —
      it is the baker emitting schema fields into readable HTML. The fix is in
      the baker, not the schema.
    file: docs/rfcs/archive/implemented/rfc-0499-media-metadata-leakage-prevention-on-surface-pages.md
  - kind: rejected-alternative
    rfcId: RFC-0500
    title: Ratgeber editorial knowledge hub — article collection and hub restructure
    status: implemented
    alternativesText: >-
      - **Keep `topics` collection, add fields.** Rejected: the collection name
      `topics` does not reflect the editorial article nature. A clean break to
      `articles` is clearer and aligns with the expert's editorial hub concept.

      - **Keep hub as `article` semantic type.** Rejected: the hub is a
      collection of articles, not a single article. `CollectionPage` is the
      correct schema.org type.

      - **Add categories as a blueprint axis.** Rejected: categories are a
      grouping mechanism for the hub layout, not a URL axis. Articles live at
      `/ratgeber/{slug}/`, not `/ratgeber/{category}/{slug}/`.
    file: docs/rfcs/archive/implemented/rfc-0500-ratgeber-editorial-knowledge-hub-article-collection-and-hub-restructure.md
  - kind: rejected-alternative
    rfcId: RFC-0501
    title: Ratgeber article types, mandatory structure, and publication gate
    status: implemented
    alternativesText: >-
      - **Extend `ratgeber.hub.validate` instead of creating a new command.**
      Rejected: `ratgeber.hub.validate` validates the surface artifact
      (`surface.generated.yaml`) — JSON-LD types, hub layout, card fields,
      reserved slugs. The 10-section structure check reads prose markdown files
      (`prose/{lang}/ratgeber-{slug}.md`), which is a different I/O pattern and
      a different concern (content structure vs. surface artifact). Mixing both
      in one command would blur the boundary between artifact validation and
      content validation.


      - **Extend `article.depth.validate` with ratgeber-specific section
      checks.** Rejected: `article.depth.validate` is generic across all
      article-typed surfaces (blog, ratgeber, future surfaces). The 10-section
      structure, seven article types, and type-specific requirements are
      ratgeber-specific editorial decisions. Embedding them in the generic
      validator would couple the generic check to one surface's editorial
      policy. A separate command keeps the generic validator clean and the
      ratgeber-specific rules explicit.


      - **Make the 10-section structure advisory (warning, not error) for
      published articles.** Rejected: the expert requires a consistent structure
      across all published articles. An advisory check would allow incomplete
      articles to publish, defeating the purpose of the publication gate. The
      structure is mandatory for `status: published` and advisory for `status:
      draft` / `status: review-required` (RG-ART-06).
    file: docs/rfcs/archive/implemented/rfc-0501-ratgeber-article-types-structure-and-publication-gate.md
  - kind: rejected-alternative
    rfcId: RFC-0502
    title: Ratgeber editorial provenance — authors, sources, claims, and review
      metadata
    status: implemented
    alternativesText: >-
      1. **Extend the existing `people` collection instead of creating a new
      `authors` collection.** Rejected because the `people` collection is
      platform-wide (PBP program, RFC-0398) and carries business-profile
      semantics (roles, offerings, evidence). Ratgeber authors are an editorial
      concept — a person who writes articles — not a business entity. Mixing the
      two would conflate editorial provenance with business identity.


      2. **Store provenance metadata directly in the article record instead of
      using CKL claim sidecars.** Rejected because the CKL claim model
      (RFC-0211) already provides a provenance sidecar with `provenance`,
      `asOf`, `validUntil`, `sourceRef`, and `confidence` fields. Creating a
      parallel provenance system would violate the one-model principle and
      duplicate the source descriptor resolution machinery.


      3. **Add provenance as a JSON-LD-only concern (no visual footer).**
      Rejected because readers benefit from seeing who wrote an article and what
      sources were used. A visual footer builds trust and is standard practice
      for editorial content. The JSON-LD `author` property is already emitted
      via `authorId` — this RFC adds the human-visible display, not the
      machine-readable output.


      4. **Use `sourceRef` instead of `sourceId` in the article record's
      `sources` field.** Rejected because `sourceRef` is a field _within_ a CKL
      claim annotation — it points from a single claim to its source. The
      article record's `sources` field is an article-level grouping that binds
      multiple claim IDs to a common source descriptor. Using `sourceId`
      distinguishes the structural role: article-level grouping vs. per-claim
      pointer. Both resolve to the same source descriptor registry.
    file: docs/rfcs/archive/implemented/rfc-0502-ratgeber-editorial-provenance-authors-sources-claims.md
  - kind: rejected-alternative
    rfcId: RFC-0503
    title: Ratgeber editorial policy page and article status workflow
    status: implemented
    alternativesText: >-
      1. **Extend `ratgeber.hub.validate` instead of creating a new command.**
      Rejected: `ratgeber.hub.validate` validates the surface artifact
      (`surface.generated.yaml`) — JSON-LD types, hub layout, card fields. The
      policy page validation reads prose markdown files and checks for required
      H2 sections, which is a different I/O pattern and a different concern
      (content structure vs. surface artifact). Mixing both in one command would
      blur the boundary between artifact validation and content validation,
      following the same separation rationale as RFC-0501.


      2. **Make the editorial policy page a surface-generated page.** Rejected:
      the policy page is a single static page per language, not a data-driven
      collection. Surface generation is for multi-record, axis-driven pages. A
      static prose page registered in `system.md` is the correct mechanism.


      3. **Skip the url-schema.yaml extension.** Rejected: `breaksC: true` is
      declared because the new URL enters the sitemap. The C-contract should
      document the URL even if `surface.contract.validate` does not check
      authored pages. Omitting it would leave the contract incomplete.
    file: docs/rfcs/archive/implemented/rfc-0503-ratgeber-editorial-policy-page-and-status-workflow.md
  - kind: rejected-alternative
    rfcId: RFC-0504
    title: Ratgeber article archetype — 12-section layout, article header, TOC, and
      changelog
    status: implemented
    alternativesText: >-
      **Keep single markdown block.** Rejected — the expert explicitly requires
      structured section rendering, not a continuous markdown blob. The current
      archetype hides most content.


      **Manual TOC in frontmatter.** Rejected — auto-generation from H2 headings
      is simpler, less error-prone, and always in sync with the prose body.


      **Separate RFC per block type.** Rejected — the article header, TOC,
      changelog, and CTA tiers are tightly coupled in the 12-section layout.
      Splitting would create artificial dependencies.
    file: docs/rfcs/archive/implemented/rfc-0504-ratgeber-article-archetype-12-section-layout-header-toc-changelog.md
  - kind: rejected-alternative
    rfcId: RFC-0505
    title: Ratgeber extended claim registry — structured claim records with source
      binding and expiry
    status: implemented
    alternativesText: >-
      **Extend RFC-0502 claim sidecars instead of new collection.** Rejected —
      claim sidecars (RFC-0211) are article-adjacent files with opaque IDs. A
      dedicated claim collection allows cross-article claim reuse, independent
      validation, and expiry tracking.


      **Use CKL claim model directly.** Rejected — the CKL model (RFC-0211) is a
      platform-wide claim registry with different fields. The ratgeber claim
      registry is editorial-specific with `calculationInputs`, `limitations`,
      and `expiresAt` that the CKL model does not carry. This RFC binds to CKL
      sources via `sourceRefs[].sourceId` but does not duplicate the CKL claim
      schema.


      **No expiry tracking.** Rejected — the expert explicitly requires that
      outdated claims be flagged for review. `expiresAt` is a lightweight
      mechanism that does not require automated monitoring.
    file: docs/rfcs/archive/implemented/rfc-0505-ratgeber-extended-claim-registry-structured-claim-records.md
  - kind: rejected-alternative
    rfcId: RFC-0506
    title: Ratgeber article JSON-LD — Article fields, BreadcrumbList, and FAQPage
      prohibition
    status: implemented
    alternativesText: >-
      **Allow FAQPage for rich results.** Rejected — the expert explicitly
      prohibits using FAQPage solely for rich results. The FAQ content is
      visible on the page as HTML blocks, which is sufficient for readers.


      **Emit Article JSON-LD from the baker.** Rejected — JSON-LD emission is
      the renderer's responsibility (`@gogol/share`), not the baker's. The baker
      produces page blocks; the renderer serializes them to HTML + JSON-LD.


      **Separate C-contract RFC.** Rejected — the C-contract change is small
      (two optional fields + one prohibited type) and directly tied to the
      ratgeber article JSON-LD policy. A separate RFC would create unnecessary
      overhead.
    file: docs/rfcs/archive/implemented/rfc-0506-ratgeber-article-json-ld-article-fields-breadcrumblist-faqpage-prohibition.md
  - kind: rejected-alternative
    rfcId: RFC-0507
    title: Ratgeber hub card fields and cross-page editorial alignment
    status: implemented
    alternativesText: >-
      **Separate RFC per page.** Rejected — the cross-page changes are all
      triggered by the same expert file and are editorial content corrections.
      Splitting would create four RFCs for four one-line content edits.


      **Package-level enforcement of claim consistency.** Rejected — cross-page
      claim consistency is an editorial responsibility, not a build-time check.
      The claim registry (RFC-0505) provides per-article claim tracking;
      cross-page consistency is enforced by editorial review.


      **Footer as a separate RFC.** Rejected — the footer changes are two link
      removals and one group rename. This does not warrant a separate RFC.
    file: docs/rfcs/archive/implemented/rfc-0507-ratgeber-hub-card-fields-and-cross-page-editorial-alignment.md
  - kind: rejected-alternative
    rfcId: RFC-0508
    title: Participant data model — canonical Participant entity, types, statuses,
      and consent
    status: implemented
    alternativesText: >-
      - **A separate `participants` collection.** Rejected — it would require
      duplicating the collection infrastructure (loader, dispatcher, routes) and
      migrating all existing people files to a new path. Extending the `people`
      collection schema is simpler and preserves file paths.

      - **Keep Person schema and add a parallel AI-agent schema.** Rejected —
      the expert requires a unified Participant abstraction with shared
      responsibility/authority/evidence fields. A parallel schema would
      duplicate these fields and require two validators.

      - **Make `participantType` optional with default `human`.** Rejected — the
      discriminator should be explicit. A default would allow records to
      silently remain `human` when they should be another type, and would make
      the schema less self-documenting.

      - **`z.discriminatedUnion("participantType", [...])` instead of a flat
      schema.** Rejected — a discriminated union would require duplicating all
      shared fields (identity, responsibility, evidence, contact, lifecycle,
      visibility) across six variants, increasing maintenance cost. The flat
      schema with `.strict()` + `participant.validate` enforcement provides the
      same type safety with less duplication.
    file: docs/rfcs/archive/implemented/rfc-0508-participant-data-model-types-statuses-and-consent.md
  - kind: rejected-alternative
    rfcId: RFC-0509
    title: Team hub page — /team/ root index route and participant directory
    status: implemented
    alternativesText: >-
      - **Generate the hub from the people collection (virtual route).**
      Rejected — the hub has authored content (hero text, section headings,
      subheadings) that is site-specific. An authored page is simpler and
      follows the block-declarative pattern. Virtual routes are for data-sourced
      pages without authored content.

      - **Keep the founder page and add a separate team hub.** Rejected — the
      founder page is redundant with the profile page. The expert recommends
      consolidating. Keeping both creates duplicate content and confusing
      navigation.

      - **Put the team hub under `/ueber-uns/` (about page).** Rejected — the
      expert recommends a dedicated `/team/` route. The about page
      (`ratgeber-redaktion`) has a different semantic type and purpose.
    file: docs/rfcs/archive/implemented/rfc-0509-team-hub-page-root-index-route-and-participant-directory.md
  - kind: rejected-alternative
    rfcId: RFC-0510
    title: Human profile archetype restructure — responsibility, authority,
      evidence-based career
    status: implemented
    alternativesText: >-
      - **Add new block types (responsibility-section, evidence-list).**
      Rejected — the existing `controlled-responsibility-block` and `markdown`
      archetypes cover the needs. Adding new block types would require new
      cosmic names, archetype registry entries, and UI components.

      - **Keep the personal story in the hero statement.** Rejected — the expert
      explicitly recommends removing personal details from the first screen. The
      hero should be professional identity only.

      - **Create a separate editorial route for the personal story (e.g.
      `/team/andrii-syrokomskyi/geschichte/`).** Rejected — the personal story
      is part of the profile page, not a separate page. A separate route would
      fragment the profile and require additional navigation.
    file: docs/rfcs/archive/implemented/rfc-0510-human-profile-archetype-restructure-responsibility-authority-evidence.md
  - kind: rejected-alternative
    rfcId: RFC-0511
    title: AI-agent profile archetype — new page type for AI workers
    status: implemented
    alternativesText: >-
      - **Use the same route pattern as humans (`/team/[slug]/`).** Rejected —
      the expert recommends visual separation between humans and AI agents. A
      shared route pattern makes it impossible to distinguish at the URL level.

      - **Use a single block type for AI agents (one big markdown block).**
      Rejected — the expert requires structured fields for autonomy, rights,
      accountability, and technical stand. A single markdown block would lose
      the structured data that feeds JSON-LD (RFC-0512).

      - **Render the full rightsMatrix with dataAccess.** Rejected — the full
      ACL is internal. The public profile shows a summary only.
    file: docs/rfcs/archive/implemented/rfc-0511-ai-agent-profile-archetype-new-page-type-for-ai-workers.md
  - kind: rejected-alternative
    rfcId: RFC-0512
    title: Team machine-readable profiles — JSON endpoints and Schema.org
    status: implemented
    alternativesText: >-
      - **Use a single JSON endpoint with query parameters (e.g.
      `/api/participants?type=human`).** Rejected — the endpoints are static
      build-time files, not a dynamic API. Static files are simpler, cacheable,
      and require no server runtime.

      - **Emit `Person` JSON-LD for AI agents.** Rejected — AI agents are not
      persons. `SoftwareApplication` is the correct Schema.org type for an AI
      system with a provider.

      - **Include `birthDate` in Person JSON-LD with consent.** Rejected — birth
      year is sensitive personal data. Even with consent, it should not be in
      Schema.org output. The expert recommends minimizing personal data in
      machine-readable formats.
    file: docs/rfcs/archive/implemented/rfc-0512-team-machine-readable-profiles-json-endpoints-and-schema-org.md
  - kind: rejected-alternative
    rfcId: RFC-0513
    title: Team validation, lifecycle, and cross-page alignment
    status: implemented
    alternativesText: >-
      - **Store transition history.** Rejected — the current state is sufficient
      for validation. Transition history would add complexity without clear
      value. The `lastReviewedAt` date tracks the last review, not the
      transition.

      - **Automated email reminders for reviews.** Rejected — the validator
      warns at build time. Automated email is an operational concern, not an
      architectural one.

      - **Delete retired participant records.** Rejected — retired participants
      retain their profile pages for organizational memory. The CTA is removed
      and a badge is shown.
    file: docs/rfcs/archive/implemented/rfc-0513-team-validation-lifecycle-and-cross-page-alignment.md
  - kind: superseded-rfc
    rfcId: RFC-0514
    title: Contact form structured fields — separate email and phone capture
    status: superseded
    closedAt: 2026-07-28
    decisionSummary: "The send-message section gains structured `emailField` and
      `phoneField` props in its manifest. `emailField` is required (enabled:
      true) for all sites using the send-message section; `phoneField` is
      opt-in. When enabled, the section renders labeled `<input>` fields
      alongside the existing textarea. The client script and server API accept
      `email` and `phone` as top-level payload fields. The regex-based
      extraction from the message body is removed entirely — no dual-path, no
      fallback. A new `contact.form.validate` command checks that sites declare
      these props consistently across locales. A migrator (RFC-0479) adds
      default `emailField`/`phoneField` props to existing sites' send-message
      block configurations."
    file: docs/rfcs/archive/implemented/rfc-0514-contact-form-structured-fields-separate-email-and-phone-capture.md
  - kind: rejected-alternative
    rfcId: RFC-0514
    title: Contact form structured fields — separate email and phone capture
    status: superseded
    alternativesText: >-
      - **Retain regex fallback as a compatibility layer.** Rejected — the
      platform is forward-only (no backward compatibility for layers A+B).
      Keeping the regex path alive alongside structured fields creates a
      dual-path that is harder to maintain and violates the forward-only
      principle. Existing sites are migrated by a migrator instead.

      - **Add a dedicated `contact-form` block type separate from
      `send-message`.** Rejected — the send-message section already handles the
      full UX (form, validation, fallback, success state). Adding a parallel
      type would duplicate logic and create migration burden.

      - **Use a third-party form service (e.g. Formspark, Web3Forms).** Rejected
      — the QStash delivery pipeline (RFC-0181) is already built and
      EU-resident. Outsourcing would add a third-party dependency and data
      residency risk.
    file: docs/rfcs/archive/implemented/rfc-0514-contact-form-structured-fields-separate-email-and-phone-capture.md
  - kind: rejected-alternative
    rfcId: RFC-0515
    title: Cosmic pages i18n — localized title and description for passport and
      star-map
    status: implemented
    alternativesText: >-
      - **Add a `cosmic.pages.i18n.validate` build-time command.** Rejected —
      the cosmic page files are generated outputs. A validator on generated
      output re-checks what the generator deterministically controls. If the
      generator is correct, the validator always passes; if the generator is
      wrong, the validator fails on every build until the generator is fixed. A
      generator unit test is more effective: it catches the regression at the
      source, runs faster, and doesn't add a build-time command to the pipeline.

      - **Use PBP reference for tagline in cosmic pages.** Rejected — the
      tagline is a brand string, not a business data field. PBP references are
      for business data (prices, policies, offerings). The tagline belongs in
      `system.md identity.tagline` and should not be injected into page
      metadata.

      - **Remove title/description from cosmic pages entirely.** Rejected —
      pages need metadata for browser tabs, sitemaps, and social sharing. The
      fix is to use a locale-neutral brand, not to remove metadata.

      - **Make cosmic pages DE-only (no UK version).** Rejected — the cosmic
      pages are part of the site's transparency layer and should be available in
      all published locales.

      - **Full Ukrainian localization of cosmic page descriptions.** Rejected —
      cosmic pages are system/audit pages with low traffic. Using `manifest.app`
      (locale-neutral) as the brand is sufficient. Full Ukrainian prose for
      audit page metadata is over-engineering for a P3 cosmetic issue.
    file: docs/rfcs/archive/implemented/rfc-0515-cosmic-pages-i18n-localized-title-and-description-for-passport-and-star-map.md
  - kind: rejected-alternative
    rfcId: RFC-0516
    title: Doorway-risk validator fixes — city+industry lookup and mode:warn
    status: implemented
    alternativesText: >-
      - **Keep demand-slug lookup, add depth-5 fallback** — rejected because
      depth-4 pages have no demand axis at all; a fallback would still fail.

      - **Remove `mode: warn` from blueprint and hardcode warning severity** —
      rejected because the blueprint dossier mode is the correct configuration
      surface for per-blueprint severity control.
    file: docs/rfcs/archive/implemented/rfc-0516-doorway-risk-validator-fixes.md
  - kind: rejected-alternative
    rfcId: RFC-0517
    title: Pre-materialize content quality gate for mission materialization
    status: implemented
    alternativesText: >-
      - **Single blocking level (all validators block):** Rejected because
      missions are frequently opened to fix content quality issues (missing
      `metaDescription`, broken links, incomplete translations). A single
      blocking level would prevent operators from materializing a workpiece
      whose entire purpose is to fix those exact issues.

      - **Standalone `mission.preflight` command:** Rejected for now. A
      standalone command adds surface area (separate scope, flags,
      workpiece-vs-cache-clone detection) without clear benefit over an embedded
      gate. Can be added as a future extension if pre-checks before
      `mission.open` become needed.

      - **Delete workpiece on critical failure:** Rejected. The workpiece is the
      primary diagnostic surface — deleting it destroys the evidence the
      operator needs to understand and fix the structural defect. Preserving it
      (without git init) is consistent with the disposable Werkstück model
      (DNA-46).

      - **Run full `build.prepare` before preflight:** Rejected. `build.prepare`
      has ~40 steps and takes significant time. The preflight is meant to be a
      fast structural check, not a full build. Generated-artifact-dependent
      validators run later in `mission.validate`.
    file: docs/rfcs/archive/implemented/rfc-0517-pre-materialize-content-quality-gate-for-mission-materialization.md
  - kind: rejected-alternative
    rfcId: RFC-0518
    title: Gate metadata on command definitions
    status: implemented
    alternativesText: >-
      - **Separate gate registry file:** Rejected. A separate registry file
      (e.g. `docs/gate-registry.yaml`) would duplicate command names and require
      manual synchronization. Putting metadata on the command definition keeps
      it colocated with the handler.

      - **Required field on all commands:** Rejected. There are 200+ commands;
      requiring `gate` on all of them in one pass is a massive churn event. The
      field is optional and populated incrementally.

      - **Gate metadata in pipeline files:** Rejected. Pipeline files define
      execution order, not gate semantics. A command's severity and conditional
      logic are intrinsic to the command, not to its placement in a pipeline.

      - **Gate metadata as a separate type from KernelCommandMetadata:**
      Rejected. The metadata is about the command; it belongs on the command
      definition. A separate type would require a parallel registration
      mechanism.
    file: docs/rfcs/archive/implemented/rfc-0518-gate-metadata-on-command-definitions.md
  - kind: rejected-alternative
    rfcId: RFC-0519
    title: Gate catalog generator and validator
    status: implemented
    alternativesText: >-
      - **Embed gate catalog in ecosystem manifest:** Rejected. The ecosystem
      manifest is already dense with packages, commands, pipelines, RFCs, and
      DNA registry. A dedicated catalog provides a focused, queryable view. The
      ecosystem manifest projects `gate` metadata on commands (RFC-0518); the
      gate catalog is a derived, focused view of that data plus pipeline
      placement.

      - **Per-site gate catalog:** Rejected. Gates are workspace-scoped (they
      are defined in packages, not per-site). A workspace-level catalog is
      sufficient.

      - **JSON instead of YAML:** Rejected. All generated artifacts in this repo
      use YAML (ecosystem manifest, maintenance debt baseline, platform version
      log). Consistency.

      - **Make GATE-CAT-03 an error:** Rejected. Initially ~200 commands lack
      `gate` metadata. Making this an error would block all builds. Warning mode
      with incremental backfill (RFC-0518) is the pragmatic path.
    file: docs/rfcs/archive/implemented/rfc-0519-gate-catalog-generator-and-validator.md
  - kind: rejected-alternative
    rfcId: RFC-0520
    title: Extract inline guards into named testable functions
    status: implemented
    alternativesText: >-
      - **Extract all inline guards:** Rejected. Only the two guards identified
      in the gate grouping analysis (C-surface regression, Bordbuch-vs-git-log)
      are extracted. Other inline guards (e.g. `mission.close` refusing null
      `reconciledAt`, `mission.materialize` refuse-downgrade) are simpler
      one-liners that do not benefit from extraction.

      - **Move guards to a separate `guards/` package:** Rejected. Both guards
      are handoff-package concerns. Moving them to a separate package adds a
      workspace dependency without benefit. The `guards.ts` file in the handoff
      package provides the shared `GuardResult` type.

      - **Unify with `GateResult` from `@gogol/surface`:** Rejected. The
      `GateResult` in `decision-composer.ts` is specific to indexability gates
      (demand, evidence, substance, freshness, budget). The handoff guards have
      different semantics (release blocking, external edit detection). A shared
      interface would be leaky. The `GuardResult` type in the handoff package is
      analogous but independent.

      - **Make `evaluateCSurfaceGate` async and run `surface.contract.validate`
      inside it:** Rejected. Keeping the I/O in the caller and the logic in the
      pure function makes the function testable without mocking. The caller
      gathers the data; the function evaluates.
    file: docs/rfcs/archive/implemented/rfc-0520-extract-inline-guards-into-named-testable-functions.md
  - kind: rejected-alternative
    rfcId: RFC-0521
    title: Extend document archiving to plans, audits, and umbrella docs.archive
      command
    status: implemented
    alternativesText: >-
      - **RFC-linked archive via `rfc.archive` (single command moves RFC + plan
      + audit).** Rejected — `rfc.archive` would need to reach into
      `docs/plans/` and `docs/audits/`, violating the principle that each
      command owns its directory. Separate commands with an umbrella provide
      clean separation while maintaining convenience.


      - **Flat archive (no status subdirectories for plans/audits).** Rejected
      in favor of mirroring the RFC structure (`archive/<status>/`). Consistency
      across all four document types makes navigation predictable and simplifies
      the `--status` filter.


      - **Frontmatter `rfcId` as primary lookup, filename as fallback.**
      Rejected in favor of filename as primary, frontmatter as fallback. The
      filename convention (`plan-rfc-XXXX-*`, `audit-rfc-XXXX-*`) is stable and
      observed in all existing files. Frontmatter `rfcId` provides a reliable
      fallback for edge cases.


      - **Own lifecycle status for plans (`draft → implemented`).** Rejected —
      plans and audits are implementation artifacts of RFCs, not independent
      governance documents. Their archive eligibility inherits from the parent
      RFC's terminal status. The plan's own `status: draft` field tracks
      document progress, not lifecycle decisions.


      - **Pipeline gate (fail build.check if terminal files are in root).**
      Rejected — archiving is housekeeping, not a correctness gate. Consistent
      with RFC-0367's decision for RFC/ADR archiving.


      - **Atomic umbrella with rollback.** Rejected — adds significant
      complexity for a rare failure mode. All archive commands are idempotent,
      so re-running `docs.archive` after a partial failure is safe and simple.


      - **Keep `rfcPath` in audit frontmatter, update it on archive.** Rejected
      — modifying file content during archive violates the "archive only moves
      files" principle. `rfcId` is the stable identifier; `rfcPath` is a stale
      convenience field. Removing it eliminates the maintenance burden.
    file: docs/rfcs/archive/implemented/rfc-0521-extend-document-archiving-to-plans-audits-and-umbrella-docs-archive-command.md
  - kind: rejected-alternative
    rfcId: RFC-0522
    title: Reconcile dirty cache clone guard, 3-way fallback, and release ID tracking
    status: implemented
    alternativesText: >-
      - **Auto-stash dirty cache clone changes.** Rejected: stashing hides the
      problem and the stash may be forgotten (as happened in the incident).
      Dirty state is a violation of DNA-51 and should be surfaced, not hidden.

      - **Exclude generated files from patches.** Rejected: generated files are
      part of the audit trail. Excluding them loses commit history for those
      files. The 3-way fallback handles generated file conflicts without
      excluding them.

      - **`git am --skip` for conflicting patches.** Rejected: skipping patches
      loses commits and breaks the audit trail. 3-way merge preserves commits
      while resolving conflicts.

      - **`mission.close` blocks on null `releaseId`.** Rejected: valid
      scenarios exist for closing without a release (empty mission, intentional
      skip). Warning is sufficient.

      - **`mission.validate` blocks on dirty cache clone.** Rejected: validate
      may be run to check workpiece state before cache clone is cleaned. Warning
      is sufficient.
    file: docs/rfcs/archive/implemented/rfc-0522-reconcile-dirty-cache-clone-guard-3way-fallback-and-release-id-tracking.md
  - kind: rejected-alternative
    rfcId: RFC-0523
    title: Granular skill concerns taxonomy
    status: implemented
    alternativesText: >-
      - **Keep binary, fix misclassifications.** Reclassify `fo-fix` to
      `implementation` and leave the taxonomy at two values. Rejected because
      the binary system still cannot distinguish content mutation from code
      mutation — the distinction matters for agent autonomy decisions.


      - **Freeform string field.** Allow any string in `concerns` and let each
      skill describe its behavior. Rejected because `forge.skill.validate`
      cannot enforce a closed set, and agents cannot programmatically compare
      risk levels across skills.


      - **Add a separate `risk` field.** Keep `concerns` binary and add a `risk:
      low | medium | high` field. Rejected because `concerns` and `risk` would
      overlap (a `code-mutation` skill is inherently higher risk than a
      `read-only` skill). Combining them into one four-level enum is simpler and
      avoids redundant metadata.
    file: docs/rfcs/archive/implemented/rfc-0523-granular-skill-concerns-taxonomy.md
  - kind: rejected-alternative
    rfcId: RFC-0524
    title: Cumulative knowledge system for forge skills
    status: implemented
    alternativesText: >-
      - **Enforced contract (forge.skill.validate requires knowledge files).**
      Rejected because not every skill needs cumulative knowledge.
      `fo-idea-status` (read-only) and `fo-idea-create-rfc` (document-only)
      would need empty files just to pass validation. Convention with opt-in is
      cleaner.


      - **Fixed three-layer structure for all skills.** Rejected because skills
      have different characteristics. `grilling` does not need L1 (no fix
      patterns). `fo-site-scan` needs all three. Fully adaptive `knowledge:`
      array lets each skill choose.


      - **Separate `confidence.md` file (L3).** Rejected because `confirmations`
      is a single integer per principle — storing it inline in L2 is simpler and
      avoids a fourth file. No need for a separate confidence layer.


      - **Derived confidence from L0 (count matching answers in qa-log).**
      Rejected because parsing L0 to compute confidence on every run is
      expensive and fragile. An explicit counter in L2 is deterministic and
      cheap.


      - **Per-skill or per-principle confidence threshold.** Rejected because
      threshold is an implementation detail, not a design decision. Fixed
      threshold 3 is simple and reasonable. Changing it requires an RFC.
    file: docs/rfcs/archive/implemented/rfc-0524-cumulative-knowledge-system-for-forge-skills.md
  - kind: rejected-alternative
    rfcId: RFC-0525
    title: Maximum-quality video encoding pipeline with AV1
    status: implemented
    alternativesText: >-
      - **AV1 in MP4 container (ISOBMFF) instead of WebM.** Rejected —
      AV1-in-MP4 has narrower browser support than AV1-in-WebM. Chrome and
      Firefox support both, but Safari's AV1 support is limited and more
      reliable with WebM.


      - **AV1 HLS segments (fMP4).** Rejected — AV1 in HLS requires fMP4
      segments and `EXT-X-MAP` declarations. Browser support for AV1 HLS is
      nascent. The complexity is not justified when progressive AV1 already
      covers the use case. HLS stays H.264 with improved parameters.


      - **Two-pass encoding for MP4 instead of CRF.** Rejected — CRF at preset
      `slow` with CRF 17 achieves near-lossless quality. Two-pass is for
      targeting a specific bitrate, not for maximum quality. CRF is the correct
      mode for quality-first encoding.


      - **`libaom-av1` instead of `libsvtav1`.** Rejected — `libsvtav1` is the
      production-grade SVT-AV1 encoder with significantly faster encoding than
      `libaom-av1` at equivalent quality. Since time is not a constraint,
      `libaom-av1` at its slowest settings could achieve marginally better
      compression, but the difference is negligible (1-3%) and `libsvtav1`
      preset 2 is already in the diminishing-returns territory.


      - **Keeping VP9 as the only WebM codec.** Rejected — AV1 delivers 20-40%
      better compression than VP9 at equivalent quality. Adding AV1 as a
      progressive source is a pure quality win with no downside (browsers
      without AV1 support fall through to VP9/MP4).


      - **AV1 for background profile.** Rejected — background videos are short
      muted loops where the encoding time is disproportionate to the size
      savings. The upgraded VP9/MP4 parameters already improve background
      quality.
    file: docs/rfcs/archive/implemented/rfc-0525-maximum-quality-video-encoding-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0526
    title: Codec-infix video file naming standard
    status: implemented
    alternativesText: >-
      - **Keep the asymmetry (AV1-only infix).** Rejected — the asymmetry is the
      problem this RFC solves. Future codecs would require their own infix
      convention, perpetuating the inconsistency.


      - **Use full codec names (`h264` → `libx264`, `vp9` → `libvpx-vp9`).**
      Rejected — the infix should be the codec family name, not the ffmpeg
      encoder name. `h264` and `vp9` are universally understood; `libx264` and
      `libvpx-vp9` are ffmpeg-specific implementation details.


      - **Use container-only disambiguation (e.g. `progressive-mp4.mp4`,
      `progressive-webm.webm`).** Rejected — this is redundant (the container is
      already in the extension) and does not disambiguate AV1-in-WebM from
      VP9-in-WebM, which was the original motivation for the infix.
    file: docs/rfcs/archive/implemented/rfc-0526-codec-infix-video-file-naming-standard.md
  - kind: rejected-alternative
    rfcId: RFC-0527
    title: Content reference index — unified indexing and resolution contour
    status: implemented
    alternativesText: >-
      - **Keep brace syntax, build index only.** Rejected — braces force YAML
      string quoting, making sidecar files noisy and hard to read. The index
      makes braceless resolution safe (index-validated), removing the need for
      braces as disambiguation.


      - **Lazy field indexing (only index referenced fields).** Rejected — adds
      complexity (two-pass scan) for marginal size savings. Full indexing is
      simpler, and the index artefact size is manageable for the content volumes
      in this ecosystem.


      - **Keep Astro-based resolver alongside index-based.** Rejected — two
      resolvers with different APIs and capabilities is the current problem. The
      index-based resolver subsumes the Astro-based one and works in both
      contexts.


      - **Store index in memory only (no artefact).** Rejected — a written
      artefact enables debugging, transparency, and cross-tool inspection. The
      `GENERATED` marker and gitignore keep it from polluting the repository.
    file: docs/rfcs/archive/implemented/rfc-0527-content-reference-index.md
  - kind: rejected-alternative
    rfcId: RFC-0528
    title: Embed metadata into derived media files from semantic profiles and
      material credits
    status: implemented
    alternativesText: >-
      - **Embed during encoding (pass metadata to ffmpeg/sharp).** Rejected —
      this couples metadata embedding to the encoding pipeline, requiring every
      encoder to know about credits and profiles. Post-hoc embedding via
      exiftool is simpler, tool-agnostic, and works uniformly for all media
      types.


      - **Post-build embedding (after Astro build, in dist/).** Rejected —
      embedding in `public/` before Astro build means metadata is preserved
      through the copy to `dist/`. Post-build embedding requires an additional
      pipeline step and risks missing files that Astro hashes or renames.


      - **ffmpeg `-metadata` for video, exiftool for images.** Rejected — using
      exiftool for both is simpler and ensures consistent metadata format across
      media types. ffmpeg metadata tags are container-specific (MP4 vs WebM) and
      less standardized than IPTC/XMP.


      - **Skip files without credits.** Rejected — every derived file should
      carry at least organizational copyright. The SemanticSiteProfile fallback
      ensures universal coverage without requiring a sidecar for every asset.
    file: docs/rfcs/archive/implemented/rfc-0528-embed-metadata-into-derived-media-files.md
  - kind: rejected-alternative
    rfcId: RFC-0529
    title: Migrate all content references to braceless syntax and remove legacy
      resolver
    status: implemented
    alternativesText: >-
      - **Deprecate brace syntax gradually (support both).** Rejected — platform
      policy (RFC-0478) prohibits backward compatibility for layers A and B. Two
      syntaxes in parallel creates confusion and doubles the test surface.


      - **Manual migration (no command).** Rejected — content files are numerous
      across multiple sites. A deterministic migration command ensures
      completeness and idempotency.


      - **Keep filesystem-based resolver as fallback.** Rejected — the
      index-based resolver subsumes all use cases. A fallback resolver creates a
      second code path that can diverge.
    file: docs/rfcs/archive/implemented/rfc-0529-migrate-content-references-to-braceless-syntax.md
  - kind: rejected-alternative
    rfcId: RFC-0530
    title: Add external identifiers to Business, Brand, and LegalIdentity and
      project to JSON-LD sameAs
    status: implemented
    alternativesText: >-
      - **Use `sameAs: string[]` on Business/Brand/LegalIdentity instead of
      `externalIdentifiers`.** Rejected — `externalIdentifiers` with structured
      `PbpExternalIdentifier` (schemeRef + value) preserves the scheme metadata
      (e.g. `https://www.wikidata.org/wiki/` + `Q123456`), enabling future
      validators to check scheme validity and distinguish Wikidata from
      Crunchbase. A flat `string[]` loses this structure. Consistency with
      Product (which already uses `externalIdentifiers`) is also a factor.


      - **Add a boolean `sameAs` flag to `PbpExternalIdentifier` to control
      projection.** Rejected — adds schema complexity for a distinction that is
      already encoded by entity type. Product `externalIdentifiers` are catalog
      identifiers (GTIN/MPN) and are not projected to JSON-LD `sameAs` because
      Product entities do not produce Organization nodes.
      Business/Brand/LegalIdentity `externalIdentifiers` are entity-equivalence
      links and are projected because they produce the Organization node. The
      entity type already determines projection behavior.


      - **Add `sameAs` to `LocalBusiness` in `jsonld-types.yaml` instead of
      declaring `Organization` type.** Rejected — `buildOrganizationNode` emits
      `@type: ["Organization", "ProfessionalService"]`, not `LocalBusiness`. The
      C-contract must match the actual emitted types. Adding `sameAs` to
      `LocalBusiness` would not validate the `Organization` node.
    file: docs/rfcs/archive/implemented/rfc-0530-add-external-identifiers-to-business-brand-and-legalidentity-and-project-to-json-ld-sameas.md
  - kind: rejected-alternative
    rfcId: RFC-0531
    title: Wikidata readiness validation command
    status: implemented
    alternativesText: >-
      - **Extend `jsonld.parity` instead of creating a new command.** Rejected —
      `jsonld.parity` checks that declared socials/logos appear in JSON-LD.
      Wikidata validation has different concerns: QID presence, URL construction
      from `schemeRef + value`, LegalIdentity readiness. Mixing them would
      overload `jsonld.parity` and make rule IDs ambiguous.


      - **Add to `build.check` pipeline immediately.** Rejected — Wikidata
      integration is not yet active. Adding to `build.check` would produce
      warnings (or errors in strict mode) on every build for all sites,
      including those not preparing for Wikidata. Standalone command gives
      operators control over when to check.


      - **Workspace-scoped command.** Rejected — PBP content is per-site. The
      command needs to read `<app>/src/content/business-profile/**/*.md` and
      `<app>/dist/client/**/*.html`, which are app-specific paths. App scope
      with `supportsAllSites: true` is the correct pattern.
    file: docs/rfcs/archive/implemented/rfc-0531-wikidata-readiness-validation-command.md
  - kind: rejected-alternative
    rfcId: RFC-0532
    title: Modernize onboarding for Sternsystem architecture
    status: implemented
    alternativesText: >-
      1. **Rewrite workflows under Sternsystem (6 phases)** — keep the 6-phase
      workflow structure but update commands and paths. Rejected: the workflow
      phases for authoring and auditing overlap with the mission lifecycle,
      creating confusion. Onboarding should end at Sternsystem registration +
      first mission; authoring and auditing are mission work.


      2. **Forge skill only (no new commands)** — encapsulate everything in
      `fo-onboard` without adding `onboarding.synthesize` or
      `sternsystem.register`. Rejected: deterministic operations (input hashing,
      registry mutation, pin creation, mission opening) belong in commands, not
      skills. Skills are for orchestration and AI synthesis; commands are for
      deterministic, testable, pipeline-integrable operations.


      3. **Mission-integrated onboarding** — a single `mission.onboard` command
      that accepts raw data, does synthesis, registers the Sternsystem, and
      materializes content. Rejected: violates separation of concerns. Synthesis
      (AI-driven, non-deterministic) and registration (deterministic, mutates
      registry) are fundamentally different operations. Combining them makes
      testing and debugging harder.


      4. **Keep global onboarding/.input/ (single active onboarding)** —
      preserve the current global directory but update commands for Sternsystem.
      Rejected: prevents parallel onboarding processes. The per-system namespace
      (`onboarding/<system-id>/`) is more flexible and aligns with the
      Sternsystem-per-site architecture.


      5. **Migrate existing onboarding content to onboarding/warpgogol-com/** —
      move the 95 files as a historical archive. Rejected: the onboarding is
      complete, the Sternsystem is registered, and the raw materials serve no
      future purpose. Archiving adds repository weight without value.

      6. **Create a separate compound command (e.g. `onboarding.register`)** —
      instead of extending `sternsystem.register`, create a new command that
      orchestrates registry + pin + mission. Rejected: `sternsystem.register`
      already exists in `@gogol/site-kernel-handoff` (RFC-0354) and is the
      natural entry point for Sternsystem creation. Creating a parallel command
      would violate the one-command-per-concern principle and create naming
      confusion. Extending the existing command with new flags is the
      forward-only path.
    file: docs/rfcs/archive/implemented/rfc-0532-modernize-onboarding-for-sternsystem-architecture.md
  - kind: rejected-alternative
    rfcId: RFC-0533
    title: Ecosystem commit command with mandatory version bump and pre-commit hook
    status: implemented
    alternativesText: >-
      - **`--bump` flag on `ecosystem.commit`** — allow the operator to override
      the bump type (`--bump minor`) without an RFC. Rejected because it creates
      a loophole around RFC-0479's migrator requirement: `minor` = Breaks-B =
      migrator required. Without an RFC, there is no migrator. Bump type must
      come from RFC frontmatter or default to `patch`.


      - **Husky for hook management** — use the `husky` npm package instead of
      `core.hooksPath`. Rejected because it adds a runtime dependency for a
      single hook file. `core.hooksPath hooks/` is a one-time git config with no
      dependency and the `hooks/` directory already exists in the repository
      (`hooks/setup-worktree.sh`).


      - **CI-only enforcement (no local hook)** — rely solely on PC-04 in CI to
      catch bypasses. Rejected because the operator explicitly requires local
      feedback to prevent errors before they reach CI. The feedback loop of CI
      is minutes; the hook is seconds.


      - **Env var only (no trailer)** — use `ECOSYSTEM_COMMIT` env var as the
      sole marker, skip commit-message trailers. Rejected because env vars are
      transient and leave no audit trail in git history. Trailers
      (`X-Platform-Bump`, `X-Platform-Version`, `X-RFC`) provide permanent
      traceability. CI (PC-04) checks trailers, not env vars.


      - **Trailer only (no env var)** — check `X-Platform-Bump` trailer in the
      pre-commit hook by reading `.git/COMMIT_EDITMSG`. Rejected because
      `ecosystem.commit` passes the message via `-m`, which may not populate
      `COMMIT_EDITMSG` reliably. The env var is a simpler, more reliable
      transient gate for the hook; the trailer is the permanent record for CI.


      - **Single RFC (RFC-0533 covers both core mechanism and setup
      automation)** — rejected in favor of two RFCs. The core mechanism (command
      + hook + PC-04) is `versionBump: patch` (platform infrastructure addition,
      no data contract break). Setup automation (skill + onboarding + docs) is
      also `versionBump: patch` (developer experience). Splitting allows
      independent implementation and release.
    file: docs/rfcs/archive/implemented/rfc-0533-ecosystem-commit-command-with-mandatory-version-bump-and-pre-commit-hook.md
  - kind: rejected-alternative
    rfcId: RFC-0534
    title: Setup ecosystem skill and onboarding hook configuration
    status: implemented
    alternativesText: >-
      - **Documentation-only (no skill)** — add a README section explaining `git
      config core.hooksPath hooks/` and `ECOSYSTEM_COMMIT`. Rejected because the
      operator prefers automation through skills over manual documentation. A
      skill is invocable by AI agents and ensures reproducible setup.


      - **Separate `setup-hooks` skill** — narrow skill only for git hooks, with
      `setup-ecosystem` as a future umbrella. Rejected in favor of a single
      `setup-ecosystem` skill that covers hooks, env documentation, and command
      verification. One skill is simpler to discover and invoke.


      - **Husky for hook management** — rejected for the same reasons as in
      RFC-0533: adds a dependency for a single hook file. `core.hooksPath` is
      dependency-free.
    file: docs/rfcs/archive/implemented/rfc-0534-setup-ecosystem-skill-and-onboarding-hook-configuration.md
  - kind: rejected-alternative
    rfcId: RFC-0535
    title: Extend wikidata.validate with Claim and EvidenceSource coverage checks
    status: implemented
    alternativesText: >-
      - **Create a separate `wikidata.evidence.validate` command.** Rejected —
      the new rules are tightly coupled to the existing QID presence check.
      Splitting them into a separate command forces operators to run two
      commands for a single readiness check. The QID gate (Business has QID →
      check evidence) is cleaner within one command.


      - **Check all claimClass types, not just factual.** Rejected —
      comparative, benefit, risk, and limitation claims are commercial content,
      not Wikidata statement candidates. Checking them would produce false
      positives on marketing claims that do not need evidence for Wikidata
      purposes. Only `claimClass: "factual"` claims map to Wikidata statements
      (founding date, industry, country, legal form).


      - **Enforce a minimum source count (e.g. 2+ independent sources).**
      Rejected — Wikidata's notability threshold is contextual and softer than a
      hard count. A single high-quality third-party-registry source (e.g.
      Handelsregister) may be sufficient. A warning for zero sources is
      appropriate; a hard count threshold would produce false positives.


      - **Add to `build.check` pipeline.** Rejected — matches RFC-0531's design
      decision. Wikidata integration is not yet active for all sites. Adding to
      `build.check` would produce warnings (or errors in strict mode) on every
      build for sites not preparing for Wikidata. Standalone command gives
      operators control.


      - **Use PBP compiler graph instead of file-system reader.** Rejected — the
      PBP compiler (`compilePbpProfile`) requires Astro content collection
      infrastructure and is not available in a static validation context.
      `wikidata.validate` is a static file-system validator (like the existing
      `readPbpEntity` pattern). A new `readPbpRepeatables` helper follows the
      same file-system-only approach.
    file: docs/rfcs/archive/implemented/rfc-0535-extend-wikidata-validate-with-claim-and-evidencesource-coverage-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0537
    title: Session documentation domain with save, archive, validate, and list
      commands
    status: implemented
    alternativesText: >-
      - **Summary-only saving (no raw export).** Rejected: the operator
      explicitly wants full session transcripts, not just summaries. Raw ATIF
      exports preserve the complete conversation for future reference and audit.


      - **Save to OS temp directory (like `fo-handoff`).** Rejected: sessions
      should be part of project documentation, committed to the repo, and
      searchable. Temp directory files are ephemeral and not shared across team
      members.


      - **Command-only approach (no skill).** Rejected: deterministic conversion
      alone produces structured but unannotated files. The `fo-session-save`
      skill adds semantic value — summaries, decision extraction, related
      artifact identification, quality checks — that a command cannot do without
      an LLM. The hybrid approach (command for deterministic, skill for
      intelligent) is the proven pattern in this codebase.


      - **Skill-only approach (no command).** Rejected: ATIF parsing,
      regex-based metadata extraction, and file I/O are deterministic tasks that
      belong in a command. Skills should handle intellectual tasks, not
      mechanical parsing. Mixing both in a skill makes it harder to test and
      debug.


      - **API-based export instead of CLI `--export`.** Deferred to future work:
      the Devin API `List session messages` endpoint provides programmatic
      access, but requires authentication setup and API client code. CLI
      `--export` is simpler and available now. The RFC mentions API-based export
      as future work.


      - **`session.search` command.** Deferred: full-text search across sessions
      is useful but not essential for initial scope. `session.list --rfc`
      provides RFC-based filtering, which is sufficient for the primary use
      case.


      - **Single `types` field (string) instead of array.** Rejected: a single
      session can involve multiple activities (e.g., grilling followed by
      implementation). An array of types accurately represents this, and the
      skill can override or refine auto-detected types.
    file: docs/rfcs/archive/implemented/rfc-0537-session-documentation-domain-with-save-archive-validate-and-list-commands.md
  - kind: rejected-alternative
    rfcId: RFC-0538
    title: Compass annotate Forge skill with full lifecycle, forge.yaml bindings,
      and kernel command cleanup
    status: implemented
    alternativesText: >-
      1. **Integrate external `@syrokomskyi/code-compass` package** — Rejected.
      Uses OpenAI API directly (requires API key), conflicts with RFC-0350's
      retirement of LLM from kernel commands, and adds external dependency.
      Forge skill is self-contained and uses the agent's built-in LLM.


      2. **Initial generation only (no lifecycle)** — Rejected. Operator
      explicitly requires "updatable" annotations. Without lifecycle management,
      headers drift from code.


      3. **Keep `compass.annotate` skeleton + skill fills TODO** — Rejected.
      Skill replaces skeleton entirely (grilling Q6). Skeleton is a placeholder,
      not structure. Keeping `compass.annotate` creates dual responsibility.


      4. **`compass.summary.trim` as separate skill** — Rejected. Cleanup is
      deterministic (trim to 30, preserve RFC/ADR items), not LLM work. Belongs
      in kernel.


      5. **File extensions in `PREFERENCES.md`** — Rejected. Preferences are
      operator behavior settings, not project topology. `forge.yaml` bindings
      are the correct mechanism for project-specific configuration.


      6. **Test file annotation** — Rejected. Test files are self-documenting
      via describe/it. Headers add noise without value.


      7. **Keep `compass.changesummary.tidy` name, only raise cap** — Rejected.
      The name `changesummary.tidy` implies a narrow focus on boilerplate
      removal. The new name `summary.trim` better reflects the broader lifecycle
      role (cap management + boilerplate removal) and aligns with the skill's
      batch-end cleanup step. The rename is forward-only — the old command is
      removed, the new one is added. Churn in fix hints, docs, and COMMANDS.md
      is accepted as part of the forward-only policy.


      8. **Keep cap at 3 unprotected items, only rename** — Rejected. The
      3-unprotected cap was too aggressive for active development — files
      accumulating legitimate change entries would be trimmed prematurely. The
      30-total cap preserves more history while still bounding block size.
    file: docs/rfcs/archive/implemented/rfc-0538-compass-annotate-skill-with-full-lifecycle-and-kernel-cleanup.md
  - kind: rejected-alternative
    rfcId: RFC-0539
    title: Portable skill registry and forge-managed project skill packs
    status: implemented
    alternativesText: >-
      - **`portability: universal | ecosystem` field on `ForgeSkillEntry`** —
      keeps one registry but ships ecosystem skill files to npm anyway (they
      live in `packages/forge/skills/`), and blurs the boundary ADR-0003 drew.
      Rejected: the package boundary is the honest portability criterion.

      - **Leave pack skills unmanaged (ADR-0003 status quo)** — rejected: two
      sync mechanisms and zero validation is accepted debt that becomes a
      support burden the moment external projects copy the pattern.

      - **Publish a separate `@wgogol/wgogol-skills` npm package** — rejected:
      ecosystem skills are meaningless outside this monorepo; `private: true`
      stands.
    file: docs/rfcs/archive/implemented/rfc-0539-portable-skill-registry-and-forge-managed-project-skill-packs.md
  - kind: rejected-alternative
    rfcId: RFC-0540
    title: Autonomous-mode binding defaults in forge.init
    status: implemented
    alternativesText: >-
      - **Stack-profile-driven defaults for all bindings** — fuller, but works
      only for supported stacks and couples this RFC to project scaffolding; the
      operator chose CLI-backed defaults with bootstrap-dialogue for stack
      commands.

      - **Resolve forge commands at runtime instead of writing them into
      forge.yaml** (skills detect "forge provides this" and bypass bindings) —
      rejected: creates a second resolution path, breaks the single-seam
      principle of DNA-54, and hides the effective command from the operator.

      - **Keep null defaults, document manual setup** — rejected: the first-run
      experience of the npm package is the product; documentation does not fix a
      broken default.
    file: docs/rfcs/archive/implemented/rfc-0540-autonomous-mode-binding-defaults-in-forge-init.md
  - kind: rejected-alternative
    rfcId: RFC-0542
    title: Self-documenting forge command output contract
    status: implemented
    alternativesText: >-
      - **Lifecycle-commands-only scope** — rejected: the operator chose
      structural coverage of all commands. Validators at `fail` benefit from a
      next step ("fix and re-run"), and the `--json` `nextSteps` field is useful
      to agents consuming any command.

      - **Interactive TUI wizard for init/create** — rejected: the operator
      specified text output with flags; the bootstrap skill handles dialogue.

      - **Localize CLI output via PREFERENCES.md** — rejected: `aiLanguage`
      governs agent chat, not the binary. Localizing the CLI would fragment the
      npm experience across operator locales and complicate programmatic
      consumption.
    file: docs/rfcs/archive/implemented/rfc-0542-self-documenting-forge-command-output-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0543
    title: NPM publication and consumer upgrade contract
    status: implemented
    alternativesText: >-
      - **No upgrade command; consumers run `npm update && forge init`** —
      rejected: `forge.init` skips existing `forge.yaml` (RFC-0391), so new
      binding defaults never reach consumers; skill copies drift silently.

      - **Full `forge.yaml` rewrite on upgrade** — rejected: overwrites operator
      customizations; the operator chose additive sync.

      - **Dedicated `@wgogol/forge-upgrade` package** — rejected: the upgrade
      logic is tightly coupled to `FORGE_CLI_BINDING_DEFAULTS` and the skill
      registry; a separate package would duplicate both.
    file: docs/rfcs/archive/implemented/rfc-0543-npm-publication-and-consumer-upgrade-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0544
    title: forge create command — single entry point for project scaffolding
    status: implemented
    alternativesText: >-
      - **Make `forge.init` create the directory** — rejected: `init` is the
      config-writing primitive; overloading it with directory creation changes
      its contract and breaks operators who run `init` in an existing directory.

      - **Interactive prompts for profile and package manager** — rejected: the
      operator specified non-interactive `forge.create` with flags; the
      bootstrap skill handles dialogue.

      - **Rollback on partial failure** — rejected: adds complexity for a rare
      case; leaving partial files lets the operator inspect what went wrong and
      re-run in a clean directory.
    file: docs/rfcs/archive/implemented/rfc-0544-forge-create-command-single-entry-point-for-project-scaffolding.md
  - kind: rejected-alternative
    rfcId: RFC-0545
    title: Forge bootstrap skill redesign — greenfield and transplant modes
    status: implemented
    alternativesText: >-
      - **Unified flow with auto-detection** — rejected: the operator chose
      explicit mode choice at the start. Auto-detection risks misclassifying a
      greenfield project that happens to have a `package.json` from a previous
      experiment.

      - **Make `forge.create` run the interview** — rejected: `forge.create` is
      non-interactive (RFC-0544); the skill is the interactive layer.

      - **Copy source code into the forge project during transplant** —
      rejected: transplant is about governance structure, not code migration.
      The operator can copy code themselves if they want; the skill does not
      make that decision.
    file: docs/rfcs/archive/implemented/rfc-0545-forge-bootstrap-skill-redesign-greenfield-and-transplant-modes.md
  - kind: rejected-alternative
    rfcId: RFC-0546
    title: Deprecate forge.init standalone CLI and add migration-adapter registry to
      forge-bootstrap transplant
    status: implemented
    alternativesText: >-
      - **Keep `forge init` but add non-empty-directory guard** — rejected:
      `forge init` in an existing project still silently skips files and
      overwrites skills. The guard would make it refuse in the exact scenario it
      was designed for. The operator's decision is to remove the standalone path
      entirely.

      - **Universal migrator with configurable rules (no adapters)** — rejected:
      a single universal migrator cannot know how to derive bindings for
      different stacks (Node/TS vs Phaser vs Python). The adapter architecture
      allows each stack to encapsulate its own detection, analysis, and
      migration logic. The registry is extensible for future stacks without
      modifying a monolithic migrator.

      - **External npm packages for adapters** — rejected for initial shipment:
      adds package management complexity for the first two adapters. Built-in
      adapters ship with `@warpgogol/forge`; the `migrationAdapters` config
      field in `forge.yaml` supports external adapters via `module:` for future
      expansion.

      - **Make `forge.create` interactive with migration** — rejected:
      `forge.create` is non-interactive (RFC-0544). The migration interview
      belongs in the `forge-bootstrap` skill, which is the interactive layer.
    file: docs/rfcs/archive/implemented/rfc-0546-deprecate-forge-init-standalone-cli-and-add-migration-adapter-registry-to-forge-bootstrap-transplant.md
  - kind: rejected-alternative
    rfcId: RFC-0547
    title: "Barrier-free onboarding: register selection, welcoming report,
      auto-doctor, auto-ADR, first creation moment, and forge-as-devDependency"
    status: implemented
    alternativesText: >-
      - **Keep CLI commands in report but add explanations** — rejected: the
      operator's vision is that non-programmers should never see CLI commands.
      Explanations of `pnpm exec forge doctor` do not help someone who does not
      use a terminal. The LLM agent handles command invocation.

      - **Separate RFC for forge-as-devDependency** — rejected: the
      devDependency change is required for auto-doctor to work inside new
      projects. Without it, auto-doctor would fail. The four changes (UX,
      auto-doctor, auto-ADR, devDependency) form a single coherent onboarding
      experience.

      - **Auto-create RFC instead of ADR** — rejected: RFCs require human
      architecture review and govern cross-workspace contracts. ADRs are local
      decisions that can be created autonomously. Adopting Forge — whether
      greenfield or transplant — is a local project decision.

      - **Ask about RFC/ADR during onboarding** — rejected: the operator's
      vision is that onboarding should lead directly to creating, not to
      learning governance. ADRs and RFCs become visible naturally during work,
      when the operator encounters them in context. Educational detours delay
      the moment the operator can start building.

      - **Technical recommendations for transplant** — rejected: recommendations
      must be creator-facing, not programmer-facing. A recommendation like
      "refactor the auth module" means nothing to a non-programmer. A
      recommendation like "add a page that tells your project's story to
      visitors" means everything.

      - **Copy `.git` directory directly** — rejected: copying `.git` transfers
      the entire repository including remote configuration, hooks, and stashes.
      `git format-patch` + `git am` transfers only commit history, which is what
      the operator needs.

      - **Make forge-bootstrap fully non-interactive** — rejected: the skill
      still needs to ask about language, register, mode, stack, and git history.
      The vision is not to remove interaction, but to make it human and
      jargon-free.

      - **Collect personal data as a separate interview before creation** —
      rejected: this contradicts the barrier-free philosophy. Personal data
      (project story, deep purpose, creative influences, audience, writing
      voice) is collected organically during the first creation moment, not as a
      questionnaire. Only name and gender are asked before creation (needed for
      addressing and grammar).

      - **Binary register without future extensibility** — rejected: the
      register parameter is stored as a string in `PREFERENCES.md`, not as a
      boolean. This allows future registers (e.g. educational, research) without
      schema changes.
    file: docs/rfcs/archive/implemented/rfc-0547-barrier-free-onboarding-welcoming-report-auto-doctor-auto-adr-and-forge-as-devdependency.md
  - kind: rejected-alternative
    rfcId: RFC-0548
    title: "Agent core behavioral layer: intent-to-skill routing,
      auto-documentation, creator-facing communication, and safety net"
    status: implemented
    alternativesText: >-
      - **Put behavioral layer in SKILL.md files instead of AGENTS.md** —
      rejected: the agent reads AGENTS.md as its primary instruction file.
      Skills are discovered but not automatically read. Putting routing in
      AGENTS.md ensures the agent sees it first.

      - **Make intent routing a separate config file** — rejected: adding
      another file increases complexity. AGENTS.md is already the agent's entry
      point; adding the behavioral section there keeps everything in one place.

      - **Hardcode skill triggers in the generator** — rejected: the routing
      table should be generated from skill descriptions, not hardcoded. This
      way, new skills automatically appear in the routing table.

      - **Make auto-grilling mandatory for all changes** — rejected: typo fixes
      and minor edits do not need grilling. The agent uses judgment, calibrated
      by examples in the behavioral layer and refined by `operator-profile.md`.

      - **Ask operator before auto-saving sessions** — rejected: session saving
      is the default (PREFERENCES.md `saveSessions: true`). Asking adds
      friction. The operator can opt out.

      - **Generate AGENTS.md in init.ts instead of create.ts** — rejected:
      `init.ts` is responsible for file deployment, not content generation.
      `agents-generate.ts` is the dedicated generator. `create.ts` orchestrates:
      scaffold → init → agents.generate.

      - **Static behavioral layer without adaptive learning** — rejected: the
      operator's vision is that the system understands them better over time. A
      static layer treats every operator the same. The `operator-profile.md`
      mechanism reuses the proven `knowledge` file pattern from
      `learned-principles.md`.

      - **Use a database for operator profile instead of Markdown** — rejected:
      Markdown files are human-readable, git-trackable, and consistent with the
      existing `knowledge` file pattern. A database adds infrastructure
      complexity for no benefit at this scale.

      - **Proactive guidance as a separate skill** — rejected: guidance is not a
      separate action the operator invokes. It is an ongoing behavior the agent
      exhibits during normal work. It belongs in the behavioral layer, not as a
      separate skill.

      - **Operator feedback only via session retrospective** — rejected: the
      operator's feedback is most valuable when applied immediately. Waiting
      until the end of the session means the operator experiences the wrong
      behavior for the rest of the session. The live feedback channel applies
      changes now.

      - **Single behavioral layer for all operators** — rejected: the two target
      user groups (web studio and game developer) have fundamentally different
      needs. A studio needs efficient, professional communication; a game
      developer needs creative partnership and emotional support. The register
      parameter allows the system to serve both without forcing one style on the
      other. The core/extended split (RFC-0548/RFC-0549) keeps the core layer
      universal and the extended layer opt-in.

      - **Legal/compliance pushback as part of purpose-drift** — rejected: legal
      and compliance issues are fundamentally different from creative purpose
      drift. Purpose drift is a soft suggestion (the operator can override
      without confirmation). Legal/compliance is a hard refusal (the operator
      must explicitly confirm they accept the risk). Mixing them would either
      make purpose drift too rigid or legal pushback too soft.

      - **Engagement optimization as a goal** — rejected: the system helps the
      operator build something lasting, not consume content. Inspiration feed,
      progress celebration, and companion mode exist to serve the operator's
      creative process, not to maximize time spent in the system. Added as a
      non-goal.

      - **Push notifications for news/inspiration** — rejected for MVP: push
      requires an external scheduler + notification channel, which is itself a
      connectable capability. MVP is pull-only (checked at session start). Push
      is a future enhancement after connectable capabilities are established.
    file: docs/rfcs/archive/implemented/rfc-0548-agent-core-behavioral-layer-intent-to-skill-routing-auto-documentation-creator-facing-communication-and-safety-net.md
  - kind: rejected-alternative
    rfcId: RFC-0549
    title: "Agent extended behavioral layer: personal connection, creative
      partnership, emotional rhythm, and companion mode"
    status: implemented
    alternativesText: >-
      - **Keep all behaviors in RFC-0548 (no split)** — rejected: the two target
      user groups have fundamentally different needs. A single layer either
      over-serves the studio or under-serves the game developer. The register
      parameter allows the system to serve both.

      - **Make extended layer a separate skill** — rejected: the extended layer
      is not a separate action the operator invokes. It is ongoing behavior the
      agent exhibits during normal work. It belongs in the behavioral layer,
      conditionally included.

      - **Declarative mood sensing ("you seem tired")** — rejected: declarative
      mood sensing creates false authority and risks misreading the operator.
      Questions respect the operator's agency and give them control over what to
      share.

      - **Effort-based praise ("great effort!")** — rejected: effort-based
      praise feels patronizing and destroys trust. Outcome-based praise ("this
      design works well because...") builds genuine confidence.

      - **Permanent emotional observations** — rejected: psychological
      observations may become inaccurate over time. Entry expiry (90 days,
      refreshable) prevents century-scale durability from being applied to
      transient emotional states.

      - **Push notifications for inspiration feed** — rejected for MVP: push
      requires external scheduler + notification channel. MVP is pull-only
      (checked at session start). Push is a future enhancement.

      - **Engagement optimization as a goal** — rejected: the system helps the
      operator build something lasting, not consume content. Inspiration feed
      and companion mode serve the creative process, not engagement metrics.
    file: docs/rfcs/archive/implemented/rfc-0549-agent-extended-behavioral-layer-personal-connection-creative-partnership-emotional-rhythm-and-companion-mode.md
  - kind: rejected-alternative
    rfcId: RFC-0550
    title: Post-transplant build verification and error repair in forge-bootstrap
    status: implemented
    alternativesText: >-
      1. **Separate `forge.build.check` CLI command** — rejected because the
      operator should not need to run commands. Build verification is part of
      the onboarding experience, not a standalone tool.


      2. **Silent auto-fix without asking** — rejected because some build errors
      may be intentional (e.g. the operator knows about a missing dependency and
      plans to handle it differently). The operator should decide whether Forge
      touches their code.


      3. **Block onboarding on build errors** — rejected because the operator
      may have pre-existing errors in their source project that are not caused
      by the migration. Blocking would trap them in onboarding. Instead, report
      and offer to fix.
    file: docs/rfcs/archive/implemented/rfc-0550-post-transplant-build-verification-and-error-repair-in-forge-bootstrap.md
  - kind: rejected-alternative
    rfcId: RFC-0551
    title: "Creative register behavioral improvements: capability showcase,
      always-next-step, and auto-commit"
    status: implemented
    alternativesText: >-
      1. **Same capabilities for both registers** — rejected because creative
      and business operators have different priorities. A creative operator
      wants to hear about idea capture and creative flow; a business operator
      wants efficiency and quality checks.


      2. **Always-next-step in both registers** — rejected because business
      operators may prefer to work at their own pace without unsolicited
      suggestions. The creative register benefits from continuous momentum; the
      business register benefits from operator control.


      3. **Auto-commit in both registers** — rejected because business operators
      may want to review changes before committing. Creative operators
      prioritize flow over git mechanics.
    file: docs/rfcs/archive/implemented/rfc-0551-creative-register-behavioral-improvements-capability-showcase-always-next-step-and-auto-commit.md
  - kind: rejected-alternative
    rfcId: RFC-0552
    title: "Forge bootstrap hardening: git init for greenfield, commit synced
      skills, and report skill name conflicts"
    status: implemented
    alternativesText: >-
      1. **Merge pack skills and Forge skills on conflict** — rejected because
      merging skill content is complex and error-prone. Forge skills are
      maintained and versioned; pack skills may be outdated or incompatible.
      Priority approach is simpler and safer.


      2. **Backup overwritten skills before overwriting** — rejected because it
      creates clutter and the operator may not know where to find the backup.
      Reporting which skills were skipped is clearer.


      3. **Ask the operator on each conflict** — rejected because it interrupts
      the onboarding flow with technical questions. The operator can rename and
      restore skills after onboarding if needed.


      4. **Run git init in `forge.create` instead of `forge-bootstrap`** —
      rejected because `forge.create` is non-interactive (RFC-0544). Git init is
      an interactive-layer decision (the operator may decline git).
      `forge-bootstrap` is the interactive layer.
    file: docs/rfcs/archive/implemented/rfc-0552-transplant-git-and-skill-sync-hardening-ensure-git-init-commit-synced-skills-and-report-name-conflicts.md
  - kind: rejected-alternative
    rfcId: RFC-0553
    title: Remove internal platform RFC/ADR references and WGogol name from Forge
      skills
    status: implemented
    alternativesText: >-
      1. **Soft warnings instead of hard failure** — rejected because internal
      platform references in consumer-facing skills are a quality issue, not a
      style preference. Hard failure ensures cleanup.


      2. **Prohibit all "RFC"/"ADR" terms** — rejected because forge IS an
      RFC/ADR governance framework. The terms "RFC" and "ADR" are forge's domain
      vocabulary, not internal platform references. Prohibiting them would make
      skill files meaningless (e.g., "RFC Audit" skill cannot avoid the word
      "RFC").


      3. **Regex-based automated replacement** — rejected as the sole mechanism
      because context matters. Specific RFC references in different contexts
      need different replacements (generic description, removal, or rewording).
      Manual cleanup with validation enforcement is more reliable.
    file: docs/rfcs/archive/implemented/rfc-0553-remove-internal-platform-references-from-forge-skills-rfc-adr-and-wgogol-name-prohibition.md
  - kind: rejected-alternative
    rfcId: RFC-0554
    title: "Silent migration on Forge upgrade: detect version change and
      auto-migrate without operator intervention"
    status: implemented
    alternativesText: >-
      1. **Prompt the operator to run `forge upgrade`** — rejected because the
      operator should not need to know about migration or CLI commands. Forge
      handles it silently.


      2. **Separate `forge.migrate` CLI command** — rejected because
      `forge.upgrade` already exists (RFC-0543) and does exactly this. A second
      command duplicates the mechanism.


      3. **Migration only on `forge-bootstrap` re-run without version check** —
      rejected because the operator may not know to re-run `forge-bootstrap`.
      Automatic version detection is more reliable.


      4. **Remove `forge.upgrade` CLI, use only silent check** — rejected
      because the CLI provides a manual recovery path for operators who want to
      force a sync. Two entry points sharing one mechanism (`runUpgrade`) is not
      a dual-path.


      5. **Run version check on every fo-skill invocation** — rejected for
      performance. `forge-bootstrap` is the natural entry point (it already
      reads `forge.yaml`); other skills should not incur upgrade-check overhead.


      6. **Reuse transplant migration adapters (RFC-0546) for upgrade
      migration** — rejected. Transplant adapters are source-directory-oriented
      (`detect(sourceDir)`, `migrate(sourceDir, targetDir)`). Upgrade migration
      syncs skills and binding defaults — a fundamentally different operation
      already handled by `forge.upgrade`.
    file: docs/rfcs/archive/implemented/rfc-0554-silent-migration-on-forge-upgrade-detect-version-change-and-auto-migrate-without-operator-intervention.md
  - kind: rejected-alternative
    rfcId: RFC-0555
    title: "Studio Gate: MCP server for site owner content editing with mission
      lifecycle"
    status: implemented
    alternativesText: >-
      1. **Extend agent-gate (RFC-0290) with stdio transport** — rejected
      because agent-gate is HTTP/JSON-RPC, Astro-integrated, for public-facing
      capability dispatch. Mixing stdio MCP for content editing with HTTP MCP
      for capability dispatch in one package conflates two different concerns,
      security models, and transport mechanisms. Two packages is cleaner.


      2. **MCP tools only (not Site OS commands)** — rejected because
      `workpiece.read` and `workpiece.write` need DNA-22 path validation, which
      belongs in the kernel (alongside `client.edit.validate`). Making them
      MCP-only would move security enforcement into the MCP server, bypassing
      the kernel's validation layer. Site OS commands ensure the kernel is the
      single enforcement point.


      3. **Auto-commit in `workpiece.write`** — rejected because LLMs need
      control over commit grouping. Multiple file writes (e.g. updating a page
      and its associated section) should be one commit, not N commits. Separate
      `mission.git.commit` tool call gives the LLM this control.


      4. **Granular content commands (`content.page.update`,
      `content.section.reorder`, etc.)** — rejected because LLMs can directly
      manipulate structured content (Markdown/YAML) read from the workpiece. Two
      generic commands (`workpiece.read`, `workpiece.write`) are sufficient.
      Granular commands would proliferate the command surface unnecessarily.


      5. **Custom LLM testing client (`scripts/studio-chat.ts`)** — rejected
      because the operator can connect any MCP-capable LLM client (Devin,
      Cursor, Claude Desktop, GLM client) directly to the studio-gate MCP
      server. A custom client would duplicate existing MCP client functionality.
    file: docs/rfcs/archive/implemented/rfc-0555-studio-gate-mcp-server-for-site-owner-content-editing-with-mission-lifecycle.md
  - kind: rejected-alternative
    rfcId: RFC-0556
    title: Inline compass and werkstatt commands into forge for full autonomous mode
    status: implemented
    alternativesText: >-
      - **Separate `@warpgogol/compass-core` npm package:** Extract compass
      logic into a standalone package (based on the existing
      `@syrokomskyi/code-compass` from the `pipelines` repo). Rejected — adds
      package management overhead for ~800 lines of code. Inlining into forge is
      simpler and consistent with the werkstatt decision. The `code-compass`
      package remains available for non-forge consumers.


      - **Keep graceful skip, add `forge.doctor` warning:** Instead of inlining,
      add a diagnostic that warns when commands are skipped. Rejected — the
      operator explicitly wants all commands functional in external projects,
      not just a warning that they are missing.


      - **Conditional registration with feature flags:** Register commands but
      disable them via feature flags in `forge.yaml`. Rejected — adds complexity
      without solving the core problem. The commands need to work, not just be
      present-but-disabled.
    file: docs/rfcs/archive/implemented/rfc-0556-inline-compass-and-werkstatt-commands-into-forge-for-full-autonomous-mode.md
  - kind: rejected-alternative
    rfcId: RFC-0557
    title: Workspace dependency resolution validator for templates and workpieces
    status: implemented
    alternativesText: >-
      1. **Single command in `PACKAGES_CHECK_PIPELINE` only.** Rejected because
      `build.prepare` already crashes on unresolved imports in the workpiece —
      but the crash is cryptic (`ERR_MODULE_NOT_FOUND` with a deep stack trace)
      and surfaces late in the pipeline. A dedicated
      `workpiece.imports.validate` as the first step in
      `SITES_BUILD_PREPARE_PIPELINE` produces a clear diagnostic before any
      other work runs.


      2. **Scoped peer dependency check (parsing `peerDependencies` from
      `node_modules/.pnpm/`).** Rejected as too complex and fragile — pnpm's
      peer resolution logic is non-trivial and duplicating it would create
      maintenance burden. `pnpm install --frozen-lockfile` delegates to pnpm's
      own resolution, which is always correct by definition.


      3. **Pinning isomorphic packages (react/react-dom) to exact versions via
      validator.** Rejected as a convention, not an enforceable invariant — the
      choice between `^19.2.7` and `19.2.8` is a packaging decision, not an
      architectural one. The `pnpm install --frozen-lockfile` check catches the
      symptom (version mismatch at install time) without mandating a specific
      versioning policy.


      4. **Manual post-rename checklist.** Rejected because it relies on human
      discipline — the exact failure mode that caused the bug in the first
      place. The rename commit `eff9b03` did not include a checklist step for
      updating root devDependencies.
    file: docs/rfcs/archive/implemented/rfc-0557-workspace-dependency-resolution-validator-for-templates-and-workpieces.md
  - kind: rejected-alternative
    rfcId: RFC-0558
    title: "Werkstatt Identity Model: VC-based actor identity and site ownership"
    status: implemented
    alternativesText: >-
      1. **OAuth2 / OIDC provider.** Use an external identity provider (Auth0,
      Keycloak, self-hosted OIDC). Rejected for pilot: adds infrastructure
      dependency, external service to maintain, and does not align with the P2P
      architecture where no central authority exists. VC-based identity is
      self-sovereign and works offline.

      2. **API keys (static tokens).** Generate per-actor API keys, store in
      registry. Rejected: no cryptographic delegation, no expiry, no scopes. API
      keys are bearer tokens — if leaked, they grant full access until manually
      revoked. VCs carry signed delegation chains and scoped permissions.

      3. **did:web with DID resolution.** Full DID document resolution per
      `did:web` spec. Rejected for pilot: adds DNS dependency and HTTP
      resolution step. The pilot uses `did:web` as an identifier format but
      resolves keys from local `werkstatt.identity.json`, not from HTTPS DID
      endpoints. Full DID resolution is future work for P2P mode.

      4. **Reuse CosmicPassportCredential for identity.** Extend the existing
      passport VC type to carry identity claims. Rejected:
      `CosmicPassportCredential` is a build-provenance credential (systemHash +
      commitSha). Identity is a different concern with different fields (siteId,
      scopes, expiry). Mixing them creates a confusing schema and breaks the
      single-responsibility principle.
    file: docs/rfcs/archive/implemented/rfc-0558-werkstatt-identity-model-vc-based-actor-identity-and-site-ownership.md
  - kind: rejected-alternative
    rfcId: RFC-0559
    title: "Studio Gate Authentication: VC-based access control for MCP-mediated
      content editing"
    status: implemented
    alternativesText: >-
      1. **Auth at each tool level.** Add auth checks inside every
      `workpiece.read`, `workpiece.write`, etc. Rejected: duplicates auth logic
      across 12+ tools, harder to maintain, and the MCP protocol naturally
      supports middleware at the request handler level.

      2. **Auth via environment variable (API key).** Pass a static API key as
      an env var to Studio Gate. Rejected: no cryptographic identity, no site
      scoping, no delegation. Does not align with RFC-0558 VC model.

      3. **Auth via MCP `initialize` handshake.** Require credentials during the
      MCP `initialize` call and cache for the session. Rejected: MCP stdio
      transport has no persistent session — each `CallToolRequest` is
      independent. Auth per-call is simpler and stateless.

      4. **OAuth2 token validation.** Validate an OAuth2 bearer token against an
      external provider. Rejected: same reasons as RFC-0558 — external
      dependency, no offline operation, does not align with P2P architecture.
    file: docs/rfcs/archive/implemented/rfc-0559-studio-gate-authentication-vc-based-access-control-for-mcp-mediated-content-editing.md
  - kind: rejected-alternative
    rfcId: RFC-0560
    title: "Mission Actor Identity and Signed Commits: Cryptographic actor binding
      for mission lifecycle"
    status: implemented
    alternativesText: >-
      1. **GPG commit signing.** Use git's native GPG signing (`git commit -S`).
      Rejected: GPG is heavyweight, requires separate keyring, and is not
      aligned with the Ed25519 VC model. Ed25519 signatures in commit trailers
      are simpler and use the same keypair as VC credentials.

      2. **Signed git tags instead of commit signing.** Sign a tag at mission
      close rather than each commit. Rejected: loses per-commit actor binding. A
      mission may have many commits from different actors (operator + delegated
      LLM). Per-commit signing preserves the full audit trail.

      3. **Actor identity in commit message only.** Put actor id in commit
      message without cryptographic signature. Rejected: no verification. Anyone
      can write any actor id in a commit message. Signature proves the commit
      was created by someone holding the private key.

      4. **Require actor from auth context only.** Remove `--actor` flag
      entirely. Rejected: breaks CLI direct access for operators who work
      without Studio Gate. The pilot needs both paths.
    file: docs/rfcs/archive/implemented/rfc-0560-mission-actor-identity-and-signed-commits-cryptographic-actor-binding-for-mission-lifecycle.md
  - kind: rejected-alternative
    rfcId: RFC-0561
    title: "Site Ownership in Sternsystem Registry: VC-linked owner field for fleet
      entries"
    status: implemented
    alternativesText: >-
      1. **Owner in pin file (system.pin.json).** Store owner in the pin file
      instead of the registry. Rejected: the pin file is a per-site artifact,
      not a fleet-level metadata source. The registry is the canonical fleet
      metadata. Owner is fleet-level information.

      2. **Owner as a separate ownership.yaml file.** Create a new
      `systems/ownership.yaml` mapping site ids to owner ids. Rejected:
      fragments ownership metadata across files. The registry already exists and
      is the natural place for fleet-level metadata.

      3. **Owner as a VC stored in the registry.** Store the full
      `SiteOwnershipCredential` VC in the registry entry instead of just the
      subject id. Rejected: the VC is a runtime artifact that may be revoked or
      re-issued. The registry should store the stable subject id, not the
      volatile VC. The VC is verified at runtime by Studio Gate.

      4. **Mandatory owner field.** Make `owner` required in the schema.
      Rejected: breaks all existing registry entries. The pilot needs backwards
      compatibility — existing sites without owner must remain valid during the
      transition period.
    file: docs/rfcs/archive/implemented/rfc-0561-site-ownership-in-sternsystem-registry-vc-linked-owner-field-for-fleet-entries.md
  - kind: rejected-alternative
    rfcId: RFC-0562
    title: "P2P Werkstatt Network Topology: 5-layer architecture for million-site
      scale"
    status: implemented
    alternativesText: >-
      1. **Centralized control plane at scale.** Keep a single control plane and
      scale it vertically (bigger VMs, more CPU, more RAM). Rejected: single
      point of failure. At million-site scale, a single control plane cannot
      handle the throughput, and a failure takes down the entire platform.


      2. **Sharded control plane (not P2P).** Shard the control plane across
      multiple VMs with a coordinator. Rejected: the coordinator is a single
      point of failure. Sharding adds complexity without eliminating the
      coordinator bottleneck. P2P eliminates the coordinator entirely.


      3. **Federated workshops with central registry.** Workshops are
      independent but share a central registry for site ownership and
      membership. Rejected: the central registry is a single point of failure.
      The DHT (Layer 3) distributes the registry across all workshops,
      eliminating the central registry.


      4. **Blockchain-based consensus.** Use a blockchain for membership,
      registry, and content placement. Rejected: blockchain is overkill for the
      pilot. The five-layer architecture uses simpler, well-understood protocols
      (git, SWIM, DHT) that are sufficient for the threat model. Blockchain may
      be revisited if the threat model requires Sybil resistance at scale.


      5. **Single layer (monolithic P2P).** Combine all five layers into a
      single P2P protocol. Rejected: the layers have different requirements
      (replication vs. membership vs. lookup vs. content vs. deploy). A
      monolithic protocol would be over-complex and difficult to upgrade
      independently. The layered approach allows each layer to evolve
      independently.
    file: docs/rfcs/archive/implemented/rfc-0562-p2p-werkstatt-network-topology-and-dna-invariant-5-layer-architecture-for-million-site-scale.md
  - kind: rejected-alternative
    rfcId: RFC-0563
    title: "Git-Mesh Platform Code Replication: P2P replication of platform monorepo
      across workshops"
    status: implemented
    alternativesText: >-
      1. **Custom replication protocol.** Build a custom P2P protocol for
      platform code replication. Rejected: git already provides replication
      (push/pull/fetch), content addressing (SHA), and signature verification
      (GPG/trailer). A custom protocol would duplicate these capabilities and
      add maintenance burden.

      2. **Git bundle transfer.** Use `git bundle` to transfer commits as files
      over HTTP. Rejected: bundles are a point-in-time snapshot, not a live
      replication channel. Standard git remotes provide incremental fetch and
      are simpler to operate.

      3. **IPFS for platform code.** Store the monorepo in IPFS and replicate
      via IPFS pins. Rejected: IPFS adds a new dependency and operational
      burden. Git remotes are already understood by all developers and
      operators.

      4. **Read-only mirrors only.** Configure workshops as read-only mirrors of
      the canonical remote, never syncing from peers. Rejected: if the canonical
      remote is unavailable, read-only mirrors cannot update. Peer-to-peer sync
      provides resilience when the canonical remote is down.
    file: docs/rfcs/archive/implemented/rfc-0563-git-mesh-platform-code-replication-p2p-replication-of-platform-monorepo-across-workshops.md
  - kind: rejected-alternative
    rfcId: RFC-0564
    title: "SWIM Membership and CRDT Genome: Gossip failure detection and persistent
      membership log for workshops"
    status: implemented
    alternativesText: >-
      1. **Centralized membership service.** A central service tracks workshop
      membership. Rejected: single point of failure. If the membership service
      is down, no workshop can join or detect failures.

      2. **Heartbeat-based membership.** Each workshop sends heartbeats to all
      others. Rejected: O(N²) message complexity. SWIM's gossip-based approach
      is O(N) per round.

      3. **Raft consensus for membership.** Use Raft to maintain a replicated
      membership log. Rejected: Raft requires a leader and quorum. SWIM is
      leaderless and eventually consistent, which is sufficient for membership.

      4. **No persistent log (in-memory only).** Keep membership in memory only.
      Rejected: a restarting workshop loses its membership view and must
      re-converge from scratch. The CRDT genome log allows instant restoration.

      5. **Gossip-based membership without SWIM.** Use a simpler gossip protocol
      without SWIM's suspicion mechanism. Rejected: without suspicion, a slow
      workshop may be incorrectly marked dead. SWIM's suspicion timeout reduces
      false positives.
    file: docs/rfcs/archive/implemented/rfc-0564-swim-membership-and-crdt-genome-gossip-failure-detection-and-persistent-membership-log-for-workshops.md
  - kind: rejected-alternative
    rfcId: RFC-0565
    title: "DHT Site Registry and Content Placement: S/Kademlia-hardened DHT for
      site lookups and mirror placement"
    status: implemented
    alternativesText: >-
      1. **Centralized registry service.** A central service stores all site
      metadata. Rejected: single point of failure. If the service is down, no
      workshop can discover sites.

      2. **Gossip-based registry replication.** Replicate the registry via
      gossip (like SWIM). Rejected: gossip is O(N) per round for all entries.
      DHT is O(log N) for a specific lookup. For million-site scale, DHT is more
      efficient.

      3. **DNS-based discovery.** Use DNS records for site discovery. Rejected:
      DNS is hierarchical and relies on registrars. The DHT is decentralized and
      does not require DNS infrastructure.

      4. **Blockchain-based registry.** Store site metadata on a blockchain.
      Rejected: blockchain is overkill for the pilot. S/Kademlia provides
      sufficient security with lower operational overhead.

      5. **No DHT (local registry only).** Each workshop maintains its own
      registry and syncs via git-mesh. Rejected: at million-site scale, the
      registry is too large for every workshop to store locally. The DHT
      distributes the registry across workshops.
    file: docs/rfcs/archive/implemented/rfc-0565-dht-site-registry-and-content-placement-s-kademlia-hardened-dht-for-site-lookups-and-mirror-placement.md
  - kind: rejected-alternative
    rfcId: RFC-0566
    title: "Immutable Platform Deploy with Atomic Rollback: Content-addressed
      artifacts and symlink-swap deployment across workshops"
    status: implemented
    alternativesText: >-
      1. **Blue-green deployments.** Run two identical environments (blue and
      green) and switch traffic between them. Rejected: blue-green requires
      double the infrastructure. Symlink swap achieves the same result with a
      single environment and zero downtime.

      2. **Canary deployments.** Deploy to a subset of workshops first, then
      gradually roll out. Rejected: canary adds complexity. The pilot uses
      trusted workshops where atomic deploy is sufficient. Canary may be
      revisited for large-scale rollouts.

      3. **Container-based deployment.** Use Docker containers with
      orchestration (Kubernetes). Rejected: containers add operational overhead.
      Symlink swap is simpler and works on any POSIX system without container
      infrastructure.

      4. **In-place deployment (overwrite current).** Overwrite the current
      deployment directory. Rejected: in-place deployment is not atomic. A
      failure during overwrite leaves the deployment in a broken state. Symlink
      swap is atomic.

      5. **Database-backed deployment state.** Track deployment state in a
      database. Rejected: a database is a single point of failure. The symlink
      filesystem is the deployment state — `current` points to the active
      artifact, `previous` points to the rollback target.
    file: docs/rfcs/archive/implemented/rfc-0566-immutable-platform-deploy-with-atomic-rollback-content-addressed-artifacts-and-symlink-swap-deployment-across-workshops.md
  - kind: rejected-alternative
    rfcId: RFC-0567
    title: Extend section-header, hero-decision-card, and FAQ schema for expert
      content recommendations
    status: implemented
    alternativesText: >-
      1. **Local `eyebrow` in `hero-decision-card` only** — rejected because
      other sections (transparency, price-card, comparison-cards) also benefit
      from eyebrow text. Adding it to the shared fragment avoids duplicating the
      prop across multiple archetype schemas.

      2. **`ctaNote` as `string[]` (paragraphs)** — rejected as over-engineered
      for a short clarifying sentence. A single string is sufficient and simpler
      to author.

      3. **`tagOrders` array of `{ tag, order }` pairs** — rejected as more
      verbose in YAML than a map. `orderTags: { home: 3, pricing: 1 }` is
      cleaner than `tagOrders: [{ tag: home, order: 3 }, { tag: pricing, order:
      1 }]`.

      4. **No schema change — content workaround** — rejected because the expert
      recommendations explicitly require these content patterns, and the
      archetype constraints prevent them. Workarounds (misusing `subheading` as
      eyebrow) produce wrong rendering positions.
    file: docs/rfcs/archive/implemented/rfc-0567-extend-section-header-hero-decision-card-and-faq-schema-for-expert-content-recommendations.md
  - kind: rejected-alternative
    rfcId: RFC-0568
    title: Clone-based workpiece materialization for reliable mission reconcile
    status: implemented
    alternativesText: >-
      1. **Shallow clone + unshallow at reconcile**: Materialize creates a `git
      clone --depth=1` for speed, then `git unshallow` before reconcile.
      Rejected — adds complexity for marginal speed gain. Full clone is simpler
      and gives agents access to full history during the mission.


      2. **Keep `git init` + fix `git am` with `--reject`**: Apply patches with
      `git am --reject` and auto-resolve rejected hunks. Rejected — this is the
      current broken behavior. Reject files require manual intervention for
      every generated file conflict. Does not solve the root cause (disconnected
      histories).


      3. **`git rebase` instead of `git merge`**: Rebase workpiece commits onto
      cache clone HEAD for linear history. Rejected — rebase rewrites SHA
      references, breaking traceability. `git merge --no-ff` preserves original
      commit SHA references and creates an explicit reconcile point in history.


      4. **Auto-delete untracked files in cache clone**: `git clean -fd` before
      reconcile. Rejected by operator — automatic deletion is dangerous. A
      previous session may have left untracked files intentionally.
      Investigation + block is safer.
    file: docs/rfcs/archive/implemented/rfc-0568-clone-based-workpiece-materialization-for-reliable-mission-reconcile.md
  - kind: rejected-alternative
    rfcId: RFC-0569
    title: "Dev/prod egress parity: apply text normalization in dev mode via Astro
      middleware"
    status: implemented
    alternativesText: >-
      - **Vite plugin (configureServer hook)** — rejected: deeper integration
      but risks conflicts with HMR and Vite's dev transform pipeline. The Astro
      middleware layer is the intended extension point and is simpler.

      - **Rehype plugin (dev-only smartypants:false + rehypeNormalizeText)** —
      the original dropped Layer 3 from RFC-0235. Rejected as the sole mechanism
      because it only covers Markdown→HTML, not HTML from `.astro` components,
      JSON-LD, `<meta>`, or `aria-*`. The middleware approach covers all HTML
      output.

      - **Only `smartypants: false` (no middleware)** — rejected: prevents
      Astro-injected signals at zero cost, but does not catch author-pasted
      signals (em-dashes from word processors, curly quotes, nbsp). The
      middleware catches everything `normalizeHtml()` handles.

      - **Only middleware (no `smartypants: false`)** — rejected: works but adds
      ~1-5ms per request for something `smartypants: false` prevents for free.
      The combined approach minimizes per-request overhead.

      - **New `dev.parity.validate` command** — rejected: dev preview is
      verified visually by the operator. An automated validator would require
      starting a dev server and making HTTP requests — over-engineered for the
      problem.

      - **Keep RFC-0235 as-is (no dev normalization)** — rejected by the
      operator: "we need to see what will be published before publishing it."
    file: docs/rfcs/archive/implemented/rfc-0569-dev-prod-egress-parity-apply-text-normalization-in-dev-mode-via-astro-middleware.md
  - kind: rejected-alternative
    rfcId: RFC-0570
    title: Add computed formula expressions to content references
    status: implemented
    alternativesText: >-
      1. **`{...}` brace delimiter for formulas** — rejected because RFC-0529
      just migrated all content references _away from_ brace syntax. The
      `BRACE_RESIDUAL_PATTERN` validator would flag formulas as malformed brace
      references, creating a conflict. The `=(...)` prefix is unambiguous and
      does not conflict with any existing syntax.


      2. **`[[...]]` double-bracket delimiter** — viable but visually heavier
      and less intuitive than `=(...)`, which mirrors spreadsheet formula
      syntax. Rejected in favor of `=(...)`.


      3. **Custom minimal expression parser (shunting-yard)** — rejected in
      favor of `expr-eval` per the project policy of preferring external
      packages over reimplementing solved problems. `expr-eval` is a sandboxed
      math evaluator (no `eval`/`Function`), 2KB, well-maintained.


      4. **Require pure numeric fields in frontmatter** — rejected as too
      invasive. Fields like `price.setup` store `"200 €"` with units; forcing
      authors to split numbers from units would break existing content and
      reduce readability. The `extractNumeric` approach handles this
      transparently.


      5. **Automatic migration of all hardcoded formulas via RFC-0529 migrator**
      — rejected because detecting formulas in free text is error-prone (false
      positives on prose that happens to contain `+` or `×`). A separate
      `content.formula.lint` + `content.formula.migrate` pair with human review
      is safer.
    file: docs/rfcs/archive/implemented/rfc-0570-add-computed-formula-expressions-to-content-references.md
  - kind: rejected-alternative
    rfcId: RFC-0571
    title: Make config.regenerate mission-aware and add it to build.prepare for
      template propagation
    status: implemented
    alternativesText: >-
      - **Separate `boilerplate.regenerate` command:** A new command that
      combines `config.regenerate` + all codegen generators. Rejected:
      unnecessary complexity. `config.regenerate` already exists and handles
      root config files. Adding it to `build.prepare` achieves the same result
      without a new command.

      - **Manual operator action:** Keep `config.regenerate` out of
      `build.prepare` and have operators run it manually when needed. Rejected:
      defeats the purpose of automatic propagation. The operator already has to
      run `build.prepare` — config regeneration should be part of it.

      - **Expand `config.regenerate` to handle all root configs (tsconfig.json,
      deploy workflow, .env.example):** Rejected: minimal scope. Other
      generators (`env.example.generate`, `kernel.wire`) already handle those
      files in `build.prepare`. Expanding `config.regenerate` would create
      overlap and conflict.
    file: docs/rfcs/archive/implemented/rfc-0571-make-config-regenerate-mission-aware-and-add-it-to-build-prepare-for-template-propagation.md
  - kind: rejected-alternative
    rfcId: RFC-0572
    title: Revert contact form to single-textarea with message-embedded contact
      extraction
    status: implemented
    alternativesText: >-
      - **Keep structured fields as optional (dual-path).** Rejected — the
      platform is forward-only. Keeping structured fields as optional alongside
      regex extraction creates a dual-path that is harder to maintain and
      violates the forward-only principle. A clean supersede is the correct
      approach.

      - **Improve regex accuracy before reverting.** Rejected — the operator has
      decided that UX simplicity outweighs regex fragility. The pre-0514 regex
      patterns are adequate for the target audience. Improving regex can be done
      in a follow-up RFC if needed.

      - **Remove referrerField too.** Rejected — the operator explicitly chose
      to keep it. It is optional and does not add friction when disabled.

      - **Repurpose contact.form.validate.** Rejected — the command was created
      specifically for RFC-0514's structured-fields invariant. Without
      structured fields, there is nothing meaningful to validate statically.
      Removing the command is cleaner than repurposing it for hint-text
      checking.
    file: docs/rfcs/archive/implemented/rfc-0572-revert-contact-form-to-single-textarea-with-message-embedded-contact-extraction.md
  - kind: rejected-alternative
    rfcId: RFC-0573
    title: Add mission.archive command for terminal-status mission archiving
    status: implemented
    alternativesText: >-
      - **Extend `mission.cleanup` to also archive:** Rejected.
      `mission.cleanup` (RFC-0480) is a disposal command — it deletes
      `workpiece/` and `distribution/`. Archiving is an organizational command —
      it moves directories. Mixing deletion and movement in one command
      conflates two distinct concerns and makes `--dry-run` output harder to
      interpret (which paths will be deleted vs. moved?). Keeping them separate
      follows the single-responsibility principle and matches the document
      archiving pattern (rfc.archive does not delete files, it moves them).


      - **Implement in `site-kernel-handoff` instead of `forge/os`:** Rejected.
      The operator requested integration into `docs.archive`, which lives in
      `packages/forge/os/core/`. The forge autonomy guard forbids `@warpgogol/*`
      imports in `packages/forge/`, so the handler cannot live in
      `site-kernel-handoff` and be called by `docs.archive`. Placing the handler
      in `packages/forge/os/mission/` keeps it alongside the other archive
      handlers (`rfc/archive.ts`, `adr/handlers/archive.ts`,
      `plan/handlers/archive.ts`, `audit/handlers/archive.ts`,
      `session/handlers/archive.ts`) and allows `docs.archive` to import it
      directly.


      - **Add `--system` flag for filtering by Sternsystem:** Rejected. The
      operator preferred a minimal flag set matching `rfc.archive` (`--dry-run`
      + `--status` only). Mission IDs already contain the systemId prefix
      (`warpgogol-com-m000010`), so operators can identify which system a
      mission belongs to from the archive output without a filter flag.


      - **Unidirectional (terminal → archive only):** Rejected. Bidirectional
      behavior is consistent with all existing archive commands (`rfc.archive`,
      `adr.archive`, `plan.archive`, `audit.archive`, `session.archive`). If a
      mission is manually moved back to `missions/` and its state is still
      terminal, re-running `mission.archive` moves it again. If a mission in
      `archive/` has its state changed to `open` (anomaly), the command moves it
      back to `missions/`.
    file: docs/rfcs/archive/implemented/rfc-0573-add-mission-archive-command-for-terminal-status-mission-archiving.md
  - kind: rejected-alternative
    rfcId: RFC-0574
    title: Relocate Sternsystem storage outside monorepo and introduce parameterized
      mirror topology
    status: implemented
    alternativesText: >-
      - **Keep `systems/<id>/` inside monorepo, enforce boundary via
      .gitignore.** Rejected: `.gitignore` prevents tracking but does not
      prevent filesystem access. Agents can still edit files. The structural
      boundary of moving outside the monorepo is stronger.


      - **Separate `sternsystem.backup` command for non-git mirrors.** Rejected:
      one command for all synchronization is simpler for operators. Star
      topology through `mirrors[0]` handles both git and backup mirrors
      uniformly. Adding a second command increases cognitive load without
      benefit.


      - **Role-based mirrors (`role: store | cache | backup`).** Rejected: the
      first-mirror-is-cache convention is simpler and sufficient. Roles add an
      extra field to maintain and validate without adding expressive power for
      the current use cases.


      - **`systems-local` as the name for the cache clone directory.** Rejected:
      "local" is ambiguous when both bare and non-bare repos are local.
      `systems-cache` preserves the existing codebase terminology
      (`syncCacheClone`, `hasGitCacheClone`) and accurately describes the
      function.
    file: docs/rfcs/archive/implemented/rfc-0574-relocate-sternsystem-storage-outside-monorepo-and-introduce-parameterized-mirror-topology.md
  - kind: rejected-alternative
    rfcId: RFC-0575
    title: Session-start pre-flight git status guard for agent work hygiene
    status: implemented
    alternativesText: >-
      - **Hard stop on foreign changes.** Reject — blocks parallel sessions
      working on different parts of the monorepo. Two sessions editing
      non-overlapping files should not block each other.


      - **Path-based conflict detection.** The skill would declare its target
      paths upfront and only stop if they overlap with dirty files. Rejected —
      skills like `fo-idea-implement` cannot know all target paths before
      reading the RFC and plan. The set of touched files emerges during
      implementation.


      - **New DNA invariant (DNA-58).** Rejected — DNA invariants describe
      architecture, not agent process. Commit hygiene rules live in AGENTS.md,
      not in DNA.


      - **Strengthen `rfc.implement.stamp` to list files on RFC-IMP-04.**
      Rejected — the command already fails with a clear error. The problem was
      agent behavior (stash workaround), not insufficient error output.
      Pre-flight catches the dirty tree earlier, making RFC-IMP-04 less likely
      to occur.


      - **Pre-flight in all mutating fo-skills.** Rejected —
      `fo-idea-create-rfc`, `fo-idea-create-adr`, `fo-review`,
      `fo-compass-annotate`, and `fo-doc-audit` produce small, well-scoped
      changes. `fo-idea-implement` and `fo-fix` are the high-risk skills that
      produce large volumes of changes and are most likely to create or
      encounter stranded work.
    file: docs/rfcs/archive/implemented/rfc-0575-session-start-pre-flight-git-status-guard-for-agent-work-hygiene.md
  - kind: rejected-alternative
    rfcId: RFC-0576
    title: Migrate content.links.validate and mirroring.validate to canonical
      Diagnostics
    status: implemented
    alternativesText: >-
      1. **fixHints only (no migration to diagnosticsResult).** Keep
      `resultFromViolations` but embed fixHint in the string message. Rejected:
      string-embedded fixHints are not machine-parseable; agents would need
      regex extraction. The canonical `Diagnostic.fixHint` field exists
      specifically for this purpose.


      2. **Normalize trailing slash in `localizeUrl` instead of `parseUrl`.**
      Add trailing slashes to route map entries. Rejected: `localizeUrl` is used
      by route generation, sitemap, canonical URLs, and all generated links —
      changing it has unbounded blast radius. `parseUrl` is only used by the
      validator; the blast radius is contained.


      3. **No trailing slash normalization — rely on fixHint to guide authors.**
      Rejected: trailing slash on internal links is a common and legitimate
      authoring convention. Rejecting it creates false positives that require
      manual investigation on every mission completion. The root cause is a
      mismatch between two internal functions, not an authoring error.


      4. **Register MIRROR-01 but leave `page.blocks.mirror.validate` on its
      custom result builder.** Rejected: the custom builder produces
      `PageBlocksMirrorResult` which is not a `CheckResult` — agents parsing
      `ForgeCommandResult.data.diagnostics` miss these violations. Full
      migration to `diagnosticsResult` is needed for consistency.
    file: docs/rfcs/archive/implemented/rfc-0576-migrate-content-links-validate-and-mirroring-validate-to-canonical-diagnostics.md
  - kind: rejected-alternative
    rfcId: RFC-0577
    title: Enrich fixHints for PSEO-GLOSS-01 and PUBTXT-07 with actionable commands
      and source resolution
    status: implemented
    alternativesText: >-
      1. **Add a `fixCommand` field to `Diagnostic` instead of enriching
      `fixHint`.** Rejected: `fixHint` already exists and is defined as
      "imperative remediation a human or agent can execute." Adding a new field
      would require changing the `Diagnostic` interface (RFC-0203 contract) and
      all consumers. The fixHint string is sufficient — agents can parse it.


      2. **Generate fixHints from a command registry instead of hardcoding
      them.** Rejected: the command name and flags are already known at the call
      site (the validator has the module ID and target language). Generating
      from a registry adds indirection without benefit. The fixHint is a string
      template, not a structured command invocation.


      3. **For PUBTXT-07, always point to `build.prepare` without source
      resolution.** Rejected: without knowing _which_ source file to fix, the
      agent must search all prose files for the broken link. Source resolution
      narrows the search to one file.
    file: docs/rfcs/archive/implemented/rfc-0577-enrich-fixhints-for-pseo-gloss-01-and-pubtxt-07-with-actionable-commands-and-source-resolution.md
  - kind: rejected-alternative
    rfcId: RFC-0578
    title: Structured build failure diagnostics in mission.validate
    status: implemented
    alternativesText: >-
      1. **Parse the Astro build output with a structured parser (AST).**
      Rejected: Astro's build output is Vite/Rollup output — not a stable API.
      Pattern matching on error signatures is more robust and maintainable.


      2. **Register BUILD-01 in `DIAGNOSTIC_RULES` and use
      `diagnosticsResult`.** Rejected: BUILD-01 is a mission-level diagnostic,
      not a content check. It does not belong in the content-surface rule
      registry. The `Diagnostic` shape is used (RFC-0203), but the rule registry
      is not extended.


      3. **Emit the full build output as the diagnostic message.** Rejected: the
      full output can be thousands of lines. The pattern-matched excerpt +
      fixHint is actionable; the full output is already available in the console
      log.


      4. **Add build failure patterns to `fix-patterns.md` instead of the
      codebase.** Rejected: `fix-patterns.md` is a reactive catalog (applied
      after error occurs). Pattern matching in the codebase is proactive — the
      diagnostic is structured at emission time, before any agent reads it.
    file: docs/rfcs/archive/implemented/rfc-0578-structured-build-failure-diagnostics-in-mission-validate.md
  - kind: rejected-alternative
    rfcId: RFC-0579
    title: Add nextSteps to mission.validate and archive commands
    status: implemented
    alternativesText: >-
      1. **Add `nextSteps` to `KernelCommandResult.data` instead of the result
      object.** Rejected: RFC-0542 established `nextSteps` as a cross-cutting
      field on the result object directly, not inside command-specific `data`.
      Keeping it on the result object ensures agents can find it without knowing
      the `data` shape of each command.


      2. **Only add `nextSteps` to `mission.validate`, not archive commands.**
      Rejected: the archive command gap is ecosystem-wide (all 6 handlers).
      Adding it to only one would be inconsistent. The coordinated change is
      small (one `nextSteps` array per handler).


      3. **Reuse `ForgeNextStep` type from forge in site-kernel.** Rejected:
      site-kernel must not import from forge (dependency direction is forge →
      site-kernel, not the reverse). Defining `KernelNextStep` with the same
      shape is the correct approach — structural typing ensures compatibility
      without a runtime dependency.


      4. **Make `nextSteps` required on all lifecycle commands.** Rejected:
      RFC-0542 says lifecycle commands MUST populate it, but making it a
      required field on `KernelCommandResult` would break all existing commands
      that don't populate it. The field is optional; the obligation is
      documented, not type-enforced.
    file: docs/rfcs/archive/implemented/rfc-0579-add-nextsteps-to-mission-validate-and-archive-commands.md
  - kind: rejected-alternative
    rfcId: RFC-0580
    title: Auto-commit werkstatt side-effects from mission lifecycle commands
    status: implemented
    alternativesText: >-
      1. **Warn instead of throw on commit failure** — rejected. Warning would
      leave the working tree dirty and shift the burden to the operator,
      violating the "minimize operator involvement" principle. Throwing forces
      the agent to resolve the issue immediately.


      2. **Separate `werkstatt.commit` command** — rejected. A separate command
      requires the operator or agent to remember to run it after every lifecycle
      transition, recreating the manual discipline problem this RFC eliminates.


      3. `git add -A` **instead of specific file paths** — rejected. `git add
      -A` risks picking up foreign uncommitted changes from concurrent sessions.
      Staging only specific side-effect files is safe and deterministic.


      4. **Push in addition to commit** — rejected. Werkstatt monorepo push is a
      separate operation controlled by the operator or CI. Lifecycle commands
      should not implicitly push.


      5. **`simple-git` library instead of `gitExec`** — rejected. `gitExec` is
      already used in the codebase (`bordbuch-io.ts`). Adding a new dependency
      for 3 git calls is unnecessary.
    file: docs/rfcs/archive/implemented/rfc-0580-auto-commit-werkstatt-side-effects-from-mission-lifecycle-commands.md
  - kind: rejected-alternative
    rfcId: RFC-0581
    title: Mandatory session-end retro with git hygiene check for agent work isolation
    status: implemented
    alternativesText: >-
      - **Auto-commit at session end.** The agent automatically commits all
      uncommitted changes with an auto-generated message. Rejected —
      auto-generated commit messages are often inaccurate, and the agent may
      sweep foreign changes from other sessions into the commit. The operator
      should decide what to commit and with what message.


      - **Amendment to RFC-0575.** Extend RFC-0575 to cover both session-start
      and session-end. Rejected — RFC-0575 is already implemented and archived.
      Amending an archived RFC complicates the lifecycle. Session-start and
      session-end are separate concerns with different triggers (automatic vs.
      operator signal), different rules, and different skill modifications. A
      separate RFC is cleaner.


      - **New DNA invariant (DNA-58).** Rejected — DNA invariants describe
      architecture, not agent process. Session hygiene rules live in AGENTS.md,
      not in DNA. This follows the precedent set by RFC-0575, which also has
      `satisfies: []`.


      - **Report-only without asking.** The agent reports uncommitted changes
      but does not offer to commit. Rejected — the operator may not know the
      correct commit path (`mission.git.commit` vs. `ecosystem.commit` vs. `git
      commit`). Offering to commit with the appropriate path is more helpful and
      reduces the chance of the operator using the wrong command.


      - **Mandatory retro at every skill pipeline completion.** Trigger the
      retro after `fo-idea-implement`, `fo-fix`, `fo-review`, etc. Rejected —
      skill pipelines are mid-session events, not session ends. The operator may
      continue working after a pipeline completes. Triggering a full retro (with
      insight triage and delegation) after every pipeline is excessive and
      disrupts workflow. The retro belongs at session end, triggered by the
      operator.
    file: docs/rfcs/archive/implemented/rfc-0581-mandatory-session-end-retro-with-git-hygiene-check-for-agent-work-isolation.md
  - kind: rejected-alternative
    rfcId: RFC-0582
    title: Fix surface.generate depth-0 hub filtering and add declared-blueprint
      post-check
    status: implemented
    alternativesText: >-
      1. **Keep the existsSync filter, add an exception for
      `minRecordsPerDepth[0] = 0`.** Rejected because it introduces a new
      concept (`minRecordsPerDepth`) that does not exist in the blueprint
      schema, and `expandBlueprint` already handles empty collections correctly
      — the filter is simply redundant.


      2. **Add `surface/**/*` to `reads` to capture `.gitkeep` files in cache
      key.** Rejected because after removing the `existsSync` filter, the
      command no longer depends on directory existence — the cache key problem
      becomes moot. Adding broader reads globs would unnecessarily invalidate
      the cache more often.


      3. **Add a separate `surface.declared.validate` command instead of a
      post-check in `surface.generate`.** Rejected because the check is trivial
      (compare two arrays) and belongs in the generation command itself — a
      separate validator would add pipeline steps and complexity for no benefit.
    file: docs/rfcs/archive/implemented/rfc-0582-fix-surface-generate-depth-0-hub-filtering-and-add-declared-blueprint-post-check.md
  - kind: rejected-alternative
    rfcId: RFC-0583
    title: Add bordbuch.repair command for hash-chain restoration and missing
      mission-open insertion
    status: implemented
    alternativesText: >-
      1. **Full repair (D3): hash chain + duplicate removal + event-id fix.**
      Rejected because duplicate mission-open removal is destructive and rare.
      Event-id gaps are already fixed as part of chain resequencing. A focused
      command that handles the most common repair scenario
      (orphan-mission-close) is safer and easier to reason about.


      2. **Hash-chain-only repair (D1).** Rejected because without inserting the
      missing `mission-open`, the `orphan-mission-close` violation persists. The
      operator would still need to manually insert the event, which is the
      fragile process this RFC eliminates.


      3. **Manual repair via a documented runbook.** Rejected because the
      hash-chain recomputation is error-prone by hand and the existing
      `computeEntryHash` function is not exported for external use. A command
      encapsulates the logic safely.
    file: docs/rfcs/archive/implemented/rfc-0583-add-bordbuch-repair-command-for-hash-chain-restoration-and-missing-mission-open-insertion.md
  - kind: rejected-alternative
    rfcId: RFC-0584
    title: Auto-resolve bordbuch delete-modify conflicts in mission.reconcile
    status: implemented
    alternativesText: >-
      1. **`.gitattributes` with `merge=ours` for `bordbuch/` in the cache
      clone.** Rejected because it changes git's merge strategy globally for the
      cache clone, making it less explicit. The auto-resolution is visible in
      the reconcile command's code and logs, while `.gitattributes` is a hidden
      git config that agents may not discover.


      2. **Preventive `git merge --no-commit` + `git checkout --ours bordbuch/`
      before conflict.** Rejected because it adds complexity to the happy path
      (no-conflict merges) and requires an extra commit step. The reactive
      approach (try merge, catch conflict, auto-resolve) is simpler and only
      activates when needed.


      3. **Generalize to all cache-clone-only paths.** Rejected because
      `bordbuch/` is the only known cache-clone-only path that causes
      delete-modify conflicts. Other paths (e.g. `system.pin.json`) are data
      paths present in the workpiece. Generalizing without a clear use case is
      premature.
    file: docs/rfcs/archive/implemented/rfc-0584-auto-resolve-bordbuch-delete-modify-conflicts-in-mission-reconcile.md
  - kind: rejected-alternative
    rfcId: RFC-0585
    title: Restore release.prepare production build and add release.publish dist guard
    status: implemented
    alternativesText: >-
      1. **Only add guards, no build in prepare**: `release.prepare` would not
      build — it would only require `distribution/dist` to exist (fail-fast if
      missing). `mission.build` would be a mandatory prerequisite. Rejected
      because it deviates from RFC-0357 §6.1 step 4, which specifies that
      `release.prepare` runs the production build itself. It also creates a
      two-command prerequisite where one should suffice.


      2. **Hybrid: optional `--build` flag**: `release.prepare --build` would
      run the build; without the flag, it would require `distribution/dist`.
      Rejected because it introduces operator-configurable behavior for a
      safety-critical step — the default path must be the safe path.


      3. **Deprecate `mission.build`**: `release.prepare` would be the only
      place where builds happen. Rejected because `mission.build` serves a
      different purpose: preview and testing without creating a release
      candidate. Removing it would force operators to create a release just to
      preview a build.
    file: docs/rfcs/archive/implemented/rfc-0585-restore-release-prepare-production-build-and-add-release-publish-dist-guard.md
  - kind: rejected-alternative
    rfcId: RFC-0587
    title: Fix Leitstand preflight checks and artifact store hashing for Cloudflare
      Workers deploy
    status: implemented
    alternativesText: >-
      1. **Tree hash only (no tar.gz)**: Rejected — tar.gz archive provides a
      single-file artifact that can be restored without the original directory
      structure. Tree hash alone requires re-hashing the entire directory for
      verification.


      2. **Latest-by-createdAt manifest lookup**: Rejected — leaves duplicate
      manifests in the store, requiring garbage collection. Idempotent put is
      cleaner: one manifest per release, no duplicates.


      3. **Generic dist size limits with override**: Rejected — adapter-declared
      limits are more accurate. Each platform (Cloudflare Workers, Netlify,
      Vercel) has different limits, and hardcoding them in
      `leitstand-commands.ts` creates a maintenance burden.


      4. **Direct wrangler binary from `node_modules/.bin`**: Rejected —
      requires wrangler in workpiece dependencies, which may not be present.
      `npx --yes wrangler` is universal and works from any directory.


      5. **Structured logger instead of `console.error`**: Rejected — the
      adapter runs in a CLI context where `console.error` is the appropriate
      output channel. A structured logger would add complexity without benefit
      for this use case.
    file: docs/rfcs/archive/implemented/rfc-0587-fix-leitstand-preflight-checks-and-artifact-store-hashing-for-cloudflare-workers-deploy.md
  - kind: rejected-alternative
    rfcId: RFC-0588
    title: Fix behavior snapshot route collection for Cloudflare Workers adapter
      dist layout
    status: implemented
    alternativesText: >-
      1. **Follow redirects in health checker and compare target hash**:
      Rejected — redirected routes should not be in the snapshot at all.
      Including them adds noise and the hash comparison is meaningless (the
      redirected page's HTML is never served).


      2. **Separate `behavior.snapshot.redirect.exclude` command**: Rejected —
      redirect exclusion is a concern of `collectRoutes`, not a separate
      command. Adding a command for this would be over-engineering.


      3. **Exclude 410 Gone routes in this RFC**: Rejected — 410 tombstone
      handling is architecturally distinct (middleware-level, not
      `_redirects`-level) and is covered by RFC-0589. Mixing 410 exclusion into
      this RFC would create a coupling between two independent fixes.
    file: docs/rfcs/archive/implemented/rfc-0588-fix-behavior-snapshot-route-collection-for-cloudflare-workers-adapter-dist-layout.md
  - kind: rejected-alternative
    rfcId: RFC-0589
    title: Handle 410 Gone tombstones in middleware instead of _redirects for
      Cloudflare Workers
    status: implemented
    alternativesText: >-
      1. **Replace 410 with 301 redirect to a tombstone page**: Rejected — 301
      is a permanent redirect, not a tombstone. Search engines would index the
      tombstone page instead of de-indexing the retired URL. 410 is semantically
      correct for permanently removed content.


      2. **Replace 410 with 404 in `_redirects`**: Rejected — 404 is also not
      supported by Cloudflare Workers `_redirects` (only 200, 301, 302, 303,
      307, 308). And 404 is semantically different from 410 (Not Found vs Gone).


      3. **Handle 410 in Cloudflare Worker script (not middleware)**: Rejected —
      the Worker script is generated by the Astro Cloudflare adapter and should
      not be hand-edited. Astro middleware is the correct extension point.


      4. **Remove 410 support entirely**: Rejected — 410 tombstones are a
      requirement of RFC-0487/RFC-0509 for retired page routes. The semantic
      meaning of "permanently removed" must be preserved.


      5. **Platform-specific `_redirects` generation**: Rejected — adding
      adapter-awareness to `_redirects` generation couples codegen to deployment
      platform. Middleware is platform-agnostic and works everywhere.


      6. **Conditional middleware file generation**: Rejected — generating the
      middleware file only when 410 routes exist would require conditional
      imports in `src/middleware.ts`, which is incompatible with Astro's static
      import model. Always generating the file (passthrough when no tombstones)
      is simpler and avoids build failures.
    file: docs/rfcs/archive/implemented/rfc-0589-handle-410-gone-tombstones-in-middleware-instead-of-redirects-for-cloudflare-workers.md
  - kind: rejected-alternative
    rfcId: RFC-0590
    title: Require closed mission state for release.prepare
    status: implemented
    alternativesText: >-
      1. **Add a `release.dry-run` command for pre-close review.** Rejected —
      adds complexity to the command surface for a use case that `mission.build`
      already covers. `mission.build` runs the same build pipeline
      (`build.prepare` → `astro build` → `build.post`) and produces a `dist/`
      for review without creating a release artifact.


      2. **Keep `open` allowed but add a `reconciledAt` guard.** Rejected —
      duplicates the `mission.close` guard. `mission.close` already refuses to
      close without `reconciledAt`. Adding the same check to `release.prepare`
      is redundant if the mission must be closed first.


      3. **Warn but allow `open` missions.** Rejected — warnings are easily
      ignored by agents and operators. A hard refusal is the only reliable
      enforcement for a contract tightening.
    file: docs/rfcs/archive/implemented/rfc-0590-require-closed-mission-state-for-release-prepare.md
  - kind: rejected-alternative
    rfcId: RFC-0591
    title: Add two-pass bitrate-capped MP4 encoding to video.variants.generate
    status: implemented
    alternativesText: >-
      - **CRF with `-maxrate` and `-bufsize` (constrained CRF).** This caps the
      peak bitrate but does not guarantee a total file size. For long content
      with complex scenes, the file can still exceed the target. Rejected — the
      operator needs a deterministic size guarantee, not a peak-bitrate cap.


      - **Post-encode size check with re-encode at higher CRF.** Encode at CRF
      17, check the file size, and if it exceeds 25 MiB, re-encode at CRF 20,
      then CRF 23, etc. Rejected — this is iterative and unpredictable (may
      require 3+ encode passes), and the final size is still not guaranteed.
      Two-pass with a calculated bitrate is deterministic in a single iteration.


      - **Apply two-pass to all progressive formats (MP4, WebM, AV1).** Rejected
      — VP9 and AV1 are significantly more efficient than H.264 and rarely
      exceed 25 MiB at their current CRF settings. Adding two-pass for VP9/AV1
      increases complexity (different two-pass syntax) without practical
      benefit. If a future VP9/AV1 file exceeds the limit, this can be extended.


      - **Global `maxSizeMb` in `system.md` instead of per-media frontmatter.**
      Rejected — different videos may need different size constraints (e.g., a
      short hero background vs. a long feature video). Per-media-config is more
      flexible. A global default of 24 MiB is already built into the field
      default.


      - **Keep CRF 17 and rely on `video.dist.prune` to remove oversized MP4s.**
      Rejected — `video.dist.prune` removes bundled source copies from
      `dist/client/_astro`, not derived files from `public/_video/`. Removing
      the progressive MP4 from the deploy would leave the `<video>` element with
      a broken source reference. The size must be guaranteed at encode time, not
      patched post-hoc.
    file: docs/rfcs/archive/implemented/rfc-0591-add-two-pass-bitrate-capped-mp4-encoding-to-video-variants-generate.md
  - kind: rejected-alternative
    rfcId: RFC-0592
    title: Fix wildcard matching in behavior snapshot route collection
    status: implemented
    alternativesText: >-
      1. **Only fix wildcard matching, no meta-refresh exclusion** — This is the
      chosen approach. Meta-refresh redirect stub detection and marking is
      handled by RFC-0595, which proposes a more complete solution (marking
      redirect routes with `contentHash: null` + `redirectTarget` and verifying
      the redirect via health checks) rather than simply excluding them.


      2. **Add `--force` flag to `leitstand.propagate`** — Rejected by the
      operator. Health check false negatives are bugs to fix in the code, not to
      bypass with flags. A `--force` flag would mask real deployment issues.
    file: docs/rfcs/archive/implemented/rfc-0592-exclude-meta-refresh-redirect-stubs-and-fix-wildcard-matching-in-behavior-snapshot-route-collection.md
  - kind: rejected-alternative
    rfcId: RFC-0593
    title: "Add validation gates to mission lifecycle: bordbuch.validate before
      mission.open and mission.validate before mission.close"
    status: implemented
    alternativesText: >-
      1. **Check `validatedAt` in mission manifest** — `mission.validate` would
      write `validatedAt` and `validationVerdict` to `mission.yaml`;
      `mission.close` would check their presence. Rejected: does not guarantee
      freshness. An operator could validate, then edit files, then close.
      Running `mission.validate` inline inside `mission.close` guarantees the
      validation reflects the current workpiece state.


      2. **Warn-only mode** — `mission.close` would warn but not block on
      validation failures. Rejected: warnings are easily ignored by agents and
      operators. The goal is to make it impossible to close an invalid mission.


      3. **`--force` bypass flag** — allow `mission.close --force` to skip
      validation. Rejected: defeats the purpose. If validation is optional, it
      will be skipped under time pressure. Hard gates only.
    file: docs/rfcs/archive/implemented/rfc-0593-add-validation-gates-to-mission-lifecycle-bordbuch-validate-before-mission-open-and-mission-validate-before-mission-close.md
  - kind: rejected-alternative
    rfcId: RFC-0594
    title: Add targeted content validators to mission.git.commit based on changed
      file paths
    status: implemented
    alternativesText: >-
      1. **Full `mission.validate` on every commit** — run the complete
      build+validate pipeline before each commit. Rejected: a full build takes
      2+ minutes. Commits are frequent (10+ per mission). 20+ minutes of
      validation per mission is unacceptable.


      2. **Fast content validators without build** — run all content validators
      (pbp, semantic.drift, faq) on every commit, regardless of which files
      changed. Rejected: wasteful. If only `pages/` changed, running
      `pbp.content.validate` and `faq.validate` adds unnecessary latency.
      Targeted validators based on changed files are faster.


      3. **Git pre-commit hook in workpiece** — install a
      `.git/hooks/pre-commit` script in the workpiece repo. Rejected: git hooks
      are easily bypassed (`--no-verify`), are not managed by the kernel, and
      would need to be installed per-workpiece. `mission.git.commit` is the
      kernel-managed commit path — validation belongs there.
    file: docs/rfcs/archive/implemented/rfc-0594-add-targeted-content-validators-to-mission-git-commit-based-on-changed-file-paths.md
  - kind: rejected-alternative
    rfcId: RFC-0595
    title: Mark redirect routes with contentHash null and redirectTarget in behavior
      snapshot
    status: implemented
    alternativesText: >-
      1. **Health check: `redirect: "manual"` for all routes** — don't follow
      redirects in the health check at all. Rejected: non-redirect routes that
      are accidentally redirected (e.g., misconfigured CDN) would not be
      detected. The fix should be targeted: only skip content-hash for routes
      explicitly marked as redirects in the snapshot.


      2. **Snapshot stores final hash** — `behavior.snapshot.capture` follows
      the redirect and stores the final page's hash. Rejected: the snapshot
      should describe what's in `dist/`, not what's at the end of a redirect
      chain. The redirect page is the actual artifact in `dist/`; the final page
      is a different route. Storing the final hash for `/de` would be
      semantically wrong — it's the hash of `/`, not `/de`.


      3. **Health check: ignore `/de` specifically** — hardcode redirect route
      paths. Rejected: not generalizable. Different sites have different
      redirect routes (e.g., `/en` → `/`, `/de/` → `/de`). The detection must be
      dynamic based on the HTML content.
    file: docs/rfcs/archive/implemented/rfc-0595-mark-redirect-routes-with-contenthash-null-and-redirecttarget-in-behavior-snapshot.md
  - kind: rejected-alternative
    rfcId: RFC-0596
    title: Call artifact.store.put automatically inside release.publish to eliminate
      missing-artifact deployment failures
    status: implemented
    alternativesText: >-
      1. **`release.publish` requires artifact in store** — `release.publish`
      would check `artifactStorePreflight` and refuse if the artifact is
      missing, directing the operator to run `artifact.store.put` first.
      Rejected: two-step workflow is error-prone. The operator can forget. The
      goal is one-step publish.


      2. **`release.prepare` calls `artifact.store.put`** — store the artifact
      during `release.prepare` instead of `release.publish`. Rejected:
      `release.prepare` builds the dist, but the release is not yet published.
      Storing an artifact for an unpublished release is premature — the operator
      might abort the release after prepare. Artifact storage should happen at
      publish time, when the release is finalized.


      3. **`leitstand.propagate` auto-creates artifact** — if the artifact is
      missing, `leitstand.propagate` would call `artifact.store.put` before
      deploying. Rejected: propagation is a deployment operation, not a release
      operation. Artifact storage belongs in the release lifecycle, not the
      deployment lifecycle. Mixing concerns makes the system harder to reason
      about.
    file: docs/rfcs/archive/implemented/rfc-0596-call-artifact-store-put-automatically-inside-release-publish-to-eliminate-missing-artifact-deployment-failures.md
  - kind: rejected-alternative
    rfcId: RFC-0597
    title: "Optimize mission materialization: skip preflight on unchanged cache,
      split build.prepare pipeline, persist media cache across missions"
    status: implemented
    alternativesText: >-
      1. **Skip entire `build.prepare` at materialization.** Rejected — the dev
      server (`astro dev`) requires generated routes, middleware, styles, and
      surface artifacts to start. Without `routes.generate`,
      `i18n.middleware.generate`, `surface.generate`, `styles.global.generate`,
      the dev server would crash or serve stale content. Splitting into `.dev`
      vs `.full` is safer.


      2. **Lazy codegen via Vite plugins.** Instead of running codegen at
      materialization, run it on-demand when the dev server requests a generated
      file. Rejected — this would require rewriting ~40 generators as Vite
      plugins, a massive change with unclear benefit. The codegen generators are
      fast (seconds total); the slow steps are media transcoding and preview
      image generation, which are excluded from `.dev`.


      3. **Store `.cache/` in git (LFS).** Rejected — video encoding caches can
      be hundreds of MB. Git LFS is already used for source media, but derived
      artifacts should not bloat the repo. The cache clone's filesystem is the
      right storage location.


      4. **Use `system.pin.json` for HEAD tracking instead of a separate state
      file.** Rejected — `system.pin.json` is updated at sync time, not at close
      time. Its semantics (platform version, RFC head) don't match "last
      validated content state". A dedicated state file is cleaner.


      5. **Add `--force-preflight` flag.** Rejected by the operator — adds
      unnecessary CLI surface. Deleting the state file achieves the same result
      without new flags or conditional logic in the command handler.
    file: docs/rfcs/archive/implemented/rfc-0597-optimize-mission-materialization-skip-preflight-on-unchanged-cache-split-build-prepare-pipeline-persist-media-cache-across-missions.md
  - kind: rejected-alternative
    rfcId: RFC-0598
    title: Add section.css.import.validate for colocated CSS import integrity
    status: implemented
    alternativesText: >-
      - **Astro built-in CSS bundling**: Astro does not auto-discover colocated
      `.css` files — explicit `import` is required. No configuration change can
      fix this; it is fundamental to how Astro/Vite bundling works.

      - **Lint rule in `tokens.colors.section-shell.lint`**: Rejected — that
      validator checks CSS token content, not import integrity. Mixing concerns
      would bloat an existing validator.

      - **Convention-only (no validator)**: Rejected — the bug was caused by
      exactly this: convention was followed for most sections but silently
      broken for two. Manual discipline is insufficient for a 30+ section
      codebase.

      - **Check only colocated imports (not cross-directory)**: Rejected —
      `effect-text.css` is legitimately imported by `section-header.astro` from
      a different directory. Restricting to colocated-only would produce false
      positives for valid cross-imports.
    file: docs/rfcs/archive/implemented/rfc-0598-add-section-css-import-validate-for-colocated-css-import-integrity.md
  - kind: rejected-alternative
    rfcId: RFC-0599
    title: Fix open-source.generate output completeness verification
    status: implemented
    alternativesText: >-
      - **Remove the fingerprint cache entirely**: Rejected — the cache provides
      a significant performance benefit by avoiding `pnpm licenses list --prod
      --json` (which takes 5-10 seconds) when nothing changed. The fix preserves
      the cache but fixes the completeness check.

      - **Add a `--force` flag that bypasses the cache**: Rejected — the
      `--force` flag already exists but does not bypass the fingerprint
      short-circuit. Adding bypass logic to `--force` would work but is the
      wrong fix: the generator should never silently skip missing outputs
      regardless of `--force`.

      - **Make `generated.files.validate` auto-fix missing files by calling the
      owning generator**: Rejected — validators must be read-only. The fix
      belongs in the generator, not the validator.
    file: docs/rfcs/archive/implemented/rfc-0599-fix-open-source-generate-output-completeness-verification.md
  - kind: rejected-alternative
    rfcId: RFC-0600
    title: Add generated.stale.validate command for orphaned git-tracked generated
      files
    status: implemented
    alternativesText: >-
      - **Extend `public.managed.clean` to handle all generator categories**:
      Rejected — `public.managed.clean` is specifically for markdown twin files
      (RFC-0166). Extending it to handle preview images, icons, and all other
      generator outputs would bloat its scope and mix cleanup with validation.

      - **Use `git ls-files` + manual diff against ownership map in CI**:
      Rejected — this is exactly what the command automates. Manual CI scripts
      are fragile and bypass the kernel command registry.

      - **Add a `--clean` flag to auto-delete stale files**: Rejected —
      validators must be read-only. Auto-deletion is destructive and should be a
      separate, explicit operator action.
    file: docs/rfcs/archive/implemented/rfc-0600-add-generated-stale-validate-command-for-orphaned-git-tracked-generated-files.md
  - kind: rejected-alternative
    rfcId: RFC-0601
    title: Add generated.drift.validate command for content drift in generated files
    status: implemented
    alternativesText: >-
      - **Regenerate in temp directory and diff**: Rejected — regenerating in a
      temp directory requires running the full generator (including side effects
      like fingerprint cache writes). The `dryRun` approach is cleaner because
      it renders in memory without side effects.

      - **Use git diff after `build.prepare`**: Rejected — this conflates drift
      detection with build success. If `build.prepare` fails, the git diff is
      meaningless. The validator must be independent of the build pipeline.

      - **Extend RFC-0236 to cover all generators**: Rejected — RFC-0236 is
      specifically for `material.credits.generate` and its nonGoals explicitly
      say "Do not add drift guards for every generated file." A new RFC is the
      right scope.
    file: docs/rfcs/archive/implemented/rfc-0601-add-generated-drift-validate-command-for-content-drift-in-generated-files.md
  - kind: rejected-alternative
    rfcId: RFC-0602
    title: Enforce timestamp determinism in generated files
    status: implemented
    alternativesText: >-
      - **Rely on RFC-0345 alone**: Rejected — RFC-0345 was a one-time fix, not
      a lint. New generators can introduce volatile timestamps without
      detection.

      - **Use eslint rule for `no-new-date`**: Rejected — eslint rules are too
      broad. `new Date()` is legitimate in many contexts (e.g., logging, cache
      invalidation). The lint must be scoped to generator source files only and
      check output, not just source.

      - **Add timestamp normalization to `generated.drift.validate`
      (RFC-0601)**: Rejected — normalizing timestamps in drift detection hides
      the root cause. The fix should be in the generator, not the validator.
    file: docs/rfcs/archive/implemented/rfc-0602-enforce-timestamp-determinism-in-generated-files.md
  - kind: rejected-alternative
    rfcId: RFC-0603
    title: Make preview image generation deterministic
    status: implemented
    alternativesText: >-
      - **Gitignore preview images (don't commit them)**: Rejected by operator.
      Preview images must be committed to git for LFS storage and CDN caching.
      Non-deterministic generation would still cause LFS bloat.

      - **Perceptual hashing for drift detection**: Rejected by operator.
      Perceptual hashing hides real content drift (e.g., wrong page title) and
      adds complexity. The fix should be at the root — make rendering
      deterministic.

      - **Use a different image format (SVG)**: Rejected — OG preview images
      must be PNG (or JPEG) for social media compatibility. SVG is not supported
      by Open Graph.

      - **Use a headless browser with `--font-render-hinting=none`**: Rejected —
      headless browsers (Puppeteer, Playwright) have non-deterministic rendering
      due to GPU rasterization, font cache state, and timing-dependent layout.
      The current pipeline already uses SVG-to-PNG via `sharp`, which is
      inherently more deterministic than a headless browser.

      - **Switch to `@resvg/resvg-js` immediately**: Deferred — the current
      `sharp` SVG-to-PNG pipeline may achieve determinism with option fixes
      (disable `adaptiveFiltering`, remove redundant `resize`). `resvg` is a
      Phase 2 fallback only if `sharp` remains non-deterministic. This avoids
      adding an unnecessary dependency.
    file: docs/rfcs/archive/implemented/rfc-0603-make-preview-image-generation-deterministic.md
  - kind: rejected-alternative
    rfcId: RFC-0604
    title: Add bordbuch.generate and passport.key.ensure to build.prepare pipeline
    status: implemented
    alternativesText: >-
      - **Keep bordbuch.generate and passport.key.rotate as separate operator
      commands**: Rejected by operator. The operator confirmed that both should
      be in `build.prepare` to ensure their outputs are always present.

      - **Add only bordbuch.generate, not passport key**: Rejected by operator.
      The operator confirmed both should be added.

      - **Use `passport.key.rotate` with `--ensure` flag**: Rejected — muddies
      the command's contract. A separate `passport.key.ensure` command
      (RFC-0605) keeps each command's semantics clean.

      - **Create a separate `build.prepare.sensitive` pipeline for these
      commands**: Rejected — adding a third pipeline variant increases
      complexity. The commands are safe for `build.prepare`.

      - **Add a `--no-sensitive` flag to `build.prepare`**: Rejected — the
      dev-mode subset already handles this. No additional flag is needed.
    file: docs/rfcs/archive/implemented/rfc-0604-add-bordbuch-generate-and-passport-key-rotate-to-build-prepare-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0605
    title: Add passport.key.ensure command for idempotent pipeline-safe key generation
    status: implemented
    alternativesText: >-
      - **Add `--ensure` flag to `passport.key.rotate`**: rejected because it
      muddies the command's contract — `rotate` means "generate a new key", and
      `--ensure` changes that to "maybe generate". A separate command keeps each
      command's semantics clean.

      - **Suppress private key output in `passport.key.rotate` via a `--quiet`
      flag**: rejected because the command still rotates the key on every run,
      which is wrong for pipeline context.

      - **Do not add passport key generation to the pipeline at all**: rejected
      because `public/.well-known/cosmic-passport-key.json` is a git-tracked
      generated file that must exist after `build.prepare` (RFC-0604).
    file: docs/rfcs/archive/implemented/rfc-0605-add-passport-key-ensure-command-for-idempotent-pipeline-safe-key-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0606
    title: Fix generated.files.validate path resolution for systems prefix
    status: implemented
    alternativesText: >-
      - **Fix in RFC-0604 itself**: rejected because the path resolution bug is
      a pre-existing issue in `generated.files.validate` that affects any
      `systems/{system}/` entry, not just bordbuch. A separate RFC keeps the fix
      focused.

      - **Remove bordbuch entries from GENERATOR_OWNERSHIP_MAP**: rejected
      because the ownership map is the canonical registry for generated file
      validation (RFC-0375). Removing entries would mean bordbuch files are not
      validated at all.
    file: docs/rfcs/archive/implemented/rfc-0606-fix-generated-files-validate-path-resolution-for-systems-prefix.md
  - kind: rejected-alternative
    rfcId: RFC-0607
    title: Establish DNA invariant for generated-file content determinism
    status: implemented
    alternativesText: >-
      - **Extend DNA-18 to cover all generated files**: Rejected — DNA-18 is
      specifically about the Uni registry as the single UI index. Broadening it
      would dilute its meaning and require rewording an established invariant. A
      new invariant is cleaner.

      - **No DNA invariant — RFC-0601 stands alone**: Rejected — the audit found
      that RFC-0601's DNA-18 claim is tenuous. Without a proper invariant, the
      enforcement command has no architectural anchor.

      - **Make this an architecture RFC instead of policy**: Rejected —
      `rfc.create` requires `--satisfies` for architecture RFCs, and this RFC
      proposes a new invariant rather than satisfying an existing one. A policy
      RFC is the appropriate vehicle for establishing a new invariant.
    file: docs/rfcs/archive/implemented/rfc-0607-establish-dna-invariant-for-generated-file-content-determinism.md
  - kind: rejected-alternative
    rfcId: RFC-0608
    title: Enforce alt-to-main deployment promotion chain with release state machine
      and public build identity verification
    status: implemented
    alternativesText: >-
      - **Keep `--channel` in `leitstand.propagate`, make gate
      state-machine-based.** Rejected: the `--channel` flag implies that main
      deployment is just another propagation, which conceptually weakens the
      alt-first invariant. Splitting into `propagate` (alt) and `promote` (main)
      makes the promotion chain explicit and self-documenting.

      - **Runtime fetch of `build-identity.json` from the open-source section.**
      Rejected: would make the open-source page depend on client-side JavaScript
      and a successful fetch. A static file written at build time is simpler,
      more reliable, and agent-friendly.

      - **Separate API endpoint for build identity.** Rejected: adds runtime
      complexity (a Worker route, request handling) for a static data file. A
      `/.well-known/` JSON file is the standard approach for agent-discoverable
      metadata and requires no runtime code.

      - **Extend the open-source page with additional hash fields instead of a
      separate file.** Rejected: the open-source page is HTML — extracting
      structured data from HTML is fragile for agents. A JSON file is
      machine-parseable by design. The open-source page continues to display
      metadata sourced from the same build identity object.
    file: docs/rfcs/archive/implemented/rfc-0608-enforce-alt-to-main-deployment-promotion-chain-with-release-state-machine-and-public-build-identity-verification.md
  - kind: rejected-alternative
    rfcId: RFC-0609
    title: Standardize command argument pattern to flag-only
    status: implemented
    alternativesText: >-
      - **Positional-only (all commands use `input.args[0]`):** Rejected. Would
      require a new `positionalArgs` field in `KernelCommandDefinition` and
      migration of 9+ flag-based commands to positional. Loses type safety,
      self-documentation, and validation. Unix-idiomatic but less safe.


      - **Hybrid (flag with positional fallback, `input.flags["id"] ??
      input.args[0]`):** Rejected. Already used in 9 handlers but creates two
      code paths in every handler, making the convention unenforceable. A
      validator cannot check compliance because both patterns are technically
      valid. This is not a standard — it is the absence of a standard.


      - **Deprecation window (add `--id`, keep positional with warning):**
      Rejected. Creates double code paths during the window. The validator
      (RFC-0610) would need to allow both patterns during the window, then be
      tightened later — two migrations instead of one.


      - **Exempt forge.create (keep `<name>` positional):** Rejected. Creates an
      exception that complicates the validator and the standard. `forge.create
      --name foo` is not significantly worse than `forge.create foo`, and
      consistency is more valuable than Unix idiom compatibility for one
      command.
    file: docs/rfcs/archive/implemented/rfc-0609-standardize-command-argument-pattern-to-flag-only.md
  - kind: rejected-alternative
    rfcId: RFC-0610
    title: Add command.args.validate enforcement command
    status: implemented
    alternativesText: >-
      - **Rely on TypeScript compiler only:** After RFC-0609 removes `args` from
      `KernelCommandInput`, TypeScript will flag `input.args` references
      (including `?? input.args[0]`) as type errors. This catches
      ARG-COMPLIANCE-01 and ARG-COMPLIANCE-03 at the type level. Rejected as
      sufficient — TypeScript does not catch ARG-COMPLIANCE-02 (flags: {} with
      handler reading a named flag), which is a structural inconsistency between
      registration and handler, not a type error. Additionally, the validator
      provides structured `CheckResult` diagnostics with fix hints integrated
      into the `packages-check` pipeline, and catches violations in CI without
      requiring a full typecheck pass.


      - **Add a lint rule to eslint:** Rejected — eslint rules are generic and
      would need custom rule development. The kernel's validator pattern
      (CheckResult, build.check integration) is already established and provides
      better error messages with fix hints.


      - **Manual review during PR:** Rejected — manual review is unreliable,
      especially with AI agents creating commands. The whole point of RFC-0609 +
      RFC-0610 is automated enforcement.
    file: docs/rfcs/archive/implemented/rfc-0610-add-command-args-validate-enforcement-command.md
  - kind: rejected-alternative
    rfcId: RFC-0611
    title: Generate nested AGENTS.md files for workspace directories during forge
      onboarding
    status: implemented
    alternativesText: >-
      **Generate AGENTS.md for all declared workspace.dirs in stack profiles.**
      Rejected because `workspace.dirs` includes non-workspace directories like
      `docs/` and `scripts/` that do not benefit from `AGENTS.md`. Workspace
      discovery by `package.json` presence is more precise.


      **Declare workspace types in stack profiles (`firstWorkspace.type:
      app`).** Rejected because it requires updating all profiles and does not
      cover workspaces created after scaffolding (e.g., transplant projects
      adding `apps/<name>/`). Auto-detection by content is more flexible and
      works retroactively.


      **Minimal stub instead of fully generated content.** Rejected because a
      stub that is immediately extended by hand loses the regenerability
      property. Fully generated files with type-specific templates provide more
      value to agents and can be regenerated when `forge.yaml` changes.


      **Modify existing hand-written AGENTS.md with generated blocks
      (markers).** Rejected because inserting generated blocks into hand-written
      files is fragile and can break the file's structure. The edit guard (skip
      hand-written, propose improvements via doctor/bootstrap) is safer and
      respects operator ownership.


      **RFC fixes specific template content for app/package/service.** Rejected
      because the template content is an implementation detail that should
      evolve without RFC amendments. The RFC defines the mechanism (discovery,
      detection, generation, skip, regenerate); the templates are refined in
      code.
    file: docs/rfcs/archive/implemented/rfc-0611-generate-nested-agents-md-files-for-workspace-directories-during-forge-onboarding.md
  - kind: rejected-alternative
    rfcId: RFC-0612
    title: Add ownership.sync.validate command for generated file registry drift
      detection
    status: implemented
    alternativesText: >-
      - **Extend `generated.stale.validate` (RFC-0600) with OWN-01 diagnostic**:
      Rejected because stale validate checks for orphaned git-tracked files, not
      registry completeness. Mixing the two concerns in one command makes
      diagnostics ambiguous — a file could be both unregistered AND stale, and
      the operator would not know which fix to apply.

      - **Extend `generated.files.validate` (RFC-0375) with OWN-02 diagnostic**:
      Rejected because `generated.files.validate` checks the forward direction
      (declared files exist), while OWN-02 is the reverse direction (entries
      matching no file). Although conceptually adjacent, combining them would
      mix two different comparison directions in one command, making the
      diagnostic output harder to interpret. A separate command keeps each
      validator's scope clean.

      - **Instrument `writeFileIfChanged` to track written files**: Rejected
      because it requires modifying the shared write utility across all
      packages, creating a coupling between the write primitive and the
      validation layer. Filesystem diff after `build.prepare` is simpler and
      sufficient.
    file: docs/rfcs/archive/implemented/rfc-0612-add-ownership-sync-validate-command-for-generated-file-registry-drift-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0613
    title: Accept null lastModified and fix YAML null parsing in markdown twin
      provenance
    status: implemented
    alternativesText: >-
      - **Replace `parseMarkdownTwinFrontmatter` with the `yaml` package**:
      Rejected because the hand-rolled parser is intentionally lightweight (no
      dependency on `yaml` for the provenance parsing path). The fix is a
      one-line change; replacing the parser would be a larger refactor with no
      additional benefit.

      - **Make `lastModified` optional instead of accepting `null`**: Rejected
      because RFC-0320 requires `lastModified` to be present in the frontmatter.
      Removing it from required fields would break the provenance contract.
      Accepting `null` preserves the field's presence while allowing
      deterministic output.
    file: docs/rfcs/archive/implemented/rfc-0613-accept-null-lastmodified-and-fix-yaml-null-parsing-in-markdown-twin-provenance.md
  - kind: rejected-alternative
    rfcId: RFC-0614
    title: Expand bordbuch conflict auto-resolution to include public well-known
      bordbuch paths in mission reconcile
    status: implemented
    alternativesText: >-
      - **Auto-resolve all delete/modify conflicts**: Rejected because
      non-bordbuch delete/modify conflicts may indicate genuine data loss (e.g.,
      a generated file was deleted in the workpiece but modified in the cache
      clone). Only bordbuch paths are safe to auto-resolve because they are
      cache-clone-only artifacts.

      - **Move bordbuch artifacts out of `public/.well-known/`**: Rejected
      because `public/.well-known/bordbuch.json` is a public API endpoint
      (RFC-0604) that must remain at that path for external consumers.
    file: docs/rfcs/archive/implemented/rfc-0614-expand-bordbuch-conflict-auto-resolution-to-include-public-well-known-bordbuch-paths-in-mission-reconcile.md
  - kind: rejected-alternative
    rfcId: RFC-0615
    title: Harden mission.validate with dist cleanup and behavior snapshot
      auto-regeneration
    status: implemented
    alternativesText: >-
      - **Warn-only mode for SNAP-01**: Rejected because a stale snapshot is a
      genuine validation failure that should be fixed, not warned about.
      Auto-regeneration is the correct fix — the snapshot is deterministic and
      should always match the current build.

      - **Clean dist/ in build.prepare instead of mission.validate**: Rejected
      because `build.prepare` is also used outside mission validation (e.g.,
      `mission.preview`), and cleaning dist/ there could remove artifacts needed
      by other consumers. `mission.validate` is the right scope because it runs
      a fresh build anyway.

      - **Manual-only snapshot regeneration**: Rejected because it requires
      operator intervention for a deterministic operation. The operator's time
      is better spent on genuine validation failures, not mechanical snapshot
      updates.
    file: docs/rfcs/archive/implemented/rfc-0615-harden-mission-validate-with-dist-cleanup-and-behavior-snapshot-auto-regeneration.md
  - kind: rejected-alternative
    rfcId: RFC-0616
    title: Re-entrant werkstatt locks by PID
    status: implemented
    alternativesText: >-
      1. **Pass lock ownership through pipeline phases** — Instead of making
      locks re-entrant, pass the acquired lock object to `runPipelinePhase` and
      have sub-commands check whether the lock is already held by the caller.
      Rejected: requires changing every pipeline phase signature and every
      sub-command that acquires locks. High blast radius for a narrow problem.


      2. **Remove lock acquisition from `bordbuch.generate`** — Since
      `release.prepare` already holds the `system:<id>` lock, the inner command
      could skip locking entirely when called from a pipeline. Rejected:
      `bordbuch.generate` is also called standalone (outside `release.prepare`).
      Removing its lock would break standalone usage.


      3. **Use a global in-memory lock registry** — Track acquired locks in a
      `Map<scope, depth>` per process instead of relying on file-based depth.
      Rejected: the lock is file-based for inter-process visibility; an
      in-memory registry would diverge from the on-disk state and complicate
      stale detection.
    file: docs/rfcs/archive/implemented/rfc-0616-re-entrant-werkstatt-locks-by-pid.md
  - kind: rejected-alternative
    rfcId: RFC-0617
    title: Compass audit baseline in mission materialization
    status: implemented
    alternativesText: >-
      1. **Run `compass.audit.baseline` in `mission.validate`** — Seed the
      ledger during validation before mission close. Rejected: `release.prepare`
      can be run after `mission.close` without `mission.validate`, so the ledger
      might still be empty.


      2. **Run `compass.audit.baseline` in `release.prepare` before
      `build.post`** — Seed the ledger at the start of the release pipeline.
      Rejected: this couples release preparation with audit bookkeeping.
      `release.prepare` should not mutate the audit ledger; that belongs in the
      mission lifecycle.


      3. **Remove `--strict` from `compass.audit.validate` in `build.post`** —
      Downgrade the release gate from hard fail to warning. Rejected: this
      weakens DNA-43 enforcement. The strict gate is intentional for releases.
    file: docs/rfcs/archive/implemented/rfc-0617-compass-audit-baseline-in-mission-materialization.md
  - kind: rejected-alternative
    rfcId: RFC-0618
    title: Cache-buster for leitstand.promote build-identity fetch
    status: implemented
    alternativesText: >-
      1. **`Cache-Control: no-cache` header** — Send a no-cache header with the
      fetch. Rejected: CDN edge nodes may not honor request-side `Cache-Control`
      headers for cached responses. Cloudflare's behavior with request-side
      headers is inconsistent and depends on edge configuration.


      2. **Retry with delay** — On 404, wait 2-3 seconds and retry. Rejected:
      adds latency and is non-deterministic. The retry might still hit the
      cached 404 if the cache TTL hasn't expired. The cache-buster is
      deterministic.


      3. **Purge CDN cache after deploy** — Call Cloudflare API to purge the
      `build-identity.json` URL after `leitstand.propagate`. Rejected: requires
      Cloudflare API credentials in the propagate flow, adds complexity, and is
      adapter-specific. The cache-buster is adapter-agnostic.
    file: docs/rfcs/archive/implemented/rfc-0618-cache-buster-for-leitstand-promote-build-identity-fetch.md
  - kind: rejected-alternative
    rfcId: RFC-0619
    title: Bypass command-result cache during mission materialization
    status: implemented
    alternativesText: >-
      1. **Include `site.directory` in the cache key (RFC-0390 amendment).**
      Rejected because the cache is designed for repeated runs against the same
      directory. Adding the directory path to the key would make every workpiece
      replacement a full cache miss, which is correct for materialization but
      would also reduce hit rates for normal pipeline runs where the directory
      changes between site resolutions (e.g., switching between mission
      workpiece and `apps/` directory). The `force` flag already provides a
      targeted bypass for the materialization case.


      2. **Clear the `command_results` cache namespace before materialization.**
      Rejected because it is heavy-handed — it clears cache entries for all
      sites, not just the one being materialized. This would slow down
      concurrent pipeline runs for other sites. The `force` flag provides a
      per-invocation bypass without affecting other consumers.


      3. **Make `executeKernelPipeline` detect workpiece replacement
      automatically.** Rejected because it would require the pipeline executor
      to understand materialization semantics (comparing directory paths,
      checking creation timestamps). This couples the cache layer to the mission
      lifecycle, violating separation of concerns. The caller
      (`mission.materialize`) knows when the directory is fresh and should
      explicitly request cache bypass.
    file: docs/rfcs/archive/implemented/rfc-0619-bypass-command-result-cache-during-mission-materialization.md
  - kind: rejected-alternative
    rfcId: RFC-0620
    title: Filter workspace-absolute generated files from mission materialization
      data-path copy
    status: implemented
    alternativesText: >-
      1. **Point removal of bordbuch files only.** Rejected because it is
      fragile — every new workspace-absolute generated file would require a
      manual update to the removal list in `mission-materialize.ts`. The
      ownership-map-driven filter is self-maintaining.


      2. **Add `bordbuch.generate` to the `build.prepare.dev` pipeline.**
      Rejected because `bordbuch.generate` writes to the cache clone
      (`resolveCachePath`), not to the workpiece. Even if added to the dev
      pipeline, it would not write bordbuch projections to the workpiece — the
      orphan files from the data-path copy would remain.


      3. **Exempt `public/.well-known/bordbuch*` from `ownership.sync.validate`
      via `STATIC_ASSET_EXEMPT_DIRS`.** Rejected because it masks the root cause
      (files that should not be in the workpiece) rather than fixing it.
      Exempting paths from ownership validation weakens the validator's coverage
      and creates a precedent for adding more exemptions instead of fixing data
      hygiene.


      4. **Do not copy `public/` at all — let all generated files be created by
      the dev pipeline.** Rejected because `public/` also contains authored
      static assets (textures, uploaded media, favicon sources) that are not
      generated by any pipeline command. These must be copied from the cache
      clone.
    file: docs/rfcs/archive/implemented/rfc-0620-filter-workspace-absolute-generated-files-from-mission-materialization-data-path-copy.md
  - kind: rejected-alternative
    rfcId: RFC-0622
    title: Auto-commit after each operator request via forge skill
    status: implemented
    alternativesText: >-
      1. **Windsurf hook (`.windsurf/hooks.json`)** — a post-edit hook that runs
      `git commit` after every file edit. Rejected: too granular (one commit per
      file edit), risks committing incomplete states, and is IDE-specific (not
      portable to other agent runtimes).


      2. **AGENTS.md rule only** — a governance rule in `AGENTS.md` without a
      forge skill. Rejected: rules in `AGENTS.md` are advisory and depend on the
      agent reading and following them. A forge skill is a structured
      instruction that the agent loads and executes, with frontmatter metadata
      for validation and discovery.


      3. **Forge command (`forge.step.commit`)** — an executable command module
      that performs the commit. Rejected: the commit logic (detect changes,
      stage, write message, commit) is agent reasoning, not deterministic code.
      The agent knows which files it changed and what message to write — a
      command would need to be told both, adding a layer without value.


      4. **Part of existing skill (`fo-fix` or `fo-doc-audit`)** — add the
      commit step to an existing skill. Rejected: `fo-fix` is specific to the
      fix workflow; `fo-doc-audit` is specific to documentation sync.
      Auto-commit after every request is a cross-cutting concern that needs its
      own skill, callable from any context.
    file: docs/rfcs/archive/implemented/rfc-0622-auto-commit-after-each-operator-request-via-forge-skill.md
  - kind: rejected-alternative
    rfcId: RFC-0623
    title: Add retry with backoff for transient Cloudflare API failures in wrangler
      deploy
    status: implemented
    alternativesText: >-
      - **Always retry on any exit 1:** Rejected — causes unnecessary 30-60s
      delays for authentication errors, syntax errors, and build failures that
      will never succeed on retry.

      - **Retry at the command level (leitstand) instead of adapter:** Rejected
      — the adapter is the correct layer for deploy-specific retry logic. The
      command layer doesn't know about wrangler internals or stderr patterns.
      Also, rollback uses the same adapter and would need duplicate retry logic.

      - **Exponential backoff with jitter:** Rejected as over-engineering for 2
      retries. Fixed delays (30s, 60s) are simpler and sufficient for transient
      Cloudflare API outages.

      - **Circuit breaker pattern:** Rejected — the Leitstand is not a
      high-frequency caller. Circuit breakers are for services making many calls
      per second, not for manual deployment commands.
    file: docs/rfcs/archive/implemented/rfc-0623-add-retry-with-backoff-for-transient-cloudflare-api-failures-in-wrangler-deploy.md
  - kind: rejected-alternative
    rfcId: RFC-0624
    title: Add post-deploy CDN cache purge for cloudflare-workers Leitstand commands
    status: implemented
    alternativesText: >-
      - **Purge everything (zone-level):** Rejected —
      `alt-warpgogol-com.syrokomskyi.workers.dev` is on the shared `workers.dev`
      zone where zone-level purge is not available. URL-level purge is the only
      option that works for both alt and main channels. Also, purge everything
      causes a cache miss spike for all visitors.

      - **Purge in the adapter instead of the command:** Rejected — the adapter
      does not have access to behavior snapshot routes. The command layer reads
      the snapshot and knows all route URLs. Putting purge in the adapter would
      limit it to homepage + build-identity only. Also, the adapter should
      remain portable and focused on deploy.

      - **Purge in adapter + command (defense in depth):** Rejected as
      over-engineering — two purge API calls, two error handling paths, and
      duplication. If command-level purge is fast enough (it is), adapter-level
      purge adds complexity without value.

      - **No delay between purge and health check:** Rejected — Cloudflare purge
      propagation takes a few seconds. Without a delay, health check may still
      see stale cache. 6 seconds is a reasonable fixed delay.

      - **Adaptive delay (poll build-identity until new releaseId):** Rejected
      as over-engineering — a fixed 6s delay is simpler and sufficient for the
      vast majority of cases.

      - **Blocking purge failure:** Rejected — the deploy already succeeded.
      Blocking on purge failure would prevent health check and leave the release
      in an ambiguous state. Non-blocking warning is the correct behavior.
    file: docs/rfcs/archive/implemented/rfc-0624-add-post-deploy-cdn-cache-purge-for-cloudflare-workers-leitstand-commands.md
  - kind: rejected-alternative
    rfcId: RFC-0625
    title: Enforce RFC and ADR implementation completion with process gates and
      drift detection
    status: implemented
    alternativesText: >-
      - **Add more prose to AGENTS.md only.** Rejected: the existing
      PREFERENCES.md already has a mandatory pre-response checklist for RFC
      implementation. Documentation alone did not prevent the agent from
      skipping the stamp step — the problem is that agents working manually from
      the plan never encounter the skill pipeline.


      - **Make V-32 an error.** Rejected: an in-progress implementation with
      intermediate `implement:` commits would break `rfc.validate` for the
      entire repository until the stamp is run. This is too aggressive for a
      workflow that legitimately has intermediate commits. Warning is the right
      severity.


      - **Add a `--check-stamp` flag to `rfc.validate`.** Rejected: the check
      should always run as part of validation. Making it opt-in means agents
      will forget to use it. V-32 is a standard rule, not a flag.


      - **Add a separate `rfc.implementation.drift.check` command.** Rejected: a
      separate command adds maintenance burden and requires agents to know about
      it. Integrating into `rfc.validate` and `adr.validate` ensures it runs
      automatically.


      - **Rely on the plan template and skill gates only (no V-32).** Rejected:
      the plan template and skill gates only work when the agent follows the
      skill pipeline. V-32 catches the case where the agent worked manually and
      skipped the pipeline entirely. Defense in depth requires all three layers.


      - **Scan all git history instead of since `createdAt`.** Rejected:
      `implement:` commits cannot exist before the RFC was created. Scanning
      since `createdAt` is sufficient and faster.
    file: docs/rfcs/archive/implemented/rfc-0625-enforce-rfc-and-adr-implementation-completion-with-process-gates-and-drift-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0626
    title: Eliminate mission workflow friction from timestamp allowlist gaps and
      bordbuch pipeline side-effects
    status: implemented
    alternativesText: >-
      ### Phase 1: Data-flow analysis instead of allowlist parity check


      **Rejected.** Automated data-flow analysis to distinguish runtime
      timestamp usage (e.g., comparing claim validity windows) from
      generated-file timestamp fields (e.g., writing `createdAt` to output)
      would require a TypeScript AST analyzer with taint tracking. This is
      disproportionate to the problem — the allowlist approach has zero false
      positives, is human-readable, and the parity check ensures no module is
      missed. Data-flow analysis would add maintenance burden and potential
      false negatives.


      ### Phase 1: Manual allowlist expansion with comment reminder


      **Rejected.** Relying on developers to remember adding modules to
      `TIMESTAMP_ALLOWLIST` when they add them to `GENERATOR_OWNERSHIP_MAP` is
      the same manual-discipline approach that caused the original gap. A
      comment reminder in `GENERATOR_OWNERSHIP_MAP` does not enforce anything —
      it is advisory only. The parity check is automated and fail-hard.


      ### Phase 2: Write bordbuch projections to workpiece instead of cache
      clone


      **Rejected.** Routing `bordbuch.generate` output to the workpiece
      (`missions/<id>/workpiece/`) instead of the cache clone would require
      changing path resolution in multiple validators
      (`generated.files.validate` RFC-0375, `generated.stale.validate` RFC-0600)
      and would violate RFC-0473's path conventions (projections live next to
      the ledger in the cache clone). It would also require a fallback path for
      non-mission `build.prepare` invocations where no workpiece exists. The
      auto-commit post-step achieves the same result (clean cache clone) with
      minimal blast radius.


      ### Phase 2: Make `bordbuch.generate` a no-op in `build.prepare` context


      **Rejected.** RFC-0604 added `bordbuch.generate` to `build.prepare`
      specifically to ensure bordbuch projections are regenerated before
      validation. Making it a no-op would break RFC-0604's success signals
      ("After running build.prepare,
      systems/<id>/public/.well-known/bordbuch.json exists and is up to date")
      and reintroduce the missing-output problem that RFC-0604 was created to
      solve.


      ### Phase 2: Add `--auto-commit` flag to `bordbuch.generate`


      **Rejected.** Adding a flag to `bordbuch.generate` would mix generation
      and git-commit responsibilities in a single command, violating
      single-responsibility principle. The `bordbuch.generate` MODULE_CONTRACT
      explicitly states: "Does not append events — use bordbuch.append for
      that." Adding git commit would expand the contract scope. A separate
      pipeline step keeps `bordbuch.generate` clean and follows the RFC-0580
      pattern of separate commit helpers.
    file: docs/rfcs/archive/implemented/rfc-0626-eliminate-mission-workflow-friction-from-timestamp-allowlist-gaps-and-bordbuch-pipeline-side-effects.md
  - kind: rejected-alternative
    rfcId: RFC-0627
    title: Add dev deployment channel with Axiom verification gate
    status: implemented
    alternativesText: >-
      1. **Keep local server for Axiom checks.** Run `mission.check` in preview
      mode (local static server) before `leitstand.propagate`, without a dev
      channel. Rejected: a local server on `127.0.0.1` does not test real DNS,
      TLS, CDN caching, or Cloudflare Workers runtime. Accessibility violations
      that only manifest under real HTTP conditions are invisible. The operator
      explicitly wants real internet verification.


      2. **Run Axiom on alt channel, not a separate dev channel.** Deploy to
      alt, run Axiom on alt URL, then promote to main. Rejected: alt is already
      the staging channel — adding Axiom checks to alt would mix verification
      with staging. If Axiom fails, alt is polluted with a broken release. The
      dev channel provides isolation: broken releases stay on dev and never
      reach alt.


      3. **Manual Axiom check with `--skip-axiom` bypass for emergencies.**
      Rejected by operator decision: the Axiom gate is absolute. No bypass flag.
      If the release has accessibility errors, it stays stuck in `dev-deployed`
      until fixed. This maximizes reliability.


      4. **Keep `--channel` on `leitstand.rollback`.** Rejected: the operator
      confirmed the auto-step model. Removing `--channel` reduces cognitive load
      during incidents — the system knows the current state and rolls back one
      step automatically.
    file: docs/rfcs/archive/implemented/rfc-0627-add-dev-deployment-channel-with-axiom-verification-gate.md
  - kind: rejected-alternative
    rfcId: RFC-0628
    title: "Amend dev deployment channel: workpiece-based dev deploy with
      pre-release Axiom verification"
    status: implemented
    alternativesText: >-
      1. **Two-cycle model (fast dev-deploy + full release deploy):** Rejected
      by operator. Maintaining two separate deployment cycles adds complexity
      and confusion. One cycle must be both fast and reliable.


      2. **`leitstand.deploy --skip-validation` flag:** Rejected. Breaks the
      RFC-0627 contract (`--release` required, state machine). Adding bypass
      flags to existing commands creates ambiguity about which path is "real".


      3. **Separate Axiom command (`leitstand.axiom.verify`):** Rejected. Splits
      the dev cycle into two commands (deploy + verify), adding friction. The
      operator wants one command that does everything.


      4. **Keep `leitstand.deploy` with `--workpiece` flag:** Rejected.
      Overloads the command's semantics — `leitstand.deploy` was designed for
      release-based deployment. A new command with clear semantics is cleaner.


      5. **Keep `dev-deployed` state for tracking:** Rejected. No release exists
      at dev-deploy time, so the state cannot be set on any release manifest.
      Registry tracking for dev is unnecessary (operator decision: dev is
      ephemeral).
    file: docs/rfcs/archive/implemented/rfc-0628-amend-dev-deployment-channel-workpiece-based-dev-deploy-with-pre-release-axiom-verification.md
  - kind: rejected-alternative
    rfcId: RFC-0629
    title: Migrate mission.check to native axiom capsules with
      automated-web-accessibility methodology
    status: implemented
    alternativesText: >-
      1. **Minimal fix: only fix CSP stripping in current implementation**.
      Rejected — the current implementation has three compounding issues (CSP,
      double browser launch, Playwright version mismatch). Fixing only CSP
      leaves the other two and the ad-hoc evidence format. The next dev-deploy
      would still be fragile.


      2. **Hybrid: use PlaywrightEvidenceDriver for capture, keep current
      evidence format**. Rejected — the ad-hoc `evidence-capsule.yaml` and
      `findings.yaml` formats lack digest-backed integrity, capability
      manifests, and closure decisions. They cannot scale to multi-site
      longitudinal studies. Keeping them would require a converter layer that
      adds complexity without value.


      3. **Full axiom-runtime kernel module**. Rejected — registering
      axiom-runtime as a kernel module is a larger architectural change (module
      interface, command table, kernel config). It is not needed for this
      migration; `mission.check` can import from axiom packages without being a
      kernel module. This can be a separate RFC if needed later.


      4. **Keep local mode alongside external-preview**. Rejected — the local
      mode (build + static server) is never used in the dev-deploy pipeline.
      `leitstand.dev-deploy` always uses `--external-preview`. Keeping local
      mode would mean maintaining two code paths, one of which is untested and
      broken.
    file: docs/rfcs/archive/implemented/rfc-0629-migrate-mission-check-to-native-axiom-capsules-with-automated-web-accessibility-methodology.md
  - kind: rejected-alternative
    rfcId: RFC-0630
    title: Harden mission.check capture contract and reliability for production pilot
    status: implemented
    alternativesText: >-
      1. **Fix only toolProfile and timeout, defer locales and
      configurability**. Rejected — running the pilot with `en-US` locale on a
      `de`/`uk` site would produce inaccurate browser locale settings, giving
      false confidence in the gate. The pilot must produce trustworthy evidence.


      2. **Add i18n auto-detection inside axiom-capture**. Rejected —
      axiom-capture is a general-purpose package that should not depend on
      warpgogol's `system.md` format. Locale resolution is a `mission.check`
      concern (it knows about missions); axiom-capture receives locales via the
      capture contract.


      3. **Auto-install chromium in pre-flight check**. Rejected —
      auto-installing browser binaries is a side-effect that belongs in setup
      scripts, not in a check command. The pre-flight check should fail fast
      with an actionable message; the operator installs chromium separately.


      4. **Multi-locale capture profiles (page × locale matrix)**. Rejected —
      for URL-based multilingual sites like warpgogol-com (`/de/...`,
      `/uk/...`), each page has a single language determined by the URL path
      segment. Capturing each page with each locale profile (94×2=188 captures)
      would not produce different axe-core results — axe-core uses the page's
      `lang` attribute, not the browser locale. Page-language matching (one
      capture per page with the correct locale) is sufficient and halves capture
      time.


      5. **Store capture contract overrides in `system.md`**. Rejected — adding
      axiom-specific fields to `system.md` couples site authoring to the
      verification tool. Flags are sufficient; `leitstand.dev-deploy` can pass
      system-specific values if needed in the future.


      6. **Crawlee Dataset purge before each run**. Rejected — `contractDigest`
      hashes the entire contract including `recordedAt = new
      Date().toISOString()`, which changes on every run. The storage name
      `axiom-${digest.slice(0,24)}` is unique per run, so stale data does not
      exist. `purgeRequestQueue: false` in `crawlee-discovery-executor.ts`
      preserves the request queue for the current run's deduplication, which is
      correct behavior.
    file: docs/rfcs/archive/implemented/rfc-0630-harden-mission-check-capture-contract-and-reliability-for-production-pilot.md
  - kind: rejected-alternative
    rfcId: RFC-0631
    title: Add site-authored favicon SVG source with buildIconSvg fallback
    status: implemented
    alternativesText: >-
      - **Site provides favicon.svg directly in public/** — rejected because
      `public.icons.generate` overwrites `public/favicon.svg` on every run. The
      source must live outside `public/` to avoid being overwritten.

      - **Flag-based override (`--source-svg <path>`)** — rejected because
      file-presence-based detection is simpler, requires no flag management, and
      follows the convention of other content-driven overrides in the kernel.

      - **Generalize to all generated artifacts (og-image, preview)** — rejected
      as premature. Each generator has different requirements (HTML rendering,
      screenshots). The pattern can be extracted to a separate RFC if it proves
      useful.

      - **Remove `buildIconSvg` entirely, require all sites to provide SVG** —
      rejected because it breaks the zero-config default for new sites and adds
      onboarding friction.
    file: docs/rfcs/archive/implemented/rfc-0631-add-site-authored-favicon-svg-source-with-buildiconsvg-fallback.md
  - kind: rejected-alternative
    rfcId: RFC-0632
    title: Auto-wrap maskable icons with Android safe-zone when no explicit maskable
      source
    status: implemented
    alternativesText: >-
      - **Keep `favicon-maskable.svg` as optional escape hatch** — rejected. No
      site has used it since RFC-0631. It adds contractual surface area (extra
      file, extra validation, ICON-SRC-03) without value. Auto-wrap from
      `favicon.svg` is simpler and correct for the common case. If a site needs
      a completely different maskable design in the future, a new RFC can
      reintroduce the escape hatch.

      - **Bitmap-level safe-zone via sharp composite** — rejected. SVG-level
      transform preserves vector quality, is deterministic, and produces a
      readable `public/favicon.svg` (for maskable). Bitmap composite would
      require rasterizing twice and compositing — more complex, less
      deterministic, and no readable intermediate SVG.

      - **85% safe zone (conservative)** — rejected. Google's standard is 80%.
      While 85% is safer for unusual masks (droplet, diamond), 80% is the
      industry baseline and produces larger visible content. Operators who need
      more padding can adjust their `favicon.svg` source.

      - **75% safe zone (aggressive)** — rejected. Too aggressive — risks
      clipping on standard circle and squircle masks.

      - **No ICON-SRC-04 diagnostic** — rejected. The warning prompts operators
      to visually verify maskable icons, which is important since auto-wrap is a
      heuristic — edge elements may still be too close to the 80% boundary for
      some designs.
    file: docs/rfcs/archive/implemented/rfc-0632-auto-wrap-maskable-icons-with-android-safe-zone-when-no-explicit-maskable-source.md
  - kind: rejected-alternative
    rfcId: RFC-0633
    title: Add axiom.report command for operator-facing HTML triage report
    status: implemented
    alternativesText: >-
      1. **Skill-only approach (fo-axiom-report skill).** An LLM skill that
      reads evidence JSON and writes HTML, similar to `fo-architecture`.
      Rejected because: (a) non-deterministic — same JSON could produce
      different HTML depending on LLM session; (b) cannot be called from
      `leitstand.dev-deploy` pipeline automatically; (c) requires an agent
      session to generate a report, adding friction for the operator.


      2. **Extend `renderReportHtml` in `check-core`.** Reuse the existing HTML
      rendering function. Rejected because: (a) `check-core` serves the
      check-warpgogol evidence-graph ecosystem, not Axiom capsules — the input
      types are incompatible; (b) the existing function produces a barebones
      `<ul>` list, not a triage dashboard; (c) `check-core` is a
      schema-and-logic package without I/O, and `axiom.report` needs filesystem
      access.


      3. **Add HTML rendering inside `mission.check` itself.** Have
      `mission.check` write `report.html` alongside the JSON files. Rejected
      because: (a) violates single-responsibility — `mission.check` is a
      check/capture command, not a presentation command; (b) `mission.check` can
      fail before writing evidence, and the report would be lost; (c) standalone
      re-generation of the report (e.g. after changing the HTML template) would
      require re-running the entire check, which takes minutes.
    file: docs/rfcs/archive/implemented/rfc-0633-add-axiom-report-command-for-operator-facing-html-triage-report.md
  - kind: rejected-alternative
    rfcId: RFC-0634
    title: Unify deployment identity across dev, alt, and main channels with
      build-identity verification at every promotion step
    status: implemented
    alternativesText: >-
      - **Set `PUBLIC_SITE_URL` to the channel URL during build.** Rejected: the
      open-source page component fetches from `Astro.url.origin`, which is
      derived from the `site` config. Setting `PUBLIC_SITE_URL` per channel
      would require different env vars for dev, alt, and main builds. This is
      fragile — easy to forget, and it still relies on a runtime fetch that
      fails if the origin is unreachable during build. Local file read is
      simpler and deterministic.


      - **Make the open-source page SSR (not prerendered).** Rejected: would
      require changing `output: "static"` or adding `export const prerender =
      false` to the open-source page. This adds runtime cost (Worker execution
      for every request) for a page that is fundamentally static. The local file
      read solves the problem without changing the rendering strategy.


      - **Add a `workpieceId` field to `buildIdentitySchema`.** Rejected: the
      schema already has `releaseId` which can hold `workpiece-<missionId>` for
      workpiece identity. Adding a field complicates the schema and the
      verification logic. Instead, the `releaseId` regex is loosened to accept
      both release IDs and workpiece IDs. The operator confirmed: use the same
      schema, `releaseId` doubles as workpiece identifier.


      - **Verify `behaviorSnapshotHash` for dev→alt.** Rejected: workpiece
      build-identity does not have a behavior snapshot (only `release.prepare`
      computes one). Verifying an empty string against a real hash would always
      fail. The `behaviorSnapshotHash` verification remains alt→main only (where
      both sides have real values from `release.prepare`).


      - **Keep the runtime fetch but fix `Astro.url.origin`.** Rejected: under
      `output: "static"`, `Astro.url.origin` is the `site` config value, not the
      request URL. There is no reliable way to make it reflect the channel URL
      during prerender. The fetch approach is fundamentally incompatible with
      static prerendering for per-channel metadata.
    file: docs/rfcs/archive/implemented/rfc-0634-unify-deployment-identity-across-dev-alt-and-main-channels-with-build-identity-verification-at-every-promotion-step.md
  - kind: rejected-alternative
    rfcId: RFC-0635
    title: Reuse distribution in mission.validate when build-input-hash matches
    status: implemented
    alternativesText: >-
      - **Skip only `build.prepare` + `astro build`, keep `build.check`:**
      Rejected because `build.check` validates generated files from
      `build.prepare`. If `build.prepare` is skipped, the generated files from
      the previous run must be on disk. While they would be identical (hash
      matched), running `build.check` without `build.prepare` creates a false
      sense of freshness. Instead, `build.check` is moved into `mission.build`,
      so a reused distribution has already passed all checks.


      - **Skip only `astro build`, keep `build.prepare` + `build.check` +
      `build.post`:** Rejected because `build.prepare` includes expensive media
      transcoding steps (image variants ~60s, video variants ~180s, live
      variants ~120s). Skipping only `astro build` would still take 5+ minutes
      for unchanged content.


      - **No `--force` flag, delete `build-input-hash.json` manually:** Rejected
      as poor UX. The `--force` pattern is already established in the kernel
      cache (RFC-0390) and is the expected operator escape hatch.
    file: docs/rfcs/archive/implemented/rfc-0635-reuse-distribution-in-mission-validate-when-build-input-hash-matches.md
  - kind: rejected-alternative
    rfcId: RFC-0636
    title: Formalize conditional flag semantics in GENERATOR_OWNERSHIP_MAP
    status: implemented
    alternativesText: >-
      - **Remove the `conditional` flag entirely.** Make all entries
      non-conditional. Rejected because transient files like
      `build-identity.json` only exist during builds. Without `conditional:
      true`, `generated.files.validate` would report GEN-FILES-01 when the file
      is absent (which is the normal state outside of builds), and
      `ownership.sync.validate` would report OWN-02 for the same reason.


      - **Add a separate `coverageExempt` flag.** Introduce a new boolean field
      for entries that should be excluded from coverage checks. Rejected because
      it adds unnecessary complexity. The current fix (removing the `continue`
      in `generated.stale.validate`) is a one-line change that aligns the
      validator with the already-correct behavior of `ownership.sync.validate`.
      The `conditional` flag's semantics are clear once documented: skip absence
      checks, not coverage checks.


      - **Document the contract in AGENTS.md only, without an RFC.** Rejected
      because `GENERATOR_OWNERSHIP_MAP` is a cross-workspace contract used by
      all sites. An AGENTS.md paragraph lacks the formality and traceability of
      an RFC, and does not amend the existing RFCs (RFC-0600, RFC-0612,
      RFC-0375) that define the validators.
    file: docs/rfcs/archive/implemented/rfc-0636-formalize-conditional-flag-semantics-in-generator-ownership-map.md
  - kind: rejected-alternative
    rfcId: RFC-0637
    title: Granular module hash for command-result cache via modulePaths declaration
    status: implemented
    alternativesText: >-
      - **Transitive import-graph analysis:** Hash the command's source file
      plus all files it transitively imports within the package. Rejected
      because it requires static import analysis (parsing import statements,
      resolving specifiers, following re-exports), which is complex, fragile,
      and adds a significant dependency. The `modulePaths` declaration is
      simpler, explicit, and auditable.


      - **Hash only the command's source file (no transitive imports):**
      Rejected because `execute()` typically imports helpers from other files.
      If those helpers change but the command file doesn't, the cache would
      return a stale result. This is a false cache hit — the worst possible
      failure mode for a cache.


      - **Hash per subdirectory (e.g., `command-tables/`, `validators/`,
      `lib/`):** Rejected because commands import across subdirectories (e.g., a
      command in `command-tables/09-build-artifacts.ts` imports from
      `../image-variants.ts` and `../lib/`). Subdirectory-level hashing would
      still invalidate many commands when a shared directory like `lib/`
      changes, and wouldn't capture top-level file dependencies.
    file: docs/rfcs/archive/implemented/rfc-0637-granular-module-hash-for-command-result-cache-via-modulepaths-declaration.md
  - kind: rejected-alternative
    rfcId: RFC-0638
    title: Domain-Neutral Profile Schema Extensions
    status: implemented
    alternativesText: >-
      - **Profile schema v2 (`forge/stack-profile@2`)**: Rejected. A new schema
      version would require migration logic and break existing consumers. The
      extension is purely additive — optional fields in v1 are sufficient.

      - **Domain-specific profiles as separate npm packages**: Rejected for now.
      The domain fields are small enough to live in the profile schema. External
      profile packages can be added later if the profile directory grows
      unmanageably.

      - **Hardcoded domain detection in forge.create**: Rejected. Hardcoding
      domain detection logic in the command handler would couple forge to
      specific domains. The profile schema approach keeps forge domain-agnostic.
    file: docs/rfcs/archive/implemented/rfc-0638-domain-neutral-profile-schema-extensions.md
  - kind: rejected-alternative
    rfcId: RFC-0639
    title: Semantic Bindings Schema Extensions
    status: implemented
    alternativesText: >-
      - **Replace software-specific keys with semantic keys**: Rejected. This
      would break all existing forge.yaml files and require a v2 schema.
      Additive extension is safer.

      - **Domain-specific key namespaces (e.g. `commands.video.render`)**:
      Rejected. This couples the bindings schema to specific domains. Semantic
      keys (`produce`, `validate`, `verify`) are domain-neutral and work for all
      domains.

      - **Terminology as a separate config file**: Rejected. Terminology is
      per-project and per-domain — it belongs in bindings (per-project) and
      profiles (per-domain), not in a separate file.
    file: docs/rfcs/archive/implemented/rfc-0639-semantic-bindings-schema-extensions.md
  - kind: rejected-alternative
    rfcId: RFC-0640
    title: Domain-Aware Project Bootstrapping and Health Checks
    status: implemented
    alternativesText: >-
      - **Domain-specific forge.create variants (e.g. `forge.create.video`)**:
      Rejected. This couples forge to specific domains. The profile-driven
      approach keeps forge domain-agnostic.

      - **Separate doctor command per domain (e.g. `forge.doctor.video`)**:
      Rejected. Same reason — forge stays domain-agnostic, the profile drives
      behavior.

      - **Hardcoded domain detection in doctor**: Rejected. Hardcoding detection
      logic in the command handler couples forge to specific domains.

      - **Unit test instead of CLI command for `forge.profile.validate`**:
      Rejected. While a unit test in `packages/forge/src/tests/` could validate
      profiles against the Zod schema, `forge.skill.validate` sets the precedent
      for forge-internal validation as a CLI command. The command also runs as
      an advisory check inside `forge.doctor` and supports standalone execution
      during profile development, which a unit test cannot do.
    file: docs/rfcs/archive/implemented/rfc-0640-domain-aware-project-bootstrapping-and-health-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0641
    title: Editframe Video Stack Profile
    status: implemented
    alternativesText: >-
      - **Editframe React profile (`editframe-react`)**: Deferred. The HTML
      composition profile is the simplest entry point. A React variant can be
      added as a separate profile later.

      - **Generic video profile (`video-generic`)**: Rejected. Editframe has
      specific CLI commands, composition elements, and rendering workflows. A
      generic video profile would be too abstract to provide useful defaults.

      - **Editframe profile as external npm package**: Deferred. If Editframe
      profiles grow to multiple variants (HTML, React, Next.js), they can be
      extracted to a separate `@editframe/forge-profiles` package. For now, one
      profile in forge core is sufficient.
    file: docs/rfcs/archive/implemented/rfc-0641-editframe-video-stack-profile.md
  - kind: rejected-alternative
    rfcId: RFC-0642
    title: Domain-Neutral Skill Language Audit
    status: implemented
    alternativesText: >-
      - **Remove software-specific keys entirely**: Rejected. Software projects
      still use `typecheck`, `test`, `scopedBuild`. Removing them would break
      existing forge.yaml files.

      - **Per-skill domain declaration**: Rejected. Skills should be
      domain-neutral by default. Domain-specific skills belong in skill packs
      (RFC-0539), not in forge core.

      - **Automatic terminology substitution in skill bodies**: Rejected. Skill
      bodies are markdown files read by AI agents. Automatic substitution at
      read time would be fragile and non-deterministic. Skills should reference
      binding keys directly.
    file: docs/rfcs/archive/implemented/rfc-0642-domain-neutral-skill-language-audit.md
  - kind: rejected-alternative
    rfcId: RFC-0643
    title: Per-Domain AGENTS.md Template Generation
    status: implemented
    alternativesText: >-
      - **Per-domain AGENTS.md command (e.g. `forge.agents.generate.video`)**:
      Rejected. The profile-driven approach keeps forge domain-agnostic. One
      command, profile-driven output.

      - **Manual AGENTS.md editing for non-software domains**: Rejected.
      Operators should not need to manually edit generated files. The generation
      should be domain-aware from the start.

      - **Jinja-style template engine**: Rejected. The `{{terminology.key}}`
      placeholder syntax is sufficient for terminology substitution. A full
      template engine would add complexity without value.
    file: docs/rfcs/archive/implemented/rfc-0643-per-domain-agents-md-template-generation.md
  - kind: rejected-alternative
    rfcId: RFC-0644
    title: Auto-commit dirty workpiece before mission.reconcile
    status: implemented
    alternativesText: >-
      1. **Block reconcile on dirty workpiece** — this is the **current
      behavior** (`isWorkpieceDirty` guard at lines 893-898). Rejected: relies
      on manual discipline and creates workflow friction for the common case of
      generated files from `build.prepare`. The operator explicitly chose
      auto-commit over blocking.


      2. **Auto-commit only generated files** — commit only files matching
      `GENERATOR_OWNERSHIP_MAP` paths, leaving manual edits dirty. Rejected:
      partial commit creates a confusing state where some changes are committed
      and others are left behind. The operator explicitly chose `git add -A`
      (all changes).


      3. **Add `commitWorkpieceIfDirty` to other lifecycle commands**
      (mission.materialize, mission.close). Rejected as non-goal: only
      `mission.reconcile` fetches from the workpiece and needs a clean tree.
      Other commands operate on the workpiece in-place and do not transfer
      state.
    file: docs/rfcs/archive/implemented/rfc-0644-auto-commit-dirty-workpiece-before-mission-reconcile.md
  - kind: rejected-alternative
    rfcId: RFC-0645
    title: Eliminate TIMESTAMP_ALLOWLIST and source-scanning timestamp validator
    status: implemented
    alternativesText: >-
      1. **Keep both validators, automate allowlist via data-flow analysis** —
      RFC-0626 non-goal explicitly rejected this: "Do not replace the manual
      allowlist with automated data-flow analysis to distinguish runtime
      timestamp usage from generated-file timestamp usage — the allowlist
      approach is sufficient and maintainable." Experience has shown the
      allowlist approach is NOT sufficient — it creates recurring friction. This
      RFC goes further than RFC-0626 anticipated and eliminates the need for any
      allowlist by removing source-scanning entirely.


      2. **Replace source-scanning with output-file scanning** — scan generated
      output files for ISO datetime patterns instead of scanning source code.
      Rejected: `generated.drift.validate` already does this better by comparing
      dryRun output against committed files. Adding another output-scanning
      check would be redundant with drift detection.


      3. **Keep `generated.timestamp.validate` as a fast pre-check** —
      source-scanning is faster than dryRun re-invocation. Rejected: the speed
      difference is negligible (source-scanning reads files, drift detection
      re-runs generators in dryRun mode which is already fast). The maintenance
      burden of the allowlist outweighs any speed benefit.
    file: docs/rfcs/archive/implemented/rfc-0645-eliminate-timestamp-allowlist-and-source-scanning-timestamp-validator.md
  - kind: rejected-alternative
    rfcId: RFC-0646
    title: Add retry resilience to bordbuch.commit for transient git failures
    status: implemented
    alternativesText: >-
      - **Non-fatal warning instead of retry**: Make `bordbuch.commit` log a
      warning and continue the pipeline on failure. Rejected because dirty
      bordbuch files block `mission.reconcile` — silencing the failure delays
      the problem, it does not solve it.

      - **Retry + non-fatal fallback**: Retry first, then warn if retries
      exhausted. Rejected by the operator — the pipeline should fail if retries
      are exhausted, forcing the operator to investigate the root cause.

      - **Increase `gitExec` timeout**: Increase the 30-second timeout to 120
      seconds. Rejected because timeout is not the only transient failure mode —
      lock contention and concurrent process conflicts also cause failures.
      Retry addresses all transient modes, not just timeout.

      - **Use `git --no-optional-locks`**: Avoid lock contention by using
      `--no-optional-locks` flag. Rejected because it only addresses lock
      contention, not timeouts or other transient failures. Can be used in
      combination with retry but does not replace it.
    file: docs/rfcs/archive/implemented/rfc-0646-add-retry-resilience-to-bordbuch-commit-for-transient-git-failures.md
  - kind: rejected-alternative
    rfcId: RFC-0647
    title: Add Playwright Chromium pre-flight check to build.post pipeline
    status: implemented
    alternativesText: >-
      - **Inline auto-install in `print.pdf.generate`**: Catch the launch error
      and auto-install inside the print.pdf.generate handler. Rejected because
      `independent-qa` has the same dependency — duplicating auto-install logic
      in each Playwright-dependent command is not maintainable. A shared ensure
      command is cleaner.

      - **Pre-flight in `build.prepare`**: Add the ensure step at the end of
      `build.prepare` instead of `build.post`. Rejected because `build.prepare`
      runs codegen steps that do not need Playwright. Adding it to `build.post`
      keeps it close to the Playwright-dependent steps.

      - **Keep `mission.materialize` inline `ensurePlaywrightChromium`**: Do not
      extract to a command, just call the function from the pipeline. Rejected
      because a registered command is visible in `command.manifest.generate`,
      composable in pipelines, and testable in isolation. The inline function in
      `mission-materialize.ts` is not reusable by `build.post` without importing
      across package boundaries.
    file: docs/rfcs/archive/implemented/rfc-0647-add-playwright-chromium-pre-flight-check-to-build-post-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0648
    title: Enforce main branch for Sternsystem repos
    status: implemented
    alternativesText: >-
      - **Global git config `init.defaultBranch main`:** Rejected because it is
      a local machine setting, not enforceable across all environments (CI
      runners, new developer machines, containers). The convention must be
      enforced per-repo by a validation command, not by a global config that may
      or may not be set.


      - **Dynamic branch detection everywhere (no hard convention):** Rejected
      because the operator explicitly wants a hard convention (`main`, not
      `master`). Dynamic detection (already used in `mission.reconcile` via `git
      rev-parse --abbrev-ref HEAD`) works for the happy path but does not
      prevent `master`-branch repos from being created. The fallback defaults in
      `sternsystem.status` and `mission.close` would still need to pick a name,
      and without a convention, the choice is arbitrary.


      - **ADR instead of RFC:** Rejected because this change adds a validation
      rule to `sternsystem.validate` (a Site OS command) and establishes a
      cross-workspace contract for all Sternsystem repos. Per the fo-idea
      classification table, changes to Site OS commands require an RFC.
    file: docs/rfcs/archive/implemented/rfc-0648-enforce-main-branch-for-sternsystem-repos.md
  - kind: rejected-alternative
    rfcId: RFC-0649
    title: Axiom gate freshness guarantee for dev deploys
    status: implemented
    alternativesText: >-
      1. **Worker URL fallback for Axiom gate**: Run Axiom against the direct
      Worker URL (`*.workers.dev`) when CDN purge fails, so the gate always
      verifies fresh content. Rejected because it hides CDN configuration
      problems — the operator would see passing Axiom results while the CDN
      serves stale content to real visitors. Fatal failure makes CDN
      misconfiguration visible.


      2. **Retry freshness check**: Fetch `build-identity.json` with retry (3
      attempts, 5s interval) to cover CDN edge propagation delay. Rejected for
      dev channel — the existing 6s sleep after purge is sufficient for dev
      iterations. Retry adds ~15s to every deploy for a rare edge case.


      3. **`--force` bypass for freshness check**: Allow operators to skip
      freshness check with `--force` for debugging. Rejected because it creates
      a path to run Axiom on stale content, which is the exact problem this RFC
      solves. If CDN purge is broken, the correct response is to fix the CDN
      configuration, not to bypass the safety check.
    file: docs/rfcs/archive/implemented/rfc-0649-axiom-gate-freshness-guarantee-for-dev-deploys.md
  - kind: rejected-alternative
    rfcId: RFC-0650
    title: Evidence preservation with R2 archive topology
    status: implemented
    alternativesText: >-
      1. **Git LFS evidence repo (Sternsystem-style mirror)**: Store evidence in
      a Git bare repo with Git LFS for `raw/` artifacts, mirroring the
      Sternsystem `mirrors[]` topology. Rejected because Git is not suited for
      153 MB of binary artifacts per run — LFS quota management, repo bloat, and
      slow clone/fetch operations make it impractical. The operator explicitly
      rejected Git for this use case.


      2. **Supabase Storage**: S3-compatible, already integrated for
      `buffer_outbox`. Rejected because the free tier has a 1 GB limit
      (exhausted in ~6 runs) and the paid tier ($25/month for 8 GB) is still
      size-constrained. R2's zero-egress model and larger free tier (10 GB) are
      better suited for 172 MB/run evidence.


      3. **Local timestamped snapshots only**: Change `mission.check` to write
      to `evidence/axiom/runs/{timestamp}/` locally, no remote backup. Rejected
      because it provides no off-site durability — a disk failure destroys all
      history. R2 provides durable storage with 11 nines advertised durability.


      4. **Content-addressed deduplication**: Store raw artifacts by SHA-256
      hash to deduplicate identical screenshots across runs. Rejected by the
      operator — "Дедупликация не нужна: Чем меньше сложность, тем лучше." Dedup
      adds a manifest layer, non-readable keys, and complexity for modest
      storage savings (~30-50% on ~$0.75/month).


      5. **R2 object metadata + ListObjectsV2 (no Data Catalog)**: Use R2 custom
      metadata on objects and `ListObjectsV2` for listing instead of R2 Data
      Catalog. Rejected because the operator specifically requested R2 Data
      Catalog for SQL queryability. Data Catalog enables aggregation queries
      (regression trends, error counts over time) that are not possible with
      object listing.
    file: docs/rfcs/archive/implemented/rfc-0650-evidence-preservation-with-r2-archive-topology.md
  - kind: rejected-alternative
    rfcId: RFC-0651
    title: Evidence sync and fetch commands with S3-compatible R2 client
    status: implemented
    alternativesText: >-
      1. **R2 Data Catalog (Iceberg) for listing**: Use the Iceberg REST catalog
      API to write run metadata rows to the `axiom_evidence_runs` table and
      query them via R2 SQL. Rejected for the initial implementation because
      there is no official Node.js Iceberg client library, the REST catalog API
      has undocumented constraints, and the operator chose `ListObjectsV2` as
      the primary mechanism to reduce implementation risk. Iceberg support can
      be added in a future RFC when the REST catalog API is better understood.


      2. **wrangler r2 CLI instead of S3 SDK**: Use `wrangler r2 object put` via
      `execSync` instead of `@aws-sdk/client-s3`. Rejected because `execSync`
      with `wrangler` is slower (process spawn per file), does not support
      streaming for large files, and requires `wrangler` to be installed in the
      execution environment. The S3 SDK provides proper streaming, error
      handling, and retry logic.


      3. **R2 custom metadata on objects**: Store run metadata (findings count,
      errors count) as R2 custom metadata on the `evidence-metadata.json`
      object. List runs via `ListObjectsV2` + `HeadObject` per object. Rejected
      because `HeadObject` per run adds N API calls, and the initial `--list`
      only needs `runTimestamp` and `commitSha` (available from the 43-byte
      `evidence-metadata.json` via `GetObject`). Full metadata (findings,
      errors, warnings) requires downloading `study-run.json` (~6 MB) which is
      too expensive for a listing operation.


      4. **Single command with subcommands**: `evidence sync`, `evidence fetch`,
      `evidence list` as subcommands of a single `evidence` command. Rejected
      because the Site OS command model uses flat `domain.command` names (e.g.,
      `mission.check`, `mission.close`, `bordbuch.generate`). Subcommands are
      not the established pattern.


      5. **Resumable uploads with multipart**: Use S3 multipart upload for
      `raw/` artifacts larger than 5 MB. Rejected for simplicity — individual
      raw files (screenshots ~200 KB, axe JSON ~500 KB) are well under the 5 MB
      multipart threshold. The largest single file is `observation-bundle.json`
      at ~7 MB, which is a single PutObject call. If file sizes grow
      significantly in the future, multipart can be added without an RFC.
    file: docs/rfcs/archive/implemented/rfc-0651-evidence-sync-and-fetch-commands-with-s3-compatible-r2-client.md
  - kind: rejected-alternative
    rfcId: RFC-0652
    title: Evidence lifecycle integration with mission close and dev deploy
    status: implemented
    alternativesText: >-
      1. **Optional evidence sync in mission.close**: Make `evidence.sync`
      opt-in via a `--sync-evidence` flag instead of mandatory. Rejected because
      the operator explicitly wants full history preservation — an opt-in flag
      would rely on manual discipline, which is the current problem. Making it
      mandatory ensures no evidence is lost.


      2. **Blocking evidence sync in leitstand.dev-deploy**: Make
      `evidence.sync` a blocking step in `leitstand.dev-deploy` — if sync fails,
      the deploy fails. Rejected because dev deploys should not be blocked by
      storage issues. The Axiom gate (RFC-0649) is the blocking quality gate;
      evidence sync is a post-hoc archival step. Best-effort is the right
      semantic.


      3. **Cron-based evidence sync**: Run `evidence.sync` on a schedule (e.g.,
      every hour) instead of integrating into lifecycle hooks. Rejected because
      it adds a cron dependency, introduces latency (evidence not synced until
      the next cron tick), and requires a daemon process. Lifecycle integration
      is simpler and more reliable.


      4. **mission.cleanup deletes all evidence immediately after close**:
      Delete local evidence as soon as `mission.close` syncs to R2. Rejected
      because the operator may need to inspect evidence locally right after
      close (e.g., review findings before approving a release). 30-day retention
      gives a reasonable local window while R2 holds the full history.


      5. **Separate evidence.cleanup command**: Create a new `evidence.cleanup`
      command instead of modifying `mission.cleanup`. Rejected because
      `mission.cleanup` already handles evidence directory preservation — it is
      the natural place for age-based cleanup. Adding a separate command
      increases the command surface without value.
    file: docs/rfcs/archive/implemented/rfc-0652-evidence-lifecycle-integration-with-mission-close-and-dev-deploy.md
  - kind: rejected-alternative
    rfcId: RFC-0653
    title: Accelerate leitstand.dev-deploy via pipeline caching and build skip
    status: implemented
    alternativesText: >-
      1. **Use `build.prepare.dev` pipeline for dev-deploy (RFC-0597)**: The dev
      pipeline skips media transcoding steps. Rejected because it omits critical
      files (adaptive image variants, OG images) needed for a functional
      deployed site and Axiom checks. The operator explicitly rejected this:
      "Нет, этого делать нельзя. Всё, что мы проверяем в Dev, нам нужно в Alt,
      нам нужно на основном сайте."


      2. **Create a `build.prepare.deploy-dev` pipeline**: A custom pipeline
      that includes all necessary deployment files but excludes truly
      unnecessary dev-channel steps. Rejected as over-engineering — the caching
      approach achieves the same speedup without maintaining a separate pipeline
      definition.


      3. **Internal cache only for `print.pdf.generate` (no split)**: Keep
      `cacheable: false` but add internal `.cache/pdf/<hash>/.done` skip logic.
      Rejected because the command would always execute (even if just for the
      internal cache check + copy), missing the full RFC-0390 pipeline-level
      skip benefit. The split approach allows the expensive Playwright step to
      be fully skipped at the pipeline level.


      4. **`buildInputHash` only for skip-build key**: Use the existing
      `computeBuildInputHash` (content tree hash + platform). Rejected because
      it only hashes `src/content/` — changes to `astro.config.mjs`,
      `package.json`, or `tsconfig.json` would not invalidate the cache.
      `commitSha` (workpiece HEAD) catches all workpiece changes.


      5. **Add `public/preview/**` to `reads` for `preview.images.generate`**:
      Self-healing when PNGs are deleted. Rejected because it delays cache hit
      to the third run (first run generates PNGs, second run sees new files in
      `reads` and invalidates, third run finally hits cache). The `reads =
      content only` approach gives cache hit from the second run, matching the
      pattern of `video.variants.generate` and `image.variants.generate`.
    file: docs/rfcs/archive/implemented/rfc-0653-accelerate-leitstand-dev-deploy-via-pipeline-caching-and-build-skip.md
  - kind: rejected-alternative
    rfcId: RFC-0654
    title: Add post-build HTML structural integrity validation
    status: implemented
    alternativesText: >-
      1. **Pre/post snapshot diff**: snapshot tag counts before mutators,
      compare after. More precise (detects which mutator caused the damage) but
      requires storing intermediate state and adding a pre-mutator snapshot
      step. Rejected: final-state tag balance catches the same bugs with simpler
      implementation.


      2. **Full HTML parser validation**: use parse5 to parse each HTML file and
      detect structural errors. More robust but significantly slower (1518
      files) and adds a parser dependency to the build pipeline. Rejected:
      lightweight tag counting is sufficient for the specific class of bug (tag
      removal by regex mutators).


      3. **Landmark presence check**: verify each page contains `<main>`.
      Catches the specific accessibility violation but requires knowledge of
      which landmarks each page should have (page-specific). Rejected: tag
      balance is more general and catches the same bug without per-page
      configuration.
    file: docs/rfcs/archive/implemented/rfc-0654-add-post-build-html-structural-integrity-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0655
    title: Release pipeline atomicity — mission.close must not pre-assign releaseId
    status: implemented
    alternativesText: >-
      - **Single atomic `mission.release` command** (close + prepare + publish
      in one step): Rejected — the three steps have different lock scopes,
      failure modes, and side effects. Combining them reduces flexibility
      without eliminating the need for individual commands.

      - **Auto-run `release.prepare` inside `mission.close`**: Rejected —
      `release.prepare` runs a full build pipeline (~25s) and requires a clean
      workpiece. Forcing this inside `mission.close` would make close
      significantly slower and more failure-prone.

      - **Remove `releaseId` from `mission.close` entirely**: Rejected — the
      field is useful for associating a release with a mission after the fact.
      The problem is consistency, not the field itself.
    file: docs/rfcs/archive/implemented/rfc-0655-release-pipeline-atomicity-mission-close-must-not-pre-assign-releaseid.md
  - kind: rejected-alternative
    rfcId: RFC-0656
    title: Deterministic dist tree hashing — normalize non-deterministic build
      artifacts
    status: implemented
    alternativesText: >-
      - **Exclude non-deterministic files from the hash entirely**: Rejected —
      PDFs and source maps are part of the deployed artifact and should
      contribute to the identity. Excluding them weakens integrity verification.

      - **Make the build itself deterministic (reproducible builds)**: Rejected
      as out of scope — Astro, Playwright PDF generation, and bundler chunk
      naming are third-party tools with their own non-determinism. Normalizing
      at the hash level is more practical.

      - **Use semantic fingerprint mode for all files**: Rejected — `mode:
      "semantic"` replaces the entire hash with an AST-based normalized hash via
      the normalizer registry, losing byte-level change detection for parseable
      files. `mode: "stable"` preserves byte hashing for all files except known
      non-deterministic types, applying targeted normalization only to volatile
      metadata (PDF timestamps, source map paths, JSON timestamp fields). See
      "Stable vs semantic mode distinction" above.
    file: docs/rfcs/archive/implemented/rfc-0656-deterministic-dist-tree-hashing-normalize-non-deterministic-build-artifacts.md
  - kind: rejected-alternative
    rfcId: RFC-0657
    title: Dev-deploy CDN freshness check retry with exponential backoff
    status: implemented
    alternativesText: >-
      - **Increase the fixed sleep from 6s to 30s**: Rejected — adds 24s of
      wasted time when the CDN is fresh after 3s. The retry loop adapts to
      actual CDN latency.

      - **Skip freshness check entirely and always run Axiom gate**: Rejected —
      RFC-0649 established that freshness verification is required before the
      Axiom gate to avoid checking stale content.

      - **Make freshness check asynchronous with a webhook callback**: Rejected
      — over-engineered for a CLI command. The retry loop is simple and
      sufficient.

      - **Add a `--skip-freshness-check` flag**: Rejected — the freshness check
      exists for correctness, not convenience. Skipping it would allow the Axiom
      gate to run on stale content.
    file: docs/rfcs/archive/implemented/rfc-0657-dev-deploy-cdn-freshness-check-retry-with-exponential-backoff.md
  - kind: rejected-alternative
    rfcId: RFC-0658
    title: Protect Bordbuch integrity in cache clone from accidental deletion
    status: implemented
    alternativesText: >-
      - **Double-check in `commitBordbuchProjections`:** Add a guard in
      `bordbuch.commit` that verifies `bordbuch/events.ndjson` is not staged for
      deletion. Rejected — the pre-commit hook is git-level and covers all
      commit paths, including manual `git add -A`. Adding a second check in
      `bordbuch.commit` is redundant and does not cover the actual failure mode
      (manual commits outside of `bordbuch.commit`).


      - **Git attributes filter instead of pre-commit hook:** Use
      `.gitattributes` with a filter to prevent bordbuch modifications. Rejected
      — git filters operate on content, not on file deletion. A `clean`/`smudge`
      filter cannot prevent `git rm`.


      - **bordbuch.validate in `leitstand.dev-deploy` only:** Add validation
      after build in dev-deploy. Rejected — this only covers the dev-deploy
      path. Adding to `build.prepare` covers all paths (dev-deploy,
      mission.validate, mission.build) with a single integration point.


      - **Soft warn in `mission.close`:** Log bordbuch violations as warnings
      but allow close to proceed. Rejected — this allows corrupted bordbuch to
      persist and accumulate more events. Hard fail is consistent with
      `mission.open`, which already blocks on violations.
    file: docs/rfcs/archive/implemented/rfc-0658-protect-bordbuch-integrity-in-cache-clone-from-accidental-deletion.md
  - kind: rejected-alternative
    rfcId: RFC-0659
    title: Cache workpiece artifacts to skip codegen on repeated mission
      materialization
    status: implemented
    alternativesText: >-
      1. **Reuse the RFC-0390 command-result cache for build.prepare.dev during
      materialization.** Rejected by RFC-0619: a fresh workpiece starts empty,
      so a cached "no files modified" result would skip writing generated files,
      leaving the workpiece incomplete. The command-result cache tracks "did the
      output change?" not "write the output to a new location."


      2. **Cache only generated (non-data-path) artifacts, not the entire
      workpiece.** Rejected: caching the entire workpiece (minus .git/ and
      node_modules/) is simpler to implement and faster to restore (one copy
      operation vs. copy data paths + restore generated files separately). The
      data paths are already in the cache clone and are small relative to
      video/media files.


      3. **Eliminate redundant codegen in `generateFullBoilerplate` (11
      generators re-run by build.prepare.dev).** Partial solution: saves ~10-20s
      but does not address the ~60-120s build.prepare.dev pipeline. This RFC
      subsumes this optimization — on cache hit, both `generateFullBoilerplate`
      and `build.prepare.dev` are skipped.


      4. **Skip `pnpm install` when package.json is unchanged.** Rejected as
      standalone optimization: `pnpm install` is already fast with cached deps
      (~10s) and is needed to link the workpiece into the pnpm workspace. The
      artifact cache makes this moot — on cache hit, `pnpm install` still runs
      but is effectively a no-op.
    file: docs/rfcs/archive/implemented/rfc-0659-cache-workpiece-artifacts-to-skip-codegen-on-repeated-mission-materialization.md
  - kind: rejected-alternative
    rfcId: RFC-0660
    title: Knowledge entry schema and lifecycle contract for cumulative skill
      knowledge
    status: implemented
    alternativesText: >-
      - **Pure YAML files per layer** (`learned-principles.yaml`) — rejected:
      loses the freeform body (rationale, examples, Q&A text) that makes entries
      useful to agents and reviewable by humans; contradicts the file-first,
      human-readable philosophy that RFC-0524 and OpenClaw-style systems
      validated.

      - **Heading-inline metadata** (`### K-0007 · confirmations: 4 · created:
      2026-07-20`) — rejected: fragile to parse, no schema validation,
      encourages malformed ad-hoc extensions.

      - **SQLite/FTS5 index as source of truth** (Hermes `state.db` style) —
      rejected: adds a binary artifact that cannot be reviewed in git diffs,
      breaks the npm portability story, and violates the zero-external-services
      constraint. A derived index may be added later for retrieval, but the
      markdown files remain the source of truth.

      - **External memory providers** (Mem0, vector DBs, knowledge graphs) —
      rejected: cloud dependency, cost, privacy, and install complexity
      contradict forge's target audience (non-technical operators, any project
      type). Recorded as a permanent non-goal.
    file: docs/rfcs/archive/implemented/rfc-0660-knowledge-entry-schema-and-lifecycle-contract-for-cumulative-skill-knowledge.md
  - kind: rejected-alternative
    rfcId: RFC-0661
    title: Hot, warm, and cold knowledge layers with token budgets for skill knowledge
    status: implemented
    alternativesText: >-
      - **Hard caps with validation errors** (Hermes-style) — rejected: a
      knowledge file that grows past a limit would break validation for
      unrelated work, punishing the wrong change. Knowledge growth is a
      maintenance signal, not a correctness failure. Warnings preserve the
      signal without the blast radius.

      - **Token-accurate budgets via a tokenizer dependency** — rejected:
      non-deterministic across models, adds a dependency to a portable package,
      and the precision buys nothing at this scale. Characters correlate well
      enough at 4–8 KB granularity.

      - **Automatic truncation/summarization when over budget** — rejected:
      silent loss of knowledge violates the files-as-source-of-truth philosophy.
      Reduction is a human-approved act (RFC-0662/0663).

      - **Per-entry budgets instead of per-file** — rejected: a per-entry cap
      does not bound total read cost, which is the actual problem. Per-entry
      length is already implicitly bounded by what a useful principle looks
      like.
    file: docs/rfcs/archive/implemented/rfc-0661-hot-warm-and-cold-knowledge-layers-with-token-budgets-for-skill-knowledge.md
  - kind: rejected-alternative
    rfcId: RFC-0662
    title: Knowledge compaction command and distillation lifecycle for skill knowledge
    status: implemented
    alternativesText: >-
      - **Extend fo-session-retro with distillation** — rejected: retro already
      triages six routing targets; adding per-skill knowledge distillation
      overloads it and mixes project-knowledge routing with
      skill-self-maintenance. A dedicated skill keeps both predictable.

      - **Fully deterministic distillation** (no skill) — rejected: grouping
      forty Q&A pairs into principles is semantic judgment; a regex cannot do
      it, and shipping a half-working heuristic violates the pragmatism bar.

      - **Cron/CI-scheduled compaction** — rejected: silent, unattended mutation
      of knowledge files contradicts the operator-approval contract the whole
      series enforces. External schedulers remain free to invoke the command
      explicitly.

      - **Single archive file per skill instead of per-layer companions** —
      rejected: per-layer companions (`qa-log.archive.md` next to `qa-log.md`)
      keep co-location (definition beside its source) and let distillation read
      one layer's archaeology without the others'.
    file: docs/rfcs/archive/implemented/rfc-0662-knowledge-compaction-command-and-distillation-lifecycle-for-skill-knowledge.md
  - kind: rejected-alternative
    rfcId: RFC-0663
    title: Cross-skill knowledge promotion from skill-local to shared layer
    status: implemented
    alternativesText: >-
      - **One central knowledge file for all skills** — rejected: destroys
      skill-locality (a skill's knowledge is co-located with the skill for a
      reason) and forces every skill to pay the full context cost of every other
      skill's lessons.

      - **Semantic deduplication with embeddings** — rejected: external
      dependency, non-determinism, and cost for a problem that normalized titles
      solve at current scale. If scale ever outgrows title matching, a derived
      index can be added without changing this contract.

      - **No shared layer; document cross-skill principles in
      writing-great-skills** — rejected: documentation prose is not loadable,
      citable, or countable knowledge; agents running a skill do not re-read
      authoring documentation.

      - **Automatic promotion on detection** — rejected: merging two principles
      is a semantic act (which body wins? are they really the same principle?);
      silent auto-merge violates the operator-approval contract.
    file: docs/rfcs/archive/implemented/rfc-0663-cross-skill-knowledge-promotion-from-skill-local-to-shared-layer.md
  - kind: rejected-alternative
    rfcId: RFC-0664
    title: Project-local file-based agent memory layer
    status: implemented
    alternativesText: >-
      - **Keep Memory DB as the primary Context home** — rejected: locks project
      knowledge to one agent runtime, invisible to other agents and unreviewable
      in git; fails the any-project-type, multi-agent, years-long constraints.

      - **Version everything, including daily logs** — rejected: raw daily
      streams are noisy, may contain transient sensitive context, and create
      commit churn; the operator chose the hybrid (curated versioned, raw local)
      during grilling.

      - **Git-ignore everything (like operator-profile)** — rejected: curated
      project context is exactly what a team (or a future agent clone) needs
      from the repo; privacy-sensitive material already has a home in
      operator-profile.md.

      - **Full RFC-0660 entry schema for memory entries** — rejected: K-id
      metadata per memory bullet costs more to write than the entries are worth
      at this write rate; the dated-file convention provides identity (file =
      day) and ordering for free.

      - **External memory SaaS (Mem0 and peers)** — rejected permanently:
      external service, cost, privacy, install complexity (series-wide non-goal,
      RFC-0660).
    file: docs/rfcs/archive/implemented/rfc-0664-project-local-file-based-agent-memory-layer.md
  - kind: rejected-alternative
    rfcId: RFC-0665
    title: Configurable Axiom methodologies with per-methodology gate for alt
      deployment
    status: implemented
    alternativesText: >-
      1. **Per-methodology separate passes (separate mission.check calls)**:
      Rejected — 8 Playwright passes would be 8x slower. One capture + many
      instruments is more efficient and matches the current architecture.


      2. **Config in registry.yaml under each system**: Rejected — the operator
      wants one config for the entire workshop, not per-system. Methodologies
      are a workshop-level concern, not per-site.


      3. **Gate with weighted aggregation**: Rejected — too complex for the
      current need. `all-must-pass` with per-methodology `blockOn` is simple,
      predictable, and covers the real use cases. Weighted aggregation can be
      added later if needed.


      4. **Hardcoded high/critical from any methodology**: Rejected — too rigid.
      Different methodologies need different thresholds (e.g.,
      performance-vitals should only block on critical, not high).
      Per-methodology `blockOn` gives this control.


      5. **Config in pure YAML (not MD with frontmatter)**: Rejected — MD with
      frontmatter is preferred by the operator. It allows documentation
      alongside the config and is consistent with other Werkstatt config files.
    file: docs/rfcs/archive/implemented/rfc-0665-configurable-axiom-methodologies-with-per-methodology-gate-for-alt-deployment.md
  - kind: rejected-alternative
    rfcId: RFC-0666
    title: Replace secretsFile env-var indirection with convention-based
      .env.alt/.env.main paths in Leitstand
    status: implemented
    alternativesText: >-
      1. **Workpiece-relative env-var resolution.** Keep `secretsFile:
      env:WERKSTATT_SECRETS_DEV` but resolve relative to workpiece path.
      Rejected — still requires operator to set an env var for no benefit. The
      env var is dead configuration that adds indirection without value.


      2. **New `.env.dev` file.** Create a separate `.env.dev` for the dev
      channel. Rejected — dev and alt are the same staging environment with the
      same secrets. `mission.materialize` does not create `.env.dev`. Adding it
      would require schema changes to materialize for no functional difference.


      3. **Stable path outside `missions/`.** Store secrets in
      `secrets/<systemId>/.env.dev` (gitignored). Rejected — introduces a new
      directory and `.gitignore` management. The workpiece already has
      `.env.alt`/`.env.main` that are preserved across re-materialization.


      4. **Keep `secretsFile` as dead optional field.** Rejected — forward-only
      ecosystem. Dead code is removed, not kept.
    file: docs/rfcs/archive/implemented/rfc-0666-replace-secretsfile-env-var-indirection-with-convention-based-env-alt-env-main-paths-in-leitstand.md
  - kind: rejected-alternative
    rfcId: RFC-0667
    title: Axiom Audit ID Boundary Contract
    status: implemented
    alternativesText: >-
      1. **Full rename `missionId` → `auditId` in all werkstatt interfaces**:
      Rejected. `missionId` is a fundamental concept in werkstatt's mission
      lifecycle (DNA-46). Renaming it would break dozens of interfaces,
      commands, and state files for no benefit — the external Axiom rename does
      not require an internal werkstatt rename.


      2. **No adapter, direct `auditId` everywhere**: Rejected. This would
      couple werkstatt internals to external Axiom's naming choices. If Axiom
      renames `auditId` again, werkstatt would need to change all internal code.
      The boundary adapter pattern isolates this.


      3. **Wrapper type in `axiom-study` that accepts both**: Rejected. This
      would require changing the external Axiom package, which has its own
      governance. The adapter pattern is cleaner — werkstatt owns the boundary.
    file: docs/rfcs/archive/implemented/rfc-0667-axiom-audit-id-boundary-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0668
    title: Axiom Gate Pipeline Resilience for Dev Deploys
    status: implemented
    alternativesText: >-
      1. **Retry on any non-zero exit code**: Rejected. Content violations (exit
      1) are deterministic — retrying produces the same findings. Only
      infrastructure errors (exit 2) are transient.


      2. **No timeout, rely on `maxDurationMs`**: Rejected. `maxDurationMs`
      controls the external Axiom CLI's internal capture deadline, but does not
      cover hangs in Chromium launch, Crawlee initialization, or Node.js event
      loop blocking. An external timeout is a safety net.


      3. **30-minute timeout**: Rejected per operator decision. 15 minutes is
      sufficient for 100 URLs × 2 locales with 1s rate limiting. 30 minutes
      would mask hangs.


      4. **Pre-flight in `leitstand.dev-deploy`**: Rejected. `mission.check` is
      also called standalone (not just via `leitstand.dev-deploy`). The
      pre-flight must be in `mission.check` to protect all callers.


      5. **Parallelize capture loop**: Rejected for this RFC. Parallelization is
      a performance optimization, not a resilience fix. It would change the
      external Axiom CLI's capture logic, which is outside werkstatt's
      governance.
    file: docs/rfcs/archive/implemented/rfc-0668-axiom-gate-pipeline-resilience-for-dev-deploys.md
  - kind: rejected-alternative
    rfcId: RFC-0669
    title: Context checkpoint between batch items in full-pipeline orchestrator
    status: implemented
    alternativesText: >-
      ### Automatic `fo-handoff` between RFCs


      After each RFC completes, automatically invoke `fo-handoff` to create a
      handoff document in the OS temp directory, then start the next RFC as if
      picked up by a fresh agent.


      **Rejected**: `fo-handoff` is designed for cross-session continuity — it
      compacts the conversation into a document for a different agent to pick
      up. Using it between RFCs within the same session would break the session
      continuity, require the agent to read its own handoff document, and add
      I/O overhead. The checkpoint block achieves the same context release
      without the overhead of a full handoff document.


      ### External context manager command


      A new Site OS command (e.g., `session.checkpoint`) that tracks context
      usage and forces a context reset between batch items.


      **Rejected**: the LLM context window is a platform-level constraint
      managed by the LLM provider, not by Site OS commands. A command cannot
      programmatically clear or compress the LLM's context window — it can only
      emit instructions that the LLM interprets. The checkpoint directive
      achieves the same effect through skill text, which is the appropriate
      layer for LLM behavior instructions. Adding a command would create the
      illusion of mechanical enforcement where none is possible.
    file: docs/rfcs/archive/implemented/rfc-0669-context-checkpoint-between-batch-items-in-full-pipeline-orchestrator.md
  - kind: rejected-alternative
    rfcId: RFC-0670
    title: Mid-RFC context checkpoint after each plan step in full-pipeline
      orchestrator
    status: implemented
    alternativesText: >-
      ### Automatic fo-handoff between plan steps


      After each plan step, invoke `fo-handoff` to create a handoff document,
      then continue as if picked up by a fresh agent.


      **Rejected**: same rationale as RFC-0669 — `fo-handoff` is designed for
      cross-session continuity, not intra-session context management. The I/O
      overhead of writing and reading handoff documents for every plan step
      would be prohibitive.


      ### Sub-RFC splitting


      Split large RFCs into smaller RFCs so each has fewer plan steps.


      **Rejected**: some RFCs are inherently large (e.g., timestamp determinism
      across 20+ modules). Splitting them would create artificial boundaries and
      cross-RFC dependencies that are harder to manage than step-level
      checkpoints. The RFC granularity should match the decision granularity,
      not the context management concern.
    file: docs/rfcs/archive/implemented/rfc-0670-mid-rfc-context-checkpoint-after-each-plan-step-in-full-pipeline-orchestrator.md
  - kind: rejected-alternative
    rfcId: RFC-0671
    title: Progress beacon after each pipeline step in full-pipeline orchestrator
    status: implemented
    alternativesText: >-
      ### Structured progress JSON


      Emit a JSON progress object after each step instead of a one-line beacon.


      **Rejected**: JSON is machine-readable but human-unfriendly. The beacon is
      for the operator, not for tooling. A one-line human-readable format is
      more useful.


      ### Progress in commit messages


      Rely on commit messages to indicate pipeline progress.


      **Rejected**: commit messages are only visible after the step completes
      and the operator checks git log. The beacon provides real-time visibility
      during step execution.
    file: docs/rfcs/archive/implemented/rfc-0671-progress-beacon-after-each-pipeline-step-in-full-pipeline-orchestrator.md
  - kind: rejected-alternative
    rfcId: RFC-0672
    title: Structured error checkpoint for pipeline step failures in full-pipeline
      orchestrator
    status: implemented
    alternativesText: >-
      ### Auto-abort on first failure


      Stop the pipeline immediately on any failure, without retrying.


      **Rejected**: the existing 2-attempt retry discipline handles most
      transient errors. The error checkpoint only fires when retries are
      exhausted, preserving auto-recovery for simple errors.


      ### Error checkpoint as a file artifact


      Write the error checkpoint to a file (e.g.,
      `docs/errors/error-<rfc-id>.yaml`) instead of conversation output.


      **Rejected**: the error checkpoint is session-scoped — it is only relevant
      during the current session. Writing it to a file would create git churn
      and require cleanup. Conversation output is the right medium for ephemeral
      session state.
    file: docs/rfcs/archive/implemented/rfc-0672-structured-error-checkpoint-for-pipeline-step-failures-in-full-pipeline-orchestrator.md
  - kind: rejected-alternative
    rfcId: RFC-0673
    title: Batch plan preview before multi-document processing in full-pipeline
      orchestrator
    status: implemented
    alternativesText: >-
      ### Mandatory operator approval


      Require the operator to confirm the batch plan before processing starts.


      **Rejected**: the orchestrator's design principle is "no pauses — the
      operator's invocation is the instruction to proceed." A mandatory approval
      gate would violate this principle. The preview is informational; the
      operator can interrupt if they disagree with the order.


      ### Automated dependency resolution


      Use a tool to automatically resolve dependencies between RFCs and
      determine the optimal processing order.


      **Rejected**: RFC dependencies are expressed in `related[]`, `amends[]`,
      and `supersedes[]` frontmatter fields, but these are not strictly
      dependency declarations — they are reference relationships. A full
      dependency graph would require a new semantic layer. The agent's heuristic
      (process in creation order, which usually matches dependency order) is
      sufficient.
    file: docs/rfcs/archive/implemented/rfc-0673-batch-plan-preview-before-multi-document-processing-in-full-pipeline-orchestrator.md
  - kind: rejected-alternative
    rfcId: RFC-0674
    title: Profile-driven lifecycle commands for Forge
    status: implemented
    alternativesText: >-
      - **Hardcoded domain commands (`forge.render`, `forge.preview`)**:
      Rejected — couples Forge to a specific framework, violates DNA-54, bloats
      the npm package with unused domain logic.

      - **Profile plugins (Forge loads a JS module per profile)**: Rejected —
      profiles are YAML data, not code. Executing arbitrary JS from profiles is
      a security and portability risk. Commands are strings executed via
      `child_process.exec`.

      - **Turborepo-only approach (no Forge lifecycle commands)**: Rejected —
      Turborepo handles task orchestration but not profile resolution,
      `--dry-run` command preview, or Forge governance integration. The
      lifecycle commands are thin wrappers that add profile resolution and
      structured output on top of raw CLI commands.
    file: docs/rfcs/archive/implemented/rfc-0674-profile-driven-lifecycle-commands-for-forge.md
  - kind: rejected-alternative
    rfcId: RFC-0675
    title: Profile invariant enforcement in forge.doctor
    status: implemented
    alternativesText: >-
      - **Domain-specific validators (VideoValidator, AudioValidator)**:
      Rejected — couples Forge to specific domains, violates DNA-54, requires a
      new validator class per domain.

      - **External check scripts (profile declares a shell command per
      invariant)**: Rejected — executing arbitrary shell commands per invariant
      is a security risk and too slow for a doctor check. The `check` field uses
      structured declarative kinds (filename-pattern, file-contains,
      file-not-contains) that Forge can evaluate internally.

      - **Leave invariants advisory-only**: Rejected — defeats the purpose of
      declaring invariants. The operator gets false confidence.
    file: docs/rfcs/archive/implemented/rfc-0675-profile-invariant-enforcement-in-forge-doctor.md
  - kind: rejected-alternative
    rfcId: RFC-0677
    title: Profile-driven artifact validation
    status: implemented
    alternativesText: >-
      - **Create separate `forge.validate.artifacts` command**: Rejected after
      audit (AUDIT-RFC-0677-01). The overlap with `forge.validate` in profile
      resolution, flags, and output shape is near-total. Extending
      `forge.validate` with `--artifact` and violation parsing in `--json`
      avoids command-surface bloat.

      - **Domain-specific validators**: Rejected — same reasoning as RFC-0675.
      Couples Forge to specific domains.

      - **External parsing scripts**: Rejected — same reasoning as RFC-0675.
      Security and portability risk.
    file: docs/rfcs/archive/implemented/rfc-0677-profile-driven-artifact-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0678
    title: Profile-driven determinism verification
    status: implemented
    alternativesText: >-
      - **Hardcode determinism checks per domain**: Rejected — couples Forge to
      specific domains, violates DNA-54.

      - **Use `@warpgogol/fingerprint` directly in profiles**: Rejected —
      profiles are YAML data, not code. Forge source handles the hashing.

      - **Single-build hash comparison (no double-build)**: Rejected — a single
      build hash cannot verify reproducibility. The double-build approach is the
      only way to detect non-determinism without a reference hash.

      - **Record reference hashes in profile YAML**: Rejected — reference hashes
      are machine-specific. The double-build approach is machine-agnostic.
    file: docs/rfcs/archive/implemented/rfc-0678-profile-driven-determinism-verification.md
  - kind: rejected-alternative
    rfcId: RFC-0679
    title: Profile-driven asset management
    status: implemented
    alternativesText: >-
      - **Domain-specific asset managers (VideoAssetManager,
      AudioAssetManager)**: Rejected — couples Forge to specific domains,
      violates DNA-54.

      - **Reuse `packages/content-source`**: Rejected — `ContentSourceProvider`
      is designed for site content collections (Markdown/YAML), not binary asset
      files.

      - **External asset management tools**: Rejected — Forge needs to
      understand asset references in composition files, which requires
      profile-driven parsing.
    file: docs/rfcs/archive/implemented/rfc-0679-profile-driven-asset-management.md
  - kind: rejected-alternative
    rfcId: RFC-0680
    title: Profile-driven release lifecycle
    status: implemented
    alternativesText: >-
      - **Reuse `mission.close` + `leitstand.propagate`**: Rejected — these are
      site-specific (Cloudflare Workers deployment, `build-identity.json`
      format). Video projects need a different release flow (object storage,
      `release-manifest.json` format).

      - **Domain-specific release managers**: Rejected — couples Forge to
      specific domains, violates DNA-54.

      - **External release tools (e.g. `release-please`)**: Rejected — these
      handle versioning, not artifact bundling and publishing. Forge needs to
      understand the profile's artifacts to bundle them.
    file: docs/rfcs/archive/implemented/rfc-0680-profile-driven-release-lifecycle.md
  - kind: rejected-alternative
    rfcId: RFC-0681
    title: Cross-platform RTK install in forge-bootstrap skill
    status: implemented
    alternativesText: >-
      - **WSL-only on Windows:** Require WSL2 for RTK on Windows. Rejected —
      adds a hard dependency on WSL2, which not all Windows users have. Cargo
      and prebuilt binaries work natively.

      - **Skip RTK on Windows entirely:** Rejected — RTK's token optimization is
      valuable on all platforms. The operator should have the option.

      - **Bundle RTK in forge npm package:** Rejected — RTK is a machine-level
      tool installed once, not a per-project dependency. Bundling would bloat
      the package.
    file: docs/rfcs/archive/implemented/rfc-0681-cross-platform-rtk-install-in-forge-bootstrap.md
  - kind: rejected-alternative
    rfcId: RFC-0682
    title: Cross-platform test fixes in packages/forge
    status: implemented
    alternativesText: >-
      - **Add `shell: true` with explicit shell path on Windows:** Rejected —
      adds complexity and still requires POSIX-compatible shell (Git Bash).
      Splitting commands is simpler and more portable.

      - **Use `cross-env` package for env vars:** Rejected — `execFileSync` with
      `env` option is built-in and sufficient. No new dependency needed.

      - **Skip Windows test support:** Rejected — the root AGENTS.md mandates
      cross-platform forge. Tests that can't run on Windows are a compliance
      gap.
    file: docs/rfcs/archive/implemented/rfc-0682-cross-platform-test-fixes-in-packages-forge.md
  - kind: rejected-alternative
    rfcId: RFC-0683
    title: RTK prefix in all agent instruction command blocks
    status: implemented
    alternativesText: >-
      - **Strengthen the rule only, without updating examples.** Rejected —
      agents copy examples, not rules. A rule that says "use rtk" while every
      example omits it produces cognitive dissonance and inconsistent behavior.

      - **Add `rtk` only to warpgogol-specific files, not Forge skills.**
      Rejected — Forge skills are the primary instruction source for agents. If
      Forge skills omit `rtk`, agents following skill pipelines will not use
      `rtk`. The graceful degradation rule covers external consumers without
      RTK.

      - **Prefix `ref()` references with `rtk`.** Rejected — `ref()` is an
      abstraction layer (DNA-54). Prefixing it with `rtk` breaks the abstraction
      and confuses agents that do not know about RTK. The rule clarifies that
      `rtk` is added after binding resolution.

      - **Make RTK mandatory.** Rejected — Forge must remain cross-platform and
      work without RTK. RTK is an optimization, not a dependency. Making it
      mandatory would break Forge consumers that do not have RTK installed.

      - **Add a validator that checks for `rtk` prefix in .md files.** Rejected
      — this would produce false positives for `ref()` references, inline code
      mentions, and archived files. The convention is enforced by code review
      and agent discipline, not by an automated validator.
    file: docs/rfcs/archive/implemented/rfc-0683-rtk-prefix-in-all-agent-instruction-command-blocks.md
  - kind: rejected-alternative
    rfcId: RFC-0684
    title: Add Axiom finding suppression layer with per-site config
    status: implemented
    alternativesText: >-
      1. **Suppress in Axiom CLI itself.** Rejected — Axiom is an external tool
      whose job is to find everything. Adding suppression logic to Axiom would
      couple it to Werkstatt-specific pipeline semantics (channels, deployment
      context) and make it less reusable. The boundary between "find everything"
      (Axiom) and "decide what's relevant" (Werkstatt) should be explicit.


      2. **Extend `systems/methodologies.md` (RFC-0665).** Rejected —
      methodologies config controls instrument activation and `blockOn`
      severity. Suppressions are a different concern: which findings from active
      instruments are false positives for the pipeline context. Mixing them in
      one config file would conflate two orthogonal concepts and complicate the
      Zod schema.


      3. **Workshop-level only (no per-site overrides).** Rejected — while the
      four default categories are universal, future sites may have unique
      false-positive patterns (e.g. a site with a non-standard CDN that triggers
      a specific rule). Per-site overrides provide an escape hatch without
      requiring an RFC for each new pattern.


      4. **Per-site only (no workshop defaults).** Rejected — the four known
      categories are universal across all client sites. Without workshop
      defaults, every site would need to duplicate the same suppression rules,
      creating maintenance burden and inconsistency.


      5. **Remove suppressed findings from evidence entirely.** Rejected —
      DNA-59 requires evidence preservation as an append-only archive. Removing
      findings would destroy the audit trail. Marking findings as `suppressed:
      true` preserves the full evidence while excluding them from gate
      decisions.
    file: docs/rfcs/archive/implemented/rfc-0684-add-axiom-finding-suppression-layer-with-per-site-config.md
  - kind: rejected-alternative
    rfcId: RFC-0685
    title: Add workspace tree index and mtime-based cache fast path for pipeline
      acceleration
    status: implemented
    alternativesText: >-
      - **File watcher daemon**: a persistent process that watches the workspace
      and maintains a live tree index. Rejected because it adds operational
      complexity (process lifecycle, crash recovery) for a problem that can be
      solved with a one-shot walk per pipeline run (~50–100ms).

      - **Git-based tree index**: use `git ls-files` to enumerate tracked files
      instead of `readdir`. Rejected because the pipeline also needs mtime+size
      (not just paths), and `git ls-files` does not provide metadata.
      Additionally, untracked files (e.g. generated content in workpieces) would
      be missed.

      - **Persistent mtime database**: store mtimes in a SQLite table that
      persists across pipeline runs. Rejected because cross-run mtime comparison
      is unreliable (clock skew, file replacement with same mtime). The per-run
      index is safer and only ~50ms.

      - **Semantic mode for all files**: keep the current behavior but memoize
      fingerprint results within a pipeline run. Rejected because it doesn't
      solve the N directory walks problem, and semantic fingerprinting of
      content files is still wasted work even when memoized.
    file: docs/rfcs/archive/implemented/rfc-0685-add-workspace-tree-index-and-mtime-based-cache-fast-path-for-pipeline-acceleration.md
  - kind: rejected-alternative
    rfcId: RFC-0686
    title: Add pipeline dependency graph for parallel step execution
    status: implemented
    alternativesText: >-
      - **Manual parallelization in pipeline definitions**: instead of a
      dependency graph, split pipelines into "parallel groups" manually.
      Rejected because it requires pipeline authors to reason about execution
      order rather than dependencies, and it's fragile — adding a step requires
      re-evaluating the entire parallel group structure.

      - **Pipeline-level `parallel: true` flag**: mark entire pipelines as
      parallel. Rejected because it's too coarse — some steps in a pipeline are
      genuinely sequential (e.g. `config.regenerate` must run before
      `surface.generate`).

      - **External task runner (e.g. Turborepo)**: delegate parallelism to
      Turborepo. Rejected because pipeline steps are kernel commands, not
      Turborepo tasks. Turborepo operates at the workspace level, not within a
      single site's pipeline.

      - **Worker threads**: run steps in worker threads instead of concurrent
      async tasks. Rejected because kernel commands use shared filesystem state
      and `tsImport` module loading — worker threads would duplicate module
      loading and complicate the cache layer. Async concurrency is sufficient
      since most steps are I/O-bound.
    file: docs/rfcs/archive/implemented/rfc-0686-add-pipeline-dependency-graph-for-parallel-step-execution.md
  - kind: rejected-alternative
    rfcId: RFC-0687
    title: Add transitive cache skip for validator chains in pipeline execution
    status: implemented
    alternativesText: >-
      - **Pipeline-level cache key**: compute a single hash for the entire
      pipeline and skip all validators if the pipeline-level hash matches.
      Rejected because it's too coarse — a single changed file would invalidate
      the entire pipeline cache, even if only one validator cares about that
      file.

      - **Output-hash-based skip**: after a generator runs, hash its output
      files and store the hash. Validators skip if the output hash matches.
      Rejected because it duplicates the `reads[]` hash mechanism — the
      validator's `reads[]` already covers the generated files. The transitive
      skip adds the "upstream was cached" signal on top of the existing
      `reads[]` check.

      - **Declarative `skipIfCached` flag**: a boolean on validators that says
      "skip if all reads are cached." Rejected because it doesn't capture the
      transitive dependency — a validator might read both generated files and
      authored files. The `validatesOutputs` field specifically tracks which
      generators' outputs are being validated, making the skip decision precise.

      - **Separate `build.check.cached` pipeline**: a minimal pipeline that runs
      only when `build.prepare` was fully cached. Rejected because it duplicates
      the pipeline definition and requires manual maintenance to keep the two
      pipelines in sync.

      - **Keep `reads[]` hash check as safety net (audit finding)**: the
      original RFC design required step 3 (`reads[]` hash check) as a
      NON-NEGOTIABLE safety net before applying transitive skip. Rejected
      because (a) it provides zero performance benefit over RFC-0390's
      per-command cache — the hash computation is the same cost, and the
      validator's own cache check would already produce a cache hit; (b) it
      makes the transitive skip impossible for `cacheable: false` validators
      whose `reads[]` hash is never stored. The revised design skips without
      hash check and relies on `generated.drift.validate` (always runs,
      `cacheable: false`) as the safety net for manual edits.

      - **In-memory process-level cache-hit passing**: pass `cacheHitCommands`
      between pipeline runs via a process-level variable or singleton. Rejected
      because `build.prepare` and `build.check` may run in separate processes
      (e.g., `pnpm run build:prepare` followed by `pnpm run build:check`). The
      JSON file persistence works across processes and is simpler.
    file: docs/rfcs/archive/implemented/rfc-0687-add-transitive-cache-skip-for-validator-chains-in-pipeline-execution.md
  - kind: rejected-alternative
    rfcId: RFC-0688
    title: Add titlePattern field to Axiom suppression schema and document Finding
      field population
    status: implemented
    alternativesText: >-
      1. **Remove `messagePattern` and `descriptionPattern` from the schema.**
      Rejected — a future Axiom version may populate these fields. Removing them
      would break forward compatibility and existing per-site rules. Keeping
      them with a warning is safer.


      2. **Auto-fallback: treat `messagePattern` as `titlePattern` when
      `message` is empty.** Rejected — this silently changes the semantics of
      `messagePattern`. A rule author who writes `messagePattern: "deprecated"`
      expects it to match the message field, not the title field. Explicit
      `titlePattern` is clearer.


      3. **Document the empty fields in AGENTS.md only, without adding
      `titlePattern`.** Rejected — documentation alone does not fix the
      non-functional default rules. Agents will still write `messagePattern`
      rules that silently fail. A schema-level `titlePattern` field with a
      validation warning is actionable.
    file: docs/rfcs/archive/implemented/rfc-0688-add-titlepattern-field-to-axiom-suppression-schema-and-document-finding-field-population.md
  - kind: rejected-alternative
    rfcId: RFC-0689
    title: Invalidate Axiom cache and regenerate behavior snapshot in
      leitstand.dev-deploy
    status: implemented
    alternativesText: >-
      1. **Pass `--no-cache` to `mission.check` in dev-deploy.** Rejected — this
      disables all caching, even when nothing changed. Clearing the cache
      directory before `mission.check` is more precise: it invalidates stale
      captures while allowing Axiom to rebuild the cache from the fresh deploy.


      2. **Add `behavior.snapshot.generate` as a build.post step before
      `behavior.snapshot.validate`.** Rejected — this would regenerate the
      snapshot on every deploy, even when nothing changed. The auto-regeneration
      approach only runs when validation fails, which is more efficient.


      3. **Make `behavior.snapshot.validate` non-blocking (warning only).**
      Rejected — the snapshot exists to catch unintended behavioral changes.
      Making it non-blocking defeats its purpose. Auto-regeneration with
      re-validation is the right balance: it handles the common case (intended
      change) while still blocking on real issues.


      4. **Reuse the existing RFC-0615 pattern from `mission.validate`.**
      Accepted — the RFC-0615 implementation in
      `mission-materialization-commands.ts:445-499` already detects SNAP-01 and
      auto-regenerates. This RFC proposes extracting that logic into a shared
      helper (`autoRegenerateSnapshotOnSnap01`) and reusing it in
      `leitstand.dev-deploy`. The difference is that `mission.validate` runs
      `build.post` via `executeKernelPipeline` (with step-level diagnostics),
      while `leitstand.dev-deploy` runs `pnpm build` via `execSync` (opaque).
      The helper must account for this by running `behavior.snapshot.validate`
      separately after `pnpm build` fails.
    file: docs/rfcs/archive/implemented/rfc-0689-invalidate-axiom-cache-and-regenerate-behavior-snapshot-in-leitstand-dev-deploy.md
  - kind: rejected-alternative
    rfcId: RFC-0690
    title: Add build-time duplicate section heading check for surface pages
    status: implemented
    alternativesText: >-
      1. **Validate at bake time (in `bake.ts` itself).** Rejected — bake
      functions produce virtual route entries, not HTML. The heading text is a
      label string, not yet rendered. Checking at bake time would require
      simulating the full rendering pipeline, which is fragile and couples the
      check to the rendering implementation. Checking the final HTML is more
      reliable.


      2. **Extend `dist.html-structure.validate` to also check heading
      uniqueness.** Rejected — `dist.html-structure.validate` checks tag balance
      (RFC-0654). Adding heading uniqueness would conflate two unrelated
      concerns and complicate the diagnostic schema. A dedicated command is
      clearer.


      3. **Rely on Axiom `landmark-unique` only.** Rejected — the 10-minute
      feedback loop is too slow for development. A build-time check catches the
      issue before deploy, saving a full build-deploy-Axiom cycle.
    file: docs/rfcs/archive/implemented/rfc-0690-add-build-time-duplicate-section-heading-check-for-surface-pages.md
  - kind: rejected-alternative
    rfcId: RFC-0691
    title: Add html-attribute-pattern check kind and Editframe time model invariants
    status: implemented
    alternativesText: >-
      - **Full HTML parser (parse5, cheerio)**: Rejected for portability. The
      invariant engine is in `packages/forge/src/` (portable, no `@warpgogol/*`
      imports). Adding an HTML parser dependency increases bundle size for all
      forge consumers, not just Editframe ones. Regex-based extraction is
      sufficient for attribute validation on well-formed HTML compositions.

      - **External validation script**: Rejected — same reasoning as RFC-0675.
      Couples Forge to external scripts, security and portability risk.

      - **Delegate all validation to `editframe check`**: Rejected — `editframe
      check` is the primary validator, but forge invariants provide
      supplementary domain-specific quality checks that the CLI may not cover
      (e.g. loop attribute scope, CSS time string format).

      - **Extend `file-contains` with element and attribute fields**: Rejected —
      `file-contains` checks raw file content, not parsed elements. Mixing the
      two semantics in one check kind is confusing. A separate
      `html-attribute-pattern` check kind is cleaner.
    file: docs/rfcs/archive/implemented/rfc-0691-add-html-attribute-pattern-check-kind-and-editframe-time-model-invariants.md
  - kind: rejected-alternative
    rfcId: RFC-0692
    title: "Editframe composition skill pack: ef-composition-review and
      ef-render-verify"
    status: implemented
    alternativesText: >-
      - **Project-specific skill pack (RFC-0539)**: Rejected — these skills are
      forge-level, not project-specific. They are referenced by the
      `editframe-html` profile which ships with forge. A project-specific skill
      pack would require each Editframe project to declare the pack in
      `forge.yaml`.

      - **Single combined skill**: Rejected — composition review and render
      verification are distinct activities with different triggers. Combining
      them would make the skill too large and reduce clarity.

      - **Delegate to existing fo-skills**: Rejected — `fo-review` and `fo-fix`
      are generic. Editframe-specific skills provide domain-specific guidance
      that generic skills can't.
    file: docs/rfcs/archive/implemented/rfc-0692-editframe-composition-skill-pack-ef-composition-review-and-ef-render-verify.md
  - kind: rejected-alternative
    rfcId: RFC-0693
    title: "Editframe onboarding skill: ef-onboard"
    status: implemented
    alternativesText: >-
      - **Vendor Editframe skills into forge**: Rejected — Editframe skills are
      MIT-licensed and maintained by Editframe Inc. Vendoring them into forge
      creates a maintenance burden and version drift. The skill installs them
      from the official source (`npm create @editframe`) or references them
      online.

      - **Add FFmpeg check to `forge doctor`**: Rejected for this RFC — it would
      require a `prerequisites` field in the profile schema, which is a schema
      change. The skill checks FFmpeg via shell commands. A future RFC can add
      profile-declared prerequisites with `forge doctor` enforcement if needed.

      - **Make `ef-onboard` a meta skill (like `forge-bootstrap`)**: Rejected —
      `forge-bootstrap` is domain-neutral and handles forge project creation for
      any profile. `ef-onboard` is Editframe-specific — it includes discovery
      questions about video types, assets, and stack preferences that are
      meaningless for other profiles.

      - **Single combined onboarding skill for all profiles**: Rejected —
      different domains have different discovery flows. An Astro site onboarding
      asks about content collections, i18n, and design tokens. An Editframe
      onboarding asks about video types, assets, and rendering. Combining them
      would produce an unfocused skill.

      - **Skip discovery, just scaffold**: Rejected — the Editframe prompt
      explicitly prioritizes discovery before building. "Prioritize getting all
      answers and ideas from the user before you start fetching assets or
      building anything." This minimizes rework and wait time.
    file: docs/rfcs/archive/implemented/rfc-0693-editframe-onboarding-skill-ef-onboard.md
  - kind: rejected-alternative
    rfcId: RFC-0694
    title: Add React template and vendor Editframe domain skills into Forge
    status: implemented
    alternativesText: >-
      - **Separate `editframe-react` profile**: Rejected. Maintaining two
      profiles (HTML + React) doubles invariant maintenance, workspace type
      definitions, and template files. React is a superset of HTML — web
      components work inside React. Standardizing on React simplifies the
      system.

      - **Keep both HTML and React templates with a `templates` field**:
      Rejected. Adding a `templates` map to the profile schema adds complexity
      for a choice that doesn't need to exist. React covers all use cases.

      - **HTML-only template**: Rejected. React provides hooks (`useTimingInfo`,
      `useMediaInfo`, `useRenderData`), TypeScript type safety, and component
      composition. HTML is simpler but less powerful. The operator explicitly
      chose React as the standard.

      - **Copy Editframe skills as-is with `<!-- skill-lint-disable SKILL-11
      SKILL-17 -->`**: Rejected. SKILL-11 (no hardcoded literals) is a DNA-54
      invariant. Blanket-disabling it for 6 skills undermines the bindings
      contract. Selective adaptation with targeted `SKILL-17` disables is the
      correct approach.

      - **Keep `html-attribute-pattern` alongside `attribute-pattern`**:
      Rejected. Having two check kinds for the same purpose is confusing.
      Generalizing to `attribute-pattern` with `elements` array is cleaner and
      handles both HTML and JSX.

      - **Auto-mapping `ef-timegroup` → `Timegroup` in the checker**: Rejected.
      Implicit name transformation is magic — the profile YAML should explicitly
      declare which element names to match. `elements: [ef-timegroup,
      Timegroup]` is explicit and extensible.
    file: docs/rfcs/archive/implemented/rfc-0694-add-react-template-and-vendor-editframe-domain-skills-into-forge.md
  - kind: rejected-alternative
    rfcId: RFC-0695
    title: Add SUPPRESS-VAL-07 warning for titlePattern containing ruleId prefix
    status: implemented
    alternativesText: >-
      1. **Error (not warning) for titlePattern containing ruleId.** Rejected —
      the rule still functions correctly. The redundancy is a style issue, not a
      correctness issue. A warning is sufficient.


      2. **Auto-strip ruleId from titlePattern during validation.** Rejected —
      silently modifying user-authored rules is surprising and destructive. A
      warning with a fix hint is the right level of intervention.


      3. **Check only `startsWith` (not `includes`).** Rejected — the ruleId may
      appear anywhere in the titlePattern (e.g. `titlePattern: "guide for
      runtime-health.console-error detection"`). `includes` catches all
      positions. The false positive risk is low because ruleIds use dotted names
      unlikely to appear in descriptive text.
    file: docs/rfcs/archive/implemented/rfc-0695-add-suppress-val-07-warning-for-titlepattern-containing-ruleid-prefix.md
  - kind: rejected-alternative
    rfcId: RFC-0696
    title: Extend heading uniqueness check to non-section block headings
    status: implemented
    alternativesText: >-
      1. **Scan all block-level elements (no `aria-labelledby` requirement).**
      Rejected — this would produce false positives from layout `<div>` wrappers
      that have headings but are not landmarks. The `aria-labelledby`
      requirement aligns with the axe landmark-unique rule.


      2. **Scan only `<article>` and `<aside>` (not `<div>`).** Rejected —
      `<div>` with `aria-labelledby` is a valid landmark role in ARIA. Excluding
      it would miss potential violations.


      3. **Add a separate HEADING-UNIQ-02 diagnostic for non-section blocks.**
      Rejected — the violation is the same (duplicate heading text on the same
      page). A separate diagnostic adds complexity without value.
    file: docs/rfcs/archive/implemented/rfc-0696-extend-heading-uniqueness-check-to-non-section-block-headings.md
  - kind: rejected-alternative
    rfcId: RFC-0697
    title: Log cache dir size before clearing and extract shared SNAP-01 helper
    status: implemented
    alternativesText: >-
      1. **Log only file count (not total size).** Rejected — file count alone
      doesn't indicate if the cache was unusually large. A cache with 10 files
      of 500 MiB each is more concerning than 100 files of 1 KiB each.


      2. **Extract only the SNAP-01 helper (not the full orchestration).**
      Rejected — the `autoRegenerateSnapshotOnSnap01` helper is already
      extracted, but the detection + re-build loop is the duplicated part.
      Extracting only the helper leaves the duplication.


      3. **Add cache size logging as a separate RFC from SNAP-01 extraction.**
      Rejected — both improvements are small and address the same RFC-0689
      implementation. Combining them reduces RFC overhead.
    file: docs/rfcs/archive/implemented/rfc-0697-log-cache-dir-size-before-clearing-and-extract-shared-snap-01-helper.md
  - kind: rejected-alternative
    rfcId: RFC-0698
    title: Auto-commit generated artifacts after leitstand.dev-deploy build
    status: implemented
    alternativesText: >-
      1. **Non-fatal warning on commit failure** — continue deploy with
      pre-build commitSha if auto-commit fails. Rejected: deploying with a stale
      commitSha in build-identity.json creates incorrect Axiom evidence and
      makes debugging harder. A fatal error forces the operator to resolve the
      issue immediately.


      2. **Two commits (after build.prepare, after build.post)** — commit
      generated files after `build.prepare`, then commit
      `behavior.snapshot.generated.yaml` after `build.post`. Rejected:
      unnecessary complexity. A single commit after the full `pnpm build`
      captures all generated files including the behavior snapshot.


      3. **Direct `git add -A && git commit` instead of `mission.git.commit`** —
      simpler, but duplicates logic and bypasses PASSPORT signing. Rejected:
      `mission.git.commit` is the canonical workpiece commit command and is
      already used by the operator manually.


      4. **`--skip-auto-commit` flag** — allow operator to opt out. Rejected:
      follows RFC-0644 precedent. If the operator has unfinished manual edits,
      they should commit or stash before running dev-deploy. An opt-out flag
      creates a footgun.


      5. **Auto-commit only after `build.prepare`, not `build.post`** — would
      leave `behavior.snapshot.generated.yaml` uncommitted. Rejected: incomplete
      coverage. The behavior snapshot is a generated artifact that should be
      committed like any other.
    file: docs/rfcs/archive/implemented/rfc-0698-auto-commit-generated-artifacts-after-leitstand-dev-deploy-build.md
  - kind: rejected-alternative
    rfcId: RFC-0699
    title: Remove forge. domain from CLI command names
    status: implemented
    alternativesText: >-
      1. **Keep `forge.*` and update README only.** Rejected because the prefix
      is genuinely redundant and the documented examples would remain verbose.

      2. **Add a generic alias table in `forge.yaml`.** Rejected as
      over-engineering. The convention should be built-in, not configured per
      project.

      3. **Rename the binary to something else.** Rejected. The binary name
      `forge` is established and changing it only shifts the redundancy
      elsewhere.
    file: docs/rfcs/archive/implemented/rfc-0699-remove-forge-domain-from-cli-command-names.md
  - kind: rejected-alternative
    rfcId: RFC-0700
    title: Allow leitstand.dev-deploy from release directory without open mission
    status: implemented
    alternativesText: >-
      - **New command `leitstand.redeploy`**: rejected — adds a separate command
      for what is essentially the same operation (deploy to dev). The
      `--release` flag on the existing command is simpler and discoverable.

      - **Extend `leitstand.propagate` to support `--channel dev`**: rejected —
      `propagate` has evidence and release-manifest gates that are inappropriate
      for dev channel (dev is for testing, not production). Mixing the two modes
      in one command creates confusing flag interactions.

      - **Allow `leitstand.dev-deploy` to auto-detect release when mission is
      closed**: rejected — implicit behavior is dangerous. The operator should
      explicitly choose `--release` to avoid accidentally deploying a stale
      release when they intended to open a new mission.
    file: docs/rfcs/archive/implemented/rfc-0700-allow-leitstand-dev-deploy-from-release-directory-without-open-mission.md
  - kind: rejected-alternative
    rfcId: RFC-0701
    title: Make distTreeHash and siteContentHash mismatches warning-only in
      leitstand.propagate when commitSha matches
    status: implemented
    alternativesText: >-
      - **Make all hash checks warning-only**: rejected — `commitSha` is the
      primary integrity check and must remain a hard error. A `commitSha`
      mismatch means the source code is different, which is a genuine integrity
      violation.

      - **Add `--skip-hash-check` flag**: rejected — operators would use it as a
      blanket bypass, losing the diagnostic value of the warnings. The
      `commitSha`-conditional approach is more precise.

      - **Fix build non-determinism root cause**: rejected as a prerequisite —
      full build determinism is a long-term goal (RFC-0656 made progress but did
      not achieve it). This RFC unblocks propagation while determinism work
      continues.

      - **Add `--strict` flag to restore hard errors**: rejected — adds
      complexity for a case that is not needed in practice. If strict checking
      is needed, it can be added later.
    file: docs/rfcs/archive/implemented/rfc-0701-make-disttreehash-and-sitecontenthash-mismatches-warning-only-in-leitstand-propagate-when-commitsha-matches.md
  - kind: rejected-alternative
    rfcId: RFC-0702
    title: Make bordbuch.commit resilient and cover distribution reuse path in
      mission.validate
    status: implemented
    alternativesText: >-
      - **Add a separate `commitAndPushBordbuch` call after `build.prepare` in
      `mission.validate`**: rejected — `bordbuch.commit` already runs inside
      `build.prepare` and commits the same files. A separate call would produce
      redundant commits and use `gitExec` (without retry), which is less
      resilient than `gitExecWithRetry` used by `bordbuch.commit`.

      - **Move bordbuch cleanup to `mission.reconcile`**: rejected — the dirty
      cache clone warning fires during `mission.validate`, and operators expect
      `mission.validate` to leave a clean state. Deferring cleanup to
      `mission.reconcile` adds an unnecessary failure surface.

      - **Suppress the dirty-cache warning instead of cleaning up**: rejected —
      the warning exists because the dirty state is a real problem for
      `mission.reconcile`. Suppressing the warning hides the issue without
      fixing it.

      - **Make `bordbuch.commit` push in addition to committing**: rejected —
      push is handled by `sternsystem.sync` and `commitAndPushBordbuch` in
      mission lifecycle commands. Adding push to a `build.prepare` pipeline step
      would couple build validation to network operations, increasing failure
      surface. See `nonGoals`.
    file: docs/rfcs/archive/implemented/rfc-0702-auto-commit-bordbuch-status-generated-yaml-after-mission-validate.md
  - kind: rejected-alternative
    rfcId: RFC-0703
    title: Enforce platform version bump discipline and auto-pin on mission close
    status: implemented
    alternativesText: >-
      - **Shell-only CI check (no kernel command):** Rejected — not reusable
      locally, not testable, not JSON-structured.

      - **Automatic pin on every `ecosystem.commit`:** Rejected — operator may
      make several platform changes before migrating a site. Auto-pin per commit
      creates registry churn.

      - **Pin on `mission.materialize` (start of mission):** Rejected — pin
      should record the version the site was validated against, not the version
      at the start of work.

      - **Trailer value validation (patch/minor/major):** Rejected — that is
      `platform.consistency.validate`'s responsibility (PC-01, PC-02). This
      command checks process discipline, not semantic correctness.
    file: docs/rfcs/archive/implemented/rfc-0703-enforce-platform-version-bump-and-auto-pin-on-mission-close.md
  - kind: rejected-alternative
    rfcId: RFC-0704
    title: Independent version packages — skip platform bump for packages with
      autonomous npm versions
    status: implemented
    alternativesText: >-
      1. **`--no-platform-bump` flag on `ecosystem.commit`** — require the
      operator or agent to pass a flag for each independent-package commit.
      Rejected: agents would need to know when to pass it, which is the same
      knowledge gap that caused the original bug. The declarative list in
      `forge.yaml` is automatic and does not require agent judgment.


      2. **Auto-detection via `"private": false` in `package.json`** —
      `ecosystem.commit` checks if all staged files belong to packages with
      `private: false`. Rejected: `private: false` means "publishable", not "has
      independent version". A package could be publishable but still tracked at
      the platform version. The explicit list is clearer and does not couple
      commit behavior to a package metadata field that serves a different
      purpose.


      3. **Exclude `packages/forge` from the pre-commit hook** — allow direct
      `git commit` for `packages/forge/**` without `ECOSYSTEM_COMMIT=1`.
      Rejected: this breaks the uniform platform-scope rule and creates an
      exception in the hook. The `ecosystem.commit` path is still valuable for
      audit trail and semantic hash computation.
    file: docs/rfcs/archive/implemented/rfc-0704-independent-version-packages-skip-platform-bump-for-packages-with-autonomous-npm-versions.md
  - kind: rejected-alternative
    rfcId: RFC-0705
    title: Enforce sternsystem.sync in mission lifecycle
    status: implemented
    alternativesText: >-
      - **Blocking sync in reconcile only** — reject if `sternsystem.sync` fails
      during reconcile. Rejected because reconcile is already network-aware (git
      push with retry) and transient GitHub outages would block the entire
      mission flow. The two-phase approach (non-fatal in reconcile, blocking in
      close) gives the operator a window to retry.


      - **Blocking sync in close only** — don't call `sternsystem.sync` from
      reconcile at all, just block in close if mirrors are desynced. Rejected
      because it shifts all sync responsibility to the operator. Reconcile is
      the natural place to attempt sync (it just pushed to bare), and close only
      needs to verify.


      - **Best-effort sync in close (non-blocking)** — log a warning in close if
      mirrors are desynced, but don't block. Rejected because this is the
      current behavior (via `closeReport.mirror.recommendation`) and it doesn't
      enforce anything — the mission closes with desynced mirrors.


      - **Add `sternsystem.sync` as a pipeline step in `build.prepare`** —
      rejected because `build.prepare` runs during `mission.validate`, which is
      too early (reconcile hasn't happened yet). Sync must happen after the
      cache clone has the merged commits.
    file: docs/rfcs/archive/implemented/rfc-0705-enforce-sternsystem-sync-in-mission-lifecycle.md
  - kind: rejected-alternative
    rfcId: RFC-0706
    title: Extend PBP and Ontology for Nachweisregister trust entities
    status: implemented
    alternativesText: >-
      - **Parallel JSON Schema (`nachweisregister-v0.1.schema.json`):** Rejected
      by ADR-0028. Would require duplicate validation, duplicate content
      collection wiring, and duplicate semantic projection.

      - **Consent as Bordbuch metadata (not PBP entity):** Rejected. Consent is
      a business-trust concept that belongs in the PBP trust layer. Bordbuch
      stores the consent lifecycle event (when, who, hash), but the consent
      declaration itself (purposes, channels, data elements) is PBP content.

      - **Nachweis as sub-feature of `trust` entitlement:** Rejected. `trust`
      gates the existing trust pages (claims, disclosures, evidence sources).
      Nachweisregister is a distinct commercial product with its own lifecycle,
      R2 storage, and Bordbuch integration. Combining them would prevent
      independent pricing.

      - **Dedicated `pbp/nachweis-record@1` entity:** Deferred. If Claim field
      proliferation becomes problematic (more than 5 Nachweis-specific optional
      fields), a future `pbp/*@2` namespace may introduce it. See ADR-0028
      Evolution section.
    file: docs/rfcs/archive/implemented/rfc-0706-extend-pbp-and-ontology-for-nachweisregister-trust-entities.md
  - kind: rejected-alternative
    rfcId: RFC-0707
    title: Add Nachweis kernel module with R2 storage and Bordbuch lifecycle
    status: implemented
    alternativesText: >-
      - **Runtime manifest endpoint (Cloudflare Worker):** Rejected for pilot.
      Static generation via `nachweis.manifest.generate` is simpler,
      deterministic, and CDN-cacheable. Revocation requires redeploy (minutes,
      within 4-hour SLA). Runtime endpoint is a future optimization if
      revocation frequency demands it.

      - **Separate `packages/nachweis/` package:** Rejected. Commands depend on
      Bordbuch internals (`appendBordbuchEntry`, `resolveBordbuchPath`), R2
      helpers from `evidence/`, and fingerprint — all in `site-kernel-handoff`.
      A separate package would create circular dependencies or require exporting
      internals.

      - **N3 timestamp token in this RFC:** Deferred. RFC 3161 qualified
      timestamp requires a TSA service integration (e.g. Digicert, Sectigo).
      This is a separate concern from the core lifecycle. Pilot operates at N1
      (source hashed) and N2 (operator signature). N3 is a future RFC.
    file: docs/rfcs/archive/implemented/rfc-0707-add-nachweis-kernel-module-with-r2-storage-and-bordbuch-lifecycle.md
  - kind: rejected-alternative
    rfcId: RFC-0708
    title: Add Nachweis UI components, site pages, and pilot content for warpgogol-com
    status: implemented
    alternativesText: >-
      - **Nachweis as archetype blocks in `packages/ui/src/sections/`:**
      Rejected. Nachweis components are not page sections (hero, CTA, card-grid)
      — they are specialized trust components. Sections are planet-named and
      registered in the archetype registry. Nachweis components are moon-named
      components, closer to `header-component` or `footer-component` in
      structure.

      - **Single `nachweis` component (not 4):** Rejected. Card, list, detail,
      and verify have distinct responsibilities and data shapes. Combining them
      would create a monolithic component with conditional rendering — harder to
      maintain and test.

      - **Defer verify and status pages:** Rejected by operator decision. All 3
      HTML pages and 2 non-HTML endpoints are built upfront, even if verify and
      status have no data for pilot records. This ensures the infrastructure is
      complete and ready for the first published record.

      - **Contextual projections on service pages:** Deferred. Adding Nachweis
      references to service pages (e.g. "See proof for this service") is
      valuable but not needed for pilot. Will be added when records are
      published.
    file: docs/rfcs/archive/implemented/rfc-0708-add-nachweis-ui-components-site-pages-and-pilot-content-for-warpgogol-com.md
  - kind: rejected-alternative
    rfcId: RFC-0709
    title: Formalize NEEDS CLARIFICATION markers in RFC drafts
    status: implemented
    alternativesText: >-
      - **Inline TODO comments:** Rejected — `TODO` is already used informally
      and has no validation. `NEEDS CLARIFICATION` is a distinct, validated
      marker.

      - **Audit-only questions (status quo):** Rejected — audit questions are in
      a separate file and invisible to agents reading the RFC body. Inline
      markers are discoverable without cross-referencing.

      - **Frontmatter field:** Rejected — a frontmatter list of questions loses
      positional context. Inline markers are co-located with the uncertain
      content.
    file: docs/rfcs/archive/implemented/rfc-0709-formalize-needs-clarification-markers-in-rfc-drafts.md
  - kind: rejected-alternative
    rfcId: RFC-0710
    title: Add explore mode for pre-specification idea exploration
    status: implemented
    alternativesText: >-
      - **Explore inside fo-idea (no new skill):** Rejected — `fo-idea` is
      already complex with classification, decomposition, and routing. Adding
      exploration logic would bloat it further. A separate skill keeps concerns
      separated.

      - **Use fo-architecture instead:** Rejected — `fo-architecture` does
      deepening scans of existing code, not forward-looking exploration of new
      ideas. Different purpose.

      - **Store explorations in docs/rfcs/ with a special prefix:** Rejected —
      pollutes the RFC directory with non-governance documents. Separates
      concerns cleanly.

      - **No persistence (chat-only exploration):** Rejected — loses context
      across sessions. The whole point is persistence for future agents.
    file: docs/rfcs/archive/implemented/rfc-0710-add-explore-mode-for-pre-specification-idea-exploration.md
  - kind: rejected-alternative
    rfcId: RFC-0711
    title: Add living feature specs with delta-merge from archived RFCs
    status: implemented
    alternativesText: >-
      - **Auto-generate living specs from code:** Rejected — code reflects
      implementation, not design intent. Living specs capture the "why" and
      "what", not the "how".

      - **Use DNA invariants for feature-level specs:** Rejected — DNA is for
      stable, cross-cutting architectural invariants. Feature-level specs change
      more frequently and are module-scoped. Mixing them would bloat DNA.

      - **Store living specs in docs/rfcs/live/:** Rejected — `docs/rfcs/` is
      for RFCs (change proposals). Living specs are not change proposals — they
      are the current state. `docs/specs/live/` separates them clearly.

      - **Full automation (no operator confirmation):** Rejected — delta
      classification has edge cases (heading matching, supersession).
      Semi-automatic with operator confirmation is safer. Future RFCs may
      automate fully once heuristics are proven.
    file: docs/rfcs/archive/implemented/rfc-0711-add-living-feature-specs-with-delta-merge-from-archived-rfcs.md
  - kind: rejected-alternative
    rfcId: RFC-0712
    title: Add multi-persona design summit skill for complex RFC review
    status: implemented
    alternativesText: >-
      - **Real multi-agent infrastructure (separate agent processes):** Rejected
      — introduces significant infrastructure complexity (inter-agent
      communication, session management, state synchronization). Simulated
      personas in a single session capture 90% of the value at 10% of the
      complexity.

      - **Add personas to fo-idea-audit:** Rejected — the audit is automated and
      structured with seven fixed axes. Adding persona perspectives would bloat
      the audit and make it less deterministic. The summit is interactive and
      separate.

      - **Use fo-review for design review:** Rejected — `fo-review` is a
      post-implementation code review. The summit is a pre-acceptance design
      review. Different timing, different input, different output.

      - **Make summit mandatory for architecture RFCs:** Rejected — adds
      overhead to every architecture RFC, including routine ones. The
      criteria-based suggestion in `fo-idea-plan` is sufficient.
    file: docs/rfcs/archive/implemented/rfc-0712-add-multi-persona-design-summit-skill-for-complex-rfc-review.md
  - kind: rejected-alternative
    rfcId: RFC-0713
    title: Isolate R2 credentials per bucket (nachweis vs axiom-evidence)
    status: implemented
    alternativesText: >-
      - **Single token, both buckets** — rejected: violates least-privilege,
      already documented in AGENTS.md as undesirable.

      - **Bucket-per-site isolation** — rejected: over-engineered for pilot;
      per-feature isolation is sufficient.

      - **Custom credentials object passed to `createR2Client`** — rejected: env
      var resolution is the established pattern; adding a credentials param
      would bifurcate the API.
    file: docs/rfcs/archive/implemented/rfc-0713-isolate-r2-credentials-per-bucket-nachweis-vs-axiom-evidence.md
  - kind: rejected-alternative
    rfcId: RFC-0714
    title: Add nachweis.approve and nachweis.public-derivative commands
    status: implemented
    alternativesText: >-
      - **Single `nachweis.gate.satisfy` command** — rejected: too generic,
      hides the distinct human actions (approval vs. public derivative creation)
      in one opaque command.

      - **Manual Bordbuch editing** — rejected: error-prone, bypasses lock
      acquisition, no structured metadata.

      - **Fold approval into `nachweis.publish`** — rejected: `publish` should
      be a gate check, not a gate satisfier. Mixing satisfaction with
      enforcement violates separation of concerns.

      - **Public derivative as a flag on `nachweis.ingest`** — rejected: the
      public derivative is created after legal review, not at ingestion time.
      Different actor, different timestamp, different file.
    file: docs/rfcs/archive/implemented/rfc-0714-add-nachweis-approve-and-nachweis-public-derivative-commands.md
  - kind: rejected-alternative
    rfcId: RFC-0715
    title: Add N3 cryptographic verification (operator signature + RFC 3161
      timestamp) to Nachweisregister
    status: implemented
    alternativesText: >-
      1. **Extend `nachweis.approve` with inline signing + timestamping (no new
      commands).**
         - Rejected: violates single-responsibility. `approve` would become a mega-command that ingests, signs, timestamps, and approves. Debugging individual failures becomes harder. The operator cannot re-sign without re-approving. The three-command split allows independent re-execution (e.g., re-timestamp after TSA outage without re-signing).

      2. **Reuse the passport Ed25519 key (DNA-34) for Nachweis signatures.**
         - Rejected: the passport key signs Verifiable Credential provenance. Mixing Nachweis operator signatures with VC signing creates a single point of compromise — a leaked passport key would forge both VCs and Nachweis records. A separate key isolates the trust domains.

      3. **Store the signing key in environment variables.**
         - Rejected: env vars are per-site and per-environment. The Nachweis signing key is per-operator (one key signs records across all sites the operator manages). A key file outside the repository is more appropriate and does not leak into process listings or CI logs.

      4. **Store the signing key in Cloudflare Workers secrets for runtime
      signing.**
         - Rejected: the site is static-first (DNA-3, Astro SSG). Signing at build time is consistent with the architecture. Runtime signing would require a Worker, adding latency, cost, and a runtime dependency for a static site.

      5. **Implement client-side signature verification in the browser.**
         - Rejected: the verify page displays verification data (hashes, signatures, timestamps). Visitors verify independently using external tools (e.g., OpenSSL, `openssl ts -verify`). Client-side verification would require shipping a WASM crypto library, adding bundle weight for a niche feature. The `nachweis.verify-signature` command provides server-side verification for the operator.

      6. **Use a single `nachweis.n3` command that signs + timestamps in one
      step.**
         - Rejected: sign and timestamp are independent operations with different failure modes. A TSA outage should not block signing. Re-timestamping (e.g., after TSA key compromise) should not require re-signing. The split allows independent re-execution and independent Bordbuch entries.
    file: docs/rfcs/archive/implemented/rfc-0715-add-n3-cryptographic-verification-operator-signature-rfc-3161-timestamp-to-nachweisregister.md
  - kind: rejected-alternative
    rfcId: RFC-0716
    title: Add Nachweisregister contextual projection on warpgogol-com homepage
    status: implemented
    alternativesText: >-
      - **Nachweis projections on every page:** Rejected. Adding Nachweis
      references to every page would dilute the signal and create maintenance
      burden. Only pages where Nachweis context adds trust value (homepage,
      services, pricing, team, notausgang) receive projections.

      - **Dynamic projections that show published records:** Rejected. No
      records are published yet (both pilot records have `status: draft`).
      Dynamic projections would show nothing. Static projections with CTA to
      `/nachweise/` are appropriate for the pilot phase.

      - **New component for contextual projections:** Rejected. Existing
      `trust-strip` and `transparency` block types are sufficient. Creating a
      new component for each projection point would violate the composition-only
      principle.

      - **Defer all projections until records are published:** Rejected by
      operator decision. The Nachweisregister concept is valuable even without
      published records — the projections explain the verification approach and
      build trust in the process.
    file: docs/rfcs/archive/implemented/rfc-0716-add-nachweisregister-contextual-projection-on-warpgogol-com-homepage.md
  - kind: rejected-alternative
    rfcId: RFC-0717
    title: Remove stale Nachweis surface module blueprint references — Nachweis
      pages are block-declarative, not surface-generated
    status: implemented
    alternativesText: >-
      - **Direct fix without RFC:** The stale entry is dead config that doesn't
      cause validation errors. A direct content data correction would suffice.
      Rejected because the operator chose to document the change as an RFC for
      traceability — it amends RFC-0708's surface configuration decision.

      - **Create `nachweis.yaml` blueprint:** Instead of removing the
      `blueprints` array, create a real blueprint YAML file. Rejected because
      Nachweis pages are not dataset-driven PSEO pages — creating a blueprint
      would be forced and unnecessary.
    file: docs/rfcs/archive/implemented/rfc-0717-remove-stale-nachweis-surface-module-blueprint-references.md
  - kind: rejected-alternative
    rfcId: RFC-0718
    title: Add pre-commit CSS design token validation for staged .css files
    status: implemented
    alternativesText: >-
      1. **Extend `biome.tokens.validate` with a `--staged-only` flag** —
      rejected because `biome.tokens.validate` is a kernel command that requires
      the full site-kernel runtime. Pre-commit hooks must be fast (bash/grep)
      and must not depend on Node.js or pnpm. The pre-commit hook runs in a bare
      shell context where `pnpm exec site-kernel` may not be available.


      2. **TypeScript script instead of bash** — rejected because the existing
      pre-commit hook is entirely bash-based (ENV-CONTRACT-05, ENV-CONTRACT-06,
      command manifest check). Adding a TypeScript script would introduce a
      Node.js dependency in the hook, increasing startup time (~200ms for `node`
      vs ~10ms for `grep`).


      3. **No pre-commit check, rely on `build.check` only** — rejected because
      the problem statement shows that invalid tokens were committed before
      `build.check` caught them. Pre-commit feedback is faster and prevents
      broken commits from entering the repository.
    file: docs/rfcs/archive/implemented/rfc-0718-add-pre-commit-css-token-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0719
    title: Add block archetype body.kind schema validation to page.block.validate
    status: implemented
    alternativesText: >-
      1. **Hardcoded Zod schemas in `page-block-validate.ts`** — Rejected.
      Duplicates the existing JSON Schema fragments in `body-fragments.ts`,
      creating a second source of truth for body shape contracts. Adding a new
      body kind would require editing both the fragment and the hardcoded map.
      The existing dynamic approach (reading `const` from composed schema)
      requires no hardcoded map.


      2. **Extend `section.body.contract.validate` instead of
      `page.block.validate`** — Rejected. `section.body.contract.validate`
      validates archetype YAMLs (the archetype side), not page content entries
      (the block side). The body kind mismatch occurs when a content author
      writes `body.kind: paragraphs` for a block whose archetype declares
      `bodyKind: list` — this is a page-level error, not an archetype-level
      error.


      3. **Rely solely on B-03 strict JSON Schema validation** — Considered but
      insufficient. B-03 catches body shape mismatches via
      `additionalProperties: false` and `required` fields, but the error
      messages are generic ("extra key not allowed" or "required field is
      missing"). B-07 provides a specific, actionable diagnostic that names the
      body kind mismatch as the root cause, guiding agents and content authors
      to the fix.
    file: docs/rfcs/archive/implemented/rfc-0719-add-block-archetype-body-schema-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0720
    title: Document generator ownership map requirement in AGENTS.md —
      ownership.sync.validate already exists
    status: implemented
    alternativesText: >-
      - **Pre-commit hook warning** (rejected for now): A pre-commit hook could
      scan for new files in `public/` and warn if they lack ownership entries.
      This would catch the problem earlier than `ownership.sync.validate`, but
      adds complexity and maintenance burden. The existing fatal check in
      `build.prepare` is sufficient as a safety net. Deferred to the Evolution
      section.

      - **ADR instead of RFC** (rejected): An ADR would be lighter weight, but
      the RFC process provides acceptance criteria and traceability. The
      operator chose RFC to ensure the policy is formally accepted and
      implemented.

      - **Root AGENTS.md note** (rejected): The root `AGENTS.md` already
      mentions `GENERATOR_OWNERSHIP_MAP` in the context of `CMD-MAN-03` (command
      manifest validation). Adding a separate note about agent registration
      requirements would duplicate the package-level note. The package-level
      AGENTS.md is the correct location for package-specific agent rules.
    file: docs/rfcs/archive/implemented/rfc-0720-document-generator-ownership-map-requirement.md
  - kind: rejected-alternative
    rfcId: RFC-0721
    title: Add behavior snapshot staleness warning to build.prepare for route changes
    status: implemented
    alternativesText: >-
      - **Flag on `behavior.snapshot.validate`:** Rejected because
      `behavior.snapshot.validate` requires `dist/client` and runs in
      `build.post`. The staleness check runs in `build.prepare` before dist
      exists — different scope, different timing.

      - **Compare against merged route registry (`getRouteRegistry()`):**
      Rejected because it adds a dependency on `@warpgogol/share` and
      complexity. The one-directional check (system.md routes absent from
      snapshot) is sufficient for the early warning use case. SNAP-01 in
      `build.post` catches all drift.

      - **No check (status quo):** Rejected because agents discover stale
      snapshots only when `build.post` fails, wasting build time. An early
      warning in `build.prepare` is cheaper.
    file: docs/rfcs/archive/implemented/rfc-0721-add-behavior-snapshot-staleness-warning.md
  - kind: rejected-alternative
    rfcId: RFC-0722
    title: Enforce RFC and ADR directory structure convention
    status: implemented
    alternativesText: >-
      - **Formalize `draft/` as a sanctioned subdirectory.** Rejected — `status:
      draft` in frontmatter already separates drafts from active RFCs. A
      physical subdirectory adds organizational overhead without semantic value.
      `rfc.create` writes to root; adding `draft/` would require changing
      `rfc.create` behavior and the archive flow, which is disproportionate to
      the benefit.


      - **Error severity for validation rules.** Rejected — warning severity is
      sufficient. The pre-commit hook is the hard enforcement at commit time.
      Making validation rules error-severity would block `rfc.validate` for all
      issues when a single file is in an unsanctioned location, preventing other
      violations from being addressed.


      - **Index-based tracking (manifest of allowed directories).** Rejected —
      same reasoning as RFC-0367's rejection of index-based tracking. A closed
      allowlist of subdirectory names in the validation rule is simpler and
      always correct.
    file: docs/rfcs/archive/implemented/rfc-0722-enforce-rfc-and-adr-directory-structure-convention.md
  - kind: rejected-alternative
    rfcId: RFC-0723
    title: Require =(ref) formula syntax for content references in mixed strings
    status: implemented
    alternativesText: >-
      1. **Keep REF-04 as warning, add lint-only check** — rejected. 315
      warnings are noise that hides real issues. Warnings are ignored by agents
      and operators. Promotion to error is the only reliable enforcement
      mechanism.


      2. **New delimiter syntax (e.g. `[[ref]]`)** — rejected. RFC-0570 already
      established `=(...)` as the formula delimiter. Introducing a second
      delimiter for the same purpose (embedding refs in mixed strings) would
      create confusion and increase cognitive load for content authors.


      3. **Require all refs to use `=(...)` syntax, including pure refs** —
      rejected as too invasive. Pure refs on their own line are unambiguous and
      have no ambiguity problem. The rule targets only the ambiguous case: refs
      inside mixed strings.


      4. **Automatic migration via build-time auto-fix** — rejected. Build-time
      auto-fix mutates content files during validation, which violates the
      separation between validation and codegen. `content.formula.migrate` is a
      manual command that the operator runs explicitly.


      5. **Do nothing — accept the 315 warnings as noise** — rejected. The
      warnings hide real validation issues and create agent confusion about
      whether a pattern is a reference or literal text.
    file: docs/rfcs/archive/implemented/rfc-0723-require-formula-syntax-for-refs-in-mixed-strings.md
  - kind: rejected-alternative
    rfcId: RFC-0724
    title: "Publishing pipeline reliability: auto-recovery, retry loops,
      release.ready rename, and mandatory Axiom gate"
    status: implemented
    alternativesText: >-
      - **Auto-generate snapshot in `build.prepare`**: Rejected — would make
      `behavior.snapshot.validate` meaningless (comparing snapshot against
      itself).

      - **Keep `release.publish` name, improve docs**: Rejected — name is
      actively misleading. "Publish" implies production deployment.

      - **Skip Axiom for release path, skip evidence check in propagate**:
      Rejected — removes live verification before alt/main deploy. Axiom checks
      the live dev site, not just the build.

      - **Purge + retry in promote**: Rejected — propagate already purges after
      deploy. The issue is CDN propagation delay, not missing purge. Retry loop
      handles this.

      - **Auto-reconcile in `mission.validate`**: Rejected — too heavy.
      Auto-commit bordbuch is sufficient.
    file: docs/rfcs/archive/implemented/rfc-0724-publishing-pipeline-reliability-auto-recovery-retry-and-release-ready-rename.md
  - kind: rejected-alternative
    rfcId: RFC-0725
    title: Expand tokens.colors.lint to scan packages/ui and verify undefined CSS
      custom properties
    status: implemented
    alternativesText: >-
      - **Separate `tokens.colors.lint.packages` command**: Rejected — splits
      the command contract and requires two pipeline entries for related checks.

      - **New `tokens.defined.lint` command**: Rejected — overlaps with
      `tokens.colors.lint` scope. Better to extend existing command.

      - **Pre-commit hook**: Rejected — pre-commit hooks are not reliable in
      monorepo with partial commits. Pipeline check is the right level.

      - **Extend `biome.tokens.validate` to run earlier**: Rejected — it
      operates on dist output (built CSS), which requires a full build. The
      check belongs on source CSS.
    file: docs/rfcs/archive/implemented/rfc-0725-expand-tokens-colors-lint-to-scan-packages-ui-and-verify-undefined-css-custom-properties.md
  - kind: rejected-alternative
    rfcId: RFC-0726
    title: Unify CLI flag --site across all leitstand and release commands, remove
      --system
    status: implemented
    alternativesText: >-
      - **Add `--system` as alias alongside `--site`**: Rejected — plagues the
      codebase with synonyms and complicates documentation. Clean break is
      simpler.

      - **Keep `--system` and document it consistently**: Rejected — the
      platform's domain terminology uses "site" for Sternsystem concepts.
      `--system` is a mismatch with authoring, composition, and onboarding
      vocabulary.

      - **Accept both `--site` and `--system` in all commands**: Rejected —
      ambiguity in flag names leads to confusion about which is canonical.
    file: docs/rfcs/archive/implemented/rfc-0726-unify-cli-flag-site-across-all-leitstand-and-release-commands-remove-system.md
  - kind: rejected-alternative
    rfcId: RFC-0727
    title: Add adr.implement.stamp command for atomic ADR status transition
    status: implemented
    alternativesText: >-
      - **Keep manual editing only (status quo).** Rejected: manual editing has
      no atomic mutation, no commit validation, no file cleanliness check, and
      no concurrent safety. RFC-0476 already proved that stamping commands
      prevent real errors. The ADR analogue closes the same gap.


      - **Extend `rfc.implement.stamp` to handle ADRs.** Rejected: RFC and ADR
      modules are separate Forge modules with separate types, file paths, and
      validation rules. Mixing them would violate module boundaries and require
      conditional logic ("if ADR, skip criteria/evidence checks"). A separate
      command in the ADR module is cleaner.


      - **Allow `proposed → implemented` only (no `accepted`).** Rejected: many
      ADRs go through `accepted` before implementation. Excluding `accepted`
      would force a manual `accepted` transition first, then stamping — two
      steps where one suffices.


      - **Make `--implementation-commit` optional.** Rejected: the commit
      validation (ADR-IMP-03) is a key safety check. Without it, an agent can
      stamp an ADR as implemented without any evidence that the implementation
      was committed. Post-hoc ADRs can reference the commit that created the ADR
      itself or the commit that implemented the decision.


      - **Add ADR-IMP-02 (required sections check).** Rejected: `adr.validate`
      already checks required sections via AV-12. Duplicating this check in the
      stamp command adds maintenance burden without value — `adr.validate` runs
      as a post-stamp verification step anyway.
    file: docs/rfcs/archive/implemented/rfc-0727-add-adr-implement-stamp-command-for-atomic-adr-status-transition.md
  - kind: rejected-alternative
    rfcId: RFC-0728
    title: Enforce pbpChargeSchema on offering pricing charges
    status: implemented
    alternativesText: >-
      - **Use `number` for money values instead of `decimalString`.** Rejected:
      ADR-012 explicitly mandates decimal string. IEEE 754 float precision
      errors (`0.1 + 0.2 = 0.30000000000000004`) are unacceptable for monetary
      values. JCS canonicalization for signatures requires stable string
      representation. This alternative was already rejected in RFC-0400.


      - **Make `model` optional in `pbpChargeAmountSchema`.** Rejected: the PBP
      spec (§17) shows `model` in every charge amount example. Making it
      optional would weaken the discriminated union — Zod cannot discriminate
      without the field. The correct fix is adding `model` to content, not
      weakening the schema.


      - **Enforce `plans` and `adjustments` in the same RFC.** Rejected: `plans`
      in `digital-foundation.md` is a placeholder string (`"{}"`), not a real
      plan object. Enforcing `pbpPlanSchema` would require designing real plan
      content — that is a content design task, not a schema enforcement task.
      Scope creep risk. Deferred to a follow-up RFC.


      - **Add a custom kernel validator for charge compliance.** Rejected: the
      Zod schema already enforces the contract through Astro content
      collections. Adding a separate validator would duplicate enforcement and
      create maintenance burden.
    file: docs/rfcs/archive/implemented/rfc-0728-enforce-pbpchargeschema-on-offering-pricing-charges.md
  - kind: rejected-alternative
    rfcId: RFC-0729
    title: Formula pipe syntax, formatter registry, and money formatter
    status: implemented
    alternativesText: >-
      - **Manual € outside formula (`=(ref) €`).** Rejected: produces `70 €` but
      with no locale awareness, no thousands separators, no decimal formatting,
      no currency conversion path. Content authors must know the currency symbol
      for each locale.


      - **Pipe as separate pass in `resolveReferencesInString`.** Rejected: pipe
      must apply to the formula's numeric result, not to a string. A separate
      pass would require re-parsing the formula output, coupling the two passes.


      - **Hardcoded formatters in `resolveFormula`.** Rejected: couples the
      formula engine to every display concern. No extensibility. Future
      formatters (date, percent, unit) would require editing `resolveFormula`
      each time.


      - **Pipe registry in a new subpath `@warpgogol/share/formatters`.**
      Rejected: adds a new module and a cross-module dependency. The registry is
      small enough to live in `formula-eval.ts` with a plugin-registration API.
      The `money` formatter uses `Intl.NumberFormat` (built-in) — no external
      dependencies.
    file: docs/rfcs/archive/implemented/rfc-0729-formula-pipe-formatter-registry-and-money-formatter.md
  - kind: rejected-alternative
    rfcId: RFC-0730
    title: Eliminate presentation duplication and route display through canonical PBP
    status: implemented
    alternativesText: >-
      - **Keep `presentation`, add validation to sync with canonical.**
      Rejected: validation that checks presentation matches canonical is
      complex, fragile, and solves the wrong problem. The right answer is to not
      duplicate data.


      - **Compiler phase derivation (generate presentation from canonical).**
      Rejected: `presentation` is a display concern. Generating it at compile
      time and storing it in the entity creates a derived field that must be
      regenerated. Render-time formatting is simpler and has no stale data risk.


      - **Build-time codegen (write presentation to entity files).** Rejected:
      writes derived data to source files, creating git churn and stale data
      risk if regeneration is skipped.


      - **Remove `presentation` from all entities.** Rejected: non-offering
      entities (legal-identity, web-presence, public-document, business) use
      `presentation` for display-only metadata (dates, domains, tax) that has no
      canonical PBP field. Removing it would lose legitimate data. Scope is
      limited to offerings.


      - **Type `fulfillment` strictly.** Rejected: `fulfillment` is
      `z.record(z.string(), z.unknown())` by design — it holds operational data
      that varies per offering. Typing it strictly for `capacity` and
      `billingDay` is scope creep. A follow-up RFC can type fulfillment if the
      pattern stabilizes.
    file: docs/rfcs/archive/implemented/rfc-0730-eliminate-presentation-duplication-route-display-through-canonical-pbp.md
  - kind: rejected-alternative
    rfcId: RFC-0731
    title: Add this self-reference syntax to content references
    status: implemented
    alternativesText: >-
      - **Pre-expansion at call sites** — Instead of adding `sourceRef` to the
      resolver, each call site (semantic-loader, validator) would replace
      `this.` with the full `collection.file.` prefix before passing the string
      to the resolver. Rejected because it duplicates expansion logic across
      multiple call sites and the resolver cannot distinguish `this.` references
      from absolute ones for error reporting.


      - **Relative references (e.g. `../sibling.field`)** — A more general
      relative reference system. Rejected as scope creep — the identified need
      is self-reference within the same file, not cross-file relative paths.
      Relative references introduce ambiguity (relative to what? collection
      root? file directory?) and are not needed for the RFC-0730 use case.


      - **No `this.` — keep absolute references** — RFC-0730 already works with
      absolute references. Rejected because the verbosity and path coupling
      actively discourage authors from using references, undermining the goal of
      eliminating duplication.
    file: docs/rfcs/archive/implemented/rfc-0731-add-this-self-reference-syntax-to-content-references.md
  - kind: rejected-alternative
    rfcId: RFC-0732
    title: Add content regression gate for resolved page content drift detection
    status: implemented
    alternativesText: >-
      ### Extend `behavior.snapshot.validate` to include content fields


      **Rejected.** `behavior.snapshot.validate` checks route-level metadata
      (title, OG tags, JSON-LD). Content fields (block text, formula results,
      FAQ answers) have different semantics, different granularity (per-block vs
      per-route), and different update frequency. Conflating metadata drift with
      content drift would produce confusing diagnostics and make it harder to
      attribute changes. Separate commands provide cleaner separation of
      concerns and independent caching.


      ### Extend `generated.drift.validate` to cover semantic content


      **Rejected.** `generated.drift.validate` works by re-invoking registered
      generators with `dryRun: true` and comparing output. Resolved page content
      has no registered generator — it is built from content files at render
      time via `loadSemanticSiteModel`. The `GENERATOR_OWNERSHIP_MAP` pattern
      does not apply. A fundamentally different mechanism (semantic model
      loading + content hashing) is needed.


      ### Commit the snapshot to the workpiece git repo


      **Rejected.** The snapshot is a derived artifact, not authored content.
      Committing it to the workpiece would create git churn on every content
      edit and pollute diffs with generated content. The cache clone is the
      correct location — it mirrors `.materialization-state.json` and
      `.cache/video/`.


      ### Use route set parity as a separate command


      **Rejected.** Route set parity (CREG-02) is fundamentally part of content
      regression — it answers "did the set of routes with content change?"
      Splitting it into a separate command adds registration overhead without
      benefit. CREG-01 and CREG-02 share the same snapshot and the same pipeline
      step.
    file: docs/rfcs/archive/implemented/rfc-0732-add-content-regression-gate-for-resolved-page-content-drift-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0733
    title: Forge pinned-files protection system
    status: implemented
    alternativesText: >-
      1. **`.gitignore`-style protection.** Instead of a YAML manifest, use a
      `.forge/pinned` file with one path per line, similar to `.gitignore`.
      Rejected — lacks per-entry mode (`protect` vs `freeze`) and reason field.
      YAML manifest is more expressive and self-documenting.


      2. **Git attributes-based protection.** Use `.gitattributes` with a custom
      filter to prevent modifications. Rejected — `.gitattributes` filters are
      designed for content transformation (e.g., line endings), not access
      control. They can be bypassed and don't provide audit logging.


      3. **OS-level file permissions (chmod 444).** Make pinned files read-only
      on the filesystem. Rejected — doesn't work in CI (containers run as root),
      doesn't prevent `git mv`, and doesn't provide audit logging. Also
      platform-dependent (Windows behavior differs).


      4. **Separate npm package (`@warpgogol/pinned-guard`).** Ship the
      protection system as a standalone package, not integrated into forge.
      Rejected — forge already owns the archive commands that need pre-checks. A
      separate package would need to wrap or monkey-patch forge, creating
      fragility. Integration into forge is cleaner.


      5. **Post-hoc only (no pre-check in forge commands).** Rely solely on
      pre-commit hook + CI. Rejected — the root cause incident showed that forge
      commands can move files before any hook runs. Pre-check in archive
      commands prevents the damage from occurring in the first place.
    file: docs/rfcs/archive/implemented/rfc-0733-add-forge-pinned-files-protection-system.md
  - kind: rejected-alternative
    rfcId: RFC-0734
    title: Add content regression review manifest and apply workflow
    status: implemented
    alternativesText: >-
      ### HTML report with interactive buttons


      **Rejected.** The operator explicitly prefers YAML over HTML. YAML is:


      - Editable in any IDE or text editor

      - Diffable in git

      - Copyable to AI agents as instructions

      - No JavaScript or browser needed

      - Simpler to generate and parse


      HTML adds complexity without value for this use case.


      ### Automatic LLM-based content fixing


      **Rejected.** The kernel should not invoke LLMs to fix content. The
      review.yaml is the bridge between the kernel (detection) and the agent
      (fixing). The agent reads the YAML and applies fixes — the kernel only
      verifies that fixes were applied.


      ### Per-block golden snapshot updates


      **Rejected as impractical.** The golden snapshot is per-route (one YAML
      file with all routes). Updating individual blocks within a route would
      require partial snapshot reconstruction, which is fragile. Instead,
      `apply` updates the entire route's golden entry when any change in that
      route is accepted. Rejected changes keep their golden values. This is
      simpler and more robust.


      ### Commit review.yaml to cache clone instead of mission evidence


      **Rejected.** The review manifest is a mission-scoped artifact — it
      documents the operator's decisions for a specific mission. It belongs in
      the mission evidence directory, not the cache clone. The cache clone
      stores only the golden snapshot (the result), not the review process.
    file: docs/rfcs/archive/implemented/rfc-0734-add-content-regression-review-manifest-and-apply-workflow.md
  - kind: rejected-alternative
    rfcId: RFC-0735
    title: Multi-Currency Pricing Module — Program Charter
    status: implemented
    alternativesText: >-
      - **Amount Rules on Charge (expert's proposal).** Replace
      `PbpCharge.amount` with an `amounts` map of per-currency rules. Rejected:
      decision #3 (one strategy per target currency) means the multi-currency
      logic belongs at the business level, not duplicated per Charge. The Charge
      keeps its single canonical `amount`.


      - **Separate PriceModel entity (expert's proposal).** Create
      `pbp/price-model@1` as a new top-level entity. Rejected: the source price
      is already embedded in `PbpCharge.amount`. A parallel entity creates
      duplication. Derived prices are materialized by the compiler, not
      authored.


      - **Runtime conversion at page-render time.** Compute derived prices on
      each page load. Rejected: the research doc explicitly recommends
      build-time materialization for consistency, reproducibility, and
      cacheability.


      - **Spec amendment to pbp-specification-package.** Add new entity types to
      the vendored spec. Rejected: the spec is `accepted` at `@1`. New entities
      are platform RFCs that reference the existing spec without modifying it.
    file: docs/rfcs/archive/implemented/rfc-0735-multi-currency-pricing-module-program-charter.md
  - kind: rejected-alternative
    rfcId: RFC-0736
    title: CurrencyPricingPolicy Entity
    status: implemented
    alternativesText: >-
      - **Embed currency strategy in Offering.** Add a `currencies` field to
      `PbpPricing`. Rejected: decision #3 requires one strategy per target
      currency across all Offerings. Embedding per-Offering allows divergence
      and violates the invariant.


      - **Embed currency strategy in Charge.** Add an `amounts` map to
      `PbpCharge`. Rejected: same reason — strategy is business-level, not
      per-Charge.


      - **Separate entity per target currency.** One `CurrencyTarget` entity per
      currency. Rejected: the policy is a single coherent business decision.
      Splitting it into per-currency entities adds complexity without benefit.
    file: docs/rfcs/archive/implemented/rfc-0736-currency-pricing-policy-entity.md
  - kind: rejected-alternative
    rfcId: RFC-0737
    title: RatePolicy and RateSchedule Entities
    status: implemented
    alternativesText: >-
      - **Single entity for both modes.** Merge RatePolicy and RateSchedule into
      one entity. Rejected: external and business-fixed modes have fundamentally
      different data shapes — external needs source refs, business-fixed needs
      entry schedules. Merging creates a confusing union type.


      - **`validUntil` on schedule entries.** Add `validUntil` to rate schedule
      entries. Rejected: decision #8 explicitly states "old rate valid until
      replaced." `validUntil` adds complexity and can create gaps if not
      maintained. The applicable-rate algorithm uses `validFrom` ordering
      instead.


      - **Rate direction as a transform flag.** Let the derivation handle
      direction normalization. Rejected: the rate policy must declare its
      quotation direction so the derivation knows whether to multiply or divide.
      Making it explicit prevents ambiguity.
    file: docs/rfcs/archive/implemented/rfc-0737-rate-policy-and-rate-schedule-entities.md
  - kind: rejected-alternative
    rfcId: RFC-0738
    title: RateSnapshot Entity
    status: implemented
    alternativesText: >-
      - **Store rates in a database.** Use a runtime database for rate storage.
      Rejected: the Werkstatt model is build-time content. Rates are content,
      not runtime state. Storing them as content files makes them
      version-controlled, reviewable, and reproducible.


      - **No digest.** Skip the digest field. Rejected: the digest provides
      tamper evidence. Without it, a snapshot could be silently modified,
      breaking the reproducibility of derived prices. The digest is part of the
      derivation provenance (RFC-0739).


      - **Mutable snapshots.** Allow updating a snapshot in place. Rejected:
      immutability is a core principle. A new observation creates a new
      snapshot. This ensures derived prices can always be traced back to a
      specific, unchanging rate.
    file: docs/rfcs/archive/implemented/rfc-0738-rate-snapshot-entity.md
  - kind: rejected-alternative
    rfcId: RFC-0739
    title: Currency Conversion Derivation Contract
    status: implemented
    alternativesText: >-
      - **Configurable pipeline steps.** Allow arbitrary ordering of pipeline
      steps. Rejected: the research document explicitly specifies a fixed
      pipeline (conversion → percentage → fixed → rounding → ending).
      Configurable ordering introduces complexity and invalid combinations.


      - **Binary float arithmetic.** Use JavaScript `number` for arithmetic.
      Rejected: pbp-specification-package/ADR-012 mandates decimal strings.
      Binary float produces rounding errors (e.g. `0.1 + 0.2 !== 0.3`).


      - **No trace.** Return only the final amount. Rejected: decision #35
      requires full calculation trace for AI agents. The trace is also essential
      for debugging and auditability.
    file: docs/rfcs/archive/implemented/rfc-0739-currency-conversion-derivation-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0740
    title: Derived Price Materialization
    status: implemented
    alternativesText: >-
      - **Compute at render time.** Run the derivation on each page render.
      Rejected: the research document explicitly recommends build-time
      materialization for consistency, reproducibility, and cacheability.
      Render-time computation would produce different results if rates change
      between page loads.


      - **Store derived prices in Offering content.** Write derived prices back
      into the Offering's `pricing` field. Rejected: the Offering's `pricing` is
      authored content. Derived prices are generated. Mixing them creates
      confusion and validation conflicts.


      - **Separate derived-price entity.** Create a `pbp/derived-price@1` entity
      type. Rejected: derived prices are materialized output, not authored
      entities. They belong in the compiled graph, not in the content
      collection.
    file: docs/rfcs/archive/implemented/rfc-0740-derived-price-materialization.md
  - kind: rejected-alternative
    rfcId: RFC-0741
    title: multi-currency Entitled Feature and Build Pipeline
    status: implemented
    alternativesText: >-
      - **Gate at route level only.** Only gate the currency selector UI, not
      the build pipeline. Rejected: without gating the pipeline, derived prices
      would be materialized even for non-entitled sites, wasting build time and
      producing unused data.


      - **Separate pipeline.** Create a `multi-currency-prepare` pipeline
      separate from `build-prepare`. Rejected: the steps must run in sequence
      with the existing build. A separate pipeline would require complex
      pipeline orchestration.


      - **Runtime entitlement check.** Check entitlement at render time instead
      of build time. Rejected: the Werkstatt model is build-time. Derived prices
      are materialized at build time. The entitlement check must happen before
      materialization.
    file: docs/rfcs/archive/implemented/rfc-0741-multi-currency-entitled-feature-and-build-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0742
    title: Currency-Aware Price Projection
    status: implemented
    alternativesText: >-
      - **Let the UI format prices.** Pass raw materialized prices to the UI and
      let components format them. Rejected: the research document specifies the
      UI receives a ready-to-display projection. Formatting logic in components
      creates duplication and inconsistency.


      - **No disclosure note in projection.** Let the UI compose the note.
      Rejected: the note depends on `commercialMeaning` and locale. Composing it
      in the projection ensures consistency and localization.


      - **Include source price in projection.** Show both source and derived
      prices. Rejected: decision #31 explicitly says "do NOT show source EUR
      price alongside derived price."
    file: docs/rfcs/archive/implemented/rfc-0742-currency-aware-price-projection.md
  - kind: rejected-alternative
    rfcId: RFC-0743
    title: Currency Selector UI Component
    status: implemented
    alternativesText: >-
      - **URL parameter for currency.** Use `?currency=UAH` instead of
      `localStorage`. Rejected: URL params require page reloads and complicate
      SEO. `localStorage` allows instant switching. URL params can be added as a
      future enhancement for shareable links.


      - **Cookie for currency.** Use a cookie instead of `localStorage`.
      Rejected: cookies are sent with every request, adding overhead.
      `localStorage` is client-side only. Cookies can be added as a future
      enhancement for server-side rendering of the selected currency.


      - **Server-side currency detection.** Detect currency from
      `Accept-Language` header. Rejected: language does not determine currency
      (e.g. a German speaker in Ukraine wants UAH). The selector gives the user
      explicit control.
    file: docs/rfcs/archive/implemented/rfc-0743-currency-selector-ui-component.md
  - kind: rejected-alternative
    rfcId: RFC-0744
    title: Rate Fetcher Service
    status: implemented
    alternativesText: >-
      - **Build-time fetching only.** Fetch rates only during the build
      pipeline, no separate service. Rejected: the build pipeline runs on every
      commit, but rates only change daily. A separate service with a daily
      schedule is more efficient and decouples rate fetching from site builds.


      - **Manual rate entry.** Require a human to manually enter rates daily.
      Rejected: this is error-prone and doesn't scale. The service automates the
      daily fetch.


      - **All adapters in the service workspace.** Put adapters in
      `services/rate-fetcher-worker/src/`. Rejected: AGENTS.md requires shared
      logic in `packages/*`. Adapters are reusable across services and sites.


      - **Extend existing lagebild-sync-worker.** Add rate fetching as a new
      operation in the Lagebild worker. Rejected: Lagebild is CRM-specific
      (outbox processing, Pipedrive sync). Adding rate fetching mixes domains. A
      separate worker following the same operational pattern is cleaner.


      - **Worker commits to Git via GitHub API.** Have the worker commit
      RateSnapshot files directly to the repository. Rejected: requires Git
      access from a Cloudflare Worker, adds credentials management complexity,
      and creates concurrent commit risks. Storing observations in Supabase and
      creating snapshots at build time is simpler and more robust.


      - **Worker triggers rebuild after fetch.** Have the worker call
      `leitstand.dev-deploy` or trigger CI after fetching rates. Rejected: the
      build pipeline already runs on a regular schedule (CI triggers, dev
      deploys, mission workflows). Adding a rebuild trigger creates unnecessary
      coupling between the fetch service and the build/deploy pipeline.
    file: docs/rfcs/archive/implemented/rfc-0744-rate-fetcher-service.md
  - kind: rejected-alternative
    rfcId: RFC-0745
    title: Currency-Aware Schema.org Mapping
    status: implemented
    alternativesText: >-
      - **Emit derived prices in Schema.org.** Include UAH/USD prices as
      additional `Offer` entries. Rejected: search engines interpret multiple
      `Offer` entries as different offers. This creates confusion and potential
      rich result penalties.


      - **Use `priceSpecification` with multiple prices.** Schema.org
      `PriceSpecification` allows compound price specifications. Rejected: this
      is intended for different pricing models (e.g. subscription vs one-time),
      not for display currencies. Using it for currency display is semantically
      incorrect.


      - **No validation.** Trust that the implementation won't emit derived
      prices. Rejected: without validation, a future change could accidentally
      leak derived prices into Schema.org. The validation rule prevents this.
    file: docs/rfcs/archive/implemented/rfc-0745-currency-aware-schema-org-mapping.md
  - kind: rejected-alternative
    rfcId: RFC-0746
    title: Make rate-snapshot.resolve idempotent for business-fixed mode
    status: implemented
    alternativesText: >-
      1. **Deterministic `buildTime` from `build-input-hash`**: Rejected —
      pipeline steps don't receive individual flags, and threading a
      deterministic timestamp through the pipeline would require changing the
      pipeline executor contract.

      2. **Skip `rate-snapshot.resolve` in distribution reuse path**: Already
      done via RFC-0635, but doesn't help when the hash doesn't match (which is
      the problem this RFC solves).

      3. **Make `mission.close` auto-commit dirty files**: Rejected — this masks
      real issues and violates the clean-workpiece invariant.
    file: docs/rfcs/archive/implemented/rfc-0746-make-rate-snapshot-resolve-idempotent-for-business-fixed-mode.md
  - kind: rejected-alternative
    rfcId: RFC-0747
    title: Add retry loop to leitstand.promote alt health check
    status: implemented
    alternativesText: >-
      1. **Reuse `verifyFreshness` retry constants (5 attempts)**: Rejected —
      health checks are more reliable than CDN freshness. 3 attempts is
      sufficient for Worker cold-start delays.

      2. **Add `--skip-health-check` flag**: Rejected — promoting an unhealthy
      deployment to main is a production risk.

      3. **Sleep before health check**: Rejected — adds latency to the common
      case (healthy on first attempt) to fix the rare case.
    file: docs/rfcs/archive/implemented/rfc-0747-add-retry-loop-to-leitstand-promote-alt-health-check.md
  - kind: rejected-alternative
    rfcId: RFC-0748
    title: Add --auto-accept flag to content.regression.review.generate
    status: implemented
    alternativesText: >-
      1. **Skip review.yaml entirely and directly update golden**: Rejected —
      loses the audit trail. The manifest documents what was accepted and when.

      2. **Add `--auto-accept` to `content.regression.apply` instead**: Rejected
      — `apply` reads the YAML; it would still require a YAML file with all
      `pending` decisions, then override them. Cleaner to pre-fill at generation
      time.

      3. **Add a `content.regression.auto-accept` convenience command**:
      Rejected — unnecessary command surface. A flag on the existing command is
      simpler.
    file: docs/rfcs/archive/implemented/rfc-0748-add-auto-accept-flag-to-content-regression-review-generate.md
  - kind: rejected-alternative
    rfcId: RFC-0749
    title: Auto-commit dirty bordbuch projections in cache clone after
      mission.validate
    status: implemented
    alternativesText: >-
      1. **Make `bordbuch.commit` retry more aggressively**: Rejected — it
      already uses `gitExecWithRetry` with 12s/60s backoff. More retries would
      slow down `build.prepare` for no benefit.

      2. **Make `bordbuch.commit` throwing again**: Rejected — RFC-0702
      explicitly made it non-throwing to prevent `build.prepare` crashes.
      Reversing that would regress.

      3. **Remove the RFC-0522 warning**: Rejected — the warning is still useful
      for non-bordbuch dirty files (e.g. content source edits in cache clone).

      4. **Add cleanup to `mission.reconcile` instead**: Rejected — reconcile
      runs after validate, but the operator may want to inspect the cache clone
      between validate and reconcile. Cleaning up at the end of validate is
      earlier and prevents the warning from firing in the first place.
    file: docs/rfcs/archive/implemented/rfc-0749-auto-commit-dirty-bordbuch-projections-after-mission-validate.md
  - kind: rejected-alternative
    rfcId: RFC-0750
    title: Auto-commit bordbuch events after every lifecycle command via
      appendAndCommitBordbuch helper
    status: implemented
    alternativesText: >-
      1. **Post-command flush**: After every kernel command, check for dirty
      bordbuch in the cache clone and commit. Rejected — adds overhead to every
      command (even ones that never touch bordbuch), and operates after the
      command's own error handling, making it impossible for the command to
      react to commit failure.


      2. **ESLint custom rule**: Implement the parity check as a custom ESLint
      rule in `eslint.config.js`. Rejected — inconsistent with the existing lint
      pattern (`fingerprint.usage.lint`, `naming.convention.lint`) which uses
      dedicated Site OS lint commands in `site-kernel-checks`. ESLint also
      requires AST analysis of import paths, which is more fragile than
      file-level whitelist checking.


      3. **Keep `commitAndPushBordbuch` exported**: Allow callers to use either
      `appendAndCommitBordbuch` or the separate `appendBordbuchEntry` +
      `commitAndPushBordbuch` pattern. Rejected — no legacy code, no backward
      compatibility. Keeping the export invites future gaps.


      4. **Throw on commit failure in the helper**: Make
      `appendAndCommitBordbuch` throw when `commitSha` is null or `pushed` is
      false. Rejected — commands like `release.rollback` and
      `leitstand.rollback` have already performed state transitions before the
      bordbuch append. Throwing after that leaves the system in an inconsistent
      state. The helper returns the result; each caller decides whether to throw
      (mission.open does, nachweis commands don't).
    file: docs/rfcs/archive/implemented/rfc-0750-auto-commit-bordbuch-events-after-every-lifecycle-command-via-appendandcommitbordbuch-helper.md
  - kind: rejected-alternative
    rfcId: RFC-0751
    title: Establish service deployment protocol for shared Cloudflare Worker services
    status: implemented
    alternativesText: >-
      - **Per-service deploy scripts only (status quo)**: Rejected — no state
      tracking, no centralized command, no health verification, no naming
      enforcement.

      - **Separate `services/registry.yaml`**: Rejected for now — keeps
      everything in one file, simpler to maintain. Can be split later if needed.

      - **Full channel model (dev/alt/main) for services**: Rejected as
      over-engineering — services are Workers with a single production endpoint.
      Multi-channel can be added later if needed.

      - **Axiom evidence gate for services**: Rejected — services have no
      visitor-facing HTML to audit. Health check (HTTP probe) is sufficient.

      - **Client-prefixed Worker names (`gogol-*`, `warpgogol-*`)**: Rejected —
      services are shared infrastructure, not per-client. Bare names enforce
      this convention.
    file: docs/rfcs/archive/implemented/rfc-0751-establish-service-deployment-protocol.md
  - kind: rejected-alternative
    rfcId: RFC-0752
    title: Subdomain management protocol for Cloudflare DNS and Workers routes
    status: implemented
    alternativesText: >-
      - **Cloudflare Dashboard only (status quo)**: Rejected — manual,
      error-prone, not reproducible, no audit trail.

      - **Terraform / Pulumi**: Rejected — adds infrastructure-as-code
      dependency. The workshop already uses Cloudflare API via wrangler; direct
      API calls are simpler for this scope.

      - **Cloudflare KV for zone ID cache**: Rejected — zone ID is stable,
      stored in registry is sufficient.

      - **Auto-update mismatched DNS records**: Rejected — changing a DNS record
      that points to the wrong target could break production. Operator must
      explicitly fix or delete first.

      - **DNS propagation waiting**: Rejected — health checks use
      `*.workers.dev` URLs (RFC-0751). DNS propagation is asynchronous and
      should not block deploys.
    file: docs/rfcs/archive/implemented/rfc-0752-subdomain-management-protocol.md
  - kind: rejected-alternative
    rfcId: RFC-0753
    title: DNS record management protocol for Cloudflare DNS zones
    status: implemented
    alternativesText: >-
      - **Cloudflare Dashboard only (status quo)**: Rejected — no version
      control, no validation, no audit trail.

      - **Terraform / Pulumi**: Rejected — adds infrastructure-as-code
      dependency. Direct API calls are simpler for this scope.

      - **Merge with RFC-0752**: Rejected — different record types, different
      lifecycle, different commands. Worker-backed subdomains need DNS + Workers
      route; arbitrary DNS records need only DNS. Merging would conflate two
      orthogonal concerns.

      - **Single global declaration file**: Rejected — per-zone declaration
      files are co-located with the system that owns the zone, keeping concerns
      separated.

      - **Auto-delete extra records**: Rejected — deleting DNS records that
      exist in Cloudflare but not in the declaration could break production.
      `dns.record.validate` reports extras as informational; operator must
      explicitly delete via `dns.record.delete`.
    file: docs/rfcs/archive/implemented/rfc-0753-dns-record-management-protocol.md
  - kind: rejected-alternative
    rfcId: RFC-0754
    title: "Unify commit path: ecosystem.commit auto-detect and fallback"
    status: implemented
    alternativesText: >-
      - **Remove the guard entirely** — rejected. The guard prevents accidental
      platform-scope commits without version bumps. Keeping it as a safety net
      is safer.

      - **Make `git commit` auto-delegate to `ecosystem.commit`** — rejected.
      Git hooks cannot reliably intercept and replace commands; the guard can
      only block, not redirect.

      - **Add a `--scope auto` flag** — rejected. Auto-detect should be the
      default, not opt-in. Adding a flag increases cognitive load.
    file: docs/rfcs/archive/implemented/rfc-0754-unify-commit-path.md
  - kind: rejected-alternative
    rfcId: RFC-0755
    title: Validate RFC frontmatter YAML parseability in rfc.validate
    status: implemented
    alternativesText: >-
      - **Pre-commit hook for YAML parsing** — rejected as the primary solution.
      Hooks are local and can be bypassed. `rfc.validate` is the canonical
      validation path.

      - **Auto-quote frontmatter strings in `rfc.create`** — complementary, not
      alternative. This prevents future issues, but `V-RFC-33` catches existing
      ones. Both should be implemented.

      - **Change `rfc.implement.stamp` only** — rejected. The stamp is too late
      in the pipeline. `rfc.validate` runs earlier and more frequently.
    file: docs/rfcs/archive/implemented/rfc-0755-validate-rfc-frontmatter-yaml.md
  - kind: rejected-alternative
    rfcId: RFC-0756
    title: Auto-detect implementation commit in rfc.implement.stamp
    status: implemented
    alternativesText: >-
      - **`--implementation-commit auto` keyword** — rejected. Auto-detect
      should be the default when the flag is omitted, not an explicit keyword.
      Adding a keyword adds cognitive load.

      - **Use the most recent commit touching `packages/`** — rejected. Too
      broad; may pick an unrelated commit. The RFC ID in the commit message is
      the precise signal.

      - **Use `HEAD`** — rejected. `HEAD` may be a doc-only commit after the
      implementation commit. The RFC ID grep is more precise.
    file: docs/rfcs/archive/implemented/rfc-0756-auto-detect-implementation-commit.md
  - kind: rejected-alternative
    rfcId: RFC-0757
    title: Extend send-message with site-defined checklist items for
      structured-field detection
    status: implemented
    alternativesText: >-
      - **Re-introduce structured input fields (RFC-0514 style).** Rejected —
      RFC-0572 explicitly superseded RFC-0514 because structured fields added UX
      friction for the target audience. The operator confirmed that the
      single-textarea approach with checklist detection is preferred over
      visible input fields.


      - **Site-local archetype for send-message.** Rejected — the operator
      decided that all archetypes remain shared. A site-local archetype would
      fragment the section registry and violate the composition-only principle.
      The generalizable `checklistItems[]` approach keeps `send-message` shared
      while allowing site-specific configuration.


      - **Arbitrary JavaScript detection functions from content.** Rejected —
      allowing content authors to define custom detection functions would create
      a code injection vector and break the content/code separation. The closed
      `ChecklistRuleType` enum ensures detection logic is reviewed and safe.


      - **Keep the 2-item checklist and fork the client script for new forms.**
      Rejected — forking shared code for each new form variant violates the
      composition-only principle and creates maintenance burden. The
      generalizable approach scales to N items without code changes.


      - **Move the checklist to a separate section archetype.** Rejected — the
      checklist is tightly coupled to the form's submit-gating behavior.
      Separating it would require inter-section communication and complicate the
      page composition model.
    file: docs/rfcs/archive/implemented/rfc-0757-extend-send-message-with-site-defined-checklist-items-for-structured-field-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0758
    title: Add dynamic-status-block archetype for data-driven status indicators
    status: implemented
    alternativesText: >-
      - **Misuse the `impact` archetype.** Rejected — `impact` is designed for
      static curated statistics with a `stats[]` array (e.g. "50 projects", "12
      years experience"). It has no single-value data-driven indicator pattern
      and no `contextText` field. Using it for a dynamic counter would require
      props outside its schema.


      - **Site-local section.** Rejected — the operator decided that all
      archetypes remain shared. A site-local section for a data-driven status
      indicator would fragment the UI catalog and prevent reuse by other sites.


      - **Use `markdown` with inline value.** Rejected — `markdown` renders
      authored prose content. A data-driven value would need to be injected into
      the markdown at build time, which is not how `contentRef` works. The value
      would be stale unless manually updated.


      - **Extend `impact` with a `dynamicValue` prop.** Rejected — `impact` has
      a specific semantic role (`impact`) and `bodyKind: stats`. Adding a
      single-value data-driven mode would overload the archetype and confuse its
      intent. A separate archetype is cleaner.
    file: docs/rfcs/archive/implemented/rfc-0758-add-dynamic-status-block-archetype-for-data-driven-status-indicators.md
  - kind: rejected-alternative
    rfcId: RFC-0759
    title: Add service-metadata-block archetype for page-level metadata footers
    status: implemented
    alternativesText: >-
      - **Use `markdown` with manually formatted metadata.** Rejected —
      `markdown` renders freeform prose. Structured metadata fields (version,
      dates, links) need consistent rendering, accessible labels, and visual
      treatment. Manually formatting them in markdown produces inconsistent
      results and cannot be validated.


      - **Site-local section.** Rejected — the operator decided that all
      archetypes remain shared. A metadata footer is a common pattern for policy
      and program pages across all sites.


      - **Extend the site-level footer component.** Rejected — the site footer
      is a shell component rendered on every page. A page-level metadata footer
      is a section that appears only on specific pages with page-specific
      metadata. They serve different purposes.


      - **Use `transparency` archetype.** Rejected — `transparency` is for
      trust-building claims ("we publish our pricing", "we have an exit
      policy"), not for structured metadata fields with version, dates, and
      links.
    file: docs/rfcs/archive/implemented/rfc-0759-add-service-metadata-block-archetype-for-page-level-metadata-footers.md
  - kind: rejected-alternative
    rfcId: RFC-0760
    title: Add Vidpovidalni Rekomendatsiyi page to warpgogol-com UK-only
    status: implemented
    alternativesText: >-
      - **Add both UK and DE routes simultaneously.** Rejected — the operator
      explicitly requested UK-only for the initial launch. The DE translation
      can be added in a follow-up mission after the content is validated in
      production.


      - **Add the page as a markdown-only page without new archetypes.**
      Rejected — the page requires dynamic status (open mandate count), custom
      form checklists, and a structured metadata footer. These cannot be
      rendered with `markdown` alone without misusing the archetype.


      - **Add the page to a different site (not warpgogol-com).** Rejected — the
      recommendation and Market Steward programs are specific to warpgogol-com's
      business model.


      - **Create a site-local section for the dynamic status and metadata
      footer.** Rejected — the operator decided that all archetypes remain
      shared. RFC-0758 and RFC-0759 create shared archetypes available to all
      sites.
    file: docs/rfcs/archive/implemented/rfc-0760-add-vidpovidalni-rekomendatsiyi-page-to-warpgogol-com-uk-only.md
  - kind: rejected-alternative
    rfcId: RFC-0761
    title: "Simplify env-file architecture: remove channel-specific env files and
      consolidate shared secrets"
    status: implemented
    alternativesText: >-
      - **Keep `.env.main` / `.env.alt` but make them optional:** Rejected. The
      merge pattern already handles missing files via `sourceDotenv` returning
      `{}`. But keeping the files as optional still requires
      `resolveConventionSecretsPath` to look for them, `deploy.scripts.validate`
      to accept both `--secrets-file .env.main` and `--secrets-file .env`, and
      `deploy.preflight` to accept `--env` or not. The complexity remains for
      zero benefit — nobody uses channel-specific overrides in practice.


      - **Introduce `.env.override` for channel-specific overrides:** Rejected.
      There is no current need for channel-specific overrides. All deployment
      channels for warpgogol-com use the same Cloudflare account and the same
      secrets. If a future client site needs channel-specific overrides, a new
      RFC will define the mechanism.


      - **Move `CLOUDFLARE_ZONE_ID` to root `.env` only:** Rejected.
      `CLOUDFLARE_ZONE_ID` is site-specific — different sites have different
      zones. It must remain in site `.env.example` and `.env`. Root `.env` has
      the warpgogol-com zone ID as a convenience default, but client sites
      override it in their `.env`.


      - **Merge root `.env` and site `.env` into a single file:** Rejected. Root
      `.env` is loaded by `dotenv/config` in the kernel CLI. Site `.env` is
      loaded by Astro/Vite for `astro dev` / `astro build`. They serve different
      consumers and must remain separate.
    file: docs/rfcs/archive/implemented/rfc-0761-simplify-env-file-architecture-remove-channel-specific-env-files-and-consolidate-shared-secrets.md
  - kind: rejected-alternative
    rfcId: RFC-0762
    title: Make mission.close sync external mirrors before completing
    status: implemented
    alternativesText: >-
      - **Block `mission.close` if sync fails**: Rejected. `mission.close` is
      irreversible (state transition, bordbuch entry). Blocking after the state
      has changed would leave the mission in an inconsistent state. Non-fatal
      warning is the correct behavior.

      - **Add a `--no-sync` flag**: Rejected. Adds complexity for no clear
      benefit. If the operator wants to skip sync, they can ignore the warning
      and not run it manually.

      - **Move sync to a post-close pipeline step**: Rejected. `mission.close`
      is not a pipeline; it is a single command. Adding a pipeline would
      over-engineer the close flow.
    file: docs/rfcs/archive/implemented/rfc-0762-make-mission-close-sync-external-mirrors-before-completing.md
  - kind: rejected-alternative
    rfcId: RFC-0763
    title: Auto-commit stale bordbuch projections in cache clone during
      mission.validate
    status: implemented
    alternativesText: >-
      - **Move the RFC-0749 cleanup before the `passed` check**: Rejected. The
      cleanup at line 602 runs after the `passed` check. Moving it before would
      mean the cleanup runs on both success and failure paths — but it would
      also run before the `validation-report.json` write, making the report
      timing inconsistent. Adding separate cleanup calls on failure paths is
      cleaner.

      - **Refactor all exit paths to use a single `finally` block**: Rejected.
      The function has complex control flow with multiple returns. A `finally`
      block would run on all paths including the reuse path (which already has
      RFC-0724 coverage) and the success path (which already has RFC-0749
      coverage). It would also run on thrown exceptions, which is a different
      error-handling concern. Targeted cleanup calls on the two failure paths
      are simpler and more predictable.

      - **Add the RFC-0522 dirty cache clone warning to failure paths instead of
      cleanup**: Rejected. The warning would tell the operator about the problem
      but not fix it. The operator would still need to manually commit bordbuch
      files or re-run `mission.validate` to trigger the RFC-0724 pre-validate
      cleanup. Auto-committing on failure paths is strictly better — the
      operator never sees the dirty state.

      - **Block `mission.validate` failure return until bordbuch is clean**:
      Rejected. The cleanup is non-fatal — if it fails, the failure return must
      still proceed. Blocking would turn a bordbuch commit issue into a
      validation blocker, which is the wrong priority.
    file: docs/rfcs/archive/implemented/rfc-0763-auto-commit-stale-bordbuch-in-cache-clone-during-mission-validate.md
  - kind: rejected-alternative
    rfcId: RFC-0764
    title: Add --auto-accept flag to content.regression.check for expected content
      drift
    status: implemented
    alternativesText: >-
      - **Extend RFC-0748's `--auto-accept` on `review.generate` to also skip
      `apply`**: Rejected. `review.generate` is an operator-invoked command
      outside the pipeline. Even with `--auto-accept`, the operator must still
      run `apply` separately and re-run `mission.validate`. The 2-step flow
      doesn't eliminate manual intervention between pipeline steps — it only
      eliminates YAML editing. RFC-0764's 1-step flow integrates auto-accept
      into the pipeline itself.

      - **`--accept-all` on `content.regression.apply`**: Rejected.
      `content.regression.apply` already requires a review manifest. Adding a
      bulk-accept there would skip the review generation step, losing the audit
      trail.

      - **Auto-accept in `mission.close`**: Rejected. `mission.close` already
      creates the golden baseline on first close (DNA-61). Auto-accepting in
      close would conflate "first close" with "subsequent close with drift."

      - **Remove the review-apply workflow entirely**: Rejected. The manual
      workflow is correct for unexpected drift. The flag is a fast path for
      expected drift only.
    file: docs/rfcs/archive/implemented/rfc-0764-add-auto-accept-flag-to-content-regression-check.md
  - kind: rejected-alternative
    rfcId: RFC-0765
    title: Document price marker syntax as component-level shorthand
    status: implemented
    alternativesText: |-
      1. **Unify price markers with `=(...)` formula syntax** — rejected. `=(...)` returns a string; `CurrencyAwarePriceDisplay` requires structured price variants (array of `{currency, formatted, note}`). Unifying would require `resolveFormula` to return structured data instead of strings, mixing content and presentation layers. Additionally, the `=(...)` syntax for the same operation would be significantly longer: `=(business-profile.offerings/referral-fee.pricing.charges.activation.amount.value | price chargeRef=activation)` vs `{price:referral-fee:activation}`. The shorthand is a deliberate domain-specific abbreviation for a frequent operation.

      2. **Change `{price:...}` to non-brace syntax (e.g. `@(price:...)`)** — rejected. The curly braces in `{price:...}` are a different namespace from content references. Changing the syntax would require migrating all existing content files and updating `parsePriceMarkers` for no functional benefit. The confusion is solved by documentation, not by syntax change.

      3. **Add a validator that flags `{price:...}` as a potential deprecated brace pattern** — rejected. `{price:...}` is not deprecated. A validator would create false positives and add maintenance burden. Documentation is the correct tool here.

      4. **Do nothing — agents should read the RFCs** — rejected. Three RFCs (0529, 0723, 0743) each describe one mechanism. No single document lists all three side-by-side. Agents who read RFC-0529 in isolation may incorrectly conclude that all `{...}` patterns are deprecated.
    file: docs/rfcs/archive/implemented/rfc-0765-document-price-marker-syntax-as-component-level-shorthand.md
  - kind: rejected-alternative
    rfcId: RFC-0766
    title: Resolve price markers in markdown prose content
    status: implemented
    alternativesText: >-
      1. **Use Astro render() with a custom remark plugin** — rejected. Astro's
      `render()` returns a component, not a string. Injecting
      `CurrencyAwarePriceDisplay` into a rendered component requires splitting
      the component at marker boundaries, which is complex and fragile.


      2. **Pre-process markers before markdown rendering** — rejected. Replacing
      `{price:...}` with HTML before micromark would cause micromark to escape
      the HTML (treating it as text). Replacing after markdown rendering is the
      correct approach — micromark leaves `{price:...}` as literal text, then
      post-processing swaps it for HTML.


      3. **Use a placeholder token approach** — replace `{price:...}` with a
      unique placeholder before markdown rendering, then replace the placeholder
      with HTML after. This is more robust but adds complexity. The direct regex
      replacement on rendered HTML is simpler and sufficient because
      `{price:...}` is not valid HTML and survives micromark intact.


      4. **Extend SectionParagraphs only, not prose** — rejected. Prose files
      are the primary long-form content surface. Price markers in prose are
      needed for the zero-hardcoded-prices policy (ADR-0033) to be enforceable.
    file: docs/rfcs/archive/implemented/rfc-0766-resolve-price-markers-in-markdown-prose-content.md
  - kind: rejected-alternative
    rfcId: RFC-0767
    title: Resolve price markers in semantic projections
    status: implemented
    alternativesText: >-
      1. **Resolve markers in JSON-LD builder only** — rejected. The
      `description` field flows into both JSON-LD and `<meta
      name="description">`. Resolving at the `buildSemanticPageModelWith` level
      ensures both are correct.


      2. **Store resolved prices in the SemanticPageModel** — rejected. This
      would change the model structure and require all consumers to handle the
      new field. Resolving markers in the string values before they enter the
      model is simpler and non-breaking.


      3. **Use multi-currency variants in JSON-LD** — rejected. JSON-LD does not
      have a standard way to express multiple currency variants of a price.
      Search engines expect a single price. Source currency (EUR) is the
      canonical price; all other currencies are derived from it.


      4. **Do nothing — markers in JSON-LD are harmless** — rejected. Search
      engines may interpret `{price:referral-fee:activation}` as a literal
      string, which is meaningless. This could affect how the page is indexed
      and displayed in search results.
    file: docs/rfcs/archive/implemented/rfc-0767-resolve-price-markers-in-semantic-projections.md
  - kind: rejected-alternative
    rfcId: RFC-0768
    title: Add scroll-to-top floating button component
    status: implemented
    alternativesText: >-
      1. **Use an existing ComponentRole (e.g. `footer-promo` or
      `section-cta`)**: Rejected because it would be semantically inaccurate.
      The closed enum exists precisely to keep component roles meaningful, and
      misusing an existing role would confuse manifest validation and archetype
      registry lookups.


      2. **Embed the logic directly in `layout-component.astro` without a
      separate component**: Rejected because it would violate the component
      decomposition pattern (DNA-17 Mirror Quintet). The layout shell's contract
      explicitly states it owns the HTML skeleton, not feature logic. A separate
      component is testable, manifestable, and replaceable.


      3. **Control visibility via shell block in system.md (like
      header/footer)**: Rejected because it adds configuration overhead for a
      feature that should be universal. The operator explicitly requested
      automatic inclusion on all sites.


      4. **Use a content-layer labels file for the aria-label**: Rejected
      because the button has a single static label per language. A full
      content-layer file would be over-engineering for one string. The component
      uses a built-in i18n fallback with an optional `ariaLabel` prop override.
    file: docs/rfcs/archive/implemented/rfc-0768-add-scroll-to-top-floating-button-component.md
  - kind: superseded-rfc
    rfcId: RFC-0769
    title: Werkstatt engine program charter and terminology
    status: superseded
    closedAt: 2026-08-15
    decisionSummary: "The Werkstatt engine becomes a family of private npm packages
      developed in this monorepo and published via `@warpgogol/repo-extract`.
      The program establishes the following terminology and taxonomy (normative
      for all downstream RFCs):"
    file: docs/rfcs/archive/superseded/rfc-0769-werkstatt-engine-program-charter-and-terminology.md
  - kind: rejected-alternative
    rfcId: RFC-0769
    title: Werkstatt engine program charter and terminology
    status: superseded
    alternativesText: >-
      - **Vendored spec package (DNA-55) instead of an RFC series.** Rejected:
      DNA-55 targets external specifications; this program is internal and
      iterates with the codebase. The PBP charter precedent (RFC-0398) fits
      better.

      - **Separate engine repository.** Rejected by the operator: development
      stays in this monorepo; publication goes through repo-extract, matching
      the forge precedent.

      - **Publishing current `packages/os/*` as ~10 packages.** Rejected by the
      operator: consumer installs one engine + one plugin; the version matrix of
      10 interdependent packages is operational noise.

      - **Keeping domain packages (ui, pbp, ontology, ...) in the workshop.**
      Rejected by the operator: any site workshop gets the full site capability
      from `@warpgogol/werkstatt-site`.
    file: docs/rfcs/archive/superseded/rfc-0769-werkstatt-engine-program-charter-and-terminology.md
  - kind: superseded-rfc
    rfcId: RFC-0770
    title: Werkstatt plugin contract and stack profile binding
    status: superseded
    closedAt: 2026-08-15
    decisionSummary: "The engine gains a **plugin contract** (`werkstatt/plugin@1`):
      a typed interface that a stack plugin implements and registers, plus a
      **profile binding** rule: `forge.yaml` `profile` field (RFC-0643) resolves
      to a forge stack profile id, and the workshop's `tools/kernel.config.ts`
      composes the engine with the plugin whose `profileId` matches."
    file: docs/rfcs/archive/superseded/rfc-0770-werkstatt-plugin-contract-and-stack-profile-binding.md
  - kind: rejected-alternative
    rfcId: RFC-0770
    title: Werkstatt plugin contract and stack profile binding
    status: superseded
    alternativesText: >-
      - **Peer-dependency imports (engine imports plugin by convention name).**
      Rejected: still a static coupling; breaks DNA-64 and makes the engine
      untestable without a plugin.

      - **Config-only plugins (pure YAML, no code).** Rejected: validators,
      codegen, and deploy adapters are code; YAML cannot express them. Profiles
      stay data; plugins stay code.

      - **Multiple plugins per workshop (site + video in one).** Rejected for
      now: one workshop = one stack keeps mission/release semantics unambiguous.
      A future RFC may relax this.
    file: docs/rfcs/archive/superseded/rfc-0770-werkstatt-plugin-contract-and-stack-profile-binding.md
  - kind: rejected-alternative
    rfcId: RFC-0771
    title: Werkstatt engine core package composition
    status: implemented
    alternativesText: >-
      - **Keep handoff as a separate package.** Rejected: handoff IS the
      engine's lifecycle core; separating it recreates the version matrix
      problem.

      - **Move `fingerprint` into the plugin.** Rejected: release hashing,
      artifact store, and build identity are engine concerns; DNA-53 is
      stack-agnostic.

      - **Split changelog entirely into the site plugin.** Rejected: the
      pipeline (collect → summarize → write) is generic; only renderers/targets
      are site-specific.
    file: docs/rfcs/archive/implemented/rfc-0771-werkstatt-engine-core-package-composition.md
  - kind: rejected-alternative
    rfcId: RFC-0772
    title: Consolidate engine core into packages werkstatt with plugin registry
    status: implemented
    alternativesText: >-
      - **git-filter moves preserving history in a new repo.** Not applicable —
      development stays in this monorepo; `git log --follow` covers history.

      - **Keep old packages as permanent re-export shims.** Rejected: charter
      principle 3 (no legacy).

      - **Invert imports without a registry (constructor injection
      everywhere).** Rejected: mission/leitstand handlers are invoked by the
      kernel command table, not constructed by consumers; a registry is the
      natural seam.
    file: docs/rfcs/archive/implemented/rfc-0772-consolidate-engine-core-into-packages-werkstatt-with-plugin-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0773
    title: Engine publication pipeline via repo-extract to private npm
    status: implemented
    alternativesText: >-
      - **Publishing from the monorepo directly (pnpm publish with workspace
      protocol rewrite).** Rejected by the operator: the repo-extract
      external-folder flow is the established model, keeps the private monorepo
      boundary clean, and produces a standalone git repo per package.

      - **Changesets-based automated versioning.** Rejected for now: four
      packages, operator-triggered releases; `ecosystem.commit` version log
      already exists.
    file: docs/rfcs/archive/implemented/rfc-0773-engine-publication-pipeline-via-repo-extract-to-private-npm.md
  - kind: rejected-alternative
    rfcId: RFC-0774
    title: Werkstatt site plugin engine modules
    status: implemented
    alternativesText: >-
      - **Keeping checks/codegen as separate npm packages.** Rejected: operator
      decision — one plugin package per stack.

      - **Splitting the plugin into engine-half and domain-half packages.**
      Rejected: two RFCs (0774/0775) for review clarity, but one published
      package — consumers install one thing.
    file: docs/rfcs/archive/implemented/rfc-0774-werkstatt-site-plugin-engine-modules.md
  - kind: rejected-alternative
    rfcId: RFC-0775
    title: Werkstatt site plugin domain layer consolidation
    status: implemented
    alternativesText: >-
      - **Keep domain packages as separate npm packages.** Rejected by the
      operator: any site workshop gets the full site capability from one
      install.

      - **Leave domain packages in the workshop, not published.** Rejected by
      the operator: other site workshops would have to reinvent or copy them.
    file: docs/rfcs/archive/implemented/rfc-0775-werkstatt-site-plugin-domain-layer-consolidation.md
  - kind: rejected-alternative
    rfcId: RFC-0776
    title: Migrate this workshop to npm engine consumption
    status: implemented
    alternativesText: >-
      - **Gradual migration package-by-package.** Rejected: the import sweep is
      mechanical and the old packages have cross-dependencies — partial
      migration creates a broken intermediate state.

      - **Keep `site-kernel` as a permanent CLI alias.** Rejected: charter
      principle 3 (no legacy).
    file: docs/rfcs/archive/implemented/rfc-0776-migrate-this-workshop-to-npm-engine-consumption.md
  - kind: rejected-alternative
    rfcId: RFC-0777
    title: Werkstatt game plugin for phaser turborepo stack
    status: implemented
    alternativesText: >-
      - **Using the site plugin for games (Astro + Phaser integration).**
      Rejected: Phaser games are not Astro sites; the build, deploy, and
      validation models are fundamentally different.

      - **No deploy adapters in the plugin (manual deploy).** Rejected: the
      Leitstand automate promote flow requires adapter plugins.
    file: docs/rfcs/archive/implemented/rfc-0777-werkstatt-game-plugin-for-phaser-turborepo-stack.md
  - kind: rejected-alternative
    rfcId: RFC-0778
    title: Werkstatt video plugin for editframe stack
    status: implemented
    alternativesText: >-
      - **Using the site plugin for video (Astro + Editframe integration).**
      Rejected: video compositions are not Astro sites; the render and deploy
      models are fundamentally different.

      - **No deploy adapter (manual render + upload).** Rejected: the Leitstand
      automate promote flow requires adapter plugins; video needs the same
      release discipline as sites and games.


      ### Render determinism (WV-03)


      WV-03 requires byte-identical render output across runs with identical
      input. Video encoding is non-deterministic by default (variable bitrate,
      metadata timestamps, encoder version differences). The plugin enforces
      determinism via:


      1. **Fixed codec settings** — H.264 with fixed profile (Main), level, and
      preset.

      2. **Constant bitrate (CBR)** — no VBR, no CRF-based encoding.

      3. **No metadata** — all metadata stripped (`-map_metadata -1` in ffmpeg).

      4. **Fixed GOP size** — no adaptive keyframe placement.

      5. **Pinned ffmpeg version** — the render hook declares the ffmpeg
      version; CI uses the same version. Local renders must match.

      6. **Fixed frame rate** — no variable frame rate input.


      The `video.render.validate` command renders the composition twice and
      compares sha256 hashes. If hashes differ, WV-03 fails with a diff report
      showing which encoding setting caused the divergence.


      ### Edge cases


      - **Empty composition project** (no assets): `video.assets.validate`
      passes with an empty manifest; `video.composition.validate` checks the
      time model only.

      - **No render output** (before first render): `video.render.validate`
      reports `WV-03: no render output found` and exits 1 — the validator
      expects a prior render to exist.

      - **Concurrent renders**: the render hook uses the engine's lock
      primitives (DNA-51) to prevent concurrent renders of the same composition.

      - **Interrupted render**: partial render output is not committed to the
      artifact store; the lock is released via the engine's idempotency
      primitives.


      ### False-positive estimation


      - `WV-01` (time model): false positives near zero — duration and frame
      rate are parsed from the composition's Timegroup attributes.

      - `WV-02` (asset references): low false-positive rate — asset paths are
      resolved relative to `src/assets/`; false positives only from dynamic path
      construction.

      - `WV-03` (determinism): moderate false-positive rate during environment
      transitions (different ffmpeg version, different OS). Suppression: the
      validator reports the environment fingerprint (ffmpeg version, OS)
      alongside the hash; mismatches are flagged as environment drift, not
      composition errors.

      - `WV-07` (orphaned assets): moderate false-positive rate — assets
      referenced dynamically (constructed paths) may appear orphaned.
      Suppression: an allowlist in the asset manifest (`dynamicRefs:
      [pattern]`).
    file: docs/rfcs/archive/implemented/rfc-0778-werkstatt-video-plugin-for-editframe-stack.md
  - kind: rejected-alternative
    rfcId: RFC-0779
    title: Consumer workshop scaffolding and onboarding
    status: implemented
    alternativesText: >-
      - **Manual workshop creation (copy from this monorepo).** Rejected: this
      monorepo has too much site-specific content; copying produces a workshop
      with hidden site assumptions.

      - **`forge.create` as the workshop scaffolder.** Rejected: `forge.create`
      scaffolds a project inside a workshop; `workshop.scaffold` creates the
      workshop itself. They are different abstraction levels.
      `workshop.scaffold` delegates to `forge.init` for forge-specific artifacts
      (forge.yaml, skills, AGENTS.md, docs dirs).
    file: docs/rfcs/archive/implemented/rfc-0779-consumer-workshop-scaffolding-and-onboarding.md
  - kind: rejected-alternative
    rfcId: RFC-0780
    title: Workshop v5.0.0 major version bump
    status: implemented
    alternativesText: >-
      - **Continue as 4.91.x**: Rejected — misrepresents the breaking nature of
      the migration.

      - **Minor bump to 4.91.0**: Rejected — SemVer minor implies
      backward-compatible features, not a package structure overhaul.

      - **No version bump**: Rejected — the root `package.json` scripts still
      referenced `site-kernel` binary paths that no longer exist; these must be
      updated and committed.
    file: docs/rfcs/archive/implemented/rfc-0780-workshop-v5-major-version-bump.md
  - kind: rejected-alternative
    rfcId: RFC-0781
    title: "PBP compiler: locale-aware entity index and JSON Merge Patch deep-merge"
    status: implemented
    alternativesText: >-
      - **Workaround: unique entity IDs per locale** (e.g.
      `currency-pricing-policy-de`, `currency-pricing-policy-uk`): Rejected —
      this breaks referential integrity. Other entities reference
      `currency-pricing-policy/default` by ID; having locale-specific IDs would
      require duplicate refs everywhere.

      - **Workaround: separate PBP directories per locale with separate compiler
      runs**: Rejected — this defeats the purpose of `resolveLocales` and
      prevents fallback to default locale for entities that don't have a
      locale-specific override.

      - **Keep `PBP-ID-DUPLICATE` for same-locale, add separate cross-locale
      handling**: Rejected — adds complexity. The locale-aware index naturally
      handles both cases: same-locale duplicates are fatal, cross-locale same-ID
      is expected.

      - **Use structured clone instead of deep-merge**: Rejected — overlays are
      partial; deep-merge is needed to fill in fields that the overlay doesn't
      specify from the base.
    file: docs/rfcs/archive/implemented/rfc-0781-pbp-compiler-locale-aware-entity-index-and-deep-merge.md
  - kind: rejected-alternative
    rfcId: RFC-0782
    title: Per-language currency policies via locale-scoped PBP overrides
    status: implemented
    alternativesText: >-
      - **Show currency selector on all languages**: Rejected — German visitors
      don't need currency selection. EUR is the only relevant currency for the
      German market. Showing a selector with one option is confusing.

      - **Keep `wg-currency` key, reset on language switch**: Rejected —
      requires a language-change event listener and explicit cleanup.
      Locale-scoped keys are simpler and more robust.

      - **Server-side currency detection**: Rejected — the site is statically
      generated. Currency selection is a client-side concern (localStorage +
      custom events). No server roundtrip.

      - **Separate currency-pricing-policy IDs per locale**: Rejected — breaks
      referential integrity. Other entities reference
      `currency-pricing-policy/default` by ID. Locale-specific IDs would require
      duplicate refs everywhere. RFC-0781's deep-merge approach is the correct
      solution.
    file: docs/rfcs/archive/implemented/rfc-0782-per-language-currency-policies-via-locale-scoped-pbp-overrides.md
  - kind: rejected-alternative
    rfcId: RFC-0783
    title: Add agent discovery metadata generators for API Catalog and MCP Server Card
    status: implemented
    alternativesText: >-
      1. **Single aggregated generator** (`agent.discovery.generate` producing
      all files) — rejected because the existing pattern is one generator per
      output format (`agent.manifest.generate`, `agent.openapi.generate`,
      `agent.knowledge.generate`). Separate generators allow independent
      pipeline scheduling and validation.


      2. **Agent Skills Discovery Index**
      (`/.well-known/agent-skills/index.json`) — excluded from this RFC.
      Workshop skills (`wg-site-content-edit`, `fo-*`) are development-time
      tools used inside the monorepo; client sites are static builds that do not
      know or advertise how they were built. The MCP `tools/list` method already
      exposes runtime capabilities to visiting agents.


      3. **Cascading generation from public `agent.json`** — rejected because
      `agent.openapi.generate` reads the internal manifest
      (`src/agent-surface.generated.yaml`), not the public mirror. Following the
      same pattern keeps the generation graph consistent.
    file: docs/rfcs/archive/implemented/rfc-0783-add-agent-discovery-metadata-generators-for-api-catalog-and-mcp-server-card.md
  - kind: rejected-alternative
    rfcId: RFC-0784
    title: Add agent discovery Link headers and Content-Signal directive in robots.txt
    status: implemented
    alternativesText: >-
      1. **Cloudflare Worker for Link headers** — generate `Link:` headers
      dynamically via a Worker instead of static `_headers`. Rejected:
      Cloudflare Pages `_headers` file supports `Link:` headers natively; a
      Worker adds runtime cost and complexity for static content.


      2. **Separate `agent.headers.generate` command** — a new generator that
      writes only the agent discovery Link headers. Rejected: the `_headers`
      file is owned by `public.infrastructure.generate` (RFC-0315); splitting
      header generation across two commands creates merge conflicts and ordering
      issues.


      3. **Content-Signal as a separate file** — a `/.well-known/content-signal`
      file instead of a `robots.txt` directive. Rejected: `Content-Signal:` is
      designed as a `robots.txt` directive, not a standalone file. Following the
      spec is more compatible.
    file: docs/rfcs/archive/implemented/rfc-0784-add-agent-discovery-link-headers-and-content-signal-directive-in-robots-txt.md
  - kind: rejected-alternative
    rfcId: RFC-0785
    title: Add markdown content negotiation for agent requests via Cloudflare Worker
    status: implemented
    alternativesText: >-
      1. **`_headers` redirect rules** — use Cloudflare Pages `_redirects` to
      redirect `Accept: text/markdown` requests to the `.md` file. Rejected:
      `_redirects` does not support `Accept` header matching; it only matches
      path patterns.


      2. **Cloudflare Pages Function (`functions/[[path]].ts`)** — deploy a
      Pages Function that intercepts requests. Rejected: RFC-0149 explicitly
      retired Pages Functions and deleted all `functions/` directories.
      Reintroducing `functions/` would regress to the pre-RFC-0149 model and
      conflict with the established Astro middleware pattern.


      3. **No content negotiation — agents discover `.md` twins via
      `agent.json`** — the `agent.json` manifest already declares
      `interfaces.twins.pattern: /**.md`. Rejected: while true,
      isitagentready.com specifically checks for Accept-header negotiation.
      Supporting both paths (direct `.md` fetch + content negotiation) maximizes
      agent compatibility.


      4. **Astro on-demand route (`src/pages/[...path].ts` with `prerender =
      false`)** — a catch-all API route that performs content negotiation.
      Rejected: a catch-all route would intercept all requests, not just
      markdown-negotiation requests, adding unnecessary overhead to every page
      load. Middleware is more selective — it only acts when `Accept:
      text/markdown` is present, otherwise it passes through with zero overhead.
    file: docs/rfcs/archive/implemented/rfc-0785-add-markdown-content-negotiation-for-agent-requests-via-cloudflare-worker.md
  - kind: rejected-alternative
    rfcId: RFC-0786
    title: Add DNS-AID record generator for agent discovery via DNS TXT records
    status: implemented
    alternativesText: >-
      1. **Automatic DNS record creation in the pipeline** — have
      `agent.dns-aid.generate` call the Cloudflare API directly instead of just
      writing a declaration. Rejected: DNS changes are infrastructure changes
      that should be explicit operator actions. The two-step process (generate
      declaration, then `dns.record.upsert`) follows the RFC-0753 pattern and
      gives the operator control.


      2. **Manual addition to `dns-records.yaml`** — operators add the DNS-AID
      TXT record manually. Rejected: without a generator, the record can drift
      from the manifest URL if the domain or agent surface changes. The
      generator ensures sync.


      3. **DNS-AID as a separate file** — a `dns-aid.yaml` fragment included by
      `dns-records.yaml`. Rejected: adds complexity. A marked section in the
      existing `dns-records.yaml` is simpler and follows the
      single-declaration-file pattern from RFC-0753.
    file: docs/rfcs/archive/implemented/rfc-0786-add-cloudflare-dns-management-commands-for-dns-aid-and-general-dns-record-management.md
  - kind: rejected-alternative
    rfcId: RFC-0787
    title: Wire agent readiness generators into build.prepare and build.check
      pipelines
    status: implemented
    alternativesText: >-
      1. **Standalone pipeline** — a separate `agent.readiness.pipeline` that
      runs independently. Rejected: agent readiness is part of the normal build,
      not a separate concern. A standalone pipeline would require operators to
      remember to run it.


      2. **Conditional pipeline steps** — only run agent generators when
      `agent.enabled: true`. Rejected: the generators already handle the
      `agent.enabled: false` case internally (skip pattern). Adding
      pipeline-level conditionals would duplicate the skip logic.
    file: docs/rfcs/archive/implemented/rfc-0787-wire-agent-readiness-generators-into-build-prepare-and-build-check-pipelines.md
  - kind: rejected-alternative
    rfcId: RFC-0788
    title: Add agent-friendly sitemap extensions for agent crawl discovery
    status: implemented
    alternativesText: >-
      1. **Separate `sitemap-agents.xml`** — a dedicated sub-sitemap for agent
      content. Rejected: markdown alternates are per-page, not per-sitemap.
      Adding them to existing entries is simpler and follows the sitemap
      protocol extension pattern.


      2. **`<xhtml:link type="application/json">` for structured data** — add
      JSON-LD alternate links. Rejected: JSON-LD is already embedded in HTML
      pages via `<script type="application/ld+json">`. Adding it to the sitemap
      is redundant.


      3. **No sitemap changes — agents discover twins via `agent.json`** —
      Rejected: sitemap is the standard crawl discovery mechanism. Adding
      markdown alternates makes the sitemap self-describing for agents.
    file: docs/rfcs/archive/implemented/rfc-0788-add-agent-friendly-sitemap-extensions-for-agent-crawl-discovery.md
  - kind: rejected-alternative
    rfcId: RFC-0789
    title: Enhance llms.txt generation with agent surface discovery links
    status: implemented
    alternativesText: >-
      1. **Single `agent.json` link only** — keep the existing single link to
      `agent.json` and let agents discover the other endpoints from there.
      Rejected: `llms.txt` is the primary entry point for many agents. Including
      direct links to all discovery endpoints reduces the number of fetches an
      agent needs to make.


      2. **Discovery links in a separate section** — add a `## Agent discovery`
      section at the bottom of `llms.txt`. Rejected: the blockquoted header is
      the standard place for cross-references per the llms.txt convention. A
      separate section would be less visible.
    file: docs/rfcs/archive/implemented/rfc-0789-enhance-llms-txt-generation-with-agent-surface-discovery-links.md
  - kind: rejected-alternative
    rfcId: RFC-0790
    title: Move per-system configuration into cache clone and replace fleet registry
      with convention-based discovery
    status: implemented
    alternativesText: >-
      - **Keep `registry.yaml` as a minimal bootstrap index (id + mirrors
      only)**: Rejected — the bootstrap circular dependency remains (registry
      defines where cache clones are, so registry cannot live in a cache clone).
      A minimal index still requires a central file that must be read for every
      operation. Convention-based path derivation (`../systems-cache/<id>/`)
      eliminates the need for any index.

      - **Configurable cache clone path via `forge.yaml`**: Rejected — adds one
      level of indirection without benefit. The convention `../systems-cache/`
      is sufficient for all workshops. If a workshop needs a different layout,
      it is a different workshop configuration, not a per-system concern.

      - **Two-phase migration (fallback compatibility, then remove registry)**:
      Rejected — the Werkstatt does not maintain backward compatibility. A
      single atomic migration is simpler and cleaner. The engine reads from one
      source only, no fallback logic.

      - **Sharded cache clone directories (`../systems-cache/{prefix}/<id>/`)**:
      Rejected for now — at current scale (single system), flat layout is
      sufficient. Sharding can be introduced in a future RFC when the system
      count reaches thousands. The convention path function can be extended to
      support sharding without breaking existing systems.
    file: docs/rfcs/archive/implemented/rfc-0790-move-per-system-configuration-into-cache-clone-and-replace-fleet-registry-with-convention-based-discovery.md
  - kind: rejected-alternative
    rfcId: RFC-0791
    title: Auto-discover well-known routes in public-surface validation
    status: implemented
    alternativesText: >-
      - **Separate `discoverWellKnownRoutes` function returning `Set<string>`**:
      A dedicated helper that scans `public/.well-known/` recursively and
      returns route paths for merging into `routePaths`. Rejected in favor of
      extending `publicPaths` because the existing `publicPaths` mechanism
      already handles the PUBTXT-07 check at `aggregate.ts:298-301` — adding to
      `publicPaths` is a 5-line change vs a new function with its own signature.
      The `publicPaths` approach is also semantically correct: these are files
      on disk, not abstract routes.


      - **Extending `isPublicTextArtifact` to include extensionless files under
      `.well-known/`**: Modifying the filter to accept extensionless paths when
      they start with `.well-known/`. Rejected because it would change the
      filter's behavior for all callers, not just `publicPaths` building. The
      complementary glob is scoped to the `publicPaths` building logic only.


      - **Contract test extracting links from `llms.txt`**: A test that parses
      `llms.txt`, extracts all links, and verifies each is in `routePaths`.
      Rejected as primary solution because it catches the problem at test time,
      not at validation time — the developer still needs to register routes
      manually. Auto-discovery eliminates the manual step entirely.


      - **Sitemap inclusion for `.well-known/` files**: Adding well-known
      endpoints to the sitemap. Rejected because `.well-known/` files are
      machine-readable endpoints, not human-navigable pages — they do not belong
      in a sitemap.
    file: docs/rfcs/archive/implemented/rfc-0791-auto-discover-well-known-routes-in-public-surface-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0792
    title: Extend sternsystem.validate with YAML syntax checking for system config
      files
    status: implemented
    alternativesText: >-
      - **Pre-commit hook**: A git pre-commit hook that lints YAML files.
      Rejected as primary solution because pre-commit hooks are local-only and
      can be bypassed. `sternsystem.validate` runs in CI and in the pipeline,
      providing a stronger guarantee.


      - **Standalone `system-config.lint` command**: A new command dedicated to
      YAML linting. Rejected because it fragments validation — operators would
      need to remember to run it separately. Extending `sternsystem.validate`
      keeps all system-level checks in one command.


      - **Schema validation (not just syntax)**: Validating that YAML files
      conform to a schema (e.g., `dns-records.yaml` has the expected fields).
      Rejected for this RFC — schema validation is a larger effort and should be
      a separate RFC if needed. Syntax validation is the minimal fix that
      prevents the failure mode observed in the RFC-0789 session.
      `dns.records.schema.validate` already provides schema validation for
      `dns-records.yaml`; this RFC adds syntax-only checking for all YAML files
      as a first-line defense within `sternsystem.validate`.
    file: docs/rfcs/archive/implemented/rfc-0792-extend-sternsystem-validate-with-yaml-syntax-checking.md
  - kind: rejected-alternative
    rfcId: RFC-0793
    title: Fix SPDX license normalization in open-source page generator
    status: implemented
    alternativesText: >-
      - **Full SPDX expression parser**: Use a library like
      `spdx-expression-parse` to parse complex expressions. Rejected — the vast
      majority of npm license fields are single SPDX IDs or simple `OR`/`AND`
      expressions. Stripping parentheses and reusing the existing split-based
      parser is sufficient. Adding a dependency for edge cases that affect <1%
      of packages is over-engineering.


      - **Filter Unknown from all artifacts**: Exclude packages with unknown
      licenses from the component table, SBOM, and THIRD_PARTY_NOTICES as well.
      Rejected — compliance requires listing all bundled dependencies, even
      those with unclear licensing. The `Unknown` group is only useless in the
      distribution chart; in the component table and SBOM it signals that the
      package needs manual license review.


      - **Keep Python-2.0 alias and make it work**: Reorder the alias lookup
      before the SPDX ID check so `Python-2.0` normalizes to `PSF-2.0`. Rejected
      — `Python-2.0` and `PSF-2.0` are distinct SPDX licenses. Normalizing one
      to the other is semantically wrong. The alias was a mistake; removing it
      is the correct fix.
    file: docs/rfcs/archive/implemented/rfc-0793-fix-spdx-license-normalization-in-open-source-page-generator.md
  - kind: rejected-alternative
    rfcId: RFC-0794
    title: Fix cache clone state sync, evidence path resolution, and lifecycle
      commit guard
    status: implemented
    alternativesText: >-
      ### Alternative 1: Change `syncCacheClone` to preserve uncommitted changes


      Instead of pushing in `writeSystemState`, modify `syncCacheClone` to stash
      or merge uncommitted changes before `git reset --hard`.


      **Rejected**: `syncCacheClone`'s `git reset --hard` is intentional — it
      ensures the cache clone is a clean mirror of the bare repo. Stashing or
      merging would introduce complexity and potential conflict resolution in a
      step that should be deterministic. The push in `writeSystemState` is the
      correct fix — it ensures the commit is durable in the bare repo before any
      reset.


      ### Alternative 2: Symlink `missions/<id>/evidence/` to
      `missions/archive/closed/<id>/evidence/`


      Instead of a code-level fallback in `leitstand.propagate`, create a
      symlink during `mission.close` that redirects the active mission path to
      the archive.


      **Rejected**: Symlinks are fragile across operating systems, git
      operations, and CI environments. A code-level fallback is explicit,
      testable, and does not require filesystem-specific behavior.


      ### Alternative 3: Have `commitWerkstattSideEffects` call
      `ecosystem.commit` instead of `git commit`


      Instead of setting `ECOSYSTEM_COMMIT=1`, route the commit through
      `ecosystem.commit`.


      **Rejected**: `ecosystem.commit` runs `pnpm install` as a post-commit deps
      status check and performs version bumping. Lifecycle auto-commits are
      internal side-effects (e.g., `mission.yaml` updates), not platform
      releases. Running `ecosystem.commit` for each would be excessive and slow.
      The `ECOSYSTEM_COMMIT=1` env var is the minimal, correct bypass.
    file: docs/rfcs/archive/implemented/rfc-0794-fix-cache-clone-state-sync-evidence-path-resolution-and-lifecycle-commit-guard.md
  - kind: rejected-alternative
    rfcId: RFC-0795
    title: Add RFC dependency tracking and batch grouping to frontmatter
    status: implemented
    alternativesText: >-
      1. **Warning-only at `fo-idea-implement` (no stamp block).** Rejected: the
      operator chose hard block at stamping. A warning at implementation start
      is insufficient because the agent may proceed and produce code that
      references contracts not yet established. The stamping gate is the last
      checkpoint before `implemented` status — blocking here prevents incomplete
      implementations from being marked done.


      2. **Structured `batch` object (`{ id, size, order }`).** Rejected: the
      order of implementation is already expressed via `dependsOn`. The size of
      a batch is a derived value (count of RFCs with the same slug). Additional
      metadata would complicate frontmatter and validation without adding value.


      3. **Separate `rfc.batch.list` command.** Rejected: `rfc.list` already
      scans all RFCs. Adding a `--batch` flag is a minimal extension. A separate
      command would duplicate scanning logic for a single filter criterion.


      4. **Transitive dependency resolution.** Rejected: transitivity would
      require building a dependency graph and topologically sorting it at stamp
      time. If RFC-C truly depends on RFC-A (transitively through RFC-B), it
      should declare `dependsOn: [RFC-B, RFC-A]` explicitly. Direct dependencies
      are readable in frontmatter without computation.


      5. **Bidirectional `dependedBy` field.** Rejected: unlike
      `supersedes`/`supersededBy` (1:1), dependencies are potentially N:M. A
      `dependedBy` field would need to be updated on RFC-A every time a new RFC
      depends on it — fragile and prone to drift. Reverse lookup is done by
      scanning all RFCs for `dependsOn` references, which `rfc.list --json`
      already supports.


      6. **Machine-enforced session affinity.** Rejected: sessions have no
      machine-readable identity in forge. Session affinity is a skill-level
      recommendation, not an enforceable contract. The plan file and RFC body
      capture the necessary context for cross-session implementation.
    file: docs/rfcs/archive/implemented/rfc-0795-add-rfc-dependency-tracking-and-batch-grouping-to-frontmatter.md
  - kind: rejected-alternative
    rfcId: RFC-0796
    title: Harden mission directory lifecycle against stale symlinks and residual
      entries
    status: implemented
    alternativesText: >-
      - **Change `pnpm-workspace.yaml` to exclude symlinks**: pnpm does not
      support excluding symlinks in workspace globs. This would require changing
      the glob pattern to `missions/*/workpiece` → something more specific, but
      pnpm's glob engine does not support negative patterns or symlink
      filtering.


      - **Watch-based symlink cleanup daemon**: A background process that
      monitors `missions/` for symlink creation and auto-removes them. Rejected
      as over-engineered — adds a daemon dependency, requires lifecycle
      management, and the problem is already addressed by the archive-at-close
      approach.


      - **Make `mission.archive` recursive on close**: Instead of calling
      `mission.archive` from `mission.close`, inline the archive logic. Rejected
      — `mission.archive` is a standalone command with its own pinned-files
      protection, dry-run support, and output format. Calling it via
      `executeKernelCommand` reuses all of that.


      - **Remove `missions/*/workpiece` from `pnpm-workspace.yaml`**: This would
      prevent workpiece dependencies from being linked into the workspace,
      breaking `mission.materialize` and `mission.preview`. Not viable.
    file: docs/rfcs/archive/implemented/rfc-0796-harden-mission-directory-lifecycle-against-stale-symlinks-and-residual-entries.md
  - kind: rejected-alternative
    rfcId: RFC-0797
    title: Eliminate manual git interventions from mission close pipeline
    status: implemented
    alternativesText: >-
      - **Remove the mirror sync blocking check (RFC-0705)**: Rejected — the
      check catches genuine desync (e.g., external mirror push failure from a
      previous sync). The fix targets false positives from the close's own
      inline validate, not the check itself.


      - **Make `bordbuch.commit` update the mirror ref**: Rejected —
      `bordbuch.commit` is a pipeline step that runs during `build.check`. It
      should not be responsible for mirror ref management. Mirror ref updates
      are `sternsystem.sync`'s job.


      - **Split `sternsystem.sync` into "update mirror ref" and "push to
      external mirrors"**: Rejected — adds complexity for a narrow use case.
      Running the full `sternsystem.sync` after the inline validate is simpler
      and safe: the bordbuch commit is a valid state to push to external
      mirrors.


      - **Add `--auto-commit` flag to `mission.close`**: Rejected — the behavior
      should be default, not opt-in. The operator should not need to know about
      internal git state. `mission.reconcile` already auto-commits the workpiece
      (RFC-0644) without a flag.


      - **Extend `commitBordbuchProjections` to cover more file paths**:
      Rejected — maintaining a whitelist of generated file paths is fragile. New
      pipeline steps can create new generated files. `git add -A` in the cache
      clone is safe because the cache clone is entirely generated.
    file: docs/rfcs/archive/implemented/rfc-0797-eliminate-manual-git-interventions-from-mission-close-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0798
    title: Add lead.submit as first agent-callable capability
    status: implemented
    alternativesText: >-
      - **Multiple capabilities at once:** Rejected — starting with one
      capability validates the end-to-end pipeline before adding complexity.

      - **A generic `contact.submit` instead of `lead.submit`:** Rejected —
      `lead` is the integration event kind already defined in the schema
      (`eventKind: "lead" | "message" | "appointment"`). Naming alignment
      between capability id and event kind reduces cognitive load.

      - **Adding entitlement gating beyond `agent.actions`:** Rejected —
      `agent.actions` is the universal gate for the action tier. Per-capability
      entitlements can be added later via `requires.entitlements` if needed.
    file: docs/rfcs/archive/implemented/rfc-0798-add-lead-submit-as-first-agent-callable-capability.md
  - kind: rejected-alternative
    rfcId: RFC-0799
    title: Browser-side WebMCP via document.modelContext.registerTool
    status: implemented
    alternativesText: >-
      - **Custom event dispatch:** Rejected — `document.modelContext` is the
      emerging standard. Custom events would require agent-side polling.

      - **Window-level global:** Rejected — `document.modelContext` is the
      proposed attachment point. `window.modelContext` is non-standard.

      - **Full MCP server in browser:** Rejected — overkill for discovery. The
      server-side endpoint handles transport; the browser script handles
      discovery only.
    file: docs/rfcs/archive/implemented/rfc-0799-browser-side-webmcp-via-document-modelcontext-registertool.md
  - kind: rejected-alternative
    rfcId: RFC-0800
    title: Guarantee template dependency sync via drift check and auto-sync on
      mission close
    status: implemented
    alternativesText: >-
      - **Warning-only drift check**: rejected — warnings can be ignored,
      defeating the guarantee. The operator explicitly chose error-level
      enforcement.


      - **Auto-sync in mission.reconcile instead of close**: rejected —
      reconcile may run multiple times per mission, producing redundant sync
      commits. Close runs once, at the end.


      - **Auto-sync only, no drift check**: rejected — if auto-sync fails
      silently, the drift goes undetected. The drift check is the safety net.


      - **Drift check only, no auto-sync**: rejected — operator would need to
      manually run `config.template.sync` to fix drift, reintroducing the manual
      discipline problem this RFC eliminates.


      - **Compare all package.json fields (scripts, engines)**: rejected —
      scripts and engines rarely change and are already controlled by the
      template. Comparing only `dependencies` and `devDependencies` is the
      minimal effective scope.
    file: docs/rfcs/archive/implemented/rfc-0800-guarantee-template-dependency-sync-via-drift-check-and-auto-sync-on-mission-close.md
  - kind: rejected-alternative
    rfcId: RFC-0801
    title: Separate mission.archive from mission.close and clean service folders on
      archive
    status: implemented
    alternativesText: >-
      1. **Keep auto-archive, fix symlinks after move** — Re-run `pnpm install`
      in the archived location. Rejected: `pnpm install` is slow (30+ seconds),
      the archived workpiece is not a workspace member after archiving, and the
      install would create new symlinks that are still wrong for the archive
      depth.


      2. **Keep auto-archive, make `release.prepare` work from archive** — Fix
      `workpiece.imports.validate` and `template.deps.drift` to search archive
      directories (already done as a bug fix). Rejected: This treats the
      symptom, not the cause. The build still fails because `astro build` cannot
      find `node_modules/astro/bin/astro.mjs`. Fixing all possible
      `node_modules` resolution paths is fragile and incomplete.


      3. **Keep auto-archive, add `--skip-auto-archive` flag** — The flag
      existed in code but was not registered in the active schema. Rejected as
      the primary solution: even with the flag registered, the workflow is
      fragile because the operator must remember to pass it every time.
      Auto-archive is the wrong default for a deployment pipeline that requires
      post-close steps.
    file: docs/rfcs/archive/implemented/rfc-0801-separate-mission-archive-from-mission-close-and-clean-service-folders-on-archive.md
  - kind: rejected-alternative
    rfcId: RFC-0802
    title: Add interactive maturity mountain page with GSAP camera pan and marker
      animation
    status: implemented
    alternativesText: >-
      1. **PixiJS for GPU-accelerated 2D rendering.** Rejected — the use case
      (one image, one SVG path, one marker, one zoom animation) does not require
      GPU acceleration. PixiJS adds significant bundle size and complexity for
      no visual benefit over SVG + GSAP.


      2. **CSS-only animation (transforms + transitions).** Rejected — CSS
      cannot animate an element along an arbitrary SVG path without JavaScript.
      The `offset-path` CSS property has limited browser support and no GSAP
      integration for timeline control.


      3. **Scroll-driven animation (ScrollTrigger pin + horizontal pan).**
      Rejected by the operator — the camera pan is triggered by form submission
      (interactive), not by scroll position. The visitor enters a URL, the
      Worker returns a score, and the animation plays once to that position.


      4. **Site-local section in warpgogol-com.** Rejected — violates the
      composition-only principle (RFC-0047). The `mountain-journey` archetype is
      reusable across sites and belongs in the shared package.


      5. **SVG-only mountain (no raster image).** Rejected — the operator
      provided a specific raster illustration (WebP) with rich visual detail
      that cannot be reproduced as vector SVG without significant artistic
      effort. The raster + SVG overlay approach preserves the visual quality
      while keeping the route path and markers as scalable vector elements.
    file: docs/rfcs/archive/implemented/rfc-0802-add-interactive-maturity-mountain-page-with-gsap-camera-pan-and-marker-animation.md
  - kind: rejected-alternative
    rfcId: RFC-0803
    title: Add per-page deployment gating for production builds
    status: implemented
    alternativesText: >-
      1. **Git branch-based gating** — keep gated features on a separate branch,
      merge to main only when ready for production. Rejected: the Werkstatt
      workflow is mission-based, not branch-based. Missions commit to the cache
      clone's main branch. Branch-based gating would conflict with the mission
      lifecycle.


      2. **Feature flags in environment variables** — use
      `import.meta.env.PUBLIC_FEATURE_REIFE` to conditionally render content.
      Rejected: requires code changes in components and pages. Not
      content-declarative. Does not prevent route generation or sitemap
      inclusion. Violates DNA-9 (no route-local `if` guards).


      3. **Separate content directory** — put gated pages in
      `src/content/pages-gated/` excluded from production builds. Rejected:
      changes the content architecture. Requires moving files when ungateing.
      Does not handle navigation filtering automatically.


      4. **Per-block gating** — gate individual blocks within a page rather than
      whole pages. Rejected for now: adds complexity with minimal benefit. The
      current use case (RFC-0802) requires whole-page gating. Per-block can be
      added as a follow-up RFC if needed.
    file: docs/rfcs/archive/implemented/rfc-0803-add-per-page-deployment-gating-for-production-builds.md
  - kind: rejected-alternative
    rfcId: RFC-0804
    title: Auto-refresh pnpm-lock.yaml after mission.archive moves
    status: implemented
    alternativesText: >-
      1. **Remove `missions/*/workpiece` from `pnpm-workspace.yaml`**: Rejected.
      The glob is needed so `pnpm install` links `@warpgogol/*` symlinks into
      the workpiece during `mission.materialize`. Removing it would break the
      materialize flow.


      2. **Run `pnpm install` in `mission.close` instead**: Rejected.
      `mission.close` (RFC-0801) no longer archives. The workpiece stays at
      `missions/<id>/workpiece/` after close — the glob still matches, so the
      lockfile is still correct. The lockfile only becomes stale after
      `mission.archive` moves the directory.


      3. **Add a separate `mission.lockfile.refresh` command**: Rejected. This
      would require the operator to remember an extra step. The lockfile refresh
      should be automatic, part of the archive operation that causes the drift.


      4. **Change glob to `missions/**/workpiece`**: Rejected. This would
      include archived workpieces in the pnpm workspace. Archived workpieces
      have no `node_modules/` (cleaned by RFC-0801) and would cause pnpm
      resolution errors.
    file: docs/rfcs/archive/implemented/rfc-0804-auto-refresh-pnpm-lock-yaml-after-mission-archive-moves.md
  - kind: rejected-alternative
    rfcId: RFC-0805
    title: Rename services to drop redundant suffixes and add service.naming.validate
    status: implemented
    alternativesText: >-
      - **Keep `-worker` suffix, no validator** — rejected. The suffix is
      redundant noise, and without a validator, naming drift will recur.

      - **Rename only, no validator** — rejected. Without automated enforcement,
      a future service could reintroduce the suffix.

      - **Validator only, no rename** — rejected. The rename is the motivating
      change; the validator prevents regression.

      - **Whitelist `check-warpgogol-runner`** — rejected.
      `check-warpgogol-runner` does not end with `-worker`, so it passes the
      SVC-NAME-06 rule automatically. The `-warpgogol` infix removal is a
      separate naming concern, not a validator whitelist issue.
    file: docs/rfcs/archive/implemented/rfc-0805-rename-cloudflare-worker-services-to-drop-worker-suffix-and-add-service-naming-validate.md
  - kind: rejected-alternative
    rfcId: RFC-0806
    title: Add service deployment pipeline with dev-deploy, promote, and rollback
    status: implemented
    alternativesText: >-
      - **Full 3-channel service pipeline (mirror sites)**: Rejected — services
      don't have HTML to audit (Axiom doesn't apply), no CDN (freshness checks
      don't apply), no build step (no dist to hash). Release manifests for
      source-only Workers add overhead without value.

      - **Enhanced single-deploy with pre-deploy gates only (no channels)**:
      Rejected — doesn't address the operator's request for a dev-deploy
      analogy. No testing surface before production.

      - **Dev-deploy via `wrangler dev` (local)**: Rejected — local `wrangler
      dev` doesn't test cron triggers reliably and can't be used from CI. Remote
      dev Worker provides a real testing surface.

      - **No rollback**: Rejected — operator explicitly requested rollback.
      `wrangler rollback` is available and just needs leitstand wrapping with
      state recording.

      - **No lock**: Rejected — operator explicitly requested lock. Low cost,
      high safety.
    file: docs/rfcs/archive/implemented/rfc-0806-add-service-deploy-pipeline-with-dev-deploy-promote-and-rollback.md
  - kind: rejected-alternative
    rfcId: RFC-0807
    title: Connect all services to SigNoz via OTLP push for health monitoring
    status: implemented
    alternativesText: >-
      - **PULL-based health probes** (`leitstand.service.health` polling
      `/health`) — rejected (see Why PUSH above).

      - **Hybrid (PULL for Workers + PUSH for Node)** — rejected, adds two
      mechanisms for no benefit.

      - **Cloudflare Workers built-in observability only** — rejected,
      traces/logs are useful but don't provide custom health gauges/counters for
      dashboards and alerting.
    file: docs/rfcs/archive/implemented/rfc-0807-connect-services-to-signoz-via-otlp-push.md
  - kind: rejected-alternative
    rfcId: RFC-0808
    title: Add obsidian-vault stack profile for knowledge-base governance
    status: implemented
    alternativesText: >-
      1. **Werkstatt plugin instead of Forge profile.** A `werkstatt-obsidian`
      plugin would provide mission lifecycle, workpiece-based editing, bordbuch
      audit trail, and integrity verification. Rejected for this RFC because it
      couples the knowledge base to the full Werkstatt engine — too heavy for
      the initial use case (single author or small team). The Forge profile
      provides governance (RFCs, ADRs, skills, validators) without engine
      coupling. A future RFC may add the plugin when mission-lifecycle
      integration is needed.


      2. **Generic `markdown-notes` profile (not Obsidian-specific).** A profile
      that supports any Markdown-based knowledge base (Logseq, Foam, etc.).
      Rejected because `[[wikilink]]` syntax, `.canvas` files, and `.obsidian/`
      config are Obsidian-specific. A generic profile would need to abstract
      over different link syntaxes, adding complexity without near-term value.
      The profile can be extended or a sibling profile added if other tools need
      support.


      3. **No profile, just skills.** Define skills for note management without
      a stack profile. Rejected because skills alone do not provide invariants
      (NOTE-01..04), terminology mapping, or workspace type detection. The
      profile is the structural contract; skills are the operational layer.


      4. **Vault location at project root (not in `vault/` subdirectory).** Make
      the entire project root the vault. Rejected because Forge needs
      non-content directories (`docs/`, `scripts/`, `.forge/`, `node_modules/`)
      that should not be part of the Obsidian vault. A `vault/` subdirectory
      cleanly separates content from governance infrastructure.
    file: docs/rfcs/archive/implemented/rfc-0808-add-obsidian-vault-stack-profile-for-knowledge-base-governance.md
  - kind: rejected-alternative
    rfcId: RFC-0809
    title: Add pipeline collect-errors mode for fail-fast diagnostic relief
    status: implemented
    alternativesText: >-
      - **Per-step `continueOnError` field on `KernelPipelineStep`**: Rejected
      because it requires updating every pipeline definition. The flag is a
      runner-level concern, not a step-level concern.

      - **`--no-fail` flag that always exits 0**: Rejected — it hides failures.
      The goal is to surface all failures, not to suppress them.

      - **Post-hoc error aggregation from telemetry**: Rejected — requires a
      second command to read telemetry. The operator wants failures in the same
      run, not a separate analysis step.
    file: docs/rfcs/archive/implemented/rfc-0809-add-pipeline-collect-errors-mode-for-fail-fast-diagnostic-relief.md
  - kind: rejected-alternative
    rfcId: RFC-0810
    title: Add generator ownership cross-check to detect unregistered generator
      outputs
    status: implemented
    alternativesText: >-
      - **Extending `generator.ownership.lint` (RFC-0087)**: Adding
      uncovered-generator detection to the existing ownership lint. Rejected
      because `generator.ownership.lint` runs in `PACKAGES_CHECK_PIPELINE`
      (`packages-check.ts:63`), while the cross-check must run in
      `SITES_BUILD_PREPARE_PIPELINE` before `ownership.sync.validate` to catch
      missing registrations before the expensive build steps. Different
      pipelines, different purposes: `generator.ownership.lint` detects
      multi-owner paths; the cross-check detects uncovered generators.

      - **Declared outputs on command registration**: Adding a `declaredOutputs:
      string[]` field to `KernelCommandDefinition`. Rejected because it
      duplicates the ownership map and requires changing every generator's
      registration. The ownership map already serves as the declared-outputs
      registry.

      - **Static analysis of generator source code**: Parsing generator `.ts`
      files to extract `writeFile` calls. Rejected — too fragile and would
      require a TypeScript AST walker.

      - **Merging ownership map into command registration**: Rejected — the
      ownership map is site-stack-specific (`werkstatt-site`), while command
      registration is in `command-tables/`. They live in the same package but
      serve different concerns.
    file: docs/rfcs/archive/implemented/rfc-0810-add-generator-ownership-cross-check-for-unregistered-outputs.md
  - kind: rejected-alternative
    rfcId: RFC-0811
    title: Switch page.markdown.validate from blacklist to whitelist scanning
    status: implemented
    alternativesText: >-
      - **Marker-based detection**: Require non-twin `.md` files to carry a
      marker comment (e.g. `<!-- not-a-twin -->`). Rejected — adds authoring
      burden and is easy to forget.

      - **Frontmatter presence as filter**: Only validate `.md` files that have
      YAML frontmatter. Rejected — `MDMETA-01` exists specifically to catch
      twins that are missing frontmatter. This would make that check
      unreachable.

      - **Separate directory for twins**: Move all twins to `public/.twins/`.
      Rejected — breaks existing URL paths and requires a migration.
    file: docs/rfcs/archive/implemented/rfc-0811-switch-page-markdown-validate-from-blacklist-to-whitelist.md
  - kind: rejected-alternative
    rfcId: RFC-0812
    title: Add DNS SVCB/HTTPS record format unit tests
    status: implemented
    alternativesText: >-
      - **Integration test with mocked Cloudflare API**: Rejected —
      over-engineered for a pure formatting function. Unit tests on
      `toApiRecord` are sufficient.

      - **Testing via `runDnsRecordUpsert` with mocked fetch**: Rejected — tests
      the full command but obscures the formatting logic. Direct unit tests on
      `toApiRecord` are clearer and more maintainable.
    file: docs/rfcs/archive/implemented/rfc-0812-add-dns-svcb-https-record-format-unit-tests.md
  - kind: rejected-alternative
    rfcId: RFC-0813
    title: Add Playwright pre-flight check to mission.validate
    status: implemented
    alternativesText: >-
      - **Making `playwright.chromium.ensure` non-fatal**: Rejected — build.post
      steps that depend on Chromium (behavior snapshots, evidence capture) would
      fail with confusing errors instead of a clear "Chromium not installed"
      message.

      - **Auto-installing in pre-flight**: Rejected — auto-install can take 30+
      seconds and may fail. The operator should explicitly run `pnpm exec
      playwright install chromium` to have control over the installation.

      - **`--check-only` flag on existing `playwright.chromium.ensure`**:
      Rejected — `playwright.chromium.ensure` is non-fatal (returns exitCode 0
      even after auto-install), while the pre-flight check must be fatal
      (exitCode 1 when Chromium is missing). A flag would conflate two
      fundamentally different exit semantics in one command. A separate command
      keeps the contract clear: `ensure` = check + remediate (non-fatal),
      `preflight.check` = check only (fatal).

      - **Checking in `mission.materialize` instead**: Rejected — materialize
      may run in environments where Playwright is not needed (e.g. content-only
      missions). The check belongs in `mission.validate` which always runs the
      full build pipeline.
    file: docs/rfcs/archive/implemented/rfc-0813-add-playwright-preflight-check-to-mission-validate.md
  - kind: rejected-alternative
    rfcId: RFC-0814
    title: Add --system flag auto-injection for workspace-scoped pipeline commands
    status: implemented
    alternativesText: >-
      - **Deriving system ID from a separate mapping**: Rejected — the 1:1
      site-to-system relationship makes this unnecessary complexity.

      - **Adding `systemId` to `DiscoveredSiteWorkspace`**: Rejected — it would
      always equal `site.name` and adds a field that needs to be maintained.

      - **Making `--system` always optional and deriving it from `--site` inside
      commands**: Rejected — pushes pipeline context knowledge into individual
      commands, violating separation of concerns.
    file: docs/rfcs/archive/implemented/rfc-0814-add-system-flag-auto-injection-for-workspace-scoped-pipeline-commands.md
  - kind: rejected-alternative
    rfcId: RFC-0815
    title: Add template.peer-deps.validate for onboarding package template integrity
    status: implemented
    alternativesText: >-
      - **CI-only check via `pnpm peers check`:** Rejected because it checks the
      workspace `package.json`, not the template. The template is a
      non-installable JSON file — `pnpm peers check` cannot target it directly.

      - **Pre-commit hook on `package.template.json`:** Rejected because it
      requires the committer to have the full dependency tree installed locally.
      A pipeline validator runs in CI with deterministic resolution.

      - **Manual discipline ("check peer deps when bumping
      @astrojs/cloudflare"):** Rejected — this is the current approach and it
      failed on 2026-08-12.

      - **Extending `template.deps.drift` with a `--check-peer-deps` flag:**
      Rejected because the two checks have different inputs (drift needs
      workpiece + template; peer-deps needs only template), different outputs
      (TEMPLATE-DEPS-DRIFT-01 vs PEER-01), and different resolution strategies
      (string comparison vs. full dep tree resolution). Combining them would
      require conditional logic and mixed output shapes. Following the existing
      pattern where each template check is a separate command
      (`template.imports.validate`, `template.deps.drift`), a separate command
      is consistent.
    file: docs/rfcs/archive/implemented/rfc-0815-add-template-peer-deps-validate-for-onboarding-package-template-integrity.md
  - kind: rejected-alternative
    rfcId: RFC-0817
    title: Enforce formal mission lifecycle in mission.preview and add systemic
      pipeline reliability protections
    status: implemented
    alternativesText: >-
      - **Auto-materialize on `mission.open`:** Rejected — materialization is
      heavy (~60s) and should be explicit. `mission.open` should be fast.

      - **Block dev server entirely until materialize:** Rejected —
      `mission.preview` auto-materializing is more ergonomic than requiring a
      separate command.

      - **Make `dns.record.upsert` a no-op step in the pipeline:** Rejected —
      the command should be callable directly. Graceful skip is the right
      behavior, not pipeline-level removal.

      - **Add `--system` detection to `resolveCommandFlags` instead of
      auto-inject:** Rejected — the auto-inject is the right place to fix this,
      since it's the source of the duplicate.
    file: docs/rfcs/archive/implemented/rfc-0817-enforce-formal-mission-lifecycle-and-add-systemic-pipeline-reliability-protections.md
  - kind: rejected-alternative
    rfcId: RFC-0818
    title: Fix bordbuch sync lag in external mirror push ordering
    status: implemented
    alternativesText: >-
      - **Push bordbuch commit separately to external mirrors after step 5:**
      Rejected — adds a second external push round-trip. Simpler to move the
      entire external push after the bordbuch commit.

      - **Skip bordbuch entry for sync events:** Rejected — bordbuch audit trail
      is valuable. The fix preserves the audit trail and ensures it reaches
      external mirrors.

      - **Change `refs/mirror` to track the pre-bordbuch SHA:** Rejected — this
      would make `refs/mirror` accurate about what was pushed, but external
      mirrors would still lag. The root cause is the ordering, not the ref
      tracking.
    file: docs/rfcs/archive/implemented/rfc-0818-fix-bordbuch-sync-lag-in-external-mirror-push-ordering.md
  - kind: rejected-alternative
    rfcId: RFC-0819
    title: Allow null as intentional not-required marker in env files
    status: implemented
    alternativesText: >-
      1. **New standalone validator `env.null.convention.validate`** — rejected.
      The convention is already enforced by `deploy.preflight` (empty values are
      rejected, `null` is accepted). A separate validator would duplicate the
      check without adding value. The problem is discoverability, not
      enforcement — solved by updating the error message.


      2. **Allow `null` in `.env.example` too** — rejected by the operator.
      `.env.example` is a template with empty values; `null` in the template
      would imply the variable is not required for any deployment, which is
      site-specific. The `null` marker belongs in `.env` only, where the
      operator makes the deployment-specific decision.


      3. **Runtime normalization of `"null"` → `undefined` in `getEnv`** —
      rejected by the operator. Services that read `process.env` directly or via
      `getEnv` handle absent values themselves. Normalizing `"null"` in a helper
      would create an implicit contract that not all code paths follow. The
      convention is file-level, not runtime-level.


      4. **Comment-based marker (e.g. `# optional` in the comment block)** —
      rejected. Comments are stripped by `wrangler deploy --secrets-file .env`;
      only `KEY=value` lines are read. A comment-based marker would not survive
      into the deployed environment and would not be visible to
      `deploy.preflight`.
    file: docs/rfcs/archive/implemented/rfc-0819-allow-null-as-intentional-not-required-marker-in-env-files.md
  - kind: rejected-alternative
    rfcId: RFC-0822
    title: Persist workpiece env files in cache clone across missions
    status: implemented
    alternativesText: >-
      1. **Git-commit `.env` in cache clone with `git add -f` (force-add).**
      Rejected: secrets in git history pose a security risk. Cache clone pushes
      to bare repo and external mirrors — secrets would propagate to all
      mirrors, including backup endpoints outside the operator's control.

      2. **Merge `.env` with `.env.example` during restore.** Rejected by the
      operator: adds complexity. If new variables are needed, the operator adds
      them directly in the workpiece `.env`, and the next `mission.close` copies
      the updated file. Simple copy is sufficient.

      3. **Copy at `mission.reconcile` instead of `mission.close`.** Rejected:
      reconcile pushes commits to bare repo — `.env` is gitignored and would not
      propagate. Close is the natural finalization point.

      4. **Preserve via old workpiece path fallback.** Rejected by the operator:
      "we don't dig into old workpieces." Cache clone is the single source of
      truth for inter-mission state.

      5. **Copy at `mission.abort` too.** Rejected by the operator: abort means
      cancellation, secrets are not preserved.
    file: docs/rfcs/archive/implemented/rfc-0822-persist-workpiece-env-files-in-cache-clone-across-missions.md
  - kind: rejected-alternative
    rfcId: RFC-0823
    title: Establish workshop testing architecture
    status: implemented
    alternativesText: >-
      - **Mocks instead of dev channel:** Rejected. The QStash debugging session
      proved that mock-based tests miss environment-specific issues (URL
      construction, signature verification). Dev channel testing catches these.

      - _*Tests in services/* instead of packages:_* Rejected by operator
      directive. Tests in packages are versioned with the platform and reusable
      across missions.

      - **Single monolithic RFC:** Rejected. Each level has distinct
      implementation concerns and can be implemented independently. Separate
      RFCs allow parallel implementation tracks.

      - **Extend Axiom instead of new test layers:** Rejected. Axiom is a
      visual/SEO check system, not a functional test framework. Conflating the
      two would dilute both.
    file: docs/rfcs/archive/implemented/rfc-0823-establish-workshop-testing-architecture.md
  - kind: rejected-alternative
    rfcId: RFC-0824
    title: Add service unit test foundation
    status: implemented
    alternativesText: >-
      - **Tests in `services/*/src/`:** Rejected by operator directive. Tests
      must live in packages.

      - **Separate test runner for services:** Rejected. vitest is already the
      workshop runner (DNA-41). Using a different runner for services adds
      complexity without value.

      - **Extend `packages-check.run` instead of new command:** Rejected.
      `packages-check.run` is for structural validation, not test execution.
      Test execution needs a dedicated command that returns test results, not
      diagnostics.
    file: docs/rfcs/archive/implemented/rfc-0824-add-service-unit-test-foundation.md
  - kind: rejected-alternative
    rfcId: RFC-0825
    title: Add post-deploy smoke testing
    status: implemented
    alternativesText: >-
      - **Inline smoke paths in `services/registry.yaml`:** Rejected. Registry
      is deployment state, not test configuration. Mixing the two makes the
      registry bloated.

      - **Programmatic smoke definitions (TypeScript):** Rejected. YAML is
      declarative and can be validated without executing code. Operators can
      edit smoke endpoints without touching code.

      - **Use Axiom for smoke testing:** Rejected. Axiom is a visual/SEO check
      system with Playwright. Smoke tests are lightweight HTTP fetches — using
      Playwright for them adds unnecessary overhead.
    file: docs/rfcs/archive/implemented/rfc-0825-add-post-deploy-smoke-testing.md
  - kind: rejected-alternative
    rfcId: RFC-0826
    title: Add service integration testing against dev channel
    status: implemented
    alternativesText: >-
      - **Mock QStash/Supabase:** Rejected by operator directive. Use existing
      credentials.

      - **Miniflare/local Worker for integration tests:** Rejected. Dev-deployed
      Workers are the real environment. Local simulators miss CDN, DNS, and
      Cloudflare runtime differences.

      - **Integration tests in CI:** Rejected for now. Integration tests require
      a dev-deployed target, which CI doesn't have. Future: a CI job that
      dev-deploys, runs integration tests, then tears down.
    file: docs/rfcs/archive/implemented/rfc-0826-add-service-integration-testing-against-dev-channel.md
  - kind: rejected-alternative
    rfcId: RFC-0827
    title: Establish site-service contract testing
    status: implemented
    alternativesText: >-
      - **OpenAPI as contract format:** Rejected. OpenAPI is generated by
      `agent.manifest.generate` for external consumption. Internal contracts
      need TypeScript-native schemas (Zod) for runtime validation and type
      inference.

      - **Pact-style consumer-driven contracts:** Rejected. Pact adds
      infrastructure (broker, verification) that is overkill for a monorepo
      where both sides are in the same repo. Zod schemas + import checking
      achieves the same goal with zero infrastructure.

      - **Runtime contract validation only:** Rejected. Runtime validation (Zod
      parse on every request) is necessary but not sufficient. Static validation
      (import checking) catches contract drift before deployment.
    file: docs/rfcs/archive/implemented/rfc-0827-establish-site-service-contract-testing.md
  - kind: rejected-alternative
    rfcId: RFC-0828
    title: Add site E2E testing with Playwright against dev channel
    status: implemented
    alternativesText: >-
      - **Use check-runner for E2E:** Rejected. check-runner is designed for
      Axiom evidence capture with methodology, not for simple functional E2E
      tests. Using it would add unnecessary complexity.

      - **E2E tests in workpiece:** Rejected by operator directive. Tests must
      live in packages.

      - **Cypress instead of Playwright:** Rejected. Playwright is already a
      workshop dependency (DNA: `@playwright/test` 1.62.1). Adding Cypress would
      duplicate browser automation infrastructure.

      - **E2E tests in CI:** Rejected for now. E2E tests require a dev-deployed
      site, which CI doesn't have. Future: a CI job that dev-deploys, runs E2E
      tests, then tears down.
    file: docs/rfcs/archive/implemented/rfc-0828-add-site-e2e-testing-with-playwright-against-dev-channel.md
  - kind: rejected-alternative
    rfcId: RFC-0829
    title: Add test evidence gates to deployment pipeline
    status: implemented
    alternativesText: >-
      - **Store evidence in a database:** Rejected. JSON files in the deployment
      state directory are simpler, git-trackable (for releases), and don't
      require infrastructure.

      - **Store evidence in the bordbuch:** Rejected. Bordbuch is for mission
      history, not test results. Test evidence is deployment-specific, not
      mission-specific.

      - **Only verify evidence for promote (not propagate):** Rejected.
      Propagate is the last gate before the operator manually verifies on alt.
      If evidence is missing at propagate, the operator wastes time on a broken
      release.

      - **Make all gates fatal from day one:** Rejected. The grace period allows
      existing workflows to adapt without breaking ongoing deployments.
    file: docs/rfcs/archive/implemented/rfc-0829-add-test-evidence-gates-to-deployment-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0830
    title: Add image.delivery.validate for responsive srcset and compression
      enforcement
    status: implemented
    alternativesText: >-
      - **Extend `image.variants.validate` to scan rendered HTML** — Rejected.
      `image.variants.validate` checks manifest-to-disk parity (file existence).
      Scanning rendered HTML for srcset/dimension/compression is a different
      concern and belongs in a separate command for single-responsibility.


      - **Post-deploy Lighthouse CI only** — Rejected. The user's requirement is
      to catch these issues pre-deploy, at build time. Post-deploy Lighthouse
      catches them too late (after the site is live).


      - **Astro `<Image>` component enforcement only** — Rejected. Composite
      sections (hero, hero-decision-card) use `ResponsiveImage` which renders
      raw `<img>` tags. Source-pattern enforcement cannot verify the rendered
      output (srcset, loading attributes). A validator must scan rendered HTML.


      - **Cloudflare Image Transformations** — Rejected for build-portable
      sites. The `build-portable` provider exists specifically to avoid runtime
      image transformation dependencies. This RFC validates build-time output
      instead.
    file: docs/rfcs/archive/implemented/rfc-0830-add-image-delivery-validate-for-responsive-srcset-and-compression-enforcement.md
  - kind: rejected-alternative
    rfcId: RFC-0831
    title: Add csp.origins.validate for CSP-to-script-origin cross-validation
    status: implemented
    alternativesText: >-
      - **Extend `headers.security.validate` with origin cross-referencing** —
      Rejected. `headers.security.validate` is an author-time validator that
      reads `public/_headers` only. Origin cross-referencing requires rendered
      HTML from `dist/client/`, making it a post-build concern. Different scope,
      different pipeline phase.


      - **Runtime CSP violation reporting** — Rejected as primary mechanism.
      Browser-reported CSP violations are post-deploy feedback. The user's
      requirement is pre-deploy detection. This RFC provides build-time
      detection; runtime reporting can be added separately.


      - **CSP generator that auto-includes all origins** — Rejected for now.
      Auto-generating CSP from scanned origins is dangerous (it would whitelist
      any origin a script happens to load, including third-party trackers).
      Validation with explicit operator action to add origins is safer.
    file: docs/rfcs/archive/implemented/rfc-0831-add-csp-origins-validate-for-csp-to-script-origin-cross-validation.md
  - kind: rejected-alternative
    rfcId: RFC-0832
    title: Add a11y.label-in-name.validate for WCAG 2.5.3 Label in Name
    status: implemented
    alternativesText: >-
      - **Author-time static analysis of `.astro` files** — Rejected.
      `aria-label` values are often dynamically constructed from content props.
      Only rendered HTML has the final accessible name. Post-build scanning is
      the reliable approach.


      - **Extend `surface.heading-uniqueness.validate`** — Rejected. That
      validator checks heading text duplication (a different WCAG concern).
      Mixing label-in-name checks into it would violate single-responsibility
      and complicate the rule set.


      - **Post-deploy Lighthouse CI only** — Rejected. The user's requirement is
      pre-deploy detection. Post-deploy Lighthouse catches this too late.


      - **Axe-core integration** — Rejected for now. Axe-core is a runtime
      accessibility scanner that requires a browser. This RFC uses static HTML
      parsing (parse5) which is faster and sufficient for the label-in-name
      check. Axe integration can be added separately for broader a11y coverage.
    file: docs/rfcs/archive/implemented/rfc-0832-add-a11y-label-in-name-validate-for-wcag-2-5-3.md
  - kind: rejected-alternative
    rfcId: RFC-0833
    title: Extend lighthouse validators with render-blocking, unused-JS, and
      forced-reflow detection
    status: implemented
    alternativesText: >-
      - **Separate commands for each rule** — Rejected. LH-11 and LH-12 are
      post-build concerns that naturally extend `lighthouse.budget.check` (which
      already scans `dist/`). LH-13 is a pre-build concern that naturally
      extends `lighthouse.validate` (which already scans `src/scripts/`).
      Creating separate commands would fragment the lighthouse rule set.


      - **Post-deploy Lighthouse CI only** — Rejected. The user's requirement is
      pre-deploy detection. Post-deploy Lighthouse catches issues after the site
      is live.


      - **Critical CSS auto-generation** — Rejected for this RFC. This RFC
      validates that CSS is non-blocking; it does not generate critical CSS.
      Critical CSS generation is a separate concern (future RFC if needed).


      - **AST-based unused JS analysis** — Rejected for now. Full AST parsing of
      production bundles is complex and slow. The heuristic approach (export
      reference scanning) is faster and catches the common case. Can be upgraded
      to AST-based in a future RFC if false negatives become a problem.
    file: docs/rfcs/archive/implemented/rfc-0833-extend-lighthouse-validators-with-render-blocking-unused-js-and-forced-reflow-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0834
    title: Commit generated variant manifests for drift detection
    status: implemented
    alternativesText: >-
      - **Keep manifests gitignored, rely on `image.variants.validate` only.**
      Rejected: the validator runs in `build.check`, not `build.prepare.dev`. A
      dev materialize that skips the full build pipeline has no drift signal
      until the operator runs `build.check` — too late for review.


      - **Commit binary variants too.** Rejected: `public/_img/**` and
      `public/_video/**` are large binary files that would bloat the git history
      and LFS. They are regenerated deterministically from the manifest + source
      hashes, so committing them adds no drift detection value.


      - **Make `image.variants.generate` run in `build.prepare.dev`.** Rejected:
      the dev pipeline is optimized for speed; running sharp on every dev
      materialize is wasteful. Committing the manifest is the cheaper fix — it
      ensures the manifest exists without paying the generation cost on every
      dev cycle.
    file: docs/rfcs/archive/implemented/rfc-0834-commit-generated-variant-manifests-for-drift-detection.md
  - kind: rejected-alternative
    rfcId: RFC-0835
    title: Add regression tests for LH-12 ignore patterns and IMG-DELIVERY-04
      404.html exemption
    status: implemented
    alternativesText: >-
      - **Separate test file**: Rejected — the tests belong with the existing
      `lighthouse.test.ts` and `image-delivery.test.ts` suites that test the
      same validators.

      - **Integration test via mission.validate**: Rejected — too slow (~60s per
      run). Unit tests with temp directories are fast (~1s) and test the
      specific behavior directly.
    file: docs/rfcs/archive/implemented/rfc-0835-add-regression-tests-for-lh-12-ignore-and-img-delivery-04-404-exemption.md
  - kind: rejected-alternative
    rfcId: RFC-0836
    title: Add component-level WCAG 2.5.3 Label in Name validator
    status: implemented
    alternativesText: >-
      - **AST-based parser for .astro files**: Rejected — `.astro` is not a
      standard language with a production-grade parser available as an npm
      package. A regex-based approach is sufficient for the common patterns
      (`aria-label={...}` + visible text) and the post-build validator remains
      the final gate.

      - **Extending the post-build validator only**: Rejected — the post-build
      validator catches violations after a ~60s build cycle. The component-level
      check catches them at authoring time, saving build cycles.

      - **Extending `section.image-props.validate`**: Rejected — that validator
      checks image prop usage (`src={props.xxx}` without `resolveImage`), a
      different rule type. Mixing aria-label/text parity checks into it would
      violate single-responsibility and complicate the rule set. A dedicated
      validator is cleaner.

      - **Lint rule in eslint**: Rejected — `.astro` files are not linted by
      eslint in this codebase. Adding eslint support for `.astro` would be a
      larger effort than a dedicated validator.
    file: docs/rfcs/archive/implemented/rfc-0836-add-component-level-wcag-2-5-3-label-in-name-validator.md
  - kind: rejected-alternative
    rfcId: RFC-0837
    title: Add static CSS analysis for mobile layout anti-patterns
    status: implemented
    alternativesText: >-
      - **Post-deploy Lighthouse mobile audit:** Lighthouse already checks some
      mobile layout issues, but it runs post-deploy (too late) and does not
      catch all six anti-patterns (e.g., missing `100dvh` fallback). This RFC
      catches issues at author time, before build.


      - **Stylelint plugin:** Stylelint has plugins for some of these rules, but
      adding Stylelint as a dependency to every site workpiece increases
      complexity. The Werkstatt pattern is self-contained validators that share
      the existing `collectFiles` / `getLineColumn` infrastructure.


      - **CSS-in-JS analysis:** Not applicable — the Werkstatt uses plain CSS
      and Astro `<style>` blocks, not CSS-in-JS.
    file: docs/rfcs/archive/implemented/rfc-0837-add-static-css-analysis-for-mobile-layout-issues.md
  - kind: rejected-alternative
    rfcId: RFC-0838
    title: Add Playwright mobile layout stability checks
    status: implemented
    alternativesText: >-
      - **Visual regression (screenshot diff):** Explicitly excluded by the user
      for now. Screenshot diffs require baseline management, are fragile across
      browser updates, and produce false positives from rendering differences.
      Geometric assertions are more stable and directly test the invariants we
      care about.


      - **Baseline-based geometry comparison:** The user explicitly requested
      "without baseline." Direct invariant assertions (no overflow, CLS <
      threshold, stability delta < N) are sufficient and avoid the maintenance
      burden of golden snapshots.


      - **Post-deploy only (Axiom):** Post-deploy checks (RFC-0839) catch issues
      in production but are too late — they affect real users. A pre-deploy
      check catches issues before they reach production.


      - **Cypress instead of Playwright:** The Werkstatt already standardizes on
      Playwright (RFC-0828, DNA-66). Adding Cypress would introduce a second
      browser automation framework.
    file: docs/rfcs/archive/implemented/rfc-0838-add-playwright-mobile-layout-stability-checks.md
  - kind: rejected-alternative
    rfcId: RFC-0839
    title: Add Axiom post-deploy mobile layout monitoring
    status: implemented
    alternativesText: >-
      - **Extend `performance-vitals` methodology:** The existing
      `performance-vitals` instrument already measures CLS. However, it does not
      check horizontal overflow, orientation stability, or address bar layout
      shift. Extending it would conflate two distinct concerns. A separate
      methodology is cleaner.


      - **Real device farm (BrowserStack, Sauce Labs):** Real device testing is
      more accurate than emulation but introduces external dependencies, cost,
      and latency. Playwright's device emulation is sufficient for the common
      cases. Real device testing can be added as a future enhancement.


      - **Web Vitals JavaScript library in production:** Injecting the
      `web-vitals` library into production pages would provide real-user CLS
      data. However, this requires client-side JavaScript, adds page weight, and
      does not cover horizontal overflow or orientation stability. Axiom's
      server-side Playwright capture is more comprehensive.
    file: docs/rfcs/archive/implemented/rfc-0839-add-axiom-post-deploy-mobile-layout-monitoring.md
  - kind: rejected-alternative
    rfcId: RFC-0840
    title: Preserve root-level operator config files during materialization
    status: implemented
    alternativesText: >-
      - **Add root-level config files to `STERNSYSTEM_DATA_PATHS`:** This would
      copy them during the data-path copy, but it would also stage them in git
      commits (violating the data-only contract for non-content files). The
      RFC-0822 untracked persistence pattern is cleaner.

      - **Git-track operator config files in the workpiece:** Some of these
      files (`.lighthouse-budget-ignore`) could be git-tracked. However, they
      are site-specific configuration that changes independently of content, and
      git-tracking them in the workpiece would cause merge conflicts during
      `mission.reconcile`. The untracked cache-clone persistence avoids this.

      - **Glob-based preservation (`*.-ignore`, `*.config.yaml`):** Too broad —
      risks persisting generated files or secrets. Explicit list is safer.
    file: docs/rfcs/archive/implemented/rfc-0840-preserve-root-level-config-files-during-materialization.md
  - kind: rejected-alternative
    rfcId: RFC-0841
    title: Add image-delivery.config.yaml location diagnostic
    status: implemented
    alternativesText: >-
      - **Auto-discover config from both locations:** Loading from both root and
      `src/` would introduce ambiguity about which overrides take precedence.
      The RFC-0830 contract is clear: `src/` is the location. Auto-discovery
      would weaken the contract.

      - **Make the config location configurable via CLI flag:** Over-engineered
      for a simple path issue. The diagnostic is sufficient.

      - **Error instead of warning:** Too aggressive — the validator can still
      run without overrides. A warning guides the operator without blocking.
    file: docs/rfcs/archive/implemented/rfc-0841-add-image-delivery-config-location-diagnostic.md
  - kind: rejected-alternative
    rfcId: RFC-0842
    title: Enforce sequential deployment pipeline (Dev → Alt → Main)
    status: implemented
    alternativesText: >-
      - **Remove `--all` from the CLI entirely:** Too broad — `--all` is useful
      for non-deployment commands (e.g., `sites list --all`, `workflow.lint
      --all`). The per-command rejection is more precise.

      - **Add `--channel` flag to `leitstand.propagate`:** Explicitly rejected
      by RFC-0628 design. The channel is hardcoded to `alt` —
      `leitstand.promote` handles alt→main. Adding `--channel` would reintroduce
      the confusion this RFC aims to eliminate.

      - **Make `leitstand.pipeline.check` a pipeline instead of a command:** A
      command is simpler — it reads state, it doesn't execute steps. A pipeline
      would imply side effects.
    file: docs/rfcs/archive/implemented/rfc-0842-enforce-sequential-deployment-pipeline.md
  - kind: rejected-alternative
    rfcId: RFC-0843
    title: Amend mobile.layout.check — robust Playwright navigation and type-safe
      evaluate
    status: implemented
    alternativesText: >-
      - **`waitUntil: "domcontentloaded"`:** Waits only for DOM parsing, not for
      CSS/images. Insufficient for layout checks that need rendered geometry.
      `load` is the correct choice — it waits for all local resources but not
      for network idle.


      - **Per-domain blocklist instead of allowlist:** Instead of blocking all
      external requests, block specific known-problematic domains (matomo,
      pulse). Rejected because new external domains may be added in the future,
      and the allowlist approach (block everything not from the local server) is
      more robust.


      - **Playwright `page.route()` instead of `context.route()`:**
      `page.route()` only applies to a single page, while `context.route()`
      applies to all pages in the context. Since validators create new contexts
      per orientation, `context.route()` is the correct scope.


      - **ESLint rule for `page.evaluate(string)`:** Rejected because
      Playwright's types intentionally allow strings, and writing a custom
      ESLint rule for a single API is disproportionate. The `evaluateInPage`
      wrapper achieves the same safety with a simple type constraint.
    file: docs/rfcs/archive/implemented/rfc-0843-amend-mobile-layout-check-robust-playwright-navigation-and-type-safe-evaluate.md
  - kind: rejected-alternative
    rfcId: RFC-0844
    title: Pre-build operator config presence check — fail fast before expensive
      build cycle
    status: implemented
    alternativesText: >-
      - **Extend `materialize.config.validate` (RFC-0840):** Rejected because
      `materialize.config.validate` is a workspace-scope check in
      `PACKAGES_CHECK_PIPELINE` that checks list sync (dead entries,
      unrecognized files). Adding per-workpiece presence checks would mix
      concerns. A dedicated pre-build gate is cleaner.


      - **Add presence check to `build.prepare`:** Rejected because
      `build.prepare` is a pipeline that generates artifacts — adding a presence
      check there mixes validation with generation. The pre-flight section of
      `mission.validate` is the correct location for infrastructure checks.


      - **Make `lighthouse.budget.check` and `image.delivery.validate` emit
      better errors when config is missing:** This is a good complementary
      improvement, but it doesn't solve the core problem — the build still runs
      for 10+ minutes before the error is reached. The pre-build gate is needed
      to fail fast.


      - **Git-track operator config files in the workpiece:** Rejected per
      RFC-0840's analysis — these files are site-specific and git-tracking them
      causes merge conflicts during `mission.reconcile`.
    file: docs/rfcs/archive/implemented/rfc-0844-pre-build-operator-config-presence-check.md
  - kind: rejected-alternative
    rfcId: RFC-0845
    title: Extend Playwright pre-flight to release.prepare and add download retry
    status: implemented
    alternativesText: >-
      - **Add retry to `preflightChromium` in
      `@syrokomskyi/axiom-factory-app`:** Rejected because that package is
      external to the Werkstatt monorepo. The retry belongs in the Werkstatt
      wrapper (`ensureChromium`) which is under our control.


      - **Make `release.prepare` depend on `mission.validate` having already
      run:** Rejected because `release.prepare` can be invoked independently
      (e.g., re-running after a fix). It should be self-contained with its own
      pre-flight.


      - **Use `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD` env var to skip download:**
      This is already handled by `preflightChromium`. The retry is for when
      download is attempted but fails — a different scenario.
    file: docs/rfcs/archive/implemented/rfc-0845-extend-playwright-preflight-to-release-prepare-and-add-download-retry.md
  - kind: rejected-alternative
    rfcId: RFC-0846
    title: Health check retry with backoff for leitstand.dev-deploy
    status: implemented
    alternativesText: >-
      - **Always run Axiom regardless of health:** Rejected because RFC-0724
      intentionally gates Axiom on health — running Axiom against an unhealthy
      deployment produces misleading evidence. The retry approach is safer.


      - **Increase the post-purge sleep from 6s to 15s:** Rejected because a
      fixed sleep is wasteful when propagation is fast (common case) and
      insufficient when propagation is slow (worst case). Retry with backoff
      adapts to both scenarios.


      - **Use the Cloudflare API to check purge status before health check:**
      Rejected because it adds a Cloudflare-specific dependency to the health
      check flow. The retry approach is adapter-agnostic.


      - **Make the retry constants configurable via flags:** Rejected for now —
      the fixed 3-attempt/3s-6s pattern is sufficient. If different environments
      need different values, env vars can be added in a future RFC.
    file: docs/rfcs/archive/implemented/rfc-0846-health-check-retry-with-backoff-for-leitstand-dev-deploy.md
  - kind: rejected-alternative
    rfcId: RFC-0847
    title: Cross-reference proxyBaseUrl hostname against service subdomain registry
    status: implemented
    alternativesText: >-
      - **New standalone command** (`analytics.proxy-subdomain.validate`):
      Rejected. The check is naturally part of `analytics.config.validate` which
      already validates `proxyBaseUrl` format. A separate command would fragment
      analytics config validation.

      - **Runtime check in `subdomain.register`**: Rejected.
      `subdomain.register` is service-scoped and doesn't have access to site
      `system.md`. The cross-reference belongs in the site-scoped audit
      validator.

      - **DNS resolution check (live DNS lookup)**: Rejected. Live DNS lookups
      are slow, network-dependent, and already handled by `subdomain.validate`
      (RFC-0752). This check is a static declaration cross-reference, not a live
      DNS probe.
    file: docs/rfcs/archive/implemented/rfc-0847-cross-reference-proxyBaseUrl-hostname-against-service-subdomain-registry.md
  - kind: rejected-alternative
    rfcId: RFC-0848
    title: Establish the core certification domain and deterministic decisions
    status: draft
    alternativesText: >-
      ### Keep the original monolithic RFC


      Rejected after two audits and operator grilling. It crossed six execution
      boundaries and was unsafe for one less capable agent session.


      ### Make RFC-0848 an umbrella with no executable checkpoint


      Rejected. Spec status would claim CERT-001 complete without proving child
      composition. A small integration suite and exact documentation map earn
      the parent RFC's existence.


      ### Let RFC-0848 itself supersede old state RFCs


      Rejected after decomposition. Supersession must land atomically with the
      code/DNA replacement in RFC-0851, not later in a parent that depends on
      it.


      ### Preserve old commands until CERT-007


      Rejected. That leaves a second uncertified authority. Truthful
      unavailability is explicitly accepted.


      ### Treat existing tests/validators/Axiom as already certified evidence


      Rejected. They remain useful producers/mechanisms only after later
      profiles and authority admission bind them to the candidate/policy/cut.
    file: docs/rfcs/rfc-0848-core-certification-domain-and-deterministic-decisions-as-amended-by-amd-003.md
  - kind: rejected-alternative
    rfcId: RFC-0849
    title: Establish bounded canonical JSON identity bytes
    status: draft
    alternativesText: >-
      ### Support scalar/array roots through an opaque wrapper


      Rejected: all current authority payloads and Diagnostic data are objects.
      A wrapper/unwrapping protocol adds permanent complexity solely to make
      weak-collection branding work for consumers that do not exist.


      ### Invent a local almost-JCS format


      Rejected: RFC 8785 already defines whitespace, primitive serialization,
      key ordering, UTF-8 output, lone-surrogate failure, and independent
      vectors. Werkstatt needs a narrower input profile, not a second unnamed
      standard.


      ### Canonicalize arbitrary `unknown` directly


      Rejected: byte/hash functions cannot safely traverse caller-owned objects
      or prove absence of Proxy behavior/mutation. A detached branded root
      closes the authority domain.


      ### Permit unsafe integers because JCS serializes them


      Rejected: JCS deterministically serializes the IEEE-754 value, but the
      intended integer may already have collapsed before canonicalization. Exact
      large numbers, money, and decimals use schema-declared strings.


      ### Reveal safe-looking object keys in failures


      Rejected: a secret or personal value may itself satisfy a safe-looking key
      pattern. Sorted ordinals are deterministic without claiming that content
      is safe to log.


      ### Reuse or globally tighten `stableJsonHash`


      Rejected: reuse leaves authority semantics implicit; global tightening
      changes unrelated identities. The new versioned API isolates certification
      authority without compatibility churn.


      ### Change `fingerprint.usage.lint` here


      Rejected: the command is plugin-owned and certification source does not
      exist until RFC-0853. RFC-0853 owns enforcement against its actual source
      boundary; RFC-0849 remains an engine-only implementation session.
    file: docs/rfcs/rfc-0849-establish-bounded-canonical-json-identity-bytes.md
  - kind: rejected-alternative
    rfcId: RFC-0850
    title: Implement deterministic certification evaluation and remediation
    status: draft
    alternativesText: >-
      ### Scan all evidence for every requirement


      Rejected: it is simple but `O(R×E)` and makes retries/large profiles an
      avoidable bottleneck. A single bounded index is equally deterministic and
      safer.


      ### Let each command aggregate for its own gate


      Rejected: orchestration, Main verification, and health would drift in
      precedence, coverage, and evidence choice.


      ### Sort by producer timestamp or filename


      Rejected: clocks skew, retries return out of order, and filenames are not
      authority. Only monotonic admission sequence plus a frozen cut preserves
      history.


      ### Emit best-effort remediation when metadata is incomplete


      Rejected: vague tasks encourage agents to guess. An authoritative non-pass
      without safe anchored remediation is itself incomplete and must fail
      construction.
    file: docs/rfcs/rfc-0850-implement-deterministic-certification-evaluation-and-remediation.md
  - kind: rejected-alternative
    rfcId: RFC-0851
    title: Replace legacy release state with deployment operations
    status: draft
    alternativesText: >-
      ### Add certification states to the release enum


      Rejected: it still overwrites concurrent operation/history/health facts
      and repeats the source of current drift.


      ### Keep commands working on old evidence until CERT-007


      Rejected: that preserves an executable uncertified promotion path and two
      authorities.


      ### Migrate or delete old manifests now


      Rejected: migration grants false authority; deletion loses provenance and
      bypasses CERT-010's audited cleanup.


      ### Block all deployment commands including services


      Rejected: the certification specification and single-site cutover target
      site deployment. Backend service workflows are a separate runtime
      composition.
    file: docs/rfcs/rfc-0851-replace-legacy-release-state-with-deployment-operations.md
  - kind: rejected-alternative
    rfcId: RFC-0852
    title: Move canonical Diagnostic ownership into the engine
    status: draft
    alternativesText: >-
      ### Keep the runtime schema in the site plugin


      Rejected: certification engine code would import a stack plugin or
      duplicate the schema, violating DNA-64.


      ### Keep deprecated aliases for gradual migration


      Rejected: this repository controls all current consumers and accepts a
      clean cut. Aliases would make the supposed single owner unverifiable and
      invite new legacy usage.


      ### Exclude `data` or snippets from evidence identity


      Rejected: diagnostics could then change materially without changing
      evidence identity. Making every field canonical and redacted closes the
      contract honestly.


      ### Allow arbitrary `data` and sanitize during persistence


      Rejected: a value can appear valid in memory but fail or change at the
      authority boundary. Identity-safe construction must precede persistence.


      ### Put full logs into Diagnostic


      Rejected: bounded summaries and digest-addressed payloads keep agent
      output usable and prevent canonical identity abuse.
    file: docs/rfcs/rfc-0852-move-canonical-diagnostic-ownership-into-the-engine.md
  - kind: rejected-alternative
    rfcId: RFC-0853
    title: Define strict certification contracts and identity builders
    status: draft
    alternativesText: >-
      ### Combine this work back into RFC-0849


      Rejected by semantic audit and operator grilling. Permanent canonical
      bytes and a complete domain schema inventory are separate independently
      verifiable execution boundaries.


      ### Start with TypeScript interfaces and add Zod later


      Rejected: storage and authority consumers would begin from unenforced
      shapes and parallel definitions would drift.


      ### Generate identities by deleting excluded fields


      Rejected: a newly added normative field could be omitted silently. Fresh
      allow-listed payload construction plus strict payload schemas makes review
      and tests fail closed.


      ### Permit the site plugin to own certification schemas


      Rejected: core storage, authority, evaluation, and deployment would depend
      on one stack plugin, violating DNA-64.


      ### Exclude Diagnostics or redaction from evidence identity


      Rejected: material evidence meaning/safety could change without changing
      evidence ID. Only physical locators and detached signature bytes are
      excluded.
    file: docs/rfcs/rfc-0853-define-strict-certification-contracts-and-identity-builders.md
  - kind: rejected-alternative
    rfcId: RFC-0854
    title: Standardize the ecosystem on Node 24
    status: draft
    alternativesText: >-
      ### Keep `>=22` and test Node 22/24


      Rejected by the operator: there are no legacy consumers worth a
      compatibility matrix, and maintaining two majors weakens deterministic
      runtime evidence.


      ### Use `>=24`


      Rejected: it silently treats Node 25/26 as supported without CI, type,
      image, or canonical-vector evidence.


      ### Change CI only


      Rejected: CI is already Node 24. Stale manifests, types, Docker images,
      generated projects, and documentation would continue to authorize
      incompatible runtimes.


      ### Enable the engine gate before upgrading the executor


      Rejected: the current executor is Node 22, so this ordering would make
      pnpm reject the commands needed to finish and commit the transition.
      Executor bootstrap is an explicit precondition, not a product fallback.


      ### Leave `warpgogol-com` for a later migration RFC


      Rejected: there is one replaceable site and no legacy estate that
      justifies a second transition document. Leaving the sole active
      Sternsystem on `>=22`/Node 26 types would make the ecosystem-wide success
      claim false.


      ### Keep Forge on the `0.x` line


      Rejected: removing Node 20/22 is the first deliberately enforced breaking
      consumer boundary and must be visible as `1.0.0`. A platform major bump
      does not version the independent npm package.


      ### Rely only on `engines` warnings


      Rejected: the transition must fail before work starts. Project engine
      mismatch and pnpm workspace engine enforcement provide a deterministic
      blocking boundary.


      ### Rewrite every old Node string


      Rejected: archived documents are immutable history, dependency metadata is
      third-party evidence, and discovery tests intentionally parse arbitrary
      versions. Only current first-party support declarations change.
    file: docs/rfcs/rfc-0854-standardize-the-ecosystem-on-node-24.md
  - kind: rejected-alternative
    rfcId: RFC-0855
    title: Establish the authority-first agent-native runtime and certification
      program
    status: implemented
    alternativesText: >-
      ### Implement release certification unchanged, then replace the runtime


      Rejected because RFC-0853 would freeze the static plugin identity into
      permanent schemas, CERT-004 would execute through a registry scheduled for
      deletion, and the system would undergo two large cutovers.


      ### Enable dynamic self-extension before certification


      Rejected because the running agent could create code before independent
      authority, immutable evidence, sandbox admission, rollback, and
      kill-switch controls existed.


      ### Use Cordis as the production runtime


      Rejected. Cordis provides valuable lifecycle semantics but does not model
      Werkstatt's four external-effect classes, protected authority,
      release/capability identity, or cross-platform/provider-neutral isolation
      boundary. Depending on its moving API would add a second adaptation
      problem for weaker agents.


      ### Keep `werkstatt/plugin@1` and add reversible registrations around it


      Rejected because the monolithic static plugin and fine-grained dynamic
      graph would become competing composition authorities. The five-hook
      contract would continue to hide dependencies and external effects.


      ### One monolithic implementation RFC


      Rejected by the operator. A single document cannot be safely implemented
      by a fresh less-capable agent without silently crossing package, trust,
      identity, storage, deployment, and migration boundaries.


      ### Parallelize independent packets


      Rejected even where a graph permits it. Lower elapsed time is not worth
      nondeterministic ownership, stale source hashes, interleaved commits, and
      ambiguous handoff evidence.


      ### Keep the old deployment path working until final cutover


      Rejected by the operator. Temporary operational unavailability is
      accepted. Maintaining dual authority would add compatibility code and
      weaken the forward-only transition.


      ### Treat any red build as acceptable transition breakage


      Rejected. Only known, enumerated operational diagnostics may remain.
      Arbitrary type, test, schema, state, or validator failures destroy the
      packet handoff contract.
    file: docs/rfcs/rfc-0855-establish-the-authority-first-agent-native-runtime-and-certification-program.md
  - kind: rejected-alternative
    rfcId: RFC-0856
    title: Establish the governed program packet control plane
    status: accepted
    alternativesText: >-
      ### Prose-only packets


      Rejected because the audit found contradictions that a weak executor
      cannot reliably resolve: stale hashes, unknown future commits,
      self-referential reports, and concurrent starts.


      ### Reuse process-local `.werkstatt/locks`


      Rejected because a packet spans multiple CLI processes and agent turns.
      PID liveness is not a durable executor lease and would either expire
      immediately or leave an unrelated process as false authority.


      ### Let the executor seal or complete its packet


      Rejected because it lets the actor under evaluation change its own scope,
      expected diagnostics, or success evidence.


      ### Store the seal or completion commit inside that same commit


      Rejected as cryptographically self-referential. Commit identity is
      observed by the next transition rather than embedded in its own content.


      ### Implement the control plane in Werkstatt


      Rejected because packet governance is stack-agnostic and reusable across
      Forge profiles. Werkstatt remains the composition host, not the owner of
      the generic protocol.


      ### Distributed lease service


      Rejected as unnecessary infrastructure. RFC-0855 prohibits cross-workspace
      parallel execution; the local lease prevents accidental concurrency in the
      shared workspace and reports its non-security limitation honestly.
    file: docs/rfcs/rfc-0856-establish-the-governed-program-packet-control-plane.md
  - kind: rejected-alternative
    rfcId: RFC-0857
    title: Make release-certification RFC materialization just-in-time
    status: accepted
    alternativesText: >-
      ### Create every CERT RFC manually during charter implementation


      Rejected because it bypasses `spec.materialize`, manual RFC numbering and
      `materializedAs` mutation, and the dependency-front guarantee.


      ### Add a blocked-node or planning mode to `spec.materialize`


      Rejected because it weakens RFC-0396 globally to accommodate one program.
      A planning scaffold that looks like a materialized RFC would be
      indistinguishable from a prematurely actionable decision to weaker agents.


      ### Accept AMD-007 before charter implementation


      Rejected because packet 040 is the explicit human reconciliation boundary.
      Early acceptance would make the packet ceremonial and could still not
      truthfully implement CERT-001 dependencies.


      ### Put `RFC-XXXX` placeholders in future packets


      Rejected because unresolved prose placeholders are forbidden by RFC-0855
      and force weak executors to make an architectural choice during
      implementation.


      ### Rewrite the packet to the repository RFC id after materialization


      Rejected because the spec node is the durable identity and the RFC number
      is a projection. Rewriting the governing identity adds avoidable packet
      churn and weakens traceability; sealing can resolve and bind both
      identities without replacing either.
    file: docs/rfcs/rfc-0857-make-release-certification-rfc-materialization-just-in-time.md
  - kind: rejected-alternative
    rfcId: RFC-0858
    title: Establish versioned component and capability contracts
    status: draft
    alternativesText: >-
      ### Extend `WerkstattPlugin`


      Rejected because a monolithic plugin cannot independently version,
      replace, drain, grant, isolate, or hash producers/evaluators/adapters.


      ### Use bare string service keys


      Rejected because they omit namespace, compatibility, and schema identity
      and make resolution nondeterministic.


      ### Let components declare arbitrary effect names


      Rejected because admission cannot prove recovery semantics for an open
      vocabulary.
    file: docs/rfcs/rfc-0858-establish-versioned-component-and-capability-contracts.md
  - kind: rejected-alternative
    rfcId: RFC-0859
    title: Establish the lifecycle fiber and effect runtime
    status: draft
    alternativesText: >-
      ### Independent AbortControllers and disposers


      Rejected because they do not encode dependency order, quiescence,
      transaction boundaries, or effect-class laws.


      ### Force unload after timeout


      Rejected because it can corrupt in-flight state and misrepresent
      irreversible effects as rolled back.


      ### Reuse plugin hook lifecycle


      Rejected because five coarse hooks do not own individual
      registrations/resources or dependent drains.
    file: docs/rfcs/rfc-0859-establish-the-lifecycle-fiber-and-effect-runtime.md
  - kind: rejected-alternative
    rfcId: RFC-0860
    title: Establish deterministic component resolution and reconciliation
    status: draft
    alternativesText: >-
      ### Let module registration order resolve dependencies


      Rejected because filesystem/import order is not a compatibility policy and
      cannot produce stable graph identity.


      ### Keep last-known plugin as fallback


      Rejected because it creates dual authority and lets invalid desired state
      appear healthy.


      ### Use a general package solver


      Rejected because the required vocabulary is smaller, policy-bound, and
      must include grants/effects/isolation rather than package versions alone.
    file: docs/rfcs/rfc-0860-establish-deterministic-component-resolution-and-reconciliation.md
  - kind: rejected-alternative
    rfcId: RFC-0861
    title: Establish runtime reflection and the conformance harness
    status: draft
    alternativesText: >-
      ### Expose the kernel registry


      Rejected because it is mutable, unfiltered, static-authority-shaped, and
      leaks irrelevant internals.


      ### Add a temporary production `component.run`


      Rejected because it would bypass sandbox, certification, promotion,
      rollback, and kill switch.


      ### Depend on Cordis tests/runtime


      Rejected; scenarios may inform expectations, but Werkstatt owns contracts
      and has no production dependency.
    file: docs/rfcs/rfc-0861-establish-runtime-reflection-and-the-conformance-harness.md
  - kind: rejected-alternative
    rfcId: RFC-0862
    title: Establish the provider-neutral isolation contract
    status: draft
    alternativesText: >-
      ### Standardize on `node:vm` or worker threads


      Rejected because neither is a hostile-code security boundary.


      ### Standardize one provider in the base contract


      Rejected because component/runtime contracts should express properties and
      bridge behavior, while provider choice and evidence belong to packet 190.


      ### Give workloads filtered environment/filesystem access


      Rejected because deny lists leak ambient authority; all access must be
      explicit brokered capability RPC.
    file: docs/rfcs/rfc-0862-establish-the-provider-neutral-isolation-contract.md
  - kind: rejected-alternative
    rfcId: RFC-0863
    title: Establish capability artifacts and the first real sandbox
    status: draft
    alternativesText: >-
      ### Load npm packages or source folders directly


      Rejected because mutable resolution and installation state cannot be the
      reviewed executable identity.


      ### Let the provider define grants and messages


      Rejected because provider APIs would become policy authority and violate
      the neutral contract.


      ### Build the evolution controller in the same packet


      Rejected because secure artifact execution must be proven independently
      before autonomous candidate transitions exist.
    file: docs/rfcs/rfc-0863-establish-capability-artifacts-and-the-first-real-sandbox.md
  - kind: rejected-alternative
    rfcId: RFC-0864
    title: Establish the governed capability evolution controller
    status: draft
    alternativesText: >-
      ### Let an agent edit a live capability in place


      Rejected because mutable activation destroys review identity,
      reproducibility, and rollback evidence.


      ### Promote from deterministic tests alone


      Rejected because runtime novelty, distribution shifts, latency, costs,
      privacy, and side effects require bounded observation.


      ### Let the controller evolve governance and isolation


      Rejected because a replaceable capability must not expand its own
      authority or redefine its evaluator.
    file: docs/rfcs/rfc-0864-establish-the-governed-capability-evolution-controller.md

