---
rfcId: RFC-0721
planId: PLAN-RFC-0721-01
status: draft
owner: architecture
createdAt: 2026-08-06
updatedAt:
scope:
  apps: []
  packages:
    - "@warpgogol/site-kernel-checks"
  services: []
  docs:
    - packages/os/site-kernel-checks/AGENTS.md
    - docs/command-manifest.generated.yaml
---

# Implementation Plan: RFC-0721

## 1. Objectives

- [ ] Objective 1 — Command `behavior.snapshot.staleness.check` is registered with `scope: "app"` and emits `SNAP-STALE-01` warnings for newRoutes only — maps to acceptance criteria [1, 2, 3, 4, 5]
- [ ] Objective 2 — `build.prepare` and `SITES_BUILD_PREPARE_DEV_PIPELINE` include the staleness check — maps to acceptance criteria [6, 7]
- [ ] Objective 3 — Documentation and command manifest are updated — maps to acceptance criteria [8, 9]
- [ ] Objective 4 — Existing SNAP-01 auto-regeneration remains unchanged — maps to acceptance criterion [10]
- [ ] Objective 5 — Unit tests cover all code paths — maps to acceptance criterion [11]

## 2. Affected artifacts

### 2.1 Code and commands

- `packages/os/site-kernel-checks/src/behavior-snapshot-staleness.ts` — command handler (already exists in working tree, needs review)
- `packages/os/site-kernel-checks/src/command-tables/build-infra.ts` — move command registration here from `01-codegen.ts` (alongside existing `behavior.snapshot.generate` and `behavior.snapshot.validate`)
- `packages/os/site-kernel-checks/src/command-tables/01-codegen.ts` — remove the staleness check entry (it belongs in `build-infra.ts`)
- `packages/os/site-kernel-checks/src/pipelines/build-prepare.ts` — verify `behavior.snapshot.staleness.check` is the last step in `SITES_BUILD_PREPARE_PIPELINE` (already present); add to `SITES_BUILD_PREPARE_DEV_PIPELINE`
- Site OS command: `behavior.snapshot.staleness.check` (scope: app, advisory)

### 2.2 Configuration and data

- No YAML/JSON/NDJSON changes needed.
- No ontology catalog changes needed.

### 2.3 Documentation and specs

- `packages/os/site-kernel-checks/AGENTS.md` — add module table entry for `behavior-snapshot-staleness.ts`
- `docs/command-manifest.generated.yaml` — regenerated by `command.manifest.generate`
- RFC file: `docs/rfcs/draft/rfc-0721-add-behavior-snapshot-staleness-warning.md` (read-only reference)

### 2.4 Validation and pipelines

- `build.prepare` pipeline — staleness check as last step (already wired)
- `SITES_BUILD_PREPARE_DEV_PIPELINE` — add staleness check as last step
- No CI workflow changes needed.

## 3. Step sequence

### Step 0. Verify pre-existing working tree changes

**Goal:** Audit the pre-existing changes in the working tree from a previous session to ensure they match the RFC design before building on top of them.

**Agent actions:**

- Run `git diff --name-only` to list all modified/new files in the working tree.
- For each pre-existing file, verify it matches the RFC:
  - `packages/os/site-kernel-checks/src/behavior-snapshot-staleness.ts` — verify one-directional (newRoutes only), correct skip behavior, correct imports, Compass blocks present.
  - `packages/os/site-kernel-checks/src/pipelines/build-prepare.ts` — verify `behavior.snapshot.staleness.check` is the last step in `SITES_BUILD_PREPARE_PIPELINE`; check if it's also in `SITES_BUILD_PREPARE_DEV_PIPELINE`.
  - `packages/os/site-kernel-checks/src/command-tables/01-codegen.ts` — verify the command entry exists but note it needs to move to `build-infra.ts` (Step 1).
  - Any other modified files (`page-block.ts`, `hooks/pre-commit`, AGENTS.md files) — determine if they belong to RFC-0721 or are unrelated changes from another session. If unrelated, do NOT stage them in RFC-0721 commits.
- If any pre-existing change contradicts the RFC (e.g. removedRoutes direction still present), fix it now before proceeding.

**Validation:**

- `git diff --name-only` — list of pre-existing changes is known and categorized.
- `pnpm --filter @warpgogol/site-kernel-checks run build:check` — TypeScript compiles with pre-existing changes.

**Completion criterion:** All pre-existing changes are verified as compatible with RFC-0721 or fixed; unrelated changes are identified and excluded from RFC-0721 commits.

**Human review:** no

---

### Step 1. Move command registration to `build-infra.ts` and fix scope

**Goal:** Move the `behavior.snapshot.staleness.check` command table entry from `01-codegen.ts` to `build-infra.ts` (where the other behavior.snapshot commands live) and fix `scope: "workspace"` → `scope: "app"`.

**Agent actions:**

- Remove the `behavior.snapshot.staleness.check` entry from `packages/os/site-kernel-checks/src/command-tables/01-codegen.ts` (lines ~644–656) and the associated import of `runBehaviorSnapshotStalenessCheck`.
- Add the entry to `packages/os/site-kernel-checks/src/command-tables/build-infra.ts` after the existing `behavior.snapshot.validate` entry, with `scope: "app"` and `supportsAllSites: true`.
- Add the import of `runBehaviorSnapshotStalenessCheck` from `../behavior-snapshot-staleness.ts` to `build-infra.ts`.
- Update the `CHANGE_SUMMARY` in `build-infra.ts` with an RFC-0721 entry.

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-checks run build:check` — TypeScript compiles without errors.

**Completion criterion:** `behavior.snapshot.staleness.check` is registered in `BUILD_INFRA_COMMANDS` with `scope: "app"`, and no stale import or entry remains in `01-codegen.ts`.

**Human review:** no

---

### Step 2. Add staleness check to `SITES_BUILD_PREPARE_DEV_PIPELINE`

**Goal:** Include the staleness check in the dev pipeline so developers see stale warnings during dev iterations.

**Agent actions:**

- Add `{ command: "behavior.snapshot.staleness.check" }` as the last step in `SITES_BUILD_PREPARE_DEV_PIPELINE` in `packages/os/site-kernel-checks/src/pipelines/build-prepare.ts`.
- Update the `CHANGE_SUMMARY` with an RFC-0721 entry.

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-checks run build:check` — TypeScript compiles without errors.

**Completion criterion:** `SITES_BUILD_PREPARE_DEV_PIPELINE` includes `behavior.snapshot.staleness.check` as its last step.

**Human review:** no

---

### Step 3. Review and fix the command handler

**Goal:** Ensure `behavior-snapshot-staleness.ts` matches the RFC design: one-directional (newRoutes only), correct skip behavior, correct imports.

**Agent actions:**

- Verify the handler uses `requireAstroSitePaths(context)` for path resolution (not `context.site` directly).
- Verify only `newRoutes` direction is checked (no `removedRoutes`).
- Verify skip behavior: returns empty diagnostics on missing app context, missing snapshot, or unreadable system.md.
- Verify the handler imports `loadSystemManifest` from `@warpgogol/site-kernel-content` and `requireAstroSitePaths` from `@warpgogol/site-kernel-astro`.
- Verify the `CHANGE_SUMMARY` and `MODULE_CONTRACT` blocks are present and accurate.

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-checks run build:check` — TypeScript compiles without errors.

**Completion criterion:** Handler matches RFC design — one-directional, correct skips, correct imports, Compass blocks present.

**Human review:** no

---

### Step 4. Write unit tests

**Goal:** Cover all code paths: stale route detected, no snapshot (skip), no app context (skip), surface routes not flagged, up-to-date snapshot (pass).

**Agent actions:**

- Create `packages/os/site-kernel-checks/src/tests/behavior-snapshot-staleness.test.ts`.
- Test cases:
  1. `SNAP-STALE-01: warns when system.md route is absent from committed snapshot` — create fixture with system.md pages[] routes and a snapshot missing one route; verify warning diagnostic with `ruleId: "SNAP-STALE-01"`.
  2. `skips when no committed snapshot exists` — no `behavior.snapshot.generated.yaml`; verify empty diagnostics, exit code 0.
  3. `skips when no app context` — no site in context; verify empty diagnostics.
  4. `does not flag removedRoutes (surface routes)` — snapshot has extra routes not in system.md; verify no warnings for those routes.
  5. `passes when all system.md routes are in snapshot` — verify empty diagnostics, exit code 0.
- Use the same fixture pattern as `behavior-snapshot.test.ts` (mkdtemp, fixtureDistClient, etc.).

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-checks run test` — all tests pass.

**Completion criterion:** All 5 test cases pass and cover the acceptance criterion for unit test coverage.

**Human review:** no

---

### Step 5. Update AGENTS.md and run command.manifest.generate

**Goal:** Update the module table in `packages/os/site-kernel-checks/AGENTS.md` and regenerate the command manifest.

**Agent actions:**

- Add a row to the module table in `packages/os/site-kernel-checks/AGENTS.md`: `| src/behavior-snapshot-staleness.ts | RFC-0721 runBehaviorSnapshotStalenessCheck — advisory pre-build warning when system.md pages[] routes are absent from behavior.snapshot.generated.yaml. Diagnostics: SNAP-STALE-01 (warning). One-directional: only checks newRoutes direction. |`
- Run `pnpm exec site-kernel run command.manifest.generate` to update `docs/command-manifest.generated.yaml`.
- Verify the new command appears in the manifest with `scope: "app"`.

**Validation:**

- `pnpm exec site-kernel run rfc.validate --id RFC-0721` — passes (RFC-CMD-02 checks command manifest).
- `git diff docs/command-manifest.generated.yaml` — shows the new command entry.

**Completion criterion:** AGENTS.md module table has the new entry; `docs/command-manifest.generated.yaml` includes `behavior.snapshot.staleness.check` with `scope: "app"`.

**Human review:** no

---

### Step 6. Final validation and acceptance criteria verification

**Goal:** Run all validation checks, verify acceptance criteria, and prepare for stamping.

**Agent actions:**

- Run `pnpm --filter @warpgogol/site-kernel-checks run build:check` — TypeScript compiles.
- Run `pnpm --filter @warpgogol/site-kernel-checks run test` — all tests pass.
- Run `pnpm exec site-kernel run rfc.validate --id RFC-0721` — passes.
- Check off each acceptance criterion in the RFC with inline `(evidence: <file:line>)` annotations.
- Commit all changes with `RFC-0721` in the commit message.

**Validation:**

- `git status` — no uncommitted changes from this session (except pre-existing modified files not related to RFC-0721).
- `pnpm exec site-kernel run rfc.validate --id RFC-0721` — passes.

**Completion criterion:** All validation checks pass; all acceptance criteria are checked off with evidence annotations.

**Human review:** no

---

### Final Step. Documentation sync, review, fix, and acceptance criteria verification

**Goal:** Synchronize all documentation artifacts, run code review and fix, verify acceptance criteria, and stamp the RFC as implemented.

**Agent actions:**

- Verify `packages/os/site-kernel-checks/AGENTS.md` is updated with the new module entry.
- Run `pnpm exec site-kernel run ecosystem.manifest.generate` if command surfaces or pipeline topology changed.
- **Run code review:** invoke `fo-review` via the `skill` tool on all session code changes. Wait for the review report in `docs/reviews/code/`.
- **Run fix if needed:** if `fo-review` reported findings, invoke `fo-fix` via the `skill` tool. Re-run `fo-review` to confirm all findings are resolved. Maximum 3 iterations.
- **Check off acceptance criteria:** verify each criterion in the RFC against the implemented code. Mark `[x]` for verified criteria with inline `(evidence: ...)` annotations.
- **Stamp the RFC as implemented:** run `pnpm exec site-kernel run rfc.implement.stamp --id RFC-0721 --implementation-commit <sha>` to atomically transition `accepted → implemented`.

**Validation:**

- `git status` — no uncommitted changes from the current session.
- `pnpm exec site-kernel run rfc.validate --id RFC-0721` — passes.
- Review report exists in `docs/reviews/code/` for this session.

**Completion criterion:** All documentation artifacts in scope are updated; code review passed (findings fixed if any); all acceptance criteria are checked off with inline `(evidence: ...)` annotations; RFC is stamped as `implemented` via `rfc.implement.stamp`.

**Human review:** no — the `accepted → implemented` transition is automated via `rfc.implement.stamp` (RFC-0476), which validates all preconditions atomically. Code review is automated via `fo-review`.

## 4. Validation suite

### 4.1 Required checks

- `pnpm exec site-kernel run rfc.validate --id RFC-0721`
- `pnpm --filter @warpgogol/site-kernel-checks run build:check`
- `pnpm --filter @warpgogol/site-kernel-checks run test`

### 4.2 Evidence artifacts

- Commit messages referencing `RFC-0721` in the subject line (RFC-0265 commit hygiene)
- Review report in `docs/reviews/code/` for this session

## 5. Risks and mitigation

| Risk (from RFC) | Mitigation (plan step) |
| --- | --- |
| Agent misinterpretation — treating warnings as fatal | Step 3 verifies severity is `warning`; Step 4 tests confirm exit code 0 with warnings |
| False positives from i18n route format mismatches | Step 4 test case 5 verifies pass when routes match; Step 3 verifies one-directional check only |
| Performance (~200ms) | Step 4 tests verify the check is lightweight; no mitigation needed — negligible vs total build.prepare runtime |

## 6. Escalation triggers

- If implementation reveals an invariant conflict with DNA-39 (route registry merge), run `pnpm exec site-kernel run rfc.supersede.propose --id RFC-0721 --reason "..." --invariant "DNA-39"` instead of working around it.
- If the staleness check needs to compare against the merged route registry instead of just `system.md pages[]`, this requires a new RFC (the current design is intentionally one-directional to avoid surface route false positives).
