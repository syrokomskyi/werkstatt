---
rfcId: RFC-0648
planId: PLAN-RFC-0648-01
status: draft
owner: architecture
createdAt: 2026-08-02
updatedAt:
scope:
  apps: []
  packages:
    - "@warpgogol/site-kernel-handoff"
  services: []
  docs:
    - packages/os/site-kernel-handoff/AGENTS.md
    - docs/command-manifest.generated.yaml
---

# Implementation Plan: RFC-0648

## 1. Objectives

- [ ] Objective 1 — Add `branch-convention` validation rule to `sternsystem.validate` (maps to acceptance criterion: "sternsystem.validate includes a branch-convention rule")
- [ ] Objective 2 — Change hardcoded `"master"` fallback to `"main"` in `sternsystem.status` and `mission.close` (maps to acceptance criteria for fallback changes)
- [ ] Objective 3 — Update `mission-materialize.ts` comments and `git init` fallback (maps to acceptance criteria for comments and line 1002)
- [ ] Objective 4 — Update all test helpers to `git init -b main` (maps to acceptance criterion for test helpers)
- [ ] Objective 5 — Document the `main` branch convention in AGENTS.md (maps to acceptance criterion for AGENTS.md)
- [ ] Objective 6 — Run `command.manifest.generate` and validate (maps to acceptance criterion for command manifest)

## 2. Affected artifacts

### 2.1 Code and commands

- `packages/os/site-kernel-handoff/src/sternsystem/sternsystem-validate.ts` — add `branch-convention` validation rule
- `packages/os/site-kernel-handoff/src/sternsystem/sternsystem-status.ts:124` — change fallback `"master"` → `"main"`
- `packages/os/site-kernel-handoff/src/mission/mission-close.ts:301` — change fallback `"master"` → `"main"`
- `packages/os/site-kernel-handoff/src/mission/mission-materialize.ts:341,367` — update comments `origin/master` → `origin/main`
- `packages/os/site-kernel-handoff/src/mission/mission-materialize.ts:1002` — change `git init` → `git init -b main`

### 2.2 Configuration and data

- `docs/command-manifest.generated.yaml` — regenerated by `command.manifest.generate` after command changes

### 2.3 Documentation and specs

- `packages/os/site-kernel-handoff/AGENTS.md` — document the `main` branch convention for Sternsystem repos

### 2.4 Validation and pipelines

- `sternsystem.validate` is not in any build pipeline — no pipeline changes needed
- `pnpm --filter @warpgogol/site-kernel-handoff run build:check` — typecheck
- `pnpm --filter @warpgogol/site-kernel-handoff run test` — unit tests

## 3. Step sequence

### Step 1. Add `branch-convention` validation rule to `sternsystem.validate`

**Goal:** Add a new validation rule that checks cache clone (mirrors[0]) and bare repo (mirrors[1]) branch name is `main`.

**Agent actions:**

- In `sternsystem-validate.ts`, after the existing mirror topology checks (around line 293), add a new `branch-convention` check:
  - For the cache clone (mirrors[0]): if `existsSync(cacheDir/.git)`, run `git symbolic-ref HEAD` and check the branch name is `main`. If it fails or returns a different name, push a violation with `rule: "branch-convention"`.
  - For the bare repo (mirrors[1]): if `existsSync(bareRepoPath)`, run `git symbolic-ref HEAD` and check the branch name is `main`. If it fails or returns a different name, push a violation with `rule: "branch-convention"`.
  - Skip if no `.git` directory exists (same skip condition as `checkBundleContract`).
  - Use `execSync("git symbolic-ref HEAD", { cwd, encoding: "utf-8", stdio: ["pipe", "pipe", "pipe"] })` and `.replace("refs/heads/", "").trim()` to get the branch name.
  - Violation message format: `cache clone branch is 'master', expected 'main' — run: git -C <cache-path> branch -m master main`

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-handoff run build:check` — typecheck passes

**Completion criterion:** `sternsystem.validate` includes a `branch-convention` rule that checks cache clone and bare repo branch name is `main`.

**Human review:** no

---

### Step 2. Change fallback from `"master"` to `"main"` in `sternsystem.status` and `mission.close`

**Goal:** Replace the hardcoded `"master"` fallback with `"main"` in both files.

**Agent actions:**

- In `sternsystem-status.ts:124`: change `branch = "master"` to `branch = "main"`
- In `mission-close.ts:301`: change `branch = "master"` to `branch = "main"`

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-handoff run build:check` — typecheck passes

**Completion criterion:** Both fallbacks changed from `"master"` to `"main"`.

**Human review:** no

---

### Step 3. Update `mission-materialize.ts` comments and `git init` fallback

**Goal:** Update comments referencing `origin/master` and change the non-git cache clone fallback `git init` to `git init -b main`.

**Agent actions:**

- In `mission-materialize.ts:341`: update comment from `fetch + reset to origin/master` to `fetch + reset to origin/main`
- In `mission-materialize.ts:367`: update comment from `Cache clone exists — fetch and reset to origin/master` to `Cache clone exists — fetch and reset to origin/main`
- In `mission-materialize.ts:1002`: change `execSync("git init", { cwd: workpieceDir, ... })` to `execSync("git init -b main", { cwd: workpieceDir, ... })`

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-handoff run build:check` — typecheck passes

**Completion criterion:** Comments updated and `git init` fallback uses `-b main`.

**Human review:** no

---

### Step 4. Update all test helpers to `git init -b main`

**Goal:** Update all `gitInit` helper functions across the test suite to use `git init -b main` instead of `git init`.

**Agent actions:**

- Update `tests/helpers/materialize-fixture.ts:16`: `execSync("git init -b main", ...)`
- Update `tests/mission-validate-snapshot-auto-regen.test.ts:132`: `execSync("git init -b main", ...)`
- Update `tests/mission-materialize-baseline.test.ts:63`: `execSync("git init -b main", ...)`
- Update `tests/mission-validate-distribution-reuse.test.ts:67`: `execSync("git init -b main", ...)`
- Update `tests/rfc-0614-public-well-known-bordbuch-conflict.test.ts:18`: `execSync("git init -b main", ...)`
- Update `tests/werkstatt-commit.test.ts:18`: `execSync("git init -b main", ...)`
- Update `tests/mission-build-check-phase.test.ts:84`: `execSync("git init -b main", ...)`
- Update `tests/mission-validate-cache-clone-warning.test.ts:18`: `execSync("git init -b main", ...)`
- Update `tests/mission-dirty-guard.test.ts:19`: `execSync("git init -b main", ...)`
- Update `tests/mission-close-state-file.test.ts:37`: `execSync("git init -b main", ...)`
- Update `tests/mission-open-clean-tree.test.ts:19`: `execSync("git init -b main", ...)`

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-handoff run test` — all tests pass

**Completion criterion:** All 11 `gitInit` helpers use `git init -b main`.

**Human review:** no

---

### Step 5. Update `sternsystem-sync-integration.test.ts`

**Goal:** Update the integration test to use `git init --bare -b main` and push to `origin main`.

**Agent actions:**

- In `sternsystem-sync-integration.test.ts:68-69`: change `git init --bare "${bareDir}"` to `git init --bare -b main "${bareDir}"` and same for `externalDir`
- In `sternsystem-sync-integration.test.ts:81`: change `git push origin master` to `git push origin main`

**Validation:**

- `pnpm --filter @warpgogol/site-kernel-handoff run test` — integration tests pass

**Completion criterion:** Integration test uses `git init --bare -b main` and pushes to `origin main`.

**Human review:** no

---

### Step 6. Document the `main` branch convention in AGENTS.md

**Goal:** Add a rule to `packages/os/site-kernel-handoff/AGENTS.md` documenting that all Sternsystem repos must use `main` as their default branch.

**Agent actions:**

- Add a new rule section to `packages/os/site-kernel-handoff/AGENTS.md` under the existing Sternsystem section:
  - "All Sternsystem cache clones (mirrors[0]) and bare mirror repos (mirrors[1]) MUST use `main` as their default branch, not `master`. Enforced by `sternsystem.validate` (`branch-convention` rule). When creating new Sternsystem bare repos, use `git init --bare -b main`. Agents MUST NOT rename branches automatically — branch renaming is a manual operator action."

**Validation:**

- Visual inspection of the AGENTS.md file

**Completion criterion:** AGENTS.md documents the `main` branch convention.

**Human review:** no

---

### Step 7. Run `command.manifest.generate` and validate

**Goal:** Regenerate the command manifest to reflect the changed commands and run full validation.

**Agent actions:**

- Run `pnpm exec werkstatt run command.manifest.generate` to update `docs/command-manifest.generated.yaml`
- Run `pnpm exec werkstatt run rfc.validate --id RFC-0648` to verify the RFC passes validation
- Run `pnpm --filter @warpgogol/site-kernel-handoff run build:check` to verify typecheck
- Run `pnpm --filter @warpgogol/site-kernel-handoff run test` to verify all tests pass

**Validation:**

- `rfc.validate` passes with 0 violations
- `build:check` passes
- All tests pass

**Completion criterion:** Command manifest regenerated, all validation passes.

**Human review:** no

---

### Final Step. Documentation sync, review, fix, and acceptance criteria verification

**Goal:** Synchronize all documentation artifacts, run code review and fix, verify acceptance criteria, and stamp the RFC as implemented.

**Agent actions:**

- Verify `packages/os/site-kernel-handoff/AGENTS.md` is updated with the `main` branch convention.
- Run `pnpm exec werkstatt run ecosystem.manifest.generate` if command surfaces or pipeline topology changed.
- **Run code review:** invoke `fo-review` via the `skill` tool on all session code changes. Wait for the review report in `docs/reviews/code/`.
- **Run fix if needed:** if `fo-review` reported findings, invoke `fo-fix` via the `skill` tool. Re-run `fo-review` to confirm all findings are resolved. Maximum 3 iterations.
- **Check off acceptance criteria:** verify each criterion in the RFC against the implemented code. Mark `[x]` for verified criteria with inline `(evidence: <file:line>)` annotations.
- **Stamp the RFC as implemented:** run `pnpm exec werkstatt run rfc.implement.stamp --id RFC-0648 --implementation-commit <sha>` to atomically transition `accepted → implemented`.

**Validation:**

- `git status` — no uncommitted changes from the current session.
- `pnpm exec werkstatt run rfc.validate --id RFC-0648`
- Review report exists in `docs/reviews/code/` for this session.

**Completion criterion:** All documentation artifacts in scope are updated; code review passed (findings fixed if any); all acceptance criteria are checked off with inline `(evidence: ...)` annotations; RFC is stamped as `implemented` via `rfc.implement.stamp`.

**Human review:** no — the `accepted → implemented` transition is automated via `rfc.implement.stamp` (RFC-0476).

## 4. Validation suite

### 4.1 Required checks

- `pnpm exec werkstatt run rfc.validate --id RFC-0648`
- `pnpm --filter @warpgogol/site-kernel-handoff run build:check`
- `pnpm --filter @warpgogol/site-kernel-handoff run test`
- `pnpm exec werkstatt run command.manifest.generate`

### 4.2 Evidence artifacts

- Commit messages referencing `RFC-0648` in the subject line (RFC-0265 commit hygiene)

## 5. Risks and mitigation

| Risk (from RFC) | Mitigation (plan step) |
| --- | --- |
| Migration risk: existing repos use `master` | Step 7 validation will catch violations; operator must rename branches before `sternsystem.validate` passes |
| Agent misinterpretation: agents may rename branches automatically | Step 6 AGENTS.md rule explicitly states agents MUST NOT rename branches |
| False positive rate: low | `branch-convention` rule only checks repos with `.git` directory and resolvable HEAD — skip conditions match existing `checkBundleContract` pattern |
| Performance impact: negligible | `git symbolic-ref HEAD` is a fast file read — Step 1 uses the same `execSync` pattern as existing checks |

## 6. Escalation triggers

- If implementation reveals an invariant conflict with DNA-44 or DNA-45, run `pnpm exec werkstatt run rfc.supersede.propose --id RFC-0648 --reason "..." --invariant "DNA-N"` instead of working around it.
