---
rfcId: RFC-0759
planId: PLAN-RFC-0759-01
status: draft
owner: architecture
createdAt: 2026-08-08
updatedAt:
scope:
  apps:
    - warpgogol-com
  packages:
    - "@warpgogol/ontology"
    - "@warpgogol/ui"
  services: []
  docs:
    - packages/ontology/AGENTS.md
    - packages/ui/AGENTS.md
---

# Implementation Plan: RFC-0759

## 1. Objectives

- [ ] Objective 1 — Create `service-metadata-block` archetype YAML in the archetype catalog (maps to acceptance criterion #1)
- [ ] Objective 2 — Regenerate archetype registry index via `archetype.registry.build` (maps to acceptance criterion #2)
- [ ] Objective 3 — Scaffold section files via `section.scaffold` and generate TypeScript prop types (maps to acceptance criteria #3, #4)
- [ ] Objective 4 — Validate section contract and block props (maps to acceptance criteria #5, #6)
- [ ] Objective 5 — Update AGENTS.md documentation and verify acceptance criteria (maps to acceptance criteria #7, #8)

## 2. Affected artifacts

### 2.1 Code and commands

- `packages/ontology/archetypes/sections/service-metadata-block.yaml` — new archetype definition
- `packages/ontology/archetypes/index.yaml` — regenerated by `archetype.registry.build`
- `packages/ui/src/sections/service-metadata-block/` — new section directory:
  - `service-metadata-block-section.astro` — section template
  - `service-metadata-block-section.manifest.yaml` — section manifest
  - `service-metadata-block-section.types.generated.ts` — generated prop types
  - `service-metadata-block-section.css` — colocated CSS
  - `service-metadata-block-section.story.md` — story example
- `packages/share/src/page.ts` — `PLANET_IMPORT_PATHS` updated automatically via registry (RFC-0091)

### 2.2 Configuration and data

- Archetype YAML: `id`, `displayName`, `version`, `semanticRole`, `description`, `expectedIntents`, `expectedIndustryFit`, `layoutHint`, `bodyKind`, `propsSchema` (compose `section-visual` + `section-header`, shape with `version?`, `effectiveDate?`, `nextReviewDate?`, `links?`, `footnote?`), `acceptedCosmicNames`, `constraints`
- Section manifest: `archetype: service-metadata-block`, `layer: section`, `cosmicName: <picked>`, `role: page-metadata-footer`, `propsSchema` mirroring archetype, `propsSchemaCompose: [section-visual, section-header]`

### 2.3 Documentation and specs

- `packages/ontology/AGENTS.md` — no new rules needed (archetype addition is standard, no closed enum changes)
- `packages/ui/AGENTS.md` — no new rules needed (section follows standard framework)
- No Compass XML updates (package-level addition, no repository-wide semantics change)
- No `docs/architecture-dna.md` update (no new DNA invariant)

### 2.4 Validation and pipelines

- `archetype.registry.validate` — validates archetype YAML against schema and registry consistency
- `section.contract.validate` — validates section manifest against archetype contract
- `manifest.contract.validate` — validates manifest against `manifestSchema`
- `mirror.quintet.validate` — validates colocated `.astro` exists
- `page.block.validate` — validates block props against archetype `propsSchema`
- `props.types.generate` — generates `.types.generated.ts`
- `archetype.registry.build` — regenerates `index.yaml` and `PLANET_IMPORT_PATHS`

## 3. Step sequence

### Step 1. Create archetype YAML

**Goal:** Add the `service-metadata-block` archetype definition to the archetype catalog.

**Agent actions:**

- Create `packages/ontology/archetypes/sections/service-metadata-block.yaml` with:
  - `id: service-metadata-block`
  - `displayName: Service metadata block`
  - `version: 1.0.0`
  - `semanticRole: page-metadata-footer`
  - `description: Page-level metadata footer. RFC-0103 bodyKind: composite; default tone muted.`
  - `expectedIntents: [build-trust, legal-clarity]`
  - `expectedIndustryFit: [trades-and-construction, consulting-and-coaching, legal-services, non-profit]`
  - `layoutHint: single-column`
  - `bodyKind: composite`
  - `propsSchema` composing `section-visual` + `section-header` with shape: `version?`, `effectiveDate?`, `nextReviewDate?`, `links?` (array of `{label, href, rel?}`), `footnote?` — `.strict()`
  - `acceptedCosmicNames: []` (to be filled by `cosmic.name.pick` in step 3)
  - `constraints: {}`

**Validation:**

- `pnpm --filter @warpgogol/ontology build:check` — ontology package compiles
- `pnpm exec werkstatt run archetype.registry.validate` — archetype YAML valid

**Completion criterion:** Archetype YAML file exists at `packages/ontology/archetypes/sections/service-metadata-block.yaml` and passes `archetype.registry.validate`.

**Human review:** no

---

### Step 2. Regenerate archetype registry

**Goal:** Update `index.yaml` and `PLANET_IMPORT_PATHS` with the new archetype entry.

**Agent actions:**

- Run `pnpm exec werkstatt run archetype.registry.build`
- Verify `packages/ontology/archetypes/index.yaml` contains a `service-metadata-block` entry with `layer: section`
- Verify `totalCount` incremented by 1

**Validation:**

- `pnpm exec werkstatt run archetype.registry.validate` — registry consistent
- `pnpm --filter @warpgogol/ontology build:check` — ontology compiles with new registry

**Completion criterion:** `index.yaml` contains `service-metadata-block` entry; `archetype.registry.validate` passes.

**Human review:** no

---

### Step 3. Pick cosmic name and scaffold section

**Goal:** Materialize the section file set via `section.scaffold` with a picked cosmic name.

**Agent actions:**

- Run `pnpm exec werkstatt run cosmic.name.pick --archetype service-metadata-block` to select a free `PlanetName`
- Update `acceptedCosmicNames` in the archetype YAML with the picked name (if not already set)
- Run `pnpm exec werkstatt run section.scaffold --name service-metadata-block --archetype service-metadata-block`
- Verify the full file set exists in `packages/ui/src/sections/service-metadata-block/`:
  - `service-metadata-block-section.astro`
  - `service-metadata-block-section.manifest.yaml`
  - `service-metadata-block-section.css`
  - `service-metadata-block-section.story.md`
- Customize the `.astro` template: replace scaffold stub with content-aware markup that renders `version`, `effectiveDate`, `nextReviewDate`, `links[]`, and `footnote` using `--ds-*` tokens with `tone: muted` default. Use `SectionShell` + optional `SectionHeader` + body.
- Customize the `.css` file with muted-tone styling using `--ds-*` tokens.
- Write a realistic `.story.md` example with version, dates, links, and footnote.
- Update the manifest `propsSchema` to mirror the archetype's `propsSchema` (JSON Schema form, not Zod) — both must match per the archetype-manifest schema sync rule.

**Validation:**

- `pnpm exec werkstatt run manifest.contract.validate` — manifest valid against `manifestSchema`
- `pnpm exec werkstatt run mirror.quintet.validate` — colocated `.astro` exists
- `pnpm exec werkstatt run section.contract.validate` — section manifest matches archetype contract
- `pnpm exec werkstatt run section.placeholder.lint` — no JSON.stringify scaffold stub remaining

**Completion criterion:** Full section file set exists; `.astro` renders content-aware markup (not scaffold stub); `section.contract.validate` passes.

**Human review:** no

---

### Step 4. Generate TypeScript prop types

**Goal:** Produce `.types.generated.ts` from the manifest `propsSchema`.

**Agent actions:**

- Run `pnpm exec werkstatt run props.types.generate`
- Verify `packages/ui/src/sections/service-metadata-block/service-metadata-block-section.types.generated.ts` exists and contains the prop interface matching the archetype `propsSchema`

**Validation:**

- `pnpm --filter @warpgogol/ui build:check` — UI package compiles with generated types

**Completion criterion:** `.types.generated.ts` exists; `@warpgogol/ui` `build:check` passes.

**Human review:** no

---

### Step 5. Validate block props and pipeline integration

**Goal:** Confirm the archetype participates correctly in the block-declarative page composition pipeline.

**Agent actions:**

- Run `pnpm exec werkstatt run page.block.validate` (workspace-wide) — verify no violations related to `service-metadata-block`
- Run `pnpm --filter @warpgogol/ui run build` — full UI build passes
- Run `pnpm --filter @warpgogol/ontology run build` — full ontology build passes

**Validation:**

- `page.block.validate` passes with zero violations
- Both package builds pass

**Completion criterion:** `page.block.validate` accepts `type: service-metadata-block` blocks; both package builds pass.

**Human review:** no

---

### Final Step. Documentation sync, review, fix, and acceptance criteria verification

**Goal:** Synchronize documentation artifacts, run code review and fix, verify acceptance criteria, and stamp the RFC as implemented.

**Agent actions:**

- Verify `packages/ontology/AGENTS.md` — no new rules needed (standard archetype addition, no closed enum changes). Document why no update was needed.
- Verify `packages/ui/AGENTS.md` — no new rules needed (standard section framework). Document why no update was needed.
- Run `pnpm exec werkstatt run ecosystem.manifest.generate` if command surfaces or pipeline topology changed (not expected for this RFC).
- **Run code review:** invoke `fo-review` via the `skill` tool on all session code changes. Wait for the review report in `docs/reviews/code/`.
- **Run fix if needed:** if `fo-review` reported findings, invoke `fo-fix` via the `skill` tool. Re-run `fo-review` to confirm all findings are resolved. Maximum 3 iterations.
- **Check off acceptance criteria:** verify each criterion in the RFC against the implemented code. Mark `[x]` for verified criteria with inline `(evidence: ...)` annotations.
- **Stamp the RFC as implemented:** run `pnpm exec werkstatt run rfc.implement.stamp --id RFC-0759 --implementation-commit <sha>` to atomically transition `accepted → implemented`.

**Validation:**

- `git status` — no uncommitted changes from the current session.
- `pnpm exec werkstatt run rfc.validate --id RFC-0759`
- Review report exists in `docs/reviews/code/` for this session.

**Completion criterion:** All documentation artifacts in scope are updated or documented as not-applicable; code review passed (findings fixed if any); all acceptance criteria are checked off with inline `(evidence: ...)` annotations; RFC is stamped as `implemented` via `rfc.implement.stamp`.

**Human review:** no — the `accepted → implemented` transition is automated via `rfc.implement.stamp` (RFC-0476).

## 4. Validation suite

### 4.1 Required checks

- `pnpm exec werkstatt run rfc.validate --id RFC-0759`
- `pnpm --filter @warpgogol/ontology run build:check`
- `pnpm --filter @warpgogol/ui run build:check`
- `pnpm exec werkstatt run archetype.registry.validate`
- `pnpm exec werkstatt run section.contract.validate`
- `pnpm exec werkstatt run page.block.validate`
- `pnpm exec werkstatt run manifest.contract.validate`
- `pnpm exec werkstatt run mirror.quintet.validate`
- `pnpm exec werkstatt run section.placeholder.lint`

### 4.2 Evidence artifacts

- No acceptance probes declared (commented out in RFC frontmatter) — `rfc.verification.emit` will produce no evidence file (expected behavior).
- Commit messages referencing `RFC-0759` in the subject line (RFC-0265 commit hygiene).

## 5. Risks and mitigation

| Risk (from RFC) | Mitigation (plan step) |
| --- | --- |
| Archetype proliferation | Step 1 — archetype is general-purpose (page-level metadata footer), not site-specific |
| Date format inconsistency | Step 3 — `.astro` renders date strings as-is; no parsing/validation (per RFC Decision) |
| Cosmic name collision | Step 3 — `cosmic.name.pick` selects a free `PlanetName` automatically |
| Agent misinterpretation (confuse with site footer) | RFC Implementation notes + `packages/ui/AGENTS.md` (no update needed — existing rules cover section vs. shell distinction) |
| Agent misinterpretation (confuse with dynamic-status-block) | RFC Implementation notes clarify: `service-metadata-block` = static metadata; `dynamic-status-block` = data-driven value |

## 6. Escalation triggers

- If implementation reveals an invariant conflict with DNA-17 (Uni manifest contract), run `pnpm exec werkstatt run rfc.supersede.propose --id RFC-0759 --reason "..." --invariant "DNA-17"` instead of working around it (RFC-0334).
- If `section.scaffold` fails because no free `PlanetName` is available in `acceptedCosmicNames`, add a new `PlanetName` to the `PlanetCatalog` first (requires a superseding RFC per DNA-19/DNA-23).
