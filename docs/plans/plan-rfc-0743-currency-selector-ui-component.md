---
rfcId: RFC-0743
planId: PLAN-RFC-0743-01
status: draft
owner: architecture
createdAt: 2026-08-07
updatedAt:
scope:
  apps:
    - warpgogol-com
  packages:
    - "@warpgogol/ui"
    - "@warpgogol/share"
    - "@warpgogol/ontology"
    - "@warpgogol/pbp"
  services: []
  docs:
    - packages/ui/AGENTS.md
    - packages/ontology/AGENTS.md
---

# Implementation Plan: RFC-0743

## 1. Objectives

- [ ] O1 — Add `currency-selector` component with Mirror Quintet compliance — maps to acceptance criteria [1, 2, 3, 6, 8]
- [ ] O2 — Add `currency-aware-price-display` component with pre-rendered variants and client-side swap — maps to acceptance criteria [4, 5, 6, 7, 8]
- [ ] O3 — Extend `ComponentRole` closed enum and create component archetypes — maps to acceptance criterion [6]
- [ ] O4 — Register cosmic names in `MoonCatalog` and `MOON_IMPORT_PATHS` — maps to acceptance criterion [6]
- [ ] O5 — Integrate selector into header next to `lang-switcher` — maps to acceptance criterion [1]
- [ ] O6 — Write unit tests for both client scripts — maps to acceptance criterion [11]
- [ ] O7 — Validate, review, fix, and stamp implemented — maps to acceptance criterion [12]

## 2. Affected artifacts

### 2.1 Code and commands

- `packages/ontology/src/enums.ts` — add `currency-selector`, `price-display` to `ComponentRoleValues`
- `packages/ontology/archetypes/components/currency-selector.yaml` — new archetype
- `packages/ontology/archetypes/components/currency-aware-price-display.yaml` — new archetype
- `packages/ontology/archetypes/index.yaml` — register new component archetypes
- `packages/ontology/archetypes/index.json` — regenerated by `archetype.registry.build`
- `packages/ui/src/components/currency-selector/currency-selector.astro` — selector component
- `packages/ui/src/components/currency-selector/currency-selector.css` — styles
- `packages/ui/src/components/currency-selector/currency-selector-component.manifest.yaml` — manifest
- `packages/ui/src/components/currency-selector/currency-selector-component.client.ts` — client script
- `packages/ui/src/components/currency-selector/currency-selector.types.ts` — types
- `packages/ui/src/components/currency-selector/currency-selector.md` — content template
- `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.astro` — price display component
- `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.css` — styles
- `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display-component.manifest.yaml` — manifest
- `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display-component.client.ts` — client script
- `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.types.ts` — types
- `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.md` — content template
- `packages/share/src/page.ts` — `MOON_IMPORT_PATHS` entries for both components
- `packages/ui/src/components/header/header-component.astro` — add `CurrencySelector` import and render next to `LangSwitcher`

### 2.2 Configuration and data

- `packages/ontology/src/cosmic/moon-catalog.ts` — no changes needed (MoonCatalog is already populated with available names; select two non-reserved names)
- Content schemas registered via `contentSchemaKey` in manifests (e.g. `currency-selector-component`, `currency-aware-price-display-component`)

### 2.3 Documentation and specs

- `packages/ui/AGENTS.md` — no structural changes needed (existing rules cover new components)
- `packages/ontology/AGENTS.md` — no structural changes needed (existing rules cover new enum values and archetypes)
- No `docs/*.xml` Compass files require updates (no repository-wide semantics change)
- No `docs/architecture-dna.md` changes (no new DNA invariant)

### 2.4 Validation and pipelines

- `pnpm --filter @warpgogol/ontology build:check` — validate enum and archetype changes
- `pnpm --filter @warpgogol/ui build:check` — validate component builds
- `pnpm exec site-kernel run archetype.registry.build` — rebuild archetype index
- `pnpm exec site-kernel run manifest.contract.validate` — validate manifests
- `pnpm exec site-kernel run rfc.validate --id RFC-0743` — validate RFC

## 3. Step sequence

### Step 1. Extend ComponentRole enum and create component archetypes

**Goal:** Add the two new component roles to the closed enum and create their archetype YAMLs so the ontology package recognizes them.

**Agent actions:**

- Add `"currency-selector"` and `"price-display"` to `ComponentRoleValues` array in `packages/ontology/src/enums.ts`
- Create `packages/ontology/archetypes/components/currency-selector.yaml` with `id`, `displayName`, `version`, `semanticRole`, `description`, `expectedIntents` (`orient-visitor`, `guide-navigation`), `expectedIndustryFit: []`, `layoutHint: split`, `propsSchema`, `acceptedCosmicNames` (pick 2-3 non-reserved MoonCatalog names), `constraints: {}`
- Create `packages/ontology/archetypes/components/currency-aware-price-display.yaml` with `id`, `displayName`, `version`, `semanticRole`, `description`, `expectedIntents` (`pricing`, `trust`), `expectedIndustryFit: []`, `layoutHint: inline`, `propsSchema`, `acceptedCosmicNames` (pick 2-3 non-reserved MoonCatalog names), `constraints: {}`
- Add both archetypes to `packages/ontology/archetypes/index.yaml` under `components`
- Run `pnpm exec site-kernel run archetype.registry.build` to regenerate `archetypes/index.json`
- Run `pnpm --filter @warpgogol/ontology build:check` to validate

**Validation:**

- `pnpm --filter @warpgogol/ontology build:check` passes
- `archetype.registry.build` produces `index.json` with both new components registered

**Completion criterion:** Both component roles exist in `ComponentRoleValues`; both archetype YAMLs exist and are registered in `index.json`.

**Human review:** no

---

### Step 2. Create currency-selector component (Mirror Quintet)

**Goal:** Create the `currency-selector` component with all five Mirror Quintet files.

**Agent actions:**

- Pick a cosmic name from the archetype's `acceptedCosmicNames` (must be non-reserved in MoonCatalog)
- Create `packages/ui/src/components/currency-selector/currency-selector.astro` — renders a `<select>` with accessible label, options from `CurrencySelectorContent.currencies`
- Create `packages/ui/src/components/currency-selector/currency-selector.css` — styles using `--ds-*` tokens only
- Create `packages/ui/src/components/currency-selector/currency-selector-component.manifest.yaml` — all required fields: `id`, `uniName`, `layer: component`, `semanticId`, `archetype: currency-selector`, `cosmicName`, `role: currency-selector`, `version: 1.0.0`, `intent`, `industryFit: []`, `contentSchemaKey: currency-selector-component`, `contentTypesPath: ./currency-selector.types.ts`, `propsSchema` with `required: [label, currencies]`
- Create `packages/ui/src/components/currency-selector/currency-selector-component.client.ts` — `getSelectedCurrency()`, `setSelectedCurrency()`, `dispatchCurrencyChange()`, `CURRENCY_STORAGE_KEY`, `CURRENCY_CHANGE_EVENT`; also listens for `wg-currency-change` to sync across multiple instances (summit Q3)
- Create `packages/ui/src/components/currency-selector/currency-selector.types.ts` — `CurrencySelectorContent` interface
- Create `packages/ui/src/components/currency-selector/currency-selector.md` — content template with label and currency examples

**Validation:**

- `pnpm --filter @warpgogol/ui build:check` passes
- `pnpm exec site-kernel run manifest.contract.validate` passes for the new manifest

**Completion criterion:** All five Mirror Quintet files exist; manifest validates; component renders a `<select>` with currency options.

**Human review:** no

---

### Step 3. Create currency-aware-price-display component (Mirror Quintet)

**Goal:** Create the `currency-aware-price-display` component with pre-rendered variants and client-side swap.

**Agent actions:**

- Pick a cosmic name from the archetype's `acceptedCosmicNames` (must be non-reserved, different from Step 2)
- Create `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.astro` — pre-renders all currency variants with `data-currency` attributes and `hidden` on non-default; `aria-live="polite"` on container
- Create `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.css` — styles using `--ds-*` tokens only
- Create `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display-component.manifest.yaml` — all required fields: `id`, `uniName`, `layer: component`, `semanticId`, `archetype: currency-aware-price-display`, `cosmicName`, `role: price-display`, `version: 1.0.0`, `intent`, `industryFit: []`, `contentSchemaKey: currency-aware-price-display-component`, `contentTypesPath: ./currency-aware-price-display.types.ts`, `propsSchema` with `required: [priceVariants]`
- Create `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display-component.client.ts` — listens for `wg-currency-change`, toggles `hidden` on variants matching `data-currency` attribute
- Create `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.types.ts` — `CurrencyAwarePriceDisplayContent` interface
- Create `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.md` — content template

**Validation:**

- `pnpm --filter @warpgogol/ui build:check` passes
- `pnpm exec site-kernel run manifest.contract.validate` passes for the new manifest

**Completion criterion:** All five Mirror Quintet files exist; manifest validates; component pre-renders variants with `data-currency` attributes.

**Human review:** no

---

### Step 4. Register cosmic names in MOON_IMPORT_PATHS

**Goal:** Register both components in `MOON_IMPORT_PATHS` so `buildPage` can resolve them.

**Agent actions:**

- Add entries to `MOON_IMPORT_PATHS` in `packages/share/src/page.ts` mapping the chosen cosmic names to import paths:
  - `"<cosmicName-selector>": "@warpgogol/ui/components/currency-selector"`
  - `"<cosmicName-display>": "@warpgogol/ui/components/currency-aware-price-display"`
- Run `pnpm --filter @warpgogol/share build:check` to validate

**Validation:**

- `pnpm --filter @warpgogol/share build:check` passes
- `MOON_IMPORT_PATHS` contains both new entries

**Completion criterion:** Both cosmic names are registered in `MOON_IMPORT_PATHS` and resolve to correct import paths.

**Human review:** no

---

### Step 5. Integrate selector into header component

**Goal:** Render `CurrencySelector` in the site header next to `LangSwitcher`, gated by `multi-currency` entitlement.

**Agent actions:**

- Import `CurrencySelector` in `packages/ui/src/components/header/header-component.astro`
- Render `<CurrencySelector>` next to `<LangSwitcher>` in the header layout
- Gate rendering with the `multi-currency` entitlement check (same pattern as existing feature gates in header)
- Add CSS rules in `header-component.css` for the currency selector placement (next to lang-switcher, same visual group)
- Run `pnpm --filter @warpgogol/ui build:check` to validate

**Validation:**

- `pnpm --filter @warpgogol/ui build:check` passes
- Header component renders `CurrencySelector` when entitled, omits it when not

**Completion criterion:** Selector appears in header next to lang-switcher; entitlement gate works.

**Human review:** no

---

### Step 6. Write unit tests

**Goal:** Test both client scripts for correct behavior.

**Agent actions:**

- Create `packages/ui/src/components/currency-selector/currency-selector.test.ts` — test `getSelectedCurrency()` returns null when unset, `setSelectedCurrency()` writes to `localStorage`, `dispatchCurrencyChange()` fires `wg-currency-change` event with correct detail
- Create `packages/ui/src/components/currency-aware-price-display/currency-aware-price-display.test.ts` — test that `wg-currency-change` event toggles `hidden` on the correct `data-currency` variant, test fallback to source currency when no projection exists
- Mock `localStorage` and `window.dispatchEvent` in test setup
- Run `pnpm --filter @warpgogol/ui vitest run` to validate

**Validation:**

- `pnpm --filter @warpgogol/ui vitest run` passes
- All test cases pass

**Completion criterion:** Both test files exist and all tests pass.

**Human review:** no

---

### Step 7. Validate, review, fix, and stamp implemented

**Goal:** Run all validation checks, code review, fix findings, verify acceptance criteria, and stamp the RFC as implemented.

**Agent actions:**

- Run `pnpm exec site-kernel run rfc.validate --id RFC-0743`
- Run `pnpm --filter @warpgogol/ui build:check`
- Run `pnpm --filter @warpgogol/ontology build:check`
- Run `pnpm --filter @warpgogol/share build:check`
- Run `pnpm --filter @warpgogol/ui vitest run`
- Run `pnpm exec site-kernel run manifest.contract.validate`
- Check off acceptance criteria with inline `(evidence: ...)` annotations
- Run code review: invoke `fo-review` via the `skill` tool on all session code changes
- Run fix if needed: if `fo-review` reported findings, invoke `fo-fix` via the `skill` tool. Re-run `fo-review` to confirm. Max 3 iterations.
- Stamp: run `pnpm exec site-kernel run rfc.implement.stamp --id RFC-0743 --implementation-commit <sha>`

**Validation:**

- `git status` — no uncommitted changes from the current session
- `pnpm exec site-kernel run rfc.validate --id RFC-0743` passes
- Review report exists in `docs/reviews/code/` for this session

**Completion criterion:** All validation passes; code review passed (findings fixed if any); all acceptance criteria checked off with inline `(evidence: ...)` annotations; RFC stamped as `implemented` via `rfc.implement.stamp`.

**Human review:** no — the `accepted → implemented` transition is automated via `rfc.implement.stamp` (RFC-0476).

## 4. Validation suite

### 4.1 Required checks

- `pnpm exec site-kernel run rfc.validate --id RFC-0743`
- `pnpm --filter @warpgogol/ontology build:check`
- `pnpm --filter @warpgogol/ui build:check`
- `pnpm --filter @warpgogol/share build:check`
- `pnpm --filter @warpgogol/ui vitest run`
- `pnpm exec site-kernel run manifest.contract.validate`

### 4.2 Evidence artifacts

- Commit messages referencing `RFC-0743` in the subject line (RFC-0265 commit hygiene)
- `docs/rfcs/verification/rfc-0743.generated.json` — verification evidence (if acceptance probes declared)

## 5. Risks and mitigation

| Risk (from RFC) | Mitigation (plan step) |
| --------------- | ---------------------- |
| Flash of source currency (FOUC) | Step 3: pre-render all variants with `hidden`; client script un-hides correct variant immediately on `DOMContentLoaded` |
| SEO impact | No mitigation needed — SEO sees default currency, Schema.org emits business-declared prices (RFC-0745) |
| Component maintenance | Steps 2-3: components are small and focused; client scripts are thin |
| DOM size with many variants (summit A3) | Step 3: pre-render only configured currencies from policy (typically 3-5, not unlimited) |
| Multiple selector instances out of sync (summit Q3) | Step 2: selector listens to `wg-currency-change` to sync its `<select>` value across instances |

## 6. Escalation triggers

- If implementation reveals an invariant conflict with DNA-17 (Mirror Quintet) that cannot be resolved by adding the missing files, run `pnpm exec site-kernel run rfc.supersede.propose --id RFC-0743 --reason "..." --invariant "DNA-17"` instead of working around it.
- If `ComponentRole` enum extension is rejected by `manifest.contract.validate` for a reason not anticipated, create a superseding RFC proposing a different role assignment mechanism.
