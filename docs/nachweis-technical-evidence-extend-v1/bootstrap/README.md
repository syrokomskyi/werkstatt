# Bootstrap screenshots

These screenshots were supplied by the operator on 2026-08-18.

They are retained only as visual/reference evidence for the migration mission.

They MUST NOT be ingested/published as the canonical technical-assessment observations because:

- the Lighthouse screenshot does not contain the full LHR metadata or the required five-run methodology;
- the Cloudflare screenshot is a UI view rather than the canonical URL Scanner API result used by the new adapter.

After implementation, rerun both providers through their canonical commands and publish the newly captured observations.

Files:

- `lighthouse-warpgogol-bootstrap.png`
- `cloudflare-agent-readiness-warpgogol-bootstrap.png`
