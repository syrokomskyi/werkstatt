---
reviewId: REVIEW-RFC-0684-01
date: 2026-08-04
reviewer:
  skill: fo-review
  model: claude-sonnet-4-20250514
diffRange: 414de101...HEAD
verdict: approved
---

# Code Review: RFC-0684 Session

## Scope

Diff `414de101...HEAD` — 3 files, 803 insertions:

- `docs/rfcs/rfc-0684-add-axiom-finding-suppression-layer-with-per-site-config.md` (RFC)
- `docs/audits/audit-rfc-0684-add-axiom-finding-suppression-layer-with-per-site-config.md` (audit)
- `docs/plans/plan-rfc-0684-add-axiom-finding-suppression-layer-with-per-site-config.md` (plan)

All changes are documentation artifacts — no source code modified.

## Mechanical floor

- `rfc.validate --id RFC-0684 --json` — **pass** (zero violations)
- No package build checks applicable (no code changes)

## Axis A — Structural correctness

**Pass** — documentation-only diff; TypeScript contracts in the RFC are illustrative, not compiled. The `Finding` interface uses a placeholder comment (`// ... existing fields ...`) but this is standard RFC notation for "augmenting an external type", not a compiled source file.

No findings.

## Axis B — DNA alignment

**Pass** — RFC-0684 claims `satisfies: [DNA-49, DNA-59]`.

- **DNA-49 (Fleet propagation):** The RFC correctly describes how suppressions integrate with `leitstand.propagate` and `leitstand.dev-deploy`. The re-apply logic (importing `applySuppressions` via subpath export) ensures pre-suppression evidence is handled without requiring re-runs. This is consistent with DNA-49's gate model.
- **DNA-59 (Evidence preservation):** Suppressed findings are marked, not removed. The append-only archive contract is preserved.

No findings.

## Axis C — Ecosystem fit

**Pass with 1 finding.**

1. **[minor] `commands.changed` lists `leitstand.dev-deploy` but the RFC body doesn't describe the change in the Design section.** The `commands.changed` array (line 57) correctly lists `leitstand.dev-deploy`, and the Rollout section (line 342) mentions passing `--channel dev`. However, the Design §CLI surface (lines 149–153) describes `leitstand.dev-deploy` only briefly. The plan (Step 6) covers this in detail. Not a blocker — the information is present, just distributed across sections.

No other findings. Package boundaries are correct: `suppressions-config.ts` and `suppressions-validate.ts` in `@warpgogol/site-kernel-checks`, subpath export for cross-package import from `@warpgogol/site-kernel-handoff`.

## Axis D — Forward-only compliance

**Pass** — No backward compatibility shims or dual-paths. The `--channel` default of `main` is the correct production value, not a legacy bridge. Suppressions are additive. The re-apply logic in `leitstand.propagate` handles old evidence forward-only (marks suppressed findings, doesn't rewrite old files).

No findings.

## Axis E — Agent-facing policy

**Pass** — Implementation notes are clear and correctly reference RFC-0224, RFC-0334, RFC-0330, RFC-CMD-02. The RFC explicitly prohibits broad patterns and requires `reason` fields. The `applySuppressions` purity requirement is stated. No self-authorizing language.

No findings.

## Axis F — Pragmatism

**Pass with 1 finding.**

1. **[minor] `descriptionPattern: "preload"` in default rules is still a broad pattern.** The audit (Axis F item 1) flagged this, and the enhanced RFC retains the pattern. The word "preload" appears in many CSS-related contexts. However, the RFC's Risks section acknowledges this and `suppressions.validate` warns on broad patterns. The pattern is specific enough when combined with `ruleId: performance-vitals.render-blocking` (AND logic). Acceptable for a default rule that operators can override per-site.

No other findings. The audit's 4 questions were all resolved during enhancement: `category` field added, `ruleId` validation source specified (from `study-run.json` evidence), rendering contract defined (separate collapsible section), per-site override semantics clarified.

## Axis G — Blind spots

**Pass** — The audit identified 4 blind spots; all were addressed in the enhanced RFC:

1. Pre-suppression evidence migration — resolved by re-apply logic in `leitstand.propagate`
2. `suppressionSummary.byCategory` — resolved by adding `category` field to `SuppressionRule`
3. Per-site override semantics — clarified (ADD new suppressions, cannot REMOVE workshop suppressions)
4. `axiom.report` rendering — specified (separate collapsible section, greyed out, summary count in header)

No findings.

## Summary

| Axis              | Verdict | Findings |
| ----------------- | ------- | -------- |
| A — Structural    | pass    | 0        |
| B — DNA alignment | pass    | 0        |
| C — Ecosystem fit | pass    | 1 minor  |
| D — Forward-only  | pass    | 0        |
| E — Agent-facing  | pass    | 0        |
| F — Pragmatism    | pass    | 1 minor  |
| G — Blind spots   | pass    | 0        |

**Total findings: 2 (both minor, neither blocking)**

The RFC, audit, and plan are structurally complete, DNA-aligned, and ready for implementation. The 2 minor findings are informational and do not block the implementation pipeline.
